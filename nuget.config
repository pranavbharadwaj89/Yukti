<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Empty by default in this sandbox (no nuget.org network access here).
       Remove this override once building with normal internet access —
       see README.md "Building this project" for details. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
