#!/bin/sh
# Keeps docs/TRACKER.html in sync with repo state on every commit.
# Install: copy this file to .git/hooks/pre-commit (chmod +x on macOS/Linux).
node tools/tracker/generate-tracker.mjs
git add docs/TRACKER.html
