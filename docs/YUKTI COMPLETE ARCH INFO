# Volume 0 — 

Product Blueprint
## Part I — Vision

---

## 1. Executive Summary

Enterprise quality engineering and operations teams today assemble their automation capability from five to eight disconnected tools: Postman or similar for API testing, Selenium or Playwright for web UI, Appium for mobile, an RPA product for desktop automation, a log/observability stack (ELK, Datadog, Splunk), and increasingly a scattering of point AI tools bolted onto one or two of the above. Each tool has its own configuration language, its own reporting format, its own CI integration story, its own credential store, and its own learning curve. A single end-to-end business scenario — "a customer places an order on the website, the mobile app reflects it, the payment API confirms it, and no error appears in the logs" — cannot be expressed as *one* automated check anywhere in this stack. It has to be stitched together by a human, across tools, by hand, every time.

Nexus is proposed as a unified automation platform that treats API, Web, Mobile, Desktop UI, Log/Observability, and AI-driven automation as six capability modules sitting behind **one orchestration core, one flow language, and one reporting model**. A single flow can open a browser, submit a form, call the resulting order-confirmation API, tail the application log for errors in the same time window, and — where a step doesn't yet exist — ask the AI module to generate it from a plain-English description. The engineering value is not "one more automation tool"; it is the elimination of the integration tax that currently sits between five or six best-of-breed tools that were never designed to talk to each other.

This document (Volume 0) is the product blueprint: the vision, the market and user reasoning behind it, the platform's modular shape, and the architectural principles that Volumes 1 through 5 will each expand into an implementation-grade specification. Volume 0 answers "what are we building and why." The volumes that follow answer "how, precisely."

The remainder of Part I sets out the vision and mission that every subsequent architectural decision in this document set should be traceable back to, the philosophy that should break ties when two valid engineering approaches compete, the business objectives that justify the investment, and the metrics by which we will know, concretely, whether the product is succeeding.

---

## 2. Vision Statement

> **Nexus exists so that any automatable behavior of a software system — however it is exposed, whatever layer it lives at — can be described once, in one language, and executed, monitored, and maintained from one platform.**

Unpacked, this vision makes three specific claims, each of which constrains everything that follows in this document set:

**"Any automatable behavior."** The platform is not a web-testing tool with API support bolted on, nor an API tool with a UI plugin. All six capability domains — API, Web, Mobile, Desktop UI, Logs, AI — are first-class citizens of the architecture from the ground up. No module should be structurally privileged over another; the same orchestrator, the same flow schema, the same reporting pipeline serves all of them identically. This is the single hardest constraint in the entire document set, because it is far easier to build a great web-automation tool with bolted-on extras than a genuinely uniform platform — and it is the difference that justifies Nexus's existence over the incumbents.

**"Described once, in one language."** A flow author should never need to context-switch languages or mental models mid-scenario. Whether a step drives a browser or asserts on a log pattern, it is expressed in the same declarative flow syntax, with the same variable-passing and conditional semantics. This is what makes cross-domain scenarios (the order-placement example above) expressible at all.

**"Executed, monitored, and maintained from one platform."** Execution is necessary but not sufficient. A platform is only genuinely unifying if the *maintenance* burden — updating a broken selector, adjusting an assertion, understanding why last night's run failed — is also unified. This is where the AI layer earns a permanent, architectural role rather than a marketing footnote: self-healing selectors, failure root-cause summarization, and flow generation from English are maintenance-cost reducers first, novelty features second.

---

## 3. Mission

While the vision describes the end state, the mission describes what the team building Nexus commits to doing, continuously, to get there and stay there:

- **Build the orchestration core before any single module is "finished."** The temptation on any team building a multi-module product is to make one module excellent and defer the others. Nexus's mission is the opposite discipline: the orchestrator, the flow schema, and the module interface contract are treated as the product, and each capability module is treated as a (replaceable, extensible) implementation of that contract. This ordering is why Volume 1 (Backend Architecture) leads with the execution engine and plugin architecture before it ever discusses a specific module.

- **Never let AI become a required dependency for core functionality.** Every AI-assisted capability (flow generation, self-healing, triage) must have a manual, deterministic fallback path. A team without API-key budget, or in a regulated environment where AI-assisted test generation is not permitted, must still have a fully functional automation platform. AI is a force-multiplier layered on top of a complete deterministic core — never the core itself.

- **Treat the CLI and the API as the primary interface, and any GUI as a client of that API.** This ordering (elaborated in Volume 2) is what keeps Nexus embeddable in CI/CD pipelines, scriptable, and free of the common enterprise-tool failure mode where automation can only be triggered by clicking a button in someone's browser.

- **Keep every module's contract small enough to be re-implemented by a third party in an afternoon.** The plugin architecture (Volume 1, Part on Domain-Driven Design & Plugin Architecture) is only real if it is genuinely easy to add a seventh module — a database automation module, a message-queue automation module — without touching the core. The mission is to keep proving this by dogfooding it internally.

---

## 4. Product Philosophy

Four philosophical commitments recur throughout every volume in this set. Where a design decision in Volumes 1–5 seems to need justification, it should trace back to one of these four:

### 4.1 Composability over completeness
A platform that tries to anticipate every possible automation need up front will always be one enterprise-specific requirement behind its users. Nexus instead optimizes for making six or seven *small, composable primitives* (an HTTP request step, a log-rule step, a click step) that can be assembled into arbitrarily complex flows, rather than trying to ship a "checkout automation" feature or a "login automation" feature as a monolithic capability. Composability is also what makes the plugin architecture viable — a new module only needs to expose primitives, not anticipate every use of them.

### 4.2 Flows are short, vertical slices — not brittle end-to-end scripts
The industry's default automation pattern is the long end-to-end script: forty steps, one failure halfway through invalidates the whole run, and a maintainer has to re-run the entire thing to find out if step 41 would have passed. Nexus's philosophy is fail-fast, short flows (this is a concrete architectural decision, not just a suggestion — see Volume 1's orchestrator design) chained together at the CI-pipeline level rather than the flow level. A ten-step flow that fails at step 3 tells you exactly what broke in under a second. A 40-step flow that fails at step 3 wastes 37 steps of execution time finding that out.

### 4.3 The maintenance cost of automation is the real cost center
Writing an automated check is cheap. Keeping five hundred automated checks passing as the underlying application changes weekly is where automation programs actually die — this is the industry's most consistent failure pattern (see Market Analysis, Section 8). Every architectural decision that trades a small amount of authoring convenience for a large reduction in maintenance burden (self-healing selectors, declarative assertions instead of imperative scripts, chained variables instead of copy-pasted values) is preferred by default.

### 4.4 Transparent over magical
Where the AI layer makes a decision — generating a flow, healing a selector, triaging a log — the platform always shows its reasoning and its confidence, and always allows a human to accept, reject, or edit the suggestion before it is persisted. Nexus's AI philosophy (elaborated fully in Volume 3) is "AI proposes, deterministic code disposes" — the AI module never silently mutates a flow definition on its own authority.

---

## 5. Business Objectives

The vision and mission above are the "why" for engineering. This section is the "why" for the business sponsoring the engineering effort. Five objectives, each with the timeframe and rationale that Volume 0 Part III (Roadmap) will decompose into milestones:

| # | Objective | Rationale |
|---|---|---|
| 1 | **Reduce the number of distinct automation tools a QA/DevOps org must license, learn, and integrate from 5–8 down to 1** | This is the core value proposition and the primary driver of both cost savings and adoption speed for a new customer. |
| 2 | **Cut the average time-to-first-passing-flow for a new team from weeks to under a day** | Tool-sprawl's biggest hidden cost is onboarding time. A unified flow language with AI-assisted generation directly targets this. |
| 3 | **Reduce automation maintenance effort (hours/week spent fixing broken checks) by a target of 40%+ within two release cycles of adoption** | Directly addresses the industry's most common automation-program failure mode (checks rot faster than teams can fix them). Self-healing and better failure diagnostics are the primary levers. |
| 4 | **Establish a plugin marketplace as a second revenue and retention channel within 18 months of GA** | Long-term defensibility does not come from the six built-in modules alone — it comes from a thriving ecosystem that makes switching costs high in the customer's favor, not just the vendor's. |
| 5 | **Achieve enterprise-grade trust (SOC2-track security posture, on-prem/self-hosted deployment option, no mandatory AI dependency) from the first GA release, not retrofitted later** | Enterprise buyers evaluating a *replacement* for existing critical infrastructure will not tolerate "security is on the roadmap." This must be true at GA, which is why Volume 4 (Infrastructure) and Volume 5 (Engineering Playbook) are peers of the product architecture volumes, not afterthoughts. |

---

## 6. Success Metrics

Business objectives without measurable success metrics are aspirations. This section defines the concrete, trackable metrics for each objective above, plus platform-health metrics that apply regardless of business objective.

### 6.1 Adoption & consolidation metrics
- **Tool consolidation ratio**: average number of legacy automation tools a customer decommissions within 6 months of Nexus adoption. Target: ≥3.
- **Time-to-first-passing-flow**: median wall-clock time from account creation to first green flow run in the customer's own environment. Target: < 4 hours for API-only flows, < 1 business day for flows spanning 3+ modules.
- **Cross-module flow adoption rate**: percentage of active customer flows that use 2 or more modules in a single flow (the number that validates the "unification" thesis specifically, as opposed to Nexus simply being used as a single-purpose tool by habit). Target: ≥25% within the first year of a customer's usage.

### 6.2 Maintenance-burden metrics
- **Selector/locator self-heal acceptance rate**: percentage of AI-proposed selector fixes that a human accepts without further edits. Target: ≥70% (below this, the feature creates more review burden than it saves).
- **Mean time to diagnose a failed run**: from failure notification to a human understanding root cause, with vs. without AI-assisted triage. Target: 50%+ reduction versus the customer's prior toolchain, self-reported via onboarding survey baseline.
- **Flake rate**: percentage of flow re-runs (identical inputs, identical target environment) that produce a different pass/fail result. This is a platform-quality metric, not just a customer metric — a high flake rate anywhere is a defect in Nexus's own execution engine, not "normal for automation." Target: < 2% across all modules at GA, trending toward < 0.5%.

### 6.3 Platform health metrics
- **Module parity index**: for a fixed checklist of orchestrator-level capabilities (retries, conditionals, variable interpolation, reporting, AI-flow-generation), the percentage of those capabilities that work identically across all six modules. Target: 100% — any module-specific gap is treated as a P1 defect against the "any automatable behavior" vision claim, not a backlog item.
- **Plugin development time**: wall-clock time for an experienced engineer unfamiliar with the specific new capability domain to build a minimally viable seventh module against the published `AutomationModule` interface. Target: < 1 day, validated quarterly by actually doing it (dogfooding the plugin architecture, per the Mission section above).
- **AI-optionality compliance**: percentage of platform capabilities (excluding the AI module's own features) that remain fully functional with zero AI/LLM API calls configured. Target: 100%, tested as a release gate (see Volume 5, CI/CD Quality Gates).

These metrics are the ones every subsequent architectural and product decision in this document set will be checked against. Where Volume 1 proposes a specific execution-engine design, or Volume 3 proposes a specific AI-guardrail approach, the test is: does this measurably move one of the numbers above, or does it just feel like good engineering in the abstract.

---

*End of Part I. Part II — Product (Problem Statement through Non-functional Requirements) continues the blueprint by grounding this vision in specific market evidence, personas, and requirements.*

_____________________________________________________________________________________________________________________________________________________________

7. Problem Statement

7.1 The core problem

Enterprise software delivery organizations need to verify that systems behave correctly across every layer those systems expose — API, web UI, mobile app, desktop client, and the operational logs that record what actually happened at runtime. No single commercial or open-source tool today does this. Instead, organizations assemble a portfolio of single-purpose automation tools, and the cost of assembling, integrating, and maintaining that portfolio has grown to rival or exceed the cost of the automation work itself.

This is not a hypothetical inefficiency. It is the single most consistent complaint surfaced in QA leadership surveys, DevOps maturity assessments, and engineering-productivity retrospectives across the industry (see Section 8, Market Analysis, for sourced figures). The problem decomposes into five concrete, independently-verifiable sub-problems.

7.2 Sub-problem 1 — Tool fragmentation prevents cross-layer scenarios

A realistic business scenario almost never lives entirely inside one layer. "A user completes checkout" touches the web UI (or mobile app), the order and payment APIs, and should leave a clean trail in the application logs with no unexpected errors. Today, verifying this scenario end-to-end requires:

A Selenium/Playwright/Cypress script for the UI portion
A separate Postman/RestAssured/custom-HTTP-client script for the API portion
A separate log-query (Splunk search, CloudWatch Insights query, grep-over-SSH) for the observability portion
A human, or a fragile shell script, gluing the three together and reconciling three different pass/fail formats into one verdict

Each of the three tools was designed in isolation, has no awareness of the others' state, and cannot pass data between them (e.g., the order ID generated by the UI step, needed by both the API check and the log query) without custom glue code that itself becomes a maintenance liability. The result: cross-layer scenarios are disproportionately rare in most automation suites relative to how common they are as actual user journeys — not because they're unimportant, but because they're disproportionately expensive to build and maintain given the tooling available.

7.3 Sub-problem 2 — Maintenance cost grows faster than the team's capacity to absorb it

Automated checks are not fire-and-forget. Every UI change can break a selector. Every API contract change can break an assertion. Every log format change can break a pattern rule. Industry data consistently shows that the ongoing cost of maintaining an existing automation suite exceeds the cost of writing it within the first one to two release cycles, and that this ratio only worsens as the suite grows (Section 8 cites specific figures). Because today's tools are fragmented, a UI change that also changes an API contract requires two separate maintenance actions in two separate tools, often performed by two different people with two different skill sets, discovered by two different failure notifications at two different times. Nothing correlates the two failures as "the same underlying change" for the maintainer.

7.4 Sub-problem 3 — No unified signal for "is the system healthy"

Because the tools don't share a reporting model, there is no single dashboard, single report, or single CI gate that says "here is the current health of the system across every layer we test." Organizations approximate this with manually-assembled status pages or weekly meetings that synthesize five separate tools' outputs by hand. This is slow, error-prone, and — critically — not available at the speed of a CI pipeline, where a single unified pass/fail signal is what actually gates a deployment.

7.5 Sub-problem 4 — AI assistance is arriving piecemeal, not architecturally

Self-healing selectors, AI-generated test cases, and AI-assisted log triage are all real, valuable, increasingly available capabilities — but each is being retrofitted into a single existing single-purpose tool (a web-testing tool gets AI selector healing; a log tool gets AI-assisted anomaly summarization). None of the incumbent tools were architected with a cross-domain AI layer in mind, so a customer who wants AI assistance for both UI maintenance and log triage today needs two different AI-enabled products from two different vendors, each with its own AI cost model, guardrails, and data-handling policy. There is a market opening for a platform where the AI layer is architected once, across all modules, from the start — which is precisely Nexus's Volume 3 mandate.

7.6 Sub-problem 5 — Cost and licensing sprawl compounds the technical problem

Each point tool in the fragmented stack carries its own commercial license, its own vendor relationship, its own renewal cycle, its own compliance/security review, and often its own infrastructure footprint (a device farm subscription here, a log-ingestion tier there). Beyond the direct license cost, this creates a hidden tax in procurement overhead, vendor-risk assessment, and integration engineering that scales with the number of tools, not with the amount of automation actually being performed. A platform that consolidates six capability domains under one commercial relationship directly removes this multiplier — this is the business case underpinning Business Objective 1 in Part I.

7.7 Why this problem is tractable now, and wasn't five years ago

Three shifts make solving this problem viable today in a way it was not a product cycle or two ago:

Modern browser and OS automation protocols have converged. Playwright's cross-browser protocol, Appium's W3C-standardized mobile automation, and mature accessibility-tree tooling for desktop mean that "one abstraction over many automation backends" is now an engineering-tractable problem rather than a research problem.
LLMs have crossed a real capability threshold for structured-output generation and code-adjacent reasoning. Turning "check that /health returns 200 and status: ok" into structurally valid flow JSON, or looking at a diff between a broken selector's context and a new DOM and proposing a fix, are both squarely within current-generation LLM capability — this was not reliably true even two to three years prior.
The economics of API-based LLM access make an AI layer viable as a platform feature rather than a moonshot R&D bet. Per-call cost and latency for the tasks in Section 7.5 above are now low enough to be a normal part of a CI pipeline's cost budget, not a special "AI experiment" line item — provided (per the Product Philosophy AI-optionality principle in Part I) that the platform never makes them mandatory.
7.8 Problem statement, summarized

Organizations cannot express, execute, or maintain a single business scenario that spans API, UI, mobile, desktop, and log layers without stitching together multiple disconnected tools by hand — and the resulting maintenance and integration burden grows faster than most teams' capacity to absorb it. No current platform treats these layers as peers under one orchestration model with a unified, architected AI layer.

Every subsequent section of this blueprint — market analysis, personas, use cases, and functional requirements — exists to test and substantiate this statement, and every module in Volumes 1–5 exists to solve one clause of it.



8. Market Analysis

8.1 Purpose and method of this section

This section frames the market opportunity in terms of the buying categories Nexus intersects, the structural trends favoring a unified platform, and the assumptions this blueprint is making about market size and willingness to pay. Where this section cites a figure, it is marked either as an industry-observed pattern (a widely-corroborated directional trend, not a single sourced statistic) or an internal planning assumption (a number this team is using for roadmap and pricing planning that must be validated against real discovery calls and analyst data before being used in any external-facing materials). This distinction matters: a product blueprint that quietly launders assumptions into false-precision statistics produces bad downstream decisions. Volume 0 should not do that.

8.2 The buying categories Nexus intersects

Nexus does not create a new budget line; it competes for, and aims to consolidate, spend that already exists across several established categories:

Test automation / QA tooling (web and API test automation, historically the largest of these categories)
Mobile test automation (frequently a separate budget and team from web QA, even within the same organization)
RPA (robotic process automation) for desktop/legacy UI automation, typically bought by an operations or business-process team rather than engineering
Observability / log management, typically bought by a platform or SRE team, rarely by QA at all
Emerging "AI-native testing" tools, a fast-growing sub-category layered on top of one of the above rather than standing alone

The core market thesis is that these five categories, while sold and budgeted separately today, are converging in practitioner intent even where they have not yet converged in procurement structure. The people who own web QA, mobile QA, and increasingly observability, are frequently the same engineering organization, often the same individuals, forced to operate five separate tools because no vendor has offered the consolidated alternative.

8.3 Industry-observed patterns supporting the consolidation thesis
Tool sprawl is a named, recurring pain point in QA and platform-engineering community discussion (conference talks, practitioner surveys, vendor-neutral engineering blogs) independent of any single vendor's marketing. "We have too many testing tools that don't talk to each other" recurs across organizations of very different sizes and industries, which suggests a structural problem rather than a company-specific process failure.
AI-assisted test maintenance features are being added to nearly every major point tool (self-healing selectors in web-testing tools; AI-assisted assertion generation in API tools) within a similar time window across competitors — a pattern consistent with genuine, broadly-felt maintenance-cost pain rather than a single vendor's differentiation strategy.
Observability and testing budgets are increasingly co-owned as organizations adopt SRE and platform-engineering models, where the same team is accountable for both "does it work" (testing) and "is it working right now" (observability) — a structural shift that supports Nexus's thesis that logs/observability belongs as a peer module, not a bolt-on, to test automation.
8.4 Internal planning assumptions (require validation)

The following are stated explicitly as assumptions, not researched facts, because Volume 0 is being written before a formal market-sizing exercise. They should be replaced with sourced analyst figures (Gartner, Forrester, or a commissioned study) before appearing in any investor or customer-facing document:

Assumption	Current planning value	Validation needed before external use
Mid-to-large engineering orgs (200+ engineers) run 4+ distinct automation tools concurrently	Working assumption for persona and roadmap planning	Primary research (discovery interviews) with 15–20 target accounts
Average enterprise automation tooling spend across the fragmented categories in 8.2	Used only as a directional pricing anchor internally	Third-party market sizing report
Time engineers spend on cross-tool "glue" work (scripts stitching one tool's output into another's input)	Assumed material but currently unquantified	Time-tracking study or survey during design-partner phase

Volume 0's roadmap and pricing sections (Part III) should be read with this caveat attached: they represent a defensible starting hypothesis, not validated market sizing.

8.5 Market timing

Section 7.7 (Problem Statement) already covered the technical timing case (protocol convergence, LLM capability threshold, AI cost economics). The market timing case rests on two additional, independently-observable shifts:

Platform engineering and internal developer platform (IDP) initiatives are actively looking for consolidation opportunities. Where five years ago each team optimized its own tool choice independently, platform engineering teams are now explicitly mandated to reduce the number of tools an organization operates, for exactly the cost and integration reasons in Section 8.3. This creates a receptive buyer that did not structurally exist at the same scale previously.
AI-native competitors are entering from the "green field" direction (new startups building AI-first testing tools with no legacy single-domain product to defend), which validates the market's appetite for AI-assisted automation but, per Section 7.5, are generally not solving the cross-domain unification problem — they are AI-first but still domain-narrow. This leaves the specific intersection Nexus targets (unified orchestration and architected AI layer) contested but not yet won by an incumbent.
8.6 Market risks

A market analysis that only lists tailwinds is not a serious one. Three risks are tracked explicitly here and revisited in Section 22 (Risks) with mitigation owners:

Point-tool incumbents may bolt on cross-domain capability faster than Nexus can build a moat. A well-funded web-testing incumbent adding a "logs" tab and an "API" tab is a plausible fast-follow response, even if their underlying architecture (Section 7.5) makes doing this well structurally harder for them.
Buyer inertia and switching cost. Organizations with years of investment in existing test suites (Selenium scripts, Postman collections) face real migration cost to move to Nexus's flow language, regardless of the long-run maintenance savings case. The roadmap (Part III) needs an explicit migration/import story, not just a green-field authoring story.
The "unified platform" pitch has a credibility tax. Buyers have been burned before by tools that claimed to "do everything" and did each thing shallowly. Nexus's go-to-market and product design both need to actively counter this — the Module Parity Index success metric (Part I, Section 6.3) exists specifically so the product can credibly demonstrate, not just claim, that no module is a second-class citizen.



9. Competitor Analysis


9.1 Competitive landscape framing

Nexus does not have a single direct competitor because, per the Problem Statement (Section 7), no existing product occupies the exact intersection it targets: unified orchestration across API/Web/Mobile/Desktop-UI/Logs with an architected, cross-domain AI layer. Instead, Nexus competes against four distinct competitor archetypes, each of which is a strong incumbent in a narrower slice of the same problem space. This section analyzes each archetype rather than naming specific commercial products, since specific competitive claims about named products should be sourced and legally reviewed before publication — the structural analysis below is what should drive architecture and roadmap decisions regardless of which specific named vendors a sales team later benchmarks against.

9.2 Archetype A — Single-domain web/API test automation tools

Profile: Mature, well-adopted tools focused primarily on web UI automation, often with API testing bolted on as a secondary feature. Typically the incumbent in a QA team's toolchain, with years of accumulated test scripts as a switching-cost moat.

Strengths: Deep, polished capability within their primary domain (web UI). Large community, extensive documentation, mature CI/CD integrations. Often free or low-cost open-source cores with paid enterprise tiers.

Weaknesses relative to Nexus's thesis: Mobile, desktop, and log automation are either absent or clearly secondary/bolted-on, with different configuration paradigms than the core web capability. AI features, where present, are typically scoped to the web-automation domain only (e.g., visual-diff-based self-healing) and don't extend to logs or cross-domain flows. Orchestration across domains is not a first-class concept — these tools assume the test is the unit of work, not a flow that might span domains.

Competitive response to watch for: The most credible threat from this archetype is a well-funded incumbent adding API and log "modules" as product-line extensions. Nexus's defensibility against this specific move rests on the Module Parity Index (Part I, Section 6.3) — a bolted-on module that doesn't share the incumbent's original execution engine will structurally struggle to reach parity quickly, but this needs to be continuously verified, not assumed.

9.3 Archetype B — Mobile-specific test automation tools

Profile: Tools and frameworks built specifically around Appium or platform-native automation (XCUITest, UiAutomator2), often used by a mobile QA team that is organizationally separate from web QA.

Strengths: Deep platform-specific capability (gesture handling, device-farm integration, app-store-adjacent workflows) that a generalist platform will always need to work hard to match.

Weaknesses relative to Nexus's thesis: Almost never integrate meaningfully with web or API testing, reinforcing the organizational silo between mobile and web QA teams rather than bridging it. Log/observability integration is essentially absent from this archetype's product surface.

Competitive response to watch for: Mobile-specific tools are less likely to pursue horizontal expansion into web/API/logs than Archetype A, because their core technical investment (deep Appium/native automation expertise) doesn't directly transfer. This makes Archetype B a lower-urgency competitive threat to the unification thesis specifically, though still a strong incumbent to displace for the mobile module's feature depth.

9.4 Archetype C — RPA (robotic process automation) platforms

Profile: Enterprise RPA tools, typically bought by operations/business-process teams rather than engineering, focused on desktop and legacy-application automation, often via image-matching or OS-level automation similar in spirit to Nexus's UI module.

Strengths: Strong presence in regulated, legacy-heavy enterprises (financial services, insurance, healthcare back-office) where desktop automation is the dominant need. Often includes workflow/orchestration capability that is more mature than a typical QA tool's, since "orchestrating a business process" is RPA's core value proposition already.

Weaknesses relative to Nexus's thesis: Positioned and sold as a business-process-automation tool, not a software-testing/quality tool — different buyer, different budget, different success metrics (cost/labor savings vs. defect prevention). API and modern web automation capability is typically present but shallow compared to purpose-built testing tools. AI features tend to focus on process mining and document understanding rather than test generation or selector healing.

Competitive response to watch for: RPA vendors moving "down" into QA-flavored automation is plausible given their existing orchestration strength, but crosses a buyer-category boundary (ops vs. engineering) that is a genuine go-to-market obstacle for them, not just a technical one. Nexus's UI module (Volume 1) should study RPA orchestration patterns for the desktop-automation use case specifically, since this archetype has the most mature engineering in exactly that one area.

9.5 Archetype D — AI-native, narrow-domain testing startups

Profile: Newer entrants building AI-first products, usually focused tightly on one domain (most commonly web UI testing, sometimes API), that lead with natural-language test authoring and self-healing as the primary pitch rather than a secondary feature.

Strengths: No legacy architecture to defend, so AI capability is genuinely built into the core execution model rather than retrofitted (validating Nexus's own Volume 3 approach). Fast iteration speed, strong developer-experience focus, often better default authoring ergonomics than the older incumbents in Archetype A.

Weaknesses relative to Nexus's thesis: Narrow domain scope, same as Archetype A's core weakness — an AI-native web-testing tool is still just a web-testing tool with better AI, not a cross-domain orchestration platform. Being early-stage, these products often lack the enterprise trust posture (Section 6.4 in Part I: security, on-prem deployment, SOC2-track compliance) that a large-enterprise buyer requires at initial evaluation, which is a real Nexus differentiation opportunity if Business Objective 5 (Part I) is executed on schedule.

Competitive response to watch for: This is the archetype most likely to expand toward Nexus's exact target position (unified + AI-native) fastest, because their architecture already assumes AI as core rather than bolted-on — the main gap they need to close is domain breadth, which is a scope problem, not an architecture problem. This is the single most important competitor category to track post-launch.

9.6 Positioning summary
Archetype	Nexus's structural advantage	Nexus's structural gap to close
A — Web/API incumbents	True cross-domain orchestration from day one, not bolted on	Years of accumulated web-automation feature depth and ecosystem
B — Mobile specialists	Mobile is a peer module in one platform, not a silo	Deep native-mobile gesture/device-farm expertise
C — RPA platforms	Purpose-built for software QA/engineering buyers and workflows	Mature business-process orchestration patterns for pure desktop use cases
D — AI-native narrow tools	Cross-domain breadth + enterprise trust posture at GA	Their AI-first execution model may already rival Volume 3's design; must not assume an AI-capability lead by default

The clearest takeaway for the roadmap (Part III): Nexus's differentiated position is real but not permanent, and the archetype most capable of closing the gap fastest (D) is exactly the one whose architecture philosophy is closest to Nexus's own. Speed of execution on breadth (adding modules 2 through 6 credibly, not just module 1) matters as much as the AI capability itself.



10. User Personas
10.1 Purpose of this section

Five personas are defined below, each representing a distinct role that interacts with Nexus differently, has different success criteria, and — critically for Volume 2 (Frontend Architecture) — needs a different primary interface into the platform. Every functional requirement in Section 12 should be traceable to at least one persona's need; a requirement that serves no persona here should be questioned before being built.

10.2 Persona 1 — Priya, QA Automation Engineer

Role context: Mid-level to senior engineer on a dedicated QA/SDET team at a mid-to-large enterprise. Owns the automated regression suite for one or more product areas. Comfortable writing code (typically C#, JavaScript, or Python) but is not a backend platform engineer.

Current pain: Maintains separate Playwright and Postman-equivalent suites today. Spends what feels like (to her) a disproportionate fraction of every sprint fixing broken selectors and assertions rather than adding new coverage. Has no good way to express "this UI flow's result should also be visible in the API and show no errors in the log" without hand-rolling a script that calls three different tools' CLIs.

What she needs from Nexus: A flow language expressive enough to replace her existing suite without a capability regression. Confidence that self-healing selectors won't silently mask a real regression (transparency, per Product Philosophy 4.4). A CI integration story that's at least as good as what she has today (JUnit-style output, parallel execution, flaky-test quarantine).

Primary interface: The flow-authoring UI (Volume 2) day to day; the CLI for CI integration and local debugging. Rarely touches raw JSON/YAML directly except when reviewing AI-generated flows.

Success looks like: Fewer hours per sprint on maintenance, not necessarily more raw test count. She would consider Nexus successful even with the same number of automated checks if the time she spends keeping them green drops materially.

10.3 Persona 2 — Marcus, DevOps / Platform Engineer

Role context: Owns CI/CD pipeline infrastructure and, increasingly, observability tooling for the whole engineering organization. Not primarily a test author — his relationship to automation is "does the pipeline gate correctly and fast," not "did we write a good test."

Current pain: Each QA/mobile/RPA tool the various product teams use needs its own CI runner configuration, its own secrets management, its own report aggregation into whatever dashboard leadership looks at. He is the one who fields the "why did the pipeline say green when the app was actually broken" escalation, usually because two tools' reports disagreed and nobody reconciled them.

What he needs from Nexus: One CI plugin, one report format, one secrets/credentials model to administer instead of five. Fine-grained control over what blocks a deploy vs. what's advisory. Observability into the platform itself (is Nexus's own execution slow/flaky, independent of the systems under test) — this is why Section 6.2's flake-rate metric in Part I is a platform metric, not just a customer-facing one.

Primary interface: The CLI and the REST API almost exclusively; the GUI only occasionally, for setup or investigating an unusual pipeline failure. He is the primary reason Volume 1's "API and CLI as primary interface, GUI as a client of that API" mission commitment exists.

Success looks like: Fewer distinct CI integrations to maintain, and a pipeline gate he trusts enough to not manually double-check.

10.4 Persona 3 — Sofia, Engineering Manager / QA Lead

Role context: Manages Priya's team (and possibly a separate mobile QA team). Reports automation coverage and quality trends upward to a VP of Engineering. Rarely writes automation herself anymore but reviews reports, sets priorities, and owns the tooling budget decision.

Current pain: Cannot get a single, trustworthy answer to "what's our automated coverage and how healthy is it" without manually synthesizing multiple tools' dashboards ahead of every leadership review. Struggles to make the business case for automation investment because the ROI story (time saved, defects caught) is scattered across disconnected reporting.

What she needs from Nexus: A single, credible cross-module reporting view (this is the direct product requirement behind the "unified signal for system health" sub-problem in Section 7.4). Trend data over time, not just the latest run's pass/fail. Enough confidence in the platform's security and reliability posture to defend the purchasing decision to her own leadership and to procurement/security review.

Primary interface: The reporting/dashboard views in the GUI almost exclusively. She is the persona most likely to never touch the flow-authoring surface or the CLI at all.

Success looks like: A leadership review she can walk into without a week of manual report-assembly beforehand, and a defensible year-over-year story of reduced maintenance burden and/or increased coverage.

10.5 Persona 4 — Raj, Backend/Platform Engineer building a custom module

Role context: Not a QA person at all — a platform engineer at an organization with an unusual automation need (e.g., automating a proprietary message-queue system, or a domain-specific hardware-in-the-loop test rig) that none of Nexus's six built-in modules cover.

Current pain: Every existing automation tool he's evaluated is a closed system; extending it to a novel domain means either abandoning the tool for that use case or hacking around its plugin model (where one exists at all) with unsupported workarounds.

What he needs from Nexus: A genuinely well-documented, genuinely small AutomationModule interface (Volume 1) that he can implement without needing deep familiarity with Nexus's internals. Confidence that his custom module gets the same orchestration, reporting, and flow-language treatment as the built-in six — this persona is the direct human referent for the Module Parity Index metric and the "plugin development time" success metric in Part I.

Primary interface: Source code and SDK documentation directly; the CLI to test his module locally; rarely the GUI except to verify his module's steps show up correctly in the flow-authoring surface.

Success looks like: Shipping a working custom module in less time than he initially budgeted, and it continuing to work without special-case maintenance after a Nexus core upgrade.

10.6 Persona 5 — Elena, Security / Compliance Reviewer

Role context: Not a hands-on user of Nexus at all in the day-to-day sense — she is the gatekeeper who must approve Nexus for use inside a regulated or security-conscious organization before Priya, Marcus, or Sofia are allowed to adopt it.

Current pain: Evaluating five separate point tools for security posture, each with its own compliance story, is a recurring, expensive process every time the organization adds or renews an automation tool. AI-enabled tools specifically raise new questions (does test data get sent to a third-party LLM provider, is that covered by existing data-processing agreements) that she has to evaluate fresh for each new AI feature that shows up in any of the point tools.

What she needs from Nexus: A clear, centralized security and data-handling story covering all six modules and the AI layer at once, rather than six separate stories. Explicit, auditable confirmation that AI features are optional and that the platform is fully functional with zero data leaving the organization's environment if AI is disabled — this is the direct human referent for the AI-optionality compliance metric in Part I, Section 6.3, and for Business Objective 5.

Primary interface: Documentation (security whitepaper, architecture diagrams, SOC2/compliance artifacts) and, for self-hosted deployments, the infrastructure configuration itself (Volume 4). She will very rarely, if ever, open the product's GUI.

Success looks like: A single review process that covers the whole platform, completed once, rather than repeated per module or per AI feature added over time.

10.7 Persona-to-interface summary
Persona	Primary interface	Volume most responsible for serving them
Priya (QA Engineer)	Flow-authoring GUI, CLI	Volume 2 (Frontend), Volume 1 (flow schema)
Marcus (DevOps/Platform)	CLI, REST API	Volume 1 (API design), Volume 4 (CI/CD integration)
Sofia (Eng Manager/QA Lead)	Reporting/dashboard GUI	Volume 2 (Frontend reporting views), Volume 1 (reporting pipeline)
Raj (Module builder)	SDK, source, docs	Volume 1 (plugin architecture), Volume 5 (contribution standards)
Elena (Security/Compliance)	Documentation, infra config	Volume 3 (AI guardrails), Volume 4 (infrastructure/security)

This table is the traceability anchor for the rest of the document set: any volume that cannot point to which persona(s) a given design decision serves should treat that as a signal to revisit the decision.



11. Use Cases
11.1 Purpose and format

Each use case below follows a consistent structure: primary actor (from Section 10), trigger, preconditions, main flow, and the specific cross-module or platform capability it exercises. Use cases are deliberately written to expose requirements, not to be exhaustive user stories — Section 12 (Functional Requirements) converts the capabilities named here into testable requirement statements.

11.2 UC-1 — Cross-domain regression flow (Priya)

Trigger: A nightly CI pipeline run.

Preconditions: A flow exists that was authored to validate the checkout journey end-to-end.

Main flow:

The flow navigates the web UI through product selection and checkout (Web module).
On order confirmation, the UI step saves the generated order ID as a flow variable.
A subsequent API step calls the order-status endpoint using the saved order ID and asserts the response reflects "confirmed" status (API module, variable chaining from Section 4.1's composability principle).
A subsequent Logs step tails the application log for the same time window and asserts no ERROR-level entries reference that order ID (Logs module).
The orchestrator produces one unified report showing all three steps' results under one flow run.

Capability exercised: Cross-module variable passing; unified reporting; fail-fast step sequencing (Product Philosophy 4.2).

11.3 UC-2 — AI-generated flow from a plain-English requirement (Priya)

Trigger: Priya receives a new acceptance criterion from a product manager: "the password reset endpoint should reject reused passwords from the last 5."

Preconditions: Priya has access to the AI module with a configured LLM provider.

Main flow:

Priya types the acceptance criterion into the flow-generation interface.
The AI module proposes a flow: an API call attempting a password reset with a previously-used password, asserting a 4xx response with a specific error code.
Priya reviews the proposed flow, edits the expected error code (the AI guessed a plausible but incorrect one), and accepts it.
The flow is saved and immediately runnable; the edit Priya made is preserved and does not get silently reverted on a future AI suggestion (Product Philosophy 4.4 — AI proposes, deterministic code disposes).

Capability exercised: AI flow generation; mandatory human review step before persistence; graceful handling of AI output that's structurally valid but semantically imperfect.

11.4 UC-3 — Self-healing selector after a UI change (Priya, unattended)

Trigger: A nightly flow run fails because a developer renamed a CSS class, breaking a selector that previously targeted the "Submit" button.

Preconditions: Self-healing is enabled for this flow (opt-in, per AI-optionality).

Main flow:

The Web module step fails with "selector not found."
Before marking the step failed and halting the flow, the AI module is invoked with the broken selector, the step's stated intent ("click submit"), and the current page's DOM.
The AI module proposes a replacement selector with a confidence score.
If confidence exceeds a configured threshold, the flow proceeds using the healed selector and flags the run report with "selector auto-healed, human review recommended" rather than silently succeeding with no trace.
If confidence is below threshold, the flow fails normally and the proposed (low-confidence) selector is attached to the failure report as a suggestion for the human who investigates.

Capability exercised: Self-healing with a confidence threshold; transparent flagging of AI intervention even on success (never silent); graceful degradation to normal failure reporting.

11.5 UC-4 — Unified pipeline gate (Marcus)

Trigger: A pull request triggers CI.

Preconditions: The repository's CI configuration invokes the Nexus CLI as a required check.

Main flow:

The Nexus CLI runs a defined set of flows spanning API, Web, and Logs modules for the affected service.
Results are emitted in a CI-native format (e.g., JUnit XML) that the existing pipeline tooling already knows how to parse and gate on — no unified Nexus-specific dashboard is required for this use case to succeed, since Marcus's need is pipeline-native, not human-dashboard-native.
On failure, the CLI output includes enough structured detail (which module, which step, which assertion) that a developer can diagnose without opening a separate GUI.
On flake (a step that fails, then passes on an automatic retry within policy), the run is marked accordingly rather than silently reported as a clean pass — this data feeds the platform-level flake-rate metric (Part I, Section 6.2).

Capability exercised: CI-native reporting formats; structured failure detail sufficient for CLI-only diagnosis; flake detection and honest reporting.

11.6 UC-5 — Cross-module executive report (Sofia)

Trigger: Sofia is preparing a monthly quality review for her VP.

Preconditions: At least one full month of flow-run history exists across API, Web, Mobile, and Logs modules.

Main flow:

Sofia opens the reporting dashboard and selects a date range.
The dashboard shows pass rate trends per module, flake rate trends, and a maintenance-burden proxy metric (count and rate of self-healed selectors, as a leading indicator of UI churn) — all without her needing to export and manually merge data from separate tools.
Sofia drills into a specific regression (a sustained pass-rate drop in the Mobile module starting on a specific date) and the dashboard correlates it with a specific flow and a specific underlying failure signature.
Sofia exports a summary suitable for a leadership deck directly from the dashboard.

Capability exercised: Cross-module trend reporting; drill-down from aggregate to root cause; export for external consumption.

11.7 UC-6 — Custom module development (Raj)

Trigger: Raj's organization needs to automate interactions with a proprietary message-queue system that has no existing Nexus module.

Preconditions: Raj has read the AutomationModule interface documentation (Volume 1) and has development access to a Nexus instance.

Main flow:

Raj implements a new module conforming to the AutomationModule interface, exposing actions like publishMessage and assertMessageReceived.
He registers the module with a local orchestrator instance and writes a test flow using it.
The flow-authoring GUI (Volume 2), without any custom frontend work from Raj, automatically surfaces his new module's actions as available flow steps, because the GUI is driven by the same module-contract metadata the orchestrator uses (this is a specific, testable architectural requirement, not just a nice-to-have — it recurs in Volume 1 and Volume 2 as a shared contract).
Raj's module's step results appear in the same unified report format as the six built-in modules, with no special-casing required.

Capability exercised: Plugin architecture end-to-end, including automatic GUI surfacing and uniform reporting — the concrete proof-point for the Module Parity Index metric.

11.8 UC-7 — Security review and AI-disabled operation (Elena)

Trigger: Elena is reviewing Nexus for approval ahead of a regulated-industry customer's adoption.

Preconditions: A self-hosted Nexus instance is available for review, with AI features disabled at the configuration level.

Main flow:

Elena reviews the architecture documentation (Volume 4) confirming no outbound network calls occur to any LLM provider when the AI module is disabled.
She runs a representative flow (API + Web + Logs) against the AI-disabled instance and confirms full functionality — every non-AI capability works identically to the AI-enabled configuration, per the AI-optionality compliance metric.
She reviews the credential/secrets handling architecture (Volume 1, Volume 4) to confirm no plaintext secrets are stored or logged.
She approves Nexus for adoption with AI features left disabled pending a separate, later review specifically scoped to the AI module's data handling.

Capability exercised: Verifiable AI-optionality; security review completed without requiring simultaneous approval of the AI layer — decoupling the two approval decisions is itself a product requirement (Section 12).

11.9 Use-case-to-requirement traceability

These seven use cases are the direct source for the functional requirements in Section 12. Where a functional requirement cannot be traced back to at least one use case here, it should be treated as a hypothesis to validate with a design partner rather than a committed scope item for the initial release.




12. Functional Requirements


12.1 Requirement notation

Each requirement is identified as FR-<area>-<number>, stated as a single testable capability, and tagged with the use case(s) from Section 11 that motivate it and the persona(s) from Section 10 it serves. Priority is MoSCoW (Must/Should/Could/Won't for this release). This numbering scheme carries forward into Volume 1, where each FR should map to at least one concrete component or API contract.

12.2 Orchestration core requirements
ID	Requirement	Source use case(s)	Persona(s)	Priority
FR-ORCH-1	The system shall execute a flow defined as an ordered sequence of steps, each targeting exactly one registered module.	UC-1	Priya, Marcus	Must
FR-ORCH-2	The system shall support passing the output of any step as an input to any later step in the same flow via variable interpolation.	UC-1	Priya	Must
FR-ORCH-3	The system shall halt flow execution on the first failed step by default (fail-fast), with an explicit per-flow override to continue-on-failure for flows that require it.	UC-1, UC-4	Priya, Marcus	Must
FR-ORCH-4	The system shall support conditional step execution based on the evaluated truthiness of an expression referencing prior step results or flow variables.	UC-3	Priya	Should
FR-ORCH-5	The system shall support automatic retry of a failed step, with a configurable retry count and backoff, and shall distinguish and report "passed on retry" (flake) from "passed on first attempt" in the flow report.	UC-4	Marcus, Sofia	Must
FR-ORCH-6	The system shall allow a new capability domain to be added by implementing a documented module interface, without modifying the orchestrator core.	UC-6	Raj	Must
FR-ORCH-7	The system shall expose the set of available modules and, per module, the set of available actions and their parameter schemas, via an introspectable API.	UC-6	Raj, Priya	Must
12.3 Module-specific requirements (representative, not exhaustive)

Full per-module requirement catalogs belong in Volume 1's component specifications; the entries below are the cross-cutting representative set that establishes the pattern every module must follow.

ID	Requirement	Source use case(s)	Persona(s)	Priority
FR-API-1	The API module shall support GET, POST, PUT, PATCH, and DELETE requests with configurable headers, body, and timeout.	UC-1	Priya	Must
FR-API-2	The API module shall support declarative assertions on response status code and on arbitrary JSON body paths (equality, contains, existence).	UC-1, UC-2	Priya	Must
FR-WEB-1	The Web module shall support standard interaction actions (navigate, click, fill, hover, select, upload) against a real browser instance.	UC-1, UC-3	Priya	Must
FR-WEB-2	The Web module shall support visibility- and text-based assertions, including a "contains" mode distinct from exact match.	UC-1	Priya	Must
FR-MOB-1	The Mobile module shall support tap, type, and swipe gestures against a connected device or emulator session.	UC-5	Priya	Should
FR-LOG-1	The Logs module shall support declarative rule-based checks (pattern, severity, maximum allowed occurrence count) against a supplied log source.	UC-1	Priya	Must
FR-LOG-2	The Logs module shall support statistical anomaly detection over time-bucketed error rates, with a configurable sensitivity threshold.	UC-5	Sofia	Should
FR-AI-1	The AI module shall generate a structurally valid flow definition from a natural-language description, scoped to a specified subset of available modules.	UC-2	Priya	Must
FR-AI-2	The AI module shall propose a replacement selector for a failed Web-module step given the broken selector, the step's stated intent, and current page markup, along with a confidence score.	UC-3	Priya	Should
FR-AI-3	Every AI module output that is used to alter flow execution (e.g., a self-heal) shall be recorded in the flow report as AI-assisted, distinguishably from a step that required no AI intervention.	UC-3	Priya, Sofia, Elena	Must
12.4 Reporting and observability requirements
ID	Requirement	Source use case(s)	Persona(s)	Priority
FR-REP-1	The system shall produce a single unified report per flow run covering every module used in that flow, with a consistent pass/fail/skip status model across modules.	UC-1, UC-4	Priya, Marcus, Sofia	Must
FR-REP-2	The system shall support exporting flow reports in at least one CI-native format (e.g., JUnit XML) in addition to its native JSON/HTML formats.	UC-4	Marcus	Must
FR-REP-3	The system shall retain historical flow-run results sufficient to compute pass-rate and flake-rate trends over a rolling window of at least 90 days.	UC-5	Sofia	Must
FR-REP-4	The reporting dashboard shall support drill-down from an aggregate trend (e.g., module pass rate over time) to the specific flow run(s) and step(s) responsible for a change in that trend.	UC-5	Sofia	Should
12.5 Platform, security, and administration requirements
ID	Requirement	Source use case(s)	Persona(s)	Priority
FR-SEC-1	The system shall support disabling the AI module entirely via configuration, with all non-AI functionality (per Section 12.2–12.4) remaining fully operational when disabled.	UC-7	Elena	Must
FR-SEC-2	When the AI module is disabled, the system shall make zero outbound network calls to any LLM provider, verifiably (e.g., via network-policy enforcement in the self-hosted deployment, not just application-level configuration).	UC-7	Elena	Must
FR-SEC-3	The system shall never persist or log credential/secret values in plaintext, in flow definitions, in reports, or in application logs.	UC-7	Elena, Marcus	Must
FR-SEC-4	The system shall support role-based access control distinguishing at minimum: flow author, flow runner (CI service account), and administrator roles.	(cross-cutting, all use cases)	All	Must
FR-PLAT-1	The system shall expose all core functionality (flow CRUD, flow execution, report retrieval) via a documented REST API, with the GUI implemented strictly as a client of that API.	UC-4, UC-6	Marcus, Raj	Must
FR-PLAT-2	The system shall provide a CLI capable of executing a flow and producing a report without requiring the GUI or a running server process beyond what the CLI itself starts.	UC-4	Marcus	Must
12.6 Requirements explicitly out of scope for this release

Stated exclusions are as important as inclusions for a blueprint of this scope, since they prevent Volumes 1–5 from silently expanding scope during detailed design:

Full RPA-style business-process orchestration (approvals, human-in-the-loop workflow steps unrelated to software testing) — Nexus automates verification of systems, not general business processes. Revisit only if Archetype C competitive pressure (Section 9.4) makes this strategically necessary.
Native support for automation targets outside the six defined modules (e.g., database-level automation, message-queue automation) as built-in capability — these are explicitly the plugin architecture's job (FR-ORCH-6), not core scope, per UC-6.
A hosted, multi-tenant SaaS AI model that pools customer data for model improvement — conflicts directly with the AI-optionality and data-handling commitments in Section 12.5 and Part I's Product Philosophy; any future consideration of this requires an explicit, separate security and legal review, not a default assumption.




13. Non-functional Requirements
13.1 Purpose and relationship to Volume 4

Non-functional requirements (NFRs) here are stated at the product-blueprint level of precision — measurable targets and the rationale behind them. Volume 4 (Infrastructure Architecture) is responsible for the concrete engineering design (scaling architecture, deployment topology, monitoring stack) that satisfies each NFR below; this section defines the bar Volume 4 must clear, not how it clears it.

13.2 Performance
ID	Requirement	Rationale
NFR-PERF-1	Orchestrator step-dispatch overhead (time from one step completing to the next step's module receiving its run() call) shall not exceed 50ms at p95, excluding the module's own execution time.	The orchestrator must never be a meaningful contributor to flow duration — its job is to get out of the way.
NFR-PERF-2	The REST API shall respond to flow-definition CRUD operations within 200ms at p95 under nominal load.	Directly affects Priya's authoring-loop experience (Persona 3, Section 10.2) — a sluggish authoring UI actively discourages iteration.
NFR-PERF-3	The reporting dashboard shall render a 90-day trend view (FR-REP-3) in under 2 seconds at p95 for an account with up to 10,000 historical flow runs.	Directly affects Sofia's monthly-review workflow (UC-5) — a report that's too slow to explore interactively defeats its own purpose.
NFR-PERF-4	AI module calls (flow generation, self-healing) shall have a defined timeout (default 15 seconds) after which the system falls back to normal (non-AI) failure handling rather than blocking flow execution indefinitely.	AI latency is inherently variable and outside Nexus's direct control; the platform must degrade gracefully, consistent with AI-optionality as an architectural principle, not just a configuration toggle.
13.3 Scalability
ID	Requirement	Rationale
NFR-SCALE-1	The system shall support at least 500 concurrent flow executions per deployment without step-dispatch latency (NFR-PERF-1) degrading beyond its stated target.	Enterprise CI pipelines fan out heavily at peak (e.g., end-of-day merge trains); the platform must not become the bottleneck.
NFR-SCALE-2	The system's execution capacity shall scale horizontally by adding worker instances, without requiring changes to existing flow definitions or client integrations.	Directly informs Volume 4's choice of execution-worker architecture (queue-based dispatch, not a single monolithic executor).
NFR-SCALE-3	Historical flow-run data storage (supporting NFR-PERF-3 and FR-REP-3) shall scale to at least 10 million stored flow-run records per deployment without requiring an architecture change, only a capacity/infrastructure scale-up.	Sets the data-model and storage-engine bar for Volume 1's persistence design — this is a "design for it now, don't retrofit later" requirement.
13.4 Reliability and availability
ID	Requirement	Rationale
NFR-REL-1	The core orchestration and execution path (flow trigger through report generation) shall achieve 99.9% availability for self-hosted deployments following the reference architecture in Volume 4, excluding availability of the systems under test themselves.	This is a CI-blocking dependency for customers (UC-4); Nexus being down is equivalent to blocking every deployment across the organization, which is a materially higher-stakes failure mode than a typical SaaS tool being briefly unavailable.
NFR-REL-2	Flake rate attributable to the platform itself (not the system under test) shall be below 0.5% of step executions, measured continuously as a release-gating metric (this is the platform-health metric first introduced in Part I, Section 6.2, restated here as a formal NFR).	A platform whose own instability is indistinguishable from the target system's instability is not trustworthy as a quality gate.
NFR-REL-3	The system shall support graceful degradation such that a failure or unavailability of the AI module never causes failure of a flow that does not use an AI-dependent step.	Direct restatement of AI-optionality (Product Philosophy 4, Business Objective 5) as a reliability requirement, not just a security/compliance one.
13.5 Security
ID	Requirement	Rationale
NFR-SEC-1	All data in transit (client-to-server, server-to-module-target, server-to-LLM-provider when AI is enabled) shall be encrypted via TLS 1.2 or higher.	Baseline enterprise expectation; explicit here because Volume 4 must treat it as non-negotiable across every deployment topology, including self-hosted.
NFR-SEC-2	Secrets and credentials referenced by flows shall be stored using envelope encryption at rest, with the encryption key managed outside the primary application database (e.g., via a dedicated secrets manager or vault integration).	Directly satisfies FR-SEC-3 at the infrastructure level; Volume 4 (Vault/Secrets section) owns the concrete design.
NFR-SEC-3	The system shall maintain an immutable audit log of all flow-definition changes, execution triggers, and administrative actions (role changes, credential updates), retained for a minimum of 1 year.	Required for Elena's compliance review use case (UC-7) and for enterprise audit requirements generally; informs Volume 1's Audit section directly.
NFR-SEC-4	The self-hosted deployment option (required by Business Objective 5, Part I) shall have no mandatory outbound internet dependency for core (non-AI) functionality.	Regulated/air-gapped environments are a named target segment (Section 10.6, Elena persona); this is a hard constraint on Volume 4's architecture, not a nice-to-have.
13.6 Usability and accessibility
ID	Requirement	Rationale
NFR-UX-1	The flow-authoring GUI shall conform to WCAG 2.1 AA accessibility standards.	Enterprise procurement in many sectors (public sector, large regulated enterprises) requires accessibility conformance as a baseline purchasing condition, independent of any individual user's needs.
NFR-UX-2	A user with prior experience in at least one existing single-domain automation tool (per the competitor archetypes in Section 9) shall be able to author a basic single-module flow within 30 minutes of first exposure to Nexus's flow-authoring UI, without external training, measured via usability testing during the design-partner phase.	Directly supports the "time-to-first-passing-flow" success metric (Part I, Section 6.1) and is testable independent of engineering completion — this is a UX research deliverable, not just a code deliverable.
13.7 Maintainability and extensibility
ID	Requirement	Rationale
NFR-MAINT-1	A new capability module conforming to the documented AutomationModule interface shall require no changes to the orchestrator core, the persistence layer, or the reporting pipeline to function.	Direct restatement of FR-ORCH-6 as an architectural constraint rather than a feature requirement — this is what Volume 1's plugin architecture section must be designed to guarantee, not just support as a best effort.
NFR-MAINT-2	The codebase shall maintain automated test coverage sufficient to catch regressions in the orchestration core and all built-in modules before release, as a formal CI quality gate (elaborated fully in Volume 5).	Nexus is a testing platform; a testing platform with an inadequately tested core is a direct credibility risk (see the "credibility tax" market risk in Section 8.6).
13.8 Compliance and data residency
ID	Requirement	Rationale
NFR-COMP-1	The self-hosted deployment shall support full data residency within a customer-controlled environment, with no data (including flow definitions, run history, or captured screenshots/logs) transmitted outside that environment unless the AI module is explicitly enabled and configured to do so.	Baseline requirement for the regulated-industry segment this blueprint explicitly targets (Business Objective 5, Persona Elena).
NFR-COMP-2	The architecture shall be designed to support a SOC 2 Type II audit without requiring fundamental redesign, even if the audit itself is not completed by initial GA.	"Architected for it, not retrofitted" — consistent with the explicit Business Objective 5 commitment in Part I that trust posture must be true at GA, not promised as a roadmap item.


13.9 Traceability note
Every NFR in this section should be revisited explicitly in Volume 4's own executive summary, with a stated design decision showing how each is satisfied. An NFR that Volume 4 cannot point to a concrete architectural answer for should be escalated back to this blueprint as a gap, not silently dropped during detailed design.
_____________________________________________________________________________________________________________________________________________________________


14. Platform Modules
14.1 Purpose of this section

Part II established why Nexus needs to exist and what capabilities it must have (Section 12's functional requirements). This section formalizes how the product is shaped around those capabilities — the module boundaries, what belongs inside each module versus in the orchestration core, and each module's maturity trajectory. This is the direct bridge between the six-module concept discussed at the outset of this project and the concrete component specifications Volume 1 will define. Nothing in this section should introduce a new capability that isn't already traceable to a Part II functional requirement; this section's job is organization and boundary-drawing, not new scope.

14.2 The seventh module: the orchestration core is not a module, it's the platform

Before describing the six capability modules, one structural point has to be stated precisely, because it is the single most important architectural distinction in the entire product: the orchestration core is not "module zero." It is the platform. The six capability modules — API, Web, Mobile, Desktop UI, Logs, AI — are all peers, all optional (a deployment could theoretically run with only one enabled), and all conform to the same AutomationModule interface introduced conceptually in Section 11.7 (UC-6) and formalized in Volume 1. The orchestration core is the only piece of the system that is never optional and never pluggable. This distinction directly enforces Product Philosophy 4.1 (Composability over completeness, Part I) and is what FR-ORCH-6 and NFR-MAINT-1 (Part II) exist to guarantee architecturally, not just aspirationally.

14.3 Module boundary principle

Every module's scope is defined by one governing question: "does this action require a specialized automation backend (a browser, a device, a screen, a log source, an LLM), or does it belong to flow logic that should be backend-agnostic?" Anything backend-agnostic (variable interpolation, conditional branching, retries) lives in the orchestration core and is available identically to every module. Anything backend-specific lives inside exactly one module. This principle is what prevents scope creep in both directions — modules staying thin and backend-focused, and the core staying free of any single backend's assumptions.

14.4 Module 1 — API

Scope: HTTP/REST (GA), with GraphQL and gRPC as later-phase extensions (Section 15). Owns request construction, response parsing, and declarative assertion evaluation against status/headers/body.

Explicitly not in scope for this module: Performance/load testing at scale (concurrent user simulation) is a distinct capability with different infrastructure needs; it may become an eighth module or a specialized mode of this module in a future phase, but is out of scope per Section 12.6's exclusions until a specific business case justifies it.

Maturity target at GA: Full (this is, per the roadmap in Section 15, the first module to reach production-grade maturity, since it has zero external environment dependencies — no browser, no device, no display — making it the fastest path to a reliable, demoable core).

14.5 Module 2 — Web

Scope: Browser-based UI automation via a modern multi-browser automation protocol (Playwright, per the Volume 1 technology decision). Owns navigation, element interaction, visual/text assertions, and network interception.

Explicitly not in scope for this module: Visual regression testing (pixel-level screenshot diffing across builds) is a related but distinct capability with its own infrastructure needs (baseline image storage, diff-tolerance tuning); it is a Should-priority extension of this module for a later phase, not a GA requirement.

Maturity target at GA: Full, immediately following API module maturity — this is the second module to reach production grade because it is the highest-volume use case among the design-partner personas (Priya, Section 10.2) and the one most existing competitors (Archetype A, Section 9.2) will be directly compared against.

14.6 Module 3 — Mobile

Scope: iOS and Android automation via Appium/WebdriverIO. Owns gesture actions, app lifecycle management, and device/emulator session handling.

Explicitly not in scope for this module: App-store submission automation, push-notification delivery testing infrastructure, and physical device-farm provisioning are all adjacent but distinct capabilities; Nexus's Mobile module automates against a device or emulator that already exists (self-provisioned or via a third-party device-farm integration), it does not provision the device farm itself.

Maturity target at GA: Partial — mobile automation has meaningfully higher environment-setup complexity than Web (Section 9.3 notes this is exactly why mobile specialists exist as a separate competitor archetype), so this blueprint targets a Should-priority, design-partner-validated release for Mobile rather than a Must-priority GA commitment. This is a deliberate, explicit scoping decision, not an oversight — overcommitting to Mobile parity at GA risks the credibility-tax risk named in Section 8.6.

14.7 Module 4 — Desktop UI

Scope: OS-level mouse/keyboard/screen automation via image-based and coordinate-based interaction (nut.js, per Volume 1). Owns the "last resort" automation layer for applications with no accessibility tree or DOM to hook into.

Explicitly not in scope for this module: General-purpose RPA business-process orchestration (Section 12.6 exclusion) — this module automates verification of a desktop application's behavior for quality-engineering purposes, not end-to-end business-process execution for operational cost savings. That distinction is what keeps Nexus positioned as a testing/quality platform (Archetype C analysis, Section 9.4) rather than drifting into RPA's buyer category.

Maturity target at GA: Minimal/Preview — this is explicitly the lowest-priority module for initial release. It exists in the platform's architecture (module contract, orchestrator support) from day one to prove the plugin model's genericity (Section 14.2), but a fully-featured Desktop UI module is a post-GA roadmap item.

14.8 Module 5 — Logs

Scope: Log ingestion (file, stream, common log-platform integrations), rule-based pattern gating, and statistical anomaly detection, per FR-LOG-1 and FR-LOG-2 (Part II).

Explicitly not in scope for this module: Full observability-platform capability (long-term log storage and search at scale, distributed tracing, metrics dashboards) — Nexus's Logs module is an automation and gating layer that can read from an organization's existing observability stack (Datadog, ELK, CloudWatch), not a replacement for that stack. This boundary is important for the competitive positioning in Section 8.2 — Nexus consolidates automation tooling, it does not attempt to also become an observability vendor.

Maturity target at GA: Full — like API, this module has comparatively low environment-setup complexity (reading text, not driving a browser/device) and directly serves the cross-domain flow use case (UC-1) that is the product's core differentiation thesis, so it is prioritized alongside API and Web.

14.9 Module 6 — AI

Scope: Flow generation from natural language, selector self-healing, log triage/summarization — the specific capabilities in FR-AI-1 through FR-AI-3 (Part II). Full strategic treatment is in Section 18 of this Part, with deep architecture in Volume 3.

Explicitly not in scope for this module as currently defined: Autonomous, unsupervised flow execution or modification (the AI module never runs a flow or persists a change without a human-reviewed step, per Product Philosophy 4.4 and FR-AI-3) — this boundary is a permanent product commitment, not a temporary GA limitation to be relaxed later without a deliberate, separately-reviewed decision.

Maturity target at GA: Partial, capability-by-capability — flow generation and log triage (lower-risk, higher-confidence capabilities) target Must-priority at GA; selector self-healing (higher-risk, since it can mask real regressions if the confidence-threshold design in UC-3 is wrong) targets Should-priority with the option-to-disable requirement (FR-SEC-1) treated as non-negotiable regardless of maturity level.

14.10 Module maturity summary at GA
Module	GA maturity target	Primary rationale
API	Full	Lowest environment complexity, fastest path to a reliable core
Logs	Full	Low environment complexity, core to the cross-domain differentiation thesis
Web	Full	Highest-volume persona need, most directly competed-on capability
AI	Partial (capability-dependent)	Strategic differentiator, but risk-scoped by capability per Product Philosophy 4.4
Mobile	Partial	Higher environment complexity; validated with design partners before full commitment
Desktop UI	Minimal/Preview	Proves plugin genericity; full build-out is explicitly post-GA

This maturity ordering is the direct input to the Product Roadmap (Section 15) and should be revisited at each roadmap milestone rather than treated as fixed once written.



15. Product Roadmap
15.1 Roadmap philosophy

This roadmap is phased around the module maturity ordering established in Section 14.10, not around a calendar. Each phase heading below includes an indicative sequencing relationship to the prior phase, but this blueprint deliberately does not assign fixed dates — dates belong in a living planning document (informed by the design-partner validation the Market Analysis in Section 8.4 flagged as required), not in an architecture blueprint that other volumes will reference for years. What is fixed here is sequencing logic and the specific exit criteria for each phase, expressed in terms of the success metrics from Part I, Section 6, and the functional requirements from Part II, Section 12.

15.2 Phase 0 — Foundation

Goal: Prove the orchestration core and plugin architecture with the minimum viable module set, before investing in module breadth or polish.

Scope:

Orchestration core: flow execution, variable interpolation, conditional steps, retry/flake handling (FR-ORCH-1 through FR-ORCH-5)
Plugin architecture and module contract (FR-ORCH-6, FR-ORCH-7), validated by actually building two structurally different modules against it (API and Logs — chosen specifically because they are maximally different backends, which is a stronger validation of genericity than building two similar modules)
API module and Logs module at Full maturity (Section 14.10)
CLI and REST API (FR-PLAT-1, FR-PLAT-2) — no GUI yet
Basic unified reporting (FR-REP-1, FR-REP-2) in JSON and JUnit XML formats

Exit criteria: UC-1 (cross-domain regression flow) and UC-4 (unified pipeline gate) both fully demonstrable end-to-end using only the API and Logs modules, via CLI, with a real CI pipeline integration proven against at least one design-partner's actual pipeline — not just an internal demo environment.

Explicitly deferred: Any GUI work at all. Phase 0 validates the engine; Persona Priya's authoring-UI needs (Section 10.2) are addressed starting Phase 1. This ordering is a direct application of the Mission commitment (Part I, Section 3) to build the orchestration core before any module is polished.

15.3 Phase 1 — Web module and initial GUI

Goal: Bring the highest-volume persona need (Web automation) to Full maturity, and introduce the flow-authoring GUI as a client of the Phase 0 REST API.

Scope:

Web module at Full maturity (FR-WEB-1, FR-WEB-2)
Flow-authoring GUI (Volume 2 scope begins here), consuming the introspectable module-action API (FR-ORCH-7) so that Web-module actions appear automatically, proving the GUI genuinely is a thin client rather than hardcoded to specific modules
Reporting dashboard, initial version (FR-REP-3, FR-REP-4) sufficient for Persona Sofia's UC-5 workflow
RBAC foundation (FR-SEC-4)

Exit criteria: UC-2's authoring workflow (minus AI generation, which is Phase 2) and UC-5's reporting workflow both fully demonstrable via GUI, not just CLI. Time-to-first-passing-flow success metric (Part I, Section 6.1) measured for the first time against real design-partner users, per NFR-UX-2's usability-testing requirement.

15.4 Phase 2 — AI layer (flow generation and log triage)

Goal: Introduce the AI module's lower-risk capabilities, proving the "AI proposes, deterministic code disposes" philosophy (Product Philosophy 4.4) in production before attempting the higher-risk self-healing capability.

Scope:

AI-assisted flow generation (FR-AI-1), scoped initially to the API and Web modules only (the two Full-maturity modules from Phases 0–1)
AI-assisted log triage, per the Logs module's AI-adjacent capability
AI-optionality infrastructure (FR-SEC-1, FR-SEC-2, NFR-REL-3) built and verified from the start of this phase, not retrofitted after — this is a direct, deliberate sequencing decision given how central AI-optionality is to Business Objective 5 and Persona Elena's adoption path
Transparent AI-attribution in reports (FR-AI-3)

Exit criteria: UC-2 fully demonstrable end-to-end including the AI generation step. UC-7 (security review with AI disabled) demonstrable against this same release, proving optionality was built in, not bolted on. AI module cost and latency behavior (NFR-PERF-4) validated under realistic design-partner usage volume.

15.5 Phase 3 — Mobile module and self-healing

Goal: Extend module breadth (Mobile) and AI capability depth (self-healing) in parallel, since they serve different personas and carry independent risk profiles.

Scope:

Mobile module to Partial maturity (Section 14.6), validated with design partners who have an active mobile QA need
Selector self-healing (FR-AI-2), with the confidence-threshold mechanism from UC-3 built and tuned against real Web-module usage data accumulated since Phase 1
Cross-module flows extended to include Mobile (extending UC-1's pattern)

Exit criteria: UC-3 (self-healing) fully demonstrable with the confidence-threshold and transparent-flagging behavior verified against real broken-selector incidents from design-partner usage, not synthetic test cases only. Module Parity Index (Part I, Section 6.3) computed for the first time across four modules (API, Web, Logs, Mobile) and tracked as an ongoing release metric from this phase forward.

15.6 Phase 4 — Desktop UI module and plugin marketplace foundation

Goal: Complete the originally-scoped six-module set, and open the plugin architecture externally (Section 17) now that it has been proven internally across five different built-in modules.

Scope:

Desktop UI module built out from Minimal/Preview to a genuinely usable maturity level (exact target to be set based on Phase 0–3 learnings about how much of the plugin-architecture investment already covers this module "for free")
Public plugin SDK documentation and a reference third-party module (built by a design partner or internal team acting as an external developer, directly exercising UC-6's Raj persona)
Plugin marketplace infrastructure (Section 17) — listing, discovery, and initial certification process, though not yet a monetized marketplace

Exit criteria: A genuinely external party (not the core engineering team) successfully builds and ships a working custom module using only public documentation, within the plugin-development-time target set in Part I, Section 6.3.

15.7 Phase 5 — Enterprise trust hardening and marketplace monetization

Goal: Complete the enterprise trust posture that Business Objective 5 requires to be true "from the first GA release" — this phase's existence in the roadmap reflects that trust hardening is treated as a first-class, resourced initiative, not an assumed byproduct of the earlier phases.

Scope:

SOC 2 Type II audit process (NFR-COMP-2) completed, not just architecturally supported
Self-hosted deployment reference architecture (Volume 4) hardened and documented for regulated-industry adoption (Persona Elena's UC-7 taken to its full conclusion)
Plugin marketplace monetization model activated (Section 17), building on Phase 4's foundation
Licensing tiers (Section 16) fully implemented in product (license enforcement, tier-gated features) rather than only defined on paper

Exit criteria: At least one design partner in a regulated industry completes a full security/compliance review (UC-7) and adopts Nexus in production. First external, revenue-generating plugin marketplace transaction completed.

15.8 Roadmap-to-business-objective traceability
Phase	Primary business objective(s) served (Part I, Section 5)
0 — Foundation	Objective 2 (time-to-first-flow), foundation for all others
1 — Web + GUI	Objective 1 (consolidation), Objective 2
2 — AI (generation, triage)	Objective 3 (maintenance reduction), Objective 5 (trust, via optionality)
3 — Mobile + self-healing	Objective 1 (consolidation), Objective 3 (maintenance reduction)
4 — Desktop UI + marketplace foundation	Objective 4 (marketplace)
5 — Trust hardening + monetization	Objective 5 (enterprise trust), Objective 4 (marketplace revenue)

Every phase above should be re-validated against this table before detailed planning begins — a phase whose scope has drifted away from serving its stated objective is a signal to re-scope, not a signal to update the table after the fact.




16. Licensing Strategy
16.1 Strategic goals a licensing model must serve

A licensing strategy for Nexus has to satisfy four goals simultaneously, and the tension between them is the actual design problem this section resolves:

Drive bottom-up adoption the way the AI-native and open-source-rooted competitors (Archetype D, Section 9.5) do — a closed, sales-led-only model would concede exactly the adoption-speed advantage those competitors currently hold.
Preserve the enterprise trust and revenue case that Business Objective 5 and the RBAC/audit/compliance NFRs (Part II, Section 13.5) depend on — a purely open-source model with no commercial tier undermines the funding needed to build and maintain the enterprise trust posture at all.
Avoid the "open-core bait-and-switch" trust failure that has damaged adoption for other infrastructure products when core functionality is quietly moved behind a paywall after a community has invested in the open version — this is a direct extension of the "credibility tax" risk from Section 8.6, applied to licensing specifically rather than product breadth.
Keep the plugin/module architecture (Section 14.2) commercially neutral — a third-party module builder (Persona Raj, UC-6) should never be structurally disadvantaged by the licensing model relative to Nexus's own built-in modules, or the Plugin Marketplace Vision (Section 17) has no credible foundation.
16.2 Recommended model — open core with a clearly-drawn, permanent line

Nexus should ship as open core: the orchestration engine, the plugin/module contract, and a defined set of built-in modules are open source under a permissive license (MIT, consistent with the reference framework built during this project's early prototyping); enterprise capabilities are commercially licensed as a separate, additive layer.

What stays open source, permanently, as an explicit and published commitment (not a policy that can quietly change):

The orchestration core (execution engine, flow language, reporting pipeline)
The AutomationModule plugin interface/contract
The API and Logs modules at Full functional maturity (chosen specifically because these are the two Phase 0 modules, Section 15.2 — the ones that prove the platform's core thesis, and gatekeeping them commercially would undermine adoption at the exact moment a new user is evaluating whether the platform's core claims are real)
The CLI

What is commercially licensed, as the enterprise tier:

RBAC beyond a single-admin default (FR-SEC-4's full role model)
Audit logging and compliance reporting (NFR-SEC-3, NFR-COMP-1, NFR-COMP-2)
The reporting dashboard's cross-module trend and drill-down capability (FR-REP-3, FR-REP-4) beyond a basic single-run report — the open-source CLI/API always produces a full single-run report; the persistent historical analytics layer is the paid capability
Managed AI-provider integration and cost-governance tooling (Section 18) — self-hosted users can always bring their own LLM API key to the open-source AI module, but fleet-wide cost controls, provider routing, and guardrail policy management are enterprise features
SSO/enterprise identity integration
Official support SLAs and the self-hosted enterprise reference architecture hardening (Volume 4)

Why the Web and Mobile modules are a deliberate gray area rather than assigned outright: Unlike API/Logs (kept open to prove the core thesis) or the clearly-enterprise capabilities above, Web and Mobile module licensing should be finalized after Phase 1–3 (Section 15) usage data is available, weighing bottom-up adoption value against the fact that these are the modules most directly competitive with existing open-source tools (Archetype A/B, Section 9.2–9.3) that a purely open Web module would need to differentiate against on capability alone, not licensing. This is stated explicitly as an open decision, not resolved prematurely by this blueprint.

16.3 Why not fully open source

A fully open-source model with no commercial tier would fail Business Objective 5 (enterprise trust funded by sustainable revenue) and would leave no clear mechanism to fund the trust-hardening work in Roadmap Phase 5 (Section 15.7) — SOC 2 audits, dedicated security engineering, and enterprise support SLAs are not typically sustained by a purely community-funded model at the reliability bar Persona Elena's use case (UC-7) requires.

16.4 Why not fully closed / proprietary

A fully closed model forfeits the single biggest structural advantage available against the AI-native narrow competitors (Archetype D): they are, in several cases, more open and more bottom-up-adoptable than the legacy incumbents (Archetype A), and a closed Nexus would be competing on trust and polish alone against competitors who can win on both community trust and AI-native architecture. The market timing analysis (Section 8.5) explicitly notes that platform engineering teams are actively evaluating and often prefer tools they can inspect, self-host, and contribute to.

16.5 Pricing structure principles (detailed pricing deferred to a commercial planning document)

This blueprint does not set specific price points — that is a commercial decision requiring the market-sizing validation flagged in Section 8.4, not an architecture decision. It does establish the structural principles the eventual pricing model must follow:

Pricing scales with organizational usage (seats, or flow-execution volume, or both), not with module count. A customer should never be penalized commercially for adopting more of the "unified platform" value proposition (using five modules instead of two) — that would directly undermine Business Objective 1 (consolidation) and the cross-module flow adoption success metric (Part I, Section 6.1).
AI usage cost is transparent and separately metered from platform licensing, consistent with AI-optionality — a customer should be able to see and control AI-related cost independent of their base platform tier, never bundled opaquely.
The plugin marketplace's commercial terms (Section 17) are independent of the core platform's licensing tier — a customer on the open-source core should still be able to purchase and use commercial third-party plugins, and a plugin developer's revenue share should not depend on which tier the purchasing customer is on.
16.6 Governance of the open/closed boundary itself

Because Section 16.2's "permanently open" commitment is only credible if it is actually enforced over time, this blueprint recommends the boundary between open-source and commercial capability be governed by a published, versioned policy document (maintained alongside, but distinct from, this blueprint) that requires an explicit, public change-log entry for any capability that moves from open to commercial — never a silent change. This is the direct, concrete mechanism for avoiding the "bait-and-switch" trust failure named as Goal 3 in Section 16.1, and should be treated as a hard requirement on the eventual governance process, not just a stated intention.



17. Plugin Marketplace Vision
17.1 Why a marketplace, and why it is a Part III platform decision, not a Part IV afterthought

Business Objective 4 (Part I, Section 5) states the marketplace must be established "within 18 months of GA... as a second revenue and retention channel." This section exists in Part III, alongside the module architecture and licensing strategy, rather than being treated as a later go-to-market initiative, for one specific reason: the marketplace's viability is entirely determined by decisions this blueprint is making now — the plugin interface's genuine simplicity (FR-ORCH-6, NFR-MAINT-1), the licensing model's neutrality toward third-party modules (Section 16.1, Goal 4), and the Module Parity Index metric (Part I, Section 6.3) that makes "third-party modules are true peers" a verifiable claim rather than a marketing statement. A marketplace bolted onto a platform that wasn't architected for it fails; this section is where that architectural intent gets stated as product strategy.

17.2 What the marketplace distributes

Three distinct artifact types, each with a different trust and review bar:

Capability modules — new AutomationModule implementations extending Nexus to a domain none of the six built-in modules cover (database automation, message-queue automation, IoT device automation, and so on — Persona Raj's UC-6 is the direct referent). Highest trust bar, since these execute with the same orchestrator-level privileges as built-in modules.
Flow templates — pre-built, parameterizable flows for common scenarios within existing modules (e.g., "OAuth2 login flow template," "Stripe webhook verification template"). Lower trust bar since these compose existing, already-reviewed module actions rather than introducing new execution capability.
AI prompt/guardrail packs — domain-specific prompt libraries and guardrail configurations for the AI module (e.g., a healthcare-specific PHI-redaction guardrail pack, elaborated further in Volume 3). Distinct trust bar again, since these affect AI behavior and data handling specifically, and require review aligned with the AI Strategy's guardrail principles (Section 18.6).
17.3 The certification model

A three-tier certification model, directly modeled on the differentiated trust bars above, and directly serving Persona Elena's need (Section 10.6) for a review process that doesn't have to be reinvented per plugin:

Tier	Review bar	Who can publish	Marketplace treatment
Community	Automated scan only (dependency vulnerabilities, license compliance, interface-conformance test suite passes)	Anyone	Listed with a clear "community, not vetted" badge; installable but with an explicit friction step (confirmation dialog) before use in a flow
Verified	Automated scan plus a manual security and code-quality review by the Nexus team or an approved partner reviewer	Vetted developers/organizations	Listed with a "verified" badge; no extra friction to install
Enterprise-certified	Verified tier requirements plus a documented data-handling and compliance review (aligned with NFR-COMP-1/NFR-COMP-2's standards)	Established vendors/partners	Eligible for inclusion in enterprise-tier deployments where an organization's policy (Persona Elena's domain) restricts installation to this tier only

This tiering is also the direct mechanism for satisfying Elena's need in UC-7 at organizational scale: an enterprise administrator should be able to configure a policy restricting their organization's Nexus instance to only install Enterprise-certified (or Verified-and-above) plugins, enforced at the platform level, not just as a marketplace UI suggestion.

17.4 Marketplace mechanics
Discovery and installation happens through the same flow-authoring GUI that surfaces built-in module actions (Section 14.2's genericity principle extends directly to marketplace-installed modules — there should be no separate "marketplace module" UI paradigm distinct from how built-in modules appear).
Versioning and compatibility follows semantic versioning against the published AutomationModule interface version, with the orchestrator core enforcing compatibility checks at install time rather than allowing a broken plugin to fail silently at flow-execution time.
Sandboxing and resource limits apply to marketplace-installed modules by default (CPU/memory/network policy), with built-in modules held to the same limits where technically applicable — this is a direct extension of NFR-SEC-2/NFR-SEC-3's security posture to third-party code specifically, and should be treated as a hard requirement given the elevated risk of executing third-party code inside an enterprise's automation pipeline.
17.5 Commercial model

Per Licensing Strategy Section 16.5, marketplace commercial terms are independent of the purchasing customer's own platform tier. The proposed structure:

Free plugins (most Community-tier and many Verified-tier modules and templates) — no revenue share, since the value to Nexus is ecosystem breadth and the Module Parity Index proof-point, not direct monetization.
Paid plugins — a revenue share between the plugin developer and Nexus (specific percentage is a commercial decision deferred to a planning document, not fixed here), modeled on established marketplace precedents (app stores, IDE plugin marketplaces) rather than invented from scratch.
Enterprise-certified plugins may carry a support/SLA component priced separately from the plugin itself, reflecting the additional compliance review investment required to reach that tier.
17.6 Risks specific to the marketplace, and mitigations
Risk: a low-quality Community-tier module damages trust in the platform generally, even though the tiering system (Section 17.3) is designed to make quality expectations legible. Mitigation: the install-time friction step for Community-tier modules and clear badge differentiation are non-negotiable UI requirements, not optional polish — a user should never be able to mistake an unvetted community module for a Nexus-maintained one.
Risk: marketplace fragmentation undermines the "one platform" value proposition if too many overlapping, low-quality modules exist for the same use case with no clear guidance on which to use. Mitigation: deliberate curation (featured/recommended listings) balanced against not gatekeeping Community-tier publishing entirely — this tension should be revisited as an explicit design review once real marketplace usage data exists (tied to Roadmap Phase 4, Section 15.6).
Risk: security review capacity becomes a bottleneck as Verified/Enterprise-certified submissions grow, creating pressure to either slow down growth or cut review corners. Mitigation: the approved-partner-reviewer model in Section 17.3 is designed specifically to scale review capacity beyond the core Nexus team alone, and should be resourced as part of Roadmap Phase 5 (Section 15.7), not treated as solvable by the core team's headcount alone.
17.7 Success measurement

The marketplace's health should be tracked against Business Objective 4 using metrics parallel in spirit to Part I, Section 6: number of Verified-or-above modules available at each roadmap phase gate, percentage of active Nexus customers using at least one marketplace-sourced module or template, and marketplace revenue as a percentage of total platform revenue by the end of Roadmap Phase 5. These specific targets should be set once Phase 4's initial marketplace launch (Section 15.6) provides real baseline data, rather than being estimated in this blueprint without evidence — consistent with this document's general discipline (Section 8.4) of not laundering unvalidated guesses into false-precision targets.




18. AI Strategy
18.1 Scope of this section versus Volume 3

This section states the product and business strategy for AI within Nexus — positioning, differentiation, the principles that govern where AI is and is not applied, and the cost/provider strategy. It deliberately does not specify prompt engineering, RAG architecture, or model-routing implementation details — those belong to Volume 3 (AI Architecture) in full. Every principle stated here is a binding constraint that Volume 3's technical design must satisfy, in the same traceability relationship Section 13.9 established between this blueprint and Volume 4.

18.2 The strategic thesis, restated precisely

Section 7.5 (Problem Statement) identified that AI-assisted automation capability is arriving piecemeal, retrofitted into single-domain point tools, while Nexus's structural opportunity is to architect an AI layer once, across all modules, from the start. This is Nexus's single clearest source of differentiation against Archetype A/B/C competitors (Section 9) and its single greatest point of convergence with Archetype D — meaning the AI Strategy is not really competing on "having AI," which will be table stakes across the whole market within the roadmap's timeframe, but on AI that is genuinely cross-domain, genuinely optional, and genuinely transparent (Product Philosophy 4.4). Those three qualifiers are the actual strategy; "we have AI" is not a strategy.

18.3 Where AI creates value — the three-capability framework

Every current and future AI capability in Nexus should map to one of exactly three value mechanisms, a discipline that prevents the "AI feature sprawl" failure mode where capabilities get added because they're technically possible rather than because they serve a real cost or quality lever:

Authoring acceleration — reducing the time to go from intent to a working flow (flow generation, FR-AI-1). Value mechanism: directly serves the time-to-first-passing-flow success metric (Part I, Section 6.1).
Maintenance reduction — reducing the ongoing human effort to keep existing automation working as the target system changes (selector self-healing, FR-AI-2). Value mechanism: directly serves the maintenance-burden success metrics (Part I, Section 6.2), which Section 7.3's problem statement identified as the industry's most consistent automation-program failure point.
Diagnostic acceleration — reducing the time to understand why something failed (log triage, future failure-analysis/root-cause capabilities referenced in Volume 3's scope). Value mechanism: directly serves Persona Sofia's UC-5 reporting workflow and the mean-time-to-diagnose metric (Part I, Section 6.2).

A proposed AI capability that does not clearly map to one of these three mechanisms should be treated as a research exploration, not a roadmap commitment, until it can be mapped to a mechanism and a metric.

18.4 Where AI does not go — permanent boundaries

Three boundaries are stated as permanent product commitments, not phase-1 limitations to be relaxed later without a deliberate, explicit, separately-reviewed decision (this qualification, consistent with Section 14.9's module-scope statement, exists specifically so a future team cannot silently cross these lines under roadmap pressure):

AI never executes or persists a change autonomously. Every AI output that affects flow definitions or flow execution passes through a human-reviewable step (UC-2, UC-3) or, at minimum, a transparent, reversible flag in the report (FR-AI-3). This is Product Philosophy 4.4 made into an unconditional product boundary.
AI is never a required dependency for core platform functionality. Restated here from NFR-REL-3 and FR-SEC-1 as a strategic, not just technical, commitment — this boundary is what makes the regulated-industry market segment (Persona Elena) addressable at all, and abandoning it would materially shrink the addressable market described in Section 8, not just create an engineering inconvenience.
AI does not become a data-pooling mechanism across customers. Explicitly excluded already in Part II, Section 12.6; restated here because it is as much a strategic commitment (trust posture is a stated competitive differentiator, Section 9.6) as a technical or legal one.
18.5 Model provider strategy
Provider abstraction, not lock-in. Volume 3 must design the AI module against a provider-agnostic interface, even though a specific default provider will be used for the reference implementation and hosted offering. This serves two goals at once: enterprise customers with existing model-provider relationships or compliance constraints can substitute their preferred provider, and Nexus is not strategically exposed to a single provider's pricing or availability changes.
Self-hosted/open-weight model support is a Should-priority, not Must-priority, capability for GA, but the provider-abstraction requirement above should be designed so that adding this support later is a configuration change, not an architecture change — directly consistent with NFR-MAINT-1's "extend without core changes" principle applied to the AI module specifically.
Cost governance is a first-class product capability, not an operational afterthought, per the enterprise-tier scoping in Licensing Strategy Section 16.2 (managed AI-provider integration and cost-governance tooling). This reflects that AI API cost is a genuinely variable, usage-scaling cost in a way that the rest of the platform's cost structure is not, and customers will require visibility and control over it as a condition of adoption at scale.
18.6 Guardrails as product strategy

Guardrails (the specific technical mechanisms are Volume 3's scope: prompt-injection resistance, output validation, PII/sensitive-data handling in prompts) are treated here as a strategic differentiator specifically because Section 8.3 identified that AI-assisted features are being retrofitted into point tools without a unified architecture — a unified guardrail strategy across all six modules' AI touchpoints is something a retrofitted competitor structurally cannot easily replicate. The AI prompt/guardrail packs described in the Plugin Marketplace Vision (Section 17.2) are the direct product mechanism for extending this guardrail strategy into domain-specific and regulated-industry contexts without the core Nexus team needing to anticipate every compliance regime up front.

18.7 Evaluation and quality strategy

AI capability quality is tracked as a first-class, ongoing product concern, not a one-time launch validation:

Self-heal acceptance rate (Part I, Section 6.2) is the primary ongoing quality signal for the self-healing capability specifically, and should gate the confidence-threshold tuning referenced in Roadmap Phase 3 (Section 15.5).
Flow-generation structural validity rate (the percentage of AI-generated flows that parse as valid flow definitions without requiring a retry, referenced in the AI module's technical design in Volume 3) is the primary quality signal for authoring acceleration.
Both metrics should be tracked per model/provider configuration, not just in aggregate, so that the provider-abstraction strategy (Section 18.5) is backed by real comparative data when a customer or the platform team considers switching providers.
18.8 Competitive AI positioning summary

Restating Section 9.6's competitive table through the specific lens of AI strategy: Nexus's AI differentiation is not raw model capability (which is substantially a function of the underlying provider, not Nexus's own engineering, and is not a durable moat by itself) but the combination of cross-domain breadth, architected optionality, and unified guardrails — three properties that require the platform-level architectural commitments made throughout this Part III to exist prior to any specific AI feature being built. This is why the AI Strategy is positioned in Volume 0 as a peer to the Platform Modules and Licensing Strategy sections, rather than as a bolt-on feature list: the strategy's credibility depends entirely on the platform decisions around it, not on the AI capabilities in isolation.

_____________________________________________________________________________________________________________________________________________________________



19. High-Level Architecture
19.1 Purpose of this section

Part III formalized the product's shape (modules, roadmap, licensing, AI strategy). This section takes the first step from product shape to system shape — a high-level view of the major components and how they relate, precise enough to hand to Volume 1 as a starting contract, but deliberately not so detailed that it pre-empts Volume 1's own component-, class-, and sequence-level design work. Everything below should be read as "the shape Volume 1 must be consistent with," not "the design Volume 1 should copy without further engineering judgment."

19.2 The five-layer model

Nexus's system architecture is organized into five layers, each with a single, clearly-bounded responsibility. This layering is the architectural expression of the module-boundary principle established in Section 14.3, applied to the whole system rather than just the module layer.

Client layer — the GUI (React, per the locked technology decision) and the CLI. Both are, without exception, clients of the API layer below; neither contains business logic that the API layer doesn't also enforce independently (this is FR-PLAT-1's "GUI as a client of the API" requirement, restated as an architectural layering rule, not just an interface-design preference).
API layer — the REST API (ASP.NET Core, per the locked .NET/C# stack) that exposes flow CRUD, execution triggering, report retrieval, and administration. Owns authentication, authorization (FR-SEC-4's RBAC), and input validation. Contains no direct knowledge of how a specific module executes a step — it delegates to the orchestration layer.
Orchestration layer — the execution engine described conceptually in Section 14.2: flow parsing, variable interpolation, conditional evaluation, retry/flake handling, and dispatch to the correct module. This layer is the direct architectural home of FR-ORCH-1 through FR-ORCH-7.
Module layer — the six capability modules (Section 14.4–14.9) plus any marketplace-installed third-party modules (Section 17), each implementing the common AutomationModule contract. This layer is intentionally the most "pluggable" layer in the system — the orchestration layer above it and the persistence layer below it should both be able to treat every module, built-in or third-party, identically.
Persistence and integration layer — flow definitions, execution history, audit logs (relational store), plus integration points outward: the LLM provider (AI module only, optional per NFR-REL-3), external log sources (Logs module), browser/device/OS automation backends (Web/Mobile/Desktop UI modules), and target systems under test (all modules, by definition).
19.3 Component diagram (C4 Container level)
                          ┌─────────────────────────────┐
                          │        Client Layer          │
                          │  ┌──────────┐  ┌──────────┐  │
                          │  │   GUI    │  │   CLI    │  │
                          │  │ (React)  │  │ (.NET)   │  │
                          │  └────┬─────┘  └────┬─────┘  │
                          └───────┼─────────────┼────────┘
                                  │   REST/HTTPS │
                          ┌───────▼─────────────▼────────┐
                          │          API Layer            │
                          │   ASP.NET Core Web API         │
                          │   AuthN / AuthZ / Validation   │
                          └───────────────┬────────────────┘
                                          │
                          ┌───────────────▼────────────────┐
                          │      Orchestration Layer         │
                          │  Flow Engine · Variable Store     │
                          │  Retry/Flake Handler · Dispatcher │
                          └───────────────┬────────────────┘
                                          │  AutomationModule contract
              ┌────────────┬─────────────┼─────────────┬────────────┬────────────┐
              ▼            ▼             ▼             ▼            ▼            ▼
          ┌───────┐   ┌───────┐     ┌───────┐     ┌───────┐   ┌───────┐   ┌───────────┐
          │  API  │   │  Web  │     │Mobile │     │Desktop│   │ Logs  │   │    AI     │
          │Module │   │Module │     │Module │     │  UI   │   │Module │   │  Module   │
          └───┬───┘   └───┬───┘     └───┬───┘     └───┬───┘   └───┬───┘   └─────┬─────┘
              │           │             │             │           │             │
              ▼           ▼             ▼             ▼           ▼             ▼
        [Target APIs] [Browsers]   [Devices/   [OS/Screen]  [Log       [LLM Provider,
                       (Playwright) Emulators]  (nut.js)     Sources]   optional]
                                    (Appium)

                          ┌────────────────────────────────┐
                          │   Persistence & Integration      │
                          │   PostgreSQL · Secrets Vault      │
                          │   Audit Log · Report Store        │
                          └────────────────────────────────┘

This diagram is intentionally simplified relative to a true C4 Container diagram (Volume 1 will produce the formal version with deployment cardinality, protocols, and data-flow direction fully annotated); its purpose here is to fix the layer boundaries and module symmetry before Volume 1's detailed design begins.

19.4 Cross-cutting architectural concerns

Four concerns don't belong to any single layer above and are called out explicitly because Volume 1 must design them as horizontal capabilities, not features of any one layer:

Observability of the platform itself (NFR-REL-2's flake-rate requirement, and general operational monitoring) — instrumented at the orchestration and module layers, aggregated and exposed via the infrastructure stack Volume 4 defines (OpenTelemetry, per the technology decisions in Section 21).
Security boundary enforcement — authentication and coarse-grained authorization live in the API layer (19.2, layer 2), but fine-grained execution-time enforcement (e.g., a module respecting a credential-access policy) must be enforced again at the module layer, not assumed to be fully handled upstream. This defense-in-depth posture is a direct response to the elevated risk of executing third-party plugin code (Section 17.4).
AI optionality — architecturally, this means the orchestration layer must be able to route around the AI module cleanly (a flow with no AI-dependent steps must have zero code-path dependency on the AI module being reachable), which is why AI-optionality is listed here as a cross-cutting concern rather than "module 6's problem alone."
Extensibility of the module layer — the AutomationModule contract (Section 19.2, layer 4) is the single most important interface in the whole system, since NFR-MAINT-1 requires new modules to require zero changes to layers 2, 3, or 5. Volume 1 should treat this contract's stability as equivalent in importance to a public API contract, versioned and change-managed accordingly.
19.5 Deployment topology (high-level)

Two supported deployment models, both required by Business Objective 5 and NFR-SEC-4:

Managed/hosted: Nexus-operated infrastructure, multi-tenant at the infrastructure level with strict tenant data isolation (detailed in Volume 4), offered for customers who prefer not to self-host.
Self-hosted: the full five-layer stack deployable inside a customer-controlled environment (Kubernetes/Helm, per Section 21's technology decisions), with the AI module optionally and verifiably disabled (FR-SEC-2) and zero mandatory outbound dependency for non-AI functionality (NFR-SEC-4).

Both deployment models run the identical codebase and layer architecture described in Section 19.2 — there is no separate "enterprise architecture" and "hosted architecture." Licensing tier (Section 16) gates feature availability, not architecture; this is a deliberate simplification that keeps the engineering team maintaining one system, not two.

19.6 What this section deliberately does not specify

Consistent with this section's stated purpose (19.1), the following are explicitly deferred to Volume 1 and are not fixed by this blueprint: internal module-layer sub-architecture (bounded contexts, aggregates, DDD structure), specific database schema, specific message/event patterns between orchestration and module layers, and specific scaling/worker-pool design for NFR-SCALE-1/2. Part IV, Section 21 (Technology Decisions) fixes the platform choices (languages, frameworks, key libraries); Volume 1 fixes everything built on top of those choices.



20. Architecture Principles
20.1 Purpose

Section 19 described the system's shape. This section states the principles that should resolve design disputes Volume 1–5 will inevitably encounter, where two technically valid approaches compete and the deciding factor should be architectural philosophy, not individual preference. Each principle below is traced back to its origin earlier in this blueprint, because a principle with no traceable rationale is just an opinion, and this document set has been explicit throughout about not doing that.

20.2 Principle: Contract stability over implementation convenience

Statement: The AutomationModule interface, the flow-definition schema, and the REST API's public surface are treated as the most expensive things in the system to change, and any implementation convenience gained by breaking one of these contracts must be weighed against that cost explicitly, not assumed to be free.

Origin: Direct consequence of NFR-MAINT-1 and the plugin architecture's centrality to Business Objective 4 (marketplace) and the Module Parity Index metric. A marketplace whose plugin contract changes incompatibly across releases has no viable third-party ecosystem.

Practical implication for Volume 1: Contract changes require a versioning strategy (semantic versioning, deprecation windows) from the first release, not retrofitted once external plugin developers exist.

20.3 Principle: Every module is executed the same way, observed the same way, and reported on the same way

Statement: The orchestration layer must not contain module-specific branching logic anywhere in its core dispatch, retry, or reporting code paths. Module-specific behavior lives entirely inside the module implementation, behind the common contract.

Origin: Direct restatement of Section 14.2's "orchestration core is not module zero" principle and the Module Parity Index success metric (Part I, Section 6.3), now elevated to a binding architectural rule rather than a product aspiration.

Practical implication for Volume 1: Any proposed orchestrator feature that only makes sense for one module (e.g., "browser-specific retry logic" living in the orchestrator rather than the Web module) should be rejected at design review, not merged with a comment to "clean up later."

20.4 Principle: Fail loud, fail fast, fail informatively

Statement: Every layer, when it encounters an error it cannot recover from, must surface that error with enough structured context (which layer, which component, what was attempted, what failed) for the layer above to make an informed decision — never swallow an error silently, and never surface a generic failure message when a specific one is available.

Origin: Direct architectural expression of Product Philosophy 4.2 (fail-fast, short flows) and 4.4 (transparent over magical), and the concrete source of NFR-REL-2's flake-rate distinguishability requirement and FR-AI-3's AI-attribution-in-reports requirement.

Practical implication for Volume 1: Exception handling and error-type design (Volume 1's coding-standards section will formalize this, and Volume 5's engineering playbook will enforce it) must treat "what do we tell the layer above" as a first-class design question for every component, not an implementation detail decided ad hoc.

20.5 Principle: Security and compliance posture is designed in, not bolted on

Statement: Every new component or capability proposed in Volumes 1–5 must state, at design time, how it satisfies (or is out of scope for) the security NFRs from Part II Section 13.5 and the AI-optionality boundary from Section 18.4 — this determination happens during design, not during a pre-release security review.

Origin: Direct consequence of Business Objective 5's explicit requirement that enterprise trust posture be true "from the first GA release," and Persona Elena's structural role (Section 10.6) as a gatekeeper whose review must be satisfiable without special-casing.

Practical implication for Volume 1: Design documents and ADRs (Architecture Decision Records) for new components should include a short, mandatory security/compliance-impact statement as a template section, not an optional appendix.

20.6 Principle: Optionality is a load-bearing architectural property, not a configuration flag

Statement: Where the product strategy states a capability is optional (AI, specific modules, specific deployment integrations), the architecture must make that capability's absence a normal, tested, first-class code path — not an untested edge case that happens to work because the flag exists.

Origin: Direct consequence of NFR-REL-3 and the repeated emphasis throughout Parts I–III that AI-optionality is a permanent commitment (Section 18.4), not a phase-1 limitation. The distinction between "a flag exists" and "the flag's off-state is genuinely first-class" is the practical difference between a real commitment and a superficial one.

Practical implication for Volume 1 and Volume 5: CI quality gates (Volume 5) should include automated tests that run the full non-AI test suite with the AI module entirely absent from the deployment, not just disabled via configuration, to catch accidental coupling early.

20.7 Principle: Short, composable primitives over large, monolithic capabilities

Statement: When a new capability could be built either as one large, purpose-specific feature or as a composition of smaller, reusable primitives already available in the orchestration and module layers, the composable approach is preferred by default, even when it requires somewhat more authoring effort per use case.

Origin: Direct restatement of Product Philosophy 4.1 (Composability over completeness) as an architectural, not just product-philosophy, principle.

Practical implication for Volume 1: Component design reviews should explicitly ask "could this be expressed as a composition of existing primitives" before approving a new, purpose-built primitive.

20.8 Principle: The self-hosted deployment is a first-class target, not a downscoped version of the hosted one

Statement: Every architectural decision must be validated against both deployment topologies from Section 19.5 before being finalized; a design that only works well in the managed/hosted environment (e.g., assuming persistent outbound internet access, or a specific managed-database service) is not acceptable as a default architecture.

Origin: Direct consequence of NFR-SEC-4 and Business Objective 5's regulated-industry target segment; also protects against the common architectural drift where a "self-hosted option" quietly becomes an afterthought relative to the vendor's own hosted environment.

Practical implication for Volume 4 especially: Reference architectures, Helm charts, and infrastructure documentation must be maintained with the same rigor for self-hosted as for the managed offering, not treated as a lower-priority deliverable.

20.9 How these principles should be used

Each principle above should be cited by number (20.2 through 20.8) in Architecture Decision Records throughout Volumes 1–5 wherever it materially influenced a decision — this is the direct mechanism that keeps a document set this large internally consistent over the months it will take to write and the years it will take to maintain, rather than each volume's author independently reinventing (and potentially contradicting) the same judgment calls.



21. Technology Decisions
21.1 Purpose and decision format

This section fixes the platform-level technology choices — the ones expensive enough to change later that they belong in Volume 0 rather than being left to Volume 1–4's discretion. Each decision states what was chosen, the alternatives considered, and the rationale, in a lightweight ADR format. Volumes 1–4 own all technology decisions below this level (specific libraries within the chosen framework, specific ORM configuration, and so on).

21.2 Backend language and framework — .NET / C#, ASP.NET Core

Decision: The backend (API layer, orchestration layer, and built-in modules) is implemented in C# on .NET, with ASP.NET Core as the web framework. Status: locked, per explicit direction at the start of this document set.

Alternatives considered: Node.js/TypeScript (the language the original prototype framework built earlier in this project used); Go; Java/Kotlin on the JVM.

Rationale: .NET provides a mature, single-vendor-supported, strongly-typed platform with first-class support for the specific technologies the original ToC already anticipated (SignalR for real-time GUI updates, strong async/concurrency primitives well-suited to an orchestration engine dispatching across many concurrent module executions). Strong typing throughout the orchestration and module-contract layers directly supports Architecture Principle 20.2 (contract stability) by making contract violations a compile-time, not runtime, concern in the primary implementation language. This decision does not preclude modules or plugins being implemented in other languages via a process/RPC boundary (relevant to the Plugin Marketplace's third-party module support, Section 17) — that cross-language plugin story is a Volume 1 design question, not foreclosed by this decision.

21.3 Frontend framework — React

Decision: The GUI (flow-authoring surface, reporting dashboard) is implemented in React. Status: locked.

Alternatives considered: Vue, Angular, Svelte.

Rationale: React has the deepest available ecosystem for the two specific, non-trivial UI capabilities the original ToC calls out by name — a code/flow editor (Monaco integration) and a visual flow designer (React Flow integration) — both of which have mature, actively-maintained React bindings. This is a pragmatic ecosystem-fit decision more than an ideological framework preference, and Volume 2 should treat it as such.

21.4 Browser automation backend — Playwright

Decision: The Web module's automation backend is Playwright.

Alternatives considered: Selenium WebDriver; Cypress.

Rationale: Playwright's single-protocol, multi-browser architecture (Chromium, Firefox, WebKit from one API) reduces the Web module's internal complexity relative to Selenium's more fragmented driver model, and its native support for network interception and modern web-app patterns (auto-waiting, shadow-DOM piercing) directly reduces the flake sources that NFR-REL-2's flake-rate budget has to account for. This choice was already validated in the early prototype framework built during this project's initial exploration (Volume-agnostic prior work referenced informally; Volume 1 should treat that prototype's WebModule design as a starting point, not a final one).

21.5 Mobile automation backend — Appium / WebdriverIO

Decision: The Mobile module's automation backend is Appium (protocol) with WebdriverIO as the Node-based client library the module wraps, consistent with the standard industry pairing.

Alternatives considered: Native platform-specific frameworks only (XCUITest and Espresso directly, with no cross-platform abstraction layer).

Rationale: Appium's W3C-standardized protocol is the only widely-adopted option that lets one module abstract over both iOS and Android without maintaining two structurally separate implementations — directly serving Architecture Principle 20.3 (every module executed and reported on the same way) at the Mobile-module level specifically. The cost (Appium's comparatively higher setup complexity, noted already in Section 9.3's competitive analysis and Section 14.6's Partial-maturity scoping for Mobile at GA) is an accepted tradeoff given the alternative's much higher maintenance burden of two independent native implementations.

21.6 Desktop UI automation backend — nut.js

Decision: The Desktop UI module's automation backend is nut.js (or a maintained fork/successor, subject to Volume 1 confirming current maintenance status at implementation time).

Alternatives considered: SikuliX; native OS automation APIs accessed directly per-platform.

Rationale: nut.js provides a maintained, cross-platform (Windows/macOS/Linux) TypeScript-native API for the mouse/keyboard/screen-image-matching primitives this module requires, avoiding the need to hand-maintain three platform-specific automation backends for what is already, deliberately, the lowest-maturity-target module at GA (Section 14.7). Given this module's Minimal/Preview GA scope, this decision carries the lowest switching cost of any in this section and should be re-validated, not assumed permanent, when Desktop UI is built out further in Roadmap Phase 4 (Section 15.6).

21.7 AI provider integration — provider-abstracted, LLM-agnostic

Decision: The AI module integrates with LLM providers through an abstraction layer rather than a hardcoded single-provider SDK dependency, per the provider strategy already stated in Section 18.5.

Rationale: Restated here as a technology decision because it constrains Volume 3's implementation choices directly — no component outside the AI module's provider-adapter layer should have any provider-specific dependency (e.g., a specific vendor's SDK type) leaking into the orchestration or module-contract layers.

21.8 Primary data store — PostgreSQL

Decision: Flow definitions, execution history, audit logs, and report data are persisted in PostgreSQL.

Alternatives considered: SQL Server (a natural default given the .NET stack); a NoSQL document store (given flow definitions are naturally JSON-shaped).

Rationale: PostgreSQL's mature JSONB support handles the naturally document-shaped flow-definition and step-result data (avoiding a rigid relational schema for inherently variable per-module params/data payloads, per the types.ts design already proven in the early prototype) while still providing full relational and transactional guarantees for execution history, audit logs, and RBAC data where strong consistency matters. It is also free of per-core licensing cost, which matters directly for the self-hosted deployment model (Section 19.5) and the open-core licensing strategy (Section 16) not wanting to impose a paid database dependency on the open-source tier.

21.9 Caching and ephemeral state — Redis

Decision: Redis is used for session state, rate-limiting counters, and short-lived orchestration state (e.g., in-flight flow-run coordination across horizontally-scaled workers, per NFR-SCALE-2).

Rationale: Standard, well-understood choice for this role; no notable alternative was seriously considered given how uncontroversial this layer is relative to the decisions above.

21.10 Real-time client updates — SignalR

Decision: Live flow-execution progress (step-by-step status updates streamed to the GUI during a run, referenced implicitly by Persona Priya's authoring-loop experience) is delivered via SignalR.

Rationale: Native .NET real-time framework, avoiding an additional cross-language real-time stack (e.g., a separate WebSocket/Socket.IO layer) given the backend's locked .NET choice (Section 21.2).

21.11 Observability stack — OpenTelemetry, Prometheus, Grafana

Decision: Platform-internal observability (the "observability of the platform itself" cross-cutting concern from Section 19.4) is instrumented via OpenTelemetry, with Prometheus for metrics collection and Grafana for visualization, in the reference self-hosted architecture.

Rationale: OpenTelemetry's vendor-neutral instrumentation avoids locking the platform's own observability into a specific backend, which matters directly for the self-hosted deployment model where a customer may already have a preferred observability stack (echoing the Logs module's own "read from existing stacks, don't replace them" positioning in Section 14.8, now applied reflexively to Nexus's own operational observability). Full detail is Volume 4's responsibility.

21.12 Deployment and orchestration — Docker, Kubernetes, Helm

Decision: Both deployment topologies (Section 19.5) are containerized via Docker, with Kubernetes as the reference orchestration platform and Helm charts as the primary self-hosted installation mechanism.

Rationale: Industry-standard choice that directly serves NFR-SCALE-2's horizontal-scaling requirement and provides the most broadly portable self-hosted story across the range of enterprise infrastructure Persona Elena's segment (Section 10.6) is likely to already operate. Full detail is Volume 4's responsibility; this decision is recorded here only because it is expensive enough to reverse that it belongs at the Volume 0 locked-decision level.

21.13 Secrets management — Vault (or equivalent)

Decision: Credential and secret storage (NFR-SEC-2's envelope-encryption requirement) integrates with HashiCorp Vault as the reference implementation, with the architecture designed to support equivalent alternatives (cloud-provider-native secret managers) for managed/hosted deployments.

Rationale: Directly satisfies NFR-SEC-2's requirement that the encryption key be managed outside the primary application database; Vault is the most broadly-adopted, deployment-agnostic option consistent with the self-hosted-first posture of Architecture Principle 20.8.

21.14 Decisions explicitly deferred to later volumes

To keep this section from over-reaching into Volume 1–4 territory: ORM/data-access library choice, specific message-queue technology (if the orchestration layer's worker-dispatch design in Volume 1 requires one beyond Redis), API versioning mechanics, specific CI/CD tooling beyond GitHub Actions (already implied by the original ToC and treated as effectively locked, but formally owned by Volume 5), and frontend state-management library choice within React are all deferred to their respective volumes.




22. Risks
22.1 Purpose and format

This section consolidates risks raised throughout Volume 0 into a single register, adds architecture- and execution-specific risks not previously covered, and assigns each a likelihood/impact assessment and mitigation owner. Risks already detailed in earlier sections are referenced back rather than re-explained in full; this section's value is consolidation and ongoing tracking, not duplication.

22.2 Risk register
ID	Risk	Source	Likelihood	Impact	Mitigation	Owner
RISK-01	A well-funded point-tool incumbent adds cross-domain modules faster than Nexus can establish its moat	Section 8.6	Medium	High	Prioritize speed of credible module breadth (Roadmap Phases 0–4) over any single module's polish; track Module Parity Index continuously as the concrete counter-evidence to "shallow bolt-on" claims	Product/Roadmap owner
RISK-02	Buyer switching cost and inertia slow adoption despite a strong long-run maintenance-savings case	Section 8.6	High	Medium	Build an explicit migration/import story (e.g., importing existing Postman collections or Selenium scripts as a starting point) as an early roadmap item, not an afterthought	Product/Roadmap owner
RISK-03	"Unified platform" positioning triggers buyer skepticism from past bad experiences with shallow all-in-one tools	Section 8.6	Medium	Medium	Lead go-to-market with the Module Parity Index and transparent module-maturity disclosure (Section 14.10) rather than overclaiming uniform depth before it's real	Product/Marketing
RISK-04	An AI-native narrow competitor (Archetype D) closes the domain-breadth gap faster than Nexus closes the enterprise-trust gap	Section 9.5	Medium	High	Treat Roadmap Phase 5 (trust hardening) as non-negotiable and resourced from the start, not a "nice to have once we have time" phase	Executive sponsor
RISK-05	Market-sizing and buyer-behavior assumptions (Section 8.4) turn out to be materially wrong once validated	Section 8.4	Medium	High	Complete primary research (design-partner discovery interviews) before finalizing Roadmap Phase 1+ resourcing commitments; treat Section 8.4's figures as provisional until then	Product/Research owner
RISK-06	Self-healing selectors mask real regressions if confidence-threshold tuning is wrong, undermining trust in the entire AI layer	UC-3, Section 18.7	Medium	High	Ship self-healing with conservative default thresholds, mandatory transparent flagging (FR-AI-3) even on "successful" heals, and track self-heal acceptance rate as a release-gating metric before loosening defaults	AI/Volume 3 owner
RISK-07	AI provider cost or availability changes materially affect unit economics or reliability	Section 18.5	Medium	Medium	Provider-abstraction architecture (Section 21.7) is a hard requirement, not optional, specifically to keep this risk mitigable by a configuration change rather than a re-architecture	Engineering/AI owner
RISK-08	Third-party plugin code introduces a security incident (data exfiltration, privilege escalation) inside a customer's automation pipeline	Section 17.4, 17.6	Low-Medium	High	Mandatory sandboxing/resource limits for all marketplace-installed modules from Phase 4 onward, tiered certification (Section 17.3), and defense-in-depth enforcement at the module layer (Architecture Principle 20.5), not just at install-time review	Security owner
RISK-09	Marketplace security-review capacity becomes a bottleneck as submissions scale	Section 17.6	Medium	Medium	Approved-partner-reviewer model resourced ahead of Phase 5, not reactively once a backlog exists	Marketplace/Trust owner
RISK-10	Open-core boundary erodes over time (capabilities quietly move from open to commercial), damaging community trust	Section 16.6	Low-Medium	High	Published, versioned open/closed policy with mandatory public change-log entries for any boundary change; treated as a governance hard requirement, not a soft norm	Product/Licensing owner
RISK-11	Mobile and Desktop UI modules' lower GA maturity (Section 14.10) creates a credibility gap with prospects who need those modules on day one	Section 14.6, 14.7	Medium	Medium	Transparent maturity disclosure in sales/marketing materials rather than overselling; explicit design-partner validation path (Roadmap Phase 3) before broader GTM claims about Mobile specifically	Product/Sales enablement
RISK-12	Contract instability in the AutomationModule interface (breaking changes without adequate versioning) fractures the plugin ecosystem before it establishes trust	Architecture Principle 20.2	Low-Medium	High	Semantic versioning and deprecation-window policy defined and published before the Phase 4 external-plugin milestone, not after the first breaking change is already needed	Engineering/Architecture owner
RISK-13	The scope of this documentation program itself (six volumes, ~550 pages) outpaces the team's capacity to keep it consistent and current as the real system evolves	Cross-cutting, this project	Medium	Medium	Traceability discipline already established (business objectives → requirements → NFRs → architecture principles → ADRs) must be actively maintained as living documentation, with an explicit owner and review cadence, not treated as a one-time authoring exercise	Documentation/Architecture owner
RISK-14	Team scaling and hiring across six volumes' worth of specialized expertise (backend, frontend, AI/ML, infrastructure, security) does not keep pace with roadmap ambition	Cross-cutting	Medium	High	Roadmap phasing (Section 15) is explicitly sequenced so that team scaling can follow module priority rather than needing all specialties staffed simultaneously from Phase 0	Executive sponsor/Hiring
RISK-15	Regulatory or legal treatment of AI-assisted software (e.g., liability questions around AI-generated test flows or AI-healed selectors) shifts in ways that affect the AI Strategy's assumptions	Section 18	Low	Medium	Maintain the human-review boundary (Section 18.4) as a permanent architectural commitment specifically because it is also a risk-mitigating legal posture, not purely a UX/trust choice; revisit if regulatory guidance changes materially	Legal/Compliance liaison
22.3 Risk review cadence

Given RISK-13's explicit acknowledgment that this document set itself is a live artifact, this risk register should be reviewed at the close of each Roadmap phase (Section 15) at minimum — likelihood and impact re-assessed against what was actually learned during that phase, closed risks marked as such with the resolving evidence noted, and new risks added as they're identified rather than waiting for a scheduled full-document revision.




23. Future Vision
23.1 Purpose — and a deliberate discipline

Every prior section of Volume 0 has been disciplined about separating validated commitment from open hypothesis (Section 8.4's assumption-labeling, Section 16.2's explicit gray areas, Section 22's living risk register). This closing section maintains that same discipline in the opposite direction: it describes what becomes possible once the Roadmap (Section 15) reaches Phase 5, without pretending any of it is committed scope. Nothing in this section carries an FR or NFR identifier, and nothing here should be cited by Volumes 1–5 as a current requirement — it exists to make sure the architecture being locked in now (Sections 19–21) doesn't accidentally foreclose these directions later.

23.2 Beyond six modules — the plugin ecosystem as primary growth engine

Once the Plugin Marketplace (Section 17) is established and the Module Parity Index has real, sustained evidence behind it, the long-run vision is that most new automation-domain coverage comes from the ecosystem, not from the core team building modules 7, 8, and 9 itself — database automation, message-queue automation, IoT/embedded-device automation, and infrastructure-as-code validation are all plausible domains a third party could credibly build, given the architecture Sections 14.2 and 20.3 already commit to. The core team's long-run focus shifts from "how many modules do we build" to "how good is the platform the modules run on" — a natural maturation of Architecture Principle 20.7's composability preference, applied at the whole-ecosystem scale.

23.3 Deeper AI capability, still inside the permanent boundary

Section 18.4's boundary (AI never executes or persists a change autonomously) is stated as permanent, and this vision section does not propose relaxing it. What it does anticipate, within that boundary, is AI capability moving further up the value chain described in Section 18.3:

Autonomous exploration and coverage suggestion — an AI capability that crawls a target application and proposes a set of candidate flows for human review, rather than requiring a human to know what to ask for in the first place (extending "authoring acceleration" from single-flow generation to whole-suite scaffolding).
Cross-flow pattern learning — using the accumulated history of self-heals, flake patterns, and failure diagnostics across a customer's own flow history (never pooled across customers, per Section 18.4's data-pooling boundary) to make future suggestions increasingly specific to that customer's actual application patterns.
Natural-language querying of the reporting layer — extending diagnostic acceleration (Section 18.3) so Persona Sofia's UC-5 workflow could include asking a question like "why did mobile pass rate drop last week" in plain English and receiving an answer grounded in the same underlying trend and drill-down data the dashboard already exposes structurally.

Each of these remains squarely inside "AI proposes, human disposes" — they are more ambitious proposals, not a change to who approves them.

23.4 Industry-specific editions

Once the guardrail-pack mechanism (Section 17.2, Section 18.6) has real usage data, a plausible future direction is formally curated, Nexus-maintained (not just marketplace-community) editions for specific regulated industries — a healthcare edition with HIPAA-aligned guardrail packs and Enterprise-certified module curation pre-configured, a financial-services edition similarly curated for relevant compliance regimes. This is explicitly downstream of, and dependent on, the trust-hardening work in Roadmap Phase 5 (Section 15.7) being genuinely complete first, not a parallel initiative competing for the same resourcing.

23.5 Beyond verification — toward continuous system understanding

The furthest-out extension of the platform's thesis is a shift in framing from "Nexus verifies that a system behaves correctly when triggered" to "Nexus continuously understands how a system behaves in production and flags divergence from expectation automatically" — effectively extending the Logs module's anomaly-detection capability (Section 14.8) from a CI-time gate into a standing, continuous capability that blurs the line between test automation and production observability. This is stated cautiously and explicitly flagged as tension with the deliberate boundary drawn in Section 14.8 ("Nexus reads from observability stacks, it does not become one") — any move in this direction would need a full re-examination of that boundary, including its competitive positioning rationale (Section 8.2's buying-category analysis), not an assumed natural extension.

23.6 Multi-region and data-residency-aware architecture

As the managed/hosted deployment model (Section 19.5) scales into regions with distinct data-residency requirements, the future architecture may need region-aware data placement (flow definitions, execution history, and AI-provider routing all respecting a configured residency boundary) beyond the single-region assumption implicit in this Volume 0's current architecture sections. This is noted here specifically so Volume 4's infrastructure design considers extensibility toward this direction even though it is not GA scope.

23.7 What this vision deliberately does not include

Consistent with the boundaries set throughout this blueprint (Section 12.6, Section 14's per-module scope statements, Section 18.4), this future vision explicitly does not include: general business-process RPA expansion (still out of scope regardless of time horizon, per the Archetype C positioning analysis in Section 9.4), autonomous AI-driven flow execution without human review (permanent boundary, Section 18.4), or a shift away from the open-core licensing commitment (Section 16.2's "permanently open" pieces stay permanently open regardless of how far the roadmap extends). A future vision that quietly walks back Volume 0's foundational commitments is not a vision worth having; it is scope creep wearing a vision's clothing, and this document set has been built, deliberately, to make that distinction checkable rather than a matter of interpretation.

23.8 Closing note for Volume 0

This section closes Part IV, and with it, Volume 0 in full. Every subsequent volume — Backend Architecture, Frontend Architecture, AI Architecture, Infrastructure Architecture, and the Development Standards & Engineering Playbook — now has a complete, internally-traceable foundation to build on: a vision and mission (Part I), a validated (and honestly-labeled) problem and requirement set (Part II), a formalized platform shape (Part III), and a locked architectural foundation with explicit principles, technology decisions, and a living risk register (Part IV). Volume 1 begins from here.



___________________________________________________________________________________________________________________________________________________________________

ADS-001 — Backend Architecture Design

Objective

Design the entire backend platform that orchestrates automation execution, plugin lifecycle, workflows, reporting, AI integration, and persistence.

Chapter 1 – Vision
Purpose
Goals
Non-goals
Functional Requirements
Non-functional Requirements


Chapter 2 – Architectural Principles
Domain-Driven Design (DDD)
Clean Architecture
SOLID
Plugin-first
Event-driven
Dependency Injection
CQRS (optional)
Open/Closed Principle


Chapter 3 – High-Level Architecture
Client

↓

REST API

↓

Automation Orchestrator

↓

Execution Engine

↓

Plugin Manager

↓

Plugins


Chapter 4 – Core Domain Model

Entities

Project
Workspace
Test Suite
Test Case
Test Step
Workflow
Execution
Environment
Browser
Device
Plugin
User
Role
Artifact
Report
Schedule

Value Objects

BrowserType
DeviceType
Locator
URL
Credentials

Aggregates

Project
Execution
Workflow



Chapter 5 – Modules
Core
Automation
Execution
Workflow
Plugin
Reporting
Authentication
Scheduling
Notification
AI Gateway


Chapter 6 – Plugin Framework
Plugin lifecycle
Registration
Discovery
Versioning
Dependency management
Isolation
Security



Chapter 7 – Execution Engine
Scheduler
Queue
Parallel execution
Retry policy
Timeout
Cancellation
Resume
Recovery


Chapter 8 – Event Bus

Events

ExecutionStarted
TestStarted
StepStarted
StepPassed
ScreenshotCaptured
ExecutionCompleted
PluginLoaded



Chapter 9 – Database Design

ER Diagram

Tables

Projects
Executions
Reports
Plugins
Users
Roles
Environments
Devices



Chapter 10 – APIs

REST

WebSocket / SignalR

Authentication

Authorization

Chapter 11 – Security

JWT

OAuth

Secrets

Encryption

Audit

Chapter 12 – Deployment

Docker

Kubernetes

Scaling

Health Checks


__________________________________________________________________________________________________________________________________________________________________


# Volume 1 — 

Backend Architecture · Part I — System Foundations


1. Executive Summary
1.1 What this volume is

Volume 0 answered what Nexus is and why it should exist, and closed with a locked high-level architecture (five layers), seven architecture principles, and a locked technology stack (.NET/C#, React, Playwright, Appium, nut.js, PostgreSQL, Redis, SignalR). Volume 1 is where that high-level shape becomes an implementation-grade specification of the backend: the actual bounded contexts, the actual domain model, the actual execution engine design, the actual database schema, and the actual coding standards an engineer opens a pull request against. Where Volume 0 could reasonably leave a decision as "Volume 1's responsibility," this volume does not have that luxury — everything here should be precise enough that two engineers reading it independently would build compatible systems.

1.2 Relationship to Volume 0 — the traceability chain continues

Every architectural decision in this volume traces back through the chain Volume 0 established: a business objective (Volume 0, Part I) motivates a functional or non-functional requirement (Volume 0, Part II), which is served by a platform-level decision (Volume 0, Part III) and constrained by an architecture principle or locked technology choice (Volume 0, Part IV). Where this volume makes a design decision, it cites the specific upstream item it satisfies (e.g., "per FR-ORCH-6" or "per Architecture Principle 20.3") rather than re-justifying from first principles. This is not a stylistic preference — it is what keeps a six-volume, 550-page document set from silently drifting from its own stated intentions as different sections are authored over time (the exact risk Volume 0's RISK-13 named explicitly).

1.3 What Volume 1 covers, and how it's organized

Six Parts, each answering a different layer of "how is the backend actually built":

Part I — System Foundations (this Part): the C4 model (Context, Container, Component diagrams) that formalizes Volume 0 Section 19's simplified diagram into implementation-grade precision.
Part II — Domain-Driven Design: the bounded contexts, domain model, aggregates, entities, value objects, domain events, commands, and queries that give the system's core concepts (Flow, Module, Execution, Report) a rigorous, code-mappable definition.
Part III — Core Engineering Patterns: the concrete design of the orchestration layer's internals — the execution engine, workflow engine, scheduler, event bus, and the supporting patterns (repository, unit of work, dependency injection, CQRS) that implement it — plus the plugin architecture that makes the AutomationModule contract real.
Part IV — Cross-Cutting Platform Services: authentication, authorization, multi-tenancy, audit, logging, and observability — the horizontal concerns Volume 0 Section 19.4 flagged as belonging to no single layer.
Part V — API & Data: the REST API surface, SignalR real-time design, and the full database design (schema, ER diagrams, and the supporting sequence/class/package diagrams that show how the domain model in Part II actually persists and flows through the system).
Part VI — Operations & Standards: deployment topology detail (building on Volume 0 Section 19.5 and 21.12), performance and scalability engineering against the NFRs from Volume 0 Section 13, and the coding standards that govern how all of the above is actually written.
1.4 Who this volume is for

Primarily Persona Raj (Volume 0, Section 10.5 — the engineer building a custom module) and any backend engineer joining the core team, with Persona Marcus (Section 10.3) as a secondary audience for the API and deployment sections specifically. Unlike Volume 0, which was written to be legible to product, business, and security stakeholders alike, Volume 1 assumes software engineering fluency throughout — it does not re-explain what a bounded context or a repository pattern is in general terms; it explains how Nexus specifically applies them.

1.5 A note on rigor versus premature specificity

Volume 0's Architecture Principle 20.2 (contract stability over implementation convenience) and 20.7 (composable primitives over monolithic capability) apply directly to how this volume itself should be read: where this volume specifies a contract (the AutomationModule interface, the domain events schema, the REST API surface), that specification should be treated as load-bearing and stable. Where this volume specifies an internal implementation detail with no external contract surface (e.g., a specific internal class's private method structure), it should be read as the current best design, revisable through normal engineering judgment and code review — not with the same weight as a contract decision. Part III will flag explicitly, at each pattern's introduction, which category it falls into.

1.6 Documents this volume produces

Consistent with the "every volume includes" checklist from the original project scope: this volume will include Architecture Decision Records (citing Volume 0's architecture principles by number per Volume 0 Section 20.9's instruction), C4 diagrams (this Part), UML diagrams (class, sequence, package — Part V), ER diagrams (Part V), folder structure and naming conventions (Part VI), sample configuration, and coding standards (Part VI). Risks specific to backend architecture decisions will be added to Volume 0's living risk register (Section 22) rather than duplicated into a separate Volume 1 risk section — one register, continuously maintained, per Volume 0's own closing guidance.



2. System Context
2.1 Purpose

Before decomposing Nexus into containers and components (Sections 4 and 5), this section fixes the system's boundary: who and what sits outside Nexus, and what crosses that boundary. This is the direct precursor to the C4 Context diagram in Section 3, and its content is drawn from the personas (Volume 0, Section 10) and the module target integrations (Volume 0, Section 14) already established.

2.2 Human actors at the system boundary
Actor Corresponds to (Volume 0 persona)   Interaction
Flow Author Priya Authors and edits flows via the GUI or directly via the REST API/CLI
CI/Pipeline Operator Marcus   Triggers flow execution from CI systems via CLI or REST API; consumes reports in CI-native formats
Engineering Manager / QA Lead Sofia Views the reporting dashboard; rarely interacts with any other surface
Module Developer  Raj   Implements and registers custom modules against the plugin SDK; does not interact with the GUI or REST API as an end user in this role
Administrator  (implicit — a superset role, not a distinct Volume 0 persona)  Manages RBAC, licensing tier configuration, plugin certification-tier policy, and system-level configuration
Security/Compliance Reviewer  Elena Interacts with documentation and, for self-hosted deployments, infrastructure configuration — not the running application directly in the common case
2.3 External systems at the boundary
External system   Which module(s) integrate with it   Nature of integration
Systems under test — arbitrary HTTP/REST APIs   API module  Outbound HTTP requests, defined per-flow, target system entirely customer-controlled
Systems under test — web applications  Web module  Outbound browser automation (Playwright driving a real or headless browser instance) against a customer-specified URL
Systems under test — mobile apps/devices  Mobile module  Outbound Appium protocol calls to a device, emulator, or device-farm endpoint
Systems under test — desktop applications Desktop UI module Local OS-level automation (mouse/keyboard/screen) against a target running in the same execution environment
Log sources Logs module Inbound read access to files, streams, or third-party observability platforms (ELK, Datadog, CloudWatch, etc., per Volume 0 Section 14.8's "read from, don't replace" boundary)
LLM provider   AI module (optional, per NFR-REL-3) Outbound API calls for flow generation, self-healing, and triage; entirely absent from the system's runtime behavior when the AI module is disabled
Secrets manager (Vault or equivalent)  Cross-cutting (all modules, via credential resolution)   Outbound calls to resolve secrets at flow-execution time; per NFR-SEC-2, secrets are never persisted in Nexus's own primary datastore in plaintext
Identity provider (SSO, enterprise tier)  API layer (authentication) Inbound/outbound per standard OIDC/SAML flows
CI/CD systems (GitHub Actions, Jenkins, GitLab CI, etc.) API layer + CLI   Inbound trigger calls; outbound report delivery in CI-native formats (FR-REP-2)
Plugin marketplace registry   Module layer (plugin installation)  Outbound calls to discover, download, and verify marketplace-sourced modules (Volume 0, Section 17)
Observability backend (Prometheus, or customer's own stack) Cross-cutting (platform self-observability)  Outbound telemetry export via OpenTelemetry (Volume 0, Section 21.11)
2.4 System boundary statement

Nexus's system boundary, precisely stated: Nexus orchestrates and executes automation flows against externally-owned target systems, and optionally consults an externally-owned LLM provider to assist with flow authoring and maintenance — but Nexus does not own, host, or modify any target system, and does not own the LLM provider's model. This boundary statement is the direct architectural expression of two Volume 0 commitments: the Logs module's "read from, don't replace" positioning (Section 14.8) and the AI module's provider-abstraction, non-data-pooling posture (Section 18.4, 18.5). Any future design that would blur this boundary (e.g., Nexus caching or persisting a full copy of a target system's data beyond what a specific flow run's step results require) should be treated as a boundary violation requiring explicit architectural review, not a routine implementation choice.

2.5 Trust boundaries

Distinct from the system boundary (2.4) is the question of trust boundaries, which matters directly for the security architecture in Part IV:

Nexus-to-target-system: target systems are not trusted by default — credential scoping (least-privilege execution, per the Prohibited/Explicit-permission-required framing that governs Nexus's own operational posture) should be assumed necessary per flow, not assumed safe by default.
Nexus-to-LLM-provider: data sent to the LLM provider (page HTML for selector healing, log excerpts for triage, natural-language descriptions for flow generation) crosses an explicit trust boundary that Persona Elena's review process (Volume 0, UC-7) is specifically designed to evaluate. Part IV's Authentication/Authorization sections and Volume 3's guardrail design both treat this boundary as requiring active data minimization (sending only what's needed for the specific AI task), not just encryption in transit.
Nexus-to-plugin-code: the highest-risk trust boundary in the system, per Volume 0 RISK-08 — third-party module code executes with orchestrator-granted privileges unless explicitly sandboxed, which is why Volume 0 Section 17.4 and Architecture Principle 20.5 both treat plugin sandboxing as a hard requirement, elaborated fully in Part III's Plugin Architecture section.
2.6 Context diagram forward reference

Section 3 renders the actors and external systems above as a formal C4 Level 1 Context diagram. The table format used in this section (rather than a diagram) is deliberate — Section 3's diagram is easier to read correctly once the boundary statement (2.4) and trust-boundary distinctions (2.5) are already understood in prose, rather than trying to convey both the what and the why in a single visual.



3. C4 Context Diagram
3.1 Purpose

This section renders Section 2's actors, external systems, and boundary statement as a formal C4 Level 1 (System Context) diagram — the highest, most abstract level of the C4 model, showing Nexus as a single box and everything outside it as either a person or an external system, with no internal structure exposed yet (that begins at Level 2, Section 4).

3.2 Diagram
                    ┌──────────────┐        ┌──────────────┐        ┌──────────────┐
                    │  Flow Author │        │ CI/Pipeline  │        │  Eng Manager │
                    │   (Priya)    │        │  Operator    │        │   (Sofia)    │
                    │  [Person]    │        │  (Marcus)    │        │  [Person]    │
                    └──────┬───────┘        │  [Person]    │        └──────┬───────┘
                           │                └──────┬───────┘               │
                           │  authors flows via GUI │ triggers runs via CLI │ views reports via GUI
                           │  (HTTPS)                │ / REST API (HTTPS)   │ (HTTPS)
                           │                         │                      │
                           ▼                         ▼                      ▼
                    ┌─────────────────────────────────────────────────────────────┐
                    │                                                               │
                    │                          N E X U S                           │
                    │      Automation Orchestration Platform  [Software System]     │
                    │                                                               │
                    │   Executes cross-domain automation flows (API, Web, Mobile,   │
                    │   Desktop UI, Logs) with an optional AI-assisted layer for     │
                    │   flow generation, selector self-healing, and log triage.      │
                    │                                                               │
                    └───┬─────────┬─────────┬─────────┬─────────┬─────────┬────────┘
                        │         │         │         │         │         │
        registers/uses  │  reads  │ outbound│ outbound│ outbound│ outbound│ outbound (optional)
        modules via SDK │  config │ browser │ mobile  │ desktop │ log     │ LLM API calls
        (local dev)     │  from   │ automa- │ device  │ OS      │ source  │ (HTTPS, only if
                        │  Vault  │ tion    │ proto-  │ automa- │ reads   │  AI module enabled)
                        │(HTTPS)  │(various)│ col     │ tion    │(various)│
                        ▼         ▼         ▼         ▼         ▼         ▼
                  ┌─────────┐┌─────────┐┌─────────┐┌─────────┐┌─────────┐┌─────────────┐
                  │ Module  ││ Secrets ││ Target  ││ Target  ││   Log   ││     LLM      │
                  │Developer││ Manager ││ Web Apps││ Mobile  ││ Sources ││  Provider    │
                  │  (Raj)  ││ (Vault) ││& REST   ││& Desktop││(ELK,    ││ (optional,   │
                  │[Person] ││[External││ APIs    ││ Apps    ││Datadog, ││ provider-    │
                  │         ││ System] ││[External││[External││etc.)    ││ abstracted)  │
                  │         ││         ││ System] ││ System] ││[External││[External     │
                  │         ││         ││         ││         ││ System] ││  System]     │
                  └─────────┘└─────────┘└─────────┘└─────────┘└─────────┘└─────────────┘

                    ┌──────────────┐                              ┌──────────────────┐
                    │ Security /   │  reviews architecture         │  Identity         │
                    │ Compliance   │  docs & infra config          │  Provider (SSO)   │
                    │ Reviewer     │  (offline, not runtime)        │  [External System] │
                    │  (Elena)     │                              └─────────┬─────────┘
                    │  [Person]    │                                        │
                    └──────────────┘                              authenticates users
                                                                    (OIDC/SAML, HTTPS)
                                                                              │
                                                                              ▼
                                                                    ┌──────────────────┐
                                                                    │      NEXUS       │
                                                                    │  (API Layer)      │
                                                                    └──────────────────┘
3.3 Reading notes
Elena (Security/Compliance Reviewer) is deliberately shown with a dashed/offline relationship rather than a runtime arrow into Nexus, consistent with Section 2.2's observation that her primary interaction surface is documentation and infrastructure configuration, not the running system.
The LLM Provider relationship is explicitly labeled optional — this is the single most important annotation on this diagram, since it is the visual proof-point of NFR-REL-3 and the AI-optionality architecture principle (Volume 0, Section 20.6) at the highest, most-viewed level of the architecture documentation. Any future edit to this diagram must preserve that "(optional)" label; its removal would be a documentation-level regression against a permanent product commitment (Volume 0, Section 18.4).
Module Developer (Raj) is shown interacting with Nexus differently from the other human actors — via SDK/local development rather than a runtime HTTPS call into a deployed instance — reflecting that his primary relationship is to the plugin contract (formalized in Part III), not to a running system as an end user.
This diagram intentionally omits the internal five-layer structure from Volume 0, Section 19.3 — that decomposition is out of scope for a C4 Context diagram by definition, and appears starting at the Container level in Section 4.
3.4 Diagram maintenance note

Per Architecture Principle 20.2 (contract stability) applied to documentation itself: this diagram's actor and external-system list should be treated as reasonably stable, changing only when a new class of external integration is added (e.g., a new module type per Volume 0's Future Vision, Section 23.2) — not redrawn casually. Internal restructuring within Nexus (which Sections 4 and 5 cover) should never require a change to this Context-level diagram; if it does, that is itself a signal the internal change may be crossing the system boundary in a way that needs Section 2's boundary statement (2.4) revisited.



4. Container Diagram
4.1 Purpose

This is the C4 Level 2 diagram: Nexus's single box from Section 3 is now decomposed into its deployable/runnable units ("containers" in the C4 sense — not necessarily Docker containers specifically, though in Nexus's case the mapping is close given the containerized deployment model from Volume 0, Section 21.12). This formalizes Volume 0 Section 19.3's simplified diagram with explicit technology annotations and protocol labels, per that section's own stated intent to defer the "formal version with deployment cardinality, protocols, and data-flow direction fully annotated" to Volume 1.

4.2 Diagram
 ┌────────────┐  ┌────────────┐
 │ Flow Author│  │CI Operator │  ...(other actors per Section 3)
 └─────┬──────┘  └─────┬──────┘
       │ HTTPS         │ HTTPS/CLI
       ▼                ▼
┌──────────────────────────────────────────────────────────────────────┐
│                            NEXUS  (System boundary)                   │
│                                                                        │
│  ┌────────────────┐        ┌────────────────┐                        │
│  │   Web GUI       │        │      CLI        │                        │
│  │  [Container:     │        │  [Container:     │                        │
│  │   React SPA]     │        │   .NET console    │                        │
│  │                 │        │   app]           │                        │
│  └────────┬────────┘        └────────┬────────┘                        │
│           │  REST/HTTPS + WebSocket   │  REST/HTTPS                     │
│           │  (SignalR for live        │                                 │
│           │   run updates)            │                                 │
│           ▼                            ▼                                │
│  ┌──────────────────────────────────────────────┐                      │
│  │              API Layer                        │                      │
│  │  [Container: ASP.NET Core Web API]              │                      │
│  │  AuthN/AuthZ · Flow CRUD · Execution trigger    │                      │
│  │  · Report retrieval · Admin endpoints           │                      │
│  └───────────────────┬────────────────────────────┘                      │
│                       │  in-process call / internal RPC                  │
│                       ▼                                                  │
│  ┌──────────────────────────────────────────────┐                      │
│  │           Orchestration Engine                 │                      │
│  │  [Container: .NET background service /          │                      │
│  │   worker process]                               │                      │
│  │  Flow Engine · Scheduler · Event Bus ·           │                      │
│  │  Execution Dispatcher · Retry/Flake handling     │                      │
│  └───┬──────┬──────┬──────┬──────┬──────┬─────────┘                      │
│      │      │      │      │      │      │  AutomationModule contract     │
│      ▼      ▼      ▼      ▼      ▼      ▼  (in-process or sandboxed RPC) │
│  ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐┌──────────┐                   │
│  │ API  ││ Web  ││Mobile││Desk- ││ Logs ││    AI     │                   │
│  │Module││Module││Module││top UI││Module││  Module   │                   │
│  │[.NET ││[.NET ││[.NET ││Module││[.NET ││ [.NET     │                   │
│  │ lib] ││ lib +││ lib +││[.NET ││ lib] ││  lib +    │                   │
│  │      ││Play- ││Appium││ lib +││      ││  provider │                   │
│  │      ││wright││/WDIO ││nut.js││      ││  adapter] │                   │
│  │      ││ proc]││ proc]││ proc]││      ││           │                   │
│  └──────┘└──────┘└──────┘└──────┘└──────┘└──────────┘                   │
│                       │                                                  │
│                       │  reads/writes                                    │
│                       ▼                                                  │
│  ┌──────────────────────┐   ┌──────────────────┐   ┌──────────────────┐ │
│  │  Primary Database      │   │   Cache/State      │   │  Audit & Report    │ │
│  │  [Container:            │   │  [Container:        │   │  Store             │ │
│  │   PostgreSQL]           │   │   Redis]            │   │  [same PostgreSQL   │ │
│  │  Flow defs · exec        │   │  Session state ·    │   │   instance,         │ │
│  │  history · RBAC data     │   │  in-flight run       │   │   separate schema]  │ │
│  │                         │   │  coordination        │   │                     │ │
│  └──────────────────────┘   └──────────────────┘   └──────────────────┘ │
│                                                                          │
└──────────────────────────────────────────────────────────────────────┘
       │                    │                    │                    │
       │ outbound            │ outbound            │ outbound (optional) │ outbound
       ▼                    ▼                    ▼                    ▼
 [Secrets Manager]    [Target Systems]      [LLM Provider]      [Observability
  (Vault, HTTPS)     (per module, various)  (HTTPS, provider-    Backend]
                                              abstracted)        (OTLP export)
4.3 Container inventory
Container   Technology  Responsibility Deployment unit
Web GUI  React SPA   Flow authoring, reporting dashboard Static assets served via CDN or app-server, independently deployable from the API
CLI   .NET console application   CI-triggered execution, local development/debugging   Distributed as a self-contained executable/NuGet tool; not a long-running service
API Layer   ASP.NET Core Web API Public REST surface, authentication/authorization, request validation   Horizontally-scalable stateless service (supports NFR-SCALE-2)
Orchestration Engine .NET background service Flow execution, scheduling, event dispatch, retry logic  Horizontally-scalable worker process, separable from the API Layer's deployment (allows independent scaling of "how many flows can run concurrently" from "how many API requests can be served," directly serving NFR-SCALE-1)
API / Web / Mobile / Desktop UI / Logs / AI Modules   .NET libraries, some with external process dependencies (Playwright browser process, Appium/WebdriverIO session, nut.js OS hooks)   Module-specific execution logic behind the common AutomationModule contract   Loaded in-process by the Orchestration Engine for built-in modules; sandboxed/isolated process boundary for marketplace-installed third-party modules (per Volume 0 Section 17.4 and the trust-boundary analysis in Section 2.5 of this Part)
Primary Database  PostgreSQL  Flow definitions, execution history, RBAC/identity data  Managed or self-hosted PostgreSQL instance per Volume 0 Section 21.8
Cache/State Redis Session state, rate limiting, in-flight cross-worker coordination Managed or self-hosted Redis instance per Volume 0 Section 21.9
Audit & Report Store PostgreSQL (separate schema, same or dedicated instance depending on deployment scale) Immutable audit log (NFR-SEC-3), historical report data feeding the dashboard (FR-REP-3)  Schema-level separation from operational data at minimum; full instance separation is a scaling option, not a GA requirement
4.4 Why the Orchestration Engine and API Layer are separate containers, not one

This is the single most consequential container-boundary decision in this diagram, so it is stated explicitly rather than left implicit: the API Layer (request/response, typically fast, stateless) and the Orchestration Engine (flow execution, potentially long-running, resource-intensive per concurrent flow) have fundamentally different scaling profiles. Coupling them into one deployable would force scaling one to scale the other, directly working against NFR-SCALE-1 (500 concurrent flow executions) and NFR-SCALE-2 (horizontal scaling by adding workers, without client-facing changes). This decision also cleanly supports Architecture Principle 20.8 (self-hosted is a first-class target) — a resource-constrained self-hosted deployment can run both containers on shared infrastructure for simplicity, while a large-scale deployment can scale them independently, without any code change, only a deployment-topology change.

4.5 Communication patterns summary
Client → API Layer: synchronous REST over HTTPS for all request/response operations; SignalR (WebSocket, falling back to long-polling) specifically for live flow-execution progress streaming to the GUI.
API Layer → Orchestration Engine: for GA, an in-process or internal-RPC call is assumed sufficient given the container-separation rationale in 4.4 is about scaling independently, not necessarily about network-separating by default — the precise mechanism (in-process for a combined deployment, message-queue-mediated for a fully split deployment) is a Part III (Execution Engine, Scheduler) design decision, not fixed at this diagram's level of abstraction.
Orchestration Engine → Modules: the AutomationModule contract call, in-process for built-in and Verified-tier trusted modules, sandboxed-process boundary for lower-trust marketplace modules (Section 2.5's trust-boundary distinction made concrete).
All containers → Observability Backend: asynchronous OTLP telemetry export, non-blocking with respect to the primary request/execution path (a telemetry-export failure must never cause a flow-execution failure — this is a direct corollary of Architecture Principle 20.4, fail loud/fast/informatively, applied to the observability path itself: observability failures should be visible in their own right, not silently swallowed, but also must not cascade into unrelated failures).
4.6 Forward reference

Section 5 takes the Orchestration Engine container specifically (the most architecturally significant container in this diagram) and decomposes it to the C4 Component level, since its internal structure — the Flow Engine, Scheduler, Event Bus, and Dispatcher named in Section 4.2's diagram — is exactly what Part III's Core Engineering Patterns will specify in full implementation detail.



## 5. Component Diagram

### 5.1 Purpose and scope

This is the C4 Level 3 diagram, decomposing one container from Section 4 — the **Orchestration Engine** — into its internal components. This container is chosen for the Level 3 treatment (rather than, say, the API Layer) because it is the architecturally central, most novel piece of the system: the API Layer is a comparatively conventional ASP.NET Core Web API (its component structure follows standard layered-API conventions and doesn't need a bespoke diagram to be understood), while the Orchestration Engine's internal design is Nexus's actual engineering core and directly implements FR-ORCH-1 through FR-ORCH-7 (Volume 0, Section 12.2).

### 5.2 Diagram

```
┌────────────────────────────────────────────────────────────────────────────┐
│                          Orchestration Engine  [Container]                    │
│                                                                                │
│  ┌───────────────┐                                                            │
│  │  Flow Parser    │  Parses a Flow definition (JSON/YAML) into a validated,     │
│  │  [Component]    │  strongly-typed in-memory Flow graph. Validates against       │
│  │                 │  the module-action schema (Section 5.4).                     │
│  └───────┬───────┘                                                            │
│          │  validated Flow                                                     │
│          ▼                                                                     │
│  ┌───────────────┐        ┌───────────────┐                                  │
│  │  Flow Engine    │◄──────►│ Variable Store │  Holds flow-scoped variables         │
│  │  [Component]    │        │  [Component]    │  (ctx.vars), resolves               │
│  │  Executes steps  │        │                 │  {{interpolation}} expressions       │
│  │  in sequence,    │        └───────────────┘  per Section 5.5.                    │
│  │  evaluates        │                                                            │
│  │  conditionals     │        ┌───────────────┐                                  │
│  │  (FR-ORCH-4)      │◄──────►│ Retry/Flake     │  Implements FR-ORCH-5's retry/       │
│  │                 │        │ Handler         │  backoff and flake-vs-genuine-       │
│  │                 │        │  [Component]    │  failure classification.             │
│  └───────┬───────┘        └───────────────┘                                  │
│          │  step dispatch request                                              │
│          ▼                                                                     │
│  ┌───────────────┐                                                            │
│  │  Module          │  Resolves the target module (per FR-ORCH-1's "each step        │
│  │  Dispatcher      │  targets exactly one registered module"), looks it up in       │
│  │  [Component]     │  the Module Registry, and invokes its `run()` method with       │
│  │                 │  interpolated params. Enforces the sandboxing/trust             │
│  │                 │  boundary from Part I Section 2.5 for non-built-in modules.     │
│  └───────┬───────┘                                                            │
│          │                                                                     │
│          ▼                                                                     │
│  ┌───────────────┐        ┌───────────────┐                                  │
│  │  Module          │◄──────►│  Module         │  The introspectable module/action     │
│  │  Registry        │        │  Registration   │  metadata store implementing          │
│  │  [Component]     │        │  API            │  FR-ORCH-7; consumed by both the      │
│  │  Runtime map of   │        │  [Component]    │  Dispatcher above and, indirectly     │
│  │  registered       │        │  Exposed to the │  via the API Layer, by the GUI's      │
│  │  AutomationModule │        │  API Layer for   │  auto-surfacing of module actions     │
│  │  instances        │        │  introspection   │  (Volume 0, UC-6).                    │
│  └───────────────┘        └───────────────┘                                  │
│                                                                                │
│  ┌───────────────┐        ┌───────────────┐        ┌───────────────┐        │
│  │  Scheduler       │        │  Event Bus       │        │  Execution         │        │
│  │  [Component]     │───────►│  [Component]     │───────►│  Reporter          │        │
│  │  Cron/webhook/    │ emits │  Publishes step-  │consumed│  [Component]       │        │
│  │  file-watch        │ events│  and flow-level    │  by   │  Assembles the      │        │
│  │  triggers (FR-       │        │  events (started,  │        │  unified              │        │
│  │  ORCH scheduling      │        │  completed,        │        │  FlowReport            │        │
│  │  requirement)         │        │  failed, healed)   │        │  (FR-REP-1),           │        │
│  │                       │        │  for SignalR        │        │  persists via the      │        │
│  │                       │        │  streaming and      │        │  Report Repository     │        │
│  │                       │        │  audit logging      │        │  (Part III).           │        │
│  └───────────────┘        └───────────────┘        └───────────────┘        │
│                                                                                │
└────────────────────────────────────────────────────────────────────────────┘
        │                          │                              │
        ▼                          ▼                              ▼
  [Module instances,        [SignalR Hub, in the        [Report/Audit persistence,
   per Container diagram]    API Layer container]         via Repository pattern —
                                                            Part III]
```

### 5.3 Component responsibilities in detail

**Flow Parser** — the single entry point for turning a serialized flow definition into the strongly-typed domain model that Part II formalizes (the `Flow`, `FlowStep` entities). Schema validation happens here, not deeper in the pipeline, consistent with Architecture Principle 20.4 (fail loud, fail fast) — a malformed flow definition should be rejected before any execution resources are committed, with a specific, structured validation error, not discovered mid-execution.

**Flow Engine** — the direct implementation of the orchestrator design already prototyped conceptually in this project's early exploration (the `Orchestrator.runFlow` logic pattern), now formalized as a first-class component with its own dependencies (Variable Store, Retry/Flake Handler) rather than a monolithic function. This component owns fail-fast sequencing (Product Philosophy 4.2, Volume 0) as its default behavior and the continue-on-failure override (FR-ORCH-3) as an explicit per-flow configuration it must respect.

**Variable Store** — scoped strictly to a single flow-run's lifetime (never shared across concurrent flow-runs, which is a specific concurrency-safety requirement given NFR-SCALE-1's 500-concurrent-execution target). Implements the `{{vars.x.y}}` interpolation resolution logic.

**Retry/Flake Handler** — a distinct component from the Flow Engine specifically so that retry/backoff policy can be unit-tested and reasoned about independently of step-sequencing logic, and so that its flake-classification output (Volume 0, NFR-REL-2's platform flake-rate metric) has one clear, auditable source rather than being computed ad hoc wherever a retry happens to occur in the code.

**Module Dispatcher** — the component that actually calls across the trust boundary described in Part I, Section 2.5. Its design must treat every module identically per Architecture Principle 20.3 (every module executed the same way) — there is no module-specific branching inside this component; any module-specific behavior is entirely inside the module implementation on the other side of the `AutomationModule` contract.

**Module Registry / Module Registration API** — the runtime and introspection halves of FR-ORCH-6 and FR-ORCH-7. The Registry is the in-memory (or distributed-cache-backed, for horizontally-scaled deployments) map the Dispatcher queries at execution time; the Registration API is what the API Layer calls to expose available modules/actions to the GUI and CLI, directly enabling the "GUI automatically surfaces new module actions" requirement demonstrated in Volume 0's UC-6.

**Scheduler** — owns trigger mechanisms (cron, webhook, file-watch) that initiate flow execution outside of a direct API call, feeding into the same Flow Engine entry point as an API-triggered run, so that a scheduled run and an API-triggered run are indistinguishable to every downstream component — another direct application of "one flow language, one execution path" (Volume 0 Vision Statement).

**Event Bus** — the internal publish/subscribe mechanism that decouples the Flow Engine's step-by-step progress from its two independent consumers: SignalR (for live GUI updates) and the audit-logging pipeline (Part IV). This decoupling is what allows a future third consumer (e.g., a webhook-notification feature) to be added without modifying the Flow Engine itself — a direct application of Architecture Principle 20.7 (composable primitives).

**Execution Reporter** — assembles the unified `FlowReport` (Volume 0's `FlowReport` type, now formalized fully in Part II's domain model) from the accumulated step results, and is the component responsible for satisfying FR-REP-1's "consistent pass/fail/skip status model across modules" — this consistency guarantee lives here, in one place, rather than being each module's individual responsibility to get right.

### 5.4 Module-action schema, referenced above

The Flow Parser's validation (5.3) and the Module Registration API's introspection (5.3) both depend on a shared schema describing what parameters a given module action accepts — this schema is defined once, per module, and consumed by both components, avoiding the drift risk of two independently-maintained descriptions of the same module surface. Full schema design is a Part II (Domain Model) and Part III (Plugin Architecture) topic; it is referenced here only to explain the Flow Parser and Registration API's shared dependency.

### 5.5 Variable interpolation, referenced above

The `{{vars.x.y}}` interpolation syntax already prototyped in this project's early exploration phase is adopted as the formal design baseline for the Variable Store component, with Part II's domain model formalizing the exact resolution-path grammar and Part III specifying its performance characteristics against NFR-PERF-1 (50ms p95 step-dispatch overhead, which the Variable Store's resolution logic must fit comfortably inside).

### 5.6 What's deliberately not shown at this level

Consistent with C4's leveling discipline, this diagram does not show classes, methods, or database tables — that level of detail is Part II (domain model, entities) and Part V (class diagrams, ER diagrams). It also does not show the API Layer's internal components, since Section 5.1 already scoped this Level 3 treatment specifically to the Orchestration Engine container as the architecturally novel piece; the API Layer's internal structure, being conventional layered-API design, is covered in Part V's REST API section at the level of detail actually needed (endpoint inventory and contract), not a bespoke component diagram.


____________________________________________________________________________________________________________________________________________________



6. Domain-Driven Design
6.1 Why DDD, specifically, for Nexus

Domain-Driven Design is not adopted here as a default enterprise-architecture reflex — it is adopted because Nexus's core problem, as stated all the way back in Volume 0's Problem Statement (Section 7), is fundamentally a modeling problem before it is an execution problem. The hard part of Nexus is not "can we call a REST endpoint" or "can we click a button" — every module in isolation is a solved problem in the industry. The hard part is defining, precisely and consistently, what a "Flow" is, what it means for a "Step" to belong to exactly one "Module," what a "Flow Run" is as distinct from a "Flow" definition, and how these concepts relate identically regardless of which of the six modules is involved — which is exactly Architecture Principle 20.3 (every module executed, observed, and reported on the same way) restated as a modeling challenge. DDD's tactical patterns (aggregates, entities, value objects, domain events) are the direct tool for making that consistency a property of the code's structure, not just a design intention documented in prose.

6.2 Strategic vs. tactical DDD — what Nexus adopts

DDD separates strategic design (bounded contexts, context mapping — Section 7) from tactical design (aggregates, entities, value objects, domain events, repositories — Sections 9 through 12, and Part III's Repository Pattern section). Nexus adopts both, but with different degrees of rigor:

Strategic patterns are adopted fully and treated as architecturally load-bearing. The bounded-context boundaries defined in Section 7 map directly onto the container boundaries already fixed in Part I (Section 4) — this is not a coincidence; the container decomposition was informed by an implicit domain analysis that Section 7 now makes explicit and formal.
Tactical patterns are adopted pragmatically, not dogmatically. Nexus does not, for example, adopt full event sourcing for every aggregate, or insist on strict aggregate-per-transaction boundaries where a pragmatic read-model shortcut serves the reporting use cases (Volume 0, UC-5) better. Section 23 (CQRS, Part III) discusses specifically where read/write separation is and isn't applied, and why.
6.3 Ubiquitous language — the discipline this section establishes

The single most valuable and most fragile output of a DDD effort is a ubiquitous language: a vocabulary that means the same thing in conversation between a product person (Volume 0's personas), in this documentation, and in the actual C# code — the same term, not a synonym translated at each boundary. Nexus's ubiquitous language was substantially already established informally across Volume 0 and Part I of this volume (Flow, Step, Module, Run, Report). Sections 8 through 14 of this Part exist specifically to make that informal vocabulary rigorous and non-negotiable: once a term is defined here, it is the only acceptable name for that concept anywhere in the codebase, the API surface, the GUI copy, and future documentation. A pull request that introduces a synonym for an already-defined ubiquitous-language term (e.g., calling a FlowRun an "Execution" in one part of the codebase and a "Run" in another) should be treated as a defect, not a style nit — this is the direct, concrete mechanism by which DDD prevents the "five tools with five vocabularies" fragmentation problem (Volume 0, Section 7.2) from recurring inside Nexus's own codebase.

6.4 Relationship between the domain model and the AutomationModule plugin contract

A specific tension needs to be named directly: the AutomationModule interface (introduced conceptually in Volume 0 and formalized in Part III's Plugin Architecture section) is, by design, generic and minimally opinionated — it has to be, since a third-party module developer (Persona Raj) must be able to implement it without deep knowledge of Nexus's internal domain model. The domain model in this Part, by contrast, is Nexus's own rich, internal understanding of what a Flow, Step, and Run mean. These two things are related but not identical: a StepResult returned across the plugin contract boundary is a deliberately minimal DTO-like shape, while the Step entity inside the domain model (Section 10) carries richer internal state (audit metadata, AI-attribution flags, retry history) that the module contract itself never needs to know about. Section 9's aggregate design makes this boundary explicit — the plugin contract is an anti-corruption-layer-adjacent boundary, translating between the module's minimal view and the orchestrator's richer internal domain model, consistent with DDD's standard treatment of integration boundaries with external/less-trusted code (directly reinforcing the trust-boundary analysis from Part I, Section 2.5).

6.5 What this Part does not attempt

Consistent with Section 1.5's rigor-versus-premature-specificity guidance: this Part defines the domain model's structure and invariants precisely, because those are contract-level decisions per Architecture Principle 20.2. It does not attempt to fully specify every private implementation method or every validation edge case — that level of detail belongs in the actual codebase and its own inline documentation/tests, not in this blueprint. Where this Part shows C# code, it should be read as an illustrative, structurally-accurate sketch of the intended shape, not as literal, ready-to-compile production code (which would require dependencies, full error handling, and test coverage this document format cannot responsibly provide).

6.6 Section roadmap for the rest of this Part
Section 7 (Bounded Contexts) — the strategic decomposition: which parts of the domain belong to which context, and how contexts relate.
Section 8 (Domain Model) — the ubiquitous-language glossary and a birds-eye view of how the concepts in Sections 9–14 fit together.
Section 9 (Aggregates) — the consistency boundaries: Flow, FlowRun, and ModuleRegistration as the three aggregate roots.
Section 10 (Entities) — the identity-bearing objects within and across those aggregates.
Section 11 (Value Objects) — the immutable, identity-free concepts (status enums, selectors, assertions, variable expressions).
Section 12 (Domain Events) — what the system announces as domain-significant occurrences, and who consumes them (directly implementing the Event Bus component from Part I, Section 5.3).
Section 13 (Commands) — the write-side operations that mutate the domain model.
Section 14 (Queries) — the read-side operations that serve the reporting and GUI-introspection use cases.



7. Bounded Contexts
7.1 Identifying the contexts

Five bounded contexts partition Nexus's domain. Each has its own internal model, its own ubiquitous-language vocabulary where terms might otherwise ambiguously overlap, and — critically — maps onto the container boundaries already fixed in Part I, Section 4, rather than introducing a competing decomposition.

Bounded Context   Owns  Maps to container(s) (Part I, Section 4)
Flow Authoring Flow definitions, versioning, validation  API Layer (write side)
Execution   Flow runs, step dispatch, retry/flake logic, live progress  Orchestration Engine
Module & Plugin   Module registration, the AutomationModule contract, marketplace metadata   Orchestration Engine (registry) + API Layer (registration endpoints)
Reporting & Audit Historical run data, trend aggregation, immutable audit log Audit & Report Store, API Layer (read side)
Identity & Access Users, roles, RBAC policy, tenant boundaries API Layer (authentication/authorization)

A sixth candidate context — AI Assistance — is deliberately not modeled as a fully independent bounded context with its own aggregates. Instead, it is treated as a supporting capability that participates in the Flow Authoring and Execution contexts (a flow-generation request is a Flow Authoring concern; a self-heal is an Execution-context concern), consistent with Volume 0's repeated framing of AI as a layer across the platform rather than a seventh peer module structurally. Section 7.5 elaborates this decision specifically, since it is a deliberate deviation from the six-module framing used elsewhere and needs its own justification.

7.2 Context map
┌─────────────────────┐        Flow definition        ┌─────────────────────┐
│   Flow Authoring      │ ─────────(Customer/Supplier)──►│     Execution         │
│                       │                                │                       │
│  Flow (draft/         │◄────────Validation feedback────│  FlowRun,             │
│  published)           │        (on publish attempt)     │  step dispatch        │
└──────────┬───────────┘                                └──────────┬───────────┘
           │                                                        │
           │ queries available                                     │ dispatches to,
           │ module actions                                        │ receives StepResult from
           │ (Open Host Service)                                    │ (Conformist — accepts the
           ▼                                                        ▼  AutomationModule contract
┌─────────────────────┐                                ┌─────────────────────┐
│  Module & Plugin       │◄───────────────────────────────│   (same context,      │
│                       │      registers/discovers         │    Module Dispatcher   │
│  ModuleRegistration,   │                                │    consumes registry)  │
│  AutomationModule      │                                └─────────────────────┘
│  contract              │
└──────────┬───────────┘
           │ publishes domain events
           │ (Published Language — Section 12)
           ▼
┌─────────────────────┐        Historical run data      ┌─────────────────────┐
│  Reporting & Audit     │◄────────(Customer/Supplier,────│    Execution          │
│                       │         Execution is upstream)  │    (continued)        │
│  FlowReport,           │                                │                       │
│  AuditEntry            │                                │                       │
└──────────┬───────────┘                                └─────────────────────┘
           │
           │ enforces access policy on
           │ (Conformist to Identity & Access's
           │  RBAC decisions)
           ▼
┌─────────────────────┐
│  Identity & Access     │  Shared Kernel: User/Role identifiers are referenced
│                       │  (not duplicated) by every other context that needs
│  User, Role, Tenant,   │  to record "who did this" — Section 7.4 elaborates.
│  Permission            │
└─────────────────────┘
7.3 Integration patterns between contexts

Using standard DDD context-mapping vocabulary, precisely applied:

Flow Authoring → Execution: Customer/Supplier. Execution is the customer — it consumes validated, published Flow definitions and has no ability to modify them. Flow Authoring is the supplier and must not break Execution's expectations about what a "published" flow guarantees (schema-valid, all referenced modules registered) — this is the direct source of Section 9's Flow aggregate invariant that a flow cannot transition to "published" status while referencing an unregistered module or action.
Execution ↔ Module & Plugin: Conformist. The Execution context's Module Dispatcher (Part I, Section 5.3) conforms entirely to the AutomationModule contract as defined by the Module & Plugin context — Execution does not get to unilaterally demand contract changes; contract evolution follows the versioning policy from Architecture Principle 20.2, owned by the Module & Plugin context.
Module & Plugin → Reporting & Audit, and Execution → Reporting & Audit: Published Language. Both upstream contexts communicate with Reporting & Audit exclusively through the formally-defined domain events (Section 12), never through direct database access or ad hoc internal API calls. This is the single most important integration-pattern decision in this context map, because it's what keeps Reporting & Audit's read models decoupled enough to evolve independently (e.g., adding new trend calculations) without requiring changes to Execution's write-side logic — a direct enabler of the CQRS approach discussed in Part III, Section 23.
All contexts → Identity & Access: Shared Kernel (identifiers only). This is deliberately the narrowest possible shared-kernel relationship: other contexts hold and reference UserId/RoleId/TenantId values, but never duplicate Identity & Access's internal logic for evaluating permissions — every permission check is a call into Identity & Access (Part IV's Authorization section formalizes this as a policy-evaluation service), never a re-implementation.
7.4 Why Identity & Access is a shared kernel rather than fully isolated

A fully isolated Identity & Access context (communicating only via events/APIs, with zero shared types) is the more "textbook-pure" DDD choice, but was rejected specifically because TenantId needs to be a first-class, type-safe, universally-present value object across every other context's aggregates to make multi-tenancy (Part IV, Section 26) structurally enforceable rather than optionally remembered. A shared kernel limited strictly to identifier value objects (never behavior) is judged the pragmatic middle ground, consistent with Section 6.2's stated preference for pragmatic over dogmatic tactical DDD application.

7.5 Why AI Assistance is not its own bounded context

Three reasons, each tracing back to Volume 0 commitments: first, per Volume 0 Section 18.4, the AI module never autonomously persists changes — every AI output becomes either a Flow Authoring concern (a generated flow draft, subject to the same publish-time validation as any human-authored draft) or an Execution concern (a self-heal proposal, subject to the same StepResult/domain-event pipeline as any other step outcome). It has no independent domain concept that isn't already "a Flow" or "a FlowRun" with an AI-attribution flag attached (Section 10's entity design formalizes this flag). Second, treating AI as a cross-cutting capability rather than an isolated context is the domain-modeling expression of Volume 0's own "AI is a layer across the platform, not a seventh peer" framing (Section 18.8) — the bounded-context map should reflect product strategy, not contradict it. Third, isolating AI as its own context would force an awkward Customer/Supplier or Conformist relationship with both Flow Authoring and Execution simultaneously for what is, in domain terms, a single coherent concern (AI-assisted authoring and healing), which is a sign the boundary is being drawn in the wrong place per standard DDD context-mapping heuristics.

7.6 Context ownership and team implications

This context map has a direct implication for team structure (relevant to Volume 5's engineering playbook and Volume 0's RISK-14 team-scaling risk): each bounded context is a natural unit of team ownership, and the Customer/Supplier and Conformist relationships above define which team's API changes require coordination with which other team, versus which changes are purely internal to one context. This should inform, not be dictated by, Volume 5's team-structure guidance.



8. Domain Model
8.1 Purpose

This section is the ubiquitous-language glossary (per Section 6.3's discipline) and the birds-eye map of how the concepts detailed fully in Sections 9–14 relate to one another, primarily within the Execution and Flow Authoring bounded contexts (Section 7), since those two contexts hold the richest domain model. Reporting & Audit, Module & Plugin, and Identity & Access have comparatively thinner domain models of their own, described briefly here and detailed further where relevant in Part III and Part IV.

8.2 Core glossary — Flow Authoring context
Term  Definition  Formalized in
Flow  A named, versioned definition of an ordered sequence of Steps, optionally parameterized by declared Variables. The aggregate root of the Flow Authoring context.  Section 9 (Aggregates), Section 10 (Entities)
FlowStep A single unit of work within a Flow, targeting exactly one Module and one Action, with declared Parameters and an optional saveAs binding and when condition.  Section 10
Draft / Published The two lifecycle states of a Flow. A Draft may be freely edited; a Published Flow is immutable and is the only state Execution may run (Section 7.3's Customer/Supplier invariant).   Section 9
Module (as referenced by a FlowStep)   Not itself owned by this context — a FlowStep holds a reference (ModuleKind + action name) resolved against the Module & Plugin context's registry at validation and execution time.   Section 11 (Value Objects), Section 7 (context relationship)
8.3 Core glossary — Execution context
Term  Definition  Formalized in
FlowRun  A single execution instance of a specific Published Flow version, with its own runtime Variable state and ordered list of StepResults. The aggregate root of the Execution context. Section 9, Section 10
StepResult  The outcome of executing one FlowStep within one FlowRun: status, timing, message/error, and any data produced (available to later steps via variable interpolation).   Section 10
RunStatus   The lifecycle state of a FlowRun: Pending, Running, Passed, Failed, Cancelled.   Section 11
StepStatus  The outcome state of an individual StepResult: Passed, Failed, Skipped. Section 11
RetryAttempt   A record of one retry of a given step, distinct from the final StepResult, retained specifically to support the flake-rate metric (Volume 0, NFR-REL-2) — a step that fails once and passes on retry produces both a RetryAttempt record and a final "passed, flaky" StepResult, not just the latter. Section 10
AiAttribution  A value object attached to a StepResult when an AI capability (self-heal) materially altered the step's execution, per FR-AI-3's transparency requirement. Never silently absent when AI intervened.  Section 11
8.4 Core glossary — Module & Plugin context
Term  Definition  Formalized in
ModuleRegistration   The record of a registered AutomationModule implementation: its ModuleKind, available actions, each action's parameter schema, and its trust tier (Built-in, Verified, Community — extending Volume 0 Section 17.3's marketplace certification tiers into the runtime domain model). Aggregate root of this context. Part III, Section 18 (Plugin Architecture)
ActionSchema   The declared shape (parameter names, types, required/optional) of one action a module exposes — the shared dependency the Flow Parser and Module Registration API both consume, per Part I, Section 5.4. Part III, Section 18
8.5 Core glossary — Reporting & Audit context
Term  Definition  Formalized in
FlowReport  The read-model aggregation of a completed (or in-progress) FlowRun's StepResults into the unified pass/fail/skip summary required by FR-REP-1. Distinct from FlowRun itself — FlowReport is a projection, not the source of truth.  Part V (Database Design), Part III (CQRS)
AuditEntry  An immutable record of a state-changing action (flow published, run triggered, role changed, credential updated) satisfying NFR-SEC-3. Part IV, Section 27
8.6 Core glossary — Identity & Access context
Term  Definition  Formalized in
User  A human or service-account identity.   Part IV, Section 24
Role  A named bundle of Permissions (FR-SEC-4's minimum three: flow author, flow runner, administrator). Part IV, Section 25
Tenant   The isolation boundary for a customer organization's data, referenced as a shared-kernel identifier (Section 7.4) by every other context's aggregates.   Part IV, Section 26
8.7 How the glossary terms relate — a worked example

To make the abstract glossary concrete, here is how the terms above compose for a single realistic scenario — Volume 0's UC-1 (cross-domain regression flow) — traced through the domain model:

Priya authors a Flow (Flow Authoring context) named "Checkout regression," containing three FlowSteps targeting the Web, API, and Logs modules respectively, and publishes it (Draft → Published transition).
Marcus's CI pipeline triggers execution. The Execution context creates a new FlowRun referencing that specific Published Flow version.
The Flow Engine (Part I, Section 5) dispatches each FlowStep in order via the Module Dispatcher, which resolves each against the Module & Plugin context's ModuleRegistration for Web, API, and Logs respectively.
Each dispatch produces a StepResult, accumulated onto the FlowRun. The second step's StepResult carries data that the third step's parameters reference via variable interpolation (Section 8.2's saveAs mechanism, resolved by the Variable Store).
On completion, the Execution context's Event Bus (Part I, Section 5.3) publishes domain events (Section 12) that the Reporting & Audit context consumes (Published Language relationship, Section 7.3) to materialize a FlowReport and append AuditEntry records.
Every step of this sequence carries the Tenant identifier from Priya's authenticated session (Identity & Access shared kernel, Section 7.4), scoping every aggregate created along the way.
8.8 Diagram — domain model overview
 Flow Authoring                Execution                    Module & Plugin
┌───────────────┐          ┌───────────────┐              ┌───────────────────┐
│     Flow        │ 1    n  │    FlowRun      │  n       1   │  ModuleRegistration  │
│ (aggregate root) │────────►│ (aggregate root) │◄─────────────│  (aggregate root)    │
│                 │published │                 │  resolves    │                     │
│  - FlowStep[]    │ version │  - StepResult[]  │  each step   │  - ActionSchema[]    │
│  - Variables[]    │  ref    │  - RetryAttempt[]│   against    │                     │
└───────────────┘         └────────┬────────┘              └───────────────────┘
                                    │ domain events
                                    │ (Published Language)
                                    ▼
                          Reporting & Audit
                         ┌───────────────────┐
                         │    FlowReport        │  (projection, not source of truth)
                         │    AuditEntry         │
                         └───────────────────┘

  Identity & Access (shared-kernel identifiers referenced by all aggregates above)
 ┌─────────────────────────────────────────────────────────────────────────┐
 │  User · Role · Tenant                                                      │
 └─────────────────────────────────────────────────────────────────────────┘
8.9 Forward reference

Sections 9 through 14 now take each piece of this overview and specify it fully: aggregate boundaries and invariants (9), entity definitions with C# sketches (10), value object definitions (11), the domain events referenced in Section 8.7 step 5 (12), the commands that drive state transitions like "Draft → Published" and "trigger a FlowRun" (13), and the queries that serve FlowReport retrieval and module introspection (14).



9. Aggregates
9.1 Aggregate design principles for Nexus

An aggregate is a consistency boundary — a cluster of entities and value objects that must be kept transactionally consistent with each other, accessed and modified only through its root, and referenced from outside only by identifier. Nexus defines exactly three aggregate roots, one per the three "richest" bounded contexts identified in Section 7: Flow (Flow Authoring), FlowRun (Execution), and ModuleRegistration (Module & Plugin). Reporting & Audit's FlowReport and AuditEntry are deliberately not modeled as aggregates in the DDD sense — they are read-model projections (Section 8.5 already flagged this), with no independent consistency invariants of their own to protect; they simply reflect what the source-of-truth aggregates have already asserted.

9.2 The Flow aggregate

Root entity: Flow. Invariants this aggregate must always enforce:

A Flow in Published status is immutable — no FlowStep may be added, removed, or modified while in this status. Editing a published flow requires creating a new version (a new Flow aggregate instance sharing a FlowFamilyId but with an incremented version number), never mutating in place. This is the direct enforcement mechanism for the Customer/Supplier invariant from Section 7.3 — Execution can trust that a FlowRun referencing a specific Flow version will never see that version's steps change underneath it mid-run or after the fact.
A Flow may only transition Draft → Published if every FlowStep's (ModuleKind, action) pair resolves against a currently-registered ModuleRegistration (a cross-aggregate check performed at the application-service layer, not inside the aggregate itself, since an aggregate should never directly query another aggregate — this distinction matters and is elaborated in 9.5).
FlowStep ordering within a Flow is significant and is part of the aggregate's consistency boundary — steps are stored as an ordered list, never a set, and any operation that reorders steps must go through the aggregate root.
csharp
public class Flow : AggregateRoot<FlowId>
{
    public FlowFamilyId FamilyId { get; private set; }
    public int Version { get; private set; }
    public string Name { get; private set; }
    public string? Description { get; private set; }
    public FlowStatus Status { get; private set; } // Draft | Published | Archived
    public IReadOnlyList<FlowStep> Steps => _steps.AsReadOnly();
    private readonly List<FlowStep> _steps = new();
    public IReadOnlyDictionary<string, VariableDefault> DeclaredVariables { get; private set; }
    public TenantId TenantId { get; private set; }

    public void AddStep(FlowStep step)
    {
        if (Status != FlowStatus.Draft)
            throw new DomainException("Cannot modify a non-draft Flow. Create a new version instead.");
        _steps.Add(step);
    }

    public FlowPublishResult Publish(IModuleActionResolver resolver)
    {
        // Validates every step's (ModuleKind, action) against the resolver
        // (an injected port to the Module & Plugin context — see Section 9.5)
        // and transitions Status to Published only if all resolve successfully.
        // Returns a result object rather than throwing, since an unresolved
        // module reference is an expected, reportable outcome, not an
        // exceptional program state (Architecture Principle 20.4).
    }
}
9.3 The FlowRun aggregate

Root entity: FlowRun. Invariants:

A FlowRun references an immutable Flow version by FlowId (never FlowFamilyId — a run is always pinned to the exact version that was published at trigger time, so that a Flow being edited in Draft after a run started can never retroactively affect that run's behavior or reporting).
StepResults are appended in step order and are themselves immutable once created — a step that is retried produces a new RetryAttempt record plus, only on the retry sequence's conclusion, one final StepResult; it never overwrites a prior result.
Once a FlowRun's Status transitions to a terminal state (Passed, Failed, Cancelled), no further StepResults may be appended — this is the aggregate-level enforcement of fail-fast sequencing (Product Philosophy 4.2) and the continue-on-failure override's boundary (FR-ORCH-3): even continue-on-failure flows have a defined terminal point after which the aggregate is closed.
csharp
public class FlowRun : AggregateRoot<FlowRunId>
{
    public FlowId FlowId { get; private set; }          // pinned exact version
    public RunStatus Status { get; private set; }
    public RunTrigger Trigger { get; private set; }       // Api | Scheduled | Webhook
    public IReadOnlyDictionary<string, object?> Variables { get; private set; }
    public IReadOnlyList<StepResult> Results => _results.AsReadOnly();
    private readonly List<StepResult> _results = new();
    public DateTimeOffset StartedAt { get; private set; }
    public DateTimeOffset? FinishedAt { get; private set; }
    public TenantId TenantId { get; private set; }

    public void RecordStepResult(StepResult result)
    {
        if (IsTerminal(Status))
            throw new DomainException("Cannot record a step result on a completed FlowRun.");
        _results.Add(result);
        SetVariable(result); // updates Variables if the step declared a saveAs binding
        RaiseDomainEvent(new StepCompletedEvent(Id, result)); // Section 12
    }

    public void Complete()
    {
        Status = _results.Any(r => r.Status == StepStatus.Failed) ? RunStatus.Failed : RunStatus.Passed;
        FinishedAt = DateTimeOffset.UtcNow;
        RaiseDomainEvent(new FlowRunCompletedEvent(Id, Status)); // Section 12
    }
}
9.4 The ModuleRegistration aggregate

Root entity: ModuleRegistration. Invariants:

A ModuleKind value (Section 11) must be unique among currently-active registrations within a tenant's accessible module set — no two simultaneously-active modules may claim the same ModuleKind identifier, since the Flow aggregate's step-resolution logic (9.2) depends on this uniqueness to be well-defined.
An ActionSchema cannot be removed from a registered module while any Published Flow still references that action — enforced again at the application-service layer via the same cross-aggregate query pattern as Flow.Publish (9.5), since this is fundamentally a cross-aggregate check.
Trust tier (BuiltIn, Verified, Community — Section 8.4) is immutable after registration within a given version; a re-certification (e.g., a Community module promoted to Verified per Volume 0's certification process, Section 17.3) creates a new ModuleRegistration version rather than mutating trust tier in place, mirroring the Flow aggregate's versioning approach (9.2) for the same auditability reasons.
csharp
public class ModuleRegistration : AggregateRoot<ModuleRegistrationId>
{
    public ModuleKind Kind { get; private set; }
    public string DisplayName { get; private set; }
    public TrustTier Trust { get; private set; }         // BuiltIn | Verified | Community
    public IReadOnlyList<ActionSchema> Actions => _actions.AsReadOnly();
    private readonly List<ActionSchema> _actions = new();
    public SemanticVersion ContractVersion { get; private set; } // AutomationModule interface version, Architecture Principle 20.2
    public bool IsActive { get; private set; }
}
9.5 Cross-aggregate consistency — the application-service pattern

Per strict DDD discipline, aggregates never directly reference or query one another. Where a cross-aggregate invariant must be checked (e.g., Flow.Publish needing to know whether every referenced module action currently exists in some ModuleRegistration), the pattern used throughout Nexus is: the aggregate exposes a method accepting an injected port interface (IModuleActionResolver in 9.2's sketch) that the calling application service satisfies using a repository query (Part III, Section 15) against the other aggregate's persisted state. This keeps each aggregate's own code free of any direct dependency on another aggregate's repository, while still allowing the invariant to be enforced synchronously at the moment it matters (publish time) rather than deferred to eventual consistency — a deliberate choice, since "can this flow even run" is not a scenario where Nexus should tolerate eventual consistency, per Architecture Principle 20.4's fail-fast philosophy.

9.6 Why not one larger aggregate

A tempting alternative is a single Flow aggregate that also contains all of its historical FlowRuns as child entities, avoiding the cross-aggregate resolver pattern in 9.5 entirely. This is deliberately rejected: a popular, frequently-run Flow could accumulate thousands of historical runs, and loading the full aggregate for any operation (even a simple flow-metadata edit) would become increasingly expensive over time — directly working against NFR-PERF-2 (200ms p95 for flow CRUD) as run history grows, which NFR-SCALE-3 explicitly anticipates scaling to millions of records. Keeping FlowRun as its own aggregate, referencing Flow only by immutable version identifier, is what keeps the two consistency boundaries independently scalable — directly mirroring the container-separation rationale already established in Part I, Section 4.4.



10. Entities
10.1 Entities versus value objects — the dividing line applied consistently

An object is modeled as an entity in this domain when it has a lifecycle and an identity that matters independent of its current attribute values — two FlowSteps with identical parameters are still distinct steps if they occupy different positions in a flow, for example. Everything else (Section 11) is a value object. This section covers the entities that live within the three aggregates defined in Section 9 (not the aggregate roots themselves, already covered there) plus the two entities belonging to the Identity & Access and Reporting & Audit contexts.

10.2 FlowStep (within the Flow aggregate)

Identity: a FlowStepId, stable across edits to the step's own parameters within a Draft, but — per Section 9.2's immutability invariant — never mutated after the parent Flow is Published.

csharp
public class FlowStep : Entity<FlowStepId>
{
    public string Name { get; private set; }
    public ModuleKind Module { get; private set; }
    public string Action { get; private set; }
    public IReadOnlyDictionary<string, object?> Params { get; private set; }
    public string? SaveAs { get; private set; }
    public string? WhenCondition { get; private set; }
    public int Order { get; private set; }  // explicit, not inferred from list position,
                                             // so that step order survives serialization
                                             // round-trips unambiguously
}

The Params dictionary is deliberately weakly-typed (object? values) at this layer — per Section 6.4's discussion of the plugin-contract boundary, FlowStep cannot know the specific shape a given module's action expects at compile time, since that shape is defined by the ActionSchema value object (Section 11) belonging to a different aggregate (ModuleRegistration), resolved only at validation/execution time. Strong typing is recovered at the module-implementation boundary, not here.

10.3 StepResult (within the FlowRun aggregate)

Identity: implicitly positional within its parent FlowRun (a StepResultId exists for referential purposes — e.g., linking an AuditEntry back to a specific result — but StepResults are never looked up or modified independent of their owning FlowRun, consistent with the aggregate-boundary discipline from Section 9.1).

csharp
public class StepResult : Entity<StepResultId>
{
    public FlowStepId SourceStepId { get; private set; }  // which FlowStep this result is for
    public string StepName { get; private set; }           // denormalized for reporting convenience —
                                                              // see the note in 10.3.1 below
    public ModuleKind Module { get; private set; }
    public string Action { get; private set; }
    public StepStatus Status { get; private set; }
    public TimeSpan Duration { get; private set; }
    public string? Message { get; private set; }
    public string? Error { get; private set; }
    public object? Data { get; private set; }
    public AiAttribution? AiAttribution { get; private set; }  // Section 11 — never silently null
                                                                  // when AI materially altered the step
    public IReadOnlyList<RetryAttempt> RetryHistory { get; private set; }
}

10.3.1 — Why StepName and Module/Action are denormalized onto StepResult rather than always joined from FlowStep: because FlowSteps can theoretically be edited in a Draft between when historical runs occurred and when a report is viewed (a new Flow version created per Section 9.2), and because FlowRun deliberately pins to an immutable Flow version — the denormalized fields on StepResult guarantee a report always reflects exactly what ran, not what the step currently looks like in a possibly-newer flow version. This is a direct, deliberate application of Architecture Principle 20.4 (fail loud, fail informatively) extended to historical accuracy: a report should never silently misrepresent what actually executed.

10.4 RetryAttempt (within the FlowRun aggregate, child of StepResult)
csharp
public class RetryAttempt : Entity<RetryAttemptId>
{
    public int AttemptNumber { get; private set; }
    public StepStatus Status { get; private set; }
    public TimeSpan Duration { get; private set; }
    public string? Error { get; private set; }
    public DateTimeOffset AttemptedAt { get; private set; }
}

This entity exists specifically to make the platform's own flake-rate metric (Volume 0, NFR-REL-2) computable directly from the domain model — a query (Section 14) can count FlowRuns whose final StepResult is Passed but whose RetryHistory is non-empty, without needing a separate, parallel tracking mechanism bolted on outside the core execution model.

10.5 ModuleActionEntry (within the ModuleRegistration aggregate)

A thin entity wrapping each ActionSchema value object (Section 11) with registration-specific metadata not intrinsic to the schema itself:

csharp
public class ModuleActionEntry : Entity<ModuleActionEntryId>
{
    public string ActionName { get; private set; }
    public ActionSchema Schema { get; private set; }        // value object, Section 11
    public bool IsDeprecated { get; private set; }
    public string? DeprecationNotice { get; private set; }
}
10.6 User (Identity & Access context)
csharp
public class User : Entity<UserId>
{
    public string Email { get; private set; }
    public string DisplayName { get; private set; }
    public TenantId TenantId { get; private set; }
    public IReadOnlyList<RoleId> AssignedRoles { get; private set; }
    public bool IsServiceAccount { get; private set; }  // distinguishes Marcus's CI-triggered
                                                            // automation identity from human users,
                                                            // relevant to audit-trail attribution
                                                            // (Part IV, Section 27)
}

User is modeled as an entity, not an aggregate root, in this Part's scope — full aggregate treatment of the Identity & Access context (including Role and Tenant) is deferred to Part IV (Authentication, Authorization, Multi-tenancy), since Identity & Access's domain model is comparatively conventional (standard RBAC modeling) and doesn't need the same tactical-DDD depth as the Flow Authoring/Execution/Module & Plugin contexts that are Nexus's actual novel domain.

10.7 AuditEntry (Reporting & Audit context)
csharp
public class AuditEntry : Entity<AuditEntryId>
{
    public DateTimeOffset OccurredAt { get; private set; }
    public UserId ActorId { get; private set; }              // shared-kernel reference, Section 7.4
    public string ActionType { get; private set; }            // e.g., "FlowPublished", "RoleChanged"
    public string TargetType { get; private set; }            // e.g., "Flow", "User"
    public string TargetId { get; private set; }
    public IReadOnlyDictionary<string, string> Metadata { get; private set; }
    public TenantId TenantId { get; private set; }
}

Per NFR-SEC-3, AuditEntry records are append-only at the persistence layer (Part V) — this entity has no update methods at all, only a constructor, which is itself a structural (not just policy-level) enforcement of immutability.

10.8 Entity identity strategy

All entity identifiers (FlowStepId, StepResultId, UserId, etc.) are strongly-typed value-object wrappers around a Guid (not raw Guid or int primitives), following the standard DDD "strongly-typed ID" pattern. This is a deliberate, non-negotiable convention across the entire domain model, for one specific reason directly traceable to Architecture Principle 20.2: a method signature like void RecordStepResult(FlowRunId runId, StepResultId resultId) cannot have its arguments accidentally transposed at a call site the way void RecordStepResult(Guid runId, Guid resultId) could — this class of bug is eliminated at compile time, not caught in code review or, worse, in production. Volume 1 Part VI's Coding Standards section formalizes this as a mandatory pattern with a code-review checklist item.



11. Value Objects
11.1 Value object discipline

Every value object in this domain is immutable, has no identity (two instances with equal component values are interchangeable and compare equal), and validates its own invariants in its constructor — a value object that exists at all is, by construction, always valid. This section catalogs the value objects referenced throughout Sections 9 and 10.

11.2 ModuleKind
csharp
public readonly record struct ModuleKind
{
    public string Value { get; }
    private ModuleKind(string value) => Value = value;

    public static readonly ModuleKind Api = new("api");
    public static readonly ModuleKind Web = new("web");
    public static readonly ModuleKind Mobile = new("mobile");
    public static readonly ModuleKind DesktopUi = new("ui");
    public static readonly ModuleKind Logs = new("logs");
    public static readonly ModuleKind Ai = new("ai");

    public static ModuleKind Custom(string value)
    {
        // Marketplace-installed modules (Volume 0, Section 17) register additional
        // ModuleKind values at runtime — this factory validates format (lowercase,
        // no whitespace, per the same convention as the six built-in kinds) without
        // restricting the value to a fixed enum, which would directly contradict
        // FR-ORCH-6's "add a module without changing the core" requirement.
        if (string.IsNullOrWhiteSpace(value) || value != value.ToLowerInvariant())
            throw new DomainException($"Invalid ModuleKind: '{value}'");
        return new ModuleKind(value);
    }
}

This is a deliberate departure from a closed C# enum — an enum would hardcode the six built-in kinds at compile time and structurally prevent third-party modules from ever being first-class ModuleKind values, directly violating Architecture Principle 20.3 (every module, built-in or not, treated identically). The record struct with named static instances plus an open Custom factory gives built-in modules ergonomic, typo-proof references in code while keeping the type open for extension.

11.3 StepStatus and RunStatus
csharp
public enum StepStatus { Passed, Failed, Skipped }
public enum RunStatus { Pending, Running, Passed, Failed, Cancelled }

Unlike ModuleKind, these are closed enums — the set of possible outcomes for a step or run is a fixed, core piece of the orchestration model (FR-ORCH-1 through FR-ORCH-5) that no plugin should ever be able to extend, per Architecture Principle 20.3's requirement that reporting be uniform across every module. A module cannot invent a fourth StepStatus value; it can only ever report one of these three.

11.4 FlowStatus
csharp
public enum FlowStatus { Draft, Published, Archived }
11.5 AiAttribution
csharp
public sealed record AiAttribution
{
    public string Capability { get; init; }       // "selector-heal" | "flow-generation" | "log-triage"
    public double Confidence { get; init; }
    public string? OriginalValue { get; init; }     // e.g., the broken selector, for audit/review
    public string? SuggestedValue { get; init; }    // e.g., the healed selector
    public bool WasAccepted { get; init; }          // false if below-threshold and only advisory (UC-3)
    public string ModelIdentifier { get; init; }     // which provider/model produced this, for
                                                        // the evaluation-metrics tracking in
                                                        // Volume 0, Section 18.7
}

This value object is the direct, concrete implementation of FR-AI-3's requirement that every AI-assisted intervention be recorded distinguishably. Its presence (or deliberate absence) on a StepResult is itself meaningful — the type system enforces that a caller checking stepResult.AiAttribution is not null is a reliable, structurally-guaranteed way to detect AI involvement, not a convention that could be silently forgotten in one code path.

11.6 Assertion

Formalizing the assertion shapes already prototyped informally in this project's earlier exploration (status/equals/contains/exists):

csharp
public abstract record Assertion;
public sealed record StatusAssertion(int ExpectedStatus) : Assertion;
public sealed record PathEqualsAssertion(string Path, object? ExpectedValue) : Assertion;
public sealed record PathContainsAssertion(string Path, object ExpectedFragment) : Assertion;
public sealed record PathExistsAssertion(string Path) : Assertion;

Modeled as a closed hierarchy of record types (a discriminated union, C#-idiomatic via inheritance plus pattern matching) rather than a single class with nullable fields for each possible assertion kind — this is a direct application of "make illegal states unrepresentable," ensuring, for instance, that a PathEqualsAssertion can never be constructed without both a Path and an ExpectedValue, where a flat-fields design would allow a partially-specified, ambiguous assertion object to exist.

11.7 VariableExpression
csharp
public sealed record VariableExpression
{
    public string RawTemplate { get; init; }  // e.g., "{{vars.orgInfo.repos_url}}"

    public IReadOnlyList<string> ReferencedPaths { get; init; }
    // Parsed at construction time (not resolution time) so that a Flow can be
    // statically validated at publish time (Section 9.2) for referencing only
    // variables that some prior step in the same Flow actually declares via
    // saveAs — catching a whole class of "flow references a variable that will
    // never exist" errors before the Flow is ever run, per Architecture
    // Principle 20.4's fail-fast philosophy applied as early as possible in
    // the lifecycle, not just at execution time.
}
11.8 ActionSchema
csharp
public sealed record ActionSchema
{
    public string ActionName { get; init; }
    public IReadOnlyList<ParamSpec> Parameters { get; init; }
    public string? Description { get; init; }
}

public sealed record ParamSpec
{
    public string Name { get; init; }
    public ParamType Type { get; init; }        // String | Number | Boolean | Object | Array
    public bool Required { get; init; }
    public object? DefaultValue { get; init; }
}

This is the formalized version of the module-action schema referenced conceptually in Part I, Section 5.4 — it is the shared contract both the Flow Parser (validating a FlowStep's Params at publish time) and the Module Registration API (exposing available actions to the GUI, per FR-ORCH-7) depend on.

11.9 TrustTier
csharp
public enum TrustTier { BuiltIn, Verified, Community }

Directly maps to Volume 0's marketplace certification tiers (Section 17.3), now a first-class part of the runtime domain model rather than existing only as a marketplace-website concept — this is what makes an enterprise policy like "only install Verified-or-above modules" (Volume 0, Section 17.3's enterprise-tier scenario) enforceable at the ModuleRegistration aggregate level (Section 9.4), not just as marketplace UI friction.

11.10 Strongly-typed identifiers

Referenced throughout Section 10; formalized here as the value-object pattern they are:

csharp
public readonly record struct FlowId(Guid Value);
public readonly record struct FlowRunId(Guid Value);
public readonly record struct FlowStepId(Guid Value);
public readonly record struct StepResultId(Guid Value);
public readonly record struct ModuleRegistrationId(Guid Value);
public readonly record struct UserId(Guid Value);
public readonly record struct TenantId(Guid Value);
// ...and so on for every entity/aggregate identifier in Sections 9-10.

Per Section 10.8's rationale, every one of these is a distinct type, not a type alias — C#'s record struct gives value-object equality semantics and zero runtime allocation overhead relative to a raw Guid, making this pattern effectively free at runtime while providing a real compile-time safety guarantee.



12. Domain Events
12.1 Purpose and relationship to the Event Bus component

Domain events are the formalized payloads that flow through the Event Bus component introduced in Part I, Section 5.3, and are the concrete mechanism implementing the Published Language integration pattern between contexts (Section 7.3). Every domain event is immutable, past-tense named (an event describes something that already happened, never a command to make something happen — that distinction is Section 13's job), and carries only the data its known consumers (SignalR streaming, audit logging, report projection) actually need — not a full aggregate snapshot, to keep the Published Language contract minimal and stable per Architecture Principle 20.2.

12.2 Event catalog — Flow Authoring context
csharp
public sealed record FlowPublishedEvent(
    FlowId FlowId, FlowFamilyId FamilyId, int Version, TenantId TenantId, UserId PublishedBy, DateTimeOffset OccurredAt
) : IDomainEvent;

public sealed record FlowArchivedEvent(
    FlowId FlowId, TenantId TenantId, UserId ArchivedBy, DateTimeOffset OccurredAt
) : IDomainEvent;
12.3 Event catalog — Execution context
csharp
public sealed record FlowRunStartedEvent(
    FlowRunId RunId, FlowId FlowId, RunTrigger Trigger, TenantId TenantId, DateTimeOffset OccurredAt
) : IDomainEvent;

public sealed record StepCompletedEvent(
    FlowRunId RunId, StepResult Result, DateTimeOffset OccurredAt
) : IDomainEvent;
// This is the single highest-frequency event in the system (one per step, per run)
// and is the direct data source for SignalR's live progress streaming (Part V,
// Section 31) — its payload is deliberately the full StepResult, since the GUI
// needs the complete outcome to render immediately, not a partial summary
// requiring a follow-up query.

public sealed record StepSelfHealedEvent(
    FlowRunId RunId, StepResultId ResultId, AiAttribution Attribution, DateTimeOffset OccurredAt
) : IDomainEvent;
// Raised in addition to (never instead of) StepCompletedEvent whenever a step's
// AiAttribution is non-null, per FR-AI-3 — giving audit logging and any future
// AI-specific monitoring a dedicated event to subscribe to without needing to
// inspect every StepCompletedEvent's payload for an optional field.

public sealed record FlowRunCompletedEvent(
    FlowRunId RunId, RunStatus FinalStatus, int PassedCount, int FailedCount, int SkippedCount, TimeSpan TotalDuration, DateTimeOffset OccurredAt
) : IDomainEvent;

public sealed record FlowRunFlakeDetectedEvent(
    FlowRunId RunId, StepResultId ResultId, int RetryCount, DateTimeOffset OccurredAt
) : IDomainEvent;
// Raised specifically to feed Volume 0's platform flake-rate metric (NFR-REL-2)
// as a first-class, independently queryable event stream, rather than requiring
// that metric to be computed by scanning RetryHistory after the fact.
12.4 Event catalog — Module & Plugin context
csharp
public sealed record ModuleRegisteredEvent(
    ModuleRegistrationId RegistrationId, ModuleKind Kind, TrustTier Trust, DateTimeOffset OccurredAt
) : IDomainEvent;

public sealed record ModuleDeprecatedActionEvent(
    ModuleRegistrationId RegistrationId, string ActionName, string Notice, DateTimeOffset OccurredAt
) : IDomainEvent;
// Consumed by Flow Authoring (to warn flow authors whose published flows
// reference a soon-to-be-removed action) — a cross-context notification
// delivered via the Published Language mechanism (Section 7.3) rather than
// a direct query, keeping the two contexts decoupled even for this warning.
12.5 Event catalog — Identity & Access context
csharp
public sealed record UserRoleChangedEvent(
    UserId UserId, IReadOnlyList<RoleId> PreviousRoles, IReadOnlyList<RoleId> NewRoles, UserId ChangedBy, TenantId TenantId, DateTimeOffset OccurredAt
) : IDomainEvent;

public sealed record CredentialRotatedEvent(
    string CredentialReference, TenantId TenantId, UserId RotatedBy, DateTimeOffset OccurredAt
) : IDomainEvent;
// Deliberately never carries the credential value itself, per NFR-SEC-3 —
// only a reference/label, since this event is consumed by the audit pipeline
// (Section 12.6) which must never persist a secret value even transiently.
12.6 Event consumers — who subscribes to what, and why
Event Consumers   Purpose
StepCompletedEvent   SignalR Hub, Audit pipeline, Report projector   Live progress (GUI), immutable audit trail, FlowReport materialization
StepSelfHealedEvent  Audit pipeline, AI evaluation-metrics collector (Volume 0, Section 18.7)   Transparent AI-attribution record, self-heal acceptance-rate tracking
FlowRunCompletedEvent   SignalR Hub, Audit pipeline, Report projector, CI webhook notifier (if configured)  Final status delivery to every downstream consumer that cares about run completion
FlowRunFlakeDetectedEvent  Platform-health metrics collector   NFR-REL-2 flake-rate computation, independent of any single flow's reporting
FlowPublishedEvent   Audit pipeline, Module & Plugin context (to track which modules/actions are actively referenced, feeding the deprecation-safety check in Section 9.4) Audit trail, cross-context deprecation safety
ModuleDeprecatedActionEvent   Flow Authoring (author-facing warnings)   Proactive breaking-change notification
UserRoleChangedEvent, CredentialRotatedEvent Audit pipeline exclusively NFR-SEC-3 compliance
12.7 Event delivery guarantees

Nexus's domain events are delivered with at-least-once semantics within a single deployment (via the Event Bus component, Part I Section 5.3, backed by an in-process publish/subscribe mechanism for GA, with the explicit option to back it with a durable message broker for larger self-hosted deployments per NFR-SCALE-2 — this specific technology choice is a Part III, Section 22 Event Bus decision, not fixed here). Every consumer listed in 12.6 is therefore required to be idempotent with respect to duplicate event delivery (e.g., the Report projector must upsert, not blindly insert, when materializing a FlowReport from a StepCompletedEvent it may have already processed). This requirement is stated here, at the domain-event-catalog level, because it constrains every consumer's design — it is a contract obligation on anyone subscribing to these events, not an implementation detail internal to the Event Bus itself.

12.8 Why domain events, not direct method calls, for cross-context communication

This is a direct, deliberate application of the Published Language integration pattern chosen in Section 7.3: if the Execution context called Reporting & Audit's projection logic directly (a synchronous method call), the two contexts would be coupled at compile time, and a change to how reports are materialized could force a redeploy of the Execution context, or — worse — a slow report-projection operation could add latency to the step-dispatch hot path, directly threatening NFR-PERF-1's 50ms p95 orchestrator-overhead budget. Publishing an event and letting Reporting & Audit consume it asynchronously keeps the Execution context's step-dispatch loop free of any dependency on reporting-specific logic or performance characteristics — a direct, load-bearing consequence of Architecture Principle 20.3's "orchestrator core has no module- or consumer-specific branching" principle, now extended to cross-context consumers generally, not just modules specifically.




13. Commands
13.1 Commands versus domain events versus queries

A command is an intent to change state, issued imperatively (present-tense-named — PublishFlow, not FlowPublished), which may be accepted or rejected by the domain model's invariants (Section 9). A domain event (Section 12) is the record that a change already happened, issued only after a command succeeds. This section catalogs Nexus's commands, organized by bounded context, and each command handler's responsibility is stated precisely: validate authorization (delegating to Identity & Access, Part IV), load the relevant aggregate via its repository (Part III, Section 15), invoke the aggregate's behavior, persist via Unit of Work (Part III, Section 16), and let the aggregate's own domain-event-raising (already shown in Section 9's code sketches) handle event publication.

13.2 Command catalog — Flow Authoring context
csharp
public sealed record CreateFlowCommand(string Name, string? Description, TenantId TenantId, UserId AuthoredBy) : ICommand<FlowId>;

public sealed record AddFlowStepCommand(FlowId FlowId, string StepName, ModuleKind Module, string Action, IReadOnlyDictionary<string, object?> Params, string? SaveAs, string? When) : ICommand;

public sealed record PublishFlowCommand(FlowId FlowId, UserId PublishedBy) : ICommand<FlowPublishResult>;
// Returns a result object, not a bare success/failure boolean, because a
// rejected publish attempt (Section 9.2's invariant — unresolved module
// reference) needs to communicate *which* step and *which* module reference
// failed resolution, per Architecture Principle 20.4's "fail informatively."

public sealed record ArchiveFlowCommand(FlowId FlowId, UserId ArchivedBy) : ICommand;

public sealed record GenerateFlowFromDescriptionCommand(string Description, IReadOnlyList<ModuleKind> AllowedModules, TenantId TenantId, UserId RequestedBy) : ICommand<FlowGenerationResult>;
// This command deliberately produces a *Draft* Flow, never a Published one —
// directly enforcing Volume 0's permanent AI boundary (Section 18.4): the
// AI module's output always lands as a command a human still has to review
// and separately issue PublishFlowCommand against, never as a bypass around
// the normal command pipeline.
13.3 Command catalog — Execution context
csharp
public sealed record TriggerFlowRunCommand(FlowId FlowId, RunTrigger Trigger, IReadOnlyDictionary<string, object?>? VariableOverrides, TenantId TenantId) : ICommand<FlowRunId>;

public sealed record CancelFlowRunCommand(FlowRunId RunId, UserId CancelledBy) : ICommand;
// Not explicitly named as a functional requirement in Volume 0's FR list,
// but required by NFR-REL-1's availability commitment in practice — a
// customer must be able to abort a run consuming resources unexpectedly,
// and this is called out here specifically so Part III's Execution Engine
// design does not treat cancellation as an afterthought.

public sealed record RequestSelectorHealCommand(FlowRunId RunId, StepResultId FailedResultId, string BrokenSelector, string Intent, string CurrentPageHtml) : ICommand<SelectorHealResult>;
// Internal command, issued by the orchestration engine itself (not directly
// by an end user) when a Web-module step fails and self-healing is enabled
// for the flow — included in this catalog because it still passes through
// the same command-handling, authorization, and audit pipeline as any
// user-issued command, per Architecture Principle 20.5 (security/compliance
// posture designed in, not bolted on) applied even to internally-originated
// commands.
13.4 Command catalog — Module & Plugin context
csharp
public sealed record RegisterModuleCommand(ModuleKind Kind, string DisplayName, TrustTier Trust, IReadOnlyList<ActionSchema> Actions, SemanticVersion ContractVersion, UserId RegisteredBy) : ICommand<ModuleRegistrationId>;

public sealed record DeprecateModuleActionCommand(ModuleRegistrationId RegistrationId, string ActionName, string Notice, UserId RequestedBy) : ICommand;

public sealed record DeactivateModuleCommand(ModuleRegistrationId RegistrationId, UserId RequestedBy) : ICommand;
13.5 Command catalog — Identity & Access context
csharp
public sealed record AssignRoleCommand(UserId UserId, RoleId RoleId, UserId AssignedBy, TenantId TenantId) : ICommand;

public sealed record RevokeRoleCommand(UserId UserId, RoleId RoleId, UserId RevokedBy, TenantId TenantId) : ICommand;

public sealed record RotateCredentialCommand(string CredentialReference, TenantId TenantId, UserId RotatedBy) : ICommand;
// The command handler for this delegates the actual secret-value rotation
// to the secrets manager integration (Vault, per Volume 0 Section 21.13) —
// this command's own payload, per Section 12.5's corresponding event,
// never carries the credential value itself.
13.6 Command validation — two distinct layers

Every command handler enforces validation at two distinct, deliberately separated layers, and this separation is stated explicitly because conflating them is a common source of bugs:

Structural/authorization validation, performed by the command handler before the aggregate is even loaded: is the command well-formed (required fields present), and is the issuing UserId authorized (per Identity & Access, Part IV) to issue this command against this TenantId's data at all. A failure here never touches the domain model.
Domain invariant validation, performed by the aggregate itself once loaded (Section 9's invariants — e.g., Flow.Publish's module-resolution check). A failure here is a legitimate domain-level rejection (like FlowPublishResult in 13.2), not a system error.

This two-layer separation is what keeps aggregates (Section 9) free of any authorization logic — an aggregate should never need to know who is asking, only whether the requested state transition is valid given the current state, keeping the domain model's testability and portability (per Architecture Principle 20.7, composable primitives) fully intact independent of the authorization system's own evolution (Part IV).

13.7 Command handler pattern (illustrative)
csharp
public class PublishFlowCommandHandler : ICommandHandler<PublishFlowCommand, FlowPublishResult>
{
    private readonly IFlowRepository _flows;          // Part III, Section 15
    private readonly IModuleActionResolver _resolver;  // the port from Section 9.5
    private readonly IUnitOfWork _unitOfWork;          // Part III, Section 16
    private readonly IAuthorizationService _authz;      // Part IV, Section 25

    public async Task<FlowPublishResult> Handle(PublishFlowCommand cmd, CancellationToken ct)
    {
        await _authz.EnsureCanPublish(cmd.PublishedBy, cmd.FlowId, ct);  // layer 1, Section 13.6

        var flow = await _flows.GetById(cmd.FlowId, ct)
            ?? throw new NotFoundException(nameof(Flow), cmd.FlowId);

        var result = flow.Publish(_resolver);  // layer 2, Section 13.6 — domain invariant, in-aggregate

        if (result.Succeeded)
        {
            await _flows.Save(flow, ct);
            await _unitOfWork.Commit(ct);       // persists state + dispatches raised domain events together
        }

        return result;
    }
}
13.8 Why commands are explicit types rather than direct service-method calls

A conventional alternative would be plain service classes with methods (FlowService.PublishFlow(flowId, userId)) rather than an explicit ICommand/ICommandHandler pattern. Nexus adopts the explicit-command pattern for three reasons directly traceable to earlier decisions: first, it gives every state-changing operation a uniform shape that the audit pipeline (Section 12.6) and the CQRS separation (Part III, Section 23) can both handle generically rather than needing bespoke logging/dispatch code per service method. Second, it makes the full inventory of "everything that can change state in this system" enumerable and reviewable as a flat catalog (this section, in its entirety) — directly supporting the security-review use case (Volume 0, UC-7) where Persona Elena benefits from a complete, auditable list of possible write operations rather than having to read through service-class implementations to discover them. Third, it composes cleanly with Volume 0's AI-generation boundary (Section 18.4) — GenerateFlowFromDescriptionCommand in 13.2 is, structurally, just another command in the same catalog, subject to the same authorization and audit treatment as any human-issued command, rather than a special-cased AI code path.



14. Queries
14.1 Queries as a distinct concept from commands

Where Section 13's commands express intent to change state, queries express intent to read state, and — per the CQRS approach this domain model anticipates (fully justified in Part III, Section 23) — queries in Nexus are free to read from denormalized, purpose-built read models rather than always reconstructing an answer from the aggregates in Section 9. This section catalogs the query surface, organized by the use case it primarily serves (tying directly back to Volume 0's Use Cases, Section 11, and Personas, Section 10) rather than strictly by bounded context, since queries — unlike commands — frequently need to read across context boundaries to answer a single question a real user actually has.

14.2 Query catalog — Flow Authoring & GUI support
csharp
public sealed record GetFlowByIdQuery(FlowId FlowId, TenantId TenantId) : IQuery<FlowDetailDto>;

public sealed record ListFlowsQuery(TenantId TenantId, FlowStatus? StatusFilter, string? SearchTerm, PageRequest Page) : IQuery<PagedResult<FlowSummaryDto>>;

public sealed record GetAvailableModuleActionsQuery(TenantId TenantId) : IQuery<IReadOnlyList<ModuleActionDto>>;
// The direct, formal query behind FR-ORCH-7's introspectable module-action
// API, and the specific query the GUI's flow-authoring surface calls to
// auto-populate available steps — this is the query-side implementation of
// Volume 0's UC-6 requirement that a newly-registered custom module's
// actions "automatically" appear in the authoring UI with no custom
// frontend work, since the GUI's rendering logic is entirely driven by
// whatever this query currently returns.

public sealed record GetFlowVersionHistoryQuery(FlowFamilyId FamilyId, TenantId TenantId) : IQuery<IReadOnlyList<FlowVersionSummaryDto>>;
14.3 Query catalog — Execution & live progress
csharp
public sealed record GetFlowRunByIdQuery(FlowRunId RunId, TenantId TenantId) : IQuery<FlowRunDetailDto>;

public sealed record ListRecentFlowRunsQuery(TenantId TenantId, FlowId? FlowIdFilter, RunStatus? StatusFilter, PageRequest Page) : IQuery<PagedResult<FlowRunSummaryDto>>;

public sealed record GetLiveRunProgressQuery(FlowRunId RunId, TenantId TenantId) : IQuery<RunProgressDto>;
// Used for the initial state fetch when a client subscribes to a SignalR
// hub (Part V, Section 31) for a run already in progress — SignalR then
// takes over for incremental updates via StepCompletedEvent (Section 12),
// so this query's role is specifically the "catch up to current state"
// case, not the ongoing streaming mechanism itself.
14.4 Query catalog — Reporting & Audit (Persona Sofia's primary surface)
csharp
public sealed record GetFlowReportQuery(FlowRunId RunId, TenantId TenantId) : IQuery<FlowReportDto>;
// Direct implementation of FR-REP-1 — returns the unified pass/fail/skip
// report for a single run.

public sealed record GetPassRateTrendQuery(TenantId TenantId, ModuleKind? ModuleFilter, DateRange Range) : IQuery<TrendSeriesDto>;
// Direct implementation of FR-REP-3/FR-REP-4 and Volume 0's UC-5 — this is
// the query backing Sofia's monthly-review dashboard trend view, required
// by NFR-PERF-3 to render in under 2 seconds at p95 for up to 10,000
// historical runs, which is the direct justification (Part III, Section 23)
// for this query reading from a materialized, pre-aggregated read model
// rather than computing trend statistics live from FlowRun aggregates on
// every request.

public sealed record GetFlakeRateQuery(TenantId TenantId, DateRange Range) : IQuery<FlakeRateDto>;
// Backs Volume 0's platform-health flake-rate metric (NFR-REL-2), sourced
// from FlowRunFlakeDetectedEvent projections (Section 12.3/12.6), not
// computed by re-scanning every FlowRun's RetryHistory per request.

public sealed record GetSelfHealAcceptanceRateQuery(TenantId TenantId, DateRange Range) : IQuery<SelfHealMetricsDto>;
// Backs Volume 0 Section 18.7's self-heal acceptance-rate quality metric.

public sealed record DrillDownFailureSignatureQuery(TenantId TenantId, ModuleKind Module, DateRange Range) : IQuery<IReadOnlyList<FailureClusterDto>>;
// Direct implementation of the drill-down capability in Volume 0's UC-5
// step 3 ("the dashboard correlates it with a specific flow and a specific
// underlying failure signature") — groups failed StepResults by normalized
// error-message signature so Sofia can go from "pass rate dropped" to
// "here's the one recurring failure causing it" in a single query.

public sealed record ExportAuditLogQuery(TenantId TenantId, DateRange Range, string? ActionTypeFilter) : IQuery<Stream>;
// Serves Persona Elena's compliance-review needs (NFR-SEC-3) — returns a
// streamed export rather than an in-memory collection, since audit exports
// can span the full 1-year minimum retention window (NFR-SEC-3) and should
// not risk memory pressure on the API Layer container.
14.5 Query catalog — Module & Plugin (Persona Raj's support)
csharp
public sealed record GetModuleRegistrationQuery(ModuleRegistrationId RegistrationId, TenantId TenantId) : IQuery<ModuleRegistrationDetailDto>;

public sealed record ListRegisteredModulesQuery(TenantId TenantId, TrustTier? TrustFilter) : IQuery<IReadOnlyList<ModuleRegistrationSummaryDto>>;
// The TrustFilter parameter directly supports the enterprise policy
// scenario from Volume 0 Section 17.3 — "restrict this org's instance to
// Verified-or-above modules only" is implementable as this query filtered
// at the API layer by an org-level policy setting, composed with this
// existing query rather than requiring a bespoke policy-aware query type.
14.6 Query catalog — Identity & Access
csharp
public sealed record GetUserPermissionsQuery(UserId UserId, TenantId TenantId) : IQuery<IReadOnlyList<Permission>>;

public sealed record ListUsersQuery(TenantId TenantId, PageRequest Page) : IQuery<PagedResult<UserSummaryDto>>;
14.7 Query handler pattern and read-model sourcing

Unlike command handlers (Section 13.7), which always load a full aggregate via its repository to enforce invariants, query handlers are explicitly permitted — and, for the reporting-heavy queries in 14.4, expected — to read from purpose-built, denormalized read models (projected asynchronously from domain events, per Section 12.6's Report projector) rather than reconstructing an answer from aggregate state on every call. This distinction is the practical essence of the CQRS pattern this domain model is designed to support, fully justified and detailed in Part III, Section 23; this section's role is only to establish that the query catalog itself already anticipates and is structured for that separation, rather than CQRS being retrofitted onto an initially command-query-symmetric design.

14.8 DTOs are a deliberate, separate concern from domain entities

Every query in this catalog returns a DTO type (FlowDetailDto, FlowRunSummaryDto, etc.), never a domain entity or aggregate directly. This is a firm rule, not a convention with exceptions: domain entities (Section 10) carry behavior and invariant-enforcement methods that have no business being exposed across the API boundary (Part V), and DTOs are free to shape data specifically for their consuming use case (e.g., FlowRunSummaryDto for a list view carries far less data than FlowRunDetailDto for a single-run deep-dive) in a way that would be awkward or invariant-violating to achieve by serializing domain entities directly. Mapping between domain entities/read-models and DTOs is the query handler's explicit responsibility, kept as a thin, well-tested mapping layer rather than mixed into either the domain model or the API controllers (Part V, Section 30).

___________________________________________________________________________________________________________________________________________________



15. Repository Pattern
15.1 Purpose within Nexus's architecture

The repository pattern is the persistence-abstraction boundary sitting directly beneath each of Part II's three aggregate roots (Flow, FlowRun, ModuleRegistration). Its job is narrow and specific: load an aggregate to full consistency, and persist an aggregate's changes atomically — nothing else. Repositories in Nexus are deliberately not generic data-access utilities; there is no IRepository<T> that every entity gets for free. Each aggregate root gets exactly one purpose-built repository interface, because a generic repository abstraction tends to leak query concerns (Part II, Section 14's queries) back into what should be a strictly write-side, consistency-boundary-respecting contract.

15.2 One repository per aggregate root — no exceptions
csharp
public interface IFlowRepository
{
    Task<Flow?> GetById(FlowId id, CancellationToken ct);
    Task<Flow?> GetLatestVersionByFamily(FlowFamilyId familyId, CancellationToken ct);
    Task Save(Flow flow, CancellationToken ct);
}

public interface IFlowRunRepository
{
    Task<FlowRun?> GetById(FlowRunId id, CancellationToken ct);
    Task Save(FlowRun run, CancellationToken ct);
}

public interface IModuleRegistrationRepository
{
    Task<ModuleRegistration?> GetById(ModuleRegistrationId id, CancellationToken ct);
    Task<ModuleRegistration?> GetByKind(ModuleKind kind, TenantId tenantId, CancellationToken ct);
    Task Save(ModuleRegistration registration, CancellationToken ct);
}

Note what is deliberately absent: no GetAll(), no Find(predicate), no pagination parameters. Per Section 14.7 of Part II, anything resembling a list, search, or filter operation is a query, served by a separate read-model-backed query handler, never by the repository. A repository that grows a Find(Expression<Func<Flow, bool>> predicate) method is a repository that has started leaking query concerns back into the write side, and should be treated as a design defect during code review, not a convenience.

15.3 Why not a generic IRepository<TAggregate, TId>

A generic repository interface is a common pattern in less domain-rich systems, and was deliberately rejected here for a reason directly traceable to Section 9.5 of Part II: each aggregate's Save operation has different consistency requirements. Flow.Save must never overwrite a Published version's steps (Section 9.2's immutability invariant) — this is naturally expressed as a specific implementation behind IFlowRepository.Save, not as a generic method that would need runtime type-checking to enforce an aggregate-specific rule. A generic interface would either force these aggregate-specific behaviors into the aggregates themselves (violating persistence-ignorance, discussed in 15.5) or into a bloated generic implementation with special-casing per aggregate type — both worse than three small, purpose-specific interfaces.

15.4 Repository implementation — illustrative sketch
csharp
public class FlowRepository : IFlowRepository
{
    private readonly NexusDbContext _db;  // EF Core DbContext, Part V, Section 32

    public async Task<Flow?> GetById(FlowId id, CancellationToken ct)
    {
        // Loads the Flow aggregate root together with its FlowStep child
        // entities in one round-trip (Include/ThenInclude, or a compiled
        // projection — the specific EF Core mechanism is an implementation
        // detail per Section 6.5 of Part II, not fixed at this level), since
        // an aggregate must always be loaded to full consistency, never
        // partially, per standard DDD repository discipline.
        return await _db.Flows
            .Include(f => f.Steps)
            .FirstOrDefaultAsync(f => f.Id == id.Value, ct)
            is { } entity ? FlowMapper.ToDomain(entity) : null;
    }

    public async Task Save(Flow flow, CancellationToken ct)
    {
        var entity = FlowMapper.ToPersistence(flow);
        _db.Flows.Update(entity);
        // Deliberately does NOT call SaveChangesAsync here — see Section 16
        // (Unit of Work). A repository's Save stages the change; committing
        // it (together with any other aggregate changes and domain event
        // dispatch in the same logical operation) is the Unit of Work's job.
    }
}
15.5 Persistence ignorance — domain entities never reference EF Core directly

The Flow, FlowStep, FlowRun, and ModuleRegistration types defined in Part II carry no Entity Framework attributes, no navigation-property conventions bending their shape to the ORM's preferences, and no dependency on NexusDbContext whatsoever. A separate, explicit mapping layer (FlowMapper in 15.4's sketch) translates between the domain model's own shape and a set of EF Core-specific persistence entities. This is a deliberate cost (an extra mapping layer to write and maintain) accepted for a specific reason: it keeps Part II's domain model — the most carefully-designed, invariant-rich part of this entire volume — fully unit-testable with zero database dependency, and fully insulated from any future decision to change persistence technology (a decision this blueprint does not anticipate needing, but Architecture Principle 20.2's contract-stability discipline suggests protecting against regardless).

15.6 Repository scope and tenant isolation

Every repository method that accepts a TenantId (directly, or implicitly via an aggregate that carries one, per Part II Section 7.4's shared-kernel design) must filter at the query level, never at the application-service level after the fact. This is stated here as a hard rule specifically because it is the concrete implementation mechanism for Part IV's multi-tenancy isolation guarantee (Section 26) — a repository that fetched a Flow without a tenant filter and relied on a calling service to check flow.TenantId == currentTenant afterward would be one missed check away from a cross-tenant data leak. Tenant filtering belongs in the repository's query itself, enforced structurally, not as an optional afterthought layered on top.

15.7 Repositories and the Module & Plugin trust boundary

IModuleRegistrationRepository has one property the other two repositories don't: its Save method must additionally enforce the TrustTier immutability invariant from Part II, Section 9.4 (a re-certification creates a new version rather than mutating trust tier in place). This is called out explicitly because it's a good illustration of Section 15.3's core argument — this is exactly the kind of aggregate-specific persistence rule that a generic repository would handle badly.

15.8 Relationship to Part V's database design

This section specifies the repository contracts (interfaces) as part of the domain/application layer's design — deliberately independent of the concrete schema. Part V, Section 32 (Database Design) specifies the actual PostgreSQL schema, table structure, and indexing strategy that FlowMapper and its siblings translate to and from. Reading this section does not require knowledge of the schema; reading the schema in Part V should make sense entirely in terms of "what does this table need to hold to satisfy the repository contracts and mappers defined here."



16. Unit of Work
16.1 Purpose

Section 15 established that a repository's Save method stages a change without committing it. The Unit of Work pattern is what actually commits — and, critically in Nexus's design, what atomically couples two things that must never happen independently: persisting an aggregate's state change, and dispatching the domain events that aggregate raised as a result of that change (Part II, Section 12). This coupling is the single most important property of Nexus's Unit of Work design, and the rest of this section explains why it's non-negotiable.

16.2 The interface
csharp
public interface IUnitOfWork
{
    Task Commit(CancellationToken ct);
}

Deliberately minimal — a single method. Nexus does not expose BeginTransaction/Rollback as separate public methods on this interface; transactional scope is managed internally by the implementation (16.3), because exposing manual transaction control to application-service code (Part II, Section 13.7's command handlers) would invite exactly the kind of ad hoc, inconsistent transaction-boundary decisions this pattern exists to prevent.

16.3 Implementation — commit and event dispatch as one atomic operation
csharp
public class UnitOfWork : IUnitOfWork
{
    private readonly NexusDbContext _db;
    private readonly IDomainEventDispatcher _dispatcher;  // publishes to the Event Bus, Section 22

    public async Task Commit(CancellationToken ct)
    {
        // 1. Collect all domain events raised by any aggregate tracked in
        //    this DbContext's change tracker during the current unit of
        //    work (every aggregate's RaiseDomainEvent calls, per Part II
        //    Section 9's code sketches, append to an internal buffer that
        //    the DbContext's change tracker exposes here).
        var pendingEvents = _db.ChangeTracker.Entries<IHasDomainEvents>()
            .SelectMany(e => e.Entity.DomainEvents)
            .ToList();

        // 2. Persist the state change and record the events transactionally
        //    in the SAME database transaction — events are written to an
        //    outbox table (Section 16.4) as part of this SaveChangesAsync
        //    call, not published directly to the Event Bus yet.
        using var transaction = await _db.Database.BeginTransactionAsync(ct);
        await _db.SaveChangesAsync(ct);
        await OutboxWriter.WritePendingEvents(_db, pendingEvents, ct);
        await transaction.CommitAsync(ct);

        // 3. Only after the transaction has successfully committed does
        //    dispatch to the in-process Event Bus occur — a separate,
        //    best-effort background process also relays any outbox
        //    entries the in-process dispatch might have missed (e.g., due
        //    to a crash between steps 2 and 3), per Section 16.4.
        foreach (var evt in pendingEvents)
            await _dispatcher.Dispatch(evt, ct);
    }
}
16.4 Why an outbox, not direct event publication

Step 2's outbox write is the specific mechanism that prevents the classic "dual-write problem": without it, a crash between saving the Flow state change and publishing its FlowPublishedEvent would leave the database correctly updated but the Reporting & Audit context (Part II, Section 12.6) never informed — a silent, hard-to-detect inconsistency directly contradicting Architecture Principle 20.4 (fail loud, fail informatively). By writing events to an outbox table in the same transaction as the state change (step 2), the two either both happen or neither does — genuine atomicity, not just "usually happens together in practice." A background outbox-relay process (mentioned in step 3's comment) then guarantees at-least-once delivery to the Event Bus even across a process crash, which is exactly the delivery guarantee Part II, Section 12.7 already commits every event consumer to handling idempotently.

16.5 Unit of Work scope — one per command, never shared across commands

Each command handler (Part II, Section 13.7) receives its own IUnitOfWork instance, scoped to the lifetime of handling that single command — never shared across multiple commands, and never held open across an await boundary that crosses into unrelated work (e.g., never held open during an outbound AI-provider call, which per NFR-PERF-4 might take up to 15 seconds; a database transaction held open that long would directly threaten NFR-SCALE-1's concurrent-execution target by holding connection-pool resources unnecessarily). Where a command handler needs an AI-provider result before it can finalize a state change (e.g., RequestSelectorHealCommand, Part II Section 13.3), the pattern is: call the AI provider first, entirely outside any transaction, then open the Unit of Work only once with the result already in hand, ready to commit quickly.

16.6 Multi-aggregate commands and Unit of Work boundaries

Most commands in Part II's catalog (Section 13) touch exactly one aggregate, which keeps Unit of Work scoping straightforward. A small number are cross-aggregate by nature — PublishFlowCommand's invariant check against ModuleRegistration (Part II, Section 9.5) is a read, not a write, so it doesn't complicate this section's concern, but a hypothetical future command that needed to atomically update both a Flow and a ModuleRegistration would require either: (a) a single Unit of Work spanning both repositories' Save calls within one transaction (acceptable for same-database aggregates, which all three of Nexus's aggregates currently are), or (b) if the aggregates ever moved to separate physical databases (not currently planned, but worth stating the fallback), a saga/process-manager pattern coordinating two separate Units of Work with compensating actions. Nexus's GA architecture relies exclusively on (a), and this fallback is documented here only so a future scaling decision doesn't have to rediscover this reasoning from scratch.

16.7 Testing implications

Because IUnitOfWork is an interface, command-handler unit tests (per Volume 5's testing standards, forward-referenced here) substitute a test double that captures committed events without touching a real database — this is a direct, practical payoff of Section 15.5's persistence-ignorance discipline extended to the Unit of Work layer: a command handler's business-logic correctness (did it call flow.Publish() and check the result correctly) is fully testable independent of whether the outbox/transaction machinery in 16.3 works correctly, which gets its own, separate integration-test coverage against a real database.



17. Dependency Injection
17.1 Purpose and scope of this section

Dependency injection in Nexus uses ASP.NET Core's built-in DI container throughout — no third-party container is introduced, since the built-in container is sufficient for every pattern this volume specifies and avoids an unnecessary dependency. This section is not a general DI tutorial; it specifies Nexus-specific conventions: service lifetime choices for each category of component defined in Sections 15–16 and Part II, and — most importantly — the specific DI design that makes the plugin architecture's dynamic module loading (Section 18) work correctly.

17.2 Lifetime conventions
Component category   Lifetime Rationale
Repositories (IFlowRepository, etc., Section 15)   Scoped   One instance per request/command-handling scope, sharing the same DbContext instance within that scope for consistent change tracking
IUnitOfWork Scoped   Must share the same DbContext instance as the repositories used within the same command, per Section 16.5's "one per command" rule
Command/query handlers  Scoped   Resolved fresh per command/query dispatch, consistent with the repositories they depend on
AutomationModule implementations (built-in)  Singleton, with explicit statelessness requirement (17.5)   Avoids per-execution allocation overhead for the six built-in modules, which are invoked at very high frequency
AutomationModule implementations (marketplace/sandboxed) Transient, scoped to a sandbox process lifetime (Section 18)   Distinct lifetime model entirely, since these run in an isolated process boundary, not the DI container's normal object graph
Domain event handlers/consumers (Section 22) Scoped or Singleton, per-consumer (see 17.6) Depends on whether the specific consumer needs per-request state (e.g., the audit pipeline, scoped) or is a long-lived background subscriber (e.g., a metrics aggregator, singleton)
NexusDbContext Scoped   Standard EF Core convention; one context per request/scope
17.3 Why the built-in DI container, not a third-party one

This is a small but explicit decision: third-party containers (Autofac, and others popular in the .NET ecosystem) offer richer features (e.g., more flexible property injection, assembly-scanning conventions) that Nexus's design does not need — every pattern in this volume is expressible with constructor injection and the built-in container's lifetime model. Adding a third-party container would be a dependency with no corresponding architectural benefit, directly contradicting Architecture Principle 20.7 (composable primitives over unnecessary complexity, applied here to the dependency graph itself).

17.4 Module registration at startup

Built-in modules are registered explicitly and statically at application startup:

csharp
services.AddSingleton<IAutomationModule, ApiModule>();
services.AddSingleton<IAutomationModule, WebModule>();
services.AddSingleton<IAutomationModule, LogsModule>();
services.AddSingleton<IAutomationModule, MobileModule>();
services.AddSingleton<IAutomationModule, DesktopUiModule>();
services.AddSingleton<IAutomationModule, AiModule>();

// Resolved as a collection by the Module Registry component (Part I, Section 5.3)
// at startup, which builds its in-memory ModuleKind -> IAutomationModule map from
// this registered set, plus dynamically-loaded marketplace modules (Section 18).
services.AddSingleton<IModuleRegistry, ModuleRegistry>();

This static registration is deliberate and appropriate specifically for built-in modules, since their set is fixed at deployment time (Volume 0's module maturity table, Section 14.10) — dynamic, runtime module loading is reserved specifically for marketplace-installed modules, where it's actually needed (Section 18.3).

17.5 Statelessness requirement for singleton-lifetime modules

Because built-in modules are registered as singletons (17.2) and Nexus targets 500 concurrent flow executions (NFR-SCALE-1), every built-in AutomationModule implementation must be safe for concurrent invocation across many simultaneous flow runs — no mutable instance fields that aren't themselves thread-safe. Per-execution state (a Playwright browser instance for the Web module, an Appium session for the Mobile module) is created and disposed within the scope of a single run() call or a single flow's setup()/teardown() lifecycle (per the AutomationModule contract's existing setup/teardown hooks, already sketched conceptually in this project's earlier exploration), never held as instance state on the singleton module object itself. This requirement is stated here, in the DI section, because it is fundamentally a consequence of the lifetime choice made in 17.2, not an independent coding-standard rule — Part VI's Coding Standards will reference this section rather than re-deriving it.

17.6 Domain event consumer registration and the outbox relay

Referenced in Section 16.4's outbox mechanism: the background outbox-relay process and the various domain-event consumers (Part II, Section 12.6 — SignalR Hub, audit pipeline, report projector, platform-health metrics collector) are registered as hosted services (IHostedService) with singleton lifetime for the relay/subscription infrastructure itself, but each individual event-handling operation creates its own scoped IServiceScope to resolve any scoped dependencies (like a fresh DbContext for the audit pipeline's write) — this is the standard, necessary pattern for background services that need to interact with scoped-lifetime infrastructure, called out explicitly here because getting it wrong (e.g., resolving a scoped DbContext directly into a singleton service's constructor) is a common and serious DI misuse that would cause subtle concurrency bugs given NFR-SCALE-1's concurrency target.

17.7 Configuration and options binding

Environment-specific configuration (database connection strings, Redis connection, LLM provider settings per Volume 0 Section 21.7's provider-abstraction requirement, secrets-manager endpoint) is bound via the standard ASP.NET Core IOptions<T> pattern, never read directly from IConfiguration inside business-logic components — this keeps every component's configuration dependency explicit and strongly-typed (echoing Section 10.8 of Part II's strongly-typed-identifier rationale, now applied to configuration), and makes the AI-optionality architecture (NFR-REL-3) concretely testable: a test can construct an AiModuleOptions with Enabled = false and verify the orchestration layer's behavior without needing to manipulate ambient configuration state.

17.8 Testability implications

Every dependency in this section's tables is injected via interface, never resolved via a static service locator or ambient context — this is the DI-layer expression of the persistence-ignorance and command-handler-testability goals already stated in Sections 15.5 and 16.7. A command handler's unit tests construct it directly with mocked/faked dependencies; DI container wiring itself is verified separately via a small set of "does the application start and can every registered service be resolved" smoke tests, per Volume 5's forthcoming CI quality-gate design.



18. Plugin Architecture
18.1 Why this section carries the most weight in Volume 1

Every prior section in this volume has, at some point, deferred a detail to "the Plugin Architecture section." That is not incidental — per Architecture Principle 20.2 (contract stability over implementation convenience) and the repeated emphasis across Volume 0 (Business Objective 4, the Module Parity Index metric, UC-6, RISK-08, RISK-12), the AutomationModule contract this section formalizes is the single most consequential interface in the entire system. Everything else in this volume is, in a real sense, built to serve this contract's stability and genericity.

18.2 The formal AutomationModule contract

Building directly on the conceptual AutomationModule interface prototyped in this project's earlier exploration phase, now formalized to production-grade precision:

csharp
public interface IAutomationModule
{
    ModuleKind Kind { get; }
    SemanticVersion ContractVersion { get; }  // the AutomationModule interface version
                                                 // this implementation targets — see 18.7

    IReadOnlyList<ActionSchema> GetSupportedActions();  // backs FR-ORCH-7's introspection

    Task Setup(ExecutionContext ctx, CancellationToken ct);   // optional lifecycle hook
    Task<StepOutcome> Run(string action, IReadOnlyDictionary<string, object?> parameters, ExecutionContext ctx, CancellationToken ct);
    Task Teardown(ExecutionContext ctx, CancellationToken ct); // optional lifecycle hook
}

public sealed record StepOutcome
{
    public StepStatus Status { get; init; }
    public string? Message { get; init; }
    public string? Error { get; init; }
    public object? Data { get; init; }
    public AiAttribution? AiAttribution { get; init; }  // Part II, Section 11.5
}

This is deliberately close to a direct C# translation of the AutomationModule interface already validated conceptually across API, Logs, Web, Mobile, UI, and AI modules earlier in this project — the formalization here adds strong typing, the explicit ContractVersion field (18.7), and the ExecutionContext parameter (18.3), but does not change the shape's fundamental simplicity, which is itself a design goal: Persona Raj (Volume 0, Section 10.5) must be able to read this interface and understand exactly what's required within minutes.

18.3 ExecutionContext — what a module is given, and what it is not given
csharp
public sealed class ExecutionContext
{
    public FlowRunId RunId { get; init; }
    public IReadOnlyDictionary<string, object?> Variables { get; init; }  // read-only view of Part II's Variable Store
    public ICredentialResolver Credentials { get; init; }  // scoped, least-privilege secret access — 18.4
    public ILogger ModuleLogger { get; init; }              // structured logging, Part IV Section 28
    public CancellationToken RunCancellation { get; init; }  // honors CancelFlowRunCommand, Part II Section 13.3
}

Deliberately absent from ExecutionContext: any reference to the Flow or FlowRun aggregates themselves, any repository interface, any ability to issue commands or queries against the rest of the system. A module receives exactly the narrow slice of context it needs to do its job and report a result — nothing that would let it reach back into the orchestration layer's internals. This is the direct architectural enforcement of Architecture Principle 20.3 (the orchestrator has no module-specific logic) applied in the reverse direction: modules have no orchestrator-specific access either. The boundary is symmetric.

18.4 Credential resolution — least privilege by design

ICredentialResolver is scoped per flow-run and per module, resolving only the specific named credentials a given FlowStep's parameters reference (e.g., {{secrets.paymentApiKey}}) — a module cannot enumerate or access any credential beyond what the flow author explicitly wired into that step's parameters. This directly implements NFR-SEC-2 and NFR-SEC-3 at the plugin-contract level: even a malicious or buggy third-party module cannot exfiltrate credentials it was never handed, because the resolver's implementation (backed by the Vault integration, Volume 0 Section 21.13) simply has no broader scope to grant.

18.5 The trust-tiered execution model

Referencing the trust boundary established in Part I, Section 2.5, and the TrustTier value object (Part II, Section 11.9), modules execute under one of two distinct runtime models:

In-process execution (Built-in and Verified-tier modules): the module assembly is loaded directly into the Orchestration Engine's process, invoked via a normal virtual method call through IAutomationModule. This is the low-overhead path, appropriate for modules that have passed the Verified-tier manual security and code-quality review (Volume 0, Section 17.3).

Sandboxed execution (Community-tier modules): the module runs in an isolated child process (or, for stronger isolation on supported platforms, a lightweight container/gVisor-style sandbox — the specific isolation technology is a deployment/infrastructure decision deferred to Volume 4, not fixed here), communicating with the Orchestration Engine via a gRPC-based RPC boundary that mirrors the IAutomationModule interface's shape exactly. Resource limits (CPU, memory, network egress policy — directly implementing Volume 0 Section 17.4's sandboxing requirement) are enforced at this process boundary. The ExecutionContext's ICredentialResolver and ILogger are proxied across this RPC boundary rather than passed as in-process object references, ensuring a sandboxed module cannot access anything beyond what the proxy explicitly exposes.

csharp
public interface IModuleExecutionStrategy
{
    Task<StepOutcome> Execute(IAutomationModule module, string action, IReadOnlyDictionary<string, object?> parameters, ExecutionContext ctx, CancellationToken ct);
}

public class InProcessExecutionStrategy : IModuleExecutionStrategy { /* direct call */ }
public class SandboxedExecutionStrategy : IModuleExecutionStrategy { /* RPC to isolated process */ }

The Module Dispatcher (Part I, Section 5.3) selects the strategy based on the resolved ModuleRegistration.Trust value (Part II, Section 9.4) — this selection logic is the only place in the entire orchestration layer that branches on trust tier, kept deliberately isolated so that Architecture Principle 20.3's "no module-specific branching in the core" is honored everywhere else.

18.6 GUI auto-surfacing — how GetSupportedActions() powers Volume 0's UC-6

The Module Registration API (Part I, Section 5.3) calls GetSupportedActions() on every registered module at startup (and on-demand when a marketplace module is newly installed) and persists the returned ActionSchema list into the ModuleRegistration aggregate (Part II, Section 9.4). The GetAvailableModuleActionsQuery (Part II, Section 14.2) then serves this data to the GUI. This is the complete, concrete mechanism by which Volume 0's UC-6 claim — "the flow-authoring GUI, without any custom frontend work from Raj, automatically surfaces his new module's actions" — is actually true rather than aspirational: the GUI's flow-step-editor component (Volume 2's responsibility) renders purely from this query's response shape, with zero hardcoded knowledge of any specific module.

18.7 Contract versioning

ContractVersion (18.2) follows semantic versioning against the IAutomationModule interface's own version, not the module's own version. A breaking change to the interface (e.g., adding a new required method) increments the interface's major version; the Module Registry (Part I, Section 5.3) refuses to register a module declaring an incompatible major version at startup, with a clear, structured error — a direct implementation of Architecture Principle 20.2 and the versioning policy Volume 0's RISK-12 flagged as required before the Phase 4 external-plugin milestone (Volume 0, Section 15.6). Minor/patch interface changes (new optional methods with default implementations, new optional ExecutionContext fields) are additive and never break existing modules — this additive-only discipline for non-major versions is a binding constraint on how this interface itself is allowed to evolve, not just a versioning label convention.

18.8 Marketplace module installation flow
An administrator (or, for Community-tier, potentially any Flow Author per tenant policy) selects a module from the marketplace registry (Volume 0, Section 17.4).
The platform downloads the module package, verifies its signature and ContractVersion compatibility (18.7).
A RegisterModuleCommand (Part II, Section 13.4) is issued, creating the ModuleRegistration aggregate with the appropriate TrustTier (18.5) already determined by the marketplace's certification metadata, not re-derived locally.
The module is loaded — in-process for Verified-tier, into a new sandboxed process for Community-tier (18.5) — and becomes immediately available for use in new flow steps, with zero orchestrator or GUI code changes, per NFR-MAINT-1.
18.9 What this section guarantees, restated plainly

A correctly-implemented IAutomationModule — whether one of the six built-in modules or a third party's custom module — is executed identically by the Flow Engine, reported on identically in the unified FlowReport, and surfaced identically in the GUI's flow-authoring interface. This is not an aspiration restated from Volume 0; it is now a concrete, auditable claim this section's design makes true by construction — the Module Parity Index (Volume 0, Section 6.3) is, at this point in the document set, a testable property of the code, not just a product goal.



19. Execution Engine
19.1 Purpose — formalizing the Flow Engine component

Part I, Section 5 introduced the Flow Engine as a component of the Orchestration Engine container, with dependencies on the Variable Store and Retry/Flake Handler. This section specifies its internal design fully: the step-execution loop, how it composes with the Plugin Architecture's execution strategies (Section 18.5), and how it satisfies FR-ORCH-1 through FR-ORCH-5 concretely.

19.2 The core execution loop
csharp
public class FlowEngine
{
    private readonly IModuleDispatcher _dispatcher;
    private readonly IVariableStore _variables;
    private readonly IRetryFlakeHandler _retryHandler;
    private readonly IUnitOfWork _unitOfWork;

    public async Task<FlowRun> Execute(Flow flow, FlowRun run, CancellationToken ct)
    {
        foreach (var step in flow.Steps.OrderBy(s => s.Order))
        {
            ct.ThrowIfCancellationRequested();  // honors CancelFlowRunCommand promptly between steps

            if (!EvaluateCondition(step.WhenCondition, run))
            {
                run.RecordStepResult(StepResult.Skipped(step));
                continue;
            }

            var interpolatedParams = _variables.Interpolate(step.Params, run.Variables);
            var outcome = await _retryHandler.ExecuteWithRetry(
                () => _dispatcher.Dispatch(step.Module, step.Action, interpolatedParams, run, ct),
                step.RetryPolicy,
                ct);

            var result = StepResult.FromOutcome(step, outcome);
            run.RecordStepResult(result);  // raises StepCompletedEvent, Part II Section 9.3

            if (step.SaveAs is not null)
                _variables.Bind(step.SaveAs, result.Data, run);

            if (result.Status == StepStatus.Failed && !flow.ContinueOnFailure)
                break;  // fail-fast default, Product Philosophy 4.2 (Volume 0)
        }

        run.Complete();  // raises FlowRunCompletedEvent, Part II Section 9.3
        await _unitOfWork.Commit(ct);
        return run;
    }
}

This loop is a direct, formalized evolution of the Orchestrator.runFlow logic already validated in this project's early prototype — every structural decision made there (fail-fast by default, saveAs/when semantics, skip-vs-fail distinction) carries forward unchanged, now expressed against the full domain model from Part II rather than the prototype's simplified types.

19.3 Step dispatch and the Module Dispatcher boundary

_dispatcher.Dispatch in 19.2 delegates to the Module Dispatcher component (Part I, Section 5.3), which resolves the target module via the Module Registry and selects the appropriate IModuleExecutionStrategy (Section 18.5) based on trust tier — the Flow Engine itself has zero knowledge of in-process versus sandboxed execution; that distinction is fully encapsulated one layer down, per Architecture Principle 20.3.

19.4 Performance engineering against NFR-PERF-1

NFR-PERF-1 (Volume 0) requires orchestrator step-dispatch overhead under 50ms at p95, excluding the module's own execution time. The execution loop in 19.2 is designed with this budget in mind at every step:

Variable interpolation (_variables.Interpolate) operates on pre-parsed VariableExpression value objects (Part II, Section 11.7) whose referenced paths were already extracted at Flow-publish time, not re-parsed from raw template strings on every step dispatch — this is the specific performance payoff of that design decision, now made explicit.
Condition evaluation (EvaluateCondition) is similarly pre-compiled where possible at publish time rather than interpreted fresh per execution.
Domain event raising (run.RecordStepResult) is an in-memory operation (appending to the aggregate's internal event buffer) — the actual persistence and dispatch cost is deferred to the Unit of Work's Commit call (Section 16), which happens once per flow run, not once per step, keeping the hot per-step loop free of I/O beyond the module dispatch itself.
The retry handler's backoff delays (Section 19.5) are explicitly not counted against the 50ms budget, since NFR-PERF-1 measures dispatch overhead, not legitimate retry-policy waiting time — this distinction should be reflected in how Volume 4's monitoring dashboards report this metric, so retries don't artificially appear to violate the SLA.
19.5 Retry and flake handling
csharp
public interface IRetryFlakeHandler
{
    Task<StepOutcome> ExecuteWithRetry(Func<Task<StepOutcome>> action, RetryPolicy policy, CancellationToken ct);
}

public sealed record RetryPolicy(int MaxAttempts, TimeSpan InitialBackoff, double BackoffMultiplier);

Implementation logic: on failure, retry up to MaxAttempts with exponential backoff; if a later attempt succeeds, the final StepOutcome is marked Passed, but the RetryAttempt history (Part II, Section 10.4) is preserved on the resulting StepResult, and a FlowRunFlakeDetectedEvent (Part II, Section 12.3) is raised — directly implementing FR-ORCH-5's requirement to distinguish "passed on retry" from "passed on first attempt," and feeding NFR-REL-2's platform flake-rate metric with zero additional bookkeeping required elsewhere in the system.

19.6 Conditional execution (when)

EvaluateCondition resolves the when expression (Part II, Section 8.2) against the current FlowRun's accumulated Variables and prior StepResults — the same resolution mechanism as saveAs-bound variable interpolation (19.4), since conditions and variable references share the same underlying expression grammar (Part II, Section 11.7). A step whose condition evaluates falsy is recorded as Skipped, never silently omitted from the FlowRun's results — this directly ensures a FlowReport (Part II, Section 8.5) always accounts for every step in the original flow definition, satisfying FR-REP-1's completeness expectation even for conditionally-skipped work.

19.7 Fail-fast versus continue-on-failure

The flow.ContinueOnFailure check in 19.2 is the direct implementation of FR-ORCH-3. This flag is set at Flow-authoring time (a property on the Flow aggregate, Part II Section 9.2, not shown in that section's abbreviated code sketch but part of its full definition) and defaults to false — fail-fast — per Product Philosophy 4.2's stated preference for short, fail-fast flows over long brittle ones. A flow author setting this to true is making a deliberate, visible choice, not relying on an implicit platform default that happens to continue past failures.

19.8 Concurrency model

Each FlowEngine.Execute invocation runs within its own async execution context, with no shared mutable state across concurrent invocations beyond what's explicitly scoped per FlowRun (the Variable Store, Section 8.3 of Part II, is instantiated fresh per run, never shared — this was already flagged as a concurrency-safety requirement in Part I, Section 5.3). The Orchestration Engine container (Part I, Section 4) hosts many concurrent FlowEngine executions simultaneously, up to and beyond the NFR-SCALE-1 target of 500, bounded in practice by a configurable worker-pool/semaphore limit (Volume 4's deployment sizing responsibility) rather than any hard limit in this engine's own design.

19.9 Relationship to the Workflow Engine and Scheduler (forward reference)

This section specifies execution of a single FlowRun from start to terminal state. Section 20 (Workflow Engine) addresses a distinct, higher-level concern: coordinating multiple related flow runs (e.g., a CI pipeline's sequence of flows, or a future saga-style multi-flow business process per Volume 0's Future Vision, Section 23) — the Execution Engine specified here is Section 20's primary building block, not a competing concept.



20. Workflow Engine
20.1 Distinguishing "Workflow Engine" from the Execution Engine

Section 19 specified how a single FlowRun executes from its first step to a terminal state. This section addresses a different, higher-level question: how multiple flow runs relate to and depend on each other as part of a larger process. The term "Workflow Engine" in this volume's scope refers specifically to this coordination layer — it is not a synonym for the Execution Engine, and the distinction matters enough to state plainly: a Flow is Nexus's unit of automation; a Workflow (in this section's sense) is an orchestrated sequence or dependency graph of Flow runs, motivated directly by Product Philosophy 4.2's guidance (Volume 0) that flows themselves should stay short, fail-fast vertical slices — with cross-flow coordination handled at this separate layer rather than by growing individual flows into long, brittle scripts.

20.2 Why this layer exists — the CI-pipeline-chaining use case

Volume 0's UC-4 (unified pipeline gate, Persona Marcus) already anticipates this: "chain several small flows in your CI pipeline instead of writing one giant one" was stated as guidance in the original framework's README, and this section formalizes the platform-native mechanism for doing exactly that, rather than leaving it entirely to external CI-tool configuration. A Workflow definition lets a customer express "run the API smoke-test flow; if it passes, run the Web regression flow and the Mobile regression flow in parallel; if both pass, run the deployment-verification flow" as a first-class Nexus concept, with unified reporting across the whole sequence — not just per individual flow.

20.3 Workflow as a thin coordination layer, not a new execution model

Critically, a Workflow does not introduce a new execution engine, a new module contract, or a new domain-event system — it is built entirely on top of the primitives Sections 19, 22 (Event Bus), and Part II's Commands/Queries already define. This is a direct application of Architecture Principle 20.7 (composable primitives over monolithic capability): a Workflow is a WorkflowDefinition (a new, lightweight aggregate — not shown in Part II's original three because it was appropriately scoped out of that Part's core domain model discussion, and is introduced here as a Part III addition since it is fundamentally an orchestration-layer concept, not a core domain concept in the same sense as Flow/FlowRun/ModuleRegistration) that references existing FlowIds and expresses dependency/parallelism relationships between them.

csharp
public sealed class WorkflowDefinition : AggregateRoot<WorkflowId>
{
    public string Name { get; private set; }
    public IReadOnlyList<WorkflowNode> Nodes { get; private set; }
    public TenantId TenantId { get; private set; }
}

public sealed record WorkflowNode(
    FlowId FlowId,
    IReadOnlyList<WorkflowNodeId> DependsOn,   // empty = can start immediately
    bool ContinueWorkflowOnFailure = false      // per-node override, distinct from
                                                   // the underlying Flow's own
                                                   // ContinueOnFailure (Section 19.7) —
                                                   // this flag controls whether the
                                                   // WORKFLOW proceeds to dependent
                                                   // nodes, not whether the FLOW
                                                   // itself continues past a failed step
);
20.4 Workflow execution semantics

A WorkflowRun (the execution-side counterpart, mirroring FlowRun's relationship to Flow) tracks the status of each node's underlying FlowRun. The Workflow Engine component:

Identifies nodes with no unsatisfied dependencies and triggers their FlowRuns concurrently via TriggerFlowRunCommand (Part II, Section 13.3) — reusing the exact same command, execution engine, and reporting pipeline as any standalone flow trigger, with zero special-casing.
Subscribes to FlowRunCompletedEvent (Part II, Section 12.3) via the Event Bus (Section 22) for each in-flight node's FlowRun.
On a node's completion, evaluates whether dependent nodes' dependencies are now satisfied (all upstream nodes passed, or the specific upstream node was flagged ContinueWorkflowOnFailure) and triggers them accordingly.
Aggregates all nodes' FlowReports into a WorkflowReport once every node has reached a terminal state.

This design means the Workflow Engine is, structurally, just another consumer of the same domain events every other Part II/Part III component already produces and consumes — it introduces no new event types beyond a WorkflowRunCompletedEvent for its own aggregate completion, and no new module contract whatsoever.

20.5 Why Workflow is scoped out of Volume 0's Must-priority requirements

This is worth stating plainly since it's a meaningful scope decision: Volume 0's functional requirements (Section 12) do not name "Workflow" explicitly as a Must-priority capability — the closest reference is FR-ORCH-3/FR-ORCH-4's flow-level conditional/continuation logic. This section's Workflow Engine is best understood as the natural, composable extension of those primitives to a multi-flow scope, consistent with how Volume 0's Roadmap (Section 15) was deliberately silent on exactly this kind of composable extension, trusting the architecture (per Architecture Principle 20.7) to make it a natural addition rather than requiring it to be pre-specified. Its priority relative to the six-module GA scope (Volume 0, Section 14.10) is a Roadmap planning decision, not an architectural one — this section specifies how it would work, cleanly, whenever product priority calls for it.

20.6 Reporting implications

A WorkflowReport is a further aggregation on top of the per-flow FlowReports already defined in Part II, Section 8.5 — not a competing reporting concept. Persona Sofia's dashboard (Volume 0, UC-5) would display workflow-level pass/fail alongside individual flow trends, using the same drill-down pattern already specified for DrillDownFailureSignatureQuery (Part II, Section 14.4), extended with one additional level: workflow → flow → step, rather than requiring an entirely separate reporting mechanism.

20.7 Cross-tenant and licensing considerations

Consistent with Volume 0 Section 16.2's licensing boundaries, Workflow-level orchestration (as a capability distinct from single-flow execution) is a reasonable candidate for the enterprise licensing tier, in the same spirit as the cross-module reporting/analytics capabilities already scoped there — this section does not make that licensing determination itself (that remains Volume 0 Part III's domain), but flags it here as the natural licensing-tier question this capability raises, consistent with this volume's practice of tracing forward to where a decision actually belongs rather than silently assuming an answer.



21. Scheduler
21.1 Purpose

The Scheduler component (introduced in Part I, Section 5.3's diagram) owns trigger mechanisms for flow (and, per Section 20, workflow) execution that originate from something other than a direct, synchronous API call — time-based (cron) schedules, inbound webhooks, and file-system-watch triggers. This section formalizes its design and, critically, specifies the invariant already asserted informally in Part I: a scheduled trigger and an API-triggered run must be indistinguishable to every downstream component.

21.2 Trigger types
csharp
public enum TriggerKind { Api, Cron, Webhook, FileWatch }

public sealed record TriggerDefinition(
    TriggerId Id,
    FlowId OrTargetWorkflowId,  // a trigger may target either a Flow or a Workflow (Section 20)
    TriggerKind Kind,
    string? CronExpression,      // populated only when Kind == Cron
    string? WebhookPath,         // populated only when Kind == Webhook
    string? WatchedPath,         // populated only when Kind == FileWatch
    bool IsEnabled,
    TenantId TenantId
);
21.3 Uniform trigger-to-execution path
csharp
public class Scheduler : IHostedService
{
    private readonly ICommandDispatcher _commands;

    // Cron-based triggers, evaluated on a periodic internal tick
    private async Task OnCronTick(TriggerDefinition trigger, CancellationToken ct)
    {
        await _commands.Send(new TriggerFlowRunCommand(
            trigger.OrTargetWorkflowId, RunTrigger.Scheduled, VariableOverrides: null, trigger.TenantId), ct);
    }

    // Webhook-based triggers, invoked from a dedicated API Layer endpoint
    // (Part V, Section 30) that resolves the incoming path to a TriggerDefinition
    // and delegates here rather than duplicating trigger logic in the API layer
    public async Task OnWebhookReceived(TriggerDefinition trigger, WebhookPayload payload, CancellationToken ct)
    {
        await _commands.Send(new TriggerFlowRunCommand(
            trigger.OrTargetWorkflowId, RunTrigger.Webhook, VariableOverrides: payload.ToVariables(), trigger.TenantId), ct);
    }
}

Every trigger path above converges on the exact same TriggerFlowRunCommand (Part II, Section 13.3) that a direct API call or CLI invocation issues — this is the concrete implementation of the design goal stated in Part I, Section 5.3: "a scheduled run and an API-triggered run are indistinguishable to every downstream component." The only observable difference anywhere in the system is the RunTrigger value recorded on the resulting FlowRun (Part II, Section 9.3), used purely for reporting/filtering (ListRecentFlowRunsQuery, Part II Section 14.3), never to alter execution behavior.

21.4 Cron scheduling implementation notes

Cron evaluation runs as a hosted background service (consistent with the IHostedService pattern already established for the outbox relay, Section 17.6), checking due triggers on a short interval (e.g., every 30 seconds) against standard cron-expression parsing. For horizontally-scaled deployments (NFR-SCALE-2), exactly-once trigger firing across multiple Orchestration Engine worker instances is coordinated via a distributed lock (backed by Redis, per Volume 0 Section 21.9) keyed per TriggerId and the current cron-tick window — this prevents the same scheduled trigger from firing multiple times simultaneously across workers, which would otherwise silently violate a customer's expectation of "this runs once per day," a subtle but real correctness bug this section deliberately flags rather than leaving implicit.

21.5 Webhook trigger security

Webhook-triggered execution is a genuine external-input entry point into the system (crossing the system boundary defined in Part I, Section 2.4) and is treated with corresponding caution: every WebhookPath is a generated, unguessable identifier (not a predictable slug), webhook requests are optionally signature-verified against a per-trigger shared secret (resolved via the same ICredentialResolver pattern used by modules, Section 18.4), and payload size/rate limits apply per Part IV's broader API security posture (Section 24). This is a direct, deliberate application of Architecture Principle 20.5 (security designed in, not bolted on) to a component that might otherwise be treated as "just an internal convenience feature."

21.6 File-watch triggers

FileWatch triggers target a specific self-hosted deployment scenario (per Architecture Principle 20.8's "self-hosted is a first-class target") where a customer's CI process drops an artifact (e.g., a newly-built application bundle) into a monitored location and wants flow execution to begin automatically without a webhook round-trip. This trigger type is scoped to self-hosted deployments specifically, since the managed/hosted deployment model (Volume 0, Section 19.5) has no equivalent concept of a customer-local filesystem to watch — a deliberate, explicit scope boundary rather than an oversight.

21.7 Trigger management as ordinary CRUD, not a special subsystem

TriggerDefinition is managed via ordinary commands (CreateTriggerCommand, EnableTriggerCommand, DisableTriggerCommand — following the same pattern established in Part II, Section 13, not separately enumerated here to avoid duplicating that catalog) and queries, subject to the same RBAC and audit treatment (Part IV) as any other state-changing operation. There is no special "scheduler admin" bypass — this is a direct, deliberate consequence of Architecture Principle 20.5 applied consistently: a component that can cause flow execution (with whatever access that execution implies, per Section 18.4's credential-scoping) must be governed by the same authorization discipline as directly triggering that execution manually would require.

21.8 Relationship to NFR-REL-1 (availability)

Because scheduled triggers are, by definition, unattended, a missed cron tick or an unprocessed webhook due to a Scheduler outage is a materially worse failure mode than an API call failing (where a human is present to notice and retry). The Scheduler's own availability is therefore held to at least the same 99.9% bar as NFR-REL-1's overall commitment, with a specific operational safeguard: on Scheduler restart (deployment, crash recovery), it reconciles against the last-known-fired timestamp per trigger and fires any cron triggers that were missed during the downtime window (bounded by a configurable maximum catch-up window, to avoid a long outage causing a flood of backlogged executions) — this reconciliation behavior is stated here explicitly because "just start fresh and skip whatever was missed" is a tempting but incorrect default that would silently violate a customer's scheduling expectations.



22. Event Bus
22.1 Purpose — formalizing the component, not re-justifying the pattern

Part II, Section 12 already catalogued every domain event and justified, in depth (Section 12.8), why cross-context communication happens via published events rather than direct calls. This section specifies the Event Bus component's technical design: how events are actually transported, the concrete technology choice deferred from Volume 0, and how the outbox pattern from Section 16.4 connects to it.

22.2 Two-tier design — in-process dispatch plus durable relay

The Event Bus is deliberately a two-tier design, not a single mechanism, directly serving two different needs simultaneously:

Tier 1 — in-process, synchronous-dispatch pub/sub, using .NET's IPublisher/ISubscriber abstractions (a thin wrapper the codebase defines over MediatR-style notification dispatch, or an equivalent lightweight in-process mechanism — the specific library is an implementation detail, not fixed here per Section 6.5 of Part II's rigor-versus-specificity guidance). This tier serves low-latency consumers within the same process — most notably the SignalR Hub (Section 22.4), which needs StepCompletedEvents delivered with minimal latency to keep the GUI's live progress view responsive, directly serving Persona Priya's authoring-loop experience.

Tier 2 — durable, outbox-backed relay, which reads the outbox table written transactionally by the Unit of Work (Section 16.4) and republishes each event reliably, at-least-once, to every registered consumer — including consumers that may be temporarily unavailable, running in a different process, or added after the event was originally raised (e.g., a newly-deployed metrics collector backfilling recent history is out of this tier's scope, but a consumer that was briefly down during the original dispatch will still receive the event via this relay).

csharp
public interface IEventBus
{
    void Subscribe<TEvent>(Func<TEvent, CancellationToken, Task> handler) where TEvent : IDomainEvent;
    Task PublishInProcess(IDomainEvent evt, CancellationToken ct);  // Tier 1, called directly by UnitOfWork.Commit (Section 16.3, step 3)
}

public class OutboxRelayService : BackgroundService  // Tier 2
{
    protected override async Task ExecuteAsync(CancellationToken ct)
    {
        while (!ct.IsCancellationRequested)
        {
            var pending = await _outboxStore.GetUnrelayedEntries(batchSize: 100, ct);
            foreach (var entry in pending)
            {
                await _eventBus.PublishInProcess(entry.Deserialize(), ct);  // reuses Tier 1's dispatch
                await _outboxStore.MarkRelayed(entry.Id, ct);
            }
            await Task.Delay(_relayInterval, ct);
        }
    }
}
22.3 Why not a full external message broker for GA

Volume 0, Section 21.14 explicitly deferred "specific message-queue technology" to this volume. The GA design specified here deliberately does not introduce an external broker (Kafka, RabbitMQ, Azure Service Bus) as a hard dependency — the two-tier in-process-plus-outbox design in 22.2 is judged sufficient for the GA scale target (NFR-SCALE-1's 500 concurrent executions) while keeping the self-hosted deployment's dependency footprint minimal, directly serving Architecture Principle 20.8 (self-hosted is a first-class target, not a downscoped afterthought) — every additional required infrastructure dependency is a real cost to a self-hosted customer's operational burden. This decision is explicitly flagged as revisitable: if a future scaling need (a much larger customer, or the Workflow Engine's cross-flow event coordination at high volume, Section 20.4) demands it, IEventBus's interface-based design (22.2) means swapping Tier 2's implementation for an external-broker-backed one is a contained change, not an architecture rewrite — directly benefiting from the same persistence-ignorance-style discipline already applied to repositories (Section 15.5).

22.4 SignalR integration
csharp
public class RunProgressNotifier
{
    private readonly IHubContext<RunProgressHub> _hub;

    public RunProgressNotifier(IEventBus bus, IHubContext<RunProgressHub> hub)
    {
        _hub = hub;
        bus.Subscribe<StepCompletedEvent>(OnStepCompleted);
        bus.Subscribe<FlowRunCompletedEvent>(OnRunCompleted);
    }

    private async Task OnStepCompleted(StepCompletedEvent evt, CancellationToken ct) =>
        await _hub.Clients.Group(evt.RunId.ToString()).SendAsync("StepUpdate", evt.Result, ct);
}

Clients subscribe to a SignalR group keyed by FlowRunId (obtained from GetLiveRunProgressQuery's initial response, Part II Section 14.3) when opening a live-progress view — this group-scoping ensures a GUI client only receives events for the specific run it's watching, not a firehose of every concurrent run across the tenant, which matters directly for NFR-PERF-1-adjacent client-side responsiveness at scale.

22.5 Consumer registration inventory

Restating Part II Section 12.6's consumer table, now mapped to which Event Bus tier each consumer subscribes through:

Consumer Tier  Rationale
SignalR Hub (RunProgressNotifier)   Tier 1 only Needs minimum latency; missing a rare in-flight notification during a brief outage is acceptable since GetLiveRunProgressQuery provides a reconciling catch-up read on reconnect
Audit pipeline Tier 1 + Tier 2 (durable)  NFR-SEC-3's audit-completeness requirement means this consumer cannot tolerate missed events — the outbox relay's at-least-once guarantee is required, not optional, here
Report projector  Tier 1 + Tier 2 (durable)  FR-REP-1's reporting completeness has the same durability requirement as audit
Platform-health metrics collector (flake rate, self-heal acceptance rate)  Tier 2 primarily  Tolerant of modest delivery latency; prioritizes completeness over immediacy
CI webhook notifier (if configured) Tier 2   Outbound webhook delivery to a customer's own systems should be retried reliably, which the durable relay's at-least-once semantics directly support
22.6 Idempotency, restated as an implementation obligation

Per Part II, Section 12.7's contract obligation, every Tier-2-subscribed consumer in 22.5's table implements idempotent handling explicitly — for the Report projector and audit pipeline specifically, this means an upsert-by-(RunId, StepResultId) pattern rather than a blind insert, so that the outbox relay's at-least-once redelivery (which will occur during normal operation, not only during failure recovery, since the relay does not guarantee exactly-once) never produces duplicate report rows or duplicate audit entries.



23. CQRS
23.1 What "adopted" means for Nexus, precisely

Volume 0's original topic list included "CQRS (if adopted)" — this section resolves that conditional explicitly: Nexus adopts CQRS in its lightweight form (separate command and query models, sharing the same underlying data store) rather than its full form (fully separate read/write databases with eventual-consistency projection for every query). This is not adopted as an architectural ideology; it is adopted because Part II's own command/query catalog (Sections 13 and 14) was already designed around this separation from the start — this section formalizes and justifies a decision that was, in practice, already made incrementally across the preceding sections, and states clearly where the boundary between "lightweight" and "full" CQRS sits.

23.2 The write side — command handlers operate on aggregates

As specified across Sections 15–19: every command (Part II, Section 13) loads a full aggregate via its repository (Section 15), invokes behavior that enforces domain invariants (Part II, Section 9), and persists via Unit of Work (Section 16). The write side is always consistent, always invariant-respecting, and always reads its own aggregate state directly — there is no separate "write model" distinct from the domain model itself. This is the "command" half of CQRS, and it is nothing more exotic than standard DDD tactical design, already fully specified.

23.3 The read side — queries operate on purpose-built projections

As specified in Part II, Section 14.7: queries are free to read from denormalized read models rather than the aggregates directly. The specific projections Nexus maintains:

Read model  Projected from Serves
FlowSummaryReadModel Flow aggregate state, updated synchronously alongside writes (not event-projected — see 23.4)   ListFlowsQuery
FlowReportReadModel  StepCompletedEvent, FlowRunCompletedEvent (async, via Event Bus Tier 2, Section 22.5)  GetFlowReportQuery, all of Part II Section 14.4's reporting queries
TrendAggregateReadModel Periodic batch aggregation over FlowReportReadModel (not per-event — see 23.5)   GetPassRateTrendQuery, GetFlakeRateQuery
ModuleActionReadModel   ModuleRegisteredEvent, ModuleDeprecatedActionEvent (async)  GetAvailableModuleActionsQuery
23.4 Why some read models are synchronous and others are event-projected

This distinction is deliberate and directly tied to each query's consistency requirements, not applied uniformly for its own sake: FlowSummaryReadModel (used for the flow-authoring GUI's list view) must reflect a just-published flow immediately — an author publishing a flow and then not seeing it in their own list a moment later would be a jarring, confusing experience directly working against NFR-UX-2's usability bar. This read model is therefore updated synchronously, within the same Unit of Work transaction as the aggregate write itself (a same-transaction denormalized projection, not an eventually-consistent one), accepting a small write-side cost in exchange for read-your-own-writes consistency exactly where a human is watching for it.

FlowReportReadModel, by contrast, is legitimately fine to lag by the Event Bus's typical delivery latency (low milliseconds for Tier 1, slightly more for Tier 2's polling interval) — nobody is watching a report update the instant a step completes with zero tolerance for lag; the SignalR live-progress path (Section 22.4) already serves that immediate-feedback need through a completely different mechanism (event-driven UI push, not a query at all). This is the direct, considered reasoning behind Nexus's "lightweight, not full" CQRS characterization from 23.1: eventual consistency is applied selectively, exactly where its latency is actually invisible to the relevant persona, never as a blanket default.

23.5 Trend aggregation — batch, not per-event

TrendAggregateReadModel, serving NFR-PERF-3's "90-day trend view in under 2 seconds at p95 for up to 10,000 historical runs," is deliberately not recomputed per individual event — recalculating a rolling 90-day statistical aggregate on every single StepCompletedEvent would be both wasteful and unnecessary, since nobody needs trend-dashboard precision at sub-second granularity. Instead, a scheduled batch job (itself just another flow-adjacent background process, conceptually similar to the Scheduler's cron handling, Section 21.4) recomputes trend aggregates on a periodic cadence (e.g., hourly), trading a bounded, well-understood staleness window for dramatically reduced computational cost — a pragmatic engineering tradeoff explicitly permitted by Section 6.2 of Part II's "adopt tactical patterns pragmatically, not dogmatically" guidance.

23.6 Consistency guarantees stated explicitly, per read model

Because CQRS's eventual-consistency tradeoffs are exactly the kind of subtlety that causes confusing bugs when left implicit, every read model's consistency guarantee is stated explicitly here as a contract, not left to be inferred from its implementation:

FlowSummaryReadModel: strongly consistent (same-transaction) — always reflects the latest committed Flow state.
FlowReportReadModel: eventually consistent, typically sub-second lag, bounded by Event Bus Tier 2's relay interval (Section 22.2) in the worst case (a missed Tier-1 delivery falling back to Tier 2's polling cadence).
ModuleActionReadModel: eventually consistent, same bound as FlowReportReadModel — acceptable since a marketplace module's newly-registered actions appearing in the GUI a moment after registration completes is not a scenario with the same immediacy expectation as FlowSummaryReadModel's read-your-own-writes case.
TrendAggregateReadModel: consistent as of the last batch run (up to the configured batch interval, e.g., 1 hour) — this is the one read model where the staleness window is measured in a unit coarser than "seconds," and this should be surfaced in the GUI itself (a "last updated" timestamp on the trend dashboard) so Persona Sofia is never misled about data freshness, directly serving Architecture Principle 20.4's fail-informatively spirit applied to staleness, not just errors.
23.7 Why not full CQRS with separate read/write databases

A fully separated read/write database (a distinct database, potentially a different technology, dedicated purely to serving queries) was considered and deliberately deferred beyond GA scope: it would add meaningful operational complexity (a second database to provision, migrate, and keep available) to every deployment, including self-hosted ones — directly working against Architecture Principle 20.8's self-hosted-first posture, for a scaling benefit not currently justified by NFR-SCALE-3's stated target (10 million stored flow-run records is comfortably within a well-indexed single PostgreSQL instance's capability, per Part V's forthcoming database design). This is explicitly flagged, per Section 6.5 of Part II's stated discipline, as a decision that should be revisited if real production scale ever approaches or exceeds that target materially — not a permanent architectural ceiling, just the appropriately-scoped GA answer.


___________________________________________________________________________________________________________________________________________________



24. Authentication
24.1 Scope and boundary with Authorization

This section covers authentication — establishing that a request is genuinely coming from who it claims to come from — as a distinct concern from authorization (Section 25 — what that established identity is permitted to do). Conflating the two in implementation is a common source of security defects; Nexus keeps them as separate middleware stages and separate service abstractions throughout, consistent with Architecture Principle 20.5 (security designed in, not bolted on) requiring each new component to state its security posture explicitly rather than inheriting an ambiguous, conflated one.

24.2 Supported authentication mechanisms
Mechanism   Used by  Notes
Username/password + session (JWT-based)   GUI (Persona Priya, Sofia) Standard interactive login; JWT access tokens with short expiry, refresh-token rotation
API key  CLI, CI service accounts (Persona Marcus) Long-lived, scoped credential, tied to a service-account User (Part II, Section 10.6's IsServiceAccount flag) rather than a human's interactive session
OIDC/SAML SSO  Enterprise-tier customers  Federated identity via the customer's own IdP (Part I, Section 2.3's external-system relationship), satisfying Volume 0's enterprise-trust requirements
mTLS (self-hosted, optional)  Service-to-service (e.g., a sandboxed plugin process authenticating back to the Orchestration Engine, per Part III Section 18.5) Not an end-user-facing mechanism; internal defense-in-depth for the trust boundary described in Part I, Section 2.5
24.3 Authentication pipeline
csharp
public class AuthenticationMiddleware
{
    public async Task InvokeAsync(HttpContext context, RequestDelegate next)
    {
        var credential = _credentialExtractor.Extract(context.Request);  // Bearer JWT, API key header, or mTLS cert
        if (credential is null)
        {
            if (_endpointRequiresAuth(context))
                throw new UnauthenticatedException();
            await next(context);
            return;
        }

        var principal = await _authenticator.Authenticate(credential, context.RequestAborted);
        context.User = principal;  // ClaimsPrincipal carrying UserId, TenantId, IsServiceAccount
        await next(context);
    }
}

Every authenticated request results in a ClaimsPrincipal carrying, at minimum, UserId and TenantId (Part II, Section 7.4's shared-kernel identifiers) as strongly-typed claims — this is the concrete mechanism by which every downstream repository's tenant-scoping (Part III, Section 15.6) and every command handler's authorization check (Part II, Section 13.6) obtain the identity they need, sourced from exactly one place in the request pipeline, never re-derived or trusted from a client-supplied header.

24.4 API key design

API keys (serving Persona Marcus's CI use case, Volume 0 UC-4) are never stored or transmitted as a raw, reusable secret without additional protection: keys are generated with a public prefix (for identification/rotation purposes in logs, without revealing the secret) and a high-entropy secret portion, stored server-side only as a salted hash (never reversibly), and validated by hash comparison — directly analogous to password storage discipline. A compromised API key can be revoked immediately by its prefix without needing to identify or invalidate the full secret value, and revocation raises the CredentialRotatedEvent (Part II, Section 12.5) that the audit pipeline durably records.

24.5 JWT design for interactive sessions

Access tokens are short-lived (target: 15 minutes) and carry the minimum claims needed for request processing (UserId, TenantId, a RoleVersion claim discussed in 24.6) — never permission lists directly in the token, for the reason given in 24.6. Refresh tokens are longer-lived, stored server-side with rotation-on-use (each refresh invalidates the previous refresh token and issues a new one), directly mitigating replay risk from a leaked refresh token, and every refresh-token use is itself subject to the same audit trail as any other authentication event.

24.6 Why permissions are never embedded in the JWT

A tempting shortcut is embedding a user's current permission set directly into the JWT at issuance time, avoiding a database round-trip on every authorization check (Section 25). This is deliberately rejected: a role change (AssignRoleCommand/RevokeRoleCommand, Part II Section 13.5) must take effect immediately, not only after the affected user's existing token expires — a security-critical requirement (revoking a compromised or terminated employee's access cannot wait 15 minutes) that a permissions-in-token design would violate. Instead, the JWT carries only a RoleVersion claim (a monotonically-incrementing counter, bumped on every role change for that user), and the Authorization service (Section 25) checks this version against the current value on each request — a cheap, cacheable check that still allows near-immediate revocation without the full permission-list round-trip on every single request.

24.7 SSO/OIDC integration

For enterprise-tier customers (Volume 0, Section 16.2's licensing scope), Nexus acts as an OIDC relying party against the customer's own identity provider (Part I, Section 2.3). On successful federation, a local User record is provisioned or matched (just-in-time provisioning, configurable per-tenant) — Nexus does not attempt to be its own identity source of truth for SSO-configured tenants, consistent with the "read from, don't replace" positioning already established as a recurring Nexus design philosophy (Volume 0, Section 14.8, applied here to identity rather than logs). Group/role claims from the IdP can optionally be mapped to Nexus roles automatically, reducing administrative burden for large enterprise rollouts, per Persona Elena's and Sofia's shared interest in low-friction, centrally-governed access management.

24.8 Authentication failure handling

Consistent with Architecture Principle 20.4 (fail loud, fail informatively) — but deliberately not at the cost of leaking information useful to an attacker: authentication failures return a generic, uniform error (not distinguishing "user doesn't exist" from "wrong password," a standard security practice to prevent user enumeration) to the caller, while the internal audit/security log (Section 27) records the specific failure reason with full detail, for legitimate incident-investigation purposes. This is a deliberate exception to the otherwise-uniform "fail informatively" principle, justified specifically because the informativeness trade-off here is a security control, not a usability one, and the principle's own intent (Volume 1, Section 20.4) was about internal diagnosability, not external-facing error verbosity.

24.9 Rate limiting and brute-force protection

Authentication endpoints specifically (login, API key validation, token refresh) are subject to tighter rate limiting than the general API surface, keyed by both source IP and target account, with progressive backoff on repeated failures — implemented using the Redis-backed rate-limiting infrastructure already established in Volume 0, Section 21.9's caching-layer decision, reused here rather than introducing a separate mechanism.



25. Authorization
25.1 The permission model — RBAC as the GA baseline

Nexus implements role-based access control (RBAC) as its GA authorization model, directly satisfying FR-SEC-4's minimum three roles (flow author, flow runner, administrator) while designed to support finer-grained custom roles as a natural extension, not a re-architecture. A Role is a named bundle of Permission values; a User (Part II, Section 10.6) holds one or more RoleId assignments, scoped per TenantId.

csharp
public enum Permission
{
    FlowCreate, FlowEdit, FlowPublish, FlowArchive, FlowExecute,
    ReportView, ReportExport,
    ModuleRegister, ModuleDeactivate,
    UserManage, RoleManage, TriggerManage,
    AuditView, CredentialManage
}

public sealed class Role : Entity<RoleId>
{
    public string Name { get; private set; }
    public IReadOnlyList<Permission> Permissions { get; private set; }
    public TenantId TenantId { get; private set; }
    public bool IsSystemDefined { get; private set; }  // the three FR-SEC-4 baseline roles
                                                            // cannot be deleted, only have
                                                            // custom roles added alongside them
}
25.2 The three baseline roles, precisely defined
Role  Permissions Corresponds to
Flow Author FlowCreate, FlowEdit, FlowPublish, FlowArchive, FlowExecute, ReportView Persona Priya (Volume 0, Section 10.2)
Flow Runner FlowExecute, ReportView only — no authoring permissions  Persona Marcus's CI service account (Volume 0, Section 10.3) — deliberately the narrowest role, since a CI credential leak should never grant flow-authoring capability
Administrator  All permissions, including UserManage, RoleManage, ModuleRegister, CredentialManage The implicit administrator actor (Part I, Section 2.2)

The Flow Runner role's deliberate narrowness in this table is worth calling out as a specific, intentional security decision rather than an oversight: Marcus's CI pipeline needs to trigger flows and read results, never to author or publish them — granting broader permissions "for convenience" to a machine-held credential would directly work against the least-privilege posture Architecture Principle 20.5 requires, and against the credential-scoping discipline already established for module execution in Part III, Section 18.4.

25.3 Authorization service — the single point of decision
csharp
public interface IAuthorizationService
{
    Task<bool> HasPermission(UserId userId, TenantId tenantId, Permission permission, CancellationToken ct);
    Task EnsurePermission(UserId userId, TenantId tenantId, Permission permission, CancellationToken ct);  // throws ForbiddenException
}

Every command handler (Part II, Section 13.7's pattern) calls EnsurePermission as its first action, per Section 13.6's "layer 1" validation — this is a hard, universal convention, not applied selectively: a command handler with no authorization check is treated as a defect at code review, full stop, regardless of how "obviously safe" the operation might seem. This uniformity is what makes the security posture auditable as a whole (directly serving Persona Elena's review process, Volume 0 UC-7) rather than requiring a case-by-case trust argument for every individual command.

25.4 Resource-level (not just operation-level) authorization

RBAC as specified in 25.1–25.3 governs operation-level permissions (can this user publish flows at all), but several of Volume 0's use cases require resource-level checks as well — a Flow Author's FlowEdit permission should not, without more, let them edit a flow belonging to a different team within the same tenant if the organization has configured team-level isolation. Nexus's authorization service supports this via an optional resource-ownership/scoping check layered on top of the base RBAC decision:

csharp
public async Task EnsureCanEdit(UserId userId, FlowId flowId, CancellationToken ct)
{
    await _authz.EnsurePermission(userId, currentTenant, Permission.FlowEdit, ct);   // operation-level
    await _authz.EnsureResourceAccess(userId, ResourceType.Flow, flowId, ct);         // resource-level, optional per-tenant policy
}

Resource-level scoping (team ownership, project-based grouping) is deliberately kept as an optional, tenant-configurable layer rather than a mandatory part of the base model — most tenants, especially smaller ones, have no need for it, and forcing it universally would add authorization-check overhead and administrative complexity with no corresponding benefit for the common case, a direct application of Architecture Principle 20.7's composability preference (add complexity only where it earns its cost).

25.5 Authorization and the RoleVersion claim

Building on Section 24.6's design: HasPermission/EnsurePermission first checks the requesting ClaimsPrincipal's RoleVersion claim against the current stored version for that user (a cheap, Redis-cached lookup keyed by UserId). If the versions match, the service proceeds to evaluate permissions from a similarly-cached role/permission set; if they differ (a role change occurred since the token was issued), the service forces a fresh permission lookup and, if the request's claimed permission is no longer valid, rejects it immediately rather than honoring a stale token's assumptions — this is the mechanism that makes the "immediate revocation" guarantee from Section 24.6 concretely true, not just a design intention.

25.6 Authorization for AI-originated and system-originated actions

Per Part II, Section 13.3's RequestSelectorHealCommand design note: even commands originating from the orchestration engine itself (not a human's direct request) pass through the same EnsurePermission check, evaluated against the identity of the FlowRun's original triggering user (or service account) — an AI-assisted self-heal inherits the authorization context of the run it's healing, never escalating privilege beyond what that run's original triggering identity already held. This closes a specific, easy-to-miss gap: without this rule, a compromised or misconfigured AI integration could not be used to perform an action beyond what the legitimate triggering identity was authorized for in the first place, directly reinforcing Volume 0's AI-boundary commitments (Section 18.4) at the authorization layer specifically, not only at the product/UX layer.

25.7 Authorization for marketplace/sandboxed modules

Referencing Part III, Section 18.5's trust-tiered execution model: a sandboxed (Community-tier) module's ICredentialResolver access (Part III, Section 18.4) is itself subject to an authorization check — the resolver verifies that the specific credential being requested was explicitly wired into the invoking FlowStep's parameters by the flow's author (who, per 25.2, held FlowEdit/FlowPublish permission when they did so), closing the loop between "who is allowed to grant a module access to a secret" (a human flow author, with real authorization) and "what a module can actually retrieve at runtime."

25.8 Authorization testing and the compliance-review use case

Because the entire authorization model in this section is designed to be exhaustively enumerable (25.1's fixed Permission enum, 25.2's baseline role table), Volume 5's testing standards will require a maintained test suite that, for every command in Part II's catalog (Section 13), asserts the exact required permission and verifies both the "authorized" and "unauthorized, rejected" paths — this exhaustive, enumerable test coverage is the concrete artifact Persona Elena's security review (Volume 0, UC-7) can be shown directly, rather than asking her to trust a prose description of the authorization model.



26. Multi-tenancy
26.1 Tenancy model — pooled by default, isolated by deployment choice

Nexus's managed/hosted deployment (Volume 0, Section 19.5) uses a pooled, shared-database multi-tenancy model: all tenants share the same application infrastructure and the same physical PostgreSQL database, isolated logically via TenantId filtering enforced at the repository layer (Part III, Section 15.6). The self-hosted deployment model, by its nature, typically serves a single tenant per deployment (a customer's own infrastructure, running their own instance) — but the domain model and codebase make no structural assumption about single-tenancy even there, so a self-hosted customer wanting to run Nexus for multiple internal business units within one deployment is fully supported without any different code path.

26.2 Why pooled rather than database-per-tenant for the hosted model

Database-per-tenant (a separate physical or logical database per customer) offers stronger isolation guarantees but was rejected as the GA default for the hosted offering for operational reasons directly tied to NFR-SCALE-2 (horizontal scaling by adding workers, without client-facing changes) and Business Objective 4's marketplace ambitions (Volume 0): schema migrations, connection-pool management, and cross-tenant aggregate reporting (a future analytics capability, per Volume 0's Future Vision Section 23.6) all become significantly more operationally complex at scale with database-per-tenant. Pooled multi-tenancy with rigorous TenantId filtering is the industry-standard tradeoff for a SaaS platform at Nexus's target scale, and is explicitly revisitable per-customer for enterprise deployments requiring stronger physical isolation (see 26.6).

26.3 Enforcement mechanism — defense in depth, not a single checkpoint

Tenant isolation is enforced at three independent layers, deliberately redundant rather than relying on any single checkpoint being perfect:

Repository-level query filtering (Part III, Section 15.6) — every aggregate query includes a TenantId predicate, sourced from the authenticated ClaimsPrincipal (Section 24.3), never from a client-supplied parameter.
Database row-level security (RLS), configured on every multi-tenant table in PostgreSQL, using the current session's TenantId (set via a connection-scoped session variable at the start of each request) as the RLS policy predicate — this is the "defense in depth" layer: even if a repository-layer bug omitted a TenantId filter, the database itself would refuse to return cross-tenant rows.
Application-service assertion, a final belt-and-suspenders check in command/query handlers that explicitly asserts a loaded aggregate's TenantId matches the requesting principal's TenantId before returning or mutating it, treating a mismatch as a serious internal error (logged at a high severity, per Section 28) rather than a normal "not found" — because a mismatch at this point indicates layers 1 and 2 both failed, which should never happen and needs immediate investigation if it does.
csharp
public class TenantScopedDbContext : NexusDbContext
{
    public TenantScopedDbContext(ITenantContextAccessor tenantAccessor, DbContextOptions options) : base(options)
    {
        // Sets the PostgreSQL session variable consumed by RLS policies (layer 2)
        Database.ExecuteSqlRaw("SET app.current_tenant_id = {0}", tenantAccessor.CurrentTenantId.Value);
    }

    protected override void OnModelCreating(ModelBuilder builder)
    {
        // Applies a global query filter (layer 1) to every multi-tenant entity type,
        // so a developer cannot accidentally write a LINQ query that forgets the
        // TenantId predicate — it's structurally impossible to omit, not just a
        // convention to remember.
        builder.Entity<FlowEntity>().HasQueryFilter(f => f.TenantId == _tenantAccessor.CurrentTenantId);
        // ... repeated for every multi-tenant entity type
    }
}
26.4 ITenantContextAccessor — resolving the current tenant safely
csharp
public interface ITenantContextAccessor
{
    TenantId CurrentTenantId { get; }
}

Implemented as a scoped service populated exclusively from the authenticated ClaimsPrincipal (Section 24.3) at the start of request processing — never from a request header, query parameter, or any other client-controllable input, which would trivially allow tenant-boundary bypass. This is stated as an absolute rule specifically because "just read the tenant from a header for convenience" is a realistic, tempting shortcut during development that must never reach production code; Volume 5's code-review checklist will include an explicit item verifying no code path derives TenantId from anything other than this accessor.

26.5 Tenant isolation and the Module & Plugin context

Referencing Part II, Section 9.4 and Part III, Section 18: ModuleRegistration is itself tenant-scoped for Community-tier marketplace modules (a module a specific tenant chooses to install is not automatically available to other tenants), while Built-in and Verified-tier modules are effectively global (available to every tenant by default, since they carry no tenant-specific trust decision). This distinction matters concretely for the enterprise policy scenario already discussed in Part II, Section 14.5 ("restrict this org's instance to Verified-or-above modules only") — that policy is itself a per-tenant configuration value, enforced at the Module Dispatcher's resolution step (Part I, Section 5.3) by checking the requesting flow's TenantId's policy before allowing dispatch to a Community-tier module, even one the tenant has technically installed.

26.6 Stronger isolation for regulated-industry customers

Directly serving Persona Elena's segment (Volume 0, Section 10.6) and Business Objective 5's regulated-industry target: for hosted customers requiring stronger isolation than the pooled model provides (a common regulated-industry requirement), Nexus's architecture supports a dedicated-instance deployment — effectively a single-tenant deployment of the same self-hosted-capable codebase, operated by Nexus on dedicated infrastructure rather than the customer's own. This is not a separate codebase or a special "enterprise fork" — it is the same application, the same multi-tenancy-capable schema (simply populated with one tenant), directly benefiting from Architecture Principle 20.8's insistence that the self-hosted deployment path be a first-class, fully-maintained target rather than an afterthought; the dedicated-instance offering is, structurally, just self-hosting operated on the customer's behalf.

26.7 Cross-tenant operations — the one deliberate exception

The only legitimate cross-tenant read in the entire system is Nexus's own internal operational tooling (e.g., a platform-health dashboard aggregating flake rates across all tenants for the Nexus engineering team's own use, distinct from any tenant-facing feature) — implemented via a separate, explicitly-named IPlatformAdminQuery interface family that bypasses the standard ITenantContextAccessor-scoped DbContext entirely, uses a distinct, heavily-audited administrative credential, and is never reachable via any tenant-facing API endpoint. This exception is named explicitly, in this section, specifically so it is never confused with or accidentally generalized into a broader "sometimes we read across tenants" pattern — it is a single, narrow, clearly-labeled escape hatch, not a precedent.



27. Audit
27.1 Purpose and relationship to AuditEntry

Part II, Section 10.7 already defined the AuditEntry entity as append-only, satisfying NFR-SEC-3. This section specifies the full audit pipeline: what triggers an audit entry, how entries flow from a command's execution to durable storage, and how the retention and export requirements (NFR-SEC-3, and Persona Elena's ExportAuditLogQuery, Part II Section 14.4) are actually satisfied end-to-end.

27.2 What gets audited — the exhaustive-by-construction approach

Rather than maintaining a manually-curated list of "important" actions to audit (which inevitably drifts out of date as new commands are added), Nexus's audit pipeline is wired to the command catalog itself (Part II, Section 13): every command handler, as a structural convention enforced by a shared base class or interceptor, emits an audit-relevant record as a natural byproduct of successful execution — not as an opt-in feature individual handlers must remember to implement.

csharp
public abstract class AuditableCommandHandler<TCommand, TResult> : ICommandHandler<TCommand, TResult>
    where TCommand : ICommand<TResult>
{
    public async Task<TResult> Handle(TCommand cmd, CancellationToken ct)
    {
        var result = await HandleCore(cmd, ct);
        _auditRecorder.Record(new AuditEntry(
            actorId: _currentUser.UserId,
            actionType: typeof(TCommand).Name,
            targetType: DeriveTargetType(cmd),
            targetId: DeriveTargetId(cmd),
            metadata: DeriveMetadata(cmd, result),
            tenantId: _tenantAccessor.CurrentTenantId));
        return result;
    }

    protected abstract Task<TResult> HandleCore(TCommand cmd, CancellationToken ct);
}

This base-class pattern means a developer adding a new command (Part II, Section 13) gets audit logging for free by construction, rather than needing to remember to add it — directly closing the class of gap where a newly-added state-changing operation is accidentally left unaudited, which would otherwise be exactly the kind of silent compliance regression Architecture Principle 20.5 (security designed in, not bolted on) exists to prevent.

27.3 Audit entries persist via the same Unit of Work, same outbox

Referencing Part III, Section 16.4: the audit entry created in 27.2 is persisted within the same transaction as the command's primary state change, via the same outbox-backed Unit of Work — this guarantees an audited action and its audit record are never inconsistent (no state change without a corresponding audit entry, and no orphaned audit entry for a change that was rolled back), directly extending the atomicity guarantee already established for domain events to audit specifically, since an AuditEntry creation is, structurally, itself just another kind of domain event consumer (Part II, Section 12.6's audit-pipeline row) receiving the same underlying signal.

27.4 What gets recorded — sensitive-data handling within audit entries

Per NFR-SEC-3's "never persist secrets in plaintext" and the CredentialRotatedEvent design already established (Part II, Section 12.5): DeriveMetadata (27.2) is implemented per-command with explicit denylisting of any parameter that might carry a secret value — a RotateCredentialCommand's audit entry records that a rotation occurred, by whom, and for which named credential reference, never the credential's old or new value. This denylisting is enforced by a compile-time-checked attribute on command properties ([SensitiveValue]) that the metadata-derivation logic automatically excludes, rather than relying on each command's author to remember to exclude it manually — the same "make the safe path the automatic path" philosophy as 27.2's base-class approach, applied specifically to data sensitivity.

27.5 Immutability enforcement

Beyond the domain-model-level immutability already noted in Part II, Section 10.7 (no update methods on the entity), the database schema itself (Part V, Section 32) enforces this at the storage layer: the audit_entries table grants the application's database role INSERT and SELECT privileges only — no UPDATE or DELETE grant exists at all, even for an administrative database connection used for routine maintenance. This is a deliberate, structural (not just application-code-level) enforcement of NFR-SEC-3's immutability requirement, directly relevant to Persona Elena's compliance review (Volume 0, UC-7), which can verify this guarantee by inspecting database grants directly rather than trusting application-code discipline alone.

27.6 Retention and archival

Per NFR-SEC-3's minimum 1-year retention: audit entries are retained in the primary, queryable store for a configurable "hot" window (default: 1 year, matching the NFR floor exactly, tenant-configurable upward), after which they are archived (not deleted) to lower-cost, still-durable storage (Volume 4's infrastructure responsibility for the specific mechanism — e.g., a scheduled export to object storage), remaining available via ExportAuditLogQuery (Part II, Section 14.4) but potentially with higher retrieval latency for the archived tier. Deletion of audit data before its configured retention floor is not a supported operation anywhere in the system's command catalog — there is no DeleteAuditEntryCommand, by deliberate design, not merely by policy.

27.7 Distinguishing Audit from operational Logging

This section covers audit records specifically — an immutable, compliance-oriented record of who did what, when, to what. Section 28 (Logging) covers a related but distinct concern: operational, debugging-oriented log output from the running system, which is higher-volume, not immutable in the same compliance sense, and governed by different retention/access rules. A single underlying event (e.g., StepCompletedEvent, Part II Section 12.3) can and does feed both concerns simultaneously through separate consumers (Section 22.5's consumer table already lists the audit pipeline as one specific Event Bus subscriber, distinct from general application logging), but the two output streams — audit trail and operational log — are never merged into one system, since conflating them would compromise both: operational logs need aggressive, cost-effective retention policies and high-volume tolerance that compliance audit data should never be subject to, and audit data needs the immutability and access-control rigor that would be excessive overhead for routine debug logging.

27.8 Audit query access control

AuditView permission (Section 25.1's Permission enum) is deliberately held only by the Administrator baseline role by default (Section 25.2) — audit data itself is sensitive (it can reveal, for instance, who has been actively working with a specific credential, or the full history of a specific user's access-level changes), and its own access is therefore subject to the same authorization rigor as any other sensitive resource in the system, with every access to audit data itself optionally logged as a further audit entry — an intentional, recursive application of this section's own principle to itself, available as a tenant-configurable option for organizations with the strictest compliance requirements (Persona Elena's segment specifically).




28. Logging
28.1 Purpose and distinction from Audit and from the Logs module

A necessary disambiguation up front, since this document set uses "log" in three genuinely different senses: this section (28) covers Nexus's own operational logging — the diagnostic output the Nexus application itself produces about its own behavior. This is distinct from Section 27 (Audit), the immutable compliance trail, and distinct from the Logs module (Volume 0, Section 14.8; Part III, Section 18) — the customer-facing automation capability that reads and analyzes a target system's logs. All three are legitimately called "logs" in casual conversation, but they are three different systems with three different purposes, retention rules, and audiences, and this document set is deliberately precise about which one is meant in each section.

28.2 Structured logging as a non-negotiable convention

Every log statement in the Nexus codebase is structured (key-value/JSON-shaped, via the standard .NET ILogger structured-logging pattern), never a freeform interpolated string:

csharp
// Required convention:
_logger.LogInformation("Flow {FlowId} published by user {UserId} in tenant {TenantId}", flowId, userId, tenantId);

// Prohibited convention:
_logger.LogInformation($"Flow {flowId} published by user {userId} in tenant {tenantId}");

The difference matters operationally, not just stylistically: the structured form preserves FlowId, UserId, and TenantId as independently queryable fields in the observability backend (Section 29), enabling a query like "show me every log line for this specific FlowId across every component that touched it" — impossible to do reliably against freeform interpolated strings. Volume 5's coding standards will enforce this via static analysis (a Roslyn analyzer rejecting string-interpolated logger calls) rather than relying on code-review vigilance alone.

28.3 Log levels and their meaning in Nexus specifically
Level Meaning in Nexus's context Example
Trace Extremely granular, disabled by default even in development Raw variable-interpolation resolution steps
Debug Detailed flow, enabled in development/troubleshooting Module dispatch decisions, retry attempts
Information Normal, expected state transitions  Flow published, run started/completed
Warning  Unexpected but handled/recovered situations  A step retried and eventually passed (flake), a deprecated module action used
Error An operation failed and could not complete as requested  A step failed, a command was rejected by a domain invariant
Critical The platform itself is degraded in a way threatening NFR-REL-1's availability commitment  Database connectivity lost, outbox relay (Part III, Section 22.2) falling significantly behind

This table is the concrete operationalization of Architecture Principle 20.4 (fail loud, fail informatively) at the logging layer — a Warning for a flaky-but-passing step versus an Error for a genuinely failed step is not a cosmetic distinction; it's what lets an on-call engineer's alerting (Section 29) be tuned to page on Critical/Error without being drowned out by expected, self-recovering Warning-level noise.

28.4 Correlation — tracing one request/run across every component

Every log statement automatically carries a correlation identifier — CorrelationId for a single API request, and FlowRunId (Part II, Section 9.3) for anything occurring within the scope of a flow execution — propagated via ILogger's scope mechanism (BeginScope) rather than requiring every individual log call to manually pass these values:

csharp
using (_logger.BeginScope(new Dictionary<string, object> { ["FlowRunId"] = runId.Value }))
{
    // Every log statement within this scope, across every component the
    // execution touches (Flow Engine, Module Dispatcher, individual module
    // implementations per Part III Section 18.3's ExecutionContext.ModuleLogger,
    // which is itself just a scoped child logger), automatically carries
    // FlowRunId without each call site needing to include it explicitly.
}

This correlation design is what makes a support/debugging workflow like "a customer reports Flow Run X failed unexpectedly, reconstruct exactly what happened" tractable — every log line relevant to that run, across every layer from the API Layer's initial request handling through the Module Dispatcher's execution, is retrievable via a single FlowRunId filter in the observability backend (Section 29), without needing to separately correlate timestamps across disjoint log streams.

28.5 What must never appear in operational logs

Directly extending Section 27.4's sensitive-data denylisting principle to operational logging specifically: credential values, full request/response bodies from target systems under test (which may themselves contain a customer's own sensitive data, per Part I Section 2.5's trust-boundary analysis), and full page HTML/screenshots passed to the AI module for selector healing (Part III, Section 18) are never logged at any level, including Trace/Debug. Where a log statement needs to reference such data for debugging purposes, it references a truncated hash or a reference identifier, never the raw value — this is enforced via the same [SensitiveValue] attribute convention already established in Section 27.4, applied consistently across both the audit and operational-logging pipelines rather than as two separately-maintained denylists that could drift out of sync.

28.6 Log retention and cost management

Unlike audit entries (Section 27.6's 1-year compliance floor), operational logs follow a much shorter, cost-optimized retention policy — a tiered approach (e.g., full-fidelity retention for 14 days, downsampled/aggregated retention for 90 days, then discarded) reflecting that operational logs exist to support near-term debugging and incident response, not long-term compliance record-keeping. This retention policy is a Volume 4 (Infrastructure) configuration concern specifically; this section establishes only the principle that operational-log retention is deliberately, appropriately shorter than audit retention, and that the two must never be conflated into a single retention policy (echoing Section 27.7's distinction).

28.7 Module-level logging and the plugin trust boundary

Per Part III, Section 18.3: a module's ExecutionContext.ModuleLogger is a genuinely scoped, restricted logger — a Community-tier sandboxed module (Part III, Section 18.5) can log freely within its own execution scope for its own debugging purposes, but its log output is tagged and isolated (never able to inject fabricated log entries attributable to a different component) and is itself subject to the same sensitive-data scrubbing (28.5) applied at the boundary where the sandboxed process's output is received back into the main logging pipeline — a third-party module's log output is treated as untrusted input requiring sanitization, not as equally-trusted first-party output, consistent with the trust-boundary discipline established in Part I, Section 2.5.

28.8 Forward reference to Observability

Logging, as specified in this section, is one of the "three pillars" of observability (alongside metrics and traces) that Section 29 covers holistically — this section deliberately scopes itself to what gets logged and how it's structured, leaving how logs are collected, stored, visualized, and alerted upon to Section 29's broader observability architecture, since that architecture (built on OpenTelemetry, per Volume 0 Section 21.11) treats logs, metrics, and traces as three correlated facets of the same underlying telemetry pipeline rather than three independent systems.



29. Observability
29.1 Purpose — the platform observing itself

This section specifies how Nexus achieves the "observability of the platform itself" cross-cutting concern first flagged in Part I, Section 19.4, and directly serves the platform-health metrics established across Volume 0: flake rate (NFR-REL-2), step-dispatch overhead (NFR-PERF-1), and general availability (NFR-REL-1). This is Nexus monitoring itself, distinct in purpose from the Logs module (Volume 0, Section 14.8), which is a product capability for monitoring customers' target systems — the same distinction already drawn precisely in Section 28.1.

29.2 The three pillars, unified via OpenTelemetry

Consistent with Volume 0's locked technology decision (Section 21.11), Nexus instruments all three observability pillars — logs (Section 28), metrics, and distributed traces — through OpenTelemetry's unified SDK, exporting via OTLP to a configurable backend (Prometheus/Grafana in the reference self-hosted architecture, or a customer's own existing observability stack, per Architecture Principle 20.8's self-hosted-first posture and the same "integrate with, don't replace" philosophy already applied to the customer-facing Logs module).

csharp
services.AddOpenTelemetry()
    .WithTracing(tracing => tracing
        .AddSource("Nexus.Orchestration")   // custom ActivitySource, Section 29.3
        .AddAspNetCoreInstrumentation()
        .AddEntityFrameworkCoreInstrumentation()
        .AddOtlpExporter())
    .WithMetrics(metrics => metrics
        .AddMeter("Nexus.Orchestration")     // custom Meter, Section 29.4
        .AddRuntimeInstrumentation()
        .AddOtlpExporter());
29.3 Distributed tracing — one trace per flow run

Every FlowRun (Part II, Section 9.3) is instrumented as a root OpenTelemetry trace, with each FlowStep's dispatch (Part III, Section 19.3) forming a child span — including, critically, spans that cross the sandboxed-module process boundary (Part III, Section 18.5), via W3C Trace Context propagation over the gRPC channel, so that a Community-tier module's execution time is visible as a distinct, attributable span within the parent trace rather than appearing as an opaque gap. This design directly makes NFR-PERF-1's "orchestrator step-dispatch overhead... excluding the module's own execution time" a measurable, not just specified, quantity — the trace structure itself cleanly separates orchestrator-attributable time (the Flow Engine, Module Dispatcher, Variable Store spans) from module-attributable time (the module's own span, further broken down by whatever internal spans a specific module chooses to emit, e.g., the Web module distinguishing "browser launch" from "page navigation" from "assertion evaluation").

29.4 Key platform metrics
Metric   Type  Directly implements
nexus.step.dispatch.duration  Histogram, tagged by ModuleKind  NFR-PERF-1 (50ms p95 dispatch overhead)
nexus.flow.run.duration Histogram, tagged by TenantId (aggregated, never per-tenant-identifiable in shared dashboards), RunStatus   General execution-time monitoring
nexus.flow.run.flake_detected Counter  NFR-REL-2 (platform flake-rate — numerator)
nexus.flow.run.completed   Counter, tagged by RunStatus  NFR-REL-2 (denominator)
nexus.orchestration.concurrent_executions UpDownCounter (gauge-like) NFR-SCALE-1 (500 concurrent execution target — direct capacity monitoring)
nexus.ai.request.duration  Histogram, tagged by Capability (flow-generation, self-heal, triage) NFR-PERF-4 (15s AI timeout — monitoring actual latency against the budget)
nexus.ai.request.timeout   Counter  Direct signal for AI-optionality graceful-degradation frequency (NFR-REL-3) — a rising count here indicates the AI provider relationship needs attention, independent of any single flow's outcome
nexus.outbox.relay.lag  Gauge Section 22.2's Tier 2 relay health — directly informs whether durable-consumer guarantees (audit, reporting) are being met with acceptable latency
nexus.eventbus.publish.duration  Histogram   Event Bus (Section 22) health
29.5 The flake-rate metric, computed transparently

Directly addressing Volume 0's NFR-REL-2 (platform flake rate below 0.5%, tracked as a release-gating metric): the ratio of nexus.flow.run.flake_detected to nexus.flow.run.completed, computed over a rolling window, is the exact, auditable formula behind this metric — stated explicitly here so there is no ambiguity between "platform flake rate" as a marketing/reporting figure and as a concretely-computed, dashboard-visible quantity any engineer can independently verify against the same raw counters the release-gating CI check (Volume 5) consumes.

29.6 Alerting philosophy

Consistent with Architecture Principle 20.4 (fail loud, fail informatively) applied to operational alerting specifically: alerts are configured to fire on symptom metrics (elevated nexus.step.dispatch.duration p95, rising nexus.outbox.relay.lag, dropping nexus.orchestration.concurrent_executions capacity headroom) rather than only on hard failures, so that NFR-REL-1's 99.9% availability commitment is protected proactively — an on-call engineer should generally see a degradation trend in dashboards before it becomes a customer-visible outage, not only after. Full alerting-rule configuration (specific thresholds, escalation policies) is Volume 4's infrastructure-operations responsibility; this section establishes the metric foundation those rules are built on.

29.7 Health checks versus deep observability

Distinct from the rich telemetry above, Nexus exposes standard lightweight health-check endpoints (/health/live, /health/ready) for load-balancer and orchestration-platform use (Kubernetes liveness/readiness probes, per Volume 0 Section 21.12) — these are intentionally minimal (process responsiveness, database connectivity) and are not a substitute for the OpenTelemetry-based observability described above; they answer "should traffic be routed to this instance right now," a narrower and faster question than "is the platform healthy," which the full telemetry pipeline answers.

29.8 Tenant data isolation within shared observability infrastructure

Directly extending Section 26's multi-tenancy discipline to the observability pipeline itself: metrics and traces that include a TenantId tag are aggregated for platform-health purposes (29.4's table) but are never exposed in a customer-facing view broken out by other tenants' identifiers — a customer's own operational dashboard (if Nexus ever exposes one directly, beyond the reporting dashboard already specified in Part II, Section 14.4) would filter strictly to that customer's own TenantId, enforced by the same ITenantContextAccessor-scoped access pattern (Section 26.4) applied to observability-data queries, not a separately-invented access-control mechanism.

29.9 Observability's relationship to the rest of Part IV

This section closes Part IV by tying together every prior section's operational signal into one coherent telemetry model: authentication failures (Section 24.8) and authorization rejections (Section 25) are logged (Section 28) with correlation identifiers (Section 28.4) and, where they represent a security-relevant pattern (e.g., a spike in authentication failures against one account, potentially indicating a credential-stuffing attempt), surfaced as metrics this section's alerting philosophy (29.6) can act on — Part IV's six sections are not six independent concerns bolted together, but one connected security-and-operations fabric, which is the concrete, technical fulfillment of Architecture Principle 20.5's instruction that security and compliance posture be designed in from the start, at every layer, rather than assembled from disconnected point solutions after the fact.


___________________________________________________________________________________________________________________________________________________



30. REST API
30.1 Purpose and design philosophy

The REST API is the single public write and read surface for Nexus, per Architecture Principle 20.5 restated from Part I, Section 19.2: the GUI and CLI are both clients of this API, never given a privileged internal shortcut. This section specifies the endpoint inventory (mapping directly onto Part II's command and query catalogs, Sections 13–14), request/response conventions, versioning, and error-handling design.

30.2 Resource-oriented endpoint inventory
Resource Endpoints   Backing command/query (Part II)
Flows POST /api/v1/flows   CreateFlowCommand (13.2)
   GET /api/v1/flows ListFlowsQuery (14.2)
   GET /api/v1/flows/{id}  GetFlowByIdQuery (14.2)
   POST /api/v1/flows/{id}/steps AddFlowStepCommand (13.2)
   POST /api/v1/flows/{id}/publish  PublishFlowCommand (13.2)
   POST /api/v1/flows/{id}/archive  ArchiveFlowCommand (13.2)
   GET /api/v1/flows/{familyId}/versions  GetFlowVersionHistoryQuery (14.2)
   POST /api/v1/flows/generate   GenerateFlowFromDescriptionCommand (13.2)
Flow Runs   POST /api/v1/flow-runs  TriggerFlowRunCommand (13.3)
   GET /api/v1/flow-runs   ListRecentFlowRunsQuery (14.3)
   GET /api/v1/flow-runs/{id} GetFlowRunByIdQuery (14.3)
   GET /api/v1/flow-runs/{id}/progress GetLiveRunProgressQuery (14.3)
   POST /api/v1/flow-runs/{id}/cancel  CancelFlowRunCommand (13.3)
   GET /api/v1/flow-runs/{id}/report   GetFlowReportQuery (14.4)
Modules  POST /api/v1/modules RegisterModuleCommand (13.4)
   GET /api/v1/modules  ListRegisteredModulesQuery (14.5)
   GET /api/v1/modules/{id}   GetModuleRegistrationQuery (14.5)
   GET /api/v1/modules/actions   GetAvailableModuleActionsQuery (14.2)
Reporting   GET /api/v1/reports/pass-rate-trend GetPassRateTrendQuery (14.4)
   GET /api/v1/reports/flake-rate   GetFlakeRateQuery (14.4)
   GET /api/v1/reports/failure-clusters   DrillDownFailureSignatureQuery (14.4)
Audit GET /api/v1/audit/export   ExportAuditLogQuery (14.4)
Identity POST /api/v1/users/{id}/roles AssignRoleCommand (13.5)
   DELETE /api/v1/users/{id}/roles/{roleId}  RevokeRoleCommand (13.5)
   GET /api/v1/users ListUsersQuery (14.6)
Triggers POST /api/v1/triggers   (Part III, Section 21.7's trigger-CRUD commands)
   POST /api/v1/webhooks/{triggerPath} Scheduler.OnWebhookReceived (Part III, Section 21.3)

This table is intentionally exhaustive and directly generated-from, not independently maintained against, the command/query catalog — Volume 5's API documentation tooling should treat this mapping as the source of truth, regenerating OpenAPI specs from the command/query type definitions themselves where feasible, rather than hand-maintaining two parallel descriptions that can drift.

30.3 Controller pattern — thin, no business logic
csharp
[ApiController]
[Route("api/v1/flows")]
public class FlowsController : ControllerBase
{
    private readonly ICommandDispatcher _commands;
    private readonly IQueryDispatcher _queries;

    [HttpPost("{id}/publish")]
    public async Task<ActionResult<FlowPublishResultDto>> Publish(Guid id, CancellationToken ct)
    {
        var result = await _commands.Send(new PublishFlowCommand(new FlowId(id), CurrentUser.Id), ct);
        return result.Succeeded ? Ok(result.ToDto()) : UnprocessableEntity(result.ToDto());
    }
}

Every controller action does exactly three things: construct a command or query from the request (mapping HTTP-layer concerns like route/query parameters into the strongly-typed Part II catalog types), dispatch it, and map the result to an HTTP response — no validation logic, no direct repository access, and no business rules of any kind live in a controller. This is a direct, deliberate consequence of Section 13.6 and 13.7's command-handler design (Part II) — the controller is not where "layer 1" or "layer 2" validation happens; it is purely a protocol-translation boundary.

30.4 Request/response conventions
Pagination: list endpoints accept ?page={n}&pageSize={n} and return a consistent PagedResult<T> envelope (items, totalCount, page, pageSize) — the same shape Part II, Section 14's PagedResult<T> DTOs already establish at the query layer, passed through unchanged rather than re-wrapped at the API layer.
Filtering: query-string parameters map directly to a query's optional filter fields (e.g., ?status=Published&search=checkout for ListFlowsQuery).
Idempotency: state-changing POST endpoints that are safe to retry (specifically TriggerFlowRunCommand) accept an optional Idempotency-Key header; a repeated request with the same key within a defined window returns the original result rather than triggering a second execution — directly relevant to Persona Marcus's CI use case, where a flaky network causing a retried API call must never accidentally trigger a flow run twice.
Content negotiation: JSON is the only supported representation for GA; the CI-native report formats (FR-REP-2 — JUnit XML) are served via a distinct Accept: application/xml negotiation on the report endpoint specifically, not as a general API-wide content-type concern.
30.5 Error response format
json
{
  "type": "https://docs.nexus.dev/errors/flow-publish-validation-failed",
  "title": "Flow could not be published",
  "status": 422,
  "detail": "Step 'Fetch order status' references action 'api.request' which is not registered.",
  "errors": [
    { "field": "steps[2].action", "code": "unresolved_module_action" }
  ],
  "correlationId": "c4a1f6e2-..."
}

Follows the RFC 7807 Problem Details standard, extended with a correlationId (Part IV, Section 28.4) so a customer reporting an API error can hand Nexus support a single identifier that ties directly back to the full, correlated internal trace/log data for that request — closing the loop between external-facing error reporting and internal observability (Part IV, Section 29) deliberately, not as an afterthought.

30.6 API versioning

The v1 path segment (30.2) is Nexus's API versioning mechanism — a breaking change to any endpoint's contract requires a new version segment (v2), with both versions served concurrently for a defined deprecation window (mirroring the AutomationModule contract versioning discipline already established in Part III, Section 18.7, applied here to the public API surface specifically). This is a direct, load-bearing application of Architecture Principle 20.2 (contract stability) to the API layer — Persona Marcus's CI integrations (Volume 0, UC-4) depend on this API not breaking underneath a working pipeline without warning.

30.7 Rate limiting and quota

Beyond the authentication-specific rate limiting already established in Part IV, Section 24.9, the general API surface applies tenant-scoped rate limits (requests per minute, tiered by licensing level per Volume 0 Section 16.5) to prevent any single tenant's usage pattern from degrading the shared, pooled hosted infrastructure's service quality for other tenants (Part IV, Section 26.2's pooled-multi-tenancy tradeoff made concrete here) — enforced via the same Redis-backed infrastructure already used elsewhere (Volume 0, Section 21.9).

30.8 Where SignalR fits relative to this REST surface

This section's endpoints are entirely request/response. Live, push-based updates (flow-run progress streaming) are explicitly out of this section's scope and are instead served by the SignalR hub specified next, in Section 31 — the two mechanisms are complementary, not overlapping: GET /api/v1/flow-runs/{id}/progress (this section) provides the initial/catch-up state; the SignalR connection (Section 31) provides the ongoing stream, exactly matching the design already established in Part II, Section 14.3's GetLiveRunProgressQuery documentation.



31. SignalR
31.1 Purpose

This section formalizes the SignalR real-time layer already referenced conceptually across Part I (Section 4.5's communication patterns), Part III (Section 22.4's RunProgressNotifier), and Part V, Section 30.8. Its sole responsibility is streaming live flow-run progress to connected GUI clients — it is not a general-purpose real-time messaging layer for arbitrary application features, a scope boundary stated explicitly to prevent this component from accumulating unrelated responsibilities over time.

31.2 Hub design
csharp
[Authorize]
public class RunProgressHub : Hub
{
    public async Task SubscribeToRun(Guid runId, CancellationToken ct)
    {
        var run = await _authz.EnsureCanView(Context.User, new FlowRunId(runId), ct);  // Part IV, Section 25
        await Groups.AddToGroupAsync(Context.ConnectionId, runId.ToString());
    }

    public async Task UnsubscribeFromRun(Guid runId)
    {
        await Groups.RemoveFromGroupAsync(Context.ConnectionId, runId.ToString());
    }
}

Consistent with Part IV's authorization discipline (Section 25.3): subscribing to a run's progress stream requires the same ReportView permission (or resource-level check, Section 25.4) as querying that run's report via REST — SignalR subscriptions are not a side-channel that bypasses the standard authorization pipeline, a point worth stating explicitly since real-time/WebSocket layers are a common place for authorization checks to be accidentally weaker than their REST counterparts.

31.3 Group-per-run scoping

As already established in Part III, Section 22.4: clients join a SignalR group keyed by FlowRunId, ensuring a client watching one run's progress never receives events for other concurrent runs — even other runs belonging to the same tenant. This is a deliberate, minimal-blast-radius design: a GUI rendering a single run's live progress view has no need for a tenant-wide event firehose, and scoping at the group level keeps both server-side fan-out cost and client-side message-handling logic proportional to what's actually being watched.

31.4 Connection lifecycle and reconnection
csharp
public class RunProgressConnection
{
    // Client-side (GUI, Volume 2's responsibility) connection lifecycle:
    // 1. Open SignalR connection, call SubscribeToRun(runId)
    // 2. On initial subscribe, immediately call GetLiveRunProgressQuery (REST,
    //    Part II Section 14.3) to fetch current state as of subscription time —
    //    closing any gap between "connection opened" and "first StepCompletedEvent
    //    received," per Section 30.8's REST-provides-catch-up, SignalR-provides-
    //    stream division of responsibility.
    // 3. On disconnect/reconnect (network blip, browser tab backgrounding), the
    //    client re-subscribes AND re-fetches via GetLiveRunProgressQuery again —
    //    never assumes no events were missed during the gap, since SignalR's
    //    automatic reconnection does not replay missed messages by default.
}

This reconnection design directly avoids a subtle class of bug: a naive implementation trusting SignalR's connection to always deliver every event exactly once would silently show stale progress after any network interruption, directly undermining the "live" promise that's the entire point of this component. The explicit re-fetch-on-reconnect pattern makes staleness self-healing within one round-trip, consistent with Architecture Principle 20.4's fail-informatively spirit applied to real-time UI state specifically.

31.5 Scaling SignalR across multiple Orchestration Engine instances

Per NFR-SCALE-2 (horizontal scaling by adding workers): a StepCompletedEvent (Part II, Section 12.3) might be raised by any one of several horizontally-scaled Orchestration Engine worker instances (Part I, Section 4.4's independent-scaling rationale), while the SignalR client connection for the GUI watching that run might be held by a different instance of the API Layer container entirely. This requires a SignalR backplane — Redis-backed (reusing the same Redis infrastructure from Volume 0, Section 21.9, rather than introducing a separate technology purely for this purpose) — so that a group message sent from any worker instance correctly reaches any client connection held by any API Layer instance, transparent to both the publishing and receiving code shown in Sections 22.4 and 31.2.

31.6 Message payload size and throttling

StepCompletedEvent's full StepResult payload (Part II, Section 12.3) is sent as-is for GA, but with an explicit size guard: if a step's Data field (Part II, Section 10.3) exceeds a configured threshold (e.g., a very large API response body captured by the API module), the SignalR payload truncates Data and includes a flag directing the client to fetch the full value via a separate REST call (GetFlowRunByIdQuery, Section 30.2) rather than pushing arbitrarily large payloads through the real-time channel — protecting both connection throughput and the GUI's rendering responsiveness from an unusually large single step result, without limiting what the underlying StepResult can legitimately store.

31.7 What SignalR is deliberately not used for

Restating and slightly expanding 31.1's scope boundary, since it matters for future-proofing: notifications unrelated to an actively-watched flow run (e.g., a general "your scheduled flow just failed" alert while the user isn't looking at that run's progress view) are explicitly not this hub's responsibility — those belong to a separate notification mechanism (webhook delivery, per Part III Section 21's CI-integration use case, or a future in-app notification feature) that is out of this section's scope. Keeping RunProgressHub narrowly scoped to "progress of a run the client has explicitly subscribed to" keeps its authorization model (31.2), scaling model (31.5), and lifecycle (31.4) all simple enough to reason about completely — a direct application of Architecture Principle 20.7's composable-primitives preference, resisting the temptation to grow one real-time channel into a general-purpose pub/sub bus for every future feature.




32. Database Design
32.1 Purpose and scope

This section specifies the concrete PostgreSQL schema realizing Part II's domain model (Sections 9–11) and Part IV's multi-tenancy (Section 26) and audit (Section 27) requirements — the schema Part III, Section 15's FlowMapper-style mapping layers translate to and from. Per Part II, Section 6.5's rigor-versus-specificity guidance, table definitions here are complete enough to be directly implementable, while acknowledging that final column sizing/type tuning is normal implementation-phase engineering judgment.

32.2 Core schema — Flow Authoring context
sql
CREATE TABLE flows (
    id              UUID PRIMARY KEY,
    family_id       UUID NOT NULL,
    version         INT NOT NULL,
    name            VARCHAR(200) NOT NULL,
    description     TEXT,
    status          VARCHAR(20) NOT NULL,   -- Draft | Published | Archived
    tenant_id       UUID NOT NULL REFERENCES tenants(id),
    declared_vars   JSONB NOT NULL DEFAULT '{}',
    created_by      UUID NOT NULL REFERENCES users(id),
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    published_at    TIMESTAMPTZ,
    UNIQUE (family_id, version)
);
CREATE INDEX idx_flows_tenant_status ON flows (tenant_id, status);
CREATE INDEX idx_flows_family ON flows (family_id);

CREATE TABLE flow_steps (
    id              UUID PRIMARY KEY,
    flow_id         UUID NOT NULL REFERENCES flows(id) ON DELETE CASCADE,
    step_order      INT NOT NULL,
    name            VARCHAR(200) NOT NULL,
    module_kind     VARCHAR(50) NOT NULL,   -- Part II, Section 11.2's open ModuleKind value
    action          VARCHAR(100) NOT NULL,
    params          JSONB NOT NULL DEFAULT '{}',
    save_as         VARCHAR(100),
    when_condition  TEXT,
    UNIQUE (flow_id, step_order)
);
CREATE INDEX idx_flow_steps_flow ON flow_steps (flow_id);

declared_vars and params are JSONB, directly reflecting Part II Section 6.4's observation that FlowStep.Params is deliberately weakly-typed at the domain-model layer — PostgreSQL's JSONB gives this flexibility efficient storage and, critically, indexability (32.6) without forcing a rigid, per-module-action-specific relational schema that would need to change every time a new module or action is registered.

32.3 Core schema — Execution context
sql
CREATE TABLE flow_runs (
    id              UUID PRIMARY KEY,
    flow_id         UUID NOT NULL REFERENCES flows(id),  -- pinned exact version, Part II Section 9.3
    status          VARCHAR(20) NOT NULL,   -- Pending | Running | Passed | Failed | Cancelled
    trigger_kind    VARCHAR(20) NOT NULL,   -- Api | Scheduled | Webhook
    variables       JSONB NOT NULL DEFAULT '{}',
    tenant_id       UUID NOT NULL REFERENCES tenants(id),
    started_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    finished_at     TIMESTAMPTZ
);
CREATE INDEX idx_flow_runs_tenant_status ON flow_runs (tenant_id, status);
CREATE INDEX idx_flow_runs_flow ON flow_runs (flow_id, started_at DESC);  -- supports
    -- ListRecentFlowRunsQuery's FlowIdFilter (Part II, Section 14.3) ordered by recency

CREATE TABLE step_results (
    id                  UUID PRIMARY KEY,
    flow_run_id         UUID NOT NULL REFERENCES flow_runs(id) ON DELETE CASCADE,
    source_step_id      UUID NOT NULL,
    step_order          INT NOT NULL,
    step_name           VARCHAR(200) NOT NULL,   -- denormalized, Part II Section 10.3.1
    module_kind         VARCHAR(50) NOT NULL,
    action              VARCHAR(100) NOT NULL,
    status              VARCHAR(10) NOT NULL,    -- Passed | Failed | Skipped
    duration_ms         INT NOT NULL,
    message             TEXT,
    error               TEXT,
    data                JSONB,
    ai_attribution      JSONB,   -- null unless AI materially altered this step, Part II Section 11.5
    UNIQUE (flow_run_id, step_order)
);
CREATE INDEX idx_step_results_run ON step_results (flow_run_id, step_order);
CREATE INDEX idx_step_results_module_status ON step_results (module_kind, status);  -- feeds
    -- DrillDownFailureSignatureQuery (Part II, Section 14.4)

CREATE TABLE retry_attempts (
    id                  UUID PRIMARY KEY,
    step_result_id      UUID NOT NULL REFERENCES step_results(id) ON DELETE CASCADE,
    attempt_number      INT NOT NULL,
    status              VARCHAR(10) NOT NULL,
    duration_ms         INT NOT NULL,
    error               TEXT,
    attempted_at        TIMESTAMPTZ NOT NULL
);
32.4 Core schema — Module & Plugin context
sql
CREATE TABLE module_registrations (
    id                  UUID PRIMARY KEY,
    module_kind         VARCHAR(50) NOT NULL,
    display_name        VARCHAR(200) NOT NULL,
    trust_tier          VARCHAR(20) NOT NULL,   -- BuiltIn | Verified | Community, Part II Section 11.9
    contract_version    VARCHAR(20) NOT NULL,   -- semantic version, Part III Section 18.7
    is_active           BOOLEAN NOT NULL DEFAULT true,
    tenant_id           UUID REFERENCES tenants(id),  -- NULL for global BuiltIn/Verified modules,
                                                          -- populated for tenant-scoped Community
                                                          -- installs, per Part IV Section 26.5
    UNIQUE (module_kind, tenant_id)
);

CREATE TABLE module_action_entries (
    id                      UUID PRIMARY KEY,
    module_registration_id  UUID NOT NULL REFERENCES module_registrations(id) ON DELETE CASCADE,
    action_name             VARCHAR(100) NOT NULL,
    schema                  JSONB NOT NULL,   -- ActionSchema, Part II Section 11.8
    is_deprecated           BOOLEAN NOT NULL DEFAULT false,
    deprecation_notice      TEXT,
    UNIQUE (module_registration_id, action_name)
);
32.5 Core schema — Identity, Access & Audit
sql
CREATE TABLE tenants (
    id              UUID PRIMARY KEY,
    name            VARCHAR(200) NOT NULL,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE users (
    id                  UUID PRIMARY KEY,
    email               VARCHAR(320) NOT NULL,
    display_name        VARCHAR(200) NOT NULL,
    tenant_id           UUID NOT NULL REFERENCES tenants(id),
    is_service_account  BOOLEAN NOT NULL DEFAULT false,
    role_version        INT NOT NULL DEFAULT 1,   -- Part IV, Section 24.6
    UNIQUE (tenant_id, email)
);

CREATE TABLE roles (
    id                  UUID PRIMARY KEY,
    name                VARCHAR(100) NOT NULL,
    tenant_id           UUID NOT NULL REFERENCES tenants(id),
    is_system_defined   BOOLEAN NOT NULL DEFAULT false,
    permissions         TEXT[] NOT NULL   -- array of Permission enum values, Part IV Section 25.1
);

CREATE TABLE user_roles (
    user_id     UUID NOT NULL REFERENCES users(id),
    role_id     UUID NOT NULL REFERENCES roles(id),
    assigned_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    PRIMARY KEY (user_id, role_id)
);

CREATE TABLE audit_entries (
    id              UUID PRIMARY KEY,
    occurred_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    actor_id        UUID NOT NULL REFERENCES users(id),
    action_type     VARCHAR(100) NOT NULL,
    target_type     VARCHAR(100) NOT NULL,
    target_id       VARCHAR(100) NOT NULL,
    metadata        JSONB NOT NULL DEFAULT '{}',
    tenant_id       UUID NOT NULL REFERENCES tenants(id)
);
CREATE INDEX idx_audit_tenant_time ON audit_entries (tenant_id, occurred_at DESC);
-- No UPDATE or DELETE grant on this table for the application role, per Part IV Section 27.5
32.6 Indexing strategy against NFR-SCALE-3

NFR-SCALE-3 (Volume 0) targets 10 million stored flow-run records without an architecture change. The indexing choices above are made specifically with this target in mind: idx_flow_runs_tenant_status and idx_flow_runs_flow are both composite, leading with the highest-selectivity, most commonly-filtered columns (tenant_id, then flow_id) so that the overwhelming majority of query patterns (list runs for a tenant, list runs for a specific flow) hit an index directly rather than scanning. JSONB columns (params, data, metadata) additionally support GIN indexes where a specific query pattern demands it (e.g., if DrillDownFailureSignatureQuery's error-signature clustering, Part II Section 14.4, needs to filter on a specific field within step_results.data at scale) — not applied blanket by default, since GIN indexes carry real write-amplification cost, and should be added deliberately per proven query pattern during implementation, not speculatively for every JSONB column up front.

32.7 Row-level security policies

Directly implementing Part IV, Section 26.3's layer 2:

sql
ALTER TABLE flows ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON flows
    USING (tenant_id = current_setting('app.current_tenant_id')::UUID);
-- Repeated identically for flow_runs, module_registrations (where tenant_id IS NOT NULL),
-- users, roles, audit_entries, and every other multi-tenant table.
32.8 Migration strategy

Schema migrations are managed via EF Core Migrations, applied through a controlled deployment step (never auto-applied on application startup in production, to keep schema changes an explicit, reviewable, rollback-capable operation distinct from a routine deployment) — consistent with Architecture Principle 20.8's self-hosted-first posture, since an uncontrolled auto-migration is a materially riskier default for a self-hosted customer's production database than for a Nexus-operated hosted environment. Full migration tooling and rollout procedure is detailed in Volume 4 (Infrastructure) and Volume 5 (deployment checklist).

32.9 Forward reference

Section 33 renders this schema as a formal ER diagram; Sections 34–36 (Sequence, Class, Package diagrams) show this schema and the domain model from Part II operating together across concrete runtime scenarios and codebase organization.



33. ER Diagrams
33.1 Purpose

This section renders Section 32's schema as a formal entity-relationship diagram, grouped by bounded context (Part II, Section 7) for readability, consistent with this volume's practice of always tying a visual artifact back to the conceptual structure that motivates it rather than presenting schema in isolation.

33.2 Flow Authoring & Execution contexts
┌────────────────────┐          ┌────────────────────┐
│      tenants          │          │       users           │
├────────────────────┤          ├────────────────────┤
│ PK id                 │◄────────┤ FK tenant_id           │
│    name                │    1  n │ PK id                 │
└────────────────────┘          │    email               │
         ▲                        │    is_service_account   │
         │ 1                       │    role_version         │
         │ n                       └────────────────────┘
┌────────────────────┐                      ▲
│       flows           │                      │ created_by
├────────────────────┤                      │
│ PK id                 │──────────────────────┘
│    family_id           │
│    version              │          ┌────────────────────┐
│    status                │          │    flow_steps         │
│ FK tenant_id            │  1     n  ├────────────────────┤
│    declared_vars        │◄─────────┤ FK flow_id             │
└─────────┬──────────┘          │ PK id                 │
          │ 1                       │    step_order           │
          │ n (pinned version)      │    module_kind          │
          ▼                        │    action               │
┌────────────────────┐          │    params (jsonb)        │
│     flow_runs         │          └────────────────────┘
├────────────────────┤
│ PK id                 │          ┌────────────────────┐
│ FK flow_id             │  1     n  │    step_results        │
│    status                │◄─────────┤ FK flow_run_id         │
│    trigger_kind          │          │ PK id                 │
│ FK tenant_id            │          │    source_step_id       │
│    variables (jsonb)     │          │    step_order           │
└────────────────────┘          │    status                │
                                    │    ai_attribution (jsonb)│
                                    └─────────┬──────────┘
                                              │ 1
                                              │ n
                                              ▼
                                    ┌────────────────────┐
                                    │   retry_attempts       │
                                    ├────────────────────┤
                                    │ PK id                 │
                                    │ FK step_result_id      │
                                    │    attempt_number       │
                                    └────────────────────┘
33.3 Module & Plugin context
┌────────────────────┐          ┌────────────────────┐
│      tenants          │          │ module_registrations   │
├────────────────────┤  0..1   n ├────────────────────┤
│ PK id                 │◄─────────┤ FK tenant_id (nullable) │
└────────────────────┘  (null =  │ PK id                 │
                          global)  │    module_kind          │
                                    │    trust_tier            │
                                    │    contract_version      │
                                    └─────────┬──────────┘
                                              │ 1
                                              │ n
                                              ▼
                                    ┌────────────────────┐
                                    │ module_action_entries  │
                                    ├────────────────────┤
                                    │ PK id                 │
                                    │ FK module_registration_id│
                                    │    action_name          │
                                    │    schema (jsonb)        │
                                    │    is_deprecated         │
                                    └────────────────────┘
33.4 Identity, Access & Audit context
┌────────────────────┐          ┌────────────────────┐
│       users           │  n    n  │       roles            │
├────────────────────┤◄────────►├────────────────────┤
│ PK id                 │  via     │ PK id                 │
│ FK tenant_id            │ user_roles│ FK tenant_id           │
│    email                │          │    permissions (text[])  │
└─────────┬──────────┘          │    is_system_defined     │
          │                        └────────────────────┘
          │  1                    ┌────────────────────┐
          │  n                     │     user_roles        │
          └───────────────────────►├────────────────────┤
                                    │ PK,FK user_id           │
                                    │ PK,FK role_id           │
                                    │    assigned_at          │
                                    └────────────────────┘

┌────────────────────┐
│    audit_entries       │
├────────────────────┤
│ PK id                 │
│ FK actor_id  ──────────┼──► users.id
│    action_type          │
│    target_type          │
│    target_id             │
│ FK tenant_id            │──► tenants.id
│    metadata (jsonb)      │
└────────────────────┘
   (append-only: no UPDATE/DELETE grant, Part IV Section 27.5)
33.5 Cross-context relationship summary

The three diagrams above are deliberately drawn separately (per bounded context, Part II Section 7) rather than as one dense combined diagram, because — per the context-mapping discipline established there — the only relationships that legitimately cross context boundaries at the data level are the shared-kernel identifier references (tenant_id, created_by/actor_id referencing users.id, Part II Section 7.4). Every other relationship shown is strictly intra-context. A combined diagram would visually suggest tighter coupling between, say, flow_runs and module_registrations than actually exists at the schema level (they relate only indirectly, through the module_kind string value matched at query time by the Module Dispatcher, Part I Section 5.3 — never via a foreign key, since a FlowStep's module_kind may reference a module not yet registered at authoring time, per the publish-time-only validation established in Part II Section 9.2).

33.6 Notation key

PK = primary key, FK = foreign key, cardinality notation (1, n, 0..1) follows standard crow's-foot convention simplified for ASCII rendering. Dashed versus solid relationship lines are not distinguished in this ASCII format; all relationships shown are enforced foreign-key constraints (Section 32) unless explicitly annotated otherwise (as in 33.5's module_kind string-matched, non-FK relationship).




34. Sequence Diagrams
34.1 Purpose

This section traces three representative scenarios end-to-end through every layer specified across Parts I–V, chosen specifically because each exercises a different, architecturally significant path: a straightforward command (publish), a full execution with live streaming, and the AI self-healing flow with its permission-inheritance and transparency requirements.

34.2 Sequence — Publish a Flow (Volume 0, Persona Priya)
Priya      GUI        API Layer    PublishFlowCmd    Flow          ModuleActionResolver   FlowRepo   UnitOfWork   AuditPipeline
 │          │              │            Handler        │                    │                │            │              │
 │ click    │              │              │             │                    │                │            │              │
 │ Publish  │──POST /flows/{id}/publish──►│             │                    │                │            │              │
 │          │              │──EnsurePermission(FlowPublish)──► [Part IV §25] │                │            │              │
 │          │              │──dispatch──►│             │                    │                │            │              │
 │          │              │             │──GetById──────────────────────────────────────────►│            │              │
 │          │              │             │◄──Flow (Draft)──────────────────────────────────────│            │              │
 │          │              │             │──flow.Publish(resolver)──►│                    │                │              │
 │          │              │             │                          │──resolve each step──►│                │              │
 │          │              │             │                          │◄──all resolved OK────│                │              │
 │          │              │             │◄──FlowPublishResult(Succeeded)──│                │                │              │
 │          │              │             │──Save(flow)──────────────────────────────────────►│                │              │
 │          │              │             │──Commit()──────────────────────────────────────────────────────────►│              │
 │          │              │             │                                                                    │──write outbox──►
 │          │              │             │                                                                    │  + FlowPublishedEvent
 │          │              │             │                                                                    │──dispatch (Tier 1)──►│
 │          │              │             │                                                                                          │──AuditEntry
 │          │              │◄──200 OK, FlowPublishResultDto──│                                                                          (async)
 │◄─────────│◄─────────────│                                 
 │  sees "Published"

Notes on this trace: the ModuleActionResolver call (Section 9.5 of Part II) is a synchronous read, not a separate transaction — the whole publish operation is one atomic Unit of Work commit. The audit entry (Part IV, Section 27) is written durably within that same commit via the outbox (Part III, Section 16.4), but its dispatch to the audit pipeline consumer happens asynchronously afterward — Priya's GUI receives its 200 OK without waiting for that final async step, since nothing about her request's success depends on the audit pipeline having finished processing yet.

34.3 Sequence — Trigger and watch a Flow Run (Volume 0, UC-1 cross-domain regression)
Marcus'CI   API Layer   TriggerFlowRunCmd   FlowEngine    ModuleDispatcher   WebModule    ApiModule    LogsModule   EventBus   SignalR    GUI(Priya, separately watching)
   │           │              Handler          │                │                │            │            │           │          │              │
   │─POST /flow-runs────────►│                │                │                │            │            │           │          │              │
   │           │──dispatch──►│                │                │                │            │            │           │          │              │
   │           │             │──create FlowRun (Pending)                                                                                          │
   │           │◄──202 Accepted, FlowRunId───│                                                                                                     │
   │                          │──FlowEngine.Execute(flow, run)──►│                                                                                  │
   │                                                             │──dispatch step1 (web)──────►│                                                    │
   │                                                             │                              │──navigate,fill,click──► [Playwright]               │
   │                                                             │                              │◄──StepOutcome(Passed, data:{orderId})               │
   │                                                             │──RecordStepResult──► raises StepCompletedEvent ──────────────────────►│           │
   │                                                             │                                                                        │──push──►│──renders step 1 ✓
   │                                                             │──bind saveAs "orderInfo"──►                                                        │
   │                                                             │──dispatch step2 (api, using {{vars.orderInfo.id}})──────►│                          │
   │                                                             │                                          │◄──StepOutcome(Passed)                   │
   │                                                             │──RecordStepResult──► raises StepCompletedEvent ──────────────────────►│──push──►│──renders step 2 ✓
   │                                                             │──dispatch step3 (logs)────────────────────────────►│                              │
   │                                                             │                                                    │◄──StepOutcome(Passed)         │
   │                                                             │──RecordStepResult──► raises StepCompletedEvent ──────────────────────►│──push──►│──renders step 3 ✓
   │                                                             │──run.Complete()──► raises FlowRunCompletedEvent ─────────────────────►│──push──►│──renders "Passed"
   │                                                             │──UnitOfWork.Commit()

Notes: this trace is the concrete, full-system realization of Volume 0's UC-1 — three different modules (Web, API, Logs) dispatched in sequence within one FlowRun, with {{vars.orderInfo.id}} interpolation (Part III, Section 19.4) carrying data from step 1's result into step 2's parameters. Priya's GUI, watching via SignalR (Section 31) in parallel with Marcus's CI-triggered execution, receives live updates through the exact mechanism specified in Part III Section 22.4 and Part V Section 31 — this diagram is where those two previously-separately-specified components are shown actually operating together in one realistic scenario.

34.4 Sequence — Selector self-heal (Volume 0, UC-3, AI transparency requirement)
FlowEngine   WebModule   ModuleDispatcher   AiModule    LLM Provider   StepResult    EventBus    AuditPipeline
    │            │               │              │             │            │             │              │
    │──dispatch step (web, click)──►│           │              │            │             │              │
    │            │──selector not found (broken)──►             │            │             │              │
    │◄───────────│  StepOutcome(Failed, error:"selector not found")         │             │              │
    │  [self-healing enabled for this flow, per Volume 0 UC-3]              │             │              │
    │──issue RequestSelectorHealCommand──────────────►│                     │             │              │
    │            │                                    │──EnsurePermission (inherits triggering identity, Part IV §25.6)
    │            │                                    │──call with broken selector, intent, page HTML────►│
    │            │                                    │◄──proposed selector, confidence:0.87──────────────│
    │            │                                    │  [confidence exceeds configured threshold]
    │◄───────────────────────────────────────────────│  SelectorHealResult(newSelector, confidence:0.87)
    │──retry dispatch with healed selector──►│         
    │            │──click succeeds──►         
    │◄───────────│  StepOutcome(Passed)
    │──build StepResult WITH AiAttribution attached (Part II §11.5)─────►│
    │──RecordStepResult──► raises StepCompletedEvent AND StepSelfHealedEvent ──►│
    │                                                                            │──durable relay──►│──AuditEntry
    │                                                                                                    (records the heal,
    │                                                                                                     FR-AI-3 compliance)

Notes: this is the trace that makes FR-AI-3's "every AI-assisted intervention recorded distinguishably" concrete and verifiable — StepSelfHealedEvent (Part II, Section 12.3) fires in addition to StepCompletedEvent, never instead of it, and the resulting StepResult's non-null AiAttribution field (Part II, Section 11.5) is what a report (Part II, Section 8.5) or the audit trail (Part IV, Section 27) surfaces to a human reviewing this run later — nothing about this heal is silent, per Product Philosophy 4.4's "AI proposes, deterministic code disposes, and never silently."

34.5 Reading these diagrams alongside Parts I–IV

Each diagram above deliberately names the exact component and section from earlier in this volume responsible for each step, rather than presenting an idealized flow disconnected from the rest of this document set — the intent is that a reader arriving at this section after reading Parts I–IV in order recognizes every actor in these traces as an already-specified component, and a reader arriving here first can use these traces as a map pointing back to where each piece is fully specified.



35. Class Diagrams
35.1 Purpose

This section renders Part II's domain model (Sections 9–11) as a formal UML-style class diagram, showing aggregate boundaries, composition versus association relationships, and the plugin contract's position relative to the domain model — the visual counterpart to Part II's prose-and-code-sketch specification.

35.2 Flow Authoring & Execution aggregates
┌───────────────────────────┐
│      Flow «AggregateRoot»    │
├───────────────────────────┤
│ + FamilyId: FlowFamilyId     │
│ + Version: int                │
│ + Name: string                 │
│ + Status: FlowStatus           │
│ + TenantId: TenantId           │
├───────────────────────────┤
│ + AddStep(step)                │
│ + Publish(resolver): Result     │
└─────────────┬─────────────┘
              │ 1
              │ composes
              │ n
┌─────────────▼─────────────┐        ┌───────────────────────┐
│   FlowStep «Entity»          │◄───────┤  VariableExpression       │
├───────────────────────────┤  uses  │      «ValueObject»          │
│ + Name: string                 │  0..n  ├───────────────────────┤
│ + Module: ModuleKind            │        │ + RawTemplate: string     │
│ + Action: string                │        │ + ReferencedPaths: [string]│
│ + Params: dict                  │        └───────────────────────┘
│ + SaveAs: string?                │
│ + WhenCondition: string?         │
│ + Order: int                     │
└───────────────────────────┘

┌───────────────────────────┐
│    FlowRun «AggregateRoot»   │
├───────────────────────────┤
│ + FlowId: FlowId (pinned)      │
│ + Status: RunStatus             │
│ + Trigger: RunTrigger            │
│ + Variables: dict                │
│ + TenantId: TenantId             │
├───────────────────────────┤
│ + RecordStepResult(result)       │
│ + Complete()                      │
└─────────────┬─────────────┘
              │ 1
              │ composes
              │ n
┌─────────────▼─────────────┐        ┌───────────────────────┐
│  StepResult «Entity»          │◄───────┤  AiAttribution              │
├───────────────────────────┤  0..1  │     «ValueObject»           │
│ + SourceStepId: FlowStepId     │        ├───────────────────────┤
│ + Status: StepStatus            │        │ + Capability: string       │
│ + Duration: TimeSpan             │        │ + Confidence: double       │
│ + Data: object?                  │        │ + WasAccepted: bool        │
└─────────────┬─────────────┘        └───────────────────────┘
              │ 1
              │ composes
              │ n
┌─────────────▼─────────────┐
│ RetryAttempt «Entity»         │
├───────────────────────────┤
│ + AttemptNumber: int             │
│ + Status: StepStatus              │
└───────────────────────────┘
35.3 Module & Plugin — aggregate and contract, shown together
┌────────────────────────────┐          ┌─────────────────────────────┐
│ ModuleRegistration              │          │      IAutomationModule           │
│    «AggregateRoot»               │          │        «Interface»                │
├────────────────────────────┤ implements├─────────────────────────────┤
│ + Kind: ModuleKind                 │◄─ ─ ─ ─ ─│ + Kind: ModuleKind               │
│ + Trust: TrustTier                  │  (the    │ + ContractVersion: SemVer       │
│ + ContractVersion: SemVer           │  registered│ + GetSupportedActions()        │
├────────────────────────────┤  entity's│ + Setup(ctx)                     │
│ + composes n ActionEntry             │  Kind must│ + Run(action, params, ctx)      │
└────────────────────────────┘  match its│ + Teardown(ctx)                 │
              │ 1                         implementation's                └─────────────────────────────┘
              │ composes                  Kind, enforced                              △
              │ n                         at registration,                            │ implements
┌─────────────▼─────────────┐  Part III §18.6)          ┌──────┼──────┬──────┬──────┬──────┐
│ ModuleActionEntry «Entity»    │                         │      │      │      │      │      │
├───────────────────────────┤                        ┌───▼──┐┌─▼───┐┌─▼───┐┌─▼────┐┌─▼───┐┌─▼──┐
│ + ActionName: string           │                         │ApiMod││WebMod││MobMod││DeskUi││Logs││ AiMod│
│ + Schema: ActionSchema          │                         └──────┘└─────┘└─────┘└──────┘└─────┘└────┘
│ + IsDeprecated: bool             │                          Built-in modules (Part III §18); marketplace-
└───────────────────────────┘                          installed modules implement the same interface
                                                              (Community/Verified tier, §18.5)

The dashed "implements" line at the top of this diagram is the single most important relationship on this page: ModuleRegistration (a Part II domain aggregate, persisted in the module_registrations table, Section 32.4) and IAutomationModule (a Part III plugin-contract interface, implemented by actual running code) are two different things describing the same conceptual module from two different angles — the aggregate is data about a module; the interface is behavior a module provides. Keeping these visually and architecturally distinct (rather than merging them into one type) is what makes the Module Registration API's introspection (Part I, Section 5.3) possible without needing to instantiate or execute a module just to describe it.

35.4 Cross-aggregate relationships (association, not composition)
Flow ─ ─ ─ (referenced by FlowId, immutable pin) ─ ─ ─► FlowRun
FlowStep ─ ─ ─ (Module+Action resolved against, NOT a foreign-key/composition relationship) ─ ─ ─► ModuleRegistration
StepResult ─ ─ ─ (AiAttribution references) ─ ─ ─► (no direct link back to AiModule instance — only the value object)
User ─────(shared-kernel reference, Part II §7.4)────► every aggregate's TenantId/CreatedBy/ActorId field

Dashed arrows throughout this section denote association or reference relationships (holding an identifier, resolved at read or validation time) as distinct from the solid-line composition relationships in 35.2–35.3 (a parent aggregate directly owning and cascading lifecycle to its child entities). This distinction is not merely notational pedantry — it's the visual expression of Part III, Section 9.5's explicit rule that aggregates never directly reference one another's objects, only their identifiers, with resolution happening through an injected port at the application-service layer.

35.5 Value object cluster (selected, per Part II Section 11)
        ┌─────────────────┐
        │   Assertion         │
        │  «abstract record»  │
        └────────△────────┘
                  │
    ┌─────────────┼─────────────┬─────────────┐
    │             │             │             │
┌───▼────┐  ┌────▼─────┐  ┌───▼─────┐  ┌────▼──────┐
│ Status   │  │PathEquals │  │PathCont- │  │PathExists   │
│Assertion │  │Assertion  │  │ainsAssert│  │Assertion     │
└─────────┘  └──────────┘  └─────────┘  └────────────┘

This closed-hierarchy pattern (Part II, Section 11.6's "make illegal states unrepresentable" rationale) is shown here specifically because it's a recurring shape worth recognizing across the domain model — the same discriminated-union pattern applies equally to Assertion (shown here), and could be applied to any future closed set of mutually-exclusive value-object variants the domain model grows to need.

35.6 Relationship to Part VI's coding standards

The strongly-typed-identifier convention (Part II, Section 10.8), the aggregate/entity/value-object distinctions shown throughout this section, and the composition-versus-association discipline (35.4) are not merely descriptive here — Part VI, Section 40 (Coding Standards) elevates each of these into an enforced code-review checklist item, making this class diagram a direct, checkable specification of what correctly-structured Nexus domain code looks like, not just an illustrative aid.



36. Package Diagrams
36.1 Purpose

This section specifies the .NET solution's project/namespace structure — the concrete folder-and-assembly organization that every prior section's design maps onto. This is the direct bridge between this volume's architectural specification and Volume 5's coding standards, which will assume this structure as given.

36.2 Solution structure
Nexus.sln
│
├── src/
│   ├── Nexus.Domain/                    # Part II — pure domain model, zero external dependencies
│   │   ├── FlowAuthoring/                #   (per bounded context, Part II §7)
│   │   │   ├── Flow.cs, FlowStep.cs        (Section 9.2, 10.2)
│   │   │   └── FlowStatus.cs, VariableExpression.cs  (Section 11.4, 11.7)
│   │   ├── Execution/
│   │   │   ├── FlowRun.cs, StepResult.cs, RetryAttempt.cs  (Section 9.3, 10.3, 10.4)
│   │   │   └── RunStatus.cs, StepStatus.cs, AiAttribution.cs  (Section 11.3, 11.5)
│   │   ├── ModulePlugin/
│   │   │   ├── ModuleRegistration.cs, ModuleActionEntry.cs  (Section 9.4, 10.5)
│   │   │   └── ModuleKind.cs, ActionSchema.cs, TrustTier.cs  (Section 11.2, 11.8, 11.9)
│   │   ├── IdentityAccess/
│   │   │   └── User.cs, Role.cs  (Section 10.6, Part IV §25.1)
│   │   ├── ReportingAudit/
│   │   │   └── AuditEntry.cs  (Section 10.7)
│   │   └── SharedKernel/
│   │       ├── Ids.cs             # every strongly-typed identifier (Section 10.8, 11.10)
│   │       ├── AggregateRoot.cs, Entity.cs, IHasDomainEvents.cs
│   │       └── DomainException.cs
│   │
│   ├── Nexus.Contracts/                 # Part III §18 — the public AutomationModule interface
│   │   ├── IAutomationModule.cs, ExecutionContext.cs, StepOutcome.cs
│   │   └── (published as a standalone NuGet package — this is THE artifact a third-party
│   │        module developer, Persona Raj, actually references, per Part III §18.1's
│   │        emphasis on this contract's central importance — kept in its own project
│   │        specifically so it can be versioned and distributed independently of the
│   │        rest of the (closed-source, or open-core per Volume 0 §16.2) solution)
│   │
│   ├── Nexus.Application/               # Part II §13-14 — commands, queries, handlers
│   │   ├── Commands/                     #   organized by bounded context, mirroring Domain/
│   │   ├── Queries/
│   │   ├── EventHandlers/                # Part III §22.5's consumer implementations
│   │   └── Abstractions/                 # IUnitOfWork, I*Repository interfaces (Part III §15-16)
│   │
│   ├── Nexus.Infrastructure/             # Part III, Part IV, Part V §32 — concrete implementations
│   │   ├── Persistence/
│   │   │   ├── NexusDbContext.cs, migrations/
│   │   │   └── Repositories/              # FlowRepository, FlowRunRepository, etc. (§15.4)
│   │   ├── Modules/                      # built-in module implementations
│   │   │   ├── Api/, Web/, Mobile/, DesktopUi/, Logs/, Ai/
│   │   ├── Plugins/
│   │   │   ├── InProcessExecutionStrategy.cs, SandboxedExecutionStrategy.cs  (§18.5)
│   │   ├── Identity/                      # authentication providers, JWT issuance (Part IV §24)
│   │   ├── Observability/                 # OpenTelemetry setup (Part IV §29)
│   │   └── Messaging/                     # Event Bus Tier 1/2 implementation (§22.2)
│   │
│   ├── Nexus.Api/                        # Part V §30-31 — ASP.NET Core Web API host
│   │   ├── Controllers/
│   │   ├── Hubs/                          # RunProgressHub (§31.2)
│   │   └── Program.cs                      # DI composition root (Part III §17)
│   │
│   ├── Nexus.Orchestration/               # Part I §5, Part III §19-21 — the Orchestration
│   │   │                                     Engine container's own host process (separate
│   │   │                                     from Nexus.Api per Part I §4.4's container split)
│   │   ├── FlowEngine.cs, ModuleDispatcher.cs, ModuleRegistry.cs
│   │   ├── Scheduler.cs                    (§21)
│   │   └── Program.cs
│   │
│   └── Nexus.Cli/                        # the CLI client (Part I §2.2, Volume 0 FR-PLAT-2)
│
├── tests/
│   ├── Nexus.Domain.Tests/                # pure unit tests, no infrastructure dependency,
│   │                                         per Part III §15.5, §16.7's testability design
│   ├── Nexus.Application.Tests/
│   ├── Nexus.Infrastructure.IntegrationTests/
│   └── Nexus.Api.IntegrationTests/
│
└── plugins-sdk/
    └── Nexus.PluginSdk.Samples/          # a reference third-party module implementation,
                                              directly serving Volume 0 §15.6's "reference
                                              third-party module" Phase 4 roadmap milestone
36.3 Dependency direction — the core rule
Nexus.Api  ──────┐
Nexus.Orchestration ─┼──► Nexus.Infrastructure ──► Nexus.Application ──► Nexus.Domain
Nexus.Cli  ──────┘                                        △
                                                            │
                                                    Nexus.Contracts
                                             (referenced by Application
                                              and Infrastructure, but
                                              NOT by Domain — Domain has
                                              zero knowledge that a plugin
                                              contract even exists, per
                                              Part II §6.4's boundary)

Dependencies point strictly inward, standard Clean/Onion Architecture discipline: Nexus.Domain references nothing else in this solution (per Section 15.5's persistence-ignorance rule, extended here to a project-reference-level enforcement — a .csproj reference from Nexus.Domain to Nexus.Infrastructure is not just discouraged, it should fail CI via a dependency-direction analyzer, per Volume 5's forthcoming enforcement). Nexus.Contracts sits adjacent to, not inside, Nexus.Domain specifically because — as 36.2 notes — it needs independent versioning and distribution as a standalone package for third-party consumption, which would be awkward if it lived inside the same assembly as the rest of the (potentially not independently distributed) domain model.

36.4 Why Nexus.Api and Nexus.Orchestration are separate executable projects

Direct implementation of the container-separation decision already justified architecturally in Part I, Section 4.4: these are not just logical namespaces within one deployable but genuinely separate ASP.NET Core / .NET worker-service host projects, each with their own Program.cs composition root, independently deployable and independently scalable, both depending on the same shared Nexus.Infrastructure and Nexus.Application projects rather than duplicating logic between them.

36.5 Package-level testability mapping

Restating Part III, Section 15.5 and 16.7's testing-design goals in terms of this concrete structure: Nexus.Domain.Tests requires zero test-container/database setup (pure in-memory object construction and assertion against Part II's aggregate behavior); Nexus.Application.Tests uses mocked repository/Unit-of-Work interfaces to test command/query handler logic in isolation; only Nexus.Infrastructure.IntegrationTests and Nexus.Api.IntegrationTests require a real (or test-container-provisioned) PostgreSQL/Redis instance — this tiering directly informs Volume 5's CI pipeline design, where the fast, dependency-free domain and application test suites can run on every commit, while the slower integration suites run on a less frequent but still mandatory gate.

36.6 Closing note for Volume 1, Part V

This section completes the mapping from Part II's domain model, through Part III's engineering patterns, Part IV's cross-cutting services, and this Part's API/data specification, into an actual, buildable solution structure. Part VI (Deployment, Performance, Scalability, Coding Standards) is the final piece of Volume 1, taking this structure and specifying how it's built, tested, deployed, and maintained as an ongoing engineering practice.


___________________________________________________________________________________________________________________________________________________



37. Deployment
37.1 Purpose — from topology to concrete deployment artifacts

Volume 0, Section 19.5 established two deployment topologies (managed/hosted, self-hosted) and Section 21.12 locked Docker/Kubernetes/Helm as the deployment technology. Part I, Section 4 fixed the container boundaries. This section specifies the concrete deployment artifacts and operational procedures that realize those decisions for the solution structure fixed in Part V, Section 36.

37.2 Container images

Each independently-deployable project from Section 36.2 produces its own container image, built from a minimal multi-stage Dockerfile (SDK image for build, runtime-only image for the final artifact, keeping production image size and attack surface minimal — a direct, concrete application of Architecture Principle 20.5's security-by-design principle to the deployment artifact itself, not just application logic):

Image From project   Notes
nexus-api   Nexus.Api   Stateless, horizontally scalable per NFR-SCALE-2
nexus-orchestration  Nexus.Orchestration  Stateless at the process level (all durable state in PostgreSQL/Redis per Part III §16, §19.8), horizontally scalable
nexus-cli   Nexus.Cli   Distributed as a standalone binary/NuGet tool per Volume 0 §21.2, not typically deployed as a long-running container, but a container image is published for CI environments that prefer container-based tooling
nexus-sandbox-runner (a minimal runtime hosting IModuleExecutionStrategy's sandboxed side, Part III §18.5)  One ephemeral container instance per sandboxed Community-tier module execution, not a long-running service
37.3 Kubernetes topology (reference architecture)
┌─────────────────────────────────────────────────────────────────┐
│                         Kubernetes Cluster                          │
│                                                                     │
│  ┌───────────────┐   ┌───────────────┐   ┌────────────────┐     │
│  │  nexus-api        │   │ nexus-          │   │  nexus-           │     │
│  │  Deployment        │   │ orchestration    │   │  sandbox-runner    │     │
│  │  (HPA: 3-20 pods)  │   │ Deployment       │   │  Job/Pod (ephemeral,│     │
│  │                    │   │ (HPA: 2-15 pods) │   │  per Community-tier │     │
│  │                    │   │                  │   │  module invocation) │     │
│  └───────┬───────┘   └───────┬───────┘   └────────────────┘     │
│          │                     │                                     │
│          └──────────┬──────────┘                                     │
│                       │                                                │
│              ┌────────▼────────┐        ┌──────────────────┐       │
│              │  Redis (backplane, │        │  Outbox Relay        │       │
│              │  cache, distributed│        │  BackgroundService    │       │
│              │  locks — §21.4,    │        │  (runs within          │       │
│              │  §31.5)            │        │  nexus-orchestration,  │       │
│              └────────────────┘        │  not a separate pod)    │       │
│                                              └──────────────────┘       │
└─────────────────────────────────────────────────────────────────┘
          │                                          │
          ▼                                          ▼
   ┌─────────────┐                          ┌─────────────────┐
   │  PostgreSQL     │                          │  Observability       │
   │  (managed or     │                          │  Backend              │
   │  self-hosted per  │                          │  (OTLP → Prometheus/  │
   │  Volume 0 §21.8)  │                          │  Grafana or customer's │
   └─────────────┘                          │  own stack, §29)      │
                                                └─────────────────┘

Helm charts (one per image in 37.2, composed into an umbrella chart for the full platform) parameterize every environment-specific value (replica counts, resource limits, database/Redis connection details, AI-provider configuration per Volume 0 §21.7's provider-abstraction) — the self-hosted reference deployment uses the identical chart set as the managed/hosted environment, differing only in values files, directly implementing Architecture Principle 20.8's "self-hosted is a first-class target, not a downscoped version."

37.4 Horizontal Pod Autoscaling and NFR-SCALE-1

nexus-orchestration's HPA is configured against nexus.orchestration.concurrent_executions (Part IV, Section 29.4's custom metric, exposed via a Prometheus adapter feeding Kubernetes' custom-metrics API) rather than generic CPU/memory utilization alone — CPU-based autoscaling would react too late for a component whose actual bottleneck (concurrent flow-execution capacity, per NFR-SCALE-1's 500-execution target) is not always CPU-bound (a flow waiting on a slow target-system API response consumes minimal CPU while still occupying execution capacity). This is a direct, deliberate application of Part IV's observability design (Section 29) feeding back into the deployment layer's own scaling decisions, not two independently-designed systems.

37.5 Database migration procedure

Consistent with Part V, Section 32.8's "never auto-migrate on startup" rule: migrations run as a distinct Kubernetes Job, executed as an explicit step in the deployment pipeline before the new nexus-api/nexus-orchestration image versions are rolled out, with the Job's success gating the subsequent rollout — a failed migration halts deployment before any pod is running code that expects a schema the database doesn't yet have. Migrations are additionally required (per Volume 5's forthcoming coding-standards enforcement) to be backward-compatible for at least one deployment cycle (the old code version must still function against the new schema) to support zero-downtime rolling deployments, directly serving NFR-REL-1's 99.9% availability commitment through the deployment process itself, not only through runtime architecture.

37.6 Blue/green and canary considerations for the Orchestration Engine specifically

nexus-api, being stateless per-request, supports standard rolling/canary deployment with no special handling. nexus-orchestration requires one additional consideration: an in-flight FlowRun (Part II, Section 9.3) should not be abruptly terminated mid-execution by a pod being torn down during a rollout. The deployment configuration sets a generous terminationGracePeriodSeconds and the Nexus.Orchestration host implements graceful shutdown (stop accepting new flow-trigger dispatches, allow in-flight FlowEngine.Execute calls, Part III Section 19.2, to reach a natural step boundary before exiting) — a direct, deployment-level extension of the same CancellationToken propagation already threaded through the execution loop for the CancelFlowRunCommand use case (Part III, Section 19.2), reused here for graceful infrastructure-initiated shutdown rather than user-initiated cancellation.

37.7 Self-hosted installation path

For a self-hosted customer (Persona Elena's segment, Volume 0 Section 10.6), the installation procedure is: helm repo add, provide a values file specifying database/Redis endpoints and (optionally, per NFR-SEC-2/NFR-REL-3) AI-provider configuration or explicit AI-disable, and helm install. No outbound internet dependency is required for the core (non-AI) platform to function post-installation, per NFR-SEC-4 — a self-hosted deployment behind a fully air-gapped network boundary is a supported, tested configuration, not merely a theoretical one, with Volume 4's infrastructure documentation responsible for the fully detailed air-gapped installation procedure (image mirroring to a private registry, etc.) that this section only summarizes at the architectural level.

37.8 Rollback procedure

Every deployment is a Helm release with an associated revision; rollback is helm rollback to the prior revision, with the database-migration backward-compatibility guarantee from Section 37.5 being the specific property that makes this safe — a rollback that reverted application code to a version expecting an older schema, against a database that had already been migrated forward, would be a serious operational hazard were it not for that guarantee, so 37.5 and this section's rollback procedure should always be read and validated together, not independently.



38. Performance
38.1 Purpose — engineering practice, not restated targets

Volume 0, Section 13.2 already stated the performance NFRs (NFR-PERF-1 through NFR-PERF-4) as targets. This section specifies the concrete engineering practices across the architecture already defined in Parts I–V that are relied upon to actually hit those targets, and the measurement discipline that verifies they're being hit continuously, not just at initial launch.

38.2 NFR-PERF-1 (50ms p95 step-dispatch overhead) — where the budget is spent and protected

Referencing Part III, Section 19.4's design-time budget allocation, restated here as an operational practice: every release, the distributed tracing data (Part IV, Section 29.3) is used to compute actual p95 dispatch-overhead — measured as the span duration from FlowEngine's step-loop entry to the ModuleDispatcher.Dispatch call's start (i.e., excluding the module's own span) — as a mandatory CI/release-gate check (Volume 5's quality-gate design will consume this metric directly). A regression here is treated with the same severity as a functional test failure, not filed as a "performance backlog" ticket to address later — this reflects Architecture Principle 20.4's fail-fast philosophy applied to the release process itself.

38.3 NFR-PERF-2 (200ms p95 for flow CRUD) — the read-your-own-writes design payoff

Part V, Section 23.4's decision to keep FlowSummaryReadModel synchronously (same-transaction) updated, rather than event-projected, is the direct performance-relevant design choice underlying this NFR — a flow-list query never waits on eventual-consistency catch-up, and the write path's cost (an additional synchronous denormalized-table update within the same Unit of Work transaction, Part III Section 16) is a bounded, small addition to the already-required aggregate-save operation, not a separate round-trip.

38.4 NFR-PERF-3 (90-day trend view under 2s at p95) — the batch-aggregation tradeoff

Directly implemented by Part V, Section 23.5's TrendAggregateReadModel batch-computation design — the 2-second budget is achievable specifically because the dashboard query reads a small, pre-aggregated table rather than scanning potentially millions of step_results rows (Part V, Section 32.3) live. This section adds the operational discipline: the batch job's own execution time and staleness window (Section 23.6 of Part V) are themselves monitored metrics (extending Part IV, Section 29.4's table with a nexus.trend_aggregation.duration and nexus.trend_aggregation.staleness_seconds pair), since a silently-failing or increasingly-slow batch job would otherwise degrade this NFR's actual user-facing guarantee without any dashboard-query-side symptom to catch it.

38.5 NFR-PERF-4 (15s AI timeout, graceful fallback) — timeout enforcement at the call boundary
csharp
public class AiProviderClient
{
    private static readonly TimeSpan DefaultTimeout = TimeSpan.FromSeconds(15);  // NFR-PERF-4

    public async Task<T> Call<T>(Func<CancellationToken, Task<T>> providerCall, CancellationToken ct)
    {
        using var timeoutCts = CancellationTokenSource.CreateLinkedTokenSource(ct);
        timeoutCts.CancelAfter(DefaultTimeout);
        try
        {
            return await providerCall(timeoutCts.Token);
        }
        catch (OperationCanceledException) when (!ct.IsCancellationRequested)
        {
            _metrics.RecordAiTimeout();  // nexus.ai.request.timeout, Part IV §29.4
            throw new AiProviderTimeoutException();  // caught by the calling command handler,
                                                          // which falls back to normal (non-AI)
                                                          // failure handling per NFR-REL-3
        }
    }
}

This is the concrete implementation of the graceful-degradation promise already specified conceptually in Part III, Section 18 and Part IV — the timeout is enforced at the narrowest possible boundary (the actual outbound provider call), not by hoping some outer layer catches a slow AI response eventually.

38.6 Database query performance discipline

Beyond the indexing strategy already specified in Part V, Section 32.6: every query handler (Part II, Section 14.7) is required, per Volume 5's forthcoming coding standard, to have its generated SQL reviewed for index utilization before merge — a query plan showing a sequential scan against a table expected to grow toward NFR-SCALE-3's 10-million-row target is treated as a defect, caught at code-review time via EXPLAIN ANALYZE output attached to the pull request, rather than discovered later as a production incident. This is a process discipline this section establishes as a binding practice, formalized fully as a checklist item in Section 40 (Coding Standards).

38.7 Caching strategy

Redis (Volume 0, Section 21.9) serves three distinct caching roles, each with a deliberately different invalidation strategy stated explicitly to avoid the class of bug where a cache's staleness assumptions are implicit and get violated by a later change:

Permission/RoleVersion cache (Part IV, Section 24.6, 25.5): invalidated explicitly on any AssignRoleCommand/RevokeRoleCommand, never time-based expiry alone — correctness-critical, so explicit invalidation is mandatory, with time-based expiry only as a defensive fallback.
ModuleActionReadModel cache (Part V, Section 23.3's table): short TTL-based expiry (e.g., 60 seconds) is acceptable, since this data's own consistency guarantee (Part V, Section 23.6) is already stated as "eventually consistent," so an additional short cache layer doesn't introduce a new class of staleness beyond what's already a documented contract.
Rate-limiting counters (Part IV, Section 24.9, Part V, Section 30.7): inherently ephemeral, Redis's native TTL/expiry mechanism used directly, no separate invalidation logic needed.
38.8 Load testing as a release gate

Volume 5's CI quality gates (forward-referenced here, fully specified there) include a mandatory load-test run against a staging environment before any GA-track release, exercising NFR-SCALE-1's 500-concurrent-execution target directly (not extrapolated from a smaller test) — this section establishes the requirement that this test exists and gates release; Volume 5 owns the specific tooling and pass/fail thresholds.

38.9 Performance regression as a first-class defect category

Consistent with the severity stance already taken in Section 38.2: this section closes by stating a general principle that governs all four NFR-PERF targets equally — a performance regression against any NFR-PERF target discovered post-release is triaged with the same urgency as a functional correctness bug, tracked in the same risk register discipline Volume 0 established (Section 22), because a platform whose own performance degrades unpredictably directly undermines the trust posture (Volume 0, Business Objective 5) this entire document set has repeatedly treated as non-negotiable.



39. Scalability
39.1 Purpose — distinguishing from Section 38

Performance (Section 38) is about how fast the system responds under normal, expected load. Scalability, this section's concern, is about how the system behaves as load grows — both how far it can scale, and by what mechanism (horizontal vs. vertical, and along which specific dimension). Volume 0's NFR-SCALE-1 through NFR-SCALE-3 are the targets; this section is the engineering account of how the architecture already specified across Parts I–V achieves them, drawn together in one place since scalability is inherently a cross-cutting property no single prior section fully owns.

39.2 NFR-SCALE-1 (500 concurrent flow executions) — the dimension that actually matters

The relevant scaling dimension is concurrent FlowEngine.Execute invocations (Part III, Section 19.8), each of which is a lightweight async task, not an OS thread — .NET's async/await model means the practical ceiling here is governed by downstream resource contention (database connection pool size, Redis connection pool, outbound HTTP client pool for the API module, browser-process pool for the Web module) far more than by raw CPU or memory per se. Section 39.3 addresses how each of these pools is sized and scaled independently.

39.3 Resource pool scaling per module category
Resource Scaling approach
Database connections (PostgreSQL)   Connection pooling via a pooler (PgBouncer or equivalent, Volume 4's specific choice) sitting in front of PostgreSQL, since NFR-SCALE-1's 500 concurrent executions would otherwise risk exceeding PostgreSQL's own reasonable direct-connection ceiling as nexus-orchestration pods scale out (Section 37.4)
Redis connections Standard client-side connection multiplexing (a single multiplexed connection per pod is sufficient for Redis's protocol design, not requiring a pool sized to 500)
Web module — browser processes   A bounded pool of headless browser instances per nexus-orchestration pod (Playwright's own browser-context reuse capability, per Volume 0 §21.4, allows many concurrent FlowRuns to share a smaller number of underlying browser processes via separate contexts, rather than requiring a 1:1 process-per-execution ratio)
Mobile module — device/emulator sessions  Inherently the tightest-constrained resource (Volume 0, Section 14.6's Partial-maturity scoping already reflects this) — session availability is bounded by actual connected devices/emulators or device-farm capacity, not something the orchestration layer's own scaling can increase; this is documented explicitly here as a scaling ceiling the architecture cannot engineer around, only queue against
Sandboxed (Community-tier) module processes  Kubernetes Job/Pod-per-invocation (Section 37.2's nexus-sandbox-runner), scaling via the cluster's own pod-scheduling capacity, bounded by cluster resource quotas configured per Volume 4
39.4 Horizontal scaling of stateless containers

Both nexus-api and nexus-orchestration are designed, per Part I Section 4.4's original container-separation rationale and Part III's persistence-ignorance/statelessness discipline (Section 15.5, 17.5), to hold zero durable in-memory state that would be lost or duplicated across horizontally-scaled instances — every piece of state that must survive or be shared across instances lives in PostgreSQL, Redis, or is coordinated via the outbox/Event Bus (Part III, Section 22). This is what makes Section 37.4's HPA-based autoscaling actually safe: a new pod added under load has zero warm-up state dependency and a terminated pod (Section 37.6's graceful shutdown aside) loses no data other than in-flight work it hands off gracefully.

39.5 NFR-SCALE-2 (horizontal scaling without client-facing changes) — verified, not assumed

This NFR is explicitly a testable claim, not just a design intention: Volume 5's release process includes a specific verification step — scale nexus-orchestration from N to 2N pods under active load and confirm zero client-visible behavior change (no dropped SignalR connections beyond the backplane's normal reconnect handling per Section 31.5, no failed flow triggers, no duplicate executions) — treated as a release gate for any change touching the Orchestration Engine's scaling-relevant code paths (the outbox relay, the distributed cron-lock mechanism from Part III Section 21.4, the SignalR backplane from Section 31.5), specifically because these are exactly the components where a subtle bug would only manifest under multi-instance operation, never in a single-developer local environment.

39.6 NFR-SCALE-3 (10 million flow-run records) — scaling the data layer specifically

Beyond Part V, Section 32.6's indexing strategy, this section adds the operational scaling mechanism for when indexing alone becomes insufficient at the far end of this target: PostgreSQL table partitioning on flow_runs and step_results, partitioned by started_at/time range (a standard, well-understood PostgreSQL scaling technique for time-series-shaped, append-heavy tables) — explicitly deferred as a "when needed, not by default at GA" mechanism, consistent with Part V Section 32.6's stated principle of adding indexing/scaling complexity deliberately per proven need rather than speculatively. The schema design in Part V, Section 32 (time-ordered primary access patterns, no cross-partition foreign-key complications since step_results always references a flow_runs row within the same or an adjacent time window) was deliberately kept compatible with a future move to partitioning without requiring a schema redesign, even though partitioning itself is not part of the GA implementation.

39.7 Scaling the Audit and Observability pipelines alongside the core

A scaling dimension easy to overlook: as nexus-orchestration scales per Section 39.4–39.5, the volume of domain events (Part II, Section 12), audit entries (Part IV, Section 27), and telemetry (Part IV, Section 29) scales proportionally. The outbox relay (Part III, Section 22.2) and observability export pipeline are therefore included in the same load-testing discipline as Section 38.8 — a load test that only verifies flow-execution throughput while ignoring whether the audit pipeline keeps pace would give a false sense of confidence, since a durable-consumer backlog (Part IV, Section 29.4's nexus.outbox.relay.lag metric) growing unboundedly under sustained load is itself a scalability failure, even if flow execution itself appears to be keeping up.

39.8 Multi-tenancy and scalability — the pooled-model implication restated

Directly connecting back to Part IV, Section 26.2's pooled-multi-tenancy decision: NFR-SCALE-1's 500-concurrent-execution target is a platform-wide figure in the pooled hosted model, not per-tenant — Section 30.7's tenant-scoped rate limiting is the mechanism that prevents one large tenant's usage from starving others' fair share of this shared capacity, and is therefore as much a scalability-fairness mechanism as it is the security/abuse-prevention mechanism it was originally introduced as in Part V — the same rate-limiting infrastructure serves both purposes simultaneously, a direct example of Architecture Principle 20.7's composable-primitives preference paying off across two different concerns from one mechanism.

39.9 Known scaling ceilings, stated honestly

Consistent with this volume's practice of stating limitations plainly rather than implying unlimited scalability: the Mobile module's device/session availability (Section 39.3) and the Desktop UI module's inherent single-machine-execution-context nature (Volume 0, Section 14.7's Minimal/Preview GA scoping) are scaling ceilings the current architecture does not attempt to engineer around — they are documented here as known, accepted constraints, consistent with Volume 0's Section 14.10 maturity table, rather than silently glossed over as if every module scaled identically to the API and Logs modules' comparatively unconstrained profile.



40. Coding Standards
40.1 Purpose — consolidating this volume's forward references

Nearly every prior section in this volume deferred a specific enforcement mechanism to "Part VI's coding standards." This section is where those deferred obligations are collected and made concrete and binding — it is not a general-purpose C# style guide (indentation, brace placement, and similar low-stakes conventions are assumed to follow standard .NET community conventions via .editorconfig and are not re-litigated here); it is specifically the set of Nexus-architecture-specific rules that keep the design integrity established across Parts I–V from eroding over time as the codebase grows and more engineers contribute to it. Volume 5's broader Engineering Playbook covers git workflow, PR process, and cross-language (React/Python) standards; this section covers only backend/C#-specific, architecture-enforcing rules.

40.2 Mandatory, statically-enforced rules

These are enforced via Roslyn analyzers or equivalent static-analysis tooling in CI — a violation fails the build, not merely a code-review comment:

Rule  Enforces Source
Every entity/aggregate identifier is a strongly-typed wrapper, never a raw Guid in a public method signature   Compile-time prevention of transposed-ID bugs   Part II, Section 10.8
No project reference from Nexus.Domain to Nexus.Application, Nexus.Infrastructure, Nexus.Api, or Nexus.Contracts  Dependency-direction discipline, persistence ignorance   Part V, Section 36.3; Part III, Section 15.5
No ILogger call using string interpolation ($"...") — structured message templates only   Queryable, correlated operational logging Part IV, Section 28.2
Every [SensitiveValue]-attributed command/event property is automatically excluded from audit metadata and log output derivation Prevents secret leakage into logs/audit trail   Part IV, Section 27.4, 28.5
Nullable reference types enabled (<Nullable>enable</Nullable>) project-wide, with warnings treated as errors   Makes optionality (e.g., AiAttribution?, Part II §11.5) a compiler-checked property, not a runtime surprise Cross-cutting
40.3 Mandatory, code-review-enforced rules

These require human judgment to apply correctly and are therefore checklist items in the PR template rather than fully automatable, but are treated as blocking, not optional, feedback:

Command handlers perform layer-1 (authorization) validation before loading any aggregate, per Part II, Section 13.6 — a reviewer checks that EnsurePermission (Part IV, Section 25.3) is the first meaningful line of every command handler's Handle method.
Every new command inherits AuditableCommandHandler (Part IV, Section 27.2) unless an explicit, documented justification for exemption is included in the PR description — an unaudited state-changing operation is the specific gap Section 27.2's base-class pattern exists to close, and an unjustified exemption is grounds for review rejection.
Aggregates never hold a direct reference to another aggregate's repository or another aggregate's instance — cross-aggregate checks go through an injected port interface (Part II, Section 9.5's IModuleActionResolver pattern), reviewed explicitly whenever a PR touches aggregate code in Nexus.Domain.
New database queries include EXPLAIN ANALYZE output in the PR description when touching a table expected to scale toward NFR-SCALE-3's target (flow_runs, step_results, audit_entries) — per Part VI, Section 38.6.
New IAutomationModule methods or ExecutionContext fields are additive only within a minor/patch contract version — a reviewer checks any change to Nexus.Contracts against Part III, Section 18.7's versioning discipline specifically, given this interface's outsized importance (Part III, Section 18.1).
Singleton-lifetime services (built-in modules, per Part III Section 17.2) hold no mutable instance state — reviewed explicitly for any new field added to a built-in module class, per Part III, Section 17.5's concurrency-safety requirement.
40.4 Naming conventions specific to this domain model
Aggregate roots, entities, and value objects use the exact ubiquitous-language term from Part II, Section 6.3's glossary — a pull request introducing a synonym (e.g., "Execution" instead of "FlowRun") is rejected on that basis alone, regardless of code quality otherwise, per Part II Section 6.3's explicit instruction that this is treated as a defect, not a style nit.
Commands are named as imperative verb phrases (PublishFlowCommand), domain events as past-tense verb phrases (FlowPublishedEvent), per Part II, Sections 12.1 and 13.1's established convention, applied without exception to any newly-added command or event.
DTOs returned from queries are suffixed Dto and never share a name with the domain entity they're derived from without that suffix (FlowDetailDto vs. Flow), per Part II, Section 14.8's explicit entity/DTO separation rule.
40.5 Async and concurrency conventions
Every I/O-bound method is async and accepts a CancellationToken as its final parameter, propagated from the originating request/execution context (Part III, Section 19.8's concurrency model, Part I Section 2's RunCancellation threading) — a method performing I/O without an accepted, honored CancellationToken is a defect, since it breaks the graceful-shutdown (Section 37.6) and run-cancellation (CancelFlowRunCommand, Part II §13.3) guarantees this volume has built on that propagation being reliable end-to-end.
No .Result or .Wait() blocking calls on Task/Task<T> anywhere in the codebase — a synchronous-over-asynchronous call is a known deadlock risk in ASP.NET Core request contexts and is flagged automatically by the same analyzer tooling as Section 40.2's rules.
Shared mutable state across concurrent flow executions is prohibited outside of explicitly-designed, thread-safe infrastructure (Redis-backed distributed locks per Part III, Section 21.4; the Variable Store's per-run scoping per Part I, Section 5.3) — any new field on a component shared across concurrent executions (any singleton-lifetime service, per Section 40.2/40.3) must be reviewed specifically for this concern.
40.6 Testing conventions

Directly extending the testability design already established in Part III, Sections 15.5, 16.7, and Part V, Section 36.5:

Nexus.Domain.Tests tests construct aggregates directly and assert on their public behavior and raised domain events — no mocking framework is needed or permitted here, since the domain layer has no external dependencies to mock, per its zero-dependency design (Section 40.2's dependency-direction rule making this structurally true, not just a testing convention).
Nexus.Application.Tests tests use hand-written or generated fakes for repository/IUnitOfWork interfaces (Part III, Sections 15 and 16), asserting both the expected aggregate method calls and the expected persisted/committed state — a test that doesn't verify IUnitOfWork.Commit was called for a state-changing command is treated as an incomplete test.
Every Permission (Part IV, Section 25.1) has at least one test asserting both the authorized-success and unauthorized-rejection path for every command it gates, per Part IV, Section 25.8's compliance-review-readiness requirement — tracked as explicit, auditable test-coverage rather than assumed.
Integration tests (Nexus.Infrastructure.IntegrationTests, Nexus.Api.IntegrationTests) run against a real, ephemeral PostgreSQL/Redis instance (test containers) — never against mocked infrastructure, since the specific concerns these tests exist to catch (row-level security policies, Part IV §26.3; outbox/transaction atomicity, Part III §16.4) are only genuinely verifiable against real infrastructure behavior.
40.7 Documentation-as-code discipline

Every IAutomationModule implementation's GetSupportedActions() return value (Part III, Section 18.2) must include a non-empty Description on every ActionSchema and ParamSpec (Part II, Section 11.8) — this is enforced by a build-time check specifically because this data is what the GUI's flow-authoring surface renders directly to end users (Part III, Section 18.6) and to Persona Raj's fellow module developers browsing the marketplace (Volume 0, Section 17) — undocumented actions are a direct, user-visible product defect, not merely a documentation gap, and are treated with that severity.

40.8 Architecture Decision Records for deviations

Any code change that deviates from a pattern specified in this volume — a new aggregate that doesn't follow Part II, Section 9's design rules; a new cross-cutting service that doesn't follow Part IV's authorization-first convention — requires an accompanying ADR (per Volume 0, Section 20.9's instruction to cite architecture principles by number) explaining the deviation's rationale, reviewed and approved by the architecture owner (Volume 0, Section 22's risk-register ownership model) before merge, not merged first and documented later. This is the concrete mechanism that keeps this 40-section volume a living, accurate description of the actual codebase rather than a document that quietly diverges from reality over time — directly addressing Volume 0's own RISK-13 concern about documentation-program staleness, now closed with a specific, binding process rather than good intentions alone.

40.9 Closing note — Volume 1 complete

This section closes Volume 1 in full: 40 sections across 6 Parts, tracing from the C4 system context (Part I) through the domain model (Part II), the engineering patterns implementing it (Part III), the cross-cutting services securing and observing it (Part IV), the API and data layer exposing and persisting it (Part V), to the operational and coding discipline that keeps it correct in production and over time (this Part). Every design decision in this volume traces back through Volume 0's business objectives, requirements, and architecture principles, and every future volume — Frontend Architecture, AI Architecture, Infrastructure Architecture, and the Development Standards & Engineering Playbook — builds on this backend specification as its foundation.


___________________________________________________________________________________________________________________________________________________



📘 ADS-002 — Frontend Architecture Design

Objective

Design a modern IDE-like frontend for creating, managing, executing, and monitoring automation.

Chapter 1 – UI Philosophy
Desktop-like experience
Minimal clicks
Productivity-first
Responsive
Accessibility



Chapter 2 – Technology Stack
React 19
TypeScript
Vite
TailwindCSS
shadcn/ui
TanStack Router
TanStack Query
Zustand
SignalR



Chapter 3 – Application Structure
Apps

Pages

Features

Components

Layouts

Services

Store

Hooks

Utilities



Chapter 4 – Routing
Dashboard
Projects
Test Suites
Workflow Designer
Reports
Plugins
AI Assistant
Settings



Chapter 5 – State Management

Global

Local

Server State

Real-time

Offline Cache

Chapter 6 – Component Libraryx

Buttons

Tables

Forms

Dialogs

Editors

Monaco

Tree View

Execution Console

Charts

Chapter 7 – Feature Modules

Dashboard

Project Explorer

Execution Monitor

Workflow Builder

Locator Manager

Object Repository

Report Viewer

Plugin Marketplace

AI Chat

Chapter 8 – UX Guidelines

Dark mode

Keyboard shortcuts

Notifications

Progress indicators

Error handling

Chapter 9 – Security

Authentication

Authorization

Session

Permissions


_________________________________________________________________________________________________________________________________________________________________



# Volume 2 — Frontend Architecture · Part I — UX Foundations

## 1. UX Principles

### 1.1 Purpose and relationship to Volume 0 and Volume 1

Volume 0 established five personas (Section 10) and a specific usability target — NFR-UX-2: a user experienced with an existing single-domain tool authors a basic flow within 30 minutes of first exposure, with no external training. Volume 1 specified the API and data contracts (Part V) this frontend consumes. This section establishes the UX principles that govern every design and implementation decision in Volumes 2 in full — the same role Volume 0's Product Philosophy (Part I, Section 4) and Volume 1's Architecture Principles (Part IV, Section 20) played for their respective volumes. Every subsequent section in this volume should be checkable against the principles stated here.

### 1.2 Principle 1 — The GUI has no authority the API doesn't already have

Direct UX consequence of the architectural rule established in Volume 1, Part I Section 19.2 and Part V, Section 30.3: the GUI is a client of the REST API, never a privileged shortcut. This means, concretely: every validation the GUI performs client-side (e.g., warning that a flow step references an unresolved variable before publish) must be a *convenience preview* of a check the API will also enforce authoritatively — never the only place a check happens. A user should never be able to do something in the GUI that circumvents server-side authorization (Volume 1, Part IV, Section 25) or domain invariants (Volume 1, Part II, Section 9). This principle protects against a common and serious class of security/consistency bug where a frontend team's convenience validation silently becomes the *only* validation over time.

### 1.3 Principle 2 — Show the system's real state, never a fiction of it

Directly extending Architecture Principle 20.4 (fail loud, fail informatively, Volume 1 Section 20.4) into the UX layer: the GUI never fabricates optimistic state that could diverge from what the backend actually persisted. Where optimistic UI updates are used for responsiveness (Section 8, State Management, will specify exactly where), they are always reconciled against the authoritative server response, and any divergence is visibly corrected, never silently left inconsistent. This is the direct UX expression of Volume 1, Part V Section 31.4's SignalR reconnection design ("never assume no events were missed... re-fetch on reconnect") generalized as a governing principle beyond just the live-progress view.

### 1.4 Principle 3 — Every module is a first-class citizen in the UI, with zero hardcoding

The single most consequential principle in this volume, directly inherited from Volume 0's Module Parity Index (Section 6.3) and Volume 1's repeated architectural enforcement (Architecture Principle 20.3; Part III, Section 18.6's `GetSupportedActions()` auto-surfacing design): **no UI component may contain module-specific branching logic** (e.g., `if (module === 'web') { renderWebSpecificEditor() }`). The flow-step editor renders itself entirely from the `ActionSchema` metadata the backend already exposes (Volume 1, Part II, Section 14.2's `GetAvailableModuleActionsQuery`) — a newly-registered marketplace module must appear, fully editable, with zero frontend code changes, exactly as Volume 0's UC-6 (Persona Raj) requires. This principle is tested directly, not just followed by convention: Section 19 (Testing Strategy) will specify an automated test that registers a synthetic module and asserts the GUI renders it correctly without any module-specific code path existing.

### 1.5 Principle 4 — Progressive disclosure over exhaustive surface area

Directly serving NFR-UX-2's 30-minute time-to-first-flow target: the GUI defaults to the simplest path for common tasks (a Flow Author building a single-module flow sees a clean, minimal editor) while making advanced capability (conditional steps, retry policy tuning, cross-module variable chaining, Workflow-level orchestration per Volume 1 Part III Section 20) available but not front-loaded. This is a direct rejection of "expose every backend capability with equal visual prominence," which would optimize for completeness at the cost of the specific, measured usability target Volume 0 committed to.

### 1.6 Principle 5 — Design for the five personas' five different relationships to the product

Restating Volume 0, Section 10.7's persona-to-interface table as a binding UX constraint: Priya needs an efficient authoring loop, Sofia needs a trustworthy reporting surface she can walk into a leadership review with, Marcus barely touches the GUI at all (his primary surface is the CLI/API, per Volume 1 Part V Section 30), Raj needs the plugin-authoring experience to be self-explanatory from documentation alone, and Elena needs the GUI's own security posture (session handling, no client-side credential exposure) to hold up under scrutiny without needing to be a GUI user herself. A design decision that serves one persona at another's direct expense (e.g., an authoring-optimized interface so dense with power-user features that Sofia's occasional dashboard visits become confusing) should be treated as a conflict requiring explicit resolution, not an acceptable default trade-off absorbed silently.

### 1.7 Principle 6 — Transparency about AI, always

Direct UX carry-through of Volume 0's Product Philosophy 4.4 ("AI proposes, deterministic code disposes, never silently") and Volume 1's `AiAttribution` value object (Part II, Section 11.5): anywhere the GUI displays a result that involved AI assistance — a generated flow, a self-healed selector, an AI-triaged log — the interface visibly and unambiguously marks it as AI-assisted, with the underlying attribution data (confidence score, original vs. suggested value) inspectable, never hidden behind an extra click a user might reasonably not take. This is a hard UX requirement, not a nice-to-have: Section 18 (Accessibility) and Section 10 (Workflow Designer) will both specify concrete rendering treatments that satisfy this principle in their respective surfaces.

### 1.8 Principle 7 — The interface degrades honestly, never pretends full capability

Directly extending Volume 1's AI-optionality architecture (NFR-REL-3) and multi-tenancy/licensing tiering (Volume 0, Section 16) into the UX layer: when a capability is unavailable — AI disabled by tenant configuration, a licensing tier that doesn't include cross-module analytics (Volume 0, Section 16.2), a module at Volume 0's "Preview" maturity level (Section 14.10) — the GUI states this plainly (a disabled control with an explanatory tooltip, not a hidden or broken one) rather than presenting a uniform interface that silently fails or behaves inconsistently depending on invisible backend configuration.

### 1.9 How these principles resolve design disputes

Consistent with the pattern established by Volume 1's Architecture Principles (Section 20.9 there): every subsequent section in this volume that makes a non-obvious design choice should cite the specific principle number here (1.2 through 1.8) that motivated it, rather than justifying from scratch — this is the same internal-consistency discipline Volume 0 and Volume 1 both established, now extended to the frontend.



2. Design System
2.1 Purpose and starting point

This section formalizes Nexus's visual design language into a reusable token system that the Component Library (Section 9) implements. Rather than inventing a visual identity from a blank page, this design system codifies and extends the "instrument panel" aesthetic already prototyped and validated earlier in this project's exploration phase (the Nexus control-deck dashboard) — a deliberate choice consistent with UX Principle 1.3's "every module is a first-class citizen": an engineering-tool aesthetic (dense, precise, technical-monospace-forward) suits a platform whose primary users are engineers (Priya, Marcus, Raj) far better than a generic SaaS-dashboard look borrowed from a consumer product.

2.2 Design rationale — why "instrument panel," not generic SaaS

Three reasons, each traceable to Volume 0: first, Persona positioning (Section 10) — every primary persona except Sofia is a hands-on technical practitioner, and Sofia's own need (Volume 0, UC-5) is for a trustworthy, data-dense reporting view, which an instrument-panel aesthetic serves better than a soft, decorative consumer style. Second, competitive differentiation (Volume 0, Section 9) — the Archetype A/B incumbents largely use conventional, interchangeable SaaS visual language; a distinct, confident technical aesthetic is a small but real differentiator supporting Volume 0's "not shallow, not generic" positioning concern (Section 8.6's credibility-tax risk). Third, and most directly: the aesthetic must communicate precision and control over six genuinely different automation domains simultaneously — a visual language built around clear signal/status states (pass/fail/warn, live/idle, AI-assisted/deterministic) rather than decorative flourish directly serves UX Principle 1.7's "degrade honestly, show real state" requirement.

2.3 Color tokens
Token	Value	Usage
--nexus-bg	
#0c1015	Application background
--nexus-panel	
#12171f	Primary surface/card background
--nexus-panel-2	
#161c25	Secondary/nested surface background
--nexus-line	
#232a35	Default border/divider
--nexus-line-strong	
#333c4a	Emphasized border, focus-adjacent
--nexus-text	
#e7eaee	Primary text
--nexus-muted	
#8892a0	Secondary text, labels
--nexus-dim	
#5b6472	Tertiary text, disabled-state text
--nexus-amber	
#e8a33d	Primary action color, "live"/active signal
--nexus-teal	
#4fb0a5	Secondary signal, AI-assisted indicator, "verified" states
--nexus-red	
#e5534b	Failure/error signal
--nexus-green	
#57ab5a	Pass/success signal

Every color token maps to a specific semantic meaning, never used decoratively — this is a direct, load-bearing design decision, not a stylistic footnote: --nexus-amber always means "primary action or actively live," --nexus-teal always means "AI-related or verified-trust," --nexus-red/--nexus-green always and only mean fail/pass. A component using --nexus-teal for a purpose unrelated to AI or trust verification is a design-system violation, because it would erode the signal-color vocabulary's reliability — a user should be able to infer meaning from color alone, consistently, across all six module surfaces (UX Principle 1.3).

2.4 Typography

Two type families, each with a deliberate, non-interchangeable role:

IBM Plex Mono — used for all data readouts, code/parameter values, identifiers (Flow IDs, correlation IDs per Volume 1 Part V Section 30.5), module/action names, and status labels. The monospace choice is functional, not aesthetic: it makes tabular data (report tables, parameter lists) align predictably and gives technical values (a selector string, a JSON parameter) unambiguous visual distinction from prose.
IBM Plex Sans — used for all prose: descriptions, help text, navigation labels, body copy in dialogs.

A component mixing the two families for the same semantic role (e.g., using Mono for one status label and Sans for another) is a design-system violation, consistent with the same rigor Section 2.3 established for color.

2.5 Spacing and layout grid

An 4px base unit, with a defined scale (4, 8, 12, 16, 20, 24, 32, 40, 56px) — every margin, padding, and gap value in the Component Library (Section 9) is drawn from this scale, never an arbitrary value, to keep visual rhythm consistent across the five distinct product surfaces (flow authoring, reporting dashboard, module marketplace, Workflow Designer, admin/settings).

2.6 Signal states — the vocabulary that spans all six modules

Directly serving UX Principle 1.3: a single, closed set of visual "signal states" is defined once and reused identically regardless of which module a given step or result belongs to — this is the visual-design-system counterpart to Volume 1's StepStatus/RunStatus closed enums (Part II, Section 11.3).

Signal state	Visual treatment	Backing domain concept
Passed	Green dot/badge, --nexus-green	StepStatus.Passed
Failed	Red dot/badge, --nexus-red	StepStatus.Failed
Skipped	Dim/outline badge, --nexus-dim	StepStatus.Skipped
Running/Live	Amber pulsing dot, --nexus-amber	RunStatus.Running
AI-assisted	Teal badge with a distinct icon, always paired with visible confidence %	Non-null AiAttribution (Volume 1, Part II §11.5)
Flaky (passed on retry)	Green badge with an amber outline/asterisk	Non-empty RetryHistory on a Passed StepResult (Volume 1, Part II §10.4)

Every one of these six states is rendered identically wherever it appears — the flow-run detail view, the live-progress stream, the reporting dashboard's trend charts — precisely because the underlying domain concepts they represent (Volume 1) are themselves module-agnostic, closed sets. This table is the single source of truth the Component Library's status-badge component (Section 9) implements against.

2.7 Iconography

A single, consistent icon set (Lucide, chosen for its comprehensive coverage and consistent stroke-width visual language) is used throughout, with a specific, documented icon assigned per ModuleKind (Volume 1, Part II, Section 11.2) — including a defined fallback icon for ModuleKind.Custom marketplace modules that haven't supplied their own icon, since UX Principle 1.3 requires the interface to handle an arbitrary, previously-unknown module gracefully, not just the six built-in ones.

2.8 Motion and animation principles

Motion is used exclusively to communicate state change, never for decorative effect — a step transitioning from "running" to "passed" animates that specific transition; nothing animates on page load or idle for visual flourish alone. Duration and easing values are similarly tokenized (--nexus-motion-fast: 150ms, --nexus-motion-standard: 250ms) and reused consistently. This restraint is a direct expression of UX Principle 1.2's "show real state" — decorative motion that doesn't correspond to an actual state change risks implying activity or change that isn't really happening.

2.9 Design system governance

Consistent with Volume 1, Part VI Section 40.8's ADR-for-deviations discipline: any new color, type treatment, or signal state not defined in this section requires a documented design-system extension proposal, reviewed against Section 2.2's rationale, before implementation — preventing the gradual, uncoordinated visual drift that undermines a design system's core value (predictability) over a project's lifetime.



3. Theme System
3.1 Purpose

This section specifies the technical implementation of Section 2's design tokens as a runtime-switchable theme system — distinct from Section 2's job of defining what the tokens are, this section defines how they're delivered to and consumed by every component in the Component Library (Section 9) and every feature module (Section 6).

3.2 CSS custom properties as the token delivery mechanism

Every token from Section 2.3–2.8 is implemented as a CSS custom property (--nexus-*, following the naming convention already shown throughout Section 2), scoped at the document root and consumed by components exclusively through these properties — never as hardcoded hex/pixel values anywhere in component code. This is a hard, statically-enforced rule (a stylelint rule rejecting hardcoded color/spacing values outside the token-definition file itself), directly analogous in spirit to Volume 1's Roslyn-analyzer-enforced coding standards (Part VI, Section 40.2) — design-system consistency is enforced by tooling, not review vigilance alone.

css
:root[data-theme="dark"] {
  --nexus-bg: #0c1015;
  --nexus-panel: #12171f;
  /* ...full token set from Section 2.3-2.8 */
}

:root[data-theme="light"] {
  --nexus-bg: #f7f8fa;
  --nexus-panel: #ffffff;
  --nexus-text: #1a1f26;
  /* ...a fully-specified light-mode counterpart for every dark-mode token,
     never a partial override — see 3.4 for why partial theming is prohibited */
}
3.3 Dark mode as the default, light mode as a fully-supported alternative

Dark mode (Section 2's instrument-panel aesthetic) is the default theme, consistent with Section 2.2's rationale — but light mode is a fully-specified, equally-complete alternative, not an afterthought or a partial "invert some colors" implementation. This matters for two Volume 0-traceable reasons: accessibility (some users have genuine visual/environmental needs that dark mode doesn't serve, directly relevant to NFR-UX-1's WCAG 2.1 AA requirement, Section 18 of this volume will elaborate) and enterprise environments (some organizations have workplace or brand-consistency policies favoring light interfaces, relevant to the Sofia/Elena personas' more occasional, business-context GUI usage).

3.4 Why partial/override-only theming is prohibited

A tempting shortcut is defining light mode as a small set of overrides layered on top of the dark-mode token set (only redefining --nexus-bg and --nexus-text, for instance, leaving other tokens computed or assumed). This is explicitly prohibited: it inevitably produces subtly broken combinations (a signal-state badge color, Section 2.6, that has acceptable contrast against a dark panel but fails WCAG contrast requirements against a light one) that are difficult to catch without exhaustive per-combination testing. Every theme is a complete, independently-specified token set, verified independently against Section 18's accessibility contrast requirements — more upfront specification work, deliberately accepted in exchange for eliminating an entire class of theme-interaction bug.

3.5 Theme resolution and persistence
typescript
type ThemePreference = 'dark' | 'light' | 'system';

function resolveTheme(preference: ThemePreference): 'dark' | 'light' {
  if (preference === 'system') {
    return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
  }
  return preference;
}

A user's theme preference (including system, which follows the OS-level setting) is persisted server-side as part of their user profile (a straightforward extension of the User entity, Volume 1 Part II Section 10.6, or a lightweight associated preferences record — a Volume 1 Part V schema addition, not specified in Volume 1 itself since it was correctly out of that volume's backend-architecture scope, but flagged here as the concrete cross-volume dependency this frontend feature creates) rather than only in browser local storage — ensuring a consistent experience across devices, directly relevant to Persona Priya potentially moving between a work laptop and a secondary machine.

3.6 Tenant-level theme/branding (enterprise consideration)

Directly anticipating Volume 0's Future Vision (Section 23.4, industry-specific editions) and the general enterprise-trust posture (Business Objective 5): the theme system's architecture supports a tenant-level accent-color override (a single additional custom property layer, applied only to --nexus-amber's role specifically, never touching the semantic signal colors in Section 2.6, which must remain universally consistent per that section's own governance rule) for enterprise customers wanting light custom branding. This is explicitly scoped as a narrow, future-facing extension point, not a GA requirement — mentioned here only so the theme system's technical design (3.2's CSS custom-property architecture) doesn't need retrofitting if and when this becomes a real roadmap item.

3.7 High-contrast mode

A third theme variant, high-contrast, is specified as a GA requirement directly serving NFR-UX-1's WCAG 2.1 AA conformance target — not merely a stretch goal. This variant increases contrast ratios beyond the standard dark/light themes' already-AA-compliant baselines (Section 18 specifies exact contrast ratio verification) and simplifies or removes decorative visual treatments (e.g., Section 2.8's motion is further reduced, gradients or subtle background treatments in Section 9's components are flattened) for users with low-vision needs — implemented as a fourth complete token set following the same non-partial-override discipline established in Section 3.4.

3.8 Theme system and the Plugin UI (forward reference)

Section 13 (Plugin UI) will specify that marketplace-sourced module UI extensions (where a Community or Verified-tier module supplies custom rendering for its own action parameters — an extension point Section 13 formalizes) consume the same theme tokens defined in this section, via a constrained, sandboxed CSS custom-property read-only interface — a third-party module's UI contribution cannot introduce its own colors or override the host application's tokens, only consume them, which is the frontend-specific expression of the same trust-boundary discipline Volume 1 established for backend plugin code (Part III, Section 18.4's ExecutionContext boundary, now mirrored here for the frontend equivalent).


____________________________________________________________________________________________________________________________________________________


4. React Architecture
4.1 Purpose and technology baseline

React is locked as the frontend framework per Volume 0, Section 21.3, chosen specifically for its ecosystem fit with Monaco (Section 10) and React Flow (Section 12). This section establishes the architectural patterns governing how React is used — component design philosophy, rendering strategy, and the boundary between this volume's concerns and Volume 1's backend contracts.

4.2 Functional components and hooks exclusively

Nexus's React codebase uses functional components with hooks throughout — no class components anywhere in new code, consistent with modern React idiom and, more specifically, because the Component Library's composability goals (Section 9) and the custom-hook-based data-fetching architecture (Section 8, State Management) both depend on hooks' composition model. This is a static-analysis-enforced rule (an ESLint rule rejecting React.Component subclassing), mirroring Volume 1's Roslyn-analyzer-enforced backend conventions (Part VI, Section 40.2) — the same "enforced by tooling, not review vigilance" discipline extended to the frontend.

4.3 Rendering strategy — client-rendered SPA, no SSR at GA

Nexus's GUI is a client-side-rendered single-page application for GA, not a server-rendered (Next.js-style) application. This is a deliberate scope decision, not an oversight: per UX Principle 1.6 (Volume 2, Section 1.6), the primary personas interacting with the GUI (Priya, Sofia) are authenticated users performing tool-like work behind a login wall — there is no public, unauthenticated, SEO-relevant surface (unlike a marketing site) that would justify SSR's added complexity. This decision is explicitly revisitable if a future public-facing surface (e.g., a public flow-template gallery, extending Volume 0's Plugin Marketplace vision, Section 17) emerges as a real requirement — noted here so a future team doesn't need to rediscover this reasoning from scratch, consistent with this document set's established practice (Volume 1, Part II, Section 6.5).

4.4 Component classification

Every component in the codebase falls into exactly one of four classifications, each with distinct rules about what it may depend on:

Classification	May contain	May depend on	Example
Primitive	Pure presentation, design-token-driven styling only	Design tokens (Volume 2, Section 2) only — no data fetching, no business logic	Badge, Button, StatusDot (Section 9)
Composite	Composition of primitives into a reusable, still-domain-agnostic unit	Primitives only	DataTable, Modal, CodeEditor wrapper (Section 9)
Feature	Domain-aware rendering, data fetching, business logic specific to one bounded context	Composites, primitives, feature-scoped hooks (Section 6)	FlowStepEditor, RunProgressView, ModuleActionPicker
Page	Route-level composition of Feature components, layout only	Feature components, routing (Section 7)	FlowEditorPage, ReportingDashboardPage

This classification is the direct frontend analogue of Volume 1's layered dependency-direction discipline (Part V, Section 36.3's Nexus.Domain → Nexus.Application → ... inward-pointing rule) — Primitives never depend on Composites, Composites never depend on Feature components, enforced by the folder structure and import-boundary rules Section 5 specifies concretely.

4.5 Data fetching — server state is never duplicated into local component state

Directly anticipating Section 8's full State Management specification: any data originating from the REST API (Volume 1, Part V, Section 30) or SignalR (Section 31 there) is treated as server state, managed exclusively through the data-fetching layer specified in Section 8, never copied into useState and manually kept in sync. This is stated here, at the architecture level, because it's a foundational rule the component-classification table in 4.4 depends on — a Feature component reads server state via a hook, it does not "own" a local copy of it.

4.6 Error boundaries — module-scoped, never a single global catch-all

Consistent with UX Principle 1.3 (module parity) and 1.7 (honest degradation): each Feature-level surface that renders module-specific content (the flow-step editor rendering an arbitrary module's action parameters, per Section 9's schema-driven rendering) is wrapped in its own React error boundary, scoped narrowly enough that a rendering failure in one module's custom UI contribution (Section 13, Plugin UI) cannot take down the entire authoring surface — directly mirroring Volume 1's Part III, Section 18.5 backend sandboxing philosophy (a misbehaving module shouldn't compromise the whole system) translated into frontend fault isolation.

tsx
function ModuleActionRenderer({ schema, module }: { schema: ActionSchema; module: ModuleKind }) {
  return (
    <ModuleErrorBoundary moduleKind={module} fallback={<ModuleRenderFailedNotice module={module} />}>
      <SchemaDrivenParamEditor schema={schema} />
    </ModuleErrorBoundary>
  );
}
4.7 TypeScript — strict mode, generated types from the API contract

The entire codebase uses TypeScript in strict mode. Critically, DTO types (Volume 1, Part II, Section 14.8's FlowDetailDto, StepResultDto, and so on) are generated, not hand-written, from Volume 1's API contract (an OpenAPI-spec-driven code generation step, consistent with Volume 1, Part V, Section 30.2's note that the endpoint inventory should be generated from, not independently maintained against, the command/query catalog) — this closes the loop so that a backend DTO shape change is caught as a frontend TypeScript compilation error immediately, rather than as a runtime bug discovered later. This generation step is a mandatory part of the CI pipeline (Volume 5's eventual responsibility to specify the exact tooling), treated as a build-breaking check if the generated types and the actual running API drift.

4.8 Concurrent rendering and Suspense

React's concurrent rendering features (Suspense for data fetching, transitions for non-urgent updates) are used deliberately for exactly two patterns: the initial load of a Feature-level surface (a route transition into the Flow Editor suspends on the initial GetFlowByIdQuery fetch, Section 8) and large, potentially-slow list renders (the reporting dashboard's trend views, Volume 1 Part II Section 14.4's GetPassRateTrendQuery, wrapped in a transition so typing in a date-range filter doesn't block the input's own responsiveness while the new data loads). This is a deliberately narrow, specified usage — not a blanket "wrap everything in Suspense" policy — consistent with this volume's general preference (inherited from Volume 1, Architecture Principle 20.7) for applying complexity only where it earns its cost.

4.9 Relationship to the remaining Part II sections

This section establishes the component and rendering philosophy; Section 5 (Folder Structure) makes the classification in 4.4 concrete as an enforced directory layout; Section 6 (Feature Modules) specifies how bounded-context-aligned features are organized; Section 7 (Routing) specifies the page-level navigation structure; Section 8 (State Management) fully specifies the server-state/client-state split introduced in 4.5.



5. Folder Structure
5.1 Purpose

This section makes Section 4.4's component classification and Volume 2's bounded-context alignment (mirroring Volume 1, Part II Section 7's Flow Authoring/Execution/Module & Plugin/Reporting & Audit/Identity & Access contexts) concrete as an enforced directory layout — the frontend counterpart to Volume 1, Part V, Section 36's package/solution structure.

5.2 Top-level structure
nexus-gui/
├── src/
│   ├── primitives/              # Section 4.4 "Primitive" components — Button, Badge, StatusDot, etc.
│   ├── composites/               # Section 4.4 "Composite" components — DataTable, Modal, CodeEditor
│   ├── design-tokens/             # Volume 2 §2-3 — CSS custom properties, theme definitions
│   │
│   ├── features/                  # Section 6 — one folder per bounded context, mirroring Volume 1 §7
│   │   ├── flow-authoring/
│   │   ├── execution/
│   │   ├── module-plugin/
│   │   ├── reporting-audit/
│   │   └── identity-access/
│   │
│   ├── pages/                     # Section 4.4 "Page" components, one per route (Section 7)
│   │
│   ├── api/                       # Section 4.7's generated DTO types + typed API client
│   │   ├── generated/              # never hand-edited — regenerated from Volume 1's OpenAPI spec
│   │   └── client.ts                # thin wrapper adding auth headers, error normalization
│   │
│   ├── state/                     # Section 8 — server-state hooks (TanStack Query) and
│   │   │                              client-only state stores (Zustand), organized by
│   │   │                              bounded context to mirror features/
│   │   ├── server/
│   │   └── client/
│   │
│   ├── realtime/                   # Section 15 — SignalR connection management
│   │
│   ├── routing/                    # Section 7 — route definitions, guards
│   │
│   ├── plugin-ui/                   # Section 13 — the sandboxed marketplace-module UI
│   │                                  extension host, kept structurally separate from
│   │                                  everything above per the trust-boundary discipline
│   │                                  Section 13 will specify in full
│   │
│   └── app/                        # composition root — App.tsx, providers, theme bootstrapping
│
├── tests/
│   ├── unit/                       # mirrors src/ structure
│   ├── integration/
│   └── e2e/                        # Section 19 — a deliberate, notable case of Nexus's own
│                                       Web module (Volume 0's product itself) being used to
│                                       test the Nexus GUI, elaborated in Section 19
│
└── public/
5.3 Enforced import boundaries

Directly extending Section 4.4's classification rules into a statically-checked structural rule (an ESLint import-boundary plugin, configured per-directory):

primitives/     → may import from: design-tokens/ only
composites/     → may import from: primitives/, design-tokens/
features/*/     → may import from: composites/, primitives/, design-tokens/, api/, state/, realtime/
pages/          → may import from: features/*/, composites/, primitives/, routing/
plugin-ui/      → may import from: design-tokens/ (read-only, per Volume 2 §3.8) — explicitly
                    FORBIDDEN from importing features/ or state/ directly, since a marketplace
                    module's UI extension must interact with the host application only through
                    the constrained extension-point API Section 13 defines, never by reaching
                    into host application internals

A violation of this table is a CI-failing lint error, not a review suggestion — mirroring the enforcement rigor Volume 1 established for its own dependency-direction rule (Part V, Section 36.3; Part VI, Section 40.2).

5.4 Why features/ mirrors Volume 1's bounded contexts exactly

This is a deliberate, non-obvious structural choice worth justifying explicitly: features/flow-authoring/, features/execution/, and so on use the exact same names as Volume 1, Part II, Section 7's bounded contexts, not a frontend-invented alternative grouping (e.g., "authoring," "monitoring," "settings" as a UX-driven taxonomy might suggest). This is a direct application of Volume 1, Part II, Section 6.3's ubiquitous-language discipline extended across the volume boundary — a frontend engineer and a backend engineer discussing "the Execution feature" or "the Execution context" are talking about the same conceptual boundary, reducing the exact cross-team vocabulary drift risk Volume 1 was originally designed to prevent within the backend alone.

5.5 Within a feature folder
features/execution/
├── components/         # Feature-classification components specific to this context
│   ├── RunProgressView.tsx
│   ├── StepResultCard.tsx
│   └── FlowRunTriggerDialog.tsx
├── hooks/               # feature-scoped hooks composing state/server/ queries
│   ├── useFlowRun.ts
│   └── useLiveRunProgress.ts    # composes realtime/ (Section 15) + state/server/
└── index.ts             # the ONLY file other features/pages may import from —
                            everything else in this folder is a private implementation
                            detail, enforced by the same ESLint boundary tooling as 5.3

The index.ts-as-sole-public-surface convention, applied to every feature folder, is what keeps features/* from becoming implicitly, unmanageably interdependent as the codebase grows — directly analogous to Volume 1, Part V, Section 36.3's project-reference discipline, expressed here at the module/folder level since TypeScript/React doesn't have a first-class project-reference mechanism as rigorous as .NET's.

5.6 Shared cross-feature concerns — where they live

A small number of concerns are legitimately needed by multiple features (e.g., the AiAttribution badge rendering from Volume 2, Section 2.6, needed by both execution and reporting-audit). These live in composites/ if purely presentational (Section 4.4's classification), or in a narrowly-scoped features/shared/ folder (an explicit, deliberate exception to 5.4's one-context-per-folder rule, used sparingly and only for genuinely cross-context domain concepts like AI attribution that don't belong to any single bounded context more than another) if they carry feature-level logic.

5.7 Path aliasing

TypeScript path aliases (@primitives/*, @composites/*, @features/*, and so on, mapped 1:1 to the top-level directories in 5.2) are configured project-wide, both for import ergonomics and because the ESLint boundary rules in 5.3 are implemented against these aliases specifically — a relative-path import that circumvents an alias (e.g., ../../../primitives/Button instead of @primitives/Button) is itself flagged as a lint violation, closing a potential loophole around the boundary enforcement.



6. Feature Modules
6.1 Purpose

Section 5.5 established the internal shape of a single feature folder. This section specifies the five feature modules themselves — their scope, their primary components, and, critically, their interaction patterns with each other, since Volume 1's bounded-context Customer/Supplier and Conformist relationships (Part II, Section 7.3) have direct frontend analogues that this section makes explicit.

6.2 features/flow-authoring

Scope: Everything Persona Priya touches to create, edit, and publish flows (Volume 0, Section 10.2; UC-2). Primary components: FlowEditor (the main authoring canvas, composing the Workflow Designer from Section 11), FlowStepEditor (schema-driven parameter editing per UX Principle 1.3), FlowVersionHistory, AiFlowGenerationPanel (the UI for Volume 1's GenerateFlowFromDescriptionCommand, Part II §13.2).

Interaction with module-plugin: Consumes GetAvailableModuleActionsQuery results (via state/server/) to render the module/action picker — this is a read-only, Customer-side relationship mirroring Volume 1's own Customer/Supplier context-map relationship (Part II, Section 7.3) exactly: flow-authoring never mutates module registration data, only reads it.

6.3 features/execution

Scope: Triggering runs, live progress (Persona Priya's authoring-loop feedback and Persona Marcus's CI-adjacent visibility, Volume 0 UC-1/UC-4), and the flow-run detail view. Primary components: RunProgressView (SignalR-driven, Section 15), StepResultCard (rendering Volume 1's StepResult, including the AI-attribution and flake-badge treatments from Volume 2, Section 2.6), FlowRunTriggerDialog, WorkflowRunView (the frontend counterpart to Volume 1, Part III Section 20's Workflow Engine, if and when that capability ships per Volume 0's roadmap prioritization).

Interaction with flow-authoring: Consumes a published Flow's definition (read-only) to know what steps to expect progress for — again a Customer/Supplier relationship, directly mirroring Volume 1's own (Part II, Section 7.3): execution never edits a flow definition, and per UX Principle 1.2, has no client-side ability to do so even if a developer were tempted to add a shortcut.

6.4 features/module-plugin

Scope: Module/marketplace browsing, installation (Volume 0, Section 17.4), and the module registration detail view (serving Persona Raj, Volume 0 Section 10.5). Primary components: ModuleMarketplaceBrowser, ModuleCertificationBadge (rendering Volume 1's TrustTier, Part II §11.9, using the exact tiered visual treatment Volume 0 Section 17.3 specifies), ModuleActionSchemaViewer.

Interaction with flow-authoring: Supplier-side only, as established in 6.2 — this feature never reads or depends on any flow-authoring state.

6.5 features/reporting-audit

Scope: Persona Sofia's primary surface (Volume 0, UC-5) — trend dashboards, drill-down views, audit log export (Persona Elena, UC-7). Primary components: PassRateTrendChart, FlakeRateWidget, FailureClusterDrilldown, AuditLogExporter.

Interaction with execution: Read-only consumer of historical FlowRun/StepResult data via the reporting-specific queries (Volume 1, Part II, Section 14.4) — critically, this feature never subscribes to SignalR live-progress events (Section 15's scope, Volume 1 Part V Section 31.7's explicit boundary that the hub is scoped to actively-watched runs, not general reporting) — Sofia's dashboard is built entirely on the REST/query layer (Section 8), consistent with the eventual-consistency read-model design Volume 1 already specified (Part V, Section 23.4–23.6) and this frontend correctly does not try to work around with a real-time shortcut that would misrepresent the dashboard's actual, documented consistency guarantee.

6.6 features/identity-access

Scope: Login, session management, user/role administration (Volume 1, Part IV, Section 25's baseline roles), and the theme/preferences settings (Volume 2, Section 3.5). Primary components: LoginForm, RoleAssignmentTable, UserProfileSettings, ApiKeyManager (serving Persona Marcus's CI service-account credential needs, Volume 1 Part IV Section 24.4).

Interaction with every other feature: This is the one feature every other feature has an implicit dependency on (via the authentication context established in Section 14), but the relationship is strictly one-directional — identity-access exposes the current user/session/permission state; no other feature ever reaches into identity-access's internal implementation, consuming only its published index.ts surface (Section 5.5) and, more specifically, the shared useAuth()/usePermission() hooks Section 14 will formalize.

6.7 Cross-feature permission checks — mirroring Volume 1's authorization discipline

Directly extending Volume 1, Part IV, Section 25.3's EnsurePermission convention to the frontend: every Feature-level component that renders a state-changing control (a "Publish" button, a "Trigger Run" button, a "Register Module" form) checks the current user's permission (via identity-access's hooks, Section 6.6) before rendering that control as enabled — but, per UX Principle 1.2, this check is understood explicitly as a UX convenience (hiding/disabling controls a user can't use, avoiding a confusing failed-request round-trip) and never as the actual security boundary, which remains entirely server-side (Volume 1, Part IV, Section 25). A frontend permission check that's stale or bypassed results, at worst, in a clean, expected 403 Forbidden API response — never in an actual unauthorized action succeeding.

6.8 Feature module ownership and team structure (forward reference)

Consistent with Volume 1, Part II, Section 7.6's observation about bounded contexts as natural team-ownership units: these five feature modules are designed to be ownable by frontend sub-teams aligned with the same team structure Volume 1 anticipated for the backend bounded contexts — a flow-authoring frontend engineer and a Flow Authoring backend engineer share a bounded context and, per Section 5.4's naming discipline, a vocabulary, even if they sit on nominally separate "frontend" and "backend" teams. This is flagged here as a Volume 5 (Engineering Playbook) consideration, not resolved in this volume, consistent with this document set's practice of tracing decisions to where they actually belong.



7. Routing
7.1 Purpose and technology choice

Client-side routing uses React Router, the de facto standard for React SPAs (Section 4.3's client-rendered-SPA decision) with mature support for the route-level code-splitting and data-loading patterns this section specifies. This section defines the route table, route-level data loading strategy, and the authorization-guard pattern that enforces Volume 1's RBAC model (Part IV, Section 25) at the navigation layer.

7.2 Route table
Route	Page component	Feature module(s)	Required permission (Volume 1, Part IV §25.1)
/login	LoginPage	identity-access	None (public)
/flows	FlowListPage	flow-authoring	FlowCreate OR FlowExecute (view access implied by either)
/flows/new	FlowEditorPage (new-draft mode)	flow-authoring	FlowCreate
/flows/:id/edit	FlowEditorPage	flow-authoring	FlowEdit
/flows/:id/versions	FlowVersionHistoryPage	flow-authoring	ReportView
/runs	FlowRunListPage	execution	ReportView
/runs/:id	FlowRunDetailPage	execution, reporting-audit	ReportView
/reports/trends	TrendDashboardPage	reporting-audit	ReportView
/reports/audit	AuditLogPage	reporting-audit	AuditView
/modules	ModuleMarketplacePage	module-plugin	ReportView (browsing); ModuleRegister for install actions, checked at the component level per Section 6.7
/modules/:id	ModuleDetailPage	module-plugin	ReportView
/admin/users	UserAdminPage	identity-access	UserManage
/admin/roles	RoleAdminPage	identity-access	RoleManage
/settings	UserSettingsPage	identity-access	None (own-profile access)

This table is the direct frontend realization of Volume 1, Part V, Section 30.2's REST endpoint inventory — every route ultimately calls the query/command combination that table specifies; no route introduces a data-access pattern not already accounted for in Volume 1.

7.3 Route guards — layered, mirroring Volume 1's authorization layers
tsx
function RequirePermission({ permission, children }: { permission: Permission; children: React.ReactNode }) {
  const { hasPermission, isLoading } = usePermission(permission);  // features/identity-access, Section 6.6
  if (isLoading) return <RouteLoadingSkeleton />;
  if (!hasPermission) return <Navigate to="/forbidden" replace />;
  return <>{children}</>;
}

// Route definition:
<Route path="/admin/users" element={
  <RequirePermission permission="UserManage">
    <UserAdminPage />
  </RequirePermission>
} />

Consistent with Section 6.7's explicit framing: this guard is a UX-layer convenience (redirecting a user away from a page they'd immediately receive 403 responses on) — the actual authorization enforcement remains Volume 1, Part IV, Section 25's server-side responsibility. A route guard existing does not reduce or replace any server-side check specified there; this is restated here specifically because routing is exactly the kind of layer where a team might be tempted to treat client-side gating as sufficient.

7.4 Route-level data loading

Using React Router's data-loading APIs (loaders), each route pre-fetches its primary data dependency before rendering, integrated with Section 8's server-state layer (TanStack Query) rather than as a separate, competing data-fetching mechanism — the loader initiates the query, and the rendered Page/Feature components then read from the same cached query result via the standard hooks, avoiding a duplicate fetch. This pattern directly supports Section 4.8's Suspense-based loading strategy for route transitions specifically.

7.5 Deep linking and state restoration

Every meaningful UI state that a user might want to bookmark or share — a specific flow-run's detail view, a trend dashboard filtered to a specific module and date range, a specific flow's specific version — is represented in the URL (path parameters or query-string parameters), never held only in ephemeral component state. This is a direct UX Principle 1.2 application ("show real state, never a fiction of it") extended to navigable state specifically: a URL that doesn't fully describe what's on screen is itself a small violation of that principle, since two users navigating to the "same" URL should see the same thing.

7.6 The Workflow Designer and React Flow's canvas state — a deliberate routing exception

Section 11 (Workflow Designer) will specify that the visual canvas's pan/zoom position is not URL-encoded, as a deliberate, narrow exception to Section 7.5's general rule — canvas viewport state is ephemeral, session-local UI convenience, not meaningful application state a user would want to bookmark or share (unlike, say, which specific flow is open, which remains a URL parameter per 7.5). This exception is stated explicitly here to preempt what would otherwise look like an inconsistency in the routing design.

7.7 Not-found and error routes

A catch-all * route renders a NotFoundPage; a top-level error boundary (distinct from Section 4.6's module-scoped boundaries, this one wrapping the entire router) renders a RouteErrorPage for any unhandled rendering exception during route transition — consistent with UX Principle 1.7's "degrade honestly": a broken route never renders a blank white screen, always a clear, actionable error state, mirroring Volume 1, Part V, Section 30.5's RFC 7807 error-format philosophy translated into a full-page UX equivalent, including surfacing the same correlationId for support escalation when the error originated from a failed API call during route loading.

7.8 Navigation and the AI-generated-flow entry point

Volume 0's UC-2 (Priya using AI flow generation) has a specific routing consideration worth calling out: POST /flows/generate (Volume 1, Part V, Section 30.2) does not have its own dedicated route — it's a modal/panel action available from /flows and /flows/new (the AiFlowGenerationPanel from Section 6.2), because generating a flow is a means to arrive at a normal Draft flow, not a distinct application state — once generated, the result is a standard Flow that opens in the exact same FlowEditorPage (/flows/:id/edit) as any human-authored draft, directly reinforcing UX Principle 1.6's transparency requirement in a structural way: there is no separate "AI flow" page implying a fundamentally different, opaque kind of object.



8. State Management
8.1 Purpose and the fundamental split

This section fully specifies the server-state/client-state split introduced conceptually in Section 4.5. This split is the single most important state-management decision in this volume: server state (anything originating from Volume 1's REST API or SignalR) and client state (purely local UI concerns — a dialog's open/closed state, a form's in-progress draft values, canvas pan/zoom per Section 7.6) are managed by two deliberately different tools, never conflated into one general-purpose store.

8.2 Server state — TanStack Query

All data originating from Volume 1's REST API (Part V, Section 30) is managed through TanStack Query (React Query), never manually fetched-and-useState'd. Every query in Volume 1's catalog (Part II, Section 14) has a corresponding typed hook:

typescript
function useFlowById(flowId: FlowId) {
  return useQuery({
    queryKey: ['flows', flowId],
    queryFn: () => apiClient.getFlow(flowId),   // Section 4.7's generated, typed client
    staleTime: 30_000,  // matches this data's read-your-own-writes-adjacent
                          // consistency expectations, see 8.3
  });
}

function usePublishFlow() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (flowId: FlowId) => apiClient.publishFlow(flowId),
    onSuccess: (_, flowId) => {
      queryClient.invalidateQueries({ queryKey: ['flows', flowId] });
      queryClient.invalidateQueries({ queryKey: ['flows', 'list'] });  // the list view
                                                                          // must reflect the
                                                                          // new Published status
    },
  });
}
8.3 Cache invalidation strategy — mirroring Volume 1's read-model consistency guarantees

This is the most architecturally significant part of this section, and it is a direct, deliberate mirror of Volume 1, Part V, Section 23.6's explicit per-read-model consistency table — restated here as a frontend cache-configuration table so the two documents' guarantees never silently diverge:

Query category	staleTime	Invalidation trigger	Mirrors (Volume 1)
GetFlowByIdQuery, ListFlowsQuery	Short (30s) + explicit invalidation on mutation	Any flow-authoring mutation for that flow	FlowSummaryReadModel — strongly consistent, Part V §23.6
GetFlowReportQuery, trend queries	Longer (2min) + SignalR-driven invalidation (Section 15) for actively-watched runs	FlowRunCompletedEvent received via SignalR, or explicit refetch	FlowReportReadModel — eventually consistent, Part V §23.6
GetPassRateTrendQuery, GetFlakeRateQuery	Long (10min), no SignalR invalidation	Manual refresh or natural cache expiry	TrendAggregateReadModel — batch-consistent, Part V §23.6 — and per that section's own requirement, the UI displays a "last updated" timestamp sourced from the query response, never implying fresher data than the backend actually guarantees
GetAvailableModuleActionsQuery	Medium (1min)	Manual refresh; rarely changes during a session	ModuleActionReadModel — eventually consistent, Part V §23.6

This table is not an independent frontend design choice — it is a derived table, and any change to Volume 1, Part V, Section 23.6's consistency guarantees requires a corresponding review of this table, flagged here explicitly as a cross-volume maintenance dependency (the same category of obligation Volume 1, Part VI, Section 40.8 established for intra-volume deviations, now extended across the volume boundary).

8.4 Client state — Zustand for cross-component local state, useState for single-component state

Purely local UI state that doesn't need to be shared beyond a single component tree uses plain useState/useReducer. State that needs to be shared across components without prop-drilling (the Workflow Designer's canvas selection state, Section 11; the currently-open dialog stack) uses small, purpose-scoped Zustand stores — never one large, global application-state store, consistent with Architecture Principle 20.7's composable-primitives preference (Volume 1, Section 20.7) applied here: many small, independently-testable stores over one monolithic one.

typescript
interface FlowCanvasStore {
  selectedStepId: FlowStepId | null;
  selectStep: (id: FlowStepId | null) => void;
}

const useFlowCanvasStore = create<FlowCanvasStore>((set) => ({
  selectedStepId: null,
  selectStep: (id) => set({ selectedStepId: id }),
}));
8.5 Why not Redux

Redux (or Redux Toolkit) was evaluated and deliberately not adopted: Nexus's state genuinely splits cleanly into server state (TanStack Query's actual purpose-built domain) and small, scoped client state (Zustand's) — Redux's value proposition (a single normalized store with time-travel debugging, middleware-based side effects) solves problems this architecture doesn't have once the server/client split is respected. Adopting Redux would mean either duplicating server state into Redux (directly violating Section 4.5's "server state is never duplicated" rule) or using Redux only for the client-state slice, at which point it offers no advantage over the lighter-weight Zustand while adding boilerplate — a direct application of the same "don't add complexity that doesn't earn its cost" principle already cited in Section 4.8 and 8.4.

8.6 Optimistic updates — where and how, per UX Principle 1.2

Referencing Volume 2, Section 1.3's principle directly: optimistic updates are used narrowly, only where the operation's success is highly predictable and the UX cost of waiting is meaningful (e.g., toggling a flow's ContinueOnFailure flag in the editor) — and every optimistic update is implemented with TanStack Query's built-in rollback-on-error mechanism, never a manual, ad hoc local-state patch:

typescript
const mutation = useMutation({
  mutationFn: updateFlowFlag,
  onMutate: async (newFlag) => {
    await queryClient.cancelQueries({ queryKey: ['flows', flowId] });
    const previous = queryClient.getQueryData(['flows', flowId]);
    queryClient.setQueryData(['flows', flowId], (old) => ({ ...old, continueOnFailure: newFlag }));
    return { previous };  // used for rollback in onError
  },
  onError: (_err, _newFlag, context) => {
    queryClient.setQueryData(['flows', flowId], context?.previous);  // exact reconciliation,
                                                                        // per UX Principle 1.3 —
                                                                        // never left inconsistent
  },
});

High-risk or slow operations (publish, trigger run, module registration) are explicitly not optimistic — the UI shows a clear pending/loading state and waits for the authoritative server response, consistent with UX Principle 1.7's honest-degradation requirement outweighing raw responsiveness for operations where a rollback would be visually jarring or where the operation's own domain invariants (Volume 1, Part II, Section 9) make the outcome genuinely uncertain until the server responds.

8.7 State management and the Plugin UI trust boundary

Directly extending Section 5.3's import-boundary rule: marketplace module UI extensions (Section 13) have no access to the host application's TanStack Query cache or Zustand stores whatsoever — a sandboxed module's UI contribution receives only the specific, narrow props/callbacks Section 13's extension-point API provides, never a reference to the surrounding application's state management internals. This is the frontend-state-management-specific instance of the same trust-boundary principle Volume 1 established for backend plugin code (Part III, Section 18.3's deliberately narrow ExecutionContext) and Section 5.3 already established for the import graph — restated here because state access is a distinct, easily-overlooked way a boundary could otherwise leak even if imports are correctly restricted.


____________________________________________________________________________________________________________________________________________________



9. Component Library
9.1 Purpose

This section catalogs the Primitive and Composite components (Section 4.4's classification) that implement Section 2's design tokens as actual, reusable React components — the concrete library every Feature module (Section 6) builds on. It also specifies the single most architecturally important component in the entire frontend: the schema-driven parameter editor that makes UX Principle 1.3 (module parity, zero hardcoding) real.

9.2 Primitives catalog
Component	Purpose	Design tokens consumed (Section 2)
Button	Primary/secondary/ghost action triggers	--nexus-amber, motion tokens
Badge	Small status/label chips	Full signal-state palette (Section 2.6)
StatusDot	The pass/fail/skip/running visual vocabulary	Signal-state palette
Input, Select, Checkbox, TextArea	Form primitives	Typography, spacing scale
Tooltip	Contextual help, required for UX Principle 1.7's "explain why disabled" pattern	Typography
Spinner	Loading indication	Motion tokens
Icon	Lucide wrapper with the ModuleKind-to-icon mapping (Section 2.7)	—

Every Primitive is built with zero data-fetching and zero business logic, per Section 4.4's rule — a Badge component knows how to render a signal state given a status prop; it has no knowledge of what a FlowRun is.

9.3 Composites catalog
Component	Purpose	Built from
DataTable	Paginated, sortable tabular display — used by ListFlowsQuery, ListRecentFlowRunsQuery, ListUsersQuery results alike	Badge, StatusDot, pagination primitives
Modal, Drawer	Overlay containers	Button
CodeEditor	Thin Monaco wrapper (full integration detail in Section 10) — deliberately classified as a Composite, not a Feature component, because it has no domain-specific knowledge of its own; it's configured by whatever Feature component uses it	—
TrendChart	Generic time-series chart primitive (built on a charting library, Recharts, chosen for React-idiomatic composition matching this volume's component-classification philosophy) — used by PassRateTrendChart and FlakeRateWidget (Section 6.5) alike, itself knowing nothing about pass rates or flake rates specifically	—
SchemaDrivenParamEditor	The component directly implementing UX Principle 1.3, detailed fully in 9.4	Input, Select, CodeEditor, Tooltip
AiAttributionBadge	The always-visible AI-transparency treatment (UX Principle 1.7)	Badge, Tooltip
9.4 SchemaDrivenParamEditor — the component that makes module parity real

This is the single component every one of Volume 0's six modules' step-parameter editing passes through, with zero module-specific code anywhere in its implementation:

tsx
interface SchemaDrivenParamEditorProps {
  schema: ActionSchema;   // Volume 1, Part II §11.8 — generated type, Section 4.7
  values: Record<string, unknown>;
  onChange: (values: Record<string, unknown>) => void;
}

function SchemaDrivenParamEditor({ schema, values, onChange }: SchemaDrivenParamEditorProps) {
  return (
    <div className="param-editor">
      {schema.parameters.map((param) => (
        <ParamField
          key={param.name}
          spec={param}                        // ParamSpec, Volume 1 Part II §11.8
          value={values[param.name]}
          onChange={(v) => onChange({ ...values, [param.name]: v })}
        />
      ))}
    </div>
  );
}

function ParamField({ spec, value, onChange }: { spec: ParamSpec; value: unknown; onChange: (v: unknown) => void }) {
  // Renders the correct input control purely from spec.type (Volume 1 Part II §11.8's
  // ParamType: String | Number | Boolean | Object | Array) — this switch is the ONLY
  // place in the entire frontend that branches on parameter shape, and critically,
  // it branches on ParamType (a small, closed, backend-defined enum), never on
  // ModuleKind or action name — satisfying UX Principle 1.3 precisely, not just in spirit.
  switch (spec.type) {
    case 'String': return <Input value={value as string} onChange={onChange} required={spec.required} />;
    case 'Number': return <Input type="number" value={value as number} onChange={onChange} required={spec.required} />;
    case 'Boolean': return <Checkbox checked={value as boolean} onChange={onChange} />;
    case 'Object': case 'Array': return <CodeEditor language="json" value={JSON.stringify(value)} onChange={(v) => onChange(JSON.parse(v))} />;
  }
}
9.5 Variable-reference autocomplete within ParamField

Referencing Volume 1, Part II, Section 11.7's VariableExpression and Section 19.6's condition-evaluation design: a String-typed ParamField additionally offers {{vars.x.y}} autocomplete, sourced from the current flow's prior steps' declared saveAs bindings (available client-side from the currently-loaded Flow draft, Section 8's server state) — this is a genuine UX enhancement built on top of the same closed, generic ParamType switch in 9.4, not a special case requiring module-specific knowledge: any String parameter, on any module's action, gets this autocomplete uniformly.

9.6 Component documentation via Storybook

Every Primitive and Composite is documented in Storybook, with stories covering every signal state (Section 2.6) and both themes (Section 3) — this is a build-time-verified requirement (a CI check that every exported component has a corresponding story file), directly serving Volume 1, Part VI Section 40.7's documentation-as-code discipline extended to the frontend: an undocumented component is treated as an incomplete one, not a documentation gap to close later.

9.7 Visual regression testing as a design-system guardrail

Every Storybook story (9.6) is captured as a visual regression test baseline (Section 19, Testing Strategy, will specify the exact tooling) — this is the concrete enforcement mechanism protecting Section 2's design-token governance rule (2.9: no undocumented visual drift) at the component-implementation level specifically, catching the case where a component's behavior is correct but its rendered appearance has silently drifted from the design system's token definitions.

9.8 Accessibility baked into primitives, not bolted onto features

Directly anticipating Section 18 (Accessibility): every Primitive component (9.2) implements its accessibility contract (ARIA roles, keyboard interaction, focus management) once, centrally — a Modal handles focus trapping and Escape-to-close for every feature that uses it, a Select implements full keyboard navigation for every dropdown across the product. This architectural choice means Feature-level components (Section 6) inherit correct accessibility behavior automatically rather than needing to reimplement it per-surface, which is both a development-efficiency win and, more importantly, the only realistic way to achieve NFR-UX-1's WCAG 2.1 AA conformance consistently across five distinct feature modules rather than auditing each one independently for the same underlying concerns.



10. Monaco Integration
10.1 Purpose and where Monaco fits

The Monaco Editor (VS Code's editor component, chosen per Volume 0, Section 21.3's rationale for React's ecosystem depth) serves three distinct roles in Nexus's GUI, each specified separately in this section because they have genuinely different requirements: raw flow-definition editing (10.3), embedded JSON/object-parameter editing (10.4, already referenced in Section 9.4's ParamField), and read-only code/log display (10.5).

10.2 The CodeEditor composite wrapper

All three roles route through the single CodeEditor composite (Section 9.3) rather than each Feature module independently instantiating Monaco — this centralization is what makes Section 10.6's custom language services (autocomplete, validation) available consistently everywhere Monaco appears, and is a direct application of the same "define once, reuse everywhere" discipline Section 2 established for design tokens.

tsx
interface CodeEditorProps {
  language: 'json' | 'yaml' | 'nexus-flow-expression';
  value: string;
  onChange?: (value: string) => void;   // omitted => read-only mode
  height?: string;
  validationSchema?: JSONSchema;         // Section 10.4
}
10.3 Raw flow-definition editing

Directly serving a specific, deliberate authoring-experience decision: alongside the visual Workflow Designer (Section 11), Nexus offers a raw JSON/YAML editing mode for the same underlying Flow definition (Volume 1, Part II, Section 8.2) — a power-user path consistent with UX Principle 1.5 (progressive disclosure: the visual designer is the default, the raw editor is available but not front-loaded). Both editing modes operate on the exact same underlying data structure and the exact same publish-time validation (Volume 1, Part II, Section 9.2's Flow.Publish), switchable without data loss — a flow author can start visually, drop into raw JSON for a complex conditional expression Section 11's canvas doesn't have a dedicated widget for, and switch back, with both views staying synchronized against the same in-memory draft state (Section 8's client-state layer holding the current unsaved draft).

10.4 JSON Schema-driven validation for embedded object/array parameters

Referencing Section 9.4's ParamField handling of Object/Array-typed parameters: the embedded CodeEditor instance is configured with a JSON Schema derived directly from the relevant ActionSchema's parameter definition (Volume 1, Part II, Section 11.8), giving real-time inline validation and autocomplete for structured parameter values — e.g., an API module's assert array parameter (referencing the Assertion discriminated union from Volume 1, Part II, Section 11.6) gets full autocomplete for status/path/equals/contains/exists fields as the user types, generated from the same backend-defined schema, not a hand-maintained frontend copy of it.

typescript
function deriveJsonSchema(paramSpec: ParamSpec): JSONSchema {
  // Maps Volume 1's ParamSpec/ActionSchema shape to a standard JSON Schema document —
  // a pure, deterministic transformation with no module-specific logic, satisfying
  // UX Principle 1.3 at the validation layer specifically, not just the rendering layer
  // Section 9.4 already covers.
}
10.5 Read-only code/log display

Log excerpts (a StepResult's Data field when it contains log content, per Volume 1's Logs module), page HTML snippets shown during self-heal review (Volume 1, Part III, Section 18's RequestSelectorHealCommand flow, surfaced in the AI-attribution detail view per UX Principle 1.6), and raw API response bodies (the API module's StepResult.Data) are all displayed via CodeEditor in read-only mode with appropriate syntax highlighting — chosen over a plain <pre> block specifically for Monaco's built-in features (line numbers, search-within, minimap for long content) that meaningfully aid Persona Priya's debugging workflow when investigating a failed run.

10.6 Custom language service — nexus-flow-expression

The {{vars.x.y}} variable-expression syntax (Volume 1, Part II, Section 11.7) gets a genuine, custom Monaco language service — not merely syntax highlighting via a regex grammar, but a real completion provider and hover provider:

typescript
monaco.languages.registerCompletionItemProvider('nexus-flow-expression', {
  triggerCharacters: ['.'],
  provideCompletionItems(model, position) {
    // Resolves available variable paths from the currently-loaded Flow's prior
    // steps' saveAs bindings (Section 9.5's same data source), offering
    // structured completion (vars.orderInfo.id, vars.orderInfo.status, etc.)
    // derived from the ACTUAL StepResult.Data shape of a representative prior
    // run if one exists (an enhancement beyond static schema knowledge alone —
    // if step 1 is an API module call whose last real execution returned a
    // JSON body with a `repos_url` field, that field is offered as a concrete
    // completion suggestion, not just a generic "any property" placeholder).
  },
});

This "completion informed by real historical execution data" capability is a deliberate, specified UX enhancement directly serving NFR-UX-2's 30-minute time-to-first-flow target — it's substantially easier for Priya to chain step 2's parameters off step 1's actual observed output shape than to guess at an unfamiliar target API's response structure from documentation alone.

10.7 Performance considerations

Monaco is a genuinely heavy dependency (a large JS bundle); per Section 17 (Performance)'s forthcoming code-splitting strategy, the CodeEditor composite is lazy-loaded — the visual Workflow Designer (Section 11), which most authoring sessions use as the primary interface per UX Principle 1.5, does not pay Monaco's bundle-size cost unless a user actually opens the raw-editing mode (10.3) or a parameter field requiring structured JSON input (10.4).

10.8 Accessibility considerations specific to Monaco

Monaco has known, well-documented accessibility limitations relative to native form controls (screen-reader interaction with a canvas-rendered code editor is inherently more complex than with a <textarea>) — Section 18 (Accessibility) will specify Monaco's built-in accessibility mode (accessibilitySupport: 'on') as a mandatory configuration, and, critically, requires that no capability be exclusively available through Monaco with no alternative path — every Object/Array parameter editable via the embedded JSON editor (10.4) must also be constructible through a structured, fully-accessible fallback form for users who cannot effectively use the code-editor surface, directly satisfying NFR-UX-1's WCAG 2.1 AA requirement rather than treating Monaco's limitations as an accepted accessibility gap.



11. Workflow Designer
11.1 Purpose and scope — product/UX specification

This section specifies the Workflow Designer as a product surface: what a user sees, does, and experiences when visually authoring a flow. Section 12 (React Flow Integration) covers the technical implementation built on the React Flow library — this section deliberately stays at the interaction-design level, since the same visual designer experience specified here could, in principle, be rebuilt on a different underlying canvas library without changing anything in this section, while Section 12 would need a full rewrite. Keeping these separate mirrors this volume's general practice of separating "what and why" from "how," the same distinction Volume 1 maintained between its domain model (Part II) and its engineering patterns (Part III).

11.2 What "Workflow Designer" names, precisely

A naming clarification worth stating directly: this section's "Workflow Designer" is the visual canvas for authoring a single Flow (Volume 1, Part II, Section 8.2's ordered step sequence) — it is not, despite the name's echo, primarily about Volume 1, Part III, Section 20's Workflow concept (multi-flow orchestration). The canvas does support authoring a WorkflowDefinition (Volume 1, Part III, Section 20.3) when that capability ships per Volume 0's roadmap, using the same underlying visual grammar (11.3) applied to flow-level nodes instead of step-level nodes — but the primary, GA-critical use case this section specifies is single-flow authoring. This naming tension is called out explicitly to avoid the exact kind of ambiguous-terminology drift Volume 1, Part II, Section 6.3's ubiquitous-language discipline exists to prevent.

11.3 Visual grammar

Each FlowStep (Volume 1, Part II, Section 10.2) renders as a node on the canvas; steps connect via directional edges reflecting execution order (Volume 1, Part II, Section 10.2's explicit Order field — the canvas is the direct visual editor for that ordering, not an independent representation that needs separate reconciliation). A node's visual treatment is driven entirely by the same ModuleKind-to-icon mapping and signal-state palette already established in Section 2.6–2.7 — a Web-module step and an AI-module step are visually distinguished only by their module icon and color accent, never by fundamentally different node shapes or layouts, directly reinforcing UX Principle 1.3's module-parity requirement at the canvas level specifically.

┌─────────────────────┐        ┌─────────────────────┐        ┌─────────────────────┐
│  🌐  Navigate to login  │───────►│  🌐  Fill credentials   │───────►│  🔌  Verify order API   │
│      web.navigate       │        │      web.fill            │        │      api.request          │
└─────────────────────┘        └─────────────────────┘        └─────────────────────┘
11.4 Conditional branches (when) — visual representation

A step with a WhenCondition (Volume 1, Part II, Section 10.2) renders its incoming edge with a distinct dashed treatment and a small condition-summary label, directly visualizing Volume 1, Part III, Section 19.6's skip-vs-execute semantics before a flow is ever run — a flow author can see, at a glance, which steps are unconditional and which are gated, without opening each step's detail panel.

11.5 Variable-flow visualization

When a node is selected, the canvas highlights every other node that either produces a variable this step consumes (via {{vars.x.y}}, Section 10.6's autocomplete data source) or consumes a variable this step produces (via saveAs) — a direct visual answer to "where does this data come from, and where does it go," which is exactly the kind of cross-step relationship that's hard to hold in your head reading a flat JSON list (10.3's raw-editing alternative) but natural to show spatially on a canvas. This is the single strongest UX argument for the visual designer's existence as the default authoring mode (UX Principle 1.5) rather than merely a cosmetic alternative to raw editing.

11.6 Adding a step — the module/action picker

Clicking "add step" opens a picker rendering every available module and action, sourced live from GetAvailableModuleActionsQuery (Volume 1, Part II, Section 14.2; Section 6.2's flow-authoring/module-plugin Customer/Supplier relationship) — this picker is the most direct, visible proof-point of UX Principle 1.3 and Volume 0's UC-6: a newly-registered marketplace module (Persona Raj's scenario) appears in this exact picker with zero frontend changes, ordered and grouped by TrustTier (Volume 1, Part II, Section 11.9) with the same certification badge treatment established in Volume 2, Section 6.4.

11.7 Error and validation surfacing on the canvas

Referencing Volume 1, Part II, Section 9.2's publish-time validation (unresolved module/action references) and Section 10.6's AiAttribution and Volume 1, Section 19.6's skip semantics: validation errors surface directly on the affected node (a red outline plus an inline error summary), not only in a separate error panel — consistent with UX Principle 1.2's "show real state" and Architecture Principle 20.4's fail-informatively philosophy (Volume 1, Section 20.4) carried through to this specific surface. A flow with unresolved references cannot be published (server-enforced, per Volume 1) and the canvas makes the specific reason visible at the specific node responsible, rather than a generic "publish failed" toast.

11.8 Live-run overlay — the canvas doubles as a progress view

During an active FlowRun (Volume 0's UC-1), the same canvas layout (not a separate view) overlays live status per Section 2.6's signal-state treatment on each node, driven by SignalR (Section 15's full specification) — this is a deliberate reuse of the authoring canvas as the live-monitoring surface, rather than building two separate visualizations of the same flow structure, directly serving Architecture Principle 20.7's composable-primitives-over-duplication preference (Volume 1, Section 20.7) applied to the frontend.

11.9 Undo/redo and draft autosave

The canvas maintains a local undo/redo stack (client state, Section 8.4) independent of the server's Flow versioning (Volume 1, Part II, Section 9.2's Draft/Published versioning is a distinct, coarser-grained concept — a new published version, not every individual canvas edit). Draft edits autosave to the server periodically (a PATCH-style incremental update, not shown explicitly in Volume 1's command catalog as a separate command but implementable as repeated AddFlowStepCommand/step-update calls against the same Draft Flow) so that Priya's in-progress work survives a browser crash or accidental tab close — directly serving NFR-UX-2's usability bar by removing "did I lose my work" as a source of authoring friction.

11.10 Relationship to Section 12

Every capability specified in this section — node rendering, edge semantics, the live-run overlay, selection-based variable-flow highlighting — is realized technically using React Flow's specific APIs (custom node/edge types, its selection and viewport state model) in Section 12, which the next section specifies in full implementation detail.



12. React Flow Integration
12.1 Purpose and scope — technical implementation

Where Section 11 specified the Workflow Designer's product behavior, this section specifies its technical realization on React Flow, the library Volume 0, Section 21.3 anticipated by name. This section is the direct implementation counterpart to Section 11's product spec — every subsection here exists to implement one of Section 11's numbered requirements, cited explicitly throughout.

12.2 Custom node types
tsx
const nodeTypes = {
  flowStep: FlowStepNode,
  // Additional node types registered only if/when Volume 1's Workflow Engine
  // (Part III §20) ships, per Section 11.2's naming clarification — a
  // `workflowFlowRef` node type representing a whole Flow as a single node
  // within a multi-flow Workflow canvas.
};

function FlowStepNode({ data, selected }: NodeProps<FlowStepNodeData>) {
  const module = useModuleRegistration(data.moduleKind);  // Section 8's server state
  return (
    <div className={cn('flow-step-node', selected && 'selected', data.validationError && 'error')}>
      <Icon name={module.icon} />                          {/* Section 2.7 */}
      <span className="step-name">{data.name}</span>
      <span className="step-action">{data.moduleKind}.{data.action}</span>
      {data.liveStatus && <StatusDot status={data.liveStatus} />}   {/* Section 11.8 */}
      {data.validationError && <ErrorIndicator message={data.validationError} />}  {/* Section 11.7 */}
    </div>
  );
}

FlowStepNodeData carries exactly the fields Section 11.3, 11.7, and 11.8 require — module/action identity, current validation state, and (during an active run) live status — sourced from a single derived data structure combining the Draft Flow's definition (Section 8's server state) with any active FlowRun's live progress (Section 15), computed once per render rather than each node independently querying multiple data sources.

12.3 Custom edge types — conditional vs. unconditional
tsx
const edgeTypes = { flowEdge: FlowEdge };

function FlowEdge({ data, ...edgeProps }: EdgeProps<FlowEdgeData>) {
  return (
    <BaseEdge
      {...edgeProps}
      style={data.isConditional ? { strokeDasharray: '5,5' } : undefined}  // Section 11.4
    />
  );
}
12.4 Layout algorithm

Node positions are computed automatically on initial load (a directed-graph auto-layout, using a library such as Dagre, laying out steps left-to-right in execution order per FlowStep.Order) and then become user-adjustable — a flow author can drag nodes to a preferred arrangement, with the adjusted layout persisted as presentation metadata separate from the Flow aggregate's actual step-order data (Volume 1, Part II, Section 9.2's Order field remains the single source of truth for execution sequence; canvas position is purely a display concern). This separation is deliberate and directly mirrors Volume 1, Part V, Section 32.2's schema design principle of keeping presentation-adjacent data (like declared_vars) distinct from execution-critical fields — canvas layout here is genuinely presentation-only and never influences execution order.

12.5 Selection state and variable-flow highlighting
tsx
function useVariableFlowHighlight(selectedNodeId: string | null, flow: FlowDetailDto) {
  return useMemo(() => {
    if (!selectedNodeId) return { highlightedNodes: [], highlightedEdges: [] };
    const selectedStep = flow.steps.find((s) => s.id === selectedNodeId);
    // Cross-references selectedStep.params against every other step's saveAs
    // (Volume 1 Part II §10.2) using the same VariableExpression.ReferencedPaths
    // data Section 10.6's autocomplete already computes — reused here, not
    // recomputed independently, per this volume's "define once" discipline.
    return computeVariableFlowGraph(selectedStep, flow.steps);
  }, [selectedNodeId, flow]);
}

This hook directly implements Section 11.5's product requirement, built on React Flow's useReactFlow() selection APIs combined with the derived variable-reference data already established for Monaco's autocomplete (Section 10.6) — a second, deliberate instance of reusing one underlying data computation across two different surfaces (code-editor autocomplete and canvas highlighting) rather than maintaining two parallel implementations of "what variables does this step reference."

12.6 Viewport state — the Section 7.6 routing exception, implemented
tsx
function WorkflowCanvas({ flowId }: { flowId: FlowId }) {
  const viewport = useFlowCanvasStore((s) => s.viewport);  // Zustand, Section 8.4 —
                                                               // NOT URL state, per Section 7.6
  const setViewport = useFlowCanvasStore((s) => s.setViewport);
  return <ReactFlow defaultViewport={viewport} onMoveEnd={(_, vp) => setViewport(vp)} ... />;
}
12.7 Live-run overlay data flow
tsx
function useLiveNodeStatuses(runId: FlowRunId | null): Record<string, StepStatus> {
  const { data: liveEvents } = useLiveRunProgress(runId);  // Section 15's SignalR hook,
                                                               // composed here rather than
                                                               // this component managing its
                                                               // own SignalR subscription directly
  return useMemo(() => Object.fromEntries(
    liveEvents.map((e) => [e.sourceStepId, e.status])
  ), [liveEvents]);
}

This is the concrete implementation of Section 11.8's "canvas doubles as progress view" — critically, WorkflowCanvas (12.6) does not know or care whether runId is null (pure authoring mode) or populated (live-monitoring mode); the same component renders both, with FlowStepNode (12.2) simply receiving liveStatus: undefined in the authoring case, directly realizing Section 11.8's "not a separate view" requirement as an actual, provable code-level fact rather than just a design intention.

12.8 Performance — large flow rendering

React Flow's built-in virtualization (only rendering nodes within or near the current viewport) is enabled by default, but Nexus's specific concern — per Product Philosophy 4.2's "flows should be short, vertical slices" (Volume 0) — is that a well-designed flow should rarely have enough steps for this to matter; a flow with node counts in the hundreds is itself a signal the flow author should consider splitting it (Volume 1, Part III, Section 20's Workflow-level composition) rather than a performance case this canvas needs to heavily optimize for. This is stated explicitly because it's a deliberate scope decision — the canvas is engineered to be smooth for realistic flow sizes (tens of steps), not exhaustively load-tested against pathological thousand-node flows that would themselves represent a violation of the product's own stated design philosophy.

12.9 Testing the canvas

Given React Flow's canvas-based (SVG/DOM-hybrid) rendering, component tests for FlowStepNode and FlowEdge render them in isolation (outside a full ReactFlow provider tree) using React Testing Library, asserting on the data-driven rendering logic in 12.2–12.3 directly; full canvas interaction (drag, connect, select) is covered by the end-to-end test suite (Section 19) rather than attempting brittle unit-level simulation of React Flow's internal drag/gesture handling — a pragmatic testing-layer split consistent with Volume 1, Part VI, Section 40.6's own tiered testing philosophy (fast, isolated unit tests plus a smaller set of slower, higher-fidelity integration/e2e tests) applied here to the frontend's most visually complex component.



13. Plugin UI
13.1 Purpose — the frontend counterpart to Volume 1's Plugin Architecture

Volume 1, Part III, Section 18 specified the backend AutomationModule contract and its trust-tiered execution model. This section specifies the equivalent frontend concern: how a marketplace-installed module can supply custom UI for its own action parameters — beyond what Section 9.4's generic, schema-driven SchemaDrivenParamEditor provides — without compromising the host application's security, design consistency, or stability. This is the most security-sensitive section in this volume, directly analogous in importance to Volume 1, Part III, Section 18's standing as that volume's most consequential section.

13.2 Why custom UI extension is needed at all, given Section 9.4's generic editor

Section 9.4's schema-driven editor handles every ParamType generically and correctly, satisfying UX Principle 1.3's baseline requirement. But some modules benefit from a richer, purpose-built parameter-entry experience beyond generic form fields — a module for a specific SaaS platform's API might want to render a live-searchable resource picker instead of asking a user to paste a raw ID string. This is an enhancement a module may optionally supply, never a requirement — every module works correctly with zero custom UI, using Section 9.4's generic editor as the guaranteed fallback, directly preserving UX Principle 1.3's "zero hardcoding, works for any module" guarantee even for modules that supply no custom UI at all.

13.3 The extension-point contract
typescript
// Published as part of the same Nexus.Contracts-equivalent frontend SDK package
// (mirroring Volume 1, Part V §36.2's decision to publish the backend
// AutomationModule interface as an independently-versioned package)
interface NexusParamEditorExtension {
  paramName: string;               // which specific ParamSpec this extension enhances
  render: (props: ParamEditorExtensionProps) => React.ReactNode;
}

interface ParamEditorExtensionProps {
  value: unknown;
  onChange: (value: unknown) => void;
  theme: Readonly<ThemeTokens>;     // Section 3.8 — read-only token access, nothing more
  // Deliberately absent: any reference to the host application's TanStack Query
  // client, Zustand stores, router, or any other feature module — per Section
  // 8.7's trust-boundary rule, restated and enforced concretely here.
}
13.4 Sandboxed rendering via iframe, not in-process component execution

This is the single most important, and most different-from-the-backend, technical decision in this section: unlike Volume 1, Part III, Section 18.5's in-process execution option for Verified/Built-in-tier backend modules, frontend plugin UI extensions always render inside a sandboxed <iframe>, regardless of the module's TrustTier (Volume 1, Part II, Section 11.9) — there is no "trusted, in-process" tier for frontend code. This asymmetry with the backend's trust-tiered model is deliberate and justified specifically: arbitrary third-party JavaScript executing directly within the host application's own React tree could read authentication tokens, manipulate the DOM outside its intended boundary, or exfiltrate data through means an in-process backend module's narrower ExecutionContext boundary (Volume 1, Part III, Section 18.3) doesn't have to guard against in the same way — a browser's cross-origin iframe sandbox is a fundamentally stronger, browser-enforced isolation primitive than anything achievable with in-process JavaScript execution, and this section adopts the strongest available option universally rather than trying to replicate the backend's tiered trust model in an environment where the strongest isolation is cheap enough to always use.

tsx
function PluginParamEditor({ extension, value, onChange, theme }: PluginParamEditorProps) {
  return (
    <SandboxedIframe
      src={extension.bundleUrl}                    // served from an isolated,
                                                       // separate origin, never the
                                                       // host application's own origin
      sandbox="allow-scripts"                       // explicitly NOT allow-same-origin
      onMessage={(msg) => handlePluginMessage(msg, onChange)}
      postMessage={{ type: 'init', value, theme }}   // theme tokens passed in,
                                                        // read-only, per Section 3.8
    />
  );
}
13.5 Communication protocol — postMessage, strictly typed and validated

All communication between the host application and a sandboxed plugin extension happens via window.postMessage, using a small, closed message-type union (init, valueChanged, resize, error) — every incoming message from the iframe is validated against this schema before being acted upon, and any malformed or unrecognized message is dropped and logged (Section 15's telemetry, extended to cover plugin-UI anomalies) rather than trusted. This mirrors Volume 1, Part III, Section 18.5's gRPC-based sandboxed-module communication design philosophy exactly — a narrow, explicit, validated protocol rather than a broad, implicit trust relationship — translated to the browser's available isolation primitives.

13.6 What a plugin UI extension cannot do

Stated exhaustively, since a security boundary is only as good as its explicitly-enumerated limits: a plugin UI extension cannot navigate the host page, cannot access cookies or localStorage of the host origin (enforced by the cross-origin iframe boundary itself, not merely a convention), cannot make network requests to the Nexus API directly (any data it needs beyond ParamEditorExtensionProps must be explicitly passed in via the init message, per 13.4's deliberately narrow props), and cannot render outside its allocated iframe bounds (no ability to create modals, tooltips, or overlays that escape its container — a real UX constraint module developers must design within, stated here as an accepted trade-off in exchange for the security guarantee).

13.7 Design consistency within the sandbox

Per Section 3.8's forward reference: the theme prop (13.3) gives a plugin extension read-only access to the current theme's design tokens (Section 2–3), so a well-behaved extension can render visually consistent with the host application despite running in an isolated iframe — this is offered as a capability, not enforced, since the sandbox boundary means Nexus cannot force a third party's rendered output to comply with the design system (Section 2.9's governance model applies to first-party code; it cannot reach across the trust boundary this section establishes). The Verified/Community certification review (Volume 0, Section 17.3) is the actual mechanism for catching a poorly-designed or inconsistent plugin UI extension before it's listed, not a runtime technical enforcement.

13.8 Graceful fallback on extension failure

Directly extending Section 4.6's module-scoped error boundary discipline: if a plugin UI extension's iframe fails to load, times out, or posts a malformed error message (13.5), the SchemaDrivenParamEditor's generic rendering (Section 9.4) is used as an automatic fallback — a broken or unavailable custom UI extension never blocks a flow author from configuring that parameter entirely, directly preserving UX Principle 1.7's honest-degradation requirement even in this most security-constrained, third-party-dependent corner of the product.

13.9 Certification tier and UI extension review

Referencing Volume 0, Section 17.3's three-tier certification model: a module's UI extension bundle is reviewed as part of the same Verified/Enterprise-certified review process as its backend code — the sandboxing in 13.4 is defense-in-depth, not a substitute for the marketplace review process, mirroring exactly the relationship Volume 1, Part III, Section 18.5 established between sandboxed execution and certification tier for backend module code (both matter; neither is sufficient alone).


____________________________________________________________________________________________________________________________________________________



14. Authentication
14.1 Purpose — the frontend's role given Volume 1's authentication design

Volume 1, Part IV, Section 24 specified JWT-based sessions, API keys, and SSO/OIDC as Nexus's supported authentication mechanisms, with a deliberate design choice (Section 24.6 there) that permissions are never embedded in the JWT, only a RoleVersion claim. This section specifies how the GUI implements the client side of that design — token handling, session lifecycle, and the useAuth()/usePermission() hooks that every Feature module (Volume 2, Section 6.6–6.7) depends on.

14.2 Token storage — memory-first, never localStorage

Access tokens (Volume 1, Part IV, Section 24.5) are held in memory (a module-scoped variable within features/identity-access, never in localStorage or sessionStorage) — this is a deliberate, non-negotiable security decision: localStorage is readable by any script executing in the page's origin, meaning any successful XSS vulnerability anywhere in the application (including, hypothetically, a compromised third-party dependency) would trivially exfiltrate a localStorage-held token, whereas an in-memory-only token is meaningfully harder to exfiltrate this way. Refresh tokens (Volume 1, Part IV, Section 24.5's rotation-on-use design) are held in an HttpOnly, Secure, SameSite=Strict cookie, set directly by the API Layer's login/refresh response — never readable by JavaScript at all, closing the exfiltration vector entirely for the longer-lived credential.

14.3 Session bootstrap and silent refresh
typescript
function useAuthBootstrap() {
  const [state, setState] = useState<'loading' | 'authenticated' | 'unauthenticated'>('loading');

  useEffect(() => {
    // On app load, no access token exists in memory (14.2) — attempt a silent
    // refresh using the HttpOnly refresh-token cookie, which the browser
    // sends automatically. A successful refresh yields a new in-memory access
    // token; a failure (expired/absent refresh token) means the user must log
    // in interactively.
    refreshAccessToken()
      .then((token) => { setAccessToken(token); setState('authenticated'); })
      .catch(() => setState('unauthenticated'));
  }, []);

  return state;
}

Access tokens are proactively refreshed shortly before their known 15-minute expiry (Volume 1, Part IV, Section 24.5), not only reactively on a 401 response — this keeps Persona Priya's authoring session (Volume 0, UC-2) from ever hitting an expired-token interruption mid-flow-edit, directly serving UX Principle 1.2's "never a fiction of the real state" by keeping the client's authenticated state genuinely, continuously valid rather than allowing it to silently lapse.

14.4 The useAuth() and usePermission() hooks
typescript
function useAuth(): { user: UserSummaryDto | null; isAuthenticated: boolean } {
  // Reads from the identity-access feature's internal state (Section 6.6) —
  // the ONLY public surface other features are permitted to import,
  // per Section 5.5's index.ts convention.
}

function usePermission(permission: Permission): { hasPermission: boolean; isLoading: boolean } {
  const { user } = useAuth();
  const { data: permissions, isLoading } = usePermissionsQuery(user?.id);  // Section 8's
                                                                              // server-state layer,
                                                                              // backed by Volume 1
                                                                              // Part II §14.6's
                                                                              // GetUserPermissionsQuery
  return { hasPermission: permissions?.includes(permission) ?? false, isLoading };
}

Every Feature module's permission-gated rendering (Volume 2, Section 6.7) and every route guard (Section 7.3) consumes exactly these two hooks — never a component's own ad hoc token-parsing or role-checking logic, keeping permission-checking behavior centralized and consistent, mirroring the same centralization discipline Section 9's Component Library established for design tokens.

14.5 RoleVersion cache invalidation on the frontend

Directly extending Volume 1, Part IV, Section 24.6/25.5's RoleVersion mechanism to the frontend cache layer: when a 401/403 response includes a RoleVersion mismatch indicator (a header the API Layer adds specifically for this purpose, an extension of Volume 1's error-response design not detailed in Volume 1 since it's a frontend-consumption concern, flagged here as the concrete cross-volume dependency this creates, consistent with this document set's practice of surfacing such gaps explicitly rather than leaving them implicit), the frontend immediately invalidates its cached usePermission() results (Section 8.2's TanStack Query invalidation) and refetches — ensuring a role change made by an administrator (Volume 1, Part II, Section 13.5's AssignRoleCommand) is reflected in an already-open browser tab within one request round-trip, not only on the affected user's next full page load.

14.6 API key management UI

Serving Persona Marcus's CI service-account needs (Volume 1, Part IV, Section 24.4): the ApiKeyManager component (Volume 2, Section 6.6) displays existing keys by their public prefix only (never the secret value, which per Volume 1's design is never retrievable after initial creation) with creation, revocation, and last-used-timestamp visibility — directly surfacing Volume 1's CredentialRotatedEvent-driven audit trail (Volume 1, Part II, Section 12.5) in a human-readable form, giving Marcus visibility into exactly when and whether his CI credential was recently used, a concrete trust-and-debugging aid for a persona who otherwise barely touches the GUI (Volume 0, Section 10.3).

14.7 SSO/OIDC flow

For enterprise-tier tenants (Volume 1, Part IV, Section 24.7), the login page conditionally renders an SSO-initiated login button (tenant-configuration-driven, resolved before rendering the standard username/password form) that redirects to the customer's IdP and returns via a standard OIDC authorization-code-with-PKCE flow — the PKCE requirement specifically because this is a public (browser-based) client per OAuth2 best practice, with the authorization code exchanged server-side (the API Layer) rather than the frontend ever handling a client secret, keeping no long-lived secret material in browser-reachable code at any point in the flow.

14.8 Logout and session termination

Logout clears the in-memory access token (14.2) and calls a dedicated API endpoint that invalidates the refresh-token cookie server-side (not merely clearing it client-side, since a stolen refresh-token cookie should be immediately unusable, not merely inconvenient to resend) — directly closing the loop with Volume 1, Part IV, Section 24.5's refresh-token-rotation design, ensuring "logout" is a real, server-enforced session termination, not a purely cosmetic client-state reset.

14.9 Cross-tab session consistency

A BroadcastChannel (or storage event fallback for older browsers) synchronizes authentication state across multiple open tabs of the same origin — a logout in one tab immediately reflects as logged-out in every other open tab, and a token refresh in one tab is shared rather than each tab independently and redundantly refreshing — directly serving UX Principle 1.2 (consistent real state) across the multi-tab usage pattern that's genuinely common for Priya's workflow (one tab authoring a flow, another watching its live run, per Volume 2 Section 11.8's canvas-as-progress-view design).



15. SignalR Integration
15.1 Purpose — the frontend client for Volume 1's RunProgressHub

Volume 1, Part V, Section 31 specified the RunProgressHub — group-per-run scoping, the REST-catch-up-plus-SignalR-stream reconnection pattern, and the explicit boundary that this hub serves only actively-watched-run progress, nothing else. This section specifies the frontend connection management, the hooks that Section 6.3 (execution feature) and Section 12.7 (the canvas live-overlay) both consume, and how this integrates with Section 8's server-state layer.

15.2 Connection lifecycle — a single, shared connection
typescript
const signalRConnection = new HubConnectionBuilder()
  .withUrl('/hubs/run-progress', { accessTokenFactory: () => getAccessToken() })  // Section 14.2
  .withAutomaticReconnect({ nextRetryDelayInMilliseconds: exponentialBackoff })
  .build();

A single SignalR connection is established once per authenticated session (not once per component watching a run) and shared across every subscriber via a lightweight connection-manager module in realtime/ (Section 5.2) — multiple components watching different runs simultaneously (e.g., a list view showing several in-progress runs at once) multiplex over this one connection, each joining its own server-side group (Volume 1, Part V, Section 31.3) rather than each opening a redundant separate connection.

15.3 The useLiveRunProgress hook
typescript
function useLiveRunProgress(runId: FlowRunId | null): { events: StepCompletedEventDto[]; status: RunStatus | null } {
  const queryClient = useQueryClient();
  const [events, setEvents] = useState<StepCompletedEventDto[]>([]);

  useEffect(() => {
    if (!runId) return;

    // 1. Catch-up fetch — Section 8's server state, backing Volume 1 Part II
    //    §14.3's GetLiveRunProgressQuery, per the REST-then-stream pattern
    //    Volume 1 Part V §31.4 specifies exactly.
    queryClient.fetchQuery(liveRunProgressQuery(runId)).then((initial) => setEvents(initial.stepResults));

    // 2. Subscribe
    signalRConnection.invoke('SubscribeToRun', runId);
    const handler = (evt: StepCompletedEventDto) => setEvents((prev) => [...prev, evt]);
    signalRConnection.on('StepUpdate', handler);

    return () => {
      signalRConnection.invoke('UnsubscribeFromRun', runId);
      signalRConnection.off('StepUpdate', handler);
    };
  }, [runId]);

  // 3. On reconnect, re-run the catch-up fetch — Section 15.4
  useOnSignalRReconnect(() => { if (runId) queryClient.invalidateQueries(liveRunProgressQuery(runId)); });

  return { events, status: deriveRunStatus(events) };
}

This hook is the single implementation every consumer (Section 6.3's RunProgressView, Section 12.7's canvas overlay) shares — directly avoiding two independently-written, potentially-divergent SignalR subscription implementations for what is conceptually one capability, consistent with this volume's repeated "define once" discipline (Sections 9.4, 10.6, 12.5 all made the same choice for their respective concerns).

15.4 Reconnection handling — implementing Volume 1's explicit warning

Volume 1, Part V, Section 31.4 was explicit: "the client re-subscribes AND re-fetches via GetLiveRunProgressQuery again — never assumes no events were missed during the gap." Section 15.3's useOnSignalRReconnect callback is the literal implementation of that instruction — a connection drop and automatic reconnect (handled by SignalR's built-in withAutomaticReconnect, 15.2) triggers a full state reconciliation via the REST catch-up query, never a bare resume-and-hope. A brief "reconnecting..." indicator (using the design system's signal-state vocabulary, Section 2.6 — specifically an amber, non-error treatment, since a reconnect-in-progress is expected transient behavior, not a failure) is shown during this window, directly serving UX Principle 1.2's "show real state" by never letting the UI silently display now-possibly-stale data without any indication.

This reconciliation is trustworthy for a stronger reason than client-side connection handling alone: Volume 1, Part III, Section 16.7's incremental per-step commit design means the catch-up query reflects genuinely durable backend state, not merely "whatever the server happened to have in memory." A reconnect that spans not just a brief client-side network blip but an actual backend process interruption (an orchestration engine pod restart mid-run, per Volume 1, Part VI, Section 37.6's graceful-shutdown handling) still resolves correctly here — the catch-up fetch returns every step genuinely completed before the interruption, because Volume 1 now guarantees each one was individually committed as it happened, not batched and at risk of being lost together. This frontend section's reconciliation logic required no design change to benefit from that backend guarantee; it was already built to distrust its own assumptions about what it might have missed (per Volume 1, Part V, Section 31.4's original instruction), and simply inherits a stronger, now-provably-accurate answer to "what actually happened" as a result.

15.5 Backplane transparency — the frontend doesn't need to know

Volume 1, Part V, Section 31.5 specified a Redis-backed SignalR backplane for horizontal scaling across multiple nexus-orchestration/API Layer instances. This is entirely transparent to the frontend client — the connection-management code in 15.2–15.3 requires zero awareness of backend scaling topology, which is precisely the point of that backend design decision: the frontend's contract is with the SignalR hub abstraction, not with the specific instances serving it, a direct, concrete instance of clean separation between this volume's concerns and Volume 1's paying off exactly as intended.

15.6 Multiplexing across multiple watched runs
typescript
function useMultiRunLiveStatus(runIds: FlowRunId[]): Record<string, RunStatus> {
  // Used by, e.g., a "runs in progress" widget on the reporting dashboard's
  // real-time-adjacent corner — but NOT the main trend dashboard itself,
  // per Section 6.5's explicit rule that reporting-audit's primary views
  // never depend on SignalR. This hook is scoped to a genuinely different,
  // narrower use case: a small, explicitly real-time widget, not the
  // dashboard's core data.
}

This hook is called out specifically to reinforce, at the implementation level, the boundary Section 6.5 already established at the feature-architecture level — SignalR usage in the reporting surface is narrow and explicitly justified per instance, never a default assumption that "more real-time is always better."

15.7 Error and degraded-connectivity handling

If the SignalR connection fails to establish at all (a restrictive network/proxy blocking WebSocket upgrade, falling back through SignalR's negotiated transport options to long-polling, and ultimately failing even that), useLiveRunProgress degrades to REST-polling at a conservative interval (e.g., every 5 seconds) rather than leaving the live-progress view entirely non-functional — a direct application of UX Principle 1.7's honest-degradation requirement: a user on a restrictive network sees slightly-less-live progress updates with a visible "live updates unavailable, refreshing periodically" notice, never a silently frozen view that looks live but isn't.

15.8 Testing SignalR-dependent components

Given the real-time, connection-stateful nature of this integration, useLiveRunProgress and its consumers are tested against a mocked HubConnection implementation (a test double satisfying the same interface as 15.2's real connection) for unit/component tests, with genuine SignalR connection behavior — including the reconnection-and-reconcile path specified in 15.4 — covered by the end-to-end test suite (Section 19) running against a real backend instance, consistent with the same unit-vs-integration testing tier split already established for the canvas in Section 12.9.



16. Offline Support
16.1 Scoping this section honestly

Consistent with this document set's established practice of stating limitations plainly rather than overclaiming (Volume 1, Part VI, Section 39.9's honest scaling-ceiling disclosure is the direct model this section follows): Nexus is not, and is not being built as, an offline-first application. Every one of its core capabilities — triggering a flow run, viewing live progress, browsing the marketplace, reading reports — fundamentally requires connectivity to the backend, since the backend is where actual automation execution happens (Volume 1, Part I, Section 2.4's system boundary). This section specifies what "offline support" concretely and honestly means for a tool like this: resilience to brief connectivity interruption, and protection against data loss during that interruption — not full offline operation.

16.2 What this section does deliver

Three concrete guarantees, each scoped to a specific, realistic failure mode rather than a general "works offline" claim:

In-progress authoring work survives a connectivity drop (16.3).
The user is always told the truth about connectivity state (16.4), never left guessing whether the app is broken or the network is.
Queued, safe-to-retry mutations are not silently lost during a brief interruption (16.5).
16.3 Draft persistence — local-first for unsaved authoring state

Building on Volume 2, Section 11.9's autosave design: in addition to periodic server-side autosave, the Workflow Designer's in-progress draft state is also persisted to IndexedDB locally, on every meaningful edit, independent of network availability. If a connectivity drop occurs mid-edit, the user can continue editing locally (the canvas remains fully interactive — it's a client-rendered application, per Section 4.3, and editing a Flow draft is a local, in-memory operation until explicitly saved) and the local IndexedDB copy is reconciled against the server the moment connectivity returns:

typescript
async function reconcileDraftOnReconnect(flowId: FlowId) {
  const localDraft = await getLocalDraft(flowId);         // IndexedDB
  const serverDraft = await apiClient.getFlow(flowId);     // Section 8's server state
  if (localDraft.lastEditedAt > serverDraft.updatedAt) {
    await apiClient.syncDraft(flowId, localDraft);          // push local changes
  } else if (serverDraft.updatedAt > localDraft.lastEditedAt) {
    // A rare case (e.g., the same draft edited in a second tab that stayed
    // connected) — surfaced to the user as an explicit conflict choice,
    // never silently overwritten in either direction, per UX Principle 1.2
    showDraftConflictDialog(localDraft, serverDraft);
  }
}

This reconciliation logic deliberately never silently discards either version — a genuine, if rare, conflict is surfaced to the human, consistent with UX Principle 1.2's "never a fiction of the real state" applied specifically to the case where two "real states" (local and server) have diverged and only a human can correctly decide which one wins.

16.4 Connectivity status — always visible, never assumed

A persistent, unobtrusive connectivity indicator (using the design system's signal-state vocabulary, Section 2.6) reflects genuine, actively-monitored connectivity — not merely navigator.onLine (a notoriously unreliable browser API that reports link-layer connectivity, not actual reachability of the Nexus backend specifically), but a real heartbeat check against the API Layer's health endpoint (Volume 1, Part IV, Section 29.7's /health/live) combined with the SignalR connection's own state (Section 15.4's reconnection handling). This directly extends UX Principle 1.7's honest-degradation requirement to connectivity itself: the application never presents a falsely-confident "everything is fine" state when it cannot actually verify that.

16.5 Mutation queuing — narrow, explicit, and only for genuinely safe operations

A small, explicitly-enumerated set of mutations queue locally and retry automatically on reconnect: draft-save operations (16.3) and, per Volume 1, Part V, Section 30.4's idempotency-key design, TriggerFlowRunCommand specifically when issued with an idempotency key already generated client-side before the network drop occurred. No other mutation is queued — publish, module registration, role assignment, and every other state-changing operation in Volume 1's command catalog (Part II, Section 13) fails immediately and visibly on a connectivity drop, requiring explicit user retry once reconnected, rather than being silently queued and executed later at an unpredictable time the user may no longer expect or want. This narrow scope is a deliberate safety decision: queuing an operation like PublishFlowCommand for automatic later execution risks a confusing, delayed side effect the user has forgotten about or no longer intends — the idempotency-key-protected run-trigger case is safe specifically because Volume 1 already designed it to be safely retryable by construction (Volume 1, Part V, Section 30.4), and this section extends that safety property to the offline case rather than inventing a new one.

16.6 What is explicitly not supported

Stated exhaustively, mirroring Section 16.1's honesty discipline: live-run progress (Section 15) does not queue or replay while offline in the sense of the client fabricating or interpolating anything about the gap — but this is a narrower limitation than it might first sound, and worth being precise about now that Volume 1, Part III, Section 16.7 guarantees incremental, per-step backend durability. A user who loses connectivity mid-run-watching sees the connectivity indicator (16.4) and, on reconnect, the SignalR reconciliation (Section 15.4) catches them up to the genuinely complete history of every step that finished during the gap — not an approximation, and not contingent on the backend having stayed up the whole time either, per Section 15.4's updated discussion of that guarantee. What the client specifically does not do is show a locally-simulated "replay" of the gap (an animated catch-up sequence re-playing each missed step's arrival) — the reconciled state simply appears, accurately and completely, as of reconnect. The reporting dashboard, module marketplace, and administration surfaces (Section 6.4–6.6) have no offline behavior at all beyond standard browser caching of already-fetched TanStack Query data (Section 8.2) incidentally remaining visible-but-stale until the connectivity indicator (16.4) makes that staleness explicit — this is existing cache behavior, not a purpose-built offline feature, and is described here only to be precise about what a user should and shouldn't expect.

16.7 Service worker scope — asset caching only, not data

A service worker is used exclusively for static-asset caching (enabling faster reload after a brief connectivity drop, and a smoother initial-load experience generally) — it does not intercept or cache API responses, and does not implement a general offline-data-sync framework (e.g., no background-sync API usage beyond what 16.5's narrow mutation queue requires). This is a deliberate, minimal-scope choice consistent with Section 16.1's honest framing: a broader service-worker-based offline-data architecture would be substantial engineering investment in service of a use case (fully offline operation of a live automation-execution platform) that doesn't coherently exist for this product, and would risk the exact kind of "unified platform" credibility-tax problem Volume 0, Section 8.6 warned about if it created an impression of offline capability the product doesn't and structurally can't fully deliver.

16.8 Self-hosted, air-gapped deployments — a different, already-solved case

Worth distinguishing explicitly from this section's "offline support" scope: Volume 0's air-gapped self-hosted deployment target (Section 19.5; Volume 1, Part VI, Section 37.7) is not an "offline" scenario in this section's sense at all — in that deployment model, the Nexus backend itself runs within the customer's network, so the GUI is fully "online" relative to its own backend even with zero general internet access. This section's offline-support scope is specifically about brief, incidental connectivity loss between an otherwise-online client and its backend, a genuinely different concern from air-gapped deployment, which Volume 1's infrastructure sections already address completely on their own terms.


___________________________________________________________________________________________________________________________________________________



17. Performance
17.1 Purpose and relationship to NFR-UX-2

Volume 0's NFR-UX-2 (30-minute time-to-first-flow) is fundamentally a usability metric, but frontend performance is a direct, measurable contributor to it — a sluggish authoring loop (Section 11's canvas, Section 10's Monaco integration) erodes that budget just as surely as a confusing information architecture would. This section specifies concrete performance budgets and the engineering practices that hold them, mirroring the rigor Volume 1, Part VI, Section 38 established for backend performance.

17.2 Bundle size budgets
Bundle	Budget (gzipped)	Rationale
Initial shell (app bootstrap, routing, identity-access)	150 KB	The only bundle every user pays for on first load, including Persona Sofia's occasional dashboard-only visits — kept lean specifically for her use case, which shouldn't require downloading authoring-surface code she'll never touch
flow-authoring feature chunk (excluding Monaco/React Flow)	200 KB	Route-level code split, loaded only on navigation to /flows/*
Monaco Editor chunk	Separately lazy-loaded, no fixed budget (inherently large)	Per Section 10.7's deferred-load strategy — never part of the initial or authoring-route bundle
React Flow / canvas chunk	Separately lazy-loaded	Same rationale as Monaco — the canvas is substantial and deferred until actually needed
reporting-audit feature chunk	150 KB	Recharts (Section 9.3's TrendChart) is the main contributor; also lazy-loaded per-route

These budgets are enforced in CI via a bundle-size-analysis step that fails the build on regression beyond a small tolerance — mirroring Volume 1, Part VI, Section 38's "performance regression treated with the same severity as a functional bug" principle, applied here to bundle weight specifically.

17.3 Code splitting strategy

Every route (Section 7.2's table) is its own lazy-loaded chunk via React.lazy, and within flow-authoring specifically, Monaco (Section 10.7) and React Flow (Section 12.8's virtualization note) are further split into their own sub-chunks loaded only when the raw-editing mode or the canvas is actually rendered — a user who only ever uses the AI-generation panel (Section 6.2) to create flows and never opens raw JSON editing never pays Monaco's bundle cost at all. This is the concrete implementation of UX Principle 1.5 (progressive disclosure) applied to loading strategy, not just UI layout: the code itself discloses progressively, matching the interface's progressive disclosure.

17.4 Rendering performance — the canvas and large lists

Beyond Section 12.8's React Flow virtualization: DataTable (Section 9.3), used for flow lists, run history, and audit logs alike, virtualizes rows beyond a threshold (e.g., 50 visible rows) regardless of the underlying dataset size, directly relevant to NFR-SCALE-3's 10-million-row backend target (Volume 1, Part V, Section 32.6) — a frontend that rendered every row of a large ListRecentFlowRunsQuery result naively would defeat the backend's own pagination design (Volume 1, Part V, Section 30.4). Pagination (server-side, per that same section) is the primary mitigation; virtualization is the secondary, defense-in-depth layer for within-page rendering smoothness.

17.5 Memoization discipline

React.memo, useMemo, and useCallback are applied deliberately at specific, identified hot paths — the FlowStepNode canvas renderer (Section 12.2, re-rendering potentially many times per second during live-run overlay updates, Section 12.7) and the SchemaDrivenParamEditor (Section 9.4, re-rendering on every keystroke of a sibling field if not properly scoped) — rather than applied reflexively everywhere. Consistent with this volume's repeated "don't add complexity that doesn't earn its cost" discipline (echoed from Volume 1, Architecture Principle 20.7): blanket memoization without profiling data justifying it is treated as a code-review concern (unnecessary complexity, potential for stale-closure bugs) rather than a default good practice.

17.6 Data-fetching performance — avoiding waterfalls

Route loaders (Section 7.4) and TanStack Query's parallel-query capabilities are used deliberately to avoid request waterfalls — a route like /runs/:id (Volume 2, Section 7.2) that needs both the FlowRun detail and its parent Flow's definition fetches both in parallel from the loader, not sequentially (fetch the run, wait, then fetch the flow only once the run response reveals the flowId, where avoidable) — reviewed explicitly for any new route added to Section 7's table.

17.7 Core Web Vitals and real-user monitoring

Standard Core Web Vitals (LCP, INP, CLS) are tracked via real-user monitoring, exported through the same OpenTelemetry pipeline Volume 1, Part IV, Section 29 established for backend observability — frontend and backend performance telemetry converge into one observability system, not two separate ones, directly extending Volume 1's "the platform observing itself" principle (Part IV, Section 29.1) to include the client experience, not just server-side execution. This gives Volume 1's alerting philosophy (Part IV, Section 29.6) a genuinely complete picture: a backend that's fast but a frontend that's slow to render results is still, from Persona Priya's perspective, a slow platform, and this telemetry convergence is what makes that visible to whoever's on call.

17.8 SignalR and canvas update-rate throttling

During a fast-executing flow (many steps completing within a short window), raw per-event canvas re-rendering (Section 12.7) could exceed a smooth frame budget; useLiveRunProgress (Section 15.3) batches rapid-succession events within a short window (e.g., 100ms) before triggering a re-render, trading negligible perceived latency for guaranteed rendering smoothness — a deliberate, narrow performance optimization justified by the specific, identified hot path (fast flows with many steps), not a general debouncing policy applied without cause, consistent with Section 17.5's memoization-discipline reasoning.

17.9 Performance budget as a release gate

Mirroring Volume 1, Part VI, Section 38.8's load-testing release gate: Lighthouse CI (or equivalent) runs against representative pages (the flow editor with a realistic flow loaded, the trend dashboard with representative data volume) as a mandatory CI check before any GA-track release, with specific, numeric thresholds for LCP and INP directly tied to NFR-UX-2's overall usability commitment — a frontend performance regression is a release blocker, not a follow-up ticket, for the same reasons Volume 1 already established for backend performance regressions.



18. Accessibility
18.1 Purpose and the NFR-UX-1 target

Volume 0's NFR-UX-1 requires WCAG 2.1 AA conformance for the flow-authoring GUI — stated there as a hard requirement tied directly to enterprise/public-sector procurement conditions (Volume 0, Section 13.6), not an aspirational goal. This section specifies how that conformance is achieved and verified across every surface this volume has specified, not just the authoring GUI narrowly.

18.2 Conformance scope — every surface, not just flow authoring

While NFR-UX-1's original wording named the authoring GUI specifically, this section extends the same AA conformance bar to every Feature module (Section 6) without exception — the reporting dashboard (Sofia's primary surface), the admin/settings pages, and the login flow are held to the identical standard. Narrowing conformance to only the literal wording of the NFR would create an inconsistent, partially-accessible product, directly contradicting UX Principle 1.6's persona-equity spirit (Section 1.6) — Sofia's occasional but important dashboard visits deserve the same accessibility guarantee Priya's daily authoring sessions get.

18.3 Color contrast — verified per theme, per Section 3's discipline

Directly extending Section 3.4's "no partial theme overrides" rule: every one of the four theme variants (dark, light, high-contrast, and the enterprise-branding extension point from Section 3.6) is independently verified against WCAG AA contrast ratios (4.5:1 for normal text, 3:1 for large text and UI component boundaries) — including, critically, the signal-state palette (Section 2.6): a Failed-red badge's text must meet contrast against its background in every theme, not just the default dark theme it was originally designed against. This verification is automated (axe-core or equivalent, integrated into the Storybook visual-regression pipeline already established in Section 9.7) rather than manually spot-checked, since a design-token change (Section 2.9's governance process) could otherwise silently introduce a contrast regression in a theme nobody happened to manually re-check.

18.4 Keyboard navigation

Every interactive surface is fully keyboard-operable, with the Workflow Designer canvas (Section 11) as the specific, hardest case worth calling out explicitly: node selection, connection creation, and step reordering all have keyboard-accessible equivalents to their mouse-drag interactions (arrow-key node navigation, a keyboard-invoked "connect mode" as an alternative to drag-to-connect, and a step-reordering command accessible via a context menu rather than requiring drag-and-drop) — directly serving the same "no capability exclusively available through an inherently-limited interaction model" principle Section 10.8 already established for Monaco's accessibility fallback, applied here to React Flow's canvas interactions specifically.

18.5 Screen reader support — the canvas's specific challenge

The Workflow Designer canvas (Section 11–12) is, per Section 10.8's Monaco discussion, an inherently harder surface for screen-reader users than standard form-based UI. Nexus's approach: the canvas is one, visually-primary representation of a Flow, but Section 10.3's raw-editing mode and, more specifically, a dedicated structured list view (a third representation, alongside canvas and raw-JSON: an accessible, linear, fully-keyboard-and-screen-reader-native list of steps with the same edit capabilities as the canvas) ensure no capability is canvas-exclusive. This structured list view is not a lesser, read-only fallback — it supports the complete set of authoring operations (add, edit, reorder, delete, connect via explicit "depends on" fields rather than visual edges) as a fully first-class alternative, directly satisfying WCAG's requirement that equivalent functionality be available through an accessible interaction model, not merely that the inaccessible one be somewhat mitigated.

18.6 ARIA live regions for real-time updates

Live-run progress (Section 15) — a core, distinguishing product experience — presents a specific accessibility challenge: visual users perceive step completion through the canvas's signal-state animation (Section 2.8), but this needs an equivalent, non-visual channel. RunProgressView (Section 6.3) includes an ARIA live region (aria-live="polite", chosen over "assertive" specifically to avoid interrupting a screen-reader user's current focus for every single step completion, which would be disruptive for a fast-running flow) announcing step completions and the final run outcome — calibrated to announce meaningfully (step name and status), not so verbosely that rapid multi-step completion becomes an unusable wall of audio.

18.7 AI-attribution accessibility — UX Principle 1.6, made concrete

Directly extending UX Principle 1.6 (Section 1.7) and the AiAttributionBadge component (Section 9.3): the AI-assisted visual treatment (Section 2.6's teal badge) is never conveyed by color alone — every AI-attributed result additionally carries a text label ("AI-assisted") and, where relevant, an icon, satisfying WCAG's general "don't rely on color alone to convey information" success criterion while also serving this volume's own transparency principle simultaneously — a case where accessibility best practice and Nexus's specific product-trust requirement happen to demand the exact same implementation, reinforcing rather than trading off against each other.

18.8 Focus management

Modal dialogs (Modal, Section 9.3) trap focus per Section 9.8's centralized-primitive accessibility contract; route transitions (Section 7) move focus to the new page's primary heading, not silently leaving focus on a now-unmounted element or resetting to the document body; the flow-step editor (Section 9.4), when opened for a newly-added step, moves focus to that step's first editable field — every one of these is a specific, deliberate focus-management decision, not an incidental byproduct of component structure, reviewed as part of the same PR checklist discipline Volume 1, Part VI, Section 40.3 established for backend code, extended here with accessibility-specific items.

18.9 Automated and manual verification

Automated accessibility testing (axe-core, integrated per Section 18.3 into Storybook and, separately, into the end-to-end test suite, Section 19) catches the class of issue that's mechanically detectable (contrast, missing ARIA labels, invalid heading hierarchy) — but is explicitly not treated as sufficient alone: manual testing with actual screen readers (NVDA, JAWS, VoiceOver) against the Workflow Designer's structured-list fallback (18.5) and the live-progress ARIA live region (18.6) specifically is a required part of the release checklist for any change touching those two surfaces, since these are exactly the interaction patterns automated tooling is least reliable at evaluating — a direct, honest acknowledgment that automated accessibility testing has real limits, consistent with this document set's general discipline of not overclaiming what a given mechanism actually verifies.

18.10 Accessibility as a design-system responsibility, restated

This section closes by reinforcing Section 9.8's original claim directly: because every Primitive component (Section 9.2) implements its accessibility contract once, centrally, the majority of this section's requirements are satisfied automatically by any Feature module correctly composing those primitives, rather than needing independent accessibility engineering per surface — the canvas (18.4–18.5) and live-progress announcements (18.6) are the two genuine, irreducible exceptions requiring bespoke accessibility work, precisely because they're the two surfaces in this entire volume that don't reduce to standard form-and-list UI patterns the Component Library's primitives already cover.



19. Testing Strategy
19.1 Purpose — consolidating this volume's testing references

Nearly every prior section in this volume deferred a specific testing obligation to "Section 19." This closing section collects those deferred obligations into one coherent, tiered testing strategy — the frontend counterpart to Volume 1, Part VI, Section 40.6's backend testing conventions, and the section that finally makes UX Principle 1.4 (module parity, zero hardcoding) a provable, continuously-verified property of the shipped product rather than a design intention.

19.2 The four testing tiers
Tier	Scope	Tooling	Runs
Unit	Pure functions, hooks in isolation (mocked dependencies)	Vitest	Every commit
Component	Individual React components rendered in isolation, including Storybook-driven visual regression (Section 9.7)	React Testing Library + Vitest, Chromatic (or equivalent) for visual diffs	Every commit
Integration	Feature modules (Section 6) with real TanStack Query/Zustand wiring, mocked API/SignalR at the network boundary	React Testing Library + MSW (Mock Service Worker)	Every commit
End-to-end	Full application against a real backend instance	Playwright — deliberately Nexus's own Web module, per Section 19.8	Pre-merge (full suite) and post-deploy smoke subset

This tiering mirrors Volume 1, Part VI, Section 40.6's fast-to-slow, isolated-to-integrated progression exactly, adapted to frontend-specific concerns.

19.3 Unit and hook testing

Every custom hook specified across this volume — useAuth, usePermission (Section 14.4), useLiveRunProgress (Section 15.3), useVariableFlowHighlight (Section 12.5) — has dedicated unit tests covering its logic independent of the components that consume it, consistent with the same "test the logic, not the rendering, where they're separable" discipline Volume 1 established for command handlers (Part VI, Section 40.6).

19.4 Component testing and the design-system guardrail

Directly implementing Section 9.7's visual-regression requirement: every Storybook story is captured as a baseline, and any pull request producing a visual diff against that baseline requires explicit reviewer approval before merge — a mechanical safeguard against the exact silent-drift failure mode Section 2.9's design-system governance exists to prevent, now enforced by tooling rather than relying on a reviewer noticing a subtle color or spacing change by eye.

19.5 The module-parity test — proving UX Principle 1.3/1.4, not just following it

This is the single most important test in this volume, existing specifically because UX Principle 1.3 (Section 1.4) made a strong, structural claim — "no UI component may contain module-specific branching logic" — that deserves more than trust:

typescript
describe('Module parity', () => {
  it('renders a completely synthetic, previously-unknown module with zero special-casing', async () => {
    const syntheticModule: ModuleActionDto = {
      moduleKind: 'synthetic-test-module-xyz',   // guaranteed not to match any
                                                    // built-in ModuleKind or any
                                                    // fixture the codebase has
                                                    // ever seen before
      actions: [{
        name: 'doSomethingNovel',
        parameters: [
          { name: 'targetId', type: 'String', required: true },
          { name: 'retryCount', type: 'Number', required: false },
          { name: 'payload', type: 'Object', required: true },
        ],
      }],
    };

    render(<FlowStepEditor availableModules={[syntheticModule]} />);

    // Asserts: the module/action picker (Section 11.6) lists it with the
    // correct fallback icon (Section 2.7); selecting it renders the
    // SchemaDrivenParamEditor (Section 9.4) with correctly-typed fields for
    // all three parameters; the canvas (Section 12.2) renders a FlowStepNode
    // for it without throwing or falling back to an error state.
    await userEvent.click(screen.getByText('synthetic-test-module-xyz'));
    expect(screen.getByLabelText('targetId')).toBeRequired();
    expect(screen.getByLabelText('retryCount')).not.toBeRequired();
    expect(screen.getByTestId('payload-json-editor')).toBeInTheDocument();
  });
});

This test runs in CI on every commit that touches SchemaDrivenParamEditor, FlowStepNode, or the module/action picker — a regression here (someone accidentally introducing a ModuleKind-specific branch, per Section 1.4's prohibition) fails the build immediately, making the module-parity principle a continuously-enforced invariant, not a one-time design review outcome.

19.6 Integration testing with MSW

Feature-module integration tests (Section 19.2's third tier) mock the network boundary with MSW rather than mocking TanStack Query or the API client directly — this ensures the tests exercise the real request/response cycle, cache-invalidation logic (Section 8.3), and error-handling paths (Section 30.5's RFC 7807 format, Volume 1) exactly as they'd behave against the real backend, with only the actual network call intercepted. SignalR is similarly mocked at this tier via a fake HubConnection (Section 15.8), never by stubbing out useLiveRunProgress itself, for the same reason — the integration test should exercise real hook logic against a controlled, fake transport, not bypass the logic entirely.

19.7 Accessibility testing integration

Per Section 18.9: automated axe-core checks run as part of both the Component tier (every Storybook story) and the Integration tier (key Feature-module flows — publishing a flow, triggering a run, navigating the reporting dashboard) — with the manual screen-reader verification Section 18.9 required for the canvas's structured-list fallback (Section 18.5) and the live-progress ARIA live region (Section 18.6) tracked as an explicit, named pre-release checklist item, not something automated tooling is trusted to cover on its own.

19.8 End-to-end testing — Nexus testing itself

The most notable architectural choice in this section: Nexus's end-to-end test suite is built using Nexus's own Web module (Volume 1, Part III, Section 18; the product this entire document set specifies) rather than a separately-adopted E2E framework unrelated to the product itself. Concretely, the E2E suite is a collection of Nexus flows (Volume 1, Part II, Section 8.2's Flow definitions) that drive the Nexus GUI itself as their target — Priya's authoring journey (Volume 0, UC-2), Marcus's CI-triggered run (UC-4), and Sofia's dashboard review (UC-5) are each expressed as an actual Nexus flow, executed by an actual Nexus flow run, asserting on the actual Nexus GUI's rendered state via the Web module's assertion capabilities (Volume 1, Part III, Section 18.2's assertText/waitForSelector actions).

This is a deliberate dogfooding choice with real, direct value beyond novelty: it exercises the Web module's genuine production capability against a real, complex application (not a toy test fixture) on every CI run, meaning a regression in Nexus's own Web automation capability is caught by Nexus testing itself before it would ever reach a customer — the most direct, continuous validation of Volume 0's core product promise (Business Objective 5's trust posture) available. It also means the E2E suite's own reports (Volume 1, Part II, Section 8.5's FlowReport) are genuine Nexus artifacts, viewable in the exact reporting dashboard (Section 6.5) this volume specifies — closing the loop completely.

19.9 E2E suite scope and the smoke/full split

Given genuine E2E test runtime cost, the full E2E suite (covering every persona journey from Volume 0's use-case catalog, Section 11) runs pre-merge on the primary integration branch; a smaller, curated smoke subset (login, create-and-publish-a-basic-flow, trigger-and-view-a-run) runs post-deploy against each environment as a fast release-health check, mirroring Volume 1, Part VI, Section 37's deployment-gating philosophy extended to the frontend's own release process.

19.10 Performance and bundle-size testing integration

Section 17.9's Lighthouse CI and Section 17.2's bundle-size budgets are both integrated into this same CI pipeline as additional, mandatory gates — not a separately-scheduled process — ensuring a single pull request's "ready to merge" state means functionally correct, accessible, visually non-regressed, and performance-budget-compliant simultaneously, never any one of these verified in isolation from the others.

19.11 Testing the Plugin UI sandbox

Directly extending Section 13's trust-boundary design: automated tests specifically verify the sandbox's negative guarantees (Section 13.6) — a test harness loads a deliberately malicious test extension attempting each prohibited action (accessing document.cookie, attempting window.top navigation, attempting a direct fetch to the Nexus API) and asserts every attempt is blocked by the iframe sandbox, not merely assumed to be blocked because the sandbox attribute is set correctly. This is the frontend-testing counterpart to Volume 1, Part VI, Section 40.6's requirement that every Permission have an explicit authorized/unauthorized test pair — a security boundary is only trustworthy once it's been actually, adversarially tested, not just configured correctly by inspection.

19.12 Closing note — Volume 2 complete

This section closes Volume 2 in full: 19 sections across 5 Parts, tracing from UX principles and the design system (Part I) through application architecture (Part II), the core authoring and reporting surfaces (Part III), platform integration (Part IV), and now the quality and testing discipline that keeps all of it correct (this Part). Every design decision in this volume traces back through Volume 0's personas and requirements and Volume 1's API/domain contracts — and, as this session demonstrated directly with the Unit of Work correction, every volume in this set remains genuinely load-bearing for the others: a gap found while writing Volume 2 flowed back into a real fix in Volume 1, exactly the kind of cross-volume consistency this document set's traceability discipline (Volume 0, Section 20.9; Volume 1, Part VI, Section 40.8) was built to make possible, not merely aspire to.


__________________________________________________________________________________________________________________________________________________________________



📘 ADS-003 — AI Architecture Design

Objective

Design an AI subsystem that assists throughout the automation lifecycle without being tightly coupled to a specific model provider.

Chapter 1 – Vision

AI-first Automation Platform



Chapter 2 – AI Responsibilities

Generate tests

Heal locators

Analyze failures

Generate reports

Summarize logs

Suggest optimizations

Natural language execution


Chapter 3 – AI Services

Planner

Generator

Analyzer

Healer

Reporter

Assistant

Knowledge Base


Chapter 4 – Provider Abstraction
OpenAI

Azure OpenAI

Anthropic

Google Gemini

Local Models

Future Providers



Chapter 5 – Prompt Framework

Reusable prompt templates

Context injection

Memory

RAG support

Chapter 6 – Knowledge Layer

Documentation

Requirements

Screenshots

Logs

Historical executions

Best practices


Chapter 7 – Self-Healing

Locator healing

Wait strategy adaptation

Retry recommendations

Fallback execution


Chapter 8 – AI Security

PII masking

Prompt validation

Provider failover

Rate limiting

Audit logs


___________________________________________________________________________________________________________________________________________________________________



# Volume 3 — AI Architecture · Part I — AI Foundations



1. AI Vision
1.1 Purpose and relationship to Volume 0's AI Strategy

Volume 0, Part III, Section 18 established Nexus's AI product strategy: the three-capability value framework (authoring acceleration, maintenance reduction, diagnostic acceleration), three permanent product boundaries (never autonomous, never required, never data-pooling), and provider-abstraction/guardrail commitments. This volume takes that strategy and specifies the actual system that implements it — every architectural decision here traces back to a specific clause of Volume 0, Section 18, restated at the top of the relevant section throughout this volume, exactly as Volume 1 and Volume 2 both traced back to their respective upstream commitments.

1.2 What "AI Architecture" means as a distinct volume, precisely

A clarification worth stating up front, since it determines this volume's entire scope: this is not a volume about using a large language model as a feature bolted onto Nexus — it is the architecture of a capability layer that sits across the entire platform, consuming the domain model Volume 1 specified (a Flow, a StepResult, a FlowRun) and producing outputs that flow back into that same domain model through the exact same commands Volume 1 already catalogued (GenerateFlowFromDescriptionCommand, RequestSelectorHealCommand, Volume 1 Part II Section 13). This volume does not introduce new domain concepts that bypass Volume 1's model — it specifies how the AI capability engines (Part III of this volume) produce values that are valid instances of types Volume 1 already defined, and it specifies the infrastructure (Parts II and IV) those engines run on.

1.3 The three-capability framework, restated as an architectural mandate

Volume 0, Section 18.3 defined three value mechanisms — authoring acceleration, maintenance reduction, diagnostic acceleration — and required every AI capability to map to exactly one. This volume's Part III (Capability Engines) is organized by this exact mapping, not by technology (e.g., not "the RAG-based features" grouped separately from "the fine-tuned-model features") — because the product value the capability delivers, not its implementation technique, is what this document set has consistently used to organize itself (Volume 1's bounded contexts mirrored Volume 0's problem decomposition; Volume 2's feature modules mirrored Volume 1's bounded contexts; this volume's capability engines mirror Volume 0's value framework).

Value mechanism (Volume 0 §18.3)  Capability engines (this volume, Part III)
Authoring acceleration  Requirement Parser (§7), Test Generator (§8)
Maintenance reduction Locator Generator (§9), Self-Healing (§10)
Diagnostic acceleration Failure Analysis (§11), Root Cause Analysis (§12), Report Summarization (§13)
1.4 The permanent boundaries, restated as architectural invariants

Volume 0, Section 18.4's three permanent boundaries become, in this volume, concrete architectural properties the system is built to structurally enforce, not merely policies engineers are asked to follow:

"AI never executes or persists a change autonomously" — architecturally, this means every capability engine in Part III has exactly one output shape: a proposal, never a direct mutation. A capability engine's method signature never accepts a repository or Unit of Work (Volume 1, Part III, Sections 15–16); it returns a value that a Volume 1 command handler subsequently, separately decides whether to persist. This is enforced by this volume's own dependency-direction rule (fully specified in Part IV once the provider-abstraction layer is defined), directly analogous to Volume 1's Nexus.Domain having zero outward dependencies (Volume 1, Part V, Section 36.3).
"AI is never a required dependency for core platform functionality" — architecturally, every capability engine implements a common interface (Section 2's AI Planning Engine formalizes this) whose absence is a first-class, tested code path throughout this entire volume, not just at the Volume 1 orchestration boundary where NFR-REL-3 was originally enforced.
"AI does not become a data-pooling mechanism across customers" — architecturally, this constrains Part II (Knowledge & Context) specifically: every Memory and Knowledge Base construct (Sections 5–6) is tenant-scoped by construction, using the exact same TenantId shared-kernel discipline Volume 1, Part II, Section 7.4 established, never a cross-tenant corpus.
1.5 The AI Vision statement for this volume

Nexus's AI layer exists to make the deterministic platform Volume 1 specifies easier to author, cheaper to maintain, and faster to diagnose — never to become a second, parallel system a user must trust independently of the platform's own guarantees.

Every capability engine in Part III should be evaluated against this statement specifically: does it make Volume 1's platform easier to author/maintain/diagnose, or does it ask a user to trust something new and separate? A proposed AI capability that fails this test — because it would require a user to trust the AI's judgment in place of, rather than in addition to, Volume 1's own deterministic verification — is out of scope for this volume, consistent with Volume 0, Section 18.4's boundary and this document set's repeated practice of naming out-of-scope territory explicitly (Volume 0, Section 12.6; Volume 1, Part I, Section 2.4's system boundary statement).

1.6 Why this volume comes after Volumes 1 and 2, architecturally

This ordering is not incidental to the six-volume plan — it reflects a real dependency direction worth stating explicitly: every capability engine in this volume produces an output that must be a valid instance of a Volume 1 domain type (a generated Flow must satisfy Flow.Publish's invariants, Volume 1 Part II Section 9.2; a healed selector must produce a valid AiAttribution, Volume 1 Part II Section 11.5) and every capability's UI surface was already scoped by Volume 2 (Section 1.7 there; Section 6.2's AiFlowGenerationPanel; Section 13's Plugin UI sandbox). This volume could not have been written first without either contradicting or duplicating decisions Volumes 1 and 2 already made — a concrete, worked illustration of why this whole project's volume ordering (Product Blueprint → Backend → Frontend → AI → Infrastructure → Engineering Playbook) was chosen the way it was.

1.7 What "depth" means for this volume specifically

Given this volume's explicit mandate ("really innovate," per the original project scope) alongside its equally explicit permanent boundaries (1.4), this volume's depth is expressed through rigor about constraint enforcement and evaluation, not through relaxing the boundaries Volume 0 already committed to. The most technically ambitious sections in this volume (Part II's RAG/Memory architecture, Part III's capability engines) are exactly as ambitious as ordinary AI-product engineering typically is — what makes this volume's treatment distinct is the density of guardrail, evaluation, and cost-governance specification surrounding that ambition (Parts IV and V), reflecting Volume 0, Section 18.6's explicit competitive-differentiation thesis: unified, architected guardrails across every AI touchpoint, not raw model capability, is Nexus's actual moat.

1.8 Section roadmap for the rest of this Part

Section 2 (AI Planning Engine) specifies the routing/orchestration layer that decides which capability engine and which prompt template handles a given AI-assisted request. Section 3 (Prompt Library) specifies how prompts themselves are authored, versioned, and tested as a managed artifact — the direct foundation every capability engine in Part III builds on.



2. AI Planning Engine
2.1 Purpose — the router above the capability engines

Part III of this volume specifies seven distinct capability engines (Requirement Parser, Test Generator, Locator Generator, Self-Healing, Failure Analysis, Root Cause Analysis, Report Summarization). This section specifies the layer that sits above all of them: the component that receives an AI-assisted request from Volume 1 (a GenerateFlowFromDescriptionCommand, a RequestSelectorHealCommand) and determines which capability engine, which prompt template (Section 3), and — per Part IV — which model/provider should handle it, then assembles the final structured output back into the shape Volume 1's command handler expects.

2.2 Why a distinct planning layer, rather than each capability engine standing alone

A tempting simpler design is letting each Volume 1 AI-related command call directly into its own, independent capability-engine implementation with no shared routing layer. This volume rejects that design for a reason directly traceable to Volume 0, Section 18.6's guardrail-centralization thesis: guardrails, cost governance (Part IV), and evaluation instrumentation (Part V) need to apply uniformly across every AI touchpoint, and a shared planning layer is what makes "uniform" an architectural property rather than a discipline seven independently-implemented engines would each need to separately, correctly re-implement — directly mirroring Volume 1's own reasoning for why the orchestrator core has zero module-specific branching (Volume 1, Architecture Principle 20.3) applied here to AI capabilities specifically.

2.3 The Planning Engine's interface
typescript
interface AiPlanningRequest {
  capability: 'flow-generation' | 'selector-healing' | 'log-triage' | 'requirement-parsing'
            | 'test-generation' | 'failure-analysis' | 'root-cause-analysis' | 'report-summarization';
  tenantId: TenantId;
  triggeringUserId: UserId;          // for the permission-inheritance rule, Volume 1 Part IV §25.6
  input: CapabilitySpecificInput;     // typed per capability, Part III
  context?: RetrievalContext;         // populated by Part II's RAG layer, when applicable
}

interface AiPlanningResult<TOutput> {
  output: TOutput;                    // a valid Volume 1 domain-adjacent value —
                                          // never persisted here, only returned
  confidence: number;
  modelUsed: string;                  // Part IV §15's routing decision, recorded
  guardrailChecks: GuardrailCheckResult[];  // Part V §17, always populated, never skipped
  costRecord: AiCostRecord;           // Part IV §16
}

interface AiPlanningEngine {
  plan<TOutput>(request: AiPlanningRequest): Promise<AiPlanningResult<TOutput>>;
}

Every field on AiPlanningResult is populated on every call, without exception — guardrailChecks and costRecord are never optional or best-effort, because Part V's evaluation metrics and Part IV's cost governance both depend on every single AI-assisted operation producing this data uniformly, not only the operations a particular engineer remembered to instrument.

2.4 The planning pipeline
AiPlanningRequest
      │
      ▼
┌─────────────────┐
│ 1. Guardrail        │  Part V §17 — input-side checks (PII detection, prompt-injection
│    pre-check         │  resistance screening, tenant policy enforcement) BEFORE any
└────────┬─────────┘  model call is made — rejecting a request here costs nothing and
         │              risks nothing, the cheapest possible point to enforce a boundary
         ▼
┌─────────────────┐
│ 2. Context           │  Part II §4-6 — RAG retrieval and Memory lookup, populating
│    retrieval          │  RetrievalContext, scoped strictly to the requesting tenant
└────────┬─────────┘  (Volume 0 §18.4's data-pooling boundary, enforced here concretely)
         │
         ▼
┌─────────────────┐
│ 3. Prompt            │  Section 3 — selects and renders the versioned prompt template
│    assembly           │  for this capability, injecting the retrieved context
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│ 4. Model routing     │  Part IV §15 — selects provider/model based on capability,
│                       │  cost policy, and tenant configuration
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│ 5. Invocation        │  Part IV §14's provider-abstracted call, with the
│                       │  NFR-PERF-4 15-second timeout (Volume 0/Volume 1) enforced
└────────┬─────────┘  at exactly this boundary
         │
         ▼
┌─────────────────┐
│ 6. Output            │  Part V §17 — structural validation (does the response parse
│    validation          │  as the expected TOutput shape) AND output-side guardrail
└────────┬─────────┘  checks (does it violate a content/safety policy)
         │
         ▼
┌─────────────────┐
│ 7. Evaluation         │  Part V §19 — records the interaction for ongoing quality
│    instrumentation     │  metrics (self-heal acceptance rate, flow-generation
└────────┬─────────┘  structural validity rate — Volume 0 §18.7)
         │
         ▼
AiPlanningResult<TOutput>

Every one of these seven steps is mandatory and non-bypassable for every capability — a specific capability engine (Part III) customizes steps 2, 3, and the shape of TOutput, but never skips steps 1, 6, or 7, which is exactly the "uniform across every AI touchpoint" property Section 2.2 argued this layer exists to guarantee.

2.5 Failure and timeout handling — the AI-optionality boundary, implemented here

Directly implementing Volume 1, Part VI, Section 38.5's AiProviderClient timeout design and Volume 0's NFR-REL-3: if step 5 (Invocation) times out or the provider is unreachable, the Planning Engine returns a typed AiUnavailableError rather than throwing an unhandled exception — every Volume 1 command handler consuming this engine (Section 2.6) is required to catch this specific error type and fall back to normal, non-AI failure handling, never letting an AI outage cascade into a platform-wide failure. This is the concrete architectural seam where Volume 0's "AI proposes, deterministic code disposes, gracefully absent when unavailable" philosophy becomes a specific, typed contract rather than a general intention.

2.6 Consumers — mapping back to Volume 1's command catalog
Volume 1 command  Planning Engine capability  Part III section
GenerateFlowFromDescriptionCommand  flow-generation Requires composition of Requirement Parser (§7) + Test Generator (§8)
RequestSelectorHealCommand  selector-healing  Locator Generator (§9) + Self-Healing (§10)
(Logs module AI-triage capability, Volume 0 §14.9)  log-triage  Failure Analysis (§11), Root Cause Analysis (§12)
(Reporting dashboard's future NL-query capability, Volume 0 Future Vision §23.3)  report-summarization  Report Summarization (§13)

This table is the direct, concrete bridge between Volume 1's already-fixed command catalog and this volume's capability engines — every row is a contract this volume must satisfy exactly, not a suggestion.

2.7 Why "planning," not just "routing"

The name reflects a deliberate scope decision worth explaining: for the simpler capabilities (log-triage, selector-healing), step 3 (Prompt assembly) is genuinely a fixed, single-template selection — "routing" would be an equally accurate name. For flow-generation specifically, the Planning Engine performs genuine multi-step planning (per Section 2.6's table: composing Requirement Parser and Test Generator outputs, potentially iterating if the Test Generator's output fails structural validation at step 6, per Volume 1's FlowGenerationResult design already anticipated in Volume 1, Part II, Section 13.2) — the "Planning Engine" name is chosen to accurately describe its most sophisticated responsibility, not its simplest one, avoiding the understatement "router" would imply for that capability specifically.



## 3. Prompt Library

### 3.1 Purpose — prompts as a managed, versioned artifact

Directly extending the Planning Engine's step 3 (Section 2.4): this section specifies how prompts are authored, stored, versioned, and evaluated as first-class engineering artifacts — never as inline strings scattered through capability-engine code. This is a direct, deliberate application of the same discipline Volume 1, Part VI, Section 40.7 established for `ActionSchema` documentation ("undocumented actions are a direct, user-visible product defect") applied here: an unversioned, untested prompt is treated as a defect in this volume's engineering practice, not an acceptable shortcut.

### 3.2 Prompt template structure

```typescript
interface PromptTemplate {
  id: string;                        // e.g., "flow-generation.v3"
  capability: AiPlanningRequest['capability'];   // Section 2.3
  version: string;                    // semantic version, mirroring Volume 1 Part III
                                          // §18.7's AutomationModule contract versioning
  systemPrompt: string;                // the instruction/role template
  outputSchema: JSONSchema;            // enforced structurally, Section 3.5
  requiredContextFields: string[];      // which RetrievalContext fields this template expects
  changelog: PromptChangelogEntry[];
}
```

### 3.3 Versioning discipline

Every prompt template change is a new version, never an in-place edit to an existing one — directly mirroring Volume 1, Part III, Section 18.7's `AutomationModule` contract-versioning discipline, applied here because a prompt change can silently alter a capability's output distribution in ways that are much harder to detect than a code change's compiler-checked type signature. A `PromptTemplate`'s version is recorded on every `AiPlanningResult` (Section 2.3's `modelUsed` field is joined with the prompt version for full reproducibility) — this is what makes Part V's evaluation metrics (self-heal acceptance rate, structural validity rate) meaningful over time: a metric regression can be correlated to a specific prompt version change, not just a vague "quality seems worse lately."

### 3.4 Prompt authoring principles

- **Capability-scoped, never general-purpose.** Each `PromptTemplate` serves exactly one capability (Section 2.3's closed capability enum) — there is no shared "general Nexus assistant" system prompt that multiple capabilities repurpose with different framing, since that pattern makes prompt-injection resistance (Part V, Section 18) and output-schema enforcement (3.5) both structurally harder to reason about per capability.
- **Structured output required, always.** Every `PromptTemplate.outputSchema` mandates a specific, machine-parseable structure (JSON, matching the target `TOutput` type from Section 2.3) — no capability in this volume relies on free-text output that a downstream regex or heuristic tries to interpret, directly serving the "make illegal states unrepresentable" discipline Volume 1, Part II, Section 11.6 established for domain value objects, now extended to AI output specifically.
- **Context injection points are explicit and typed**, per `requiredContextFields` (3.2) — a prompt template declares exactly what retrieved context (Part II) it expects, and the Planning Engine (Section 2.4, step 3) fails fast with a clear error if that context wasn't successfully retrieved, rather than silently rendering a prompt with missing sections and hoping the model compensates.

### 3.5 Output schema enforcement — where it happens and why it's not optional

Referencing Section 2.4's step 6: every model response is parsed against `outputSchema` before being treated as a valid `TOutput`. A response that fails to parse is **not** silently retried with a more lenient parser or passed through with best-effort coercion — it is treated as a capability failure, surfaced through the exact same `AiUnavailableError`-adjacent typed-failure path Section 2.5 established for provider timeouts, letting the calling Volume 1 command handler fall back to normal failure handling. This directly mirrors the exact behavior Volume 1's original `AiModule` prototype already established for `generateFlow`/`triageLogs`/`healSelector` ("Model did not return valid JSON" → typed failure, never a silent guess) — this volume formalizes that prototype behavior as a mandatory, uniform rule enforced at the Planning Engine layer for every capability, not an ad hoc per-capability choice.

### 3.6 Prompt testing — the evaluation-set discipline

Every `PromptTemplate` version ships with an associated **evaluation set**: a curated collection of representative inputs and expected-acceptable-output characteristics (not necessarily exact-match expected outputs, given LLM output's inherent variability, but structural and semantic acceptance criteria — does a generated flow reference only valid modules, does a proposed selector fix stay within a reasonable edit distance of the original) that must pass before a new prompt version can be promoted from a staging configuration to production traffic. This is the direct mechanism that makes Part V, Section 19's evaluation metrics a *release gate*, not just a *dashboard* — mirroring Volume 1, Part VI, Section 38.8's load-testing release-gate philosophy, applied here to prompt-quality regression specifically.

### 3.7 Prompt library storage and deployment

Prompt templates are stored as versioned configuration (not compiled into application code), deployed independently of the Planning Engine's own code releases — this decouples "ship a prompt improvement" from "ship a code deployment," a meaningfully faster iteration loop for prompt engineering specifically, while still going through the same evaluation-set gate (3.6) before any production traffic sees a new version. A/B or canary rollout of a new prompt version (routing a small percentage of a specific capability's traffic to the new version, comparing evaluation metrics, Part V Section 19, before full rollout) is a supported deployment pattern this storage design enables, detailed operationally in Volume 4.

### 3.8 Localization and tenant-specific prompt variants (forward-looking, scoped)

Consistent with this document set's established practice of naming a plausible extension without over-building it now (Volume 0, Section 23's Future Vision discipline): `PromptTemplate` is designed to support tenant-specific or locale-specific variants of a given capability's prompt (e.g., a tenant whose target systems are documented in a specific domain vocabulary benefiting from a tailored system prompt) as an additive, optional override resolved at Section 2.4's step 3 — but no such variant exists in the GA scope this volume specifies; this is noted here only so the storage design (3.7) doesn't need retrofitting if and when a real tenant-customization requirement emerges, mirroring exactly how Volume 2, Section 3.6 handled the analogous tenant-branding extension point for the theme system.


### 3.9 Relationship to Part III's capability engines

Every capability engine specified in Part III (Sections 7–13) consumes exactly one or more `PromptTemplate`s defined per the structure in this section — Part III does not re-specify prompt authoring or versioning per capability; it specifies each capability's *specific* input/output types, its *specific* retrieval-context requirements (feeding into Part II), and its *specific* evaluation criteria (feeding into Part V), always assuming this section's prompt-management infrastructure as the substrate underneath.

____________________________________________________________________________________________________________________________________________________



4. Retrieval-Augmented Generation (RAG)
4.1 Purpose — what gets retrieved and why

Section 2.4's Planning Engine pipeline names "Context retrieval" as step 2, populating a RetrievalContext before prompt assembly. This section specifies that retrieval system: what content is indexed, how it's retrieved, and — the single most important constraint governing this entire section — how tenant isolation (Volume 0, Section 18.4's data-pooling boundary) is enforced structurally, not just by policy.

4.2 Why RAG, specifically, for Nexus's capabilities

Every one of Part III's capability engines benefits from context beyond what fits in a prompt template's static text: the Requirement Parser (Section 7) benefits from knowing which modules and actions actually exist and how they're typically composed; the Self-Healing engine (Section 10) benefits from knowing how a similar selector was successfully healed before; the Root Cause Analysis engine (Section 12) benefits from knowing this tenant's historical failure patterns. None of this is knowledge a general-purpose LLM has out of the box, and none of it should be baked into a fine-tuned model (which would be slow to update as a tenant's own flows and modules evolve, and would risk exactly the cross-tenant data-mixing Volume 0, Section 18.4 prohibits) — retrieval against a live, tenant-scoped index is the correct architectural fit specifically because Nexus's knowledge is per-tenant and continuously changing (Volume 1's flows, step_results, and module_registrations tables, Part V Section 32, are themselves living, growing data).

4.3 What gets indexed — four distinct corpora
Corpus  Source (Volume 1) Used by (Part III)
Module/action documentation ActionSchema.Description, ParamSpec.Description (Volume 1, Part II §11.8; the mandatory documentation Volume 1, Part VI §40.7 requires) Requirement Parser (§7), Test Generator (§8)
Historical flow examples  Published Flow definitions, tenant-scoped (Volume 1, Part V §32.2)  Test Generator (§8) — "how does this tenant typically structure a checkout-verification flow"
Historical execution outcomes StepResult.Data, AiAttribution records where self-heals occurred (Volume 1, Part II §10.3, §11.5) Self-Healing (§10), Root Cause Analysis (§12)
Log pattern history Aggregated, anonymized-within-tenant patterns from the Logs module's rule/anomaly history (Volume 0, Section 14.8)  Failure Analysis (§11), Root Cause Analysis (§12)

Every corpus above is populated from data Volume 1 already persists — this volume's RAG system indexes Nexus's own operational history, it does not introduce a new, independent data-collection mechanism. This is a direct, deliberate design choice: the platform's own accumulated usage is its richest source of tenant-specific context, and building the RAG index as a derived, continuously-updated view over Volume 1's existing schema (rather than a separately-maintained corpus) keeps this volume's data honest and current by construction.

4.4 Indexing pipeline
Volume 1 domain events (Part II §12) ──► RAG Indexer (this volume) ──► Vector Store
   FlowPublishedEvent                                                    (tenant-partitioned)
   StepCompletedEvent (with AiAttribution)
   ModuleRegisteredEvent

The RAG Indexer is itself an Event Bus consumer (Volume 1, Part III, Section 22.5's consumer table gains a new row for this volume: "RAG Indexer — Tier 2 durable — embeds and upserts the relevant document into the tenant's vector-store partition"), directly reusing Volume 1's existing outbox-backed, at-least-once delivery infrastructure rather than this volume inventing a separate change-data-capture mechanism. Idempotency (required of every Tier-2 consumer per Volume 1, Part II, Section 12.7) is satisfied by upserting on a stable document key (e.g., flow:{flowId}:{version}), consistent with every other durable consumer's obligation established there.

4.5 Vector store architecture and tenant isolation
typescript
interface VectorStore {
  upsert(tenantId: TenantId, documentId: string, embedding: number[], metadata: DocumentMetadata): Promise<void>;
  query(tenantId: TenantId, queryEmbedding: number[], topK: number): Promise<RetrievedDocument[]>;
}

Every method on this interface requires a TenantId as its first parameter, and the underlying implementation (a vector database, or a pgvector extension on the same PostgreSQL instance Volume 1 already specifies, Part V Section 32.6's indexing discipline extended to vector similarity search — a deliberate infrastructure-minimization choice consistent with Volume 1, Part III, Section 22.3's "no new hard dependency for GA scale" reasoning) enforces physical partitioning by tenant, not merely a filtered query — directly mirroring Volume 1, Part IV, Section 26.3's three-layer tenant-isolation defense-in-depth (repository filter, row-level security, application-service assertion), now applied to vector retrieval specifically: a query for tenant A's context can structurally never retrieve tenant B's embedded documents, verified the same way Volume 1's row-level-security policies are (Volume 1, Part V, Section 32.7).

4.6 Retrieval pipeline
1. Query construction — capability-specific (Section 2.4 step 2 is invoked with the
   capability's specific input, e.g., a natural-language flow description for
   Requirement Parser, or a broken selector + page HTML for Self-Healing)
2. Embedding — the query is embedded using the same model/dimension as the
   indexed corpora (Section 4.4) — a versioned choice, Part IV §14's provider
   abstraction covers embedding-model selection alongside generation-model selection
3. Tenant-scoped similarity search — VectorStore.query(tenantId, ...), topK
   configurable per capability (a broad Requirement Parser query might retrieve
   more candidate module examples; a narrow Self-Healing query might retrieve
   just the single most similar prior heal)
4. Re-ranking (optional, capability-dependent) — for capabilities where recall
   quality materially affects output quality (Test Generator, Self-Healing),
   a lightweight re-ranking step reorders the topK results before they're
   assembled into RetrievalContext
5. Context assembly — trimmed and formatted to fit the target PromptTemplate's
   requiredContextFields (Section 3.4), with an explicit token budget per
   capability to keep prompt-assembly cost bounded and predictable (feeding
   directly into Part IV §16's cost-optimization concerns)
4.7 Freshness and staleness

Directly mirroring Volume 1, Part V, Section 23.6's explicit per-read-model consistency table (a pattern this document set has now applied at every layer — backend read models, frontend cache invalidation in Volume 2 Section 8.3, and now RAG indexing): the RAG index is eventually consistent relative to Volume 1's source-of-truth data, with staleness bounded by the Event Bus Tier 2 relay's typical latency (Volume 1, Part III, Section 22.2). This is an accepted, stated tradeoff — a flow published moments ago might not yet be retrievable as a "similar prior example" for a Test Generator request, which is a materially less severe consequence than, say, FlowSummaryReadModel's strong-consistency requirement (Volume 1, Part V, Section 23.4), so no synchronous-indexing exception is warranted here the way one was warranted there.

4.8 Retrieval quality evaluation

Consistent with Part V's evaluation-metrics discipline (forward reference, fully specified there): retrieval quality itself — not just final capability-output quality — is tracked as a distinct metric (e.g., a periodic, curated "does this query retrieve the document a human expert would consider most relevant" evaluation set, mirroring Section 3.6's prompt evaluation-set discipline applied to retrieval specifically) — because a capability engine's output can only be as good as the context it was given, and conflating "the model produced a bad output" with "the model was given bad context" would make debugging a capability-quality regression significantly harder than treating them as two separately-measured, separately-improvable concerns.



5. Memory
5.1 Purpose and distinction from RAG

Section 4 specified retrieval against a persistent, continuously-indexed corpus — Nexus's accumulated operational history. This section specifies a genuinely different concern: conversational and interaction memory — what the AI layer remembers about the current, in-progress interaction (a flow-generation session where a user iteratively refines a generated draft) and, more narrowly, what it's permitted to carry forward about a specific user's or tenant's interaction patterns over time. Conflating these two concerns (as "memory" and "RAG" are sometimes loosely conflated in general AI-system discourse) would be a mistake this document set's precision-about-terminology discipline (Volume 1, Part II, Section 6.3's ubiquitous-language rule) exists specifically to prevent.

5.2 Session memory — scoped to one Planning Engine interaction sequence

The most common memory need: Volume 0's UC-2 (Priya using AI flow generation) is not always a single request-response exchange — a user might review a generated flow, ask for a specific adjustment ("also check that the response includes a tracking number"), and expect the AI to understand this as a refinement of the prior output, not a from-scratch new request.

typescript
interface AiSession {
  sessionId: string;
  tenantId: TenantId;
  userId: UserId;
  capability: AiPlanningRequest['capability'];
  turns: AiSessionTurn[];             // bounded — see 5.3
  createdAt: Date;
  expiresAt: Date;                     // short-lived, see 5.4
}

interface AiSessionTurn {
  request: AiPlanningRequest;
  result: AiPlanningResult<unknown>;
}

A session is explicitly scoped to one capability and one Planning Engine interaction sequence — a flow-generation session's memory is never implicitly available to a subsequent, unrelated self-healing request, even within the same user's browser session (Volume 2, Section 14's authentication session is a distinct, longer-lived concept from this AiSession, deliberately not conflated with it).

5.3 Session memory bounds — why unbounded conversation history is rejected

AiSession.turns is capped (a configurable maximum, e.g., 10 turns) rather than allowed to grow indefinitely, for two compounding reasons: unbounded conversation history directly threatens Part IV, Section 16's cost-optimization goals (every additional turn's history is repeated context in every subsequent prompt, per standard LLM context-window economics), and — more importantly for this volume's own stated priorities (Section 1.7) — a very long, drifting conversation is exactly the shape of interaction most vulnerable to the prompt-injection and goal-drift risks Part V, Section 17's guardrails are designed to catch early, not after many compounding turns have accumulated. A session reaching its turn limit prompts the user to start a fresh session (with the option, per 5.2's session-turn history, to reference the prior session's final output as an explicit starting point) rather than silently truncating history in a way that could produce confusing, context-losing behavior.

5.4 Session lifetime and expiry

Sessions expire after a short period of inactivity (e.g., 30 minutes, deliberately shorter than Volume 2, Section 14.5's 15-minute access-token lifetime is long relative to, chosen independently based on realistic authoring-session behavior rather than tied to the authentication token's own expiry) and are never persisted beyond that window — this is a direct, deliberate architectural choice reinforcing Volume 0, Section 18.4's data-minimization spirit: conversational context is ephemeral working memory, not a permanent record Nexus retains about how a user phrases their requests. (Contrast this explicitly with Volume 1, Part IV, Section 27's AuditEntry — the fact that an AI flow-generation request occurred is audited permanently per NFR-SEC-3, but the specific back-and-forth conversational content refining that request is not retained beyond the session, a deliberate and different retention posture for a deliberate and different reason.)

5.5 Tenant-scoped pattern memory — the Future Vision capability, scoped honestly

Volume 0's Future Vision (Section 23.3) named "cross-flow pattern learning... using the accumulated history of self-heals, flake patterns, and failure diagnostics across a customer's own flow history" as a plausible future direction, explicitly not committed GA scope. This section names the architectural seam for it without over-building it now, consistent with this document set's established practice (Section 3.8 of this volume already applied this same discipline to prompt localization): a TenantPatternMemory construct — a longer-lived, tenant-scoped (never cross-tenant) summary derived periodically from Section 4's RAG corpora (e.g., "this tenant's Web-module selectors most commonly break due to CSS-class renames, and heals involving data-testid fallback attributes have historically had a 90%+ acceptance rate for this tenant specifically") — is architecturally anticipated as an optional, additional RetrievalContext input (Section 2.3) that Section 10's Self-Healing engine could consume, but is not part of this volume's GA-scoped capability engines (Part III). This is stated explicitly rather than silently implied, matching Volume 0's own discipline of clearly marking aspiration versus commitment (Volume 0, Section 8.4's assumption-labeling; Section 23.7's explicit non-inclusions).

5.6 What memory never does — restating the permanent boundaries here specifically

Directly re-applying Section 1.4's architectural invariants to this section's specific concern: session memory (5.2) and tenant pattern memory (5.5) are both read-only inputs to a capability engine's context — neither is ever treated as authoritative in place of Volume 1's actual domain state. A session's remembered "the user asked to check for a tracking number" is a hint informing the next prompt's construction, never a substitute for the actual generated Flow draft's real, persisted content — if a session's memory and the actual current state of a referenced Flow draft ever diverge (e.g., the user edited the draft directly in the Workflow Designer, Volume 2 Section 11, between AI turns), the actual persisted Flow state wins, and the Planning Engine's next turn is constructed from that authoritative state, not from a possibly-stale memory of what the AI last proposed.

5.7 Memory and multi-tab/multi-device consistency

Referencing Volume 2, Section 14.9's cross-tab session-consistency design: AiSession (5.2) is deliberately not synchronized across browser tabs the way authentication state is — an AI flow-generation conversation happening in one tab is scoped to that tab's interaction sequence, and opening the same flow in a second tab starts a fresh session rather than attempting to merge or share conversational memory across tabs. This is a deliberate simplicity choice: the complexity of synchronizing conversational AI state across concurrent tabs was judged not to earn its cost (Volume 1, Architecture Principle 20.7's recurring standard) relative to the straightforward alternative of scoping each tab's AI interaction independently, with Section 5.6's "actual persisted state always wins" rule ensuring no data-consistency hazard results from this choice even if a user does have the same flow open in two tabs simultaneously.




6. Knowledge Base
6.1 Purpose — the curated layer above raw RAG retrieval

Section 4 specified RAG as retrieval against Nexus's own accumulated operational data (flows, executions, module schemas). This section specifies a distinct, complementary corpus: curated, authored knowledge that doesn't arise automatically from platform usage — module developer documentation beyond the mandatory ActionSchema descriptions (Volume 1, Part VI, Section 40.7), guardrail/prompt packs (Volume 0, Section 17.2's marketplace artifact type), and platform-level best-practice content Nexus's own team authors and maintains. Where Section 4's RAG corpora are derived from Volume 1's live data, this section's Knowledge Base is authored content with its own lifecycle, review process, and versioning.

6.2 Knowledge Base content types
Content type  Authored by Scope Used by
Platform best-practice guides Nexus's own team  Global (not tenant-scoped — this is Nexus's own product knowledge, not customer data) Requirement Parser (§7) — e.g., "how to phrase a testable acceptance criterion"
Module developer documentation  Module authors (built-in team or marketplace developers, Volume 0 §17)  Per-module, global once published Test Generator (§8) — deeper usage examples beyond the minimum ActionSchema.Description
Guardrail/prompt packs  Nexus's team or Enterprise-certified marketplace partners (Volume 0, Section 17.2's third artifact type)  Tenant-installable, never auto-global Part V, Section 17 — domain-specific guardrail policy content (e.g., a healthcare PHI-redaction pack)
Tenant-authored knowledge (optional, tenant-supplied) The tenant itself Strictly tenant-scoped  Any capability, when a tenant chooses to supply supplementary context (e.g., internal API naming conventions)
6.3 Why a separate system from Section 4's vector store, not the same index

A deliberate architectural distinction: Knowledge Base content is reviewed, versioned, and explicitly published content (mirroring Volume 1, Part II, Section 9.2's Draft/Published Flow lifecycle discipline, applied here to knowledge content) — it does not get silently, automatically indexed the instant it's authored the way Section 4's RAG corpora are event-driven-indexed from live platform activity. This matters specifically for the global content types in 6.2's table (platform best-practice guides, module documentation): this content directly shapes AI capability behavior for potentially every tenant, so it goes through the same rigor Volume 0, Section 17.3 established for marketplace module certification, before it's retrievable at all — an unreviewed draft of a best-practice guide must never silently influence a live flow-generation request.

6.4 Retrieval integration — one unified RetrievalContext, two distinct sources
typescript
interface RetrievalContext {
  ragResults: RetrievedDocument[];        // Section 4 — tenant-scoped, live-data-derived
  knowledgeBaseResults: KnowledgeBaseEntry[];  // this section — curated, published content
}

Both sources feed into the same RetrievalContext the Planning Engine assembles (Section 2.3, 2.4 step 2) and the same prompt-assembly step (Section 2.4 step 3) consumes — a PromptTemplate's requiredContextFields (Section 3.4) can reference either or both, but the two remain architecturally distinct sources with different governance (6.3), different tenant-scoping rules (6.5), and different freshness guarantees (Section 4.7's eventual consistency versus this section's explicit-publish-triggered consistency).

6.5 Tenant scoping for Knowledge Base content

Directly extending Volume 0, Section 18.4's data-pooling boundary and Section 4.5's vector-store tenant partitioning to this section's different content shape: global content (best-practice guides, module documentation) is retrievable by every tenant equally — this is not customer data, it's Nexus's own product knowledge, so no isolation concern applies. Guardrail/prompt packs and tenant-authored knowledge (6.2's third and fourth rows) are strictly opt-in and tenant-scoped, using the identical TenantId-partitioned enforcement Section 4.5 established for the RAG vector store — a marketplace guardrail pack a tenant hasn't installed is never retrievable in that tenant's RetrievalContext, mirroring exactly how Volume 1, Part IV, Section 26.5 scoped Community-tier module registration per-tenant rather than globally.

6.6 Knowledge Base authoring and review workflow
Draft (author writes/edits content)
   │
   ▼
Review (Nexus team for platform content; Verified/Enterprise-certified
         review process, Volume 0 §17.3, for marketplace guardrail packs)
   │
   ▼
Published (indexed and retrievable — mirrors Volume 1 Part II §9.2's
            Flow Draft→Published transition exactly, including the same
            "immutable once published, new version for changes" discipline,
            Section 6.3 already flagged)
   │
   ▼
Deprecated (superseded by a newer version, retained for audit/rollback
             per Volume 1 Part III §18.7's contract-deprecation pattern,
             no longer retrieved for new requests)
6.7 Module developer authoring experience

Directly serving Persona Raj (Volume 0, Section 10.5): a module developer supplying documentation beyond the mandatory ActionSchema.Description (Volume 1, Part VI, Section 40.7) does so through the same marketplace publishing flow already established (Volume 0, Section 17.4's installation flow; Volume 1, Part III, Section 18.8's registration flow), with an additional, optional documentation-authoring step — this is explicitly framed as an enhancement to a module's AI-assisted discoverability (a well-documented module's actions are more likely to be correctly suggested by the Requirement Parser and Test Generator, Sections 7–8), giving Raj a concrete, tangible incentive to invest in thorough documentation beyond the bare minimum Volume 1 requires.

6.8 Freshness and cache invalidation

Unlike Section 4.7's eventually-consistent RAG index, Knowledge Base retrieval is synchronously consistent with the Published state — the moment a piece of content transitions Draft → Published (6.6), it is immediately retrievable, and the moment a version is superseded (Deprecated), it is immediately no longer retrieved for new requests. This stronger consistency guarantee is justified, unlike the RAG index's acceptable eventual consistency (Section 4.7), because Knowledge Base publish events are comparatively rare, deliberate, human-reviewed actions (6.6) rather than the high-frequency, automatic platform-activity events driving Section 4's index — the same reasoning Volume 1, Part V, Section 23.4 used to justify FlowSummaryReadModel's synchronous consistency over FlowReportReadModel's eventual consistency, applied here to an analogous low-frequency-write, consistency-matters case.

6.9 Relationship to Part III's capability engines

As with Section 4's RAG corpora, Part III's capability engines consume this section's Knowledge Base purely through the unified RetrievalContext (6.4) — no capability engine queries Knowledge Base content directly or independently of the Planning Engine's step-2 retrieval, preserving the same "one retrieval boundary, uniformly applied" discipline Section 2.2 established as this volume's core organizing principle.


____________________________________________________________________________________________________________________________________________________



7. Requirement Parser
7.1 Purpose and position in the flow-generation pipeline

Volume 1's GenerateFlowFromDescriptionCommand (Part II, Section 13.2) takes a single natural-language description and must produce a valid, structurally-sound Flow draft. Section 2.6's mapping table split this into two composed capabilities: the Requirement Parser (this section) and the Test Generator (Section 8). This section specifies the first stage — turning unstructured English into a structured, intermediate Parsed Intent representation, before any actual flow steps are constructed. This staged design exists for a concrete reason: separating "what does the user want verified" from "how do we construct valid steps to verify it" makes each stage independently testable and independently improvable, mirroring the same separation-of-concerns discipline Volume 1 applied to its own orchestration layers (Part I, Section 5's Flow Parser versus Flow Engine as genuinely distinct components).

7.2 Output type — Parsed Intent
typescript
interface ParsedIntent {
  targetSystemHints: string[];        // e.g., ["health endpoint", "/api/health"] — extracted
                                          // mentions of what system/endpoint is under test
  candidateModules: ModuleKind[];      // which of the tenant's available modules
                                          // (Volume 1 Part II §14.2's GetAvailableModuleActionsQuery
                                          // result, retrieved via this volume's Section 4 RAG,
                                          // not guessed from general knowledge) plausibly apply
  assertionIntents: AssertionIntent[]; // structured, not yet module-specific
  ambiguities: AmbiguityFlag[];        // explicit — see 7.5
}

interface AssertionIntent {
  description: string;                 // "the response body contains status: ok"
  assertionKind: 'status' | 'bodyField' | 'visualState' | 'logPattern' | 'unclear';
}

interface AmbiguityFlag {
  aspect: string;                       // e.g., "target module unclear"
  candidates: string[];
  requiresClarification: boolean;
}
7.3 Why a distinct intermediate representation, not direct English-to-Flow generation

A tempting simpler design has the Test Generator (Section 8) work directly from the raw natural-language string. This section's staged design is preferred for three reasons: first, ParsedIntent is a much smaller, more constrained structure for output-schema validation (Section 3.5) to check — catching a Requirement Parser failure ("the model couldn't identify any candidate module") early, before the more expensive Test Generator stage runs, directly serving Part IV's cost-optimization goals by failing cheaply. Second, ParsedIntent.ambiguities gives the Planning Engine (Section 2.4) an explicit, structured signal for when to prompt the user for clarification rather than guessing — a capability directly serving UX Principle 1.2's "never a fiction of the real state" (Volume 2, Section 1.3) applied to AI-generated content specifically: an ambiguous request should surface as a visible ambiguity, not a silently-guessed, confidently-wrong flow. Third, ParsedIntent is independently reusable — a future capability (e.g., Volume 0's Future Vision Section 23.3's "autonomous exploration and coverage suggestion") could consume ParsedIntent without depending on flow-construction logic at all.

7.4 Retrieval context this capability consumes

Per Section 4.3's corpora table, the Requirement Parser primarily retrieves module/action documentation — a natural-language description mentioning "check the health endpoint" needs grounding in which of this specific tenant's registered modules and actions could plausibly implement that check, retrieved via Section 4.6's pipeline rather than relying on the model's general training-data knowledge of what a generic testing tool might support. This retrieval step is precisely why candidateModules (7.2) is populated from actually-registered modules, never a hallucinated module name — a direct, concrete enforcement mechanism for UX Principle 1.3's module-parity guarantee (Volume 2, Section 1.4) extended into this volume: the Requirement Parser structurally cannot suggest a module that doesn't exist in this tenant's environment, because its retrieval context never contains one.

7.5 Handling ambiguity — the clarification loop

When ParsedIntent.ambiguities contains an entry with requiresClarification: true, the Planning Engine (Section 2.4) does not proceed to Section 8's Test Generator — it returns the ambiguity to the calling Volume 1 command handler as a distinct, structured outcome (an extension of FlowGenerationResult, Volume 1 Part II Section 13.2, carrying a clarification-needed variant alongside its success/failure variants), which Volume 2's AiFlowGenerationPanel (Section 6.2 there) surfaces as a follow-up question rather than silently picking the most likely interpretation. This is the Requirement Parser's most direct implementation of Section 1.5's AI Vision statement — making the platform "easier to author" specifically means surfacing genuine ambiguity for a human to resolve, not hiding it behind confident-sounding but potentially wrong output.

7.6 Multi-turn refinement via Session Memory

Referencing Section 5.2's AiSession: a follow-up request within the same session ("also check for a tracking number in the response") is parsed with the prior turn's ParsedIntent included as additional context — the Requirement Parser's prompt template (Section 3.2) has a distinct variant specifically for refinement turns versus initial-request turns, since a refinement should produce an updated ParsedIntent (adding to or modifying the prior one), not a wholly independent parse that discards everything previously established.

7.7 Evaluation criteria for this capability

Per Section 3.6's evaluation-set discipline: the Requirement Parser's evaluation set consists of representative natural-language descriptions (drawn from real, anonymized-where-needed usage patterns once available, and from Volume 0's own use-case catalog, Section 11, as an initial seed set before real usage data exists) with human-annotated expected candidateModules and assertionIntents — measuring precision (did it suggest only genuinely relevant modules) and recall (did it identify every assertion the description actually implies) separately, since these two failure modes have different product consequences: low precision produces a cluttered, confusing generated flow; low recall produces an incomplete one that looks complete, the more dangerous failure mode per this volume's general bias toward surfacing rather than hiding uncertainty (7.5).



8. Test Generator
8.1 Purpose — from Parsed Intent to a valid Flow

Where Section 7's Requirement Parser produces a structured, module-agnostic-until-grounded ParsedIntent, this section specifies the capability that consumes it and produces an actual, structurally valid Flow draft (Volume 1, Part II, Section 8.2) — the second and final stage of the pipeline backing GenerateFlowFromDescriptionCommand (Volume 1, Part II, Section 13.2), directly implementing Volume 0's UC-2.

8.2 Output type — a Flow draft, not a new type

Unlike Section 7's bespoke ParsedIntent type, the Test Generator's output is deliberately the exact same Flow domain type Volume 1 already defines (Part II, Section 9.2), never a parallel "AI-generated flow" type requiring separate handling downstream:

typescript
interface TestGenerationOutput {
  flow: FlowDraftDto;              // structurally identical to a human-authored draft
  stepProvenance: Map<FlowStepId, StepProvenance>;   // per-step: which ParsedIntent
                                                          // assertion/module hint it was
                                                          // derived from, and confidence
}

interface StepProvenance {
  derivedFromIntent: string;         // which AssertionIntent.description this step implements
  confidence: number;
  retrievedExampleUsed?: string;      // if a historical flow example (Section 4.3) informed this step
}

This is a direct, deliberate application of Section 1.2's framing — this volume does not introduce new domain concepts; it produces valid instances of Volume 1's existing ones. A generated Flow draft is, from the moment it's returned, indistinguishable in type from a human-authored one; stepProvenance is the only AI-specific addition, and it travels alongside the draft as metadata for the review experience (Volume 2, Section 6.2's AiFlowGenerationPanel), never as part of the Flow aggregate itself (Volume 1, Part II, Section 9.2 defines that aggregate's shape completely; this volume does not extend it).

8.3 Step construction — grounded in retrieved ActionSchemas, never invented

For each AssertionIntent in the input ParsedIntent, the Test Generator constructs one or more FlowSteps (Volume 1, Part II, Section 10.2), with every step's params validated against the actual retrieved ActionSchema (Section 4.3's module/action documentation corpus) for the chosen module and action — this is the same JSON-Schema-derived validation Volume 2, Section 10.4 specified for the frontend's manual parameter editor, now applied here to verify AI-generated parameters before they're ever returned to a user. A generated step whose parameters fail this validation is not silently included with best-effort values (directly consistent with Section 3.5's "no lenient coercion" rule) — it's either regenerated with corrective feedback (a bounded retry, not unlimited, per Part IV's cost-optimization concerns) or omitted with the gap surfaced via stepProvenance's confidence field dropping low enough to flag for human attention.

8.4 Variable chaining in generated flows

Where a ParsedIntent implies multi-step data dependency (e.g., "create an order, then check its status via the API" implies the order ID from step 1 feeds step 2), the Test Generator constructs the saveAs/{{vars.x.y}} chaining (Volume 1, Part II, Section 10.2, Section 11.7) explicitly — informed by retrieved historical flow examples (Section 4.3) showing how this tenant's existing flows typically chain similar data, when such examples exist, falling back to generic, schema-informed chaining (matching a saveAs binding's likely field name against the target step's parameter names) when no closely-relevant historical example is retrieved.

8.5 Retrieval context this capability consumes

Beyond the ActionSchema documentation already used in Section 7.4 (shared, not re-retrieved independently — Section 2.4's context-retrieval step populates RetrievalContext once per Planning Engine invocation, and the Test Generator stage reuses the same retrieved module documentation the Requirement Parser stage already grounded its module suggestions in): this capability additionally retrieves historical flow examples (Section 4.3) specifically to inform step structure and chaining patterns (8.4), and, where the Knowledge Base (Section 6) has published best-practice content relevant to the target module (e.g., a guide on structuring API-assertion flows), that content is included as well.

8.6 Structural validation before returning to Volume 1

Before TestGenerationOutput is returned from the Planning Engine (Section 2.4, step 6), the constructed Flow draft is validated against the exact same publish-time-adjacent structural checks Volume 1's Flow.Publish performs (Volume 1, Part II, Section 9.2) — module/action resolution, parameter schema conformance — run here proactively, before the user ever sees the draft, so that a generated flow that would fail publication is caught and either corrected or flagged, not discovered only when the user attempts to publish it themselves. This double-checking (once here, once again authoritatively by Volume 1's own Flow.Publish at actual publish time) is intentional redundancy, not waste — this volume's capability engines never get to skip Volume 1's authoritative validation just because they performed a similar check first, directly reinforcing Section 1.4's "AI never executes or persists autonomously" boundary: this capability's own validation is advisory quality-control, Volume 1's is the real, load-bearing gate.

8.7 Human review remains mandatory regardless of confidence

Even a TestGenerationOutput with uniformly high per-step confidence (8.2) never bypasses the human-review step Volume 0's UC-2 specifies — the generated Flow always lands as a Draft, requiring an explicit, separate PublishFlowCommand (Volume 1, Part II, Section 13.2) issued by the human reviewing it. This is stated here explicitly, even though it's already a structural consequence of 8.2's design (a Draft is simply what this capability produces), because it's worth restating as a substantive product guarantee, not just an implementation detail: no confidence threshold, however high, ever promotes this capability's output directly to Published — that would cross Section 1.4's permanent autonomy boundary regardless of measured accuracy, a line this volume does not treat as negotiable based on quality metrics improving over time.

8.8 Evaluation criteria

Directly extending Section 7.7's evaluation-set discipline: the Test Generator's evaluation set measures structural validity rate (does the generated Flow pass Section 8.6's proactive check, the exact metric Volume 0, Section 18.7 named as this capability's primary quality signal) and, separately, a human-rated semantic-fidelity score (does the generated flow actually verify what the original natural-language description asked for, judged against Section 7's ParsedIntent as the intermediate ground truth) — tracked as two distinct numbers specifically because a flow can be structurally perfect while missing the user's actual intent, or vice versa, and conflating them into one score would hide which failure mode a given prompt or retrieval change is actually improving or regressing.



9. Locator Generator
9.1 Purpose — a shared capability, not a standalone product feature

Unlike Sections 7–8 (which compose to serve exactly one Volume 1 command) and Section 10 (which this capability directly feeds), the Locator Generator is specified as its own section because it is consumed by two distinct callers: the Test Generator (Section 8.3, when constructing Web-module steps that need a selector for a described UI element) and the Self-Healing engine (Section 10, when an existing selector has broken). Separating it out avoids duplicating selector-generation logic in two places, directly consistent with this document set's repeated "define once, reuse everywhere" discipline (Volume 2, Sections 9.4, 10.6, 12.5 all made this same choice for their respective concerns).

9.2 Output type
typescript
interface LocatorProposal {
  selector: string;                    // e.g., "[data-testid='submit-button']"
  selectorStrategy: 'testId' | 'role' | 'text' | 'css' | 'xpath';
  confidence: number;
  alternativeSelectors: RankedAlternative[];   // ranked fallback options, see 9.5
  reasoning: string;                    // human-readable — always populated, see 9.6
}

interface RankedAlternative {
  selector: string;
  selectorStrategy: LocatorProposal['selectorStrategy'];
  confidence: number;
}
9.3 Two distinct inputs, one shared output shape

The Locator Generator accepts either of two input shapes, reflecting its two calling contexts:

typescript
type LocatorGeneratorInput =
  | { mode: 'describe'; elementDescription: string; pageContext: PageStructureSummary }
  | { mode: 'heal'; brokenSelector: string; originalIntent: string; currentPageHtml: string };

The 'describe' mode serves Section 8.3's Test Generator use case ("a submit button" → a proposed selector against a described or retrieved page structure); the 'heal' mode serves Section 10's Self-Healing use case, directly matching Volume 1's already-specified RequestSelectorHealCommand input shape (Volume 1, Part II, Section 13.3: brokenSelector, intent, pageHtml) exactly — this volume's LocatorGeneratorInput.heal variant is a direct, typed realization of what Volume 1 already anticipated needing, not a new design.

9.4 Selector strategy preference — why testId and role are preferred over css/xpath

The Locator Generator's prompt template (Section 3.2) explicitly instructs a preference ordering — data-testid attributes and ARIA-role-based selectors are preferred over raw CSS selectors or XPath, and CSS/XPath are used only when no more stable alternative is identifiable — because this preference ordering directly correlates with real-world selector stability (a data-testid attribute is far less likely to change during unrelated styling refactors than a CSS class chain). This is a concrete, worked example of Section 1.5's AI Vision statement in practice: preferring stable selectors is what makes the platform genuinely "cheaper to maintain" (Volume 0, Section 18.3's maintenance-reduction value mechanism), not merely "produces a working selector today with no regard for tomorrow's flake rate."

9.5 Ranked alternatives — never a single point of failure

Every LocatorProposal includes alternativeSelectors — a ranked list of fallback strategies for the same target element, not just the single top choice. This directly informs the Web module's own resilience: Volume 1, Part III, Section 18's WebModule implementation (originally prototyped with a single-selector click/fill design) is extended, per this volume's influence flowing back into how the Web module could optionally attempt ranked alternatives before failing outright — noted here as a forward-looking architectural connection between this volume and Volume 1's module implementation, flagged explicitly (consistent with this document set's practice of surfacing cross-volume dependencies rather than leaving them implicit) as a Volume 1 amendment worth considering, similar in spirit to the Unit of Work correction made earlier in this project, though not applied unilaterally here — this volume specifies the capability; whether the Web module actually consumes alternativeSelectors for automatic fallback-attempt behavior is a Volume 1 implementation decision this volume flags but does not unilaterally impose.

9.6 reasoning — mandatory, never omitted

Every LocatorProposal.reasoning field is populated, always, even for high-confidence proposals — directly implementing UX Principle 1.6's transparency requirement (Volume 2, Section 1.7) at the data-shape level: the Volume 2 self-heal review UI (Volume 2, Section 10.5's read-only Monaco display of page HTML during self-heal review) surfaces this reasoning text alongside the proposed selector, so a human reviewing a heal sees not just what is proposed but why — e.g., "the original selector targeted a class that no longer exists on this element; the data-testid='submit-btn' attribute is present and semantically matches the original intent."

9.7 Confidence calibration — why this is the capability Part V scrutinizes most

Because Section 10's Self-Healing engine uses LocatorProposal.confidence directly against Volume 1, Part III, Section 18.5's configured confidence threshold to decide whether a heal auto-applies or only advises, this capability's confidence calibration is held to a materially higher evaluation bar than the other capability engines in this Part — Part V, Section 19's evaluation metrics track not just whether proposals are generally good, but specifically whether confidence scores are well-calibrated (a proposal scored 0.9 confidence should be correct roughly 90% of the time across a large evaluation set, not just "usually pretty good") — poorly-calibrated confidence is arguably more dangerous than uniformly-low confidence, since it's what Volume 1's threshold-based auto-apply decision (Volume 1, Part V, Section 34.4's sequence diagram) directly trusts.

9.8 Retrieval context this capability consumes

Primarily historical execution outcomes (Section 4.3) — specifically, prior successful heals for similar broken-selector patterns on this tenant's flows, retrieved to inform both the proposed selector and its confidence (a pattern that has healed successfully many times before for this tenant warrants materially higher confidence than a novel one) — directly the mechanism Section 5.5 named as a Future Vision extension point (TenantPatternMemory) but, consistent with that section's honest scoping, this capability's GA behavior relies on Section 4's already-specified RAG retrieval over historical AiAttribution records (Volume 1, Part II, Section 11.5), not the not-yet-built tenant-pattern-memory summarization layer.



10. Self-Healing
10.1 Purpose — the orchestrating capability around Locator Generator

Section 9 specified the Locator Generator as a pure proposal-generation capability. This section specifies Self-Healing as the capability that consumes a LocatorProposal and makes the confidence-threshold-gated decision Volume 1, Part III, Section 18.5 and Part V, Section 34.4 already anticipated architecturally — this is the direct, full specification of the capability those Volume 1 sections referenced but correctly deferred to this volume.

10.2 Output type
typescript
interface SelfHealOutcome {
  decision: 'auto-applied' | 'advisory-only' | 'no-viable-proposal';
  proposal: LocatorProposal;            // Section 9.2 — always included, even when advisory-only
  attribution: AiAttribution;            // Volume 1 Part II §11.5 — constructed here, directly
                                            // consumed by Volume 1's StepResult (Volume 1 Part II §10.3)
}
10.3 The threshold decision — implemented exactly where Volume 1 specified it would be
typescript
function decideSelfHeal(proposal: LocatorProposal, threshold: number): SelfHealOutcome['decision'] {
  if (!proposal) return 'no-viable-proposal';
  return proposal.confidence >= threshold ? 'auto-applied' : 'advisory-only';
}

This is the literal implementation of the decision Volume 1, Part III, Section 18.5 named ("if confidence exceeds a configured threshold, the flow proceeds using the healed selector... if confidence is below threshold, the flow fails normally") and Volume 1, Part V, Section 34.4's sequence diagram traced end-to-end. This volume's contribution is specifying how the confidence number itself is produced and trusted (Section 9.7's calibration discipline) — Volume 1 correctly specified the decision's mechanics, deferring the harder problem of making that confidence number meaningful to this volume, exactly as this document set's volume-ordering was designed to allow (Section 1.6).

10.4 Threshold configuration — tenant and flow-level, never hardcoded

The confidence threshold is not a single, global constant — it is configurable per-tenant (a tenant with lower risk tolerance sets a higher bar for auto-apply) and, per Volume 1's flow-authoring flexibility, overridable per-flow (a flow author can disable self-healing entirely for a specific flow, or set a stricter threshold for a particularly sensitive flow) — surfaced in Volume 2's flow-authoring UI (a settings panel extension to Section 11 there, the Workflow Designer) as an explicit, visible configuration a flow author sets deliberately, never an invisible platform default a user might not realize is active.

10.5 Permission inheritance — restating Volume 1's rule as this capability's binding constraint

Directly re-citing Volume 1, Part IV, Section 25.6: a self-heal action, even when auto-applied, executes under the authorization context of the FlowRun's original triggering identity — this volume's Self-Healing capability never has, requests, or is granted any elevated privilege beyond what that triggering identity already held. This section restates the rule here specifically because it's the exact boundary that makes "auto-applied" safe to say at all — an auto-applied heal is not the AI acting with independent authority; it is the AI proposing an action that executes within the already-authorized triggering identity's existing permission envelope, a distinction worth being precise about given how easily "auto-applied" could be misread as a relaxation of Section 1.4's autonomy boundary (it is not — the boundary that's actually relaxed is human-in-the-loop-per-instance, not authorization scope, which never changes).

10.6 Transparency — every heal is visible, auto-applied or not

Directly implementing FR-AI-3 (Volume 0, Section 12.3) and Volume 1, Part II, Section 12.3's StepSelfHealedEvent, which fires in addition to StepCompletedEvent regardless of whether the decision (10.3) was auto-applied or advisory-only: there is no code path in this capability where a heal occurs and no attribution record results. SelfHealOutcome.attribution (10.2) is always constructed, always attached to the resulting StepResult, and always visible in Volume 2's reporting surface (Section 9.3's AiAttributionBadge) and Volume 1's audit trail (Part IV, Section 27) — this is the single most heavily cross-volume-enforced guarantee in this entire document set, appearing as a binding requirement in Volume 0 (product strategy), Volume 1 (domain model, events, sequence diagram), Volume 2 (UI treatment, accessibility), and now this volume (the capability that actually produces the attribution data all those other volumes render and persist).

10.7 The no-viable-proposal outcome — honest failure, not a forced guess

When the Locator Generator (Section 9) cannot produce any proposal meeting even a minimal quality bar (distinct from, and lower than, the auto-apply threshold — a floor below which even an advisory suggestion isn't worth showing), SelfHealOutcome.decision is 'no-viable-proposal', and Volume 1's RequestSelectorHealCommand handler (Part II, Section 13.3) treats this identically to the AI module being entirely unavailable (Section 2.5's AiUnavailableError path) — the flow fails normally, with no AI-attributed content at all, rather than this capability manufacturing a low-confidence proposal just to have something to show. This is a direct, deliberate application of Section 7.5's "surface ambiguity, don't hide it behind confident-sounding guesses" principle, now applied to the case where the honest answer is "no good proposal exists."

10.8 Retrieval context this capability consumes

Entirely inherited from Section 9.8 — Self-Healing does not perform independent retrieval; it consumes the LocatorProposal (and the retrieval context that informed it) that Section 9's engine already produced, since Self-Healing's job is the threshold decision (10.3) and attribution construction (10.6), not additional context-gathering.

10.9 Evaluation criteria — self-heal acceptance rate, the metric Volume 0 already named

Directly implementing Volume 0, Section 18.7's designated primary quality signal for this capability: the percentage of auto-applied heals that a human, reviewing the resulting run, does not subsequently revert or flag as incorrect (tracked via a lightweight, optional "this heal was wrong" action surfaced in Volume 2's run-detail view alongside the AiAttributionBadge) — this is the exact metric Volume 0, Part I, Section 6.2 set a ≥70% target for, now given its full, precise operational definition: the denominator is every auto-applied decision (10.3), the numerator is every one not subsequently flagged incorrect, computed on a rolling window and reviewed as part of Part V, Section 19's release-gating discipline for any change to this capability's threshold defaults, prompt template (Section 3), or the underlying Locator Generator (Section 9).



11. Failure Analysis
11.1 Purpose — the first-tier diagnostic capability

This section specifies the direct formalization of the triageLogs capability already sketched in this project's original AiModule prototype and named explicitly in Volume 0, Section 14.9 ("log-triage capability"). Failure Analysis is the fast, first-tier diagnostic engine — given a failed StepResult or a batch of log lines, it produces an immediate severity/category classification. Section 12 (Root Cause Analysis) is the deeper, slower capability that Failure Analysis's output feeds into when a more thorough investigation is warranted — the two are distinct capabilities specifically because they serve different points in Persona Sofia's and Priya's diagnostic workflow (Volume 0, Section 10.2–10.3): a quick triage label appears immediately in a failed run's report; a full root-cause narrative is available on demand, not computed eagerly for every failure.

11.2 Output type
typescript
interface FailureAnalysisResult {
  severity: 'info' | 'warn' | 'error' | 'critical';   // matches Volume 1 Part II §10.4's
                                                          // RetryAttempt/StepStatus vocabulary
                                                          // and the LogModule's LogRule severity
                                                          // (Volume 1's original prototype), never
                                                          // a separately-invented severity scale
  category: FailureCategory;
  affectedComponent: string;
  summary: string;                      // one-sentence, human-readable
  suggestedNextStep: string;
  rootCauseAnalysisRecommended: boolean; // signals to the caller whether Section 12 is warranted
}

type FailureCategory =
  | 'target-system-error' | 'network-timeout' | 'assertion-mismatch'
  | 'selector-not-found' | 'auth-failure' | 'data-dependency-missing' | 'unknown';

This is a direct, typed evolution of the original prototype's triageLogs output shape (severity, likelyRootCause, affectedComponent, suggestedNextStep), now formalized with a closed FailureCategory enum (replacing the prototype's free-text likelyRootCause field, which becomes this capability's summary) — the closed-enum change is a direct application of Section 3.4's "structured output required, always" principle, since a free-text root-cause field is exactly the kind of unstructured output this volume's design discipline has consistently moved away from.

11.3 Two distinct inputs — step failure and log batch
typescript
type FailureAnalysisInput =
  | { mode: 'stepFailure'; stepResult: StepResultDto; flowContext: FlowContextSummary }
  | { mode: 'logBatch'; logText: string; sourceContext: string };

The 'stepFailure' mode serves the automatic, eager-triage case (every failed StepResult, per Volume 1, Part II, Section 12.6's event-consumer table, could trigger this capability as an additional Event Bus consumer reacting to a failed outcome, surfaced immediately in the run report). The 'logBatch' mode serves the Logs module's direct triage capability (Volume 0, Section 14.9), operating on raw log text independent of any specific FlowRun.

11.4 Why eager, automatic triage for every failure — and why it's still optional

Unlike Sections 7–10, which are always explicitly user-invoked (a user asks for flow generation, self-healing is opt-in per flow per Section 10.4), Failure Analysis is specified as an eager, automatic capability — triggered on every failed StepResult by default, not only when a user explicitly requests it, directly serving Section 1.5's "diagnostic acceleration" value mechanism by having the triage already available the instant a user opens a failed run's detail view, rather than making them wait for an on-demand call. This eagerness is still subject to Section 1.4's optionality boundary: it is tenant-configurable to disable (directly reusing NFR-REL-3's AI-optionality infrastructure, Volume 1, Part IV, Section 24.6 area), and its absence never blocks or delays the underlying StepResult from being recorded and reported — Failure Analysis output, when present, is an additive annotation on an already-complete report, never a gate on report availability, consistent with Volume 1, Part III, Section 19's fail-fast execution loop never waiting on this capability before proceeding.

11.5 Retrieval context this capability consumes

Primarily log pattern history and historical execution outcomes (Section 4.3) — specifically, how similar failures on this tenant's flows were previously categorized and what their eventual actual root cause turned out to be (informed by human feedback where available, Section 11.7) — allowing the category/severity classification to improve in accuracy specifically for this tenant's own recurring failure patterns over time, without any cross-tenant data mixing (Section 4.5's tenant-partitioned vector store enforced identically here).

11.6 Cost-conscious model routing for this specific capability

Forward-referencing Part IV, Section 15: because Failure Analysis runs eagerly and potentially frequently (every failed step, per 11.4), it is a primary candidate for routing to a smaller, cheaper, faster model than the Test Generator (Section 8) or Root Cause Analysis (Section 12) use — this capability's relatively narrow, well-structured output (11.2's closed enums) is well-suited to a lighter model, and Part IV, Section 15's routing policy explicitly names this capability as a default target for cost-optimized routing, directly serving Part IV, Section 16's cost-optimization goals without sacrificing meaningful quality, since the evaluation-set discipline (Section 3.6) validates this routing choice empirically rather than assuming it.

11.7 Human feedback loop — the mechanism that actually improves categorization over time

Volume 2's run-detail view (Section 6.3 there) surfaces a lightweight "was this triage accurate" affordance alongside the FailureAnalysisResult display — feedback that flows back into two places: Part V's evaluation metrics (was this specific categorization correct, aggregated over time) and, indirectly, Section 11.5's retrieval corpus (a human-corrected categorization becomes part of the historical-outcomes corpus future retrievals draw on for this tenant specifically) — this is the concrete mechanism by which this capability's accuracy for a specific tenant improves with usage, without any model fine-tuning or cross-tenant learning, consistent with Section 1.4's permanent data-pooling boundary.

11.8 Evaluation criteria

Category and severity classification accuracy against a human-labeled evaluation set (Section 3.6), plus the rootCauseAnalysisRecommended flag's own precision (does a "yes, dig deeper" recommendation actually correlate with cases where Section 12's deeper analysis reveals something Failure Analysis's own summary didn't already make clear) — this second metric matters specifically because over-recommending deep analysis undermines the two-tier design's entire cost/speed rationale (11.1), while under-recommending it risks a genuinely complex failure being under-diagnosed by a deliberately lightweight first pass.



12. Root Cause Analysis
12.1 Purpose — the second-tier, on-demand diagnostic capability

Where Section 11's Failure Analysis is eager, fast, and narrow (a single failed step, classified quickly), Root Cause Analysis is on-demand, slower, and broad — it correlates across multiple steps, multiple runs, and potentially multiple modules to answer a genuinely harder question: not "what kind of failure is this" but "why did this actually happen, and what's the underlying cause." This directly serves Volume 0's diagnostic-acceleration value mechanism (Section 1.3) at its deepest point, and is the capability most directly responsible for reducing Persona Sofia's and Priya's mean-time-to-diagnose (Volume 0, Part I, Section 6.2's metric).

12.2 Output type
typescript
interface RootCauseAnalysisResult {
  rootCause: string;                    // human-readable narrative, structured per 12.4
  contributingFactors: ContributingFactor[];
  correlatedFailures: CorrelatedFailure[];   // other steps/runs sharing the likely cause
  confidence: number;
  recommendedActions: RecommendedAction[];
}

interface ContributingFactor {
  description: string;
  evidenceSource: 'log-pattern' | 'historical-correlation' | 'step-sequence' | 'timing-analysis';
  weight: number;                        // relative contribution, not independently meaningful
                                             // in isolation — see 12.4
}

interface CorrelatedFailure {
  flowRunId: string;
  stepResultId: string;
  similarityScore: number;
}

interface RecommendedAction {
  action: string;
  actionKind: 'fix-flow' | 'fix-target-system' | 'adjust-assertion' | 'investigate-further';
}
12.3 When this capability is invoked — always on-demand, never eager

Unlike Section 11's default-eager triage, Root Cause Analysis is invoked only explicitly — either directly by a user (a "dig deeper" action in Volume 2's run-detail view, Section 6.3) or automatically when Section 11's rootCauseAnalysisRecommended flag is true and the tenant has configured automatic escalation (a tenant-level setting, not a platform default, per Section 1.4's optionality discipline applied per-capability, not just per-volume). This on-demand-by-default posture is a direct cost-optimization decision (Part IV, Section 16 will elaborate): this capability's broader retrieval scope (12.5) and typically larger model requirement (Part IV, Section 15's routing) make it meaningfully more expensive per invocation than Section 11's lightweight triage, and running it eagerly for every failure would be a poor cost/value tradeoff for the majority of failures Section 11's faster triage already explains adequately.

12.4 Narrative construction — grounded claims only, never speculative fabrication

The rootCause narrative field is constructed with an explicit prompt-template constraint (Section 3.4's structured-output discipline extended to narrative-shaped output specifically): every claim in the narrative must trace to at least one ContributingFactor with a concrete evidenceSource — the model is instructed, and the output is validated (Section 3.5), to never assert a root cause with no corresponding evidence entry. This is a direct, deliberate guard against a known LLM failure mode (confident-sounding but ungrounded causal narratives) that matters more for this capability than almost any other in this volume, since a root-cause explanation is exactly the kind of output a time-pressured engineer (Priya, mid-incident) might act on without independently re-verifying — Part V, Section 17's guardrails treat unsupported causal claims in this capability's output as a specific, named check, not just general output-quality monitoring.

12.5 Retrieval context — the broadest in this volume

Root Cause Analysis draws on all four corpora from Section 4.3 simultaneously — module/action documentation (does this failure pattern match a known limitation or common misconfiguration of the target module), historical flow examples (has a similarly-structured flow had similar issues), historical execution outcomes (has this exact tenant seen this failure signature before, and if so what was it eventually determined to be), and log pattern history (does the surrounding log context, beyond just the failed step itself, suggest a broader system issue) — this breadth is precisely why this capability is the most expensive in this Part and the strongest candidate for on-demand-only invocation (12.3).

12.6 Cross-step and cross-run correlation

correlatedFailures (12.2) is populated by a similarity search (Section 4.6's retrieval pipeline, applied here specifically against the historical-execution-outcomes corpus) identifying other StepResults — potentially from entirely different FlowRuns, even different Flows — that share a likely common cause with the failure under analysis. This is the capability most directly responsible for answering a question no single-run view could answer alone: "is this an isolated failure, or part of a broader pattern" — directly feeding Volume 2's FailureClusterDrilldown component (Section 9.3 there), which was originally specified against Volume 1's DrillDownFailureSignatureQuery (Volume 1, Part II, Section 14.4) using purely statistical/textual signature clustering; this capability offers a richer, causally-reasoned alternative or complement to that purely statistical approach, worth noting as a concrete example of where this volume's AI capability meaningfully extends, rather than merely accelerates, something Volume 1 already specified in simpler form.

12.7 Recommended actions — always advisory, categorized by who acts on them

recommendedActions (12.2) are explicitly categorized (fix-flow vs. fix-target-system vs. adjust-assertion vs. investigate-further) specifically because different categories imply different next actors — a fix-target-system recommendation is information for a team Nexus has no direct relationship with (the tenant's own backend team), while fix-flow/adjust-assertion are directly actionable within Nexus itself (potentially, in a future capability, even pre-populating a suggested flow edit — explicitly out of this volume's GA scope, per Section 1.4's autonomy boundary: this capability recommends, it never constructs or proposes an actual flow modification the way Section 8's Test Generator does for new-flow generation). This categorization is what keeps recommendedActions genuinely useful rather than a generic, one-size-fits-all "investigate this" suggestion.

12.8 Evaluation criteria

Given 12.4's grounding requirement, evaluation specifically measures claim-evidence alignment (does every asserted contributing factor trace to real evidence, verified against a held-out evaluation set with known-correct root causes) as a distinct metric from general narrative quality — directly analogous to Section 8.8's "structural validity versus semantic fidelity" split, applied here as "grounded versus fabricated" instead, and treated as the higher-priority of the two metrics given this capability's specific vulnerability to confident-sounding fabrication (12.4).



13. Report Summarization
13.1 Purpose — serving Persona Sofia specifically

Where Sections 11–12 serve the diagnostic-acceleration need of an engineer investigating a specific failure (Priya, primarily), this capability serves Persona Sofia's distinct need (Volume 0, Section 10.3; UC-5): turning accumulated report and trend data into a digestible narrative suitable for a leadership review, and — per Volume 0's Future Vision, Section 23.3 — eventually supporting natural-language querying of the reporting layer ("why did mobile pass rate drop last week"). This section specifies both the GA-scoped capability (narrative summarization of existing report data) and the architecturally-anticipated-but-not-GA natural-language query extension, following this volume's established practice of clearly separating the two (Section 5.5 already did this for tenant pattern memory).

13.2 Output type — GA scope
typescript
interface ReportSummaryResult {
  summary: string;                      // 2-4 sentence executive summary
  keyFindings: KeyFinding[];
  trendDirection: 'improving' | 'stable' | 'degrading' | 'insufficient-data';
  suggestedFocusAreas: string[];         // e.g., "Mobile module flake rate" — names a
                                            // dimension worth attention, never a specific
                                            // prescribed fix (this capability summarizes
                                            // and highlights; it does not diagnose root
                                            // cause — that remains Section 12's job)
}

interface KeyFinding {
  finding: string;
  supportingMetric: string;              // e.g., "flake rate: 3.2% (up from 1.1%)" —
                                             // every finding traces to an actual number
                                             // from Volume 1's reporting queries, never
                                             // an unsupported qualitative claim
}
13.3 Input — read-only over Volume 1's existing reporting queries

This capability's input is constructed entirely from Volume 1's already-specified reporting query results (Part II, Section 14.4 — GetPassRateTrendQuery, GetFlakeRateQuery, DrillDownFailureSignatureQuery, GetSelfHealAcceptanceRateQuery) over a caller-specified date range — this capability performs no independent data access, it summarizes data Volume 1's existing, already-built reporting infrastructure provides. This is a deliberate, minimal-footprint design: Report Summarization adds a narrative layer on top of Volume 1's reporting system, it does not duplicate or bypass it, directly consistent with Section 1.2's framing that this volume produces outputs consuming Volume 1's existing model rather than introducing parallel data infrastructure.

13.4 Every claim traces to a real number — the same grounding discipline as Section 12

Directly extending Section 12.4's grounding requirement to this capability: every KeyFinding.finding must have a corresponding supportingMetric sourced from the actual query results in 13.3, never a qualitative claim without a number behind it. This matters specifically for this capability's audience — Sofia is explicitly walking into a leadership review with this content (Volume 0, UC-5), and an ungrounded claim discovered as wrong in front of leadership is a direct, serious trust failure for exactly the persona this document set has repeatedly identified as needing a "trustworthy reporting surface" (Volume 2, Section 1.6) above all else.

13.5 Retrieval context — narrower than Root Cause Analysis, historical-trend-focused

This capability's retrieval (Section 4) draws primarily on historical execution outcomes across the requested date range — but unlike Section 12's cross-run correlation search, Report Summarization's retrieval is deliberately time-bounded and aggregate-focused (matching Volume 1, Part V, Section 23.5's TrendAggregateReadModel batch-consistency window) rather than searching for individual similar failures — this capability summarizes patterns over time, not individual incidents, and its retrieval scope is designed accordingly.

13.6 The GA scope stops at structured summarization — natural-language querying is explicitly Future Vision

Consistent with Section 1.7's stated depth philosophy (rigor about constraints, not relaxing them for ambition's sake): this section's GA-scoped capability takes a date range as input and produces a summary — it does not yet accept an arbitrary natural-language question ("why did mobile pass rate drop last week") and resolve it into the appropriate underlying queries dynamically. Volume 0's Future Vision (Section 23.3) named this natural-language-query capability explicitly as a plausible extension; this volume names the architectural seam for it (a query-resolution stage that would sit before Section 13.2's summarization, translating a natural-language question into the specific GetPassRateTrendQuery/GetFlakeRateQuery parameters needed to answer it — architecturally similar in spirit to Section 7's Requirement Parser, but resolving to reporting queries rather than flow steps) without building it now, exactly matching the honesty discipline this document set applied to Volume 2's offline support (Section 16.1 there) and this volume's own tenant pattern memory (Section 5.5).

13.7 Delivery surface

ReportSummaryResult is surfaced in Volume 2's reporting dashboard (Section 6.5 there) as an optional, explicitly-requested addition to the existing PassRateTrendChart/FlakeRateWidget components — never replacing the underlying numeric/chart data those components already render, consistent with UX Principle 1.2's "never a fiction of the real state": the narrative summary is presented alongside the real numbers it was derived from (13.4's grounding requirement made visually concrete), letting Sofia verify the AI's characterization against the actual chart in the same view, never requiring blind trust in the narrative alone.

13.8 Evaluation criteria

Grounding accuracy (every KeyFinding traces to a real, correctly-cited metric, verified automatically since the underlying query results are known ground truth — a stronger, more mechanically-verifiable check than Section 12's evaluation, since this capability's inputs are exact numbers rather than ambiguous log evidence) and, separately, a human-rated usefulness score (would this summary genuinely save Sofia preparation time for a real leadership review, evaluated periodically with actual design-partner feedback per Volume 0, Section 8.4's validation discipline) — the second metric acknowledging that a summary can be perfectly grounded and still not be useful, a distinct failure mode worth tracking independently.


____________________________________________________________________________________________________________________________________________________



14. AI Provider Abstraction
14.1 Purpose — fulfilling Volume 0's locked commitment

Volume 0, Section 18.5 committed to "provider abstraction, not lock-in" as a strategic requirement, and Volume 0, Section 21.7 locked this as a technology decision: "no component outside the AI module's provider-adapter layer should have any provider-specific dependency." This section is where that commitment becomes a concrete interface — the layer Section 2.4's Planning Engine pipeline invokes at step 5 (Invocation).

14.2 The provider interface
typescript
interface AiProvider {
  id: string;                          // e.g., "anthropic", "self-hosted-llama"
  generateStructured<TOutput>(request: StructuredGenerationRequest<TOutput>): Promise<ProviderResponse<TOutput>>;
  embed(text: string): Promise<number[]>;   // Section 4.6's embedding step — same
                                                abstraction covers both generation and
                                                embedding models, never two separate
                                                un-unified provider concepts
  capabilities: ProviderCapabilities;    // what this provider actually supports —
                                             see 14.5
}

interface StructuredGenerationRequest<TOutput> {
  systemPrompt: string;                  // rendered from Section 3's PromptTemplate
  userContent: string;
  outputSchema: JSONSchema;
  maxTokens: number;
  timeout: number;                       // Volume 1 Part VI §38.5's 15s NFR-PERF-4 default,
                                             overridable per Part IV §15's routing policy
}

interface ProviderResponse<TOutput> {
  parsed: TOutput | null;                // null if schema validation failed, Section 3.5
  raw: string;
  tokenUsage: { input: number; output: number };
  latencyMs: number;
}

Every capability engine in Part III interacts with this interface exclusively — no capability engine imports a provider-specific SDK type or references a provider-specific API shape directly, mirroring exactly the discipline Volume 1, Part V, Section 36.3 established for Nexus.Domain's zero-outward-dependency rule, applied here to this volume's capability engines relative to any specific AI vendor.

14.3 Why abstraction lives here, not inside each capability engine

A tempting alternative is letting each capability engine (Part III) call its preferred provider's SDK directly, with abstraction as an optional, later refactor. This volume rejects that design from the start, for a reason directly traceable to Volume 0, Section 18.5's stated rationale: enterprise customers with existing model-provider relationships or compliance constraints (Persona Elena's segment, Volume 0 Section 10.6) must be able to substitute their preferred provider, and retrofitting abstraction after seven capability engines have each grown provider-specific assumptions would be significantly more expensive than establishing the boundary correctly from this volume's first capability-engine specification (Section 7) forward — directly consistent with Volume 1, Architecture Principle 20.2's "contract stability over implementation convenience" applied here at this volume's founding, not as an afterthought.

14.4 Structured output enforcement at the provider-adapter layer

Different providers have different native mechanisms for constrained/structured output (native JSON-schema-constrained generation, function-calling-style tool use, or, for providers lacking native support, prompt-engineered JSON output with post-hoc parsing) — this variance is entirely absorbed inside each provider adapter's generateStructured implementation, never leaking into Section 3's PromptTemplate.outputSchema design or any capability engine's expectations. A capability engine receives either a correctly-typed TOutput or null (14.2), regardless of which underlying mechanism a specific provider used to get there — this is the single most valuable thing this abstraction layer does, since Section 3.5's "no lenient coercion" rule depends on every provider adapter honoring the same strict pass/fail contract uniformly.

14.5 ProviderCapabilities — honest declaration, not assumed uniformity
typescript
interface ProviderCapabilities {
  supportsNativeStructuredOutput: boolean;
  maxContextTokens: number;
  supportsEmbedding: boolean;
  averageLatencyClass: 'fast' | 'standard' | 'slow';
}

Not every provider supports every capability this volume needs (14.2's embed method, for instance, may be unavailable on a provider used only for generation) — ProviderCapabilities is declared explicitly and checked by Part IV, Section 15's routing layer before a provider is selected for a given request, rather than assumed uniform and discovered to be missing only at runtime failure. This is a direct application of Volume 1, Architecture Principle 20.4 (fail loud, fail informatively) to provider selection specifically: a misconfigured routing rule that would send an embedding request to a generation-only provider is caught at routing-decision time, not as a confusing downstream API error.

14.6 Self-hosted / open-weight model support

Per Volume 0, Section 18.5's explicit scoping ("self-hosted/open-weight model support is a Should-priority, not Must-priority, capability for GA, but... adding this support later is a configuration change, not an architecture change"): this interface is designed so a self-hosted model server (exposing an OpenAI-compatible or similarly standard API surface) is just another AiProvider implementation — no different in kind from a commercial hosted provider from the Planning Engine's or any capability engine's perspective. This directly serves the air-gapped, self-hosted deployment scenario Volume 1, Part VI, Section 37.7 already specified at the infrastructure level: an air-gapped Nexus deployment with AI enabled (as opposed to Volume 1's more common AI-disabled air-gapped case) is architecturally supported by pointing this abstraction at a self-hosted provider, requiring zero changes to this volume's capability engines.

14.7 Provider health and circuit-breaking

Each AiProvider is wrapped with a circuit breaker (tracking recent success/failure/timeout rates) — a provider experiencing sustained failures is temporarily excluded from Part IV, Section 15's routing decisions, with automatic recovery probing, rather than every request continuing to attempt and time out against a degraded provider. This directly extends Volume 1, Part VI, Section 38.5's per-call timeout enforcement with a higher-level, cross-request health signal, feeding the same nexus.ai.request.timeout metric Volume 1, Part IV, Section 29.4 already specified, now informing routing decisions in addition to the graceful-degradation behavior Volume 1 already established at the individual-request level.

14.8 Authentication and credential handling

Provider API credentials are resolved through the same ICredentialResolver pattern Volume 1, Part III, Section 18.4 established for module execution — this volume's provider adapters are, in this specific respect, treated identically to a built-in Nexus module accessing a scoped secret, never given broader credential access than the specific provider connection requires, and never logging or persisting a credential value, directly extending Volume 1, Part IV, Section 28.5's sensitive-data logging denylist to this volume's provider-adapter layer explicitly.

14.9 Testing provider adapters

Every AiProvider implementation is tested against a shared contract-test suite (asserting every implementation correctly handles the full StructuredGenerationRequest/ProviderResponse contract, including timeout behavior and malformed-response handling) before being eligible for production routing — directly mirroring Volume 1, Part VI, Section 40.6's testing-tier discipline, applied here to ensure a new provider adapter (added to support a new commercial provider or a self-hosted deployment) cannot silently violate the uniform contract Section 14.2 defines, which every capability engine in Part III depends on holding exactly.



15. Multi-model Routing
15.1 Purpose — the decision layer between capabilities and providers

Section 14 specified the uniform interface every provider implements. This section specifies the policy that decides which provider and which specific model a given request should use — Section 2.4's Planning Engine pipeline step 4, sitting between context retrieval (step 2, prompt assembly step 3) and invocation (step 5).

15.2 Routing dimensions

A routing decision considers four inputs, in order of precedence:

typescript
interface RoutingPolicy {
  resolve(request: AiPlanningRequest): ModelSelection;
}

interface ModelSelection {
  providerId: string;
  modelId: string;
  fallbackChain: { providerId: string; modelId: string }[];   // Section 15.6
}
Tenant configuration (highest precedence) — an enterprise tenant may pin specific capabilities to a specific provider/model (e.g., a compliance requirement mandating a specific certified provider), overriding platform defaults entirely.
Capability-specific default policy — per Section 11.6's example, Failure Analysis defaults to a smaller/faster/cheaper model tier; Test Generator (Section 8) and Root Cause Analysis (Section 12), given their higher output-quality bar and broader context needs, default to a larger model tier.
Provider health (Section 14.7's circuit-breaker state) — a provider currently circuit-broken is excluded from consideration regardless of the above two inputs' preference.
Cost policy (Part IV, Section 16) — where multiple providers/models satisfy the above three inputs at acceptable quality, the lowest-cost option is selected, per Section 16's budget-governance rules.
15.3 Model tiers — a deliberately small, closed set
typescript
type ModelTier = 'lightweight' | 'standard' | 'advanced';

Rather than routing decisions referencing specific model names directly throughout this volume's specification (which would create a maintenance burden every time a provider releases a new model generation), every capability's default policy (15.2, precedence 2) references one of these three tiers, with a separate, independently-updatable mapping table (ModelTier → actual providerId/modelId) maintained as operational configuration, not embedded in this volume's architectural specification — directly mirroring Section 3.7's decision to store prompts as versioned configuration rather than compiled code, applied here to model selection for the same "decouple frequently-changing configuration from infrequently-changing architecture" reasoning.

15.4 Capability-to-tier default mapping
Capability (Part III) Default tier  Rationale
Requirement Parser (§7) Standard  Moderate reasoning demand, structured but not deeply complex output
Test Generator (§8) Advanced  Highest output-quality bar — a structurally invalid or semantically wrong generated flow has real downstream cost
Locator Generator (§9)  Standard  Well-scoped task, benefits from reasoning quality but not the most demanding in this Part
Self-Healing (§10)  (inherits Locator Generator's tier — no independent model call, per §10.1's orchestration-only design)  —
Failure Analysis (§11)  Lightweight High frequency, narrow closed-enum output, explicitly named in §11.6 as a cost-optimization target
Root Cause Analysis (§12) Advanced  Broadest context, highest grounding-accuracy bar (§12.4)
Report Summarization (§13)  Standard  Grounded-numeric-summary task, moderate complexity

This table is the concrete, capability-by-capability resolution of Volume 0, Section 18.3's three-mechanism framework applied to cost/quality tradeoffs specifically — authoring-acceleration and diagnostic-acceleration capabilities that carry higher downstream risk if wrong (Test Generator, Root Cause Analysis) default to the highest-quality tier; the highest-frequency, narrowest-output capability (Failure Analysis) defaults to the most cost-efficient tier, with Part V's evaluation metrics (Section 3.6's evaluation-set discipline) empirically validating that this tier assignment doesn't sacrifice acceptable quality for any specific capability, rather than this table being permanently fixed by initial judgment alone.

15.5 Tenant override granularity

A tenant may override tier assignment per-capability (not only globally) — a tenant especially sensitive to Self-Healing accuracy might upgrade that capability to advanced while accepting lightweight defaults elsewhere, directly serving the same per-tenant risk-tolerance configurability Section 10.4 already established for the self-heal confidence threshold specifically, now generalized as a routing-policy capability spanning every engine in Part III.

15.6 Fallback chains — provider-outage resilience

ModelSelection.fallbackChain (15.2) specifies an ordered list of alternative provider/model pairs to attempt if the primary selection is circuit-broken (Section 14.7) or times out (Volume 1, Part VI, Section 38.5's 15-second budget) — critically, a fallback is only attempted if it satisfies the same ProviderCapabilities requirements (Section 14.5) the primary selection was chosen for, never a lower-capability substitute silently accepted just to return something. If no viable fallback exists, the Planning Engine returns AiUnavailableError (Section 2.5) exactly as if no provider had ever been configured — graceful degradation to the non-AI path, never a degraded-but-still-attempted AI response the calling capability engine's evaluation criteria (Part III, each section's closing subsection) never accounted for.

15.7 Routing decisions are recorded, not just applied

Every ModelSelection actually used is recorded on the resulting AiPlanningResult.modelUsed field (Section 2.3) — this is not incidental logging; it is the specific data Section 3.3's prompt-version correlation and Part V's evaluation metrics depend on to answer "did this specific model/prompt/tier combination perform well," making routing policy changes (15.4's table, or a tenant's override, 15.5) empirically evaluable over time rather than a permanent, unverified assumption.

15.8 Multi-model routing and cost — the direct connection to Section 16

This section's precedence-4 cost consideration (15.2) is deliberately underspecified here and fully elaborated in Section 16 — this section owns which dimensions influence a routing decision and in what order; Section 16 owns the actual budget mechanics (per-tenant spend tracking, alerting, hard limits) that precedence-4 consults. This split mirrors the same "what versus how" separation Volume 2, Section 11 and Section 12 used for the Workflow Designer (product behavior versus React Flow implementation) — routing policy is the product-level decision; cost governance is the infrastructure enforcing it.



16. Cost Optimization
16.1 Purpose — fulfilling Volume 0's cost-governance commitment

Volume 0, Section 18.5 named "cost governance is a first-class product capability, not an operational afterthought," and Volume 0, Section 16.2 scoped "managed AI-provider integration and cost-governance tooling" as an enterprise-licensing-tier capability. This section specifies the mechanics both of those commitments depend on — how AI spend is tracked, budgeted, and optimized, at both the platform-operational level and the tenant-visible level.

16.2 Cost tracking — every invocation, not sampled

Every provider invocation (Section 14.2's generateStructured/embed calls) records exact token usage (ProviderResponse.tokenUsage) and computed cost (token usage × the specific provider/model's known per-token pricing, maintained as operational configuration alongside Section 15.3's tier-to-model mapping) — this is exhaustive, not sampled, tracking, directly because Section 16.4's per-tenant budget enforcement and Section 16.6's cost-attribution reporting both require complete data, not statistically-estimated data, to be trustworthy at the level Persona Elena's and Sofia's review needs (Volume 0, Sections 10.3, 10.6) demand.

typescript
interface AiCostRecord {
  tenantId: TenantId;
  capability: AiPlanningRequest['capability'];
  providerId: string;
  modelId: string;
  tokenUsage: { input: number; output: number };
  computedCost: number;                  // in a normalized currency unit
  timestamp: Date;
  flowRunId?: string;                     // correlates back to Volume 1's FlowRun,
                                              // when the invocation originated from
                                              // one (e.g., a self-heal during execution,
                                              // versus a standalone flow-generation request)
}

This is the same costRecord field already named on AiPlanningResult in Section 2.3 — this section specifies its full shape and what happens to it after the Planning Engine populates it.

16.3 Where cost records are persisted and how they relate to Volume 1's audit trail

AiCostRecords are persisted to their own store (not Volume 1's audit_entries table, Volume 1 Part V Section 32.5, since cost records are high-frequency, append-only operational data with different query patterns — trend aggregation, budget-threshold checking — than the compliance-oriented audit trail), but every AI-assisted command that Volume 1 already audits (Volume 1, Part IV, Section 27's AuditableCommandHandler base class, applied to GenerateFlowFromDescriptionCommand, RequestSelectorHealCommand) additionally references its associated AiCostRecord(s) via flowRunId/session correlation — giving a complete picture (what happened, per Volume 1's audit trail; what it cost, per this volume's cost records) without merging two data stores that Volume 1, Part IV, Section 27.7 already established should remain conceptually and physically distinct (that section's Audit-vs-Logging distinction, extended here to a third, similarly-distinct concern: cost).

16.4 Per-tenant budget enforcement
typescript
interface TenantAiBudget {
  tenantId: TenantId;
  monthlyLimit: number | null;           // null = unlimited (typically self-hosted-with-
                                             // own-provider-key tenants, who bear their own
                                             // provider cost directly, per Volume 0 §16.2's
                                             // "self-hosted users bring their own LLM API key"
  currentSpend: number;                   // rolling, computed from Section 16.2's records
  alertThresholds: number[];              // e.g., [0.5, 0.8, 1.0] — percentage-of-limit
                                             // alerting, not just a hard cutoff at 100%
  onLimitExceeded: 'block' | 'degrade-to-lightweight-tier' | 'alert-only';
}

The onLimitExceeded policy is deliberately tenant-configurable rather than a single platform-wide behavior: 'block' (AI capabilities become unavailable, gracefully degrading per Section 2.5's AiUnavailableError path — never a platform failure, consistent with NFR-REL-3) suits a tenant prioritizing predictable cost; 'degrade-to-lightweight-tier' (Section 15.4's tier assignments are overridden downward, trading quality for continued availability) suits a tenant prioritizing continuity; 'alert-only' suits a tenant that wants visibility without any automatic behavior change. This range of policies is a direct, concrete implementation of Volume 0, Section 18.5's "customers will require visibility and control over it as a condition of adoption at scale."

16.5 Optimization technique 1 — prompt and context caching

Where a capability's retrieval context (Part II) or prompt-template rendering (Section 3) is identical or near-identical across nearby requests (e.g., repeated Failure Analysis calls against similar log patterns in a short window), a short-lived cache (keyed by a hash of the rendered prompt plus retrieval context, scoped strictly per-tenant per Section 4.5's isolation discipline extended here) avoids a redundant provider call entirely — this is a narrow, specific optimization, not a general response-memoization layer, since most capability inputs (a specific broken selector, a specific natural-language description) are unique enough that caching's hit rate would otherwise be low; this technique is deliberately scoped to the capabilities and scenarios (primarily Failure Analysis, Section 11, given its high invocation frequency per 11.6) where repeated near-identical inputs are actually common.

16.6 Optimization technique 2 — context trimming and token budgets

Directly extending Section 4.6's retrieval-pipeline step 5 ("an explicit token budget per capability"): every capability's PromptTemplate (Section 3.2) declares a maximum context-token budget, and the retrieval pipeline trims (via the re-ranking step, Section 4.6 step 4) retrieved context to fit that budget before assembly — rather than sending maximally broad context on every request and letting cost scale unnecessarily with retrieval breadth. This budget is tuned per capability against Section 3.6's evaluation-set discipline specifically: a capability's token budget is reduced only as far as evaluation metrics confirm no meaningful quality regression results, never cut purely for cost savings without that empirical check.

16.7 Optimization technique 3 — tier-appropriate routing as the primary lever

Restating Section 15.4's table as this section's single largest cost lever: routing Failure Analysis (the highest-frequency capability, per Section 11.4's eager-by-default design) to the lightweight tier by default is, in practice, the most impactful cost-optimization decision in this entire volume — a single tier misassignment on a high-frequency capability affects total platform AI spend far more than any prompt-trimming or caching refinement, which is why Section 15.4's table is treated as a cost-critical artifact requiring the same empirical-validation discipline (evaluation sets, Section 3.6) as any other quality-affecting decision, not a one-time judgment call made once and forgotten.

16.8 Cost visibility for enterprise tenants

Directly implementing Volume 0, Section 16.2's "managed AI-provider integration and cost-governance tooling" enterprise capability: a dedicated reporting view (a Volume 2 extension to the reporting-audit feature module, Volume 2 Section 6.5, gated behind the enterprise licensing tier per Volume 0's licensing boundary) surfaces AiCostRecord data aggregated by capability, by tenant-configured budget period, and by trend over time — giving Sofia and Elena's segment the same trustworthy, numbers-grounded reporting experience (directly reusing Section 13's Report Summarization capability, applied reflexively to AI cost data itself) that Volume 0's Business Objective 5 requires for adoption at enterprise scale.

16.9 Cost optimization and quality — the tension this section does not pretend away

Consistent with this document set's established honesty discipline: every optimization technique in this section (caching, trimming, tier-downgrading) carries a real, non-zero quality risk, and this section does not claim otherwise. The governing rule, restated from Section 16.6's specific case and generalized: no cost-optimization technique is applied without first being validated against Section 3.6's evaluation-set discipline — cost and quality are managed as two explicitly-tracked, jointly-optimized dimensions (Part V, Section 19's evaluation metrics track both), never treated as though minimizing cost were free or as though quality could be assumed constant while cost is optimized without verification.


____________________________________________________________________________________________________________________________________________________



17. Guardrails
17.1 Purpose — the section Volume 0 named as Nexus's actual moat

Volume 0, Section 18.6 was explicit: "a unified guardrail strategy across all six modules' AI touchpoints is something a retrofitted competitor structurally cannot easily replicate." This section is where that strategic claim becomes a concrete, specified system — the full detail behind Section 2.4's pipeline steps 1 (input pre-check) and 6 (output validation), which every one of Part III's seven capability engines passes through without exception.

17.2 Guardrail check taxonomy
typescript
interface GuardrailCheck {
  id: string;
  stage: 'input' | 'output';
  category: GuardrailCategory;
  severity: 'block' | 'warn' | 'flag-for-review';
  evaluate(payload: GuardrailPayload): Promise<GuardrailCheckResult>;
}

type GuardrailCategory =
  | 'prompt-injection' | 'pii-detection' | 'content-safety'
  | 'grounding-verification' | 'schema-conformance' | 'tenant-policy';

interface GuardrailCheckResult {
  checkId: string;
  passed: boolean;
  details?: string;
}

Every GuardrailCheckResult produced during a single Planning Engine invocation (Section 2.4) is collected into AiPlanningResult.guardrailChecks (Section 2.3) — populated on every request, every capability, unconditionally, exactly as Section 2.3 originally specified.

17.3 Input-stage checks — cheapest point to enforce a boundary

Referencing Section 2.4's step 1: input-stage checks run before any provider call, catching problems at the point where rejecting a request costs nothing and risks nothing.

Prompt-injection resistance: every piece of retrieved context (Part II) and user-supplied input is scanned for patterns characteristic of instruction-injection attempts (text embedded in, for instance, a target system's page HTML during self-healing, Section 9.3's 'heal' mode, that tries to redirect the model's behavior) — detected content is stripped or the request is flagged for review (per severity, 17.2), never silently passed through unmodified. This matters specifically because Section 9.3's currentPageHtml input is, by definition, content Nexus does not control — it's whatever the tenant's target system happens to render, making this capability's input-stage guardrail the single highest-exposure injection surface in this entire volume.
PII detection: input content is scanned for patterns matching common PII shapes (email addresses, credit-card-like number sequences, government-ID-like patterns) before being included in any prompt sent to an external provider — a detected match triggers redaction (replacing the matched span with a placeholder) rather than blocking the request outright by default, tenant-configurable toward stricter blocking behavior per 17.6.
Tenant policy enforcement: a tenant's configured restrictions (e.g., "never send raw log content to any AI provider," a stricter posture than the platform default) are checked here, before retrieval even completes — directly extending Volume 1, Part IV, Section 26's tenant-isolation discipline into AI-specific policy space.
17.4 Output-stage checks — catching what the model itself produced

Referencing Section 2.4's step 6:

Schema conformance: Section 3.5's structural-validation rule, formalized here as the first and most basic output-stage guardrail — a response failing to parse against outputSchema never proceeds further.
Grounding verification: directly implementing Section 12.4's and Section 13.4's "every claim traces to real evidence" requirement, generalized here as a reusable guardrail check applicable to any capability producing narrative or causal output (currently Root Cause Analysis and Report Summarization specifically, per those sections) — an automated check cross-referencing claimed evidence citations against the actual RetrievalContext (Part II) that was provided, flagging any claim with no traceable source.
Content safety: standard harmful-content screening (the same category of check any responsible AI-product deployment applies) — included here for completeness, though Nexus's capability engines' narrow, structured-output nature (Section 3.4) makes this category of risk meaningfully lower than in an open-ended conversational product, since there's little surface for a capability engine to produce, say, harmful creative content when its output schema is a closed FailureCategory enum or a Flow definition.
17.5 Marketplace guardrail packs — the extension mechanism, made concrete

Directly implementing Section 6.2's third Knowledge Base content type: a guardrail pack is a bundle of additional GuardrailCheck implementations (17.2), reviewed and certified per Volume 0, Section 17.3's tiering (Enterprise-certified specifically, given the elevated trust required to add checks that run against every AI-assisted request for an installing tenant), installed per-tenant exactly like a marketplace module (Volume 1, Part III, Section 18.8's installation flow, structurally mirrored here for guardrail packs rather than automation modules). A healthcare-focused pack, for instance, might add a PHI-specific detection check stricter than Section 17.3's general PII pattern-matching — additive to the platform's baseline checks, never replacing or weakening them, a hard rule enforced by the Planning Engine only ever running the union of platform-baseline checks and installed-pack checks, never allowing an installed pack to disable a baseline check.

17.6 Guardrail configuration — tenant-adjustable severity, never tenant-removable checks

A tenant may adjust a check's severity (e.g., upgrading PII detection, 17.3, from warn to block) but cannot disable a platform-baseline check entirely — this asymmetry (adjustable strictness, non-removable presence) is deliberate: Volume 0's trust posture (Business Objective 5) depends on every tenant's AI-assisted requests passing through the same baseline guardrail floor, regardless of that tenant's own configured tolerance, while still respecting that different tenants have legitimately different strictness needs above that floor.

17.7 Guardrail check failures — what happens next

A block-severity failure at any stage terminates the Planning Engine's pipeline immediately, returning a structured GuardrailBlockedError (a sibling to Section 2.5's AiUnavailableError, handled identically by calling Volume 1 command handlers — falling back to normal, non-AI failure handling). A warn-severity failure allows the pipeline to continue but attaches the warning to AiPlanningResult.guardrailChecks for downstream visibility (surfaced in Volume 2's AI-attribution UI, Volume 2 Section 9.3, alongside the AiAttributionBadge). A flag-for-review failure behaves like warn but additionally creates a queued item in a guardrail-review workflow (Volume 1, Part IV, Section 27's audit-adjacent infrastructure, extended here) for periodic human review of edge cases — this three-tier severity model exists specifically so guardrails can be tuned (moving a check from flag-for-review toward block as confidence in its accuracy grows) without a binary "the guardrail system works or doesn't" all-or-nothing posture.

17.8 Guardrails and Part IV's cost/routing interaction

Referencing Section 15.6's fallback-chain design: a block-severity input-stage failure (17.3) prevents any provider call from being made at all — this is, incidentally, also the cheapest possible cost-optimization outcome (Section 16), a case where guardrail enforcement and cost governance align rather than trade off, worth noting explicitly since Section 16.9 was careful to name where cost and quality/safety do trade off; this is a case where they don't.

17.9 Testing guardrails — adversarial, not just functional

Directly extending Volume 2, Section 19.11's plugin-sandbox adversarial-testing discipline to this volume: every guardrail category (17.2) has a maintained adversarial test suite — deliberately crafted inputs designed to attempt to evade detection (a prompt-injection attempt phrased to avoid known-pattern matching, a PII value formatted to dodge a regex) — run continuously in CI, since a guardrail system's real-world value is entirely a function of how well it holds up against inputs specifically designed to defeat it, not just well-behaved test fixtures that were never going to trigger it in the first place.



18. Security
18.1 Purpose and scope — AI-specific security concerns beyond Section 17's guardrails

Section 17 specified content-level guardrails (what the model receives and produces). This section specifies the surrounding security architecture — data flow, credential handling, and deployment posture — treating AI-specific risks as an extension of, never a replacement for, the security architecture Volume 1, Part IV already established. Every subsection here names the specific Volume 1 mechanism it extends, consistent with this volume's practice throughout (Section 1.2).

18.2 Data flow mapping — what leaves the Nexus deployment boundary, and when

Restating and completing Part I, Section 2.5's trust-boundary analysis specifically for AI: the only data that ever crosses the Nexus-to-LLM-provider trust boundary (Volume 1, Part I, Section 2.5) is what a specific capability engine's RetrievalContext and input payload (Part III, each section's input type) explicitly include — never a broader export of tenant data. This is a direct, auditable claim: Section 14.2's StructuredGenerationRequest shape is exhaustive and typed, meaning "what does Nexus send to an AI provider for capability X" is answerable precisely by reading that capability's input type definition (Part III) plus Section 17.3's redaction behavior, not by inspecting runtime behavior empirically.

Capability  Highest-sensitivity data potentially sent to provider Mitigations
Requirement Parser (§7) User's natural-language description (may reference internal system names) PII detection (§17.3); no target-system credentials ever included
Test Generator (§8) Retrieved historical flow examples (tenant's own prior flow structures) Tenant-isolated retrieval (§4.5) — never another tenant's flows
Locator Generator (§9), 'heal' mode Page HTML from the tenant's target system — the highest-exposure case, per §17.3  Prompt-injection screening (§17.3); this is also the strongest argument for self-hosted-provider support (§14.6) for tenants whose target systems render genuinely sensitive content
Failure Analysis (§11), Root Cause Analysis (§12) Log content, StepResult.Data (may contain response bodies from tenant's own APIs) PII/secret redaction (§17.3); NFR-SEC-3-aligned logging discipline (Volume 1 Part IV §28.5) applied to what's sent, not just what's logged locally
Report Summarization (§13)  Aggregate metrics only — no raw execution data  Lowest-sensitivity capability in this volume by design (§13.3's read-only-over-aggregates scope)

This table exists specifically to make Persona Elena's security review (Volume 0, UC-7) tractable without requiring her to independently reverse-engineer this volume's data flow from source code — it is the direct, AI-specific extension of the data-flow documentation Volume 1, Part I, Section 2.3 already provided at the system-context level.

18.3 Credential and secret handling — restating Section 14.8 with the full security frame

Section 14.8 already established that provider credentials resolve through Volume 1's ICredentialResolver pattern. This section adds the reverse direction, which matters equally: tenant target-system credentials must never be included in any AI provider request. This is enforced structurally, not just by convention — every capability engine's input type (Part III) is defined without any credential-shaped field, meaning a capability engine has no way to accidentally include a secret in a prompt even if a calling Volume 1 command handler mistakenly tried to pass one, since the type system rejects it at the call site, directly extending Volume 1, Part II, Section 10.8's strongly-typed-identifier discipline ("this class of bug is eliminated at compile time") to secret-handling specifically.

18.4 Data residency and the AI-disabled deployment guarantee

Directly extending Volume 1's NFR-SEC-4 ("no mandatory outbound internet dependency for core functionality") and NFR-COMP-1 (full data residency, self-hosted): when a tenant's deployment has the AI module disabled (Volume 1, Part IV, Section 24's AI-optionality architecture), this volume's entire provider-abstraction layer (Section 14) is never invoked — verified the same way Volume 1, Part VI, Section 40.2 statically enforces zero outbound calls in that configuration (a build-time/network-policy check, not merely a runtime conditional that could have a bug). When AI is enabled but the tenant requires strict data residency, Section 14.6's self-hosted-provider support is the concrete mechanism satisfying that requirement — the entire data flow in Section 18.2's table stays within the tenant's own infrastructure boundary, never reaching a third-party commercial provider at all.

18.5 Model and provider security review

Before any new AiProvider implementation (Section 14) is eligible for production routing, it undergoes a security review distinct from Section 14.9's functional contract-test suite — verifying the provider's own data-handling commitments (does the provider retain submitted data for model training, what is the provider's own compliance certification posture) are compatible with Volume 0's Business Objective 5 trust commitments. This review's outcome is recorded and surfaced as part of Section 18.2-style documentation available to Persona Elena's review process — a tenant evaluating whether to enable AI at all is evaluating not just Nexus's own architecture but, transitively, whatever provider Nexus or the tenant has configured, and this volume's documentation is designed to make that transitive trust chain inspectable rather than opaque.

18.6 Prompt-injection defense in depth — beyond Section 17.3's detection

Section 17.3 specified detection (scanning for injection patterns). This section adds the complementary, structural mitigation: every PromptTemplate's system prompt (Section 3.2) is constructed so that retrieved/user-supplied content is clearly delineated from instructions (e.g., wrapped in explicit boundary markers the model is instructed to treat as inert data, never as directives) — a standard, structural prompt-engineering mitigation applied uniformly across every capability's template, reviewed as part of Section 3.6's evaluation-set discipline (an injection-resistance test case is a standard, mandatory entry in every capability's evaluation set, not an optional addition). Detection (17.3) and structural mitigation (this section) are treated as complementary layers, consistent with Volume 1, Part IV, Section 26.3's "defense in depth, not a single checkpoint" philosophy, now applied to prompt-injection resistance specifically.

18.7 Incident response — AI-specific extensions to Volume 1's posture

An AI-specific security incident (a confirmed prompt-injection success, a guardrail bypass, a provider-side data-handling concern discovered post-hoc) follows Volume 1's general incident posture (informed by, though not fully detailed within, Volume 1's own scope) with one AI-specific addition: because AiCostRecord (Section 16.2) and guardrailChecks (Section 17.2) together provide a complete, per-invocation forensic trail (what was sent, what was retrieved, what checks fired, what was returned), incident investigation for an AI-related concern is tractable at the individual-request level, not just in aggregate — a direct, practical payoff of Section 2.3's "every field populated on every call, without exception" design discipline, now shown to matter for security-incident response specifically, not only for the evaluation and cost-governance purposes it was originally justified by.

18.8 Relationship to Section 19

This section's data-flow mapping (18.2) and review process (18.5) establish what should be true; Section 19 (Evaluation Metrics) specifies how this volume continuously verifies, in production, that guardrails (Section 17) and this section's security posture are actually holding — closing the loop from architectural intention to measured, ongoing reality, the same closing move this document set has made at the end of every prior volume's trust-related sections.



19. Evaluation Metrics
19.1 Purpose — the section that closes every open evaluation reference in this volume

Every section in Part III ended with an "Evaluation criteria" subsection; Section 3.6 established the evaluation-set discipline those subsections all depend on; Section 15.7 and Section 16.9 both named evaluation as the mechanism validating routing and cost decisions. This closing section of Volume 3 unifies all of it into one coherent evaluation framework — the system that makes every quality claim in this volume a continuously-measured fact, not a one-time design assertion.

19.2 Two evaluation regimes — offline and online
typescript
interface EvaluationRegime {
  offline: OfflineEvaluation;    // Section 19.3 — pre-release, evaluation-set-based
  online: OnlineEvaluation;      // Section 19.4 — production, real-usage-based
}

This split mirrors a distinction familiar from Volume 1's own testing tiers (Part VI, Section 40.6's fast-unit versus slower-integration split) and Volume 2's testing strategy (Section 19.2 there's four-tier model) — offline evaluation is fast, reproducible, and gates releases (19.3); online evaluation is slower to accumulate but measures what actually happens with real tenant usage, which no offline evaluation set can fully substitute for (19.4).

19.3 Offline evaluation — the release gate

Directly formalizing Section 3.6's evaluation-set discipline as a complete system: every capability engine (Part III) and every guardrail category (Part V, Section 17) maintains a versioned evaluation set — curated inputs with human-annotated acceptance criteria, stored and versioned alongside the PromptTemplates (Section 3.7) and RoutingPolicy configuration (Section 15.3) they evaluate. A release (a new prompt version, a new routing-tier assignment, a new or updated guardrail check) runs the full relevant evaluation set and must meet or exceed the previous version's scores on every named metric before promotion to production traffic — directly mirroring Volume 1, Part VI, Section 38.8's load-testing release gate and Volume 2, Section 17.9's Lighthouse CI performance gate, applied here to AI quality specifically.

Capability  Primary offline metric  Threshold discipline
Requirement Parser (§7) Precision/recall on candidate-module and assertion-intent identification  No regression versus prior version
Test Generator (§8) Structural validity rate + semantic-fidelity score  Structural validity: no regression; semantic fidelity: tracked, reviewed
Locator Generator (§9)  Confidence calibration accuracy (§9.7)  Calibration error must stay within a defined band — this is the strictest offline gate in this volume, per §9.7's own reasoning
Self-Healing (§10)  Self-heal acceptance rate (§10.9) Volume 0's named ≥70% target — release-blocking if a change would predictably drop below it
Failure Analysis (§11)  Category/severity classification accuracy No regression
Root Cause Analysis (§12) Claim-evidence alignment (grounding accuracy, §12.8)  No regression — treated as the higher-priority of its two metrics, per §12.8
Report Summarization (§13)  Grounding accuracy (mechanically verifiable, §13.8) Must remain at or near 100%, given this metric's mechanical verifiability
19.4 Online evaluation — production signal

Continuous, real-usage-derived metrics, computed from the same data every capability engine already produces as a matter of course — no separate instrumentation beyond what Sections 2.3, 10.6, 11.7, and 16.2 already specify:

Self-heal acceptance rate (Section 10.9) — Volume 0's named metric, computed continuously in production, not only against the offline evaluation set.
Structural validity rate (Section 8.8) — the percentage of real, production flow-generation requests producing a structurally valid Flow on the first attempt, tracked as a rolling production metric alongside its offline evaluation-set counterpart.
Human feedback signals — Section 11.7's "was this triage accurate" affordance, and equivalent lightweight feedback mechanisms for other capabilities where a human reviewing AI output can cheaply signal correctness, aggregated as an ongoing production quality signal distinct from, and complementary to, the offline evaluation set's necessarily limited coverage.
Guardrail trigger rates (Section 17.2's GuardrailCheckResult data) — tracked by category and severity over time; a rising flag-for-review rate for a specific check, for instance, might indicate either a genuine increase in adversarial activity or a guardrail that's becoming miscalibrated (too sensitive) as usage patterns shift, requiring investigation either way.
19.5 The relationship between online metrics and offline evaluation sets

Online production data is the primary source for growing offline evaluation sets over time — a production case where a human feedback signal (19.4) indicates a capability got something wrong is a strong candidate for addition to that capability's offline evaluation set (Section 19.3), turning a real, encountered failure mode into a permanent regression check. This is the concrete mechanism by which this volume's evaluation infrastructure improves continuously rather than being fixed at whatever the initial evaluation-set authors anticipated — directly analogous to how Volume 1, Part VI, Section 40's coding standards are themselves expected to evolve via the ADR-for-deviations process (Volume 1, Section 40.8), rather than being permanently frozen at their initial specification.

19.6 Dashboards and visibility

Every metric named in Sections 19.3–19.4 is surfaced on an internal (Nexus-team-facing, not tenant-facing by default) evaluation dashboard, built — consistent with this volume's "eat your own cooking" ethos already demonstrated in Volume 2, Section 19.8's E2E-testing-via-Nexus's-own-Web-module design — using Nexus's own Report Summarization capability (Section 13) applied reflexively to this volume's own evaluation data, alongside standard numeric/trend visualization. Tenant-facing visibility is narrower and more targeted: Section 16.8's enterprise cost-visibility dashboard is joined by an equivalent quality-visibility view for enterprise tenants specifically wanting insight into self-heal acceptance rate and structural validity rate for their own tenant's data specifically — never aggregate cross-tenant figures, per Section 1.4's permanent data-pooling boundary applied here to metrics visibility, not just to underlying training/retrieval data.

19.7 Metric ownership and review cadence

Directly extending Volume 0's risk-register review discipline (Section 22.3 there: reviewed at the close of each Roadmap phase) to this volume's evaluation metrics specifically: every metric in Section 19.3's table has a named owner (the engineer or team responsible for that capability engine) and is reviewed at a minimum monthly cadence — a metric trending toward its threshold (19.3) without yet crossing it is exactly the kind of leading indicator this cadence exists to catch before it becomes a release-blocking regression discovered only at the worst possible moment (mid-release, under time pressure).

19.8 Evaluation metrics and Volume 0's success metrics — the final traceability link

This section closes the specific traceability chain Volume 0, Part I, Section 6 opened at the very start of this entire six-volume project: Volume 0 named self-heal acceptance rate (≥70% target) and flow-generation structural validity rate as headline AI-quality success metrics for the whole product. This volume has now specified, precisely: what these metrics mean (Sections 10.9, 8.8), how they're measured (this section, both offline and online), what infrastructure produces the underlying data (every capability engine in Part III, the Planning Engine in Section 2, the guardrail system in Section 17), and what governs changes that might affect them (Section 19.3's release gate). A metric named in Volume 0's opening pages as a business commitment is, by this volume's closing section, a fully-specified, continuously-measured, release-gated engineering reality — the same "vision through to verified implementation" arc this entire document set has traced at every level, from the whole platform (Volume 0) down to this one, specific, strategically-named number.

19.9 Closing note — Volume 3 complete

This section closes Volume 3 in full: 19 sections across 5 Parts, tracing from AI vision and planning (Part I) through knowledge and context infrastructure (Part II), the seven capability engines that do the actual work (Part III), the provider and cost infrastructure they run on (Part IV), to the guardrails, security posture, and evaluation discipline that make all of it trustworthy (this Part) — directly fulfilling the mandate Volume 0 set for this volume specifically: not raw AI capability as the differentiator, but unified, architected, continuously-verified trust across every AI touchpoint in the platform. Volume 4 (Infrastructure Architecture) now begins from a complete backend (Volume 1), frontend (Volume 2), and AI (Volume 3) specification, turning to how all three are actually deployed, monitored, and operated at scale.


___________________________________________________________________________________________________________________________________________________________________



📘 ADS-004 — Infrastructure Architecture Design

Objective

Define a scalable, observable, secure deployment architecture for enterprise environments.

Chapter 1 – Deployment Strategy

Development

QA

UAT

Production


Chapter 2 – Containers

Backend

Frontend

AI Service

Redis

PostgreSQL

Elasticsearch


Chapter 3 – Kubernetes

Pods

Deployments

Services

Ingress

ConfigMaps

Secrets

Autoscaling


Chapter 4 – CI/CD

GitHub Actions

Build

Test

Security Scan

Docker Build

Deployment

Rollback


Chapter 5 – Monitoring

Prometheus

Grafana

OpenTelemetry

Health Checks

Metrics

Tracing


Chapter 6 – Logging

Centralized logs

Structured logging

Correlation IDs

Retention



Chapter 7 – Security

TLS

Secrets management

Image scanning

RBAC

Network policies



Chapter 8 – Backup & Recovery

Database backups

Artifact storage

Disaster recovery

High availability


____________________________________________________________________________________________________________________________________________________________________


# Volume 4 — Infrastructure Architecture · Part I — Developer Environment & Containerization

## 1. Development Environment

### 1.1 Purpose and the dev/prod parity principle

This volume specifies how the system Volumes 1–3 designed is actually built, deployed, and operated. This opening section specifies the local development environment every engineer works in day to day, governed by one overriding principle worth stating before any tooling detail: **local development should be as structurally similar to the self-hosted production topology as practical**, directly extending Architecture Principle 20.8 (Volume 1, Section 20.8 — "self-hosted is a first-class target, not a downscoped afterthought") to development itself. An engineer's laptop is not a special, permanently-simplified environment with its own divergent architecture — it is the smallest instance of the same topology Volume 1, Part VI, Section 37 already specified.

### 1.2 Local stack — docker-compose as the development topology

```yaml
# docker-compose.dev.yml (abbreviated)
services:
  postgres:
    image: postgres:16
    environment: { POSTGRES_DB: nexus_dev }
    ports: ["5432:5432"]
  redis:
    image: redis:7
    ports: ["6379:6379"]
  nexus-api:
    build: { context: ., dockerfile: src/Nexus.Api/Dockerfile }
    depends_on: [postgres, redis]
    volumes: ["./src:/app/src"]     # hot-reload, Section 1.4
  nexus-orchestration:
    build: { context: ., dockerfile: src/Nexus.Orchestration/Dockerfile }
    depends_on: [postgres, redis]
  nexus-gui:
    build: { context: ./nexus-gui }
    ports: ["3000:3000"]
    volumes: ["./nexus-gui/src:/app/src"]
```

This directly mirrors Volume 1, Part VI, Section 37.3's container inventory — `nexus-api` and `nexus-orchestration` remain separate services even in local development, never collapsed into one process for developer convenience, specifically because Volume 1, Part I, Section 4.4 identified their independent scaling profile as architecturally significant; a developer who only ever runs them combined would never notice a bug that only manifests when they're genuinely separate processes (e.g., an accidental in-process assumption leaking across what should be a container boundary).

### 1.3 AI provider stubbing for offline/low-cost local development

Directly extending Volume 3, Part IV, Section 14.2's `AiProvider` interface: local development uses a stub implementation (`StubAiProvider`) by default, returning deterministic, schema-valid fixture responses for each of Volume 3's seven capabilities (Part III there) — never requiring a real, costed API key for routine local development, and never making a real outbound call by default. This is a direct, practical application of Volume 3's own AI-optionality architecture (Section 1.4 there) to the development workflow itself: an engineer working on, say, Volume 2's Workflow Designer UI should never need an AI provider credential just to run the application locally. A developer actively working on Volume 3's capability engines opts in to a real provider connection via a local environment variable, with Volume 3, Part IV, Section 16's cost-tracking infrastructure equally active in that mode, so even ad hoc developer experimentation is visible in cost reporting, not a blind spot.

### 1.4 Hot reload and inner-loop speed

`nexus-api` and `nexus-gui` (1.2) mount source directories as volumes with framework-native hot-reload (`dotnet watch` for the API, Vite's dev server for the React GUI, Volume 2 Section 4) — `nexus-orchestration`, given its background-worker nature and the incremental-commit execution loop specified in Volume 1, Part III, Section 19.2, restarts on change rather than hot-reloading, since a mid-flow-execution hot-reload would risk exactly the kind of ambiguous, hard-to-reason-about state Volume 1's per-step commit design (Section 16.7 there) was built to avoid — a deliberate, narrow exception to blanket hot-reload, justified by that specific component's execution semantics.

### 1.5 Seed data

A deterministic seed script populates local PostgreSQL with a representative dataset — a handful of `Flow`s spanning multiple modules, a few completed `FlowRun`s with realistic `StepResult` histories (including at least one self-healed step, ensuring `AiAttribution` rendering, Volume 2 Section 9.3, is exercisable locally without a real AI provider call, using Section 1.3's stub), and baseline RBAC roles (Volume 1, Part IV, Section 25.2) — giving every new engineer a working, realistic environment within minutes of first setup, directly serving the same onboarding-speed concern Volume 0's NFR-UX-2 targeted for end users, now applied to engineer onboarding specifically.

### 1.6 TLS in local development

Local development uses locally-trusted, self-signed certificates (via `mkcert` or equivalent) rather than plain HTTP — not because local traffic faces real external threats, but because Section 16 (TLS, later in this volume) specifies TLS as universally required in every environment including self-hosted (per NFR-SEC-1), and a development environment that's structurally different in this specific respect risks an engineer's mental model of "how Nexus handles TLS" being subtly wrong, or a TLS-dependent bug (mixed-content issues, cookie `Secure` flag behavior per Volume 2, Section 14.2) going undetected until a staging or production environment surfaces it.

### 1.7 Developer environment as versioned, reviewed configuration

The full `docker-compose.dev.yml` and its accompanying setup scripts are versioned in the same repository as application code, reviewed via the same pull-request process (Volume 5's eventual responsibility to formalize in full) as any other change — a modification to the local development topology is treated with the same rigor as a change to the production Helm charts (Section 4), since Section 1.1's parity principle only holds if both are kept deliberately, continuously synchronized rather than allowed to drift independently over time.

### 1.8 What local development deliberately does not replicate

Consistent with this document set's practice of stating scope honestly (Volume 2, Section 16.1's model): local development does not replicate Volume 1, Part VI, Section 37.3's horizontal autoscaling, the Redis-backed SignalR backplane's multi-instance behavior (Volume 1, Part V, Section 31.5 — a single local `nexus-api` instance is sufficient for local UI development), or Volume 3, Part IV, Section 14.7's circuit-breaker behavior under real provider degradation. These are explicitly reserved for the staging environment (Section 5, CI/CD, will specify the deployment pipeline that promotes code there) — local development optimizes for fast inner-loop iteration on business logic, not for validating infrastructure-scale behavior, which is a deliberate, different environment's job.



2. Docker
2.1 Purpose — the image-build specification behind Volume 1's container inventory

Volume 1, Part VI, Section 37.2 named the four container images (nexus-api, nexus-orchestration, nexus-cli, nexus-sandbox-runner) at the architectural level. This section specifies how each is actually built — base images, layer strategy, and the security scanning that gates promotion.

2.2 Multi-stage build pattern

Every image follows the same multi-stage structure Volume 1, Part VI, Section 37.2 already committed to in principle ("SDK image for build, runtime-only image for the final artifact"), specified here concretely:

dockerfile
# nexus-api/Dockerfile (representative structure)
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src
COPY src/Nexus.Domain/*.csproj src/Nexus.Domain/
COPY src/Nexus.Application/*.csproj src/Nexus.Application/
COPY src/Nexus.Infrastructure/*.csproj src/Nexus.Infrastructure/
COPY src/Nexus.Api/*.csproj src/Nexus.Api/
RUN dotnet restore src/Nexus.Api/Nexus.Api.csproj    # dependency layer, cached
                                                          # independently of source changes
COPY src/ src/
RUN dotnet publish src/Nexus.Api -c Release -o /app --no-restore

FROM mcr.microsoft.com/dotnet/aspnet:8.0-alpine AS runtime
RUN addgroup -S nexus && adduser -S nexus -G nexus    # non-root, Section 2.5
WORKDIR /app
COPY --from=build /app .
USER nexus
ENTRYPOINT ["dotnet", "Nexus.Api.dll"]

The .csproj-only copy before the full source copy is a deliberate Docker-layer-caching optimization directly serving CI build-time performance (Part II, Section 5's CI/CD pipeline speed) — a source-only change doesn't invalidate the (comparatively slow) dotnet restore layer, since only a dependency change would have changed the .csproj files that layer depends on.

2.3 Base image selection

Alpine-based runtime images (aspnet:8.0-alpine, and for nexus-gui's static-asset-serving image, nginx:alpine) are preferred over full Debian-based images specifically to minimize image size and attack surface — directly serving both Volume 1, Part VI, Section 37.2's "minimal image size and attack surface" goal and this volume's later disaster-recovery concerns (Part V — smaller images mean faster image pulls during a scale-out or recovery event). The one deliberate exception: nexus-sandbox-runner (Volume 1, Part III, Section 18.5's Community-tier module isolation) uses a purpose-built minimal image with an even more restrictive package set than Alpine's default, reviewed specifically for this image's elevated security requirements (running untrusted third-party module code) as part of Section 2.5's scanning discipline.

2.4 Layer strategy for the nexus-orchestration image specifically

nexus-orchestration's image additionally bundles Playwright's browser binaries (Volume 0, Section 21.4's locked Web-module technology) as a distinct, cached layer separate from the application code layer — since browser binaries are large and change far less frequently than application code, this ordering keeps routine application deployments from re-downloading/re-layering multi-hundred-megabyte browser binaries on every release, a meaningful CI/CD and registry-storage cost optimization directly relevant to this Part's later Cost Optimization section (21).

2.5 Image scanning and security gates

Every built image is scanned (Trivy or equivalent) for known CVEs in both OS packages and application dependencies before being eligible for deployment — this scan is a mandatory CI gate (Part II, Section 5), not an advisory report reviewed after the fact, directly extending Volume 1, Part VI, Section 40's "security designed in, not bolted on" discipline (Architecture Principle 20.5) to the container-image layer specifically. Every runtime image runs as a non-root user (2.2's USER nexus directive) with a read-only root filesystem where the application's own design permits it (true for nexus-api; nexus-orchestration requires limited writable scratch space for the Web module's temporary browser-profile data, explicitly scoped and documented as the one exception to this rule).

2.6 Image tagging and registry strategy

Images are tagged with the full Git commit SHA (immutable, traceable to exact source) as the primary tag, with a mutable latest tag reserved strictly for local development convenience (2.2's compose file) and never used in any deployed environment — deploying by SHA-pinned tag is a hard requirement (enforced by Helm chart validation, Section 4), directly serving Volume 1, Part VI, Section 37.8's rollback procedure: a rollback to a specific prior Helm revision must deterministically resolve to the exact same image that was actually running at that revision, which a mutable tag could not guarantee.

2.7 Registry architecture for self-hosted, air-gapped deployments

Directly extending Volume 1, Part VI, Section 37.7's air-gapped installation path: images are published to a public registry for the standard managed/hosted and internet-connected self-hosted cases, with an explicit, documented image-mirroring procedure (docker save/docker load, or a private-registry-sync tool) for air-gapped customers — this section is where that Volume 1-referenced "fully detailed air-gapped installation procedure (image mirroring to a private registry)" is actually specified, closing a forward reference Volume 1 deliberately left to this volume.

2.8 Local build parity with CI builds

The exact same Dockerfiles specified in this section build both local development images (Section 1.2's compose file) and CI/production images — no separate "dev Dockerfile" diverging from what actually ships, directly reinforcing Section 1.1's dev/prod parity principle at the image-build level specifically, and eliminating an entire class of "works locally, breaks in the built image" bug that a divergent local build process would otherwise risk.

2.9 Image build performance budget

Consistent with Volume 1, Part VI, Section 38's performance-as-release-gate discipline, applied here to build infrastructure itself: full CI image builds (all four images, cold cache) are budgeted at under 10 minutes total, with warm-cache incremental builds (the common case, per 2.2's layer-caching design) budgeted under 3 minutes — a build-time regression beyond these budgets is investigated with the same seriousness Volume 1 and Volume 2 both established for their respective performance regressions, since slow CI builds directly degrade every engineer's inner-loop velocity, not just one team's.



3. Kubernetes
3.1 Purpose — deepening Volume 1's reference topology

Volume 1, Part VI, Section 37.3 established the reference Kubernetes topology at the architectural level — which containers exist, how they relate. This section specifies the operational Kubernetes detail beneath that: namespace strategy, resource governance, and cluster-level policies that make the topology actually production-ready, not just architecturally correct.

3.2 Namespace strategy
nexus-system          # nexus-api, nexus-orchestration, outbox relay (Volume 1 Part III §22.2)
nexus-data             # PostgreSQL, Redis (self-hosted deployments only — managed
                          deployments typically use external managed services here instead)
nexus-observability     # Prometheus, Grafana, OpenTelemetry Collector (Section 8-10)
nexus-sandbox           # ephemeral nexus-sandbox-runner pods (Volume 1 Part III §18.5),
                           isolated into its own namespace specifically so its distinct,
                           stricter network and resource policies (3.5) don't need to be
                           reasoned about alongside nexus-system's trusted workloads

For multi-tenant managed/hosted deployments (Volume 1, Part IV, Section 26.1's pooled model), this namespace structure is per-cluster, not per-tenant — tenant isolation is enforced at the application/database layer exactly as Volume 1, Part IV, Section 26.3 specified, not by Kubernetes namespace boundaries, consistent with that section's explicit rejection of database-per-tenant (and, by extension, cluster-or-namespace-per-tenant) as the GA isolation model. For dedicated-instance deployments (Volume 1, Part IV, Section 26.6), this entire namespace structure is deployed once per dedicated cluster, giving that isolation model genuine infrastructure-level separation matching its stronger isolation promise.

3.3 Resource requests and limits

Every container specifies both CPU/memory requests (guaranteed minimum, used for scheduling) and limits (hard ceiling), sized per Volume 1's own performance specification — nexus-orchestration pods are sized with headroom for Volume 1, Part VI, Section 39.3's browser-process-pool resource profile (the Web module's Playwright instances being the dominant memory consumer per that section's resource-pool table), while nexus-api pods are sized for its comparatively lightweight, stateless request-handling profile. Requests are set conservatively (never zero, avoiding the Kubernetes anti-pattern of unbounded best-effort scheduling that risks node-level resource contention) and validated periodically against actual observed usage (Section 7's monitoring stack) rather than set once at initial deployment and never revisited.

3.4 Pod Disruption Budgets and graceful shutdown

Directly implementing Volume 1, Part VI, Section 37.6's graceful-shutdown requirement at the Kubernetes-policy level: a PodDisruptionBudget for nexus-orchestration ensures voluntary disruptions (node draining during a cluster upgrade, for instance) never take down more than a configured fraction of running orchestration pods simultaneously, giving Volume 1's terminationGracePeriodSeconds and in-flight-execution-draining logic (Section 37.6 there) room to actually complete rather than being raced by an aggressive, unbudgeted rolling node replacement.

3.5 Network policies — the sandbox namespace's stricter isolation

Directly implementing Volume 1, Part III, Section 18.5's sandboxed-execution security model at the Kubernetes-network level: nexus-sandbox namespace pods (running potentially-untrusted Community-tier module code) are governed by a default-deny NetworkPolicy, with explicit, narrow allow-rules only for the specific gRPC callback path back to nexus-orchestration (Volume 1, Part III, Section 18.5's communication design) — no sandboxed pod can initiate a connection to nexus-data, the public internet, or any other namespace, directly enforcing at the cluster-network layer the same trust boundary Volume 1, Part I, Section 2.5 established conceptually and Volume 1, Part III, Section 18.3's narrow ExecutionContext enforced at the application layer — this is defense-in-depth's third layer for that specific boundary, network policy joining application-layer scoping and process isolation.

3.6 Ingress and TLS termination

A single Ingress controller (NGINX Ingress or a cloud-provider-native equivalent for managed deployments) routes external traffic to nexus-api (REST and SignalR, Volume 1, Part V, Sections 30–31) with TLS termination at the ingress layer — full detail of the TLS/certificate strategy is Section 16 (TLS) of this Part's later coverage; this section notes only that Ingress-level termination, rather than per-pod TLS, is the chosen architecture, re-encrypting internally only where Section 16's zero-trust posture requires it for the self-hosted regulated-industry deployment profile (Persona Elena's segment, Volume 0 Section 10.6).

3.7 Horizontal Pod Autoscaling — restating and completing Volume 1's design

Volume 1, Part VI, Section 37.4 already specified that nexus-orchestration's HPA scales against the custom nexus.orchestration.concurrent_executions metric (Volume 1, Part IV, Section 29.4) rather than generic CPU/memory. This section adds the concrete Kubernetes mechanics: a PrometheusAdapter (or equivalent custom-metrics-API adapter) exposes that Prometheus-collected metric to Kubernetes' HPA controller, with scale-up/scale-down thresholds and cooldown windows tuned to avoid flapping (rapid, oscillating scale up/down driven by a bursty metric) — a specific, named operational concern this section adds beyond Volume 1's architectural commitment.

3.8 Node pools and workload separation

Where cluster size justifies it (typically larger managed/hosted deployments, per Volume 1, Part IV, Section 26.2's pooled-scale reasoning), nexus-orchestration (compute- and memory-intensive, per 3.3) and nexus-api (lightweight, high-request-rate) run on separate node pools sized appropriately for each workload's profile, with nexus-sandbox on a third, more restrictively-configured pool (smaller instances, since individual sandbox executions are bounded, per Volume 1, Part III, Section 18.5's resource-limit requirement) — a cost-and-performance optimization available to larger deployments, while smaller self-hosted deployments (Architecture Principle 20.8) run everything on a single, undifferentiated node pool without needing this separation to function correctly, only to scale most efficiently.

3.9 Cluster-level RBAC — Kubernetes RBAC, distinct from Volume 1's application RBAC

A clarification worth stating precisely, given Volume 1, Part IV, Section 25 already specified an application-level RBAC model: Kubernetes' own RBAC (governing which humans/service accounts can modify cluster resources — deployments, secrets, network policies) is a distinct, infrastructure-layer permission system, never conflated with Volume 1's Permission enum (Volume 1, Part IV, Section 25.1). A Nexus platform administrator (Volume 1's Administrator role) does not thereby gain Kubernetes cluster-admin access — the two systems are deliberately unconnected, each governing its own layer, consistent with this document set's recurring practice of keeping distinctly-scoped concerns architecturally separate rather than collapsing them for convenience (the same discipline Volume 1, Part IV, Section 27.7 applied to Audit versus Logging, now applied here to application RBAC versus infrastructure RBAC).



4. Helm
4.1 Purpose — the packaging and deployment mechanism

Volume 1, Part VI, Section 37.3 committed to Helm charts as the deployment mechanism for both managed/hosted and self-hosted topologies, from one identical chart set. This section specifies that chart's actual structure and the values-file strategy that lets one chart set serve genuinely different deployment scales and environments without forking.

4.2 Chart structure — umbrella chart over per-component subcharts
nexus-helm/
├── Chart.yaml                    # umbrella chart
├── values.yaml                    # platform-wide defaults
├── values.managed-hosted.yaml      # overrides for Nexus-operated infrastructure
├── values.self-hosted.yaml         # overrides for customer-operated, internet-connected
├── values.air-gapped.yaml          # overrides for Volume 1 §37.7's air-gapped path
├── charts/
│   ├── nexus-api/
│   ├── nexus-orchestration/
│   ├── nexus-sandbox-runner/
│   ├── postgresql/                 # bundled subchart, used only when values specify
│   │                                  self-hosted-without-managed-database (4.4)
│   └── redis/                      # same conditional-inclusion pattern
└── templates/
    ├── networkpolicy.yaml           # Section 3.5
    ├── ingress.yaml                 # Section 3.6
    └── hpa.yaml                     # Section 3.7

The umbrella-chart-with-subcharts structure directly mirrors Volume 1, Part V, Section 36.2's solution structure decision (per-component projects under one solution) — the same organizational philosophy applied one layer up, at the deployment-packaging level, for the same reason: each component (nexus-api, nexus-orchestration, nexus-sandbox-runner) has its own independently-versionable chart, composed by the umbrella chart, rather than one monolithic template set that couples every component's deployment configuration together.

4.3 Values-file hierarchy and precedence

values.yaml establishes safe, sensible defaults; environment-specific files (values.managed-hosted.yaml, etc.) override only what genuinely differs — replica counts, resource sizing (Section 3.3), and which subcharts are enabled (4.4) — never duplicating the full configuration surface per environment. A customer's actual deployment additionally supplies a final, thin values.customer.yaml layer (database connection strings, AI-provider configuration per Volume 3, Part IV, Section 14, license key) — this three-tier layering (platform defaults → environment profile → customer specifics) is what lets Volume 1, Part VI, Section 37.3's "identical chart set, differing only in values files" claim hold precisely, with each tier's role kept distinct and minimal.

4.4 Conditional subchart inclusion — managed database vs. bundled
yaml
# values.self-hosted.yaml (excerpt)
postgresql:
  enabled: true      # bundled subchart deploys PostgreSQL within the cluster
redis:
  enabled: true

# values.managed-hosted.yaml (excerpt)
postgresql:
  enabled: false     # Nexus-operated managed database service used instead;
                        connection details supplied via values.customer.yaml-equivalent
redis:
  enabled: false

This conditional pattern is what allows a resource-constrained self-hosted customer (Architecture Principle 20.8) to helm install a single, complete, self-contained deployment with zero external dependencies beyond the cluster itself, while a larger managed deployment or a self-hosted customer with existing database infrastructure points at external, already-managed PostgreSQL/Redis instances (Volume 0, Section 21.8–21.9's technology choices, deployment-topology-agnostic by design) — the same chart, genuinely different operational posture, purely through values configuration.

4.5 Chart versioning and release correlation

Chart versions follow semantic versioning independent of, but correlated with, the application image tags they reference (Section 2.6's SHA-based tagging) — a chart version bump accompanies any change to the deployment topology itself (a new NetworkPolicy rule, a changed resource default), while an image-only update (a routine application release with no infrastructure change) can reuse the same chart version with only the image tag values changed, keeping the two versioning concerns — "how is this deployed" versus "what code is running" — appropriately decoupled, mirroring the same decoupling Volume 3, Part I, Section 3.7 established between prompt-version releases and Planning Engine code releases.

4.6 Chart testing

Every chart change is validated via helm lint, a helm template dry-run diffed against the previous version (catching unintended resource changes), and a full deployment test against an ephemeral test cluster (spinning up the complete umbrella chart, running Volume 2, Section 19.9's E2E smoke-test subset against it) before being eligible for release — directly extending Volume 1, Part VI, Section 37.5's migration-gates-rollout principle to the chart level generally: infrastructure changes are verified before they reach any real environment, never validated for the first time against staging or, worse, production.

4.7 Self-hosted installation experience — restating Volume 1's procedure with full detail

Directly completing Volume 1, Part VI, Section 37.7's installation summary: helm repo add nexus https://charts.nexus.dev (or, for air-gapped deployments per Section 2.7 of this Part, a locally-mirrored chart repository), then helm install nexus nexus/nexus-helm -f values.self-hosted.yaml -f values.customer.yaml — a single command producing a complete, running deployment, with helm upgrade following the same migration-gated pattern Volume 1, Part VI, Section 37.5 specified for schema changes (the chart's upgrade hooks trigger the migration Job before the application Deployment rolls out the new image version).

4.8 Secrets in Helm values — what never appears in a values file

Directly extending Volume 1, Part IV, Section 24.4 and NFR-SEC-2's secret-handling discipline to the deployment-configuration layer: no values.yaml file, at any tier (4.3), ever contains a raw secret value (database password, AI-provider API key, TLS private key) — these are referenced by name/path against Section 15's Secrets management and Section 15's Vault integration, with Helm's role limited to specifying which secret reference to use, never the secret's actual value, keeping every values file safe to commit to version control (4.9) without exception.

4.9 Chart source control and review

The Helm chart repository is versioned and reviewed with the same rigor as application source code (Section 1.7's principle restated at the deployment-packaging layer specifically) — a chart change is a pull request, reviewed against Section 4.6's automated checks plus human review for anything touching Section 3.5's network policies or Section 3.9's RBAC configuration specifically, those being the highest-security-impact categories of change this chart can express, warranting the same elevated review attention Volume 1, Part VI, Section 40.3 gave to security-sensitive backend code changes.


____________________________________________________________________________________________________________________________________________________



5. CI/CD
5.1 Purpose — the pipeline that consolidates every quality gate this document set has specified

This section is a deliberate synthesis point: across four volumes, this document set has specified more than a dozen distinct quality gates — Volume 1's coding-standard analyzers and load-testing gate, Volume 2's bundle-size budgets and Lighthouse CI checks, Volume 3's evaluation-set release gates, Volume 4's own image scanning and chart testing. None of these are useful in isolation; they only function as a coherent quality bar when assembled into one pipeline every change passes through identically. This section is where that assembly is specified, referencing every prior gate by its originating section rather than re-deriving any of them.

5.2 Pipeline stages
1. Lint & Static Analysis
   - Backend: Roslyn analyzers (Volume 1, Part VI §40.2) — strongly-typed IDs,
     dependency-direction, structured-logging-only rules
   - Frontend: ESLint import-boundary rules (Volume 2, Section 5.3), TypeScript
     strict-mode compilation (Volume 2, Section 4.7)
   - Infrastructure: helm lint (Section 4.6), Dockerfile linting (hadolint)

2. Unit & Component Tests
   - Backend: Nexus.Domain.Tests, Nexus.Application.Tests (Volume 1, Part VI §40.6)
   - Frontend: Vitest unit tier, React Testing Library component tier (Volume 2,
     Section 19.2)
   - AI: prompt template offline evaluation sets (Volume 3, Section 3.6, 19.3)

3. Build & Image Creation
   - dotnet publish, Vite build, Docker multi-stage builds (Section 2.2)
   - Bundle-size budget check (Volume 2, Section 17.2) — build-breaking on regression

4. Security & Vulnerability Scanning
   - Image CVE scanning (Section 2.5)
   - Dependency vulnerability scanning (npm audit / NuGet equivalent)
   - AI guardrail adversarial test suite (Volume 3, Section 17.9)

5. Integration Tests
   - Nexus.Infrastructure.IntegrationTests, Nexus.Api.IntegrationTests against
     ephemeral test-container PostgreSQL/Redis (Volume 1, Part VI §40.6)
   - Frontend integration tier with MSW (Volume 2, Section 19.6)

6. End-to-End Tests
   - Full Playwright/Nexus-self-testing E2E suite (Volume 2, Section 19.8) against
     a fully-deployed ephemeral environment (Section 4.6's test-cluster pattern)

7. Deploy to Staging
   - Migration Job gate (Volume 1, Part VI §37.5) before application rollout
   - Helm chart deployment (Section 4.7)

8. Staging Verification
   - E2E smoke subset (Volume 2, Section 19.9)
   - Load test against NFR-SCALE-1's target (Volume 1, Part VI §38.8)
   - Lighthouse CI performance gate (Volume 2, Section 17.9)
   - AI evaluation-set regression check (Volume 3, Section 19.3)

9. Promote to Production
   - Requires all above stages green, plus explicit approval gate (Section 5.5)

Every stage after stage 1 depends on the previous stage's success — this is a strictly sequential pipeline, not a parallel-everything race to the fastest possible feedback, because several later stages (integration tests, E2E) are expensive enough that running them against a build that hasn't even passed linting would waste real compute cost for no benefit, directly consistent with Volume 3, Section 17.8's "reject cheaply before doing anything expensive" reasoning, applied here to the pipeline's own structure.

5.3 Environments
Environment Purpose Deployment trigger
Local Individual developer inner loop (Section 1) Manual
Ephemeral (per-PR)  Isolated environment per pull request, torn down after merge/close, used for stage 6's E2E run and reviewer manual verification Automatic, per PR
Staging Pre-production verification (stages 7–8), production-topology-equivalent  Automatic, on merge to main
Production  Live customer traffic (managed/hosted)  Manual approval (5.5) after staging verification

Self-hosted customer deployments (Volume 1, Part VI, Section 37.7) are a distinct concept from this internal environment progression — a self-hosted customer's own environment is their production, reached via the release artifact this pipeline produces (a specific, versioned Helm chart + image set), not a stage this pipeline directly deploys to.

5.4 The ephemeral per-PR environment — why it exists

A full, isolated deployment per open pull request (rather than a shared, persistent staging environment alone) directly serves two goals: it lets stage 6's E2E suite (Volume 2, Section 19.8) run against exactly the code under review, with zero risk of interference from a concurrently-merged, unrelated change also deploying to a shared staging environment at the same time — and it gives a human reviewer a genuinely live, clickable environment to manually verify a UI change against, rather than reviewing only static code diffs. This is judged worth its infrastructure cost specifically because Volume 2's UX-quality bar (Section 1, UX Principles) is difficult to fully verify from a code diff alone — seeing the actual rendered Workflow Designer (Volume 2, Section 11) change is a meaningfully better review signal than reading the React code that produces it.

5.5 Production promotion — manual gate, automated everything else

Every stage through 5.2's stage 8 (Staging Verification) is fully automated with no human approval required — a change that passes every gate is, by this pipeline's own definition, ready. Stage 9 (Promote to Production) still requires an explicit human approval click, not because the automated gates are distrusted, but because production promotion timing itself is a business/operational decision (avoiding a deploy immediately before a weekend, coordinating with a customer-communication plan for a significant change) distinct from whether the change is correct — a deliberate, narrow separation of "is this safe to ship" (fully automated) from "is this the right moment to ship it" (human judgment), consistent with Volume 1, Architecture Principle 20.4's fail-fast philosophy not being about removing human judgment entirely, only about removing it from the correctness determination specifically.

5.6 Rollback integration

A failed stage-8 verification, or a post-promotion incident, triggers the exact rollback procedure Volume 1, Part VI, Section 37.8 already specified (helm rollback to the prior revision) — this pipeline's role in rollback is limited to making that prior-revision Helm release readily available (every successful pipeline run's artifacts are retained per Section 20's backup/retention policy, this Part's later coverage) rather than reinventing rollback mechanics this volume already owns elsewhere.

5.7 Pipeline observability — the pipeline monitors itself

Consistent with Volume 1, Part IV, Section 29.1's "the platform observing itself" principle, extended here to development infrastructure: pipeline stage duration, failure rate per stage, and flake rate for the E2E suite specifically (a flaky E2E test undermines trust in the entire gate it belongs to) are themselves tracked metrics, feeding the same OpenTelemetry-based observability stack (Section 8, later in this Part) the running application uses — a pipeline that's slow or unreliable is treated as an infrastructure quality problem worth the same monitoring rigor as the production system it exists to protect.



6. GitHub Actions
6.1 Purpose — the concrete implementation of Section 5's pipeline

Section 5 specified the pipeline's stages and philosophy in tool-agnostic terms. This section specifies the actual GitHub Actions implementation — the technology choice implied by Volume 0, Section 21.14's reference to "GitHub Actions" as effectively locked, formally confirmed and detailed here.

6.2 Workflow structure — composite actions over duplicated YAML
yaml
# .github/workflows/pr-validation.yml (structure, not complete)
name: PR Validation
on: pull_request

jobs:
  lint:
    uses: ./.github/workflows/_reusable-lint.yml
  backend-tests:
    needs: lint
    uses: ./.github/workflows/_reusable-backend-tests.yml
  frontend-tests:
    needs: lint
    uses: ./.github/workflows/_reusable-frontend-tests.yml
  build-images:
    needs: [backend-tests, frontend-tests]
    uses: ./.github/workflows/_reusable-build-images.yml
  security-scan:
    needs: build-images
    uses: ./.github/workflows/_reusable-security-scan.yml
  deploy-ephemeral:
    needs: security-scan
    uses: ./.github/workflows/_reusable-deploy-ephemeral.yml
  e2e:
    needs: deploy-ephemeral
    uses: ./.github/workflows/_reusable-e2e.yml

Every stage from Section 5.2 is implemented as a reusable workflow (_reusable-*.yml), never duplicated inline across the several top-level workflow files that need it (pr-validation.yml, a separate main-branch-deploy.yml for the staging/production promotion path, a scheduled nightly-full-regression.yml running the complete Volume 2/Volume 3 evaluation suites beyond what every-PR feedback-speed budgets allow) — this is the CI-pipeline-level application of the same "define once, reuse everywhere" discipline this document set has applied consistently since Volume 2 (Sections 9.4, 10.6, 12.5 there).

6.3 backend-tests implementation detail
yaml
# _reusable-backend-tests.yml (excerpt)
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres: { image: postgres:16, ports: ["5432:5432"] }
      redis: { image: redis:7, ports: ["6379:6379"] }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '8.0.x' }
      - name: Restore (cached)
        run: dotnet restore
      - name: Domain & Application tests (fast tier)
        run: dotnet test --filter "Category=Unit" --logger trx
      - name: Infrastructure & API integration tests
        run: dotnet test --filter "Category=Integration" --logger trx
        env:
          ConnectionStrings__Default: "Host=localhost;Database=nexus_test;..."

GitHub Actions' native services: block provisions ephemeral PostgreSQL/Redis containers scoped to this job's lifetime, directly serving Volume 1, Part VI, Section 40.6's requirement that integration tests run against real infrastructure, never mocked — the CI environment satisfies this without requiring a persistent shared test database that could develop cross-run state contamination.

6.4 Caching strategy

Dependency caching (actions/cache, keyed by packages.lock.json/package-lock.json hash) covers both dotnet restore and npm install outputs, directly complementing Section 2.2's Docker-layer caching (the two caching layers — CI dependency cache and Docker build cache — serve different parts of the pipeline and are both necessary, not redundant: dependency caching speeds up the test stages that run before any image is built at all). Cache invalidation is automatic and hash-based, never manually managed, avoiding the class of stale-cache bug that manually-keyed caching strategies are prone to.

6.5 Matrix builds — where they're used, and where they're deliberately not

A build matrix runs the frontend test suite (Volume 2, Section 19.2) across the minimum supported browser set for E2E purposes (since Volume 0, Section 21.4 locked Playwright specifically for its multi-browser support, and that capability should be exercised in CI, not just claimed architecturally) — but backend tests do not run a multi-OS or multi-.NET-version matrix, since Volume 0, Section 21.2 locked a single .NET version and Linux-container-only deployment target (Section 2.3's Alpine base images), making a broader compatibility matrix pure CI cost with no corresponding product requirement it would actually validate.

6.6 Secrets management in CI — Vault integration, never GitHub Secrets alone for production credentials

Directly extending Section 4.8's "no raw secret in a values file" discipline to the pipeline itself: CI-time secrets needed only within the pipeline's own ephemeral execution (a test database password scoped to that single job run, per 6.3) use native GitHub Actions encrypted secrets, appropriate for their narrow, ephemeral scope. Secrets needed for actual deployment (Section 5.2 stage 7's staging deployment, and production promotion) are retrieved at deploy-time directly from Section 15's Vault infrastructure via a short-lived, workflow-scoped OIDC token (GitHub's native OIDC federation with Vault, avoiding any long-lived Vault credential ever being stored as a GitHub secret at all) — this is a deliberate, stronger posture than simply storing a Vault token as a GitHub secret, since a short-lived, workflow-run-scoped credential minimizes the blast radius of any single compromised workflow run.

6.7 Required status checks and branch protection

The main branch requires every job in Section 6.2's pr-validation.yml to pass before merge is permitted (GitHub's native branch-protection required-status-checks feature), with no administrator override path enabled — directly enforcing that Section 5's "every stage automated, human judgment reserved only for production-promotion-timing" philosophy (Section 5.5) cannot be quietly bypassed under time pressure, which is exactly the scenario such a bypass exists to be tempting during and exactly the scenario this document set's repeated "fail loud, never silently skip a check" discipline (Volume 1, Architecture Principle 20.4, applied consistently through every subsequent volume) argues most strongly against permitting.

6.8 Workflow-run cost and the nightly/PR split

Consistent with Volume 2, Section 19.9's smoke/full E2E split and Volume 3, Section 16's cost-optimization discipline, applied here to CI compute cost specifically: every-PR workflows run Volume 2's E2E smoke subset and a representative (not exhaustive) slice of Volume 3's evaluation sets, budgeted to keep PR feedback latency within a target window (e.g., under 20 minutes total, directly serving developer inner-loop velocity per Section 2.9's build-time-budget reasoning extended to the full pipeline); the complete E2E suite (every Volume 0 use case, Section 11) and full evaluation-set regression run on a nightly schedule against the current main branch, catching anything the necessarily-narrower PR-time checks didn't, with any nightly failure treated as a release-blocking incident requiring immediate triage, not a routine report reviewed at leisure.

6.9 Closing note for this Part

Sections 5 and 6 together complete the pipeline that enforces every quality standard this document set has specified across four volumes — from Volume 1's coding-standard analyzers through Volume 3's AI evaluation gates to this Part's own infrastructure checks. The next Part (Observability Stack) specifies how the running system — not just the pipeline that ships it — is continuously monitored once deployed.


____________________________________________________________________________________________________________________________________________________




7. Monitoring
7.1 Purpose — the strategy layer above Sections 8–11's tooling

Volume 1, Part IV, Section 29 already established observability's three pillars (logs, metrics, traces) and Volume 0, Section 21.11 locked the OpenTelemetry/Prometheus/Grafana stack. This section specifies the monitoring strategy those tools serve — SLIs/SLOs derived directly from the NFRs this document set has specified since Volume 0, and the alerting/escalation philosophy governing how a threshold breach becomes human attention. Sections 8–11 then specify the concrete tooling implementing this strategy.

7.2 Service Level Indicators — derived, not invented

Every SLI this section defines is a direct restatement of an NFR already committed to in Volume 0 or Volume 1 — this section adds no new quality targets, only operationalizes existing ones as continuously-monitored indicators:

SLI Derived from  Measurement
API availability  NFR-REL-1 (99.9%) /health/live synthetic checks + real request success rate
Step-dispatch latency (p95) NFR-PERF-1 (50ms) nexus.step.dispatch.duration (Volume 1, Part IV §29.4)
Platform flake rate NFR-REL-2 (<0.5%) nexus.flow.run.flake_detected / nexus.flow.run.completed ratio (Volume 1, Part IV §29.5)
Concurrent execution headroom NFR-SCALE-1 (500) nexus.orchestration.concurrent_executions against configured capacity
Frontend Core Web Vitals  NFR-UX-2 (30-min onboarding, indirectly)  LCP/INP/CLS via RUM (Volume 2, Section 17.7)
AI request latency  NFR-PERF-4 (15s timeout)  nexus.ai.request.duration (Volume 1, Part IV §29.4)
Self-heal acceptance rate Volume 0 §6.2 (≥70%)  Volume 3, Section 10.9's computed metric
7.3 Service Level Objectives — the threshold that triggers action

Each SLI in 7.2 has a corresponding SLO — the specific threshold and time window that, when breached, constitutes an incident. SLOs are set with deliberate headroom below the NFR's own hard commitment (e.g., an internal SLO alerting at p95 dispatch latency exceeding 40ms, before NFR-PERF-1's 50ms commitment is actually violated) — directly extending Volume 1, Part IV, Section 29.6's "alert on symptom metrics before they become customer-visible" philosophy into a formal, numeric early-warning margin, so the team has time to respond before a customer-facing NFR is actually breached, not only after.

7.4 Error budgets

Each SLO (7.3) implies an error budget — the amount of acceptable deviation within a rolling window before it's treated as fully consumed. Consistent with Volume 1, Part VI, Section 38.9's "performance regression treated with the same severity as a functional bug" principle, an exhausted error budget for any SLO triggers a deliberate, visible policy: new feature deployment for the affected component pauses (not a hard technical block, but a required, explicit decision to proceed anyway, made visible to the same stakeholders Volume 0's risk register, Section 22, already involves) until the budget recovers or the underlying regression is fixed — directly operationalizing the tension between shipping velocity and reliability commitments as a conscious, tracked tradeoff rather than an implicit one.

7.5 Alerting severity and escalation
Critical  → pages on-call immediately (NFR-REL-1 breach in progress, security
             incident per Volume 3 §18.7, data-loss risk)
Warning   → notifies the responsible team's channel, next-business-day triage
             acceptable (SLO trending toward breach, per 7.3's early-warning margin)
Info      → dashboard-visible only, no notification (routine capacity/usage trends)

This three-tier severity model directly mirrors Volume 3, Section 17.7's guardrail-severity model (block/warn/flag-for-review) — the same "not every deviation deserves the same urgency" principle, applied here to operational alerting rather than AI guardrails, a deliberate consistency choice across this document set's various severity-tiering decisions (Volume 1, Part IV, Section 24.8's authentication-failure handling used a similar graduated-response philosophy).

7.6 On-call and ownership

Every SLI in 7.2's table has a named owning team (mirroring Volume 3, Section 19.7's per-metric ownership model exactly) — the Orchestration Engine's dispatch-latency SLI is owned by the backend team that owns Volume 1, Part III's execution engine; the AI-latency SLI is owned by the team owning Volume 3's Planning Engine; frontend Core Web Vitals are owned by the Volume 2 frontend team. A Critical alert (7.5) pages the specific owning team first, with a documented escalation path to a broader on-call rotation only if the primary owner doesn't acknowledge within a defined window — avoiding the common anti-pattern of a single, undifferentiated on-call rotation that lacks the specific context Volume 1 through Volume 3's deep, specialized architecture actually requires to diagnose quickly.

7.7 Runbooks — connecting an alert to a specific, known response

Every Critical and Warning alert links to a maintained runbook — for the platform flake-rate SLI specifically, for instance, a runbook referencing Volume 1, Part III, Section 19.5's retry/flake-handling design and Section 22's Event Bus health metrics, giving the responding engineer a starting investigation path grounded in this document set's own architecture rather than requiring them to rediscover it under incident pressure. Runbooks are versioned alongside the code they describe (mirroring Volume 1, Part VI, Section 40.7's documentation-as-code discipline) and are a required deliverable for any new SLI added to 7.2's table — an alert with no runbook is treated as an incomplete monitoring implementation, not shipped as-is.

7.8 Monitoring the monitoring — meta-observability

Directly extending Volume 4, Section 5.7's "the pipeline monitors itself" principle one layer further: the observability stack's own health (Prometheus scrape success rate, OpenTelemetry Collector queue depth, Grafana dashboard load latency — Sections 8–10's tooling) is itself monitored, with a dedicated, minimal, independently-hosted "is monitoring itself healthy" check that doesn't depend on the same infrastructure it's monitoring — avoiding the specific failure mode where an observability-stack outage is invisible precisely because the tool that would normally surface it is the thing that's down.



8. OpenTelemetry
8.1 Purpose — the unified collection layer

Volume 1, Part IV, Section 29.2 committed to OpenTelemetry as the unified instrumentation layer for logs, metrics, and traces. This section specifies the Collector architecture that actually receives, processes, and routes that telemetry — the infrastructure component sitting between every instrumented source (Volume 1's backend, Volume 2's frontend RUM, Volume 4's own CI pipeline) and the storage backends Sections 9–11 specify.

8.2 Collector architecture — gateway pattern
┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ nexus-api      │  │ nexus-        │  │ nexus-gui       │  │ CI pipeline    │
│ (Volume 1)     │  │ orchestration  │  │ (Volume 2, RUM) │  │ (Volume 4 §5.7)│
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │ OTLP/gRPC        │ OTLP/gRPC        │ OTLP/HTTP        │ OTLP/HTTP
       └──────────────────┴─────────┬───────┴──────────────────┘
                                     ▼
                        ┌─────────────────────────┐
                        │  OTel Collector (Gateway)  │
                        │  - Batch processor          │
                        │  - Tail sampling (8.5)       │
                        │  - PII/secret scrubbing (8.6)│
                        │  - Resource attribution       │
                        └───┬──────────┬──────────┬───┘
                            │          │          │
                     traces │  metrics │      logs │
                            ▼          ▼          ▼
                     [Section 9:  [Section 9:  [Section 11:
                      trace store  Prometheus]  Elasticsearch]
                      or Prometheus-
                      exemplar linked]

Every application component (Volume 1's backend, Volume 2's frontend, and — per Section 5.7 of this volume — the CI pipeline itself) exports via the standard OTLP protocol to this single Collector gateway, rather than each source shipping directly to Prometheus/Elasticsearch independently — this gateway pattern is what allows Section 8.5's sampling and Section 8.6's scrubbing to apply uniformly, exactly once, regardless of which of the many instrumented sources produced a given piece of telemetry.

8.3 Instrumentation libraries — restating and completing Volume 1's setup

Directly implementing the AddOpenTelemetry() configuration Volume 1, Part IV, Section 29.2 sketched: the ASP.NET Core and Entity Framework Core auto-instrumentation packages cover the majority of nexus-api's baseline tracing (HTTP request spans, database query spans) automatically; Volume 1, Part I, Section 5's custom ActivitySource ("Nexus.Orchestration") and Meter provide the domain-specific spans and metrics (per-FlowRun trace structure, per Volume 1, Part IV, Section 29.3) that no auto-instrumentation could produce, since they require semantic knowledge of Nexus's own domain model that a generic library instrumentation cannot have.

8.4 Frontend RUM ingestion

Volume 2, Section 17.7 specified that Core Web Vitals are "exported through the same OpenTelemetry pipeline Volume 1... established" — this section specifies the mechanism: the browser-based OpenTelemetry JS SDK exports via OTLP/HTTP (browsers cannot use gRPC directly) to this same Collector gateway (8.2), with a dedicated, rate-limited ingestion path distinct from the backend's OTLP/gRPC path, since browser-originated telemetry has a fundamentally different trust and volume profile (many more, smaller, less-trusted sources) than server-originated telemetry — the Collector's configuration treats these as two distinct pipelines converging on the same processing stages (8.2's batch/sampling/scrubbing), not one undifferentiated input.

8.5 Sampling strategy

Full-fidelity trace retention for every request is neither necessary nor economical at scale — a tail-based sampling policy (sampling decisions made after a trace completes, based on its actual characteristics) retains 100% of traces exhibiting error status, unusually high latency (beyond the SLO thresholds Section 7.3 defines), or any AI-capability involvement (Volume 3's capability engines, given their standing evaluation and forensic-trail value per Volume 3, Section 18.7), while sampling routine, fast, successful traces at a lower rate (e.g., 5-10%) — directly consistent with Volume 1, Part IV, Section 29.5's flake-rate computation needing complete data (flake-relevant traces are, by definition, non-routine and therefore always retained under this policy) while avoiding the cost of exhaustively storing every uneventful, successful request trace.

8.6 PII and secret scrubbing at the Collector

Directly extending Volume 1, Part IV, Section 28.5's "never logged, even at Trace level" denylist and Volume 3, Section 17.3's input-stage PII detection to the telemetry-collection layer specifically: the Collector applies a scrubbing processor (pattern-matching the same categories Volume 1, Section 28.5 and Volume 3, Section 17.3 already define) to every span attribute and log record before export to any storage backend — a defense-in-depth layer catching anything that might have slipped past application-level discipline, directly consistent with Volume 1, Part IV, Section 26.3's "defense in depth, not a single checkpoint" philosophy, now applied to telemetry scrubbing specifically as a third enforcement point beyond the two application-level ones already specified.

8.7 Resource attribution and multi-tenancy

Every span, metric, and log record is tagged with resource attributes including tenant_id where applicable (Volume 1, Part IV, Section 29.8's tenant-scoped observability requirement), service.name (distinguishing nexus-api from nexus-orchestration from nexus-sandbox-runner), and deployment.environment (local/staging/production, Section 5.3) — this consistent tagging is what makes Section 9's Prometheus queries and Section 10's Grafana dashboards able to filter and aggregate correctly across every one of this document set's cross-cutting dimensions without each dashboard needing bespoke, inconsistent labeling logic.

8.8 Collector deployment topology

The Collector gateway runs as its own Kubernetes Deployment within the nexus-observability namespace (Section 3.2), horizontally scaled independently of nexus-api/nexus-orchestration — a Collector outage or backlog must never block or slow the primary application (telemetry export is fire-and-forget from the application's perspective, buffered and retried by the OTel SDK's own export queue, never a synchronous dependency of request handling), directly consistent with Volume 1, Part I, Section 4.5's original design note that "a telemetry-export failure must never cause a flow-execution failure."

8.9 Relationship to Sections 9–11

This section specifies collection and routing; Sections 9 (Prometheus), 10 (Grafana), and 11 (Elasticsearch) specify what happens to the three telemetry types after this Collector routes them to their respective storage and visualization backends.



9. Prometheus
9.1 Purpose — metrics storage and the HPA feedback loop

This section specifies Prometheus's role as the metrics-storage backend receiving the Collector's metric export (Section 8.2), and — critically, since this is the one piece of the observability stack that feeds back into the running system's own behavior, not just human-facing dashboards — the custom-metrics-to-HPA pipeline Volume 1, Part VI, Section 37.4 and Volume 4, Section 3.7 both already referenced.

9.2 Metrics ingestion — remote-write from the Collector, not direct scraping

Rather than Prometheus directly scraping each application pod (the more traditional Prometheus pattern), the OpenTelemetry Collector (Section 8.2) exports metrics to Prometheus via remote-write — this is a deliberate choice consistent with Section 8.2's gateway architecture: every metric passes through the Collector's uniform processing (batching, resource attribution per Section 8.7) before reaching storage, rather than Prometheus scraping raw, unprocessed metrics directly from application pods and bypassing that processing layer entirely.

9.3 Retention and downsampling

Raw-resolution metrics are retained for 15 days (sufficient for the incident-investigation and SLO-tracking windows Section 7.3–7.4 specify), with downsampled (5-minute-aggregate) data retained for 13 months — directly enabling Volume 0's Success Metrics (Part I, Section 6) to be tracked as genuine year-over-year trends, not just recent-window snapshots, while keeping raw-resolution storage cost bounded. This tiered retention directly mirrors the same short-hot/long-cold pattern Volume 1, Part IV, Section 28.6 established for operational logs, applied here to metrics for the analogous cost/utility reasoning.

9.4 The custom-metrics-to-HPA pipeline — completing Volume 1 and Volume 4's forward references
nexus-orchestration → OTel SDK → Collector (Section 8) → Prometheus (remote-write)
                                                                    │
                                                                    ▼
                                                        prometheus-adapter
                                                     (implements Kubernetes
                                                      custom.metrics.k8s.io API)
                                                                    │
                                                                    ▼
                                                        HPA controller reads
                                                        nexus.orchestration.
                                                        concurrent_executions
                                                                    │
                                                                    ▼
                                                        scales nexus-orchestration
                                                        Deployment replica count

This is the concrete, final implementation of the mechanism Volume 1, Part VI, Section 37.4 named ("a PrometheusAdapter... exposes that Prometheus-collected metric to Kubernetes' HPA controller") and Volume 4, Section 3.7 restated — prometheus-adapter's configuration maps the specific nexus.orchestration.concurrent_executions metric (Volume 1, Part IV, Section 29.4) to the Kubernetes custom-metrics API surface the HPA controller queries, with the scale-up/down thresholds and cooldown windows Section 3.7 already flagged as needing tuning to avoid flapping specified as adapter configuration, not application code — keeping this autoscaling policy's tuning fully within infrastructure-team ownership (Section 7.6's ownership model), separate from the Volume 1 backend team's application-code concerns.

9.5 Recording rules — pre-computing the SLIs

Every SLI in Section 7.2's table that requires a computed ratio (platform flake rate — a ratio of two counters, per Volume 1, Part IV, Section 29.5) is defined as a Prometheus recording rule, pre-computing the ratio on a regular interval rather than requiring every dashboard query (Section 10) or alert rule (Section 7.5) to independently recompute it from raw counters — this is both a query-performance optimization and a correctness safeguard: the flake-rate formula is defined exactly once, as a recording rule, and every consumer (dashboards, alerts) references that single, authoritative computed series, eliminating the risk of two different dashboards computing "flake rate" with subtly different, drifted formulas.

9.6 Alert rules and Alertmanager

Prometheus's rule evaluation engine, paired with Alertmanager for routing and deduplication, implements Section 7.5's three-tier severity model directly — a Critical-severity alert rule (e.g., API availability SLO breach) routes to the paging integration for the owning team (Section 7.6); Warning-severity rules route to team chat channels; Info-level conditions are deliberately not alert rules at all, remaining dashboard-only (Section 10) per Section 7.5's explicit "no notification" design for that tier. Alertmanager's grouping and inhibition rules prevent alert storms during a genuine incident (e.g., suppressing redundant per-pod alerts when the underlying cause is a single, already-alerted cluster-level issue) — a specific, deliberate mechanism protecting the on-call escalation model (Section 7.6) from being undermined by notification noise during exactly the moments focus matters most.

9.7 Multi-cluster considerations — federation, scoped to actual need

For the dedicated-instance deployment model (Volume 1, Part IV, Section 26.6), each customer's cluster runs its own independent Prometheus instance, with no cross-cluster federation to any Nexus-operated central instance by default — directly consistent with that deployment model's stronger isolation promise and Architecture Principle 20.8's self-hosted-first posture; a dedicated-instance customer's metrics remain within their own infrastructure boundary exactly as their data does (Volume 1, Part IV, Section 26.6). For Nexus's own managed/hosted pooled deployment (Volume 1, Part IV, Section 26.2), a limited, specifically-scoped federation aggregates only the platform-health metrics Volume 3, Section 19.6 already named as Nexus-team-facing (never tenant-specific data) into a central operational view — federation is applied narrowly, for a named purpose, not as a default "aggregate everything centrally" posture.

9.8 Query performance and cardinality management

High-cardinality labels (a raw flow_run_id as a metric label, for instance) are explicitly avoided in Prometheus metric definitions — Volume 1, Part IV, Section 29's custom metrics (nexus.step.dispatch.duration, etc.) are labeled only by bounded-cardinality dimensions (ModuleKind, RunStatus, TenantId for aggregate reporting per Section 9.7's isolation note), with per-run detail available instead through Section 8.5's trace data, where fine-grained, high-cardinality identification is the entire point and traces are architecturally suited to carrying it — a deliberate division of labor between metrics (aggregate, low-cardinality, cheap to query broadly) and traces (individual, high-cardinality, queried by specific ID) that this section maintains discipline around rather than letting metrics cardinality creep upward over time as new dimensions seem convenient to add.



10. Grafana
10.1 Purpose — the visualization layer, internal and tenant-facing

This section specifies Grafana's dashboard architecture — both the internal, Nexus-team-facing operational dashboards implementing Section 7's SLI/SLO monitoring, and the narrower tenant-facing views this document set has referenced at several points (Volume 3, Section 16.8's cost visibility, Section 19.6's quality visibility) without yet specifying their common technical foundation.

10.2 Dashboard-as-code

Every dashboard is defined as versioned JSON (Grafana's native dashboard-as-code format) stored in the same repository as the application code it monitors, provisioned automatically on deployment (Section 4's Helm chart includes a Grafana provisioning subchart) rather than manually clicked-together in the Grafana UI and never version-controlled — directly extending Volume 1, Part VI, Section 40.7's documentation-as-code discipline to observability dashboards specifically: a dashboard that visualizes a specific metric is treated as a first-class artifact tied to that metric's definition, reviewed via the same pull-request process as the metric-emitting code itself, never a separate, informally-maintained side artifact that can silently drift out of sync with what it's supposed to represent.

10.3 Dashboard inventory — internal
Dashboard Audience  Content
Platform Health On-call, all engineering teams  Section 7.2's full SLI table, current SLO status (7.3), error-budget consumption (7.4)
Orchestration Engine Deep-Dive  Backend team (Volume 1, Part III owners)  Step-dispatch latency breakdown by module (Volume 1, Part IV §29.4), concurrent-execution headroom, outbox relay lag
AI Capability Health  AI team (Volume 3 owners) Per-capability latency and evaluation metrics (Volume 3, Section 19.6's dashboard, technically hosted here), guardrail trigger rates (Volume 3, Section 17.2)
Frontend RUM  Frontend team (Volume 2 owners) Core Web Vitals trends (Volume 2, Section 17.7), bundle-size trend over releases
CI/CD Pipeline Health All teams Stage duration and failure rate (Section 5.7), E2E flake rate
Cost Overview Engineering leadership  Infrastructure cost (Section 21) alongside AI provider cost (Volume 3, Section 16.2), correlated
10.4 Dashboard inventory — tenant-facing (enterprise tier)

Directly implementing Volume 3, Section 16.8 and Section 19.6's previously-named-but-unspecified tenant-facing views: a scoped Grafana instance (or Grafana's native organization/folder-permission isolation, configured per-tenant) exposes exactly two dashboards to enterprise-tier tenants — AI Cost (Volume 3, Section 16.8) and AI Quality (Volume 3, Section 19.6) — both filtered, at the data-source query level, to that tenant's own TenantId exclusively, using the identical tenant-scoped-query enforcement Volume 1, Part IV, Section 26.3 established for the application's own data layer, now applied to the observability layer for the first time with a genuinely tenant-facing (not just internal) consumer. This is the concrete technical mechanism closing the gap those two Volume 3 sections named but left unspecified — resolved here, in this volume, exactly where the rest of this document set's infrastructure specification lives.

10.5 Grafana Alerting vs. Prometheus Alertmanager — division of responsibility

Consistent with Section 9.6's Alertmanager-based routing already specified: Grafana's own native alerting feature is used narrowly, only for alert conditions that are more naturally expressed against a dashboard visualization than a raw Prometheus query (a visual anomaly-detection rule against the Frontend RUM dashboard's trend shape, for instance) — the primary, authoritative alerting path for every SLI in Section 7.2's table remains Prometheus rules plus Alertmanager (Section 9.6), avoiding two independent, potentially-inconsistent alerting systems both claiming ownership of the same underlying SLOs.

10.6 Access control

Internal dashboards (10.3) are gated by the observability stack's own authentication (SSO-integrated, reusing the same OIDC provider integration Volume 1, Part IV, Section 24.7 established for the application itself, rather than a separate credential set engineers must manage) with role-based folder permissions matching Section 7.6's per-SLI ownership model — a team's dashboards are editable by that team, viewable platform-wide. Tenant-facing dashboards (10.4) are embedded within Volume 2's reporting-audit feature module (Volume 2, Section 6.5) via Grafana's embedding/iframe capability, authenticated through the same session Volume 2, Section 14 already establishes for the rest of the application — a tenant user never has separate Grafana credentials to manage, consistent with UX Principle 1.2's "one consistent, real state" applied to authentication specifically.

10.7 Dashboard performance and the query-cost discipline

Every dashboard panel's underlying query is reviewed against Section 9.8's cardinality-management discipline before merge (10.2's PR-review process) — a dashboard panel that would require a high-cardinality, expensive Prometheus query is redesigned to query a recording rule (Section 9.5) instead, keeping dashboard load time itself within a reasonable budget (directly relevant to Section 7.8's meta-observability concern: a slow-loading Platform Health dashboard during an active incident is itself a monitoring-system failure worth preventing proactively).

10.8 Annotations — correlating deployments with metric changes

Every production deployment (Section 5.2, stage 9) automatically creates a Grafana annotation marking that exact timestamp across every dashboard — directly serving incident investigation and the general "did this recent change cause this metric shift" question that's otherwise surprisingly hard to answer precisely without this correlation being automatic rather than requiring an engineer to manually cross-reference a deployment log against a metric timestamp by hand.



11. Elasticsearch
11.1 Purpose and precise scope — operational logs only

A scope clarification worth stating immediately, given how many times this document set has had to disambiguate the word "log": this section covers storage for Volume 1, Part IV, Section 28's operational logging exclusively — the diagnostic output the Nexus application produces about its own behavior. It has no relationship to the customer-facing Logs module (Volume 0, Section 14.8; Volume 1, Part III, Section 18), which reads a target system's logs as a product capability, and no relationship to Volume 1, Part IV, Section 27's Audit trail, which is stored in PostgreSQL's audit_entries table specifically because it needs the immutability and access-control rigor Volume 1, Part IV, Section 27.5 specified — a rigor level Elasticsearch's typical operational-log usage pattern doesn't need and Volume 1, Part IV, Section 27.7 explicitly required staying architecturally separate from.

11.2 Log shipping — from the Collector, not a separate agent

Operational logs reach Elasticsearch via the same OpenTelemetry Collector gateway (Section 8.2) already routing traces and metrics — the Collector's log-export pipeline (a standard OTel Collector exporter for Elasticsearch) is used rather than deploying a separate log-shipping agent (e.g., Filebeat) alongside it, keeping Section 8.6's PII/secret scrubbing and Section 8.7's resource attribution (correlation IDs, tenant_id, service.name) applied uniformly to logs exactly as they are to traces and metrics, rather than requiring a second, independently-configured scrubbing and tagging layer that could drift out of sync with the primary one.

11.3 Index strategy

Indices are time-based (daily rolling indices, nexus-logs-YYYY.MM.DD), with an Index Lifecycle Management (ILM) policy directly implementing the exact retention tiers Volume 1, Part IV, Section 28.6 already specified: full-fidelity, fast-query "hot" tier for 14 days, a downsampled/lower-resources "warm" tier for the remaining balance of a 90-day window, then deletion — this section is the concrete infrastructure realization of a policy Volume 1 named as a principle ("a tiered approach... reflecting that operational logs exist to support near-term debugging... not long-term compliance record-keeping") without specifying the mechanism, closed here exactly as this volume has closed several other Volume 1 forward references (Section 2.7's air-gapped registry mirroring, Section 4.7's install command).

11.4 Correlation with traces — the unified investigation experience

Every log record carries the same CorrelationId/FlowRunId scope Volume 1, Part IV, Section 28.4 established via ILogger.BeginScope — indexed as a searchable field in Elasticsearch, and, critically, the same identifier is attached as a span attribute on the corresponding OpenTelemetry trace (Section 8.3) reaching the trace backend. This is what makes Grafana (Section 10) able to offer trace-to-logs correlation natively — an engineer viewing a slow or failed trace in the Orchestration Engine Deep-Dive dashboard (Section 10.3) can pivot directly to the exact log lines sharing that trace's FlowRunId, without manually re-entering an identifier into a separate search interface — directly realizing the investigative workflow Volume 1, Part IV, Section 28.4 described in principle ("reconstruct exactly what happened") as an actual, one-click Grafana feature.

11.5 Index sizing and cardinality

Given operational logging's inherently higher volume than metrics (Section 9.8's cardinality discipline doesn't directly apply to log records the same way, since Elasticsearch is architecturally suited to high-cardinality, full-text-searchable data in a way Prometheus is not), index sharding is sized based on observed daily volume per environment (production sized meaningfully larger than staging, per Section 5.3's environment tiers), with shard count reviewed periodically against actual growth rather than fixed permanently at initial deployment — mirroring Volume 4, Section 3.3's "sized with headroom, validated periodically against actual observed usage" discipline, applied here to log storage specifically.

11.6 Query access and Kibana

Kibana (Elasticsearch's native query/visualization UI) is available to engineers for ad hoc log investigation beyond what Grafana's trace-to-logs pivot (11.4) covers — access-gated identically to Section 10.6's internal-dashboard SSO integration, with query access scoped by the same per-team ownership model (Section 7.6) where relevant (a team can freely query logs from services they own; cross-team log access for incident investigation follows the same escalation path Section 7.6 already established for alert ownership, not a separate, independently-designed access policy).

11.7 Self-hosted deployment — bundled or external, mirroring Section 4.4's pattern

Directly extending Section 4.4's conditional-subchart pattern (already established for PostgreSQL and Redis) to Elasticsearch: a self-hosted, resource-constrained deployment can enable a bundled, single-node Elasticsearch subchart for a genuinely complete, self-contained installation, while a larger self-hosted or managed deployment points at an external, properly-clustered Elasticsearch service — the same values-file-driven choice (Volume 4, Section 4.3–4.4) already established for the other stateful dependencies this platform relies on, applied consistently here rather than introducing a differently-patterned configuration approach just because this is a different underlying technology.

11.8 Air-gapped deployment considerations

For air-gapped self-hosted deployments (Volume 1, Part VI, Section 37.7; Volume 4, Section 2.7's image-mirroring procedure), Elasticsearch's own container images are included in the same mirroring procedure as every other component this document set has specified — no operational-logging capability is lost in an air-gapped deployment, since (unlike Volume 3's AI capabilities, which are the one deliberately, permanently AI-provider-dependent piece of this platform per Volume 1's NFR-SEC-4) operational logging has no external-network dependency of any kind once the images themselves are mirrored, consistent with Architecture Principle 20.8's insistence that the self-hosted, air-gapped path be a fully-capable deployment target, not a degraded one.

11.9 Closing note for this Part

This section closes Part III's observability specification: Section 7 established the strategy (SLIs, SLOs, alerting philosophy), Section 8 established unified collection, and Sections 9–11 specified the three storage/visualization backends that strategy and collection layer feed. Part IV (Data & Secrets) now turns to the data layer's own infrastructure specifics — PostgreSQL and Redis operational detail, and the secrets/credential infrastructure (Vault, TLS) this Part's own OTel Collector, Prometheus, and Elasticsearch deployments all themselves depend on for their own credentials, closing a dependency loop this Part deliberately left to the next.


____________________________________________________________________________________________________________________________________________________



12. Redis
12.1 Purpose — operationalizing three distinct roles

Volume 1, Part VI, Section 38.7 already named three distinct roles Redis serves — permission/RoleVersion cache, rate-limiting counters, and cross-worker execution coordination — plus Volume 1, Part V, Section 31.5's SignalR backplane role. This section specifies the operational deployment supporting all four roles, including where their genuinely different consistency and durability needs lead to different configuration choices rather than one-size-fits-all defaults.

12.2 Deployment topology — Redis Sentinel for HA

Redis is deployed with Sentinel-managed replication (a primary plus at least two replicas, with Sentinel processes monitoring and orchestrating automatic failover) rather than a single, unreplicated instance — directly serving NFR-REL-1's 99.9% availability commitment, since Volume 1, Part IV, Section 24.6's RoleVersion cache and Section 31.5's SignalR backplane are both load-bearing, active dependencies whose unavailability would degrade the running platform, not merely a convenience layer that could tolerate a single point of failure. Redis Cluster (sharded, as opposed to Sentinel's replicated-single-primary model) is explicitly not used for GA — Nexus's Redis working set (session/permission cache, rate-limit counters, backplane state) does not approach the scale where sharding's added operational complexity would be justified, consistent with this document set's repeated "add complexity only where it earns its cost" discipline (Volume 1, Architecture Principle 20.7).

12.3 Persistence configuration — differentiated by role

A single Redis deployment serves all four roles (12.1), but persistence configuration is chosen for the role with the strictest requirement, since Redis's persistence setting is deployment-wide: AOF (Append-Only File) persistence with everysec fsync policy is enabled, favoring durability over the small latency cost, because the RoleVersion permission cache (Volume 1, Part IV, Section 24.6) losing state on a crash would incorrectly allow a just-revoked permission to appear valid until the next natural cache-invalidation event — an acceptable risk for pure ephemeral data (rate-limit counters, SignalR backplane state, both fully self-healing after a restart with no correctness impact) but not for the permission-cache role specifically. This is a deliberate, single, documented tradeoff — accepting AOF's modest overhead platform-wide to correctly serve the one role that needs it, rather than attempting a more complex per-key persistence policy Redis doesn't natively support well.

12.4 Memory eviction policy

noeviction is configured rather than an LRU-based eviction policy — Redis reaching its configured memory limit should surface as an explicit, alertable error (Section 7.5's Critical severity) rather than silently evicting keys, since silent eviction of, for instance, in-flight SignalR backplane coordination state (Volume 1, Part V, Section 31.5) would produce confusing, hard-to-diagnose downstream symptoms (a client mysteriously missing live-progress events) rather than a clear, actionable capacity alert. This is a direct, deliberate application of Architecture Principle 20.4 (fail loud, fail informatively) to a memory-management policy specifically — Redis running out of memory is treated as an infrastructure-capacity incident to be alerted and resolved, never as a condition the system should silently paper over by discarding data.

12.5 Connection pooling and multiplexing

Both nexus-api and nexus-orchestration use a multiplexed Redis client connection (a single, long-lived TCP connection handling many concurrent logical requests, standard for the .NET StackExchange.Redis client Volume 1's stack implies) rather than a traditional connection-pool-of-many-connections pattern — directly consistent with Volume 2, Section 15.5's frontend-facing note that "Redis client-side connection multiplexing is sufficient... not requiring a pool sized to 500," now specified as the backend's own connection strategy for the same underlying reason: Redis's single-threaded command-processing model makes connection multiplexing, not connection-count scaling, the correct approach to handling NFR-SCALE-1's concurrency target.

12.6 Rate-limiting counter implementation

Directly implementing Volume 1, Part IV, Section 24.9 and Volume 1, Part V, Section 30.7's rate-limiting requirements: sliding-window counters are implemented via Redis sorted sets (a standard, well-understood pattern — timestamped entries with periodic trimming) rather than simple fixed-window counters, avoiding the boundary-edge burst-traffic problem fixed windows are prone to, keeping the rate-limiting behavior's actual enforcement precision matching what those two Volume 1 sections' stated intent implies, not merely an approximation.

12.7 SignalR backplane configuration

Directly completing Volume 1, Part V, Section 31.5's deployment note: the backplane uses Redis Pub/Sub (not Redis Streams, which would offer replay capability the backplane's design explicitly doesn't need, per Volume 2, Section 15.4's client-side reconciliation already handling the "what did I miss" problem at the REST-catch-up layer rather than expecting the transport layer to solve it) — a deliberately simple choice matching Volume 1, Part V, Section 31.7's narrow scope for this hub ("progress of a run the client has explicitly subscribed to," nothing more).

12.8 Monitoring Redis itself

Redis exports its own operational metrics (memory usage, connected clients, command latency, replication lag between primary and Sentinel-monitored replicas) through the standard Redis Exporter, ingested via Section 8's OpenTelemetry Collector pipeline alongside every other platform metric — Redis health is a named row in the Platform Health dashboard (Section 10.3), with replication lag specifically alerted on at Warning severity (Section 7.5) since sustained lag threatens the failover guarantee 12.2's Sentinel configuration exists to provide.

12.9 Self-hosted and dedicated-instance deployment

Following the same conditional-subchart pattern already established for PostgreSQL (Section 4.4) and Elasticsearch (Section 11.7): a self-hosted deployment can enable a bundled Redis-with-Sentinel subchart for a complete, self-contained installation, or point at an external managed Redis service — the dedicated-instance deployment model (Volume 1, Part IV, Section 26.6) always runs its own isolated Redis instance, never shared across tenants, consistent with that model's stronger-isolation promise extended to every stateful dependency this platform has, not just the primary database.



13. PostgreSQL
13.1 Purpose — operationalizing Volume 1's schema and scaling design

Volume 1, Part V, Section 32 specified PostgreSQL's schema in full; Volume 1, Part VI, Section 39.3 and 39.6 named connection pooling and future partitioning as scaling mechanisms without fully specifying their operational deployment. This section closes those references and specifies the complete operational posture: high availability, connection management, and maintenance.

13.2 High availability — streaming replication with automated failover

PostgreSQL runs with at least one synchronous streaming replica (ensuring zero data loss on primary failover, at the cost of slightly higher write latency — an accepted tradeoff given NFR-SEC-3's audit-data durability requirement and Volume 1, Part III, Section 16.7's per-step-commit durability guarantee both depending on writes genuinely being durable, not just fast) plus additional asynchronous replicas for read-scaling where warranted. Automated failover is managed by a dedicated PostgreSQL HA controller (Patroni, the standard choice for this pattern) coordinating leader election through the same distributed-consensus mechanism (etcd, or Kubernetes-native equivalents) rather than a custom-built failover script — directly serving NFR-REL-1's 99.9% availability commitment for the component every other piece of this platform ultimately depends on.

13.3 Connection pooling — PgBouncer, closing Volume 1's forward reference

Directly implementing Volume 1, Part VI, Section 39.3's named-but-undetailed connection pooler: PgBouncer runs in transaction-pooling mode (not session-pooling), sitting between every application connection (nexus-api, nexus-orchestration, and critically, Section 16.7's IUnitOfWorkFactory-created short-lived contexts, Volume 1, Part III) and PostgreSQL itself — transaction-pooling mode is specifically chosen because Volume 1, Part III, Section 16.7's incremental per-step commit design creates many short-lived transactions per FlowRun rather than one long-lived one, a connection-usage pattern transaction-pooling mode is architecturally suited to (returning a connection to the pool the instant a transaction commits, not holding it for a client's full session), directly serving NFR-SCALE-1's 500-concurrent-execution target without requiring 500 concurrent raw PostgreSQL connections, which would exceed a reasonably-sized PostgreSQL instance's own connection ceiling.

13.4 Row-level security performance considerations

Volume 1, Part IV, Section 26.3's row-level security policies (enforced on every multi-tenant table) are indexed to ensure the RLS predicate itself (tenant_id = current_setting('app.current_tenant_id')) resolves via the same composite indexes Volume 1, Part V, Section 32.6 already specified with tenant_id as the leading column — this section adds the operational verification: RLS policy performance is included in Section 5.2's staging-verification load-testing stage specifically, since an RLS predicate that silently forces a sequential scan (if an index doesn't actually cover it the way intended) would be a serious, easy-to-miss performance regression given RLS applies to literally every query against a protected table.

13.5 Partitioning — implementing Volume 1's deferred mechanism

Volume 1, Part VI, Section 39.6 named table partitioning (flow_runs, step_results, partitioned by started_at time range) as the mechanism for scaling toward NFR-SCALE-3's 10-million-row target, explicitly deferred as "when needed, not by default at GA." This section specifies the concrete trigger and implementation: partitioning is activated (via pg_partman or an equivalent automated partition-management extension, rather than hand-rolled partition-creation scripts) when a tenant's flow_runs table crosses a defined row-count threshold (e.g., 5 million rows, chosen with headroom below the 10-million target) — monitored as a named metric (Section 9) feeding a Warning-severity alert (Section 7.5) well before the threshold is reached, giving the team time to activate partitioning deliberately rather than reactively under pressure. Because Volume 1, Part V, Section 32.6 already confirmed the schema was "kept compatible with a future move to partitioning without requiring a schema redesign," this activation is purely an operational/DBA action at the point it's needed, not a code or schema change.

13.6 Vacuum and maintenance

Autovacuum is tuned more aggressively than PostgreSQL's conservative defaults specifically for the step_results and audit_entries tables (Volume 1, Part V, Section 32.3, 32.5) — both are high-insert-volume, append-heavy tables where default autovacuum settings would let table and index bloat accumulate meaningfully given this platform's expected write patterns (every flow-run step, per Volume 1, Part III, Section 16.7's per-step commit design, is an insert). Tuned autovacuum thresholds are reviewed as part of the same periodic capacity-validation discipline Section 3.3 already established for Kubernetes resource sizing, not set once at initial deployment and forgotten.

13.7 Backup integration — forward reference

Full backup strategy (frequency, retention, restore-testing procedure) is specified in this Part's Section 20 (Backup Strategy) — this section notes only the mechanism PostgreSQL-specifically supports: continuous WAL (Write-Ahead Log) archiving alongside periodic base backups, enabling point-in-time recovery to any moment within the retention window, not merely restoration to the most recent full-backup snapshot — the specific capability Section 20's disaster-recovery objectives (Section 17) depend on PostgreSQL's own backup mechanism providing.

13.8 Migration execution — restating Volume 1's gate with infrastructure detail

Directly completing Volume 1, Part VI, Section 37.5's migration-gating design: the Kubernetes Job running EF Core migrations (Volume 1, Part V, Section 32.8) connects directly to PostgreSQL's primary, bypassing PgBouncer's transaction-pooling mode (which is incompatible with some DDL operations' session-level requirements) via a dedicated, narrowly-scoped migration connection string — a specific, easy-to-overlook operational detail included here precisely because it's the kind of thing that works fine in casual local testing (Section 1's docker-compose setup, which doesn't include PgBouncer at all) and only surfaces as a problem against the full production topology, exactly the class of environment-parity gap Section 1.1's dev/prod parity principle exists to minimize, with this specific, documented exception flagged rather than silently discovered later.

13.9 Self-hosted and dedicated-instance deployment

Following the same conditional pattern established throughout this Part (Sections 4.4, 11.7, 12.9): a resource-constrained self-hosted deployment can run a bundled, single-instance PostgreSQL (accepting reduced availability guarantees, explicitly documented as a tradeoff for that deployment profile, consistent with Architecture Principle 20.8's "self-hosted is first-class, not assumed to always match managed-hosting's full HA posture") or point at an existing, already-HA-configured customer database — the dedicated-instance model always provisions its own full Patroni-managed HA cluster (13.2), matching that model's stronger commitment.



14. Secrets
14.1 Purpose — the general architecture, before Section 15's specific tool

This section specifies secrets-management architecture at the conceptual level — what counts as a secret, how ICredentialResolver (Volume 1, Part III, Section 18.4) is actually backed, and the rotation discipline governing every credential this platform touches. Section 15 then specifies HashiCorp Vault as the concrete implementation of the architecture this section defines.

14.2 What counts as a secret — an exhaustive classification
Secret category Examples  Consumer
Platform infrastructure credentials PostgreSQL password, Redis auth, Elasticsearch credentials  Application startup configuration (Section 4.8 — never in Helm values directly)
Tenant-supplied target-system credentials Credentials a flow author wires into a FlowStep to authenticate against the system under test ICredentialResolver, scoped per Volume 1, Part III, Section 18.4's least-privilege design
AI provider API keys  Volume 3, Part IV, Section 14.8's provider credentials  AiProvider implementations, resolved identically to module credentials
TLS private keys  Ingress and internal mTLS certificates  Section 16 (TLS) — a distinct lifecycle from other secret types, detailed there
Signing/encryption keys JWT signing key (Volume 1, Part IV §24.5), envelope-encryption data-encryption keys (14.5)  Application runtime, never exposed outside the resolving process's memory

This table is the complete enumeration this volume works from — any new credential type introduced by a future feature must be classified into one of these rows (or justify a new row, reviewed with the same rigor Section 4.9 already established for security-sensitive chart changes) before implementation, never handled ad hoc outside this classification.

14.3 ICredentialResolver — the backend Volume 1 deferred to this volume

Volume 1, Part III, Section 18.4 specified ICredentialResolver's interface and scoping behavior (narrow, per-flow-step, least-privilege) without specifying its backing implementation — deliberately, since that's an infrastructure concern belonging to this volume. The implementation resolves a credential reference (never a raw value, per Volume 1, Part II, Section 13's RotateCredentialCommand design already establishing this pattern) against Section 15's Vault instance at the moment a flow step actually executes, not earlier — a credential value is fetched just-in-time for use and never cached beyond the single step's execution scope, directly minimizing the window during which a resolved secret value exists in application memory at all.

14.4 Envelope encryption — implementing NFR-SEC-2

Volume 0's NFR-SEC-2 required "envelope encryption at rest, with the encryption key managed outside the primary application database." This section specifies the mechanism: tenant-supplied credentials (14.2's second row) are encrypted with a per-tenant Data Encryption Key (DEK) before being written to PostgreSQL; the DEK itself is encrypted ("wrapped") by a Key Encryption Key (KEK) that never leaves Section 15's Vault instance (via Vault's Transit secrets engine, specified fully in Section 15.4) — PostgreSQL therefore only ever stores a doubly-encrypted value it has no ability to decrypt on its own, satisfying NFR-SEC-2's "outside the primary application database" requirement precisely: the KEK genuinely never resides in or is derivable from anything PostgreSQL holds.

14.5 Credential rotation

Every credential category in 14.2's table has a defined rotation cadence and procedure: platform infrastructure credentials rotate on a scheduled cadence (e.g., quarterly) via Section 15's Vault-native rotation capability where available (the database secrets engine, Section 15.5, natively supports this for PostgreSQL credentials specifically); tenant-supplied target-system credentials rotate at the tenant's own discretion via Volume 1, Part II, Section 13.5's RotateCredentialCommand, with Nexus never imposing a forced rotation schedule on credentials it doesn't own; AI provider keys rotate per that provider's own security recommendations, tracked as an operational calendar item. Every rotation event — regardless of category — raises Volume 1, Part II, Section 12.5's CredentialRotatedEvent, durably audited (Volume 1, Part IV, Section 27) exactly as already specified there, this section adding only the operational cadence driving when that event fires for platform-owned credentials specifically.

14.6 Break-glass access — the deliberate, audited exception

For genuine emergency scenarios (a security incident requiring immediate credential revocation outside the normal rotation cadence, per Section 18.7's incident-response process referenced from Volume 3), a narrowly-scoped "break-glass" access procedure exists — a small number of designated administrators can trigger emergency rotation or revocation outside Vault's normal policy-gated access flow, with every break-glass action generating an immediate, unsuppressable Critical-severity alert (Section 7.5) and a mandatory post-incident review, ensuring this necessary emergency capability is never usable quietly or routinely, only as a genuinely exceptional, fully-visible action.

14.7 Secrets in transit — never logged, restated as an infrastructure guarantee

Directly restating and closing the loop on Volume 1, Part IV, Section 28.5's "credential values never logged, at any level" rule and Volume 4, Section 8.6's Collector-level scrubbing: this volume's infrastructure adds a final, structural guarantee — ICredentialResolver's resolved values are typed with a wrapper type (SecretValue<T>) whose ToString() override always returns a redacted placeholder rather than the actual value, meaning even an accidental future logging statement referencing a resolved credential cannot leak its actual value — a compile-time-adjacent safeguard directly analogous to Volume 1, Part II, Section 10.8's strongly-typed-identifier philosophy ("eliminated at the type-system level, not just by convention"), now applied to secret values specifically as this volume's closing contribution to a discipline every prior volume has reinforced in its own way.

14.8 Auditing secret access itself

Every resolution of a tenant-supplied credential (14.2's second row) — not just its rotation — generates an audit-adjacent record (distinct from, but correlated with, Volume 1, Part IV, Section 27's AuditEntry for the triggering flow-run event) noting which flow step accessed which named credential reference and when, satisfying Persona Elena's likely compliance-review expectation (Volume 0, UC-7) that credential usage, not just credential changes, be traceable — a deliberate extension beyond Volume 1's originally-specified audit scope, added here because this volume's infrastructure-level visibility into actual secret resolution calls is the first point in this document set where such fine-grained usage auditing becomes practically implementable.



15. Vault
15.1 Purpose — the concrete implementation of Section 14's architecture

Volume 0, Section 21.13 locked HashiCorp Vault as the reference secrets-management implementation. This section specifies its concrete deployment: authentication methods, secret engines, and the high-availability configuration this platform's entire credential architecture (Section 14) ultimately depends on.

15.2 Deployment topology — Vault HA with integrated storage

Vault runs in HA mode using its integrated Raft storage backend (avoiding an external storage dependency like Consul, keeping this platform's total infrastructure footprint smaller — directly consistent with Architecture Principle 20.8's self-hosted-first posture, where every additional required infrastructure dependency is a real cost to a self-hosted customer's operational burden, the exact reasoning Volume 1, Part III, Section 22.3 already applied when rejecting an external message broker for GA) — at least three Vault nodes participating in Raft consensus, tolerating a single node failure without service interruption, directly serving NFR-REL-1's availability commitment extended to this platform's most security-critical dependency.

15.3 Authentication methods
Auth method Used by Detail
Kubernetes auth nexus-api, nexus-orchestration pods Each pod authenticates using its Kubernetes service-account token, scoped by namespace (Section 3.2) — nexus-sandbox namespace pods are deliberately granted no Vault authentication path at all, directly enforcing Section 3.5's network-policy isolation at the credential layer too: a compromised sandboxed module cannot even attempt to authenticate to Vault, not merely blocked by network policy from reaching it
OIDC (CI) GitHub Actions workflows  Volume 4, Section 6.6's short-lived, workflow-scoped federation — no long-lived Vault token ever stored as a CI secret
OIDC (human)  Break-glass access (Section 14.6) Federated through the same enterprise SSO integration Volume 1, Part IV, Section 24.7 established, ensuring break-glass access is tied to a real, individually-accountable human identity, never a shared credential
15.4 Secret engines — KV and Transit

KV (Key-Value) v2 engine stores platform infrastructure credentials (Section 14.2's first row) as versioned static secrets, with version history enabling rollback if a rotation (Section 14.5) introduces a problem.

Transit engine implements Section 14.4's envelope-encryption design directly — the platform's per-tenant Key Encryption Keys (KEKs) live exclusively within Transit, which performs encrypt/decrypt operations on demand without ever exposing the KEK's raw key material to the calling application at all (Transit's defining property: the application sends plaintext or ciphertext to Vault and receives the opposite, but the key itself never leaves Vault) — this is a stronger guarantee than Section 14.4's "KEK never resides in PostgreSQL" alone; the KEK never resides in application memory either, at any point, closing a gap a naive envelope-encryption implementation (fetching the KEK into application memory to perform encryption locally) would leave open.

15.5 Database secrets engine — dynamic, short-lived database credentials

Beyond static KV storage, Vault's Database secrets engine generates dynamic, short-lived PostgreSQL credentials on demand for administrative/operational tooling (Section 13's DBA-adjacent maintenance operations, Section 6's CI migration-job execution) — each request produces a unique username/password pair with a defined TTL (e.g., 1 hour), automatically revoked by Vault at expiry, meaning no long-lived, shared administrative database credential exists anywhere in this platform's operational tooling at all. The application's own runtime database connections (via PgBouncer, Section 13.3) use a longer-lived, but still Vault-managed and rotatable, service credential — dynamic per-request credentials are specifically for human/tooling operational access, not the high-frequency application connection path, where the rotation cadence Section 14.5 already specified is the appropriate mechanism instead.

15.6 Unsealing strategy

Vault's unseal mechanism uses auto-unseal via a cloud KMS (for managed/hosted deployments) or, for self-hosted deployments without a cloud KMS available (the air-gapped case specifically, Volume 1, Part VI, Section 37.7), Shamir's Secret Sharing with a defined threshold (e.g., 3-of-5 key shares distributed among designated administrators) — auto-unseal is strongly preferred where available since it enables Vault to recover automatically after a restart without requiring manual key-share entry, directly serving NFR-REL-1's availability commitment; the Shamir fallback for air-gapped deployments is explicitly documented as carrying a real operational tradeoff (a Vault restart requires manual intervention from key-share holders) that self-hosted, air-gapped customers accept as part of that deployment profile's inherent constraints, consistent with this document set's practice of stating such tradeoffs honestly rather than glossing over them.

15.7 Policy design — least privilege per consumer

Every Vault authentication identity (15.3) is bound to a narrowly-scoped policy granting access only to the specific secret paths that consumer needs — nexus-api's Kubernetes-auth identity can read the JWT signing key (Volume 1, Part IV, Section 24.5) and platform database credentials, but has no access to Transit's tenant KEKs (which only nexus-orchestration's credential-resolution path, Section 14.3, needs); the CI OIDC identity can read only the specific deployment-time secrets Section 6.6 named, nothing broader. This is the direct, Vault-policy-level implementation of the least-privilege principle Volume 1, Part III, Section 18.4 established conceptually for ExecutionContext's scoping, now applied uniformly to every Vault-authenticating identity in this platform, not just module execution specifically.

15.8 Vault monitoring and the meta-observability principle

Consistent with Section 7.8's "monitoring the monitoring" principle, extended to this platform's most security-critical dependency: Vault's own health, seal status, and audit log (Vault's native audit-logging feature, distinct from but complementary to Volume 1, Part IV, Section 27's application-level audit trail) are monitored independently, with seal-status specifically alerted at Critical severity (Section 7.5) — a sealed Vault instance means every credential-resolution path across this entire platform stops working simultaneously, making this the single highest-severity infrastructure dependency failure this volume's monitoring strategy needs to catch fastest.

15.9 Self-hosted and air-gapped Vault

Following this Part's now-consistent conditional-deployment pattern: a self-hosted deployment bundles its own Vault instance (15.2's HA configuration, resource permitting, or a single-node instance for smaller deployments with the same explicitly-documented availability tradeoff Section 13.9 already established for a single-instance PostgreSQL choice); air-gapped deployments use Section 15.6's Shamir-based unsealing and require zero external network dependency for Vault's own operation, consistent with NFR-SEC-4's "no mandatory outbound internet dependency for core functionality" — Vault itself, being the very foundation this platform's secrets architecture rests on, is held to that requirement without exception.



16. TLS
16.1 Purpose — completing Volume 1's NFR-SEC-1 and Volume 4's forward references

Volume 1's NFR-SEC-1 required TLS 1.2+ for all data in transit; Volume 4, Section 3.6 named Ingress-level TLS termination and flagged "re-encrypting internally only where Section 16's zero-trust posture requires it" as this section's responsibility. This section specifies the complete certificate lifecycle — external termination, internal mTLS, rotation, and the air-gapped certificate-authority scenario.

16.2 External TLS — cert-manager and automated issuance

External-facing TLS (the Ingress layer, Section 3.6) uses cert-manager, automated against a public ACME-compatible certificate authority (Let's Encrypt or an equivalent) for the managed/hosted deployment and standard internet-connected self-hosted deployments — certificates are automatically issued and renewed well before expiry (cert-manager's default renewal window, typically 30 days before a 90-day certificate's expiry), with renewal failure alerting at Warning severity (Section 7.5) escalating to Critical as expiry actually approaches, ensuring certificate expiry — a historically common, embarrassingly preventable cause of production outages — is caught by this platform's own monitoring discipline (Section 7) well before it could threaten NFR-REL-1's availability commitment.

16.3 Internal mTLS — the zero-trust posture for the sandbox boundary

Directly implementing Volume 1, Part III, Section 18.5's sandboxed-module gRPC communication design and Section 3.5's network-policy isolation: the gRPC channel between nexus-orchestration and nexus-sandbox-runner pods (Section 3.2) uses mutual TLS — both ends present and verify certificates, not just the client verifying the server — issued by an internal certificate authority (cert-manager's internal CA issuer, distinct from the external ACME-issued certificates in 16.2) and automatically rotated on a short cadence (e.g., 24 hours, since these are short-lived, frequently-reissued certificates for internal service identity rather than long-lived external-facing certificates). This mTLS requirement is the concrete implementation of "zero-trust" specifically for this one boundary: even though Section 3.5's network policy already restricts which pods can reach nexus-orchestration's sandbox-callback endpoint, mTLS additionally ensures the connecting pod is cryptographically verified to be a legitimate sandbox runner, not merely a pod that happened to be scheduled within the allowed network path — a meaningful additional layer given this boundary's elevated risk profile (Volume 1, Part I, Section 2.5).

16.4 Internal TLS elsewhere — encryption without full mTLS

Every other internal service-to-service connection (nexus-api/nexus-orchestration to PostgreSQL via PgBouncer, to Redis, to Vault, to the OTel Collector) uses TLS for encryption-in-transit but not full mutual TLS with per-connection certificate verification — a deliberate, narrower posture than Section 16.3's sandbox-boundary treatment, justified by the same risk-proportionate reasoning this document set has applied consistently (Volume 1, Part III, Section 18.5's trust-tiered execution model itself being the original template for "not every boundary needs the same level of defense"): these are trusted, first-party components within the same security domain, where TLS's encryption property (protecting against network-level eavesdropping, relevant even within a cluster given Kubernetes' own network model) is the primary requirement, and Section 3.5's network policies already provide the connection-restriction property mTLS would additionally add.

16.5 Certificate rotation and zero-downtime reload

Every TLS-terminating component (the Ingress controller, nexus-api for any direct TLS it terminates itself, Vault per Section 15.2) supports certificate hot-reload — a renewed certificate (16.2's automated renewal, or 16.3's 24-hour internal rotation) is picked up without requiring a pod restart or connection interruption, directly serving NFR-REL-1's availability commitment by ensuring routine certificate rotation is never itself a source of downtime, a specific, concrete instance of this volume's general "routine operational events must not threaten availability commitments" principle already applied to Kubernetes node draining (Section 3.4) and database failover (Section 13.2).

16.6 Air-gapped deployments — the self-signed internal CA scenario

For air-gapped self-hosted deployments (Volume 1, Part VI, Section 37.7), Section 16.2's public-ACME-CA approach is unavailable by definition (no outbound internet access to reach Let's Encrypt or any public CA) — these deployments use a customer-provided or Nexus-generated self-signed internal Certificate Authority instead, with cert-manager configured against that internal CA rather than an external ACME issuer. This requires the customer's own client machines (for the GUI, Volume 2) and any external system integrations to trust that internal CA explicitly — a documented, expected part of the air-gapped deployment's setup procedure (extending Section 4.7's self-hosted installation documentation), not an oversight; this is the one meaningful operational difference between an air-gapped and an internet-connected self-hosted deployment's TLS posture, called out explicitly here because it's a genuine, unavoidable consequence of the air-gapped constraint itself, not a gap in this volume's design.

16.7 TLS and the frontend — cookie security depending on TLS being genuinely universal

Directly connecting back to Volume 2, Section 14.2's Secure cookie flag (the refresh-token cookie, HttpOnly/Secure/SameSite=Strict) — that flag's entire security value depends on TLS being genuinely, universally present on every environment the GUI could ever be served from, including local development (Section 1.6's mkcert-based local TLS, specified precisely so this dependency holds even in the least security-critical environment) — this section's universal-TLS posture is what makes Volume 2's cookie-security design actually correct in every deployment context, not merely correct in the specific environments someone happened to test.

16.8 Closing note for this Part

This section closes Part IV: Redis (Section 12) and PostgreSQL (Section 13) specified the data layer's operational posture; Secrets (Section 14) and Vault (Section 15) specified the credential architecture protecting that data layer and everything else; TLS (this section) specified the encryption-in-transit layer protecting all of it in motion. Part V (Resilience & Operations) now turns to how this complete infrastructure picture behaves under failure — disaster recovery, high availability synthesis across every component this Part has specified individually, scaling, backup strategy, and cost optimization for the infrastructure layer specifically, completing this volume.


____________________________________________________________________________________________________________________________________________________



17. Disaster Recovery
17.1 Purpose — the failure scale beyond Part III's individual-component alerting

Part III's monitoring strategy (Section 7) and Part IV's per-component HA configurations (Sections 12, 13, 15) address individual-component failure. This section addresses the harder, less frequent scenario: a failure severe enough to threaten the platform's overall availability commitment — a full cluster loss, a region-level outage, or a catastrophic data-corruption event — and specifies the recovery objectives and tested procedures this document set's NFR-REL-1 commitment ultimately rests on.

17.2 Recovery objectives — RTO and RPO, derived from NFR-REL-1
Scenario  RTO (Recovery Time Objective) RPO (Recovery Point Objective)
Single-region cluster loss (managed/hosted) 4 hours 5 minutes (WAL-streaming lag, Section 13.7)
PostgreSQL catastrophic corruption  1 hour (via Patroni failover, Section 13.2, if replica-level; 4 hours if requiring full restore)  5 minutes
Vault data loss 2 hours (Raft snapshot restore, Section 15.2) 15 minutes (snapshot frequency, Section 20.4)
Self-hosted customer's own infrastructure failure Customer-owned — Nexus provides the tooling (this section's procedures), the customer's own infrastructure team owns execution and timeline 

These targets are derived directly from NFR-REL-1's 99.9% availability commitment — a 4-hour RTO for the rare, severe full-cluster-loss scenario is mathematically compatible with a 99.9% annual availability target (roughly 8.7 hours of acceptable downtime per year) only if such severe events are genuinely rare, which is precisely why Section 18's HA design exists to prevent the vast majority of failures from ever escalating to this section's DR scenarios in the first place — this section is the deliberate, tested fallback for the failure modes HA alone cannot fully prevent, not the primary availability strategy.

17.3 DR posture — pooled managed/hosted vs. dedicated-instance vs. self-hosted

Directly extending Volume 1, Part IV, Section 26's three deployment models to disaster recovery specifically: the pooled managed/hosted deployment (Volume 1, Part IV, Section 26.2) maintains a warm-standby capability in a secondary region, with the RTO in 17.2 reflecting the time to redirect traffic and promote that standby, not a cold rebuild. The dedicated-instance model (Volume 1, Part IV, Section 26.6) — consistent with its stronger isolation promise extending to DR posture as well — maintains an equivalent per-customer secondary-region capability where the customer's contract specifies it, at a cost tier reflecting that dedicated redundancy (a direct, natural extension of Volume 0, Section 16.5's pricing principles). Self-hosted deployments (Architecture Principle 20.8) own their own DR posture entirely — Nexus provides the backup/restore tooling (Section 20) and this section's documented procedures, but execution, RTO/RPO targets, and secondary-region infrastructure (if any) are the customer's own responsibility, consistent with the self-hosted model's fundamental operational-ownership boundary.

17.4 The DR runbook

A maintained, versioned runbook (Section 7.7's documentation-as-code discipline extended to this platform's most severe failure scenarios) specifies, step by step: how to detect that a DR scenario (as opposed to a routine, HA-absorbed failure) is actually in progress; the decision authority for declaring a DR event (avoiding a premature, costly full-DR response to what might resolve as a routine incident); the exact sequence of actions (secondary-region promotion, DNS/traffic cutover, Vault unsealing at the secondary per Section 15.6's procedure, verification steps confirming the recovered environment is genuinely healthy before declaring recovery complete) — this runbook is the concrete artifact Section 17.5's game-day exercises validate, and the artifact an actual incident is executed against, never improvised in the moment.

17.5 Failure testing — game days

Scheduled, deliberate DR exercises (quarterly, at minimum) simulate each scenario in 17.2's table against a genuinely isolated test environment (never production) — a full secondary-region failover drill, a Vault Raft-snapshot restore drill, a PostgreSQL point-in-time-recovery drill — measuring actual RTO/RPO achieved against the targets in 17.2 and updating the runbook (17.4) with any gap discovered. This is a direct, deliberate application of Volume 1, Part VI, Section 38.8's "load testing as a release gate" philosophy extended to disaster recovery specifically: an RTO/RPO target that has never been actually exercised is an untested claim, not a verified capability, and this document set has consistently treated untested claims about critical properties as inadequate (the same reasoning behind Volume 2, Section 19.11's adversarial plugin-sandbox testing and Volume 3, Section 17.9's adversarial guardrail testing).

17.6 Communication during a DR event

The DR runbook (17.4) includes a defined customer-communication procedure — status-page updates at defined intervals during an active DR event, a post-incident report within a committed timeframe after resolution — directly serving Volume 0's Business Objective 5 trust posture: a DR event handled competently but silently, with customers discovering the outage only through their own monitoring, would undermine exactly the enterprise trust this entire document set has repeatedly prioritized, even if the technical recovery itself met every RTO/RPO target in 17.2.

17.7 AI capability during DR

Directly extending Volume 3's AI-optionality architecture (NFR-REL-3) to the DR scenario specifically: a DR event affecting the primary region does not need to restore AI capability (Volume 3's Planning Engine, Section 2 there) with the same urgency as core platform capability (flow execution, Volume 1's Orchestration Engine) — AI capabilities gracefully degrade to unavailable (Volume 3, Section 2.5's AiUnavailableError path) exactly as they would during a routine AI-provider outage, meaning the DR runbook's priority ordering restores core execution capability first, with AI capability restoration following as a lower-priority, non-blocking subsequent step — a direct, practical payoff of Volume 3's entire AI-optionality architecture being tested under genuine disaster conditions, not just routine operation.

17.8 Relationship to Section 18

This section specified recovery from severe failure. Section 18 (High Availability) specifies the complementary, preventive side — the synthesis of every component's individual HA design across this volume into one coherent picture of what failure this platform absorbs without ever escalating to this section's DR procedures at all.



18. High Availability
18.1 Purpose — the synthesis section

Every prior section in this volume specified one component's HA design individually — Kubernetes' Pod Disruption Budgets (Section 3.4), Redis Sentinel (Section 12.2), PostgreSQL Patroni-managed replication (Section 13.2), Vault Raft consensus (Section 15.2). This section is the deliberate synthesis: one table showing what failure this platform survives without customer-visible impact, and what failure escalates to Section 17's disaster-recovery procedures instead — the single artifact that answers "how available is Nexus, really" comprehensively, rather than requiring that answer to be assembled by reading every component section individually.

18.2 The failure/survival matrix
Failure scenario  Survives without impact?  Mechanism Escalates to DR if...
Single nexus-api pod crash  Yes Kubernetes restarts pod; other replicas serve traffic (Section 3.3's multi-replica baseline)  Never — routine, self-healing
Single nexus-orchestration pod crash mid-run  Yes, run-level  Volume 1, Part III §16.7's per-step commits mean only the in-flight step is lost, not the run; Kubernetes reschedules the pod Never — routine, and specifically the scenario Volume 1's Unit of Work fix (this project's own mid-session correction) was designed to handle correctly
Single Kubernetes node failure  Yes Pod rescheduling to healthy nodes; PDB (Section 3.4) ensures this doesn't cascade Only if node loss rate exceeds cluster's spare capacity
PostgreSQL primary failure  Yes, brief (seconds to low minutes) Patroni-orchestrated automatic failover to synchronous replica (Section 13.2) If failover itself fails or replica is also unavailable
Redis primary failure Yes, brief  Sentinel-orchestrated failover (Section 12.2) If all replicas are simultaneously unavailable
Vault node failure (1 of 3+)  Yes Raft consensus tolerates minority node loss (Section 15.2)  If quorum is lost (majority of nodes down)
Single availability zone failure (managed/hosted) Yes, if multi-AZ deployed Node pools (Section 3.8) and stateful components (13.2, 12.2, 15.2) spread across AZs If the affected AZ hosts a disproportionate, unreplicated share of stateful components
AI provider outage  Yes Volume 3's AiProvider abstraction (Part IV §14) + fallback chains (§15.6) + graceful AiUnavailableError degradation (NFR-REL-3) Never for core platform; AI capability itself is unavailable until provider recovers, by design
Full region loss  No  — Always — Section 17's DR procedures
Correlated multi-component failure (e.g., a bad deployment breaking multiple services simultaneously) Depends Section 5.6's rollback procedure, if caught before widespread impact  If rollback itself is insufient or the root cause is data corruption, not just bad code
18.3 Reading this matrix — what "survives without impact" actually means

Every "Yes" in this table means specifically: no customer-visible availability impact beyond a brief, sub-SLO-threshold blip (the failover windows Section 13.2 and 12.2 specify are measured in seconds to low minutes, well within NFR-REL-1's error budget for routine operational events) — it does not mean zero internal disruption. A PostgreSQL failover, for instance, briefly increases connection errors at the PgBouncer layer (Section 13.3) during the failover window itself; this is expected, monitored (Section 7's alerting), and does not constitute an SLO breach given its brief duration, but it is not literally invisible at the infrastructure level, only at the customer-experience level this matrix is scoped to describe.

18.4 Multi-AZ as the load-bearing assumption behind most "Yes" answers

Section 18.2's table makes clear that a large fraction of this platform's HA posture depends on multi-AZ deployment specifically (18.2's AZ-failure row, and implicitly, every stateful-component row above it, since Patroni/Sentinel/Raft's automatic failover only protects against AZ-level failure if the replicas they failover to are genuinely in a different AZ, not just a different node in the same AZ). This is stated explicitly here because it's a deployment-configuration decision (Section 3.8's node-pool design, and the corresponding Helm values, Section 4.3) that a cost-conscious or smaller self-hosted deployment might reasonably choose not to replicate — Architecture Principle 20.8's self-hosted-first posture means this volume documents multi-AZ as the recommended, HA-complete configuration while being honest that a smaller, single-AZ self-hosted deployment is a supported, lower-cost alternative with a correspondingly weaker AZ-failure row in this matrix, a tradeoff that deployment profile's operators knowingly accept.

18.5 HA testing — continuous, not just Section 17's periodic game days

Distinct from Section 17.5's quarterly DR game days (which test full-scale recovery from severe failure): the routine HA mechanisms in this section's matrix (pod rescheduling, database failover, Redis failover) are exercised far more frequently, semi-continuously, via chaos-engineering practices — scheduled, automated fault injection (randomly terminating a non-critical pod, briefly partitioning a node) run against staging (Section 5.3) on a regular cadence, verifying that the "Yes, survives without impact" claims in Section 18.2's table remain true as the system evolves, rather than being verified once at initial design time and assumed to hold indefinitely afterward.

18.6 HA and the cost tradeoff — forward reference

Every "Yes" in Section 18.2's table has a real infrastructure cost (running replicated PostgreSQL/Redis/Vault instances, multi-AZ node pools, standby capacity) — this tradeoff is not hidden or free, and Section 21 (Cost Optimization) specifically addresses how this cost is managed and right-sized per deployment tier without silently eroding the HA guarantees this section documents; the two sections are companion pieces, not independent concerns, consistent with Volume 3, Section 16.9's explicit refusal to treat cost optimization as free everywhere this document set has confronted the same tension.

18.7 Closing note

This matrix is the artifact Persona Elena's security/reliability review (Volume 0, UC-7) and Sofia's leadership reporting (Volume 0, UC-5, extended to infrastructure trust questions a customer's own leadership might raise) can both be shown directly — the concrete, comprehensive answer to "what happens when X fails" that this entire volume's component-by-component specification was building toward.



19. Scaling
19.1 Purpose — the capacity-planning synthesis

Volume 1, Part VI, Section 39 specified scaling architecture — which dimensions scale and by what mechanism. This section specifies the operational capacity-planning practice: how this volume's concrete mechanisms (HPA, PgBouncer, partitioning) are actually sized, monitored, and adjusted over time against Volume 1's NFR-SCALE targets, and — consistent with this document set's practice of honesty about limits — restates Volume 1, Section 39.9's known scaling ceilings without softening them.

19.2 Scaling mechanism inventory — mapping NFR-SCALE targets to this volume's concrete infrastructure
NFR-SCALE target  Volume 1's architectural mechanism  This volume's concrete implementation
NFR-SCALE-1 (500 concurrent executions) Horizontal pod scaling on custom metric Section 3.7, 9.4's HPA + prometheus-adapter pipeline
NFR-SCALE-2 (horizontal scaling, zero client-facing change) Stateless containers, Redis-coordinated state Section 12.7's SignalR backplane, Section 3.3's resource sizing
NFR-SCALE-3 (10M flow-run records)  Indexing, then partitioning when needed Section 13.5's partitioning activation trigger

Every NFR-SCALE target Volume 1 committed to now has a traceable, concrete infrastructure mechanism specified somewhere in this volume — this table exists so that traceability is visible in one place rather than requiring a reader to independently discover which of this volume's many sections implements which Volume 1 commitment.

19.3 Capacity planning cadence

Resource requests/limits (Section 3.3), HPA thresholds (Section 3.7), PgBouncer pool sizing (Section 13.3), and Redis memory allocation (Section 12.4) are reviewed against actual observed usage on a monthly cadence — directly extending Section 3.3's "sized with headroom, validated periodically" principle into a formal, scheduled practice rather than an ad hoc one, with findings feeding both this section's capacity documentation and, where a genuine scaling-mechanism gap is found, a proposal to adjust the mechanism itself (not merely its configuration) — the same escalation path Volume 1, Part VI, Section 39.6 already established for when indexing alone becomes insufficient and partitioning must actually activate.

19.4 Growth projection and headroom

Capacity planning maintains a rolling 6-month growth projection (derived from Volume 0's Roadmap phasing, Part III Section 15, and observed tenant onboarding rate) against every mechanism in 19.2's table — HPA's maximum replica ceiling, PgBouncer's maximum pool size, the Kubernetes cluster's own node-pool capacity ceiling — ensuring headroom exists before growth demands it, not discovered reactively when a scaling mechanism's own configured ceiling becomes the actual bottleneck rather than the underlying resource it was meant to manage. This is the concrete operational practice behind Section 7.4's error-budget philosophy applied to capacity specifically: a capacity ceiling approaching its limit is a Warning-severity signal (Section 7.5), not a surprise discovered only when NFR-SCALE-1's 500-execution target is actually exceeded and requests start queuing or failing.

19.5 Load testing cadence — restating and scheduling Volume 1's gate

Volume 1, Part VI, Section 38.8 established load testing as a pre-release gate (Section 5.2, stage 8, of this volume's CI/CD pipeline). This section adds the complementary, non-release-triggered practice: a scheduled, standalone load test (independent of any specific release) runs quarterly against a full-scale staging replica, specifically to validate that 19.4's growth-projection assumptions still hold against the current actual system, not just against the system as it existed whenever the most recent release happened to trigger the pipeline's own gate — catching a slow, cumulative capacity drift (many small, individually-passing releases each adding a small amount of overhead) that no single release's own gate would individually flag.

19.6 Known scaling ceilings — restated honestly, not softened

Directly restating Volume 1, Part VI, Section 39.9's explicit, deliberate disclosure: the Mobile module's device/session availability and the Desktop UI module's single-machine-execution-context nature remain genuine scaling ceilings this volume's infrastructure does not attempt to engineer around — no Kubernetes mechanism, no database partitioning strategy, and no HPA configuration changes that constraint, because it is inherent to what those two modules automate (a physical or emulated device, a single OS session), not an infrastructure limitation this volume's tooling could address even in principle. This section adds one further, infrastructure-specific ceiling worth naming with the same honesty: the nexus-sandbox-runner pod pool (Volume 1, Part III, Section 18.5; this volume's Section 3.2, 3.8) has a genuine, real ceiling on concurrent Community-tier module executions, bounded by the sandbox node pool's own capacity (Section 3.8) — a tenant running an unusually large number of concurrent Community-tier-module-dependent flows could, in principle, encounter sandbox-capacity queuing that a Verified-tier or Built-in-module-only flow would never experience, a direct, honest consequence of the trust-tiered execution model's own architecture (Volume 1, Part III, Section 18.5) that this volume's infrastructure correctly implements rather than papers over.

19.7 Scaling and multi-tenancy fairness — restating Volume 1's rate-limiting role

Directly restating Volume 1, Part VI, Section 39.8's observation that Volume 1's tenant-scoped rate limiting (Volume 1, Part V, Section 30.7) serves double duty as both an abuse-prevention and a scaling-fairness mechanism in the pooled managed/hosted model — this section adds the infrastructure-configuration detail: rate-limit thresholds (enforced via the Redis-backed sliding-window implementation, Section 12.6) are themselves reviewed as part of 19.3's monthly capacity-planning cadence, ensuring one large tenant's legitimate, growing usage doesn't silently consume a disproportionate share of the platform's shared capacity ceiling (19.4) at the expense of other tenants' fair access to the same shared infrastructure.

19.8 Scaling documentation as a sales/procurement artifact

Consistent with Section 18.7's observation that the HA matrix serves Persona Elena's and Sofia's review needs directly: this section's capacity-planning documentation (19.2's mechanism table, 19.6's honestly-disclosed ceilings) is maintained in a form suitable for direct use in enterprise sales and procurement conversations — a prospective enterprise customer asking "what happens at our expected scale" receives this section's actual, tested answer, including its honestly-disclosed limits, rather than an unsubstantiated assurance — directly reinforcing Volume 0, Section 8.6's "credibility tax" risk mitigation: a platform that's honest about where its ceilings are is more credible, not less, to a sophisticated enterprise buyer evaluating a genuine infrastructure commitment.



20. Backup Strategy
20.1 Purpose — the concrete mechanism behind Section 17's RPO targets

Section 17.2 stated RPO targets (5 minutes for PostgreSQL, 15 minutes for Vault) without specifying the backup mechanism producing them. This section specifies that mechanism precisely, per component, plus the retention policy and — consistent with Section 17.5's "untested claims are inadequate" discipline — the restore-testing cadence that keeps these backups genuinely usable, not merely present.

20.2 PostgreSQL — continuous WAL archiving plus periodic base backups

Directly implementing Section 13.7's forward reference: continuous WAL (Write-Ahead Log) shipping to object storage (S3-compatible, chosen for broad availability across both managed-cloud and self-hosted-with-MinIO-or-equivalent scenarios) runs constantly, with a full base backup taken daily — this combination is what enables point-in-time recovery to any moment within the retention window (20.5), not merely restoration to the most recent daily snapshot, directly producing the 5-minute RPO Section 17.2 committed to (bounded by WAL-shipping lag, monitored per Section 7 as a named metric with Warning-severity alerting if lag exceeds a defined threshold).

20.3 Redis — persistence as backup, not a substitute for it

Section 12.3's AOF persistence configuration provides crash-recovery durability for a single Redis restart, but is explicitly not treated as a substitute for genuine backup — Redis's RDB snapshots are additionally exported to object storage on a scheduled cadence (hourly), specifically because AOF alone protects against process crash but not against a corrupted or accidentally-flushed dataset, a distinct failure mode a point-in-time RDB snapshot in separate, immutable object storage protects against that AOF's continuously-rewritten log file does not.

20.4 Vault — Raft snapshots

Vault's integrated Raft storage (Section 15.2) is backed up via Vault's native snapshot capability, taken every 15 minutes (directly producing Section 17.2's 15-minute Vault RPO) and shipped to the same object storage backend as PostgreSQL and Redis backups — critically, Vault snapshots themselves contain encrypted data (Vault's own storage-layer encryption), meaning the snapshot's own security posture doesn't depend on the backup storage location's access control alone, a deliberate defense-in-depth property directly relevant given what Vault snapshots actually contain (this platform's entire secrets and envelope-encryption key material, Section 14.4).

20.5 Retention policy
Data type Hot retention Cold retention  Rationale
PostgreSQL WAL + base backups 7 days (immediate PITR window)  90 days (compressed, less-frequent access tier) Balances immediate-recovery flexibility against long-tail incident investigation needs
Redis RDB snapshots 24 hours (hourly snapshots) 7 days (daily rollup) Redis holds ephemeral/cache-adjacent data (Section 12.1's roles) — even the permission-cache role, the most durability-sensitive of the three, self-heals from PostgreSQL as source of truth within one cache-refresh cycle, so long-tail retention has limited marginal value
Vault Raft snapshots  7 days (every 15 minutes) 30 days (daily rollup)  Matches the elevated but not indefinite retention need for this platform's most security-critical backup

This tiered structure directly mirrors the hot/cold pattern already established for operational logs (Volume 1, Part IV, Section 28.6) and Elasticsearch's ILM policy (Section 11.3) — the same cost/utility reasoning applied consistently across every retained-data category this platform manages, rather than each backup type inventing its own independent retention philosophy.

20.6 Cross-region backup storage

For the managed/hosted deployment's secondary-region DR posture (Section 17.3), backups are replicated cross-region as part of the object storage layer's own configuration (standard cross-region replication, not a Nexus-built mechanism) — ensuring a full primary-region loss doesn't also take the backups needed to recover from that loss with it, a specific, easy-to-overlook failure mode (backups stored only in the same region/failure domain as the data they protect) this section deliberately guards against explicitly.

20.7 Restore testing — the discipline that makes a backup genuinely trustworthy

Directly extending Section 17.5's game-day philosophy to backups specifically, at a higher frequency given how foundational backup integrity is to every other resilience claim in this volume: an automated, weekly restore-test pipeline provisions a fresh, isolated environment, restores the most recent PostgreSQL base backup plus WAL replay to a specific recent point in time, restores the most recent Vault snapshot, and runs Volume 2, Section 19.9's E2E smoke-test subset against the restored environment — verifying not just that the restore operation completes without error, but that the restored data is actually usable by the actual application, the specific, stronger verification this document set's "untested claims are inadequate" discipline (Section 17.5) requires rather than accepting "the restore command exited successfully" as sufficient evidence of a genuinely working backup.

20.8 Backup encryption and access control

Every backup artifact (PostgreSQL, Redis, Vault) is encrypted at rest in object storage using a key managed independently of the backup storage location itself (mirroring Section 14.4's envelope-encryption principle — the key protecting the backup is never stored alongside the backup it protects), with access to the backup storage location itself gated by the same least-privilege Vault-issued dynamic credentials (Section 15.5) used for other operational database access — a backup is exactly as sensitive as the data it contains, and this section's access-control posture treats it with that seriousness rather than as a lower-scrutiny operational afterthought.

20.9 Self-hosted backup responsibility

Directly extending Section 17.3's DR-posture distinction to backups specifically: self-hosted deployments receive the same backup tooling this section specifies (the WAL-archiving configuration, the Vault snapshot schedule, the restore-test pipeline definition, all shipped as part of the Helm chart, Section 4) but execution — where backups are actually stored, cross-region replication if any, and the restore-testing cadence — is the customer's own operational responsibility, consistent with Architecture Principle 20.8's self-hosted operational-ownership boundary already established throughout this volume. Nexus's documentation (this section) specifies the recommended, tested configuration; a self-hosted customer's own infrastructure team decides whether and how closely to follow it for their own environment.



21. Cost Optimization
21.1 Purpose and scope — infrastructure cost, distinct from Volume 3's AI cost

A scope clarification worth stating precisely, given this is the second "cost optimization" section this document set has specified: Volume 3, Section 16 addressed AI provider cost (tokens, model tiers, routing policy) — a cost driven by usage of a third-party service this platform doesn't control the pricing of. This section addresses infrastructure cost — the Kubernetes cluster, database, storage, and networking spend this platform's own architecture directly determines through its resource-sizing and deployment-topology choices. The two are related (Section 21.7 shows exactly how) but are governed by genuinely different levers, which is why this document set treats them as two distinct sections rather than one merged discussion.

21.2 Compute right-sizing — closing the loop on Section 3.3 and 19.3

Directly extending Section 3.3's resource-request/limit sizing and Section 19.3's monthly capacity-review cadence with an explicit cost lens: the same periodic review that validates capacity headroom (19.3) also flags over-provisioning — a component whose actual observed resource usage sits persistently well below its configured requests represents pure wasted spend, not just unused headroom, and is right-sized downward with the same rigor 19.4's growth-projection process applies to sizing upward. This bidirectional discipline (review can trigger either a scale-up or scale-down recommendation) is stated explicitly because capacity-planning processes commonly develop an asymmetric bias toward only ever adding headroom, never reclaiming it — this section's review process is designed against that specific bias.

21.3 Storage tiering

Directly extending Section 9.3's Prometheus retention tiers, Section 11.3's Elasticsearch ILM policy, and Section 20.5's backup retention tiers with the cost rationale underlying all three: hot-tier storage (fast, expensive, low-latency) is scoped as narrowly as each data type's genuine access-pattern needs require (Section 9.3's 15-day raw-metric window, Section 11.3's 14-day log hot-tier), with automatic transition to cold-tier (cheaper, higher-latency) storage for the remainder of each retention window — this is not a new policy introduced here; this section's contribution is naming the cost rationale that was implicit in those three sections' retention designs from the start, now made explicit as this volume's unifying cost principle rather than three independently-justified tiering decisions that happened to look similar.

21.4 Image size as a cost lever, not just a security one

Directly restating Section 2.3's Alpine-base-image and Section 2.4's layer-caching decisions through a cost lens Section 2 itself only partially addressed: smaller images mean lower registry storage cost, faster (cheaper) CI build and image-pull time (Section 2.9's build-time budget), and reduced data-transfer cost for multi-region image distribution (relevant to Section 17.3's secondary-region DR posture, which requires images available in a second region too) — Section 2's security-driven minimal-image-size decision and this section's cost-driven interest in the same property are two independent justifications converging on the identical engineering practice, worth noting explicitly as a case where security and cost optimization align rather than compete, a useful contrast to Volume 3, Section 16.9's more common case of cost and quality genuinely trading off against each other.

21.5 Reserved capacity vs. on-demand

For the managed/hosted deployment's baseline, predictable capacity (Section 19.4's growth-projection floor), reserved/committed-use infrastructure pricing (available from cloud providers at a meaningful discount relative to on-demand pricing, in exchange for a committed spend term) covers the steady-state baseline, while HPA-driven burst capacity (Section 3.7, 9.4) above that baseline uses on-demand pricing — this hybrid approach directly serves cost efficiency for the predictable majority of load while preserving Section 3.7's autoscaling responsiveness for genuine demand spikes, rather than either over-committing to reserved capacity that would sit idle most of the time or paying on-demand rates for load that's actually entirely predictable.

21.6 Sandbox execution cost — the trust-tiered model's cost dimension

Directly extending Volume 1, Part III, Section 18.5's trust-tiered execution model with its cost consequence, a dimension that section didn't itself address: Community-tier sandboxed module execution (Section 3.2, 3.8's dedicated nexus-sandbox node pool) carries meaningfully higher per-execution infrastructure cost than in-process Built-in/Verified-tier execution, given the dedicated pod-per-invocation isolation model (Volume 1, Part III, Section 18.5) — this cost difference is a legitimate input to Volume 0, Section 16's licensing/pricing considerations for marketplace-heavy usage patterns, flagged here as a concrete infrastructure-cost data point that pricing strategy (owned by Volume 0) should account for, consistent with this document set's practice of surfacing cross-volume-relevant findings explicitly rather than leaving them for another volume to independently rediscover.

21.7 The unified Cost Overview — infrastructure and AI cost, correlated

Directly completing Section 10.3's "Cost Overview" dashboard, named there but not detailed: infrastructure cost (this section) and AI provider cost (Volume 3, Section 16.2's AiCostRecord data) are surfaced on the same dashboard, correlated by time and, where meaningful, by tenant (for the enterprise cost-visibility view, Volume 3, Section 16.8, extended here to include the infrastructure-cost-per-tenant estimate the pooled model's resource-attribution tagging, Section 8.7, makes computable) — giving engineering leadership (this dashboard's primary internal audience, per Section 10.3's table) one place to answer "what does it cost us to run Nexus, in total" rather than needing to separately consult this volume's infrastructure billing and Volume 3's AI provider billing as two disconnected data sources.

21.8 Cost as a release consideration, not just an operational afterthought

Consistent with Volume 3, Section 16.1's framing of cost governance as "a first-class product capability, not an operational afterthought," extended here to infrastructure specifically: a proposed architectural change with a material infrastructure-cost impact (a new stateful dependency, a significant resource-sizing increase) is required to include a cost-impact estimate in its design review, using the same ADR-for-deviations process Volume 1, Part VI, Section 40.8 established — cost is treated as a design constraint reviewed at decision time, not a bill discovered after the fact and addressed reactively.

21.9 Closing note — Volume 4 complete

This section closes Volume 4 in full: Part I specified the developer and containerization foundation, Part II the pipeline that ships changes safely, Part III the observability stack that watches the running system, Part IV the data and secrets infrastructure protecting it, and this Part — Disaster Recovery, High Availability, Scaling, Backup Strategy, and now Cost Optimization — the resilience and operational discipline that keeps all of it running reliably and affordably over time. Every infrastructure decision in this volume traces back through Volume 1's NFRs and architecture principles to Volume 0's original business objectives, completing the fourth of six volumes in this document set. Volume 5 (Development Standards & Engineering Playbook) now turns to the human and process discipline — git workflow, code review, release management, and team practices — that governs how the system this and the prior three volumes specified is actually built and maintained day to day.

______________________________________________________________________________________________________________________________________________________________________



# Volume 5 — Development Standards & Engineering Playbook · Part I — Source Control Workflow

## 1. Git Branching Strategy

### 1.1 Purpose — the branch model Volume 4's pipeline already assumes

Volume 4, Section 5.3 specified environments (local, ephemeral-per-PR, staging, production) and Section 6.7 specified branch protection on `main` — both sections implicitly assumed a specific branching model without this volume having yet defined it. This section closes that gap: Nexus uses **trunk-based development**, not GitFlow or a long-lived release-branch model, and this section states precisely why, given everything Volumes 1–4 already committed to.

### 1.2 The model — one long-lived branch, short-lived feature branches

```
main (protected, always deployable — Volume 4 §5.3, §6.7)
 │
 ├── feature/flow-step-retry-policy    (short-lived, days not weeks)
 ├── fix/signalr-reconnect-race         (short-lived)
 └── feature/locator-generator-v2       (short-lived, even for Volume 3 capability work)
```

Every feature branch merges directly to `main` via pull request (Section 2), never through an intermediate `develop` or `release` branch — `main` is the single source of truth or trunk, continuously deployable, exactly as Volume 4, Section 5.4's ephemeral-per-PR-environment design already assumes (that design only makes sense against a trunk-based model; a long-lived `develop` branch would make "deploy this PR's exact state" an ambiguous question relative to which branch it's ultimately headed toward).

### 1.3 Why trunk-based, not GitFlow — reasoning from what this document set already committed to

This is not a default, unexamined choice — it follows directly from decisions already made: Volume 4, Section 5.2's nine-stage pipeline runs in full on every merge to `main`, and Section 5.5 established that production promotion is a *timing* decision, not a *correctness* gate (correctness is fully automated). GitFlow's long-lived `release` branches exist specifically to stabilize a release candidate over time before it ships — a pattern that only makes sense when correctness verification is slow, manual, or incomplete at merge time. Volume 4's pipeline is none of those things by design (Section 5.2's full gate suite runs before merge is even permitted, per Section 6.7's required-status-checks), which makes a stabilization branch pure overhead: there is no additional correctness work left to do on a release branch that the trunk-based pipeline hasn't already done before merge.

### 1.4 Branch naming and scope discipline

```
feature/<short-description>    # new capability, any volume's scope
fix/<short-description>         # bug fix
chore/<short-description>       # tooling, dependency, non-functional change
docs/<short-description>        # this document set itself, or code documentation
```

Every branch is scoped to a single, reviewable unit of change — directly serving Section 2's PR-size guidance and, more fundamentally, Volume 1, Part VI, Section 40.8's ADR-for-deviations discipline, which depends on a change being small and coherent enough that its architectural rationale (if any) is legible in one review, not buried inside an unrelated bundle of changes.

### 1.5 Branch lifetime — short by policy, not just convention

Feature branches are expected to merge within days, not weeks — a branch living longer than a defined threshold (e.g., 5 business days) triggers an automated reminder, not because long-lived branches are forbidden outright, but because they directly work against trunk-based development's core value: the longer a branch diverges from `main`, the more expensive its eventual merge-conflict resolution and the less its ephemeral-per-PR environment (Volume 4, Section 5.4) reflects what `main` will actually look like once it lands. Large, genuinely multi-week efforts (a new capability engine in Volume 3's scope, for instance) are broken into a sequence of smaller, individually-mergeable branches behind a feature flag (Section 1.6) rather than developed on one long-lived branch.

### 1.6 Feature flags — how incomplete work reaches `main` safely

Directly enabling Section 1.5's short-branch-lifetime discipline: incomplete or not-yet-customer-visible functionality merges to `main` behind a feature flag (a simple, centrally-managed boolean/percentage-rollout configuration, distinct from and simpler than Volume 3, Part IV's AI-specific tenant configuration, though following the same "configuration, not code branching" philosophy) — this is what makes "merge early and often" compatible with "never ship incomplete work to a customer," the two properties trunk-based development needs to hold simultaneously to actually work.

### 1.7 Hotfix handling — no separate branch model

Consistent with Section 1.3's reasoning: an urgent production fix follows the *exact same* `fix/` branch → PR → full pipeline → merge → deploy path as any other change, never a separate, abbreviated "hotfix branch" process that skips gates for the sake of speed. This is a deliberate, hard rule directly extending Volume 4, Section 6.7's "no administrator override" branch-protection posture to incident response specifically — the temptation to bypass review and testing under incident pressure is exactly when Volume 1, Architecture Principle 20.4's fail-loud discipline matters most, not a scenario where it's suspended. Volume 4, Section 5.4's ephemeral-per-PR environment makes this fast enough in practice (a hotfix's correctness is verified against a real, isolated environment within minutes, not hours) to not meaningfully trade safety for speed.

### 1.8 Tagging and release correlation

Every merge to `main` that reaches production (Volume 4, Section 5.2, stage 9) is tagged with a semantic version (Section 13, Release Management, specifies the full versioning scheme) — this tag is the same artifact Volume 4, Section 2.6's immutable image-tagging and Section 4.5's Helm chart versioning both correlate against, making "what code is running in production right now" answerable by a single, authoritative Git tag rather than requiring cross-referencing multiple, potentially-drifting version identifiers across this document set's various volumes' artifacts.

### 1.9 Relationship to Volume 1's bounded-context team ownership

Directly extending Volume 1, Part II, Section 7.6's observation that bounded contexts are natural team-ownership units, and Volume 4, Section 7.6's per-SLI ownership model: branch naming (1.4) and PR review assignment (Section 2) both route through this same ownership structure — a branch touching `features/flow-authoring` (Volume 2, Section 5.4) or the Flow Authoring bounded context (Volume 1, Part II, Section 7) is reviewed by that context's owning team by default, giving this volume's process discipline a concrete, already-established organizational structure to route through rather than inventing a new one.



2. Pull Request Guidelines
2.1 Purpose — the unit of change this entire document set's gates apply to

Every quality gate specified across Volumes 1–4 (Volume 1, Part VI, Section 40's coding-standard analyzers; Volume 4, Section 5.2's nine-stage pipeline) fires against a pull request. This section specifies what a well-formed PR looks like — size, description content, and the linkage between a PR and the architectural decisions it might represent — so those gates have a consistently well-shaped artifact to run against.

2.2 PR size — small by policy, with a stated exception process

Target PR size is under 400 lines changed (excluding generated code — Volume 2, Section 4.7's generated DTO types, Volume 1's EF Core migrations — which don't count against this budget since they're not hand-authored, reviewable logic). This is a guideline enforced by CI-surfaced warning (not a hard block, since a small number of legitimate changes — a large but mechanical rename, a generated-migration-heavy schema change — genuinely exceed this size without being harder to review) but treated as the default expectation, directly serving Section 1.5's short-branch-lifetime discipline and the basic principle that a smaller PR gets a more careful review than a large one, an effect worth designing process around rather than treating as an unfortunate but unavoidable human limitation.

2.3 PR description template
markdown
## What
[One or two sentences: what does this change do]

## Why
[The motivating requirement, bug, or improvement — link to an issue/ticket]

## Which volume(s) / bounded context(s) does this touch
[Volume 1 Flow Authoring / Volume 2 flow-authoring feature / Volume 4 CI pipeline / etc.
 — directly informs reviewer routing, Section 1.9]

## ADR required?
[ ] Yes — this deviates from or extends a pattern specified in Volumes 0-4 (link the ADR)
[ ] No — this follows existing, already-specified patterns

## Testing
[What tiers of Volume 1 §40.6 / Volume 2 §19.2 / Volume 3 §19 / Volume 4 §5.2 testing
 does this change add or modify coverage for]

## Security/compliance impact
[Per Volume 1 Architecture Principle 20.5's "security designed in" — does this touch
 authentication, authorization, tenant isolation, secrets, or AI guardrails? If yes, detail.]

The "ADR required?" checkbox directly operationalizes Volume 1, Part VI, Section 40.8's rule that "any code change that deviates from a pattern specified in this volume... requires an accompanying ADR" — making that rule a literal, unskippable checklist item on every PR rather than a norm a reviewer must remember to check for independently.

2.4 Draft PRs and early feedback

A PR may be opened as a draft before it's ready for full review, deliberately triggering Volume 4, Section 5.4's ephemeral-per-PR environment and the fast-feedback tiers of Section 5.2's pipeline (lint, unit tests) without yet requesting human review time — this is the concrete mechanism for getting early, automated signal on a large or uncertain change before investing a reviewer's attention, directly serving Section 1.5's goal of catching problems while a branch is still young and cheap to redirect.

2.5 Linking commits to PRs — squash merge as the default

main's history (Section 1.2) consists of one commit per merged PR (squash-merge), with the PR's description (2.3) becoming that commit's message — this keeps main's history legible at the level that actually matters for future archaeology (what change, why, per Section 2.3's template) rather than preserving every intermediate "fix typo" / "address review comment" commit a branch's development process naturally accumulates. Commit message format for the squashed commit follows Section 4's Commit Conventions specification exactly.

2.6 Cross-volume PRs — when a change spans multiple bounded contexts

A PR touching both a Volume 1 backend command (Part II, Section 13) and its Volume 2 frontend consumer (Section 8's TanStack Query hook) is not split into two separate PRs by default — Section 1.9's ownership-routing model handles this by requesting review from both owning teams on the single PR, since Volume 2, Section 4.7's generated-DTO-type contract means the backend and frontend changes are often genuinely coupled (a backend DTO shape change requires a corresponding frontend regeneration in the same unit of deployable change) and splitting them would create an interim, broken state between the two PRs' merges.

2.7 PR review SLA

A PR requesting non-draft review receives an initial response (approval, requested changes, or a clarifying question) within one business day — directly serving Section 1.5's short-branch-lifetime goal, since a PR waiting days for its first review is functionally a long-lived branch regardless of how small the diff itself is. This SLA is a team-level commitment tracked as a process health metric (Volume 4, Section 7's monitoring philosophy extended to engineering process itself, not just running infrastructure), not merely an aspiration.

2.8 Reverting — a normal, low-friction action

Any merged PR can be reverted by any engineer without requiring the same review SLA (2.7) that landed it — a revert is treated as a safety action, not a normal change, and Volume 1, Architecture Principle 20.4's fail-fast philosophy extends here directly: if a merged change is suspected of causing a regression, reverting first and investigating afterward (rather than debugging forward under production pressure) is the explicitly sanctioned default response, directly consistent with Volume 4, Section 5.6's rollback-integration design at the infrastructure layer, now stated as the equivalent source-control-level practice.

2.9 What a PR is not a substitute for

Consistent with Section 2.1's framing: a PR review is not where architectural decisions get made for the first time — a change significant enough to need an ADR (2.3's checkbox) should have that ADR drafted and, ideally, discussed before the implementing PR is opened, not negotiated for the first time in PR review comments. This mirrors Volume 1, Part VI, Section 40.8's explicit instruction that a deviation be "reviewed and approved by the architecture owner... before merge, not merged first and documented later" — PR review verifies correct implementation of an already-agreed decision, it is not the forum for making that decision.



3. Code Review Checklist
3.1 Purpose — one consolidated checklist for scattered review requirements

Across Volumes 1 through 4, more than a dozen sections established a specific review-time check without any of them being collected into one place a reviewer could actually use — "a reviewer checks that X" appears repeatedly, each time in the section that originally motivated it. This section is the consolidation: the master checklist every PR reviewer works from, with each item citing its origin so a reviewer (or a future volume revision) can trace back to the full reasoning, not just the rule.

3.2 Domain and architecture integrity
 Strongly-typed identifiers used, never raw Guid/int in public signatures (Volume 1, Part II, §10.8)
 No cross-aggregate direct reference — aggregates communicate only via injected port interfaces, never holding another aggregate's repository or instance (Volume 1, Part II, §9.5; Part VI, §40.3)
 No project reference from Nexus.Domain to any outer layer — dependency direction intact (Volume 1, Part V, §36.3)
 Composition vs. association used correctly — a parent aggregate's owned entities use composition; cross-aggregate references use identifiers only (Volume 1, Part V, §35.4, §35.6)
 Command handlers check authorization before loading any aggregate — EnsurePermission is the first meaningful line (Volume 1, Part II, §13.6; Part IV, §25.3; Part VI, §40.3)
 New commands inherit AuditableCommandHandler, or carry an explicit, reviewed justification for exemption (Volume 1, Part IV, §27.2; Part VI, §40.3)
 TenantId is never derived from a client-supplied header, query parameter, or any input other than ITenantContextAccessor (Volume 1, Part IV, §26.4)
3.3 Plugin and module-parity integrity
 No ModuleKind-specific branching in orchestrator-core or frontend authoring/canvas code — the module-parity principle (Volume 1, Architecture Principle 20.3; Volume 2, UX Principle 1.3–1.4)
 New IAutomationModule interface changes are additive-only within a minor/patch contract version (Volume 1, Part III, §18.7)
 Singleton-lifetime services hold no new mutable instance state — reviewed explicitly for any field added to a built-in module (Volume 1, Part III, §17.5, §40.3)
 A change to the module-parity test (Volume 2, §19.5) is itself reviewed with elevated scrutiny — this test's own weakening is a standing risk, not just its failure
3.4 Data and query integrity
 New queries against flow_runs, step_results, or audit_entries include EXPLAIN ANALYZE output in the PR if the table is expected to scale toward NFR-SCALE-3 (Volume 1, Part VI, §38.6, §40.3)
 Row-level security policy coverage verified for any new multi-tenant table (Volume 1, Part IV, §26.3; Volume 4, §13.4)
 DTOs, never domain entities, cross the API boundary (Volume 1, Part II, §14.8)
3.5 Security and secrets
 No credential-shaped field on any capability-engine or module input type (Volume 3, Part V, §18.3)
 No raw secret value in any Helm values file, at any tier (Volume 4, §4.8)
 [SensitiveValue]-attributed fields correctly excluded from audit metadata and log derivation (Volume 1, Part IV, §27.4, §28.5)
 New NetworkPolicy or Kubernetes RBAC changes reviewed with elevated attention — the highest-security-impact category of infrastructure change (Volume 4, §3.5, §4.9)
 New container images pass CVE scanning before merge is possible — automated gate, but a reviewer confirms no scan-suppression was added without justification (Volume 4, §2.5)
3.6 AI-specific integrity
 New capability-engine output types enforce structured output — no free-text field a downstream consumer must parse heuristically (Volume 3, Part I, §3.4)
 Every AiPlanningResult field is populated on every code path — guardrailChecks and costRecord are never optional (Volume 3, Part I, §2.3)
 New guardrail checks are additive, never disabling or weakening a platform-baseline check (Volume 3, Part V, §17.5, §17.6)
 New capability-engine changes include an evaluation-set update and pass the offline gate before merge (Volume 3, Part V, §19.3)
3.7 Frontend-specific integrity
 Import boundaries respected — primitives/ → composites/ → features/*/ → pages/, no upward or lateral violation (Volume 2, §5.3)
 Server state never duplicated into local component state (Volume 2, §4.5, §8.2)
 No hardcoded design-token value — colors, spacing, motion all reference --nexus-* custom properties (Volume 2, §2.9, §3.2)
 New interactive components implement their accessibility contract — keyboard operability, ARIA roles — before merge, not as a follow-up (Volume 2, §18.4, §18.8)
3.8 Documentation and traceability
 "ADR required?" checkbox (Section 2.3) correctly answered — a deviation from an established pattern has a linked ADR
 New ActionSchema/ParamSpec entries have non-empty Description fields (Volume 1, Part VI, §40.7)
 New dashboards, runbooks, or alert rules are versioned alongside the code they describe, not created ad hoc outside version control (Volume 4, §7.7, §10.2)
3.9 How this checklist is maintained

This checklist is not frozen at this volume's publication — per Volume 1, Part VI, Section 40.8's ADR discipline extended to this document set's own review process: a new pattern established anywhere in Volumes 0–4 (or a future amendment to any of them) that implies a new review-time check is added here, in the same PR that establishes the pattern, keeping this section synchronized with the architecture it enforces rather than drifting stale — directly the same "documentation as code, updated in the same change that necessitates it" discipline this document set has applied consistently since Volume 1.

3.10 What this checklist does not replace

This checklist covers the specific, previously-scattered rules this document set has already established — it is not a substitute for a reviewer's own judgment about code clarity, test adequacy, or whether a change actually solves the problem it claims to. A PR that satisfies every item here but is otherwise confusing, undertested by ordinary standards, or solving the wrong problem should still receive that feedback — this checklist is a floor, ensuring architectural integrity specifically, not a ceiling defining the entirety of what good review looks like.



4. Commit Conventions
4.1 Purpose — the format behind Section 2.5's squash-merge commits

Section 2.5 established that main's history consists of one commit per merged PR, with the PR description becoming that commit's message. This section specifies the exact format that message follows — Conventional Commits, chosen specifically because Section 13 (Release Management) depends on it for automated changelog generation and semantic-version determination.

4.2 Format
<type>(<scope>): <short description>

<body — the "why," expanded from the PR's Why section, §2.3>

<footer — breaking changes, issue references>
4.3 Type taxonomy
Type	Meaning	Version impact (Section 13.3)
feat	New capability	Minor version bump
fix	Bug fix	Patch version bump
perf	Performance improvement with no behavior change	Patch version bump
refactor	Code restructuring with no behavior or performance change	No version bump (internal only)
docs	Documentation-only change (including this document set itself)	No version bump
chore	Tooling, dependency, CI/CD configuration	No version bump
test	Test-only addition or change	No version bump
breaking (via footer, not type)	Any type combined with a BREAKING CHANGE: footer	Major version bump

This taxonomy directly drives Section 13.3's automated semantic-versioning — a feat commit automatically proposes a minor bump, a BREAKING CHANGE: footer automatically proposes a major bump, removing manual version-number judgment from the release process for the common case (Section 13.5 covers the exception process for when automated determination needs override).

4.4 Scope — mapping to this document set's bounded contexts and volumes
feat(flow-authoring): add conditional step branching to the visual canvas
fix(orchestration): correct retry backoff calculation for flaky steps
feat(ai-planning): add claim-evidence grounding check to Root Cause Analysis
chore(helm): bump PostgreSQL subchart to 16.2

Scope values map directly to Volume 1's bounded contexts (Part II, Section 7), Volume 2's feature modules (Section 6), Volume 3's capability engines (Part III), or Volume 4's infrastructure components — reusing, once again, the exact same ubiquitous-language vocabulary Volume 1, Part II, Section 6.3 established and every subsequent volume has continued to reuse rather than inventing a parallel commit-scope taxonomy that would need independent maintenance.

4.5 Breaking change footer — the specific trigger for major version bumps
BREAKING CHANGE: the AutomationModule interface's Setup method now requires
a CancellationToken parameter. Existing modules must update their
implementation signature.

Directly correlated with Volume 1, Part III, Section 18.7's AutomationModule contract-versioning discipline and Volume 3, Part I, Section 3.3's prompt-versioning discipline — a commit introducing a breaking change to either of these two especially consequential contracts (this document set's two most-emphasized "everything depends on this staying stable" interfaces) must carry this footer, making the breaking change mechanically detectable by Section 13's release tooling rather than relying on a human remembering to flag it during release preparation.

4.6 Commit message quality — the body as durable documentation

The commit body (4.2) is written for a future engineer with zero context on the original PR discussion — directly extending this document set's "why," not just "what," documentation philosophy (every volume's sections have consistently explained rationale, not just stated decisions) to the individual-commit level, the most granular unit of durable project history. A commit body that says only "fixes bug" fails this bar; a commit body explaining what was actually wrong and why the fix is correct meets it — this distinction is a named item reviewers check for (extending Section 3's checklist informally, since commit-message quality is judgment-based rather than mechanically checkable the way Section 3's items mostly are).

4.7 Automated enforcement

Commit message format (4.2's structure, 4.3's type taxonomy) is validated by a commit-lint check integrated into Volume 4, Section 5.2's pipeline (stage 1, Lint & Static Analysis) — a malformed commit message fails the same build-blocking gate as a linting violation, consistent with this document set's consistent preference for tooling-enforced discipline over convention-dependent discipline (Volume 1, Part VI, Section 40.2's analyzer-enforced coding rules being the original template this practice extends).

4.8 Relationship to Section 1's squash-merge and Section 13's release automation

This section's format is not an isolated stylistic preference — it is the specific, mechanically-parseable input Section 13's automated changelog generation and version-bump determination directly consume. A commit that doesn't follow this format isn't merely "less readable" — it's a data-integrity gap in the automated release pipeline Section 13 specifies, which is the concrete reason this convention is enforced by tooling (4.7) rather than left as team guidance a busy engineer might reasonably skip under deadline pressure.


____________________________________________________________________________________________________________________________________________________



5. Coding Standards (.NET, React, Python)
5.1 Purpose — pointing to authoritative sources, not duplicating them

Two of this section's three languages already have a fully-specified coding standard elsewhere in this document set: Volume 1, Part VI, Section 40 specified .NET/C# standards in complete, enforced detail; Volume 2's Sections 4, 5, and 9 collectively specified React/TypeScript standards. This section does not restate either — it points to them as authoritative and adds only what those volumes correctly left unaddressed: cross-language consistency principles, and a full, honest specification of Python's role, which no prior volume locked down.

5.2 .NET/C# — authoritative source

Volume 1, Part VI, Section 40 is the complete standard. Every rule there — strongly-typed identifiers, dependency-direction enforcement, structured-logging-only, the AuditableCommandHandler inheritance requirement, async/CancellationToken propagation, the testing-tier conventions — applies without modification. This section's only addition: Volume 1, Section 40's rules are enforced via Roslyn analyzers (Section 40.2 there) configured as .editorconfig + custom analyzer package, versioned in the same repository and updated through the same PR process as any other code change (Volume 5, Section 2), never configured ad hoc per-project or left to individual IDE settings.

5.3 React/TypeScript — authoritative source

Volume 2, Sections 4 (React Architecture), 5 (Folder Structure), and 9 (Component Library) collectively form the complete standard. Functional components and hooks exclusively; strict TypeScript with generated DTO types (Volume 2, Section 4.7); the four-tier component classification (Section 4.4) and its enforced import boundaries (Section 5.3); design-token-only styling (Section 2.9, 3.2). This section's only addition: these rules are enforced via ESLint configuration (the import-boundary plugin named in Volume 2, Section 5.3) plus TypeScript's own strict-mode compiler, both configured and versioned identically to Section 5.2's .NET analyzer approach — one consistent enforcement philosophy across both primary languages, even though the specific tooling necessarily differs.

5.4 Python — honest scoping, since no prior volume locked this down

This is the one language in this section's title that genuinely needs new specification here, and this document set's practice of honest scoping (Volume 2, Section 16.1's offline-support disclosure; Volume 1, Section 39.9's scaling-ceiling disclosure) applies directly: Python has no role in Nexus's production request path. The backend is .NET exclusively (Volume 0, Section 21.2, locked); the frontend is React exclusively (Volume 0, Section 21.3, locked). Python's actual, legitimate scope is narrower and specific:

Use case	Where it appears
AI evaluation-set tooling and offline evaluation harnesses	Volume 3, Section 3.6, 19.3 — running evaluation sets against capability engines, computing structural-validity/grounding-accuracy scores
RAG indexing and embedding-pipeline experimentation	Volume 3, Section 4.4 — prototyping and validating retrieval-quality changes before they're operationalized in the production indexing pipeline
Data analysis for capacity planning and cost modeling	Volume 4, Section 19.3, 21.2 — ad hoc analysis of usage/cost trends, not the production monitoring pipeline itself (which remains Prometheus/Grafana, Volume 4 Sections 9–10)
One-off infrastructure/migration scripting	Rare, reviewed case-by-case — most infrastructure automation is Helm/YAML (Volume 4, Section 4) or bash (CI workflow steps, Volume 4, Section 6), with Python reserved for genuinely data-processing-heavy one-off tasks poorly suited to either

Python code in every one of these categories is tooling and analysis code, never a deployed service — it does not run in any of the containers Volume 4, Section 2.2 specified, is not part of any request-serving path, and has no NFR-PERF-style latency commitment of its own. This boundary is stated as a hard architectural constraint, not a soft preference: a proposal to implement any customer-facing capability in Python would be a significant, ADR-requiring deviation from Volume 0's locked technology decisions (Section 21.2–21.3), not a routine implementation choice available to an individual engineer.

5.5 Python style and tooling

Where Python is used (5.4's four categories), it follows PEP 8 via ruff (linting and formatting in one tool, chosen over the historically-separate black/flake8/isort combination for its speed and configuration simplicity) and mypy in strict mode for type checking — Python code is type-annotated throughout, directly consistent with this document set's cross-language preference for strong typing wherever the language supports it (Volume 1's strongly-typed IDs, Volume 2's TypeScript strict mode, now Python's mypy strict mode as the third instance of the same underlying principle).

5.6 Cross-language consistency principles

Three principles apply identically regardless of which of these three languages a given piece of code is written in, restated here as the unifying thread across Sections 5.2–5.5:

Strong typing wherever the language allows it — strongly-typed IDs (.NET), strict TypeScript (React), mypy strict mode (Python).
Structured logging, never string interpolation — Volume 1, Section 28.2's rule, extended to Python tooling via Python's standard logging module's structured-extra-fields capability rather than f-string-interpolated log messages.
Tooling-enforced, not convention-dependent — every standard in this section has an automated enforcement mechanism (analyzers, ESLint, ruff/mypy), never a rule that exists only as documentation a developer might or might not follow.
5.7 Adding a fourth language — the bar this section sets

Consistent with Volume 0, Section 21.2's original locked-technology reasoning: introducing any additional language beyond these three requires the same ADR-and-architecture-owner-approval process Volume 1, Part VI, Section 40.8 established for any deviation, with a specific, required justification addressing why none of the three already-standardized languages (5.2–5.5) adequately serve the new use case — this section's three-language scope is a deliberate constraint on this platform's own complexity, not an arbitrary historical accident to be casually expanded.



6. API Versioning
6.1 Purpose — one unified policy across this platform's three versioned contracts

This document set has specified three genuinely distinct versioned contracts, each in the volume where it was architecturally motivated: the REST API (Volume 1, Part V, Section 30.6), the AutomationModule plugin interface (Volume 1, Part III, Section 18.7), and AI prompt templates (Volume 3, Part I, Section 3.3). Each of those sections specified versioning correctly for its own context, but never as one unified, cross-referenced policy — this section is that unification, stated once so a future contract (a fourth versioned surface this platform doesn't yet have) has one clear policy to follow rather than three scattered precedents to reconcile.

6.2 The three existing contracts, compared
Contract	Versioning scheme	Breaking-change trigger	Deprecation window
REST API	Path segment (/api/v1/...)	Any change removing/renaming a field, changing a field's type, or changing an endpoint's semantics	Both versions served concurrently for a defined period (Volume 1, Part V, §30.6)
AutomationModule interface	Semantic version of the interface itself	Adding a required method, removing any method	Additive-only for minor/patch; major requires a defined migration window (Volume 1, Part III, §18.7)
AI Prompt Templates	Semantic version per template (flow-generation.v3)	Any change to outputSchema or requiredContextFields	New version must pass Section 3.6's evaluation gate before traffic shifts; old version retained for rollback (Volume 3, Part I, §3.3)
6.3 The common thread — semantic versioning as the universal scheme

Every contract in 6.2 uses semantic versioning (major.minor.patch or an equivalent path-segment analog), with the same major/minor/patch semantics applying uniformly: major = breaking, requires explicit migration; minor = additive, backward-compatible; patch = non-functional refinement, invisible to consumers. This uniformity is deliberate — an engineer who understands what "major version bump" means for the REST API (6.2's first row) should not need to relearn the concept for the plugin contract or prompt templates; the underlying discipline is identical even though the mechanics of expressing a "version" differ (a URL path segment versus a semantic-version string versus a template identifier suffix).

6.4 Deprecation policy — the universal minimum window

Every deprecation, regardless of which of 6.2's three contracts it applies to, carries a minimum 90-day concurrent-availability window between announcing a deprecation and actually removing the deprecated version — chosen as a floor, not a ceiling (a contract with especially broad external consumption, like the REST API's third-party CI integrations per Volume 0's UC-4, may warrant a longer window, decided case-by-case, but never shorter than this floor). This directly protects Persona Marcus's CI integrations (Volume 0, Section 10.3) and any marketplace module developer's (Persona Raj's) integration against Nexus's own release cadence — a deprecation window shorter than this floor would directly contradict Volume 1, Part V, Section 30.6's original stated rationale ("Marcus's CI integrations depend on this API not breaking underneath a working pipeline without warning").

6.5 Deprecation notification

Every deprecation is announced through three simultaneous channels: the API's own response (a Deprecation HTTP header for REST endpoints, directly extending Volume 1's already-specified error-response discipline, Part V Section 30.5, to non-error responses too), the changelog (Section 13's release-management artifact), and — for the AutomationModule interface specifically, given Volume 1, Part III, Section 18's emphasis on this contract's outsized importance to the plugin ecosystem — a direct notification to registered marketplace module developers (Volume 0, Section 17), not merely a passive changelog entry a developer would need to actively check for.

6.6 Versioning and the evaluation-gate difference — why prompts aren't purely mechanical

Worth naming explicitly, since it's a genuine, deliberate asymmetry between 6.2's three rows: REST API and AutomationModule version bumps are triggered by a structural, mechanically-detectable change (a field removed, a method signature changed) — but a prompt template version bump (Volume 3, Section 3.6) additionally requires passing an evaluation-set quality gate before the new version can carry production traffic, even for a version bump that wouldn't itself be "breaking" in the structural sense. This is not an inconsistency; it's the correct reflection of a genuine difference in what could go wrong: a REST API's correctness is determined by its schema, fully verifiable by static analysis, while a prompt's correctness depends on the model's actual behavior against real inputs, which only Volume 3's evaluation-set discipline can verify — the versioning policy (6.3's shared semantic-versioning discipline) is unified; the promotion gate is appropriately different per contract type.

6.7 Internal versioning — what this policy does not cover

This section's scope is deliberately limited to contracts with external or semi-external consumers (a third-party CI integration, a marketplace module developer, a specific model/prompt pairing whose behavior matters to evaluation history) — purely internal interfaces (a private method signature within Nexus.Application, an internal Zustand store's shape within Volume 2's frontend) are not versioned under this policy at all; they change freely as part of normal refactoring, governed only by Section 3's code-review checklist and ordinary testing, not by this section's deprecation-window discipline. Applying full versioning ceremony to purely internal code would be exactly the kind of "complexity that doesn't earn its cost" this document set has consistently avoided (Volume 1, Architecture Principle 20.7).

6.8 Versioning documentation as a release artifact

Every version bump across all three contracts (6.2) is documented in the same changelog (Section 13) using the same Conventional Commits BREAKING CHANGE: footer convention (Volume 5, Section 4.5) that already specifically named the AutomationModule and prompt-versioning contracts as requiring this footer — this section closes the loop Section 4.5 opened, confirming that footer's purpose is precisely to drive this section's unified versioning-and-deprecation policy, not an independent, disconnected commit-message convention.



7. Error Handling
7.1 Purpose — the topic prior volumes touched but never fully specified

Every volume in this document set has handled errors within its own scope — Volume 1's RFC 7807 REST error format (Part V, Section 30.5), domain-invariant rejections returning result objects rather than throwing (Part II, Section 9.2's FlowPublishResult), Volume 3's typed failures (AiUnavailableError, GuardrailBlockedError). This section is where those scattered, individually-correct decisions get unified into one coherent error-handling philosophy — the "when do we throw, when do we return a result, and how does an error surface to a human" policy this document set has practiced consistently but never stated as a single, named standard.

7.2 The core decision — exceptions for the exceptional, result types for the expected

The single governing rule: an outcome that is a normal, anticipated possibility of an operation is represented as a typed result, never an exception; an outcome that indicates something has gone genuinely wrong with the system itself is an exception. This is not a novel rule invented here — it is the exact pattern already present throughout Volumes 1 and 3, now named explicitly:

Outcome	Exceptional or expected?	Representation
A Flow fails to publish because a referenced module doesn't exist	Expected — a normal outcome of a normal operation	FlowPublishResult (Volume 1, Part II, §9.2) — a typed result, not an exception
A StepResult fails because the target system returned an error	Expected — this is what the flow was testing for	StepStatus.Failed (Volume 1, Part II, §11.3) — a typed status, not an exception
An AI provider times out	Expected — provider unavailability is anticipated, per NFR-REL-3	AiUnavailableError typed exception (Volume 3, Part I, §2.5) — see 7.3 for why this is an exception despite being "expected"
A database connection is lost mid-transaction	Genuinely exceptional — the system itself is in a degraded state	A thrown, unhandled-at-the-business-logic-layer exception, caught only at the infrastructure boundary (Section 7.5)
A required configuration value is missing at startup	Genuinely exceptional — the application cannot function	A thrown exception, crashing startup deliberately (fail-fast, not a degraded partial start)
7.3 Why AiUnavailableError is an exception despite being an "expected" outcome — resolving the apparent tension

Row three of 7.2's table deserves explanation, since it looks like it violates the rule stated in 7.2: an AI provider timeout is expected (NFR-REL-3 anticipates it), yet it's represented as a thrown exception, not a result type. The resolution: the type of representation (exception vs. result) is chosen based on how many layers need to react to it, not purely on how "expected" the outcome is. A FlowPublishResult's failure is handled by exactly one layer (the calling command handler, which returns it directly to the API response) — a result type is the right fit. AiUnavailableError needs to propagate through the Planning Engine (Volume 3, Section 2.4), potentially through Section 15.6's fallback-chain logic, and finally to a Volume 1 command handler that catches it specifically — an exception's automatic propagation-until-caught behavior is the better structural fit for a condition that legitimately needs to unwind through multiple layers before reaching the place equipped to handle it, even though the underlying event is entirely expected. This is the precise, stated resolution to what would otherwise look like an inconsistency between Volume 1's and Volume 3's error-handling choices.

7.4 Exception hierarchy
csharp
public abstract class NexusException : Exception { }                    // root — never thrown directly

public class DomainException : NexusException { }                        // Volume 1, Part II §9's
                                                                              // aggregate invariant violations —
                                                                              // used sparingly, since most domain
                                                                              // rejections are result types (7.2),
                                                                              // reserved for truly invalid states
                                                                              // a well-behaved caller should never
                                                                              // actually trigger

public class InfrastructureException : NexusException { }                // database, cache, external-service
                                                                              // failures — Section 7.5's boundary

public class AiCapabilityException : NexusException { }                   // AiUnavailableError, GuardrailBlockedError
    // : AiCapabilityException                                              (Volume 3, §2.5, §17.7) both derive here

public class AuthorizationException : NexusException { }                  // Volume 1, Part IV §25's ForbiddenException

Every custom exception in the codebase derives from NexusException, never directly from the base .NET Exception — this is what makes Section 7.6's uniform correlation-ID attachment and Section 7.7's uniform logging behavior possible to enforce as a single base-class concern rather than requiring every individual exception type to separately remember to implement it.

7.5 The catch boundary — where exceptions stop propagating

Exceptions are caught at exactly two layers, never scattered as ad hoc try/catch blocks throughout business logic: the command/query handler boundary (Volume 1, Part II, Section 13.7's pattern — a handler catches domain-specific exceptions it knows how to translate into a result, like AiCapabilityException becoming a graceful non-AI fallback per Volume 3, Section 2.5) and the API controller boundary (Volume 1, Part V, Section 30.3 — any exception surviving to this point is translated into the RFC 7807 error response, Section 30.5, with InfrastructureException and any truly unhandled exception type mapped to a generic 500 response, never leaking internal exception details to an API consumer). No business-logic code catches a generic Exception — every catch clause is specific to a named exception type this section's hierarchy (7.4) defines, directly enforcing Architecture Principle 20.4's "fail loud, fail informatively": a broad catch-and-swallow is exactly the pattern that principle exists to prevent.

7.6 Correlation and context on every exception

Every NexusException carries the same CorrelationId/FlowRunId scope Volume 1, Part IV, Section 28.4 established for logging — attached automatically via the exception's construction within an active ILogger.BeginScope, not manually threaded through by each throw site — ensuring Section 7.5's API-boundary translation into an RFC 7807 response's correlationId field (Volume 1, Part V, Section 30.5) is always populated correctly, regardless of how deep in the call stack the original exception was thrown.

7.7 Frontend error handling — consistency with the backend philosophy

Volume 2's error boundaries (Section 4.6's module-scoped boundaries, Section 7.7's route-level boundary) implement the identical underlying philosophy: a known, anticipated failure (a query returning an error response, per Volume 2, Section 8.2's TanStack Query error states) is handled as data — rendered as an explicit error UI state — never thrown as a JavaScript exception the component tree must catch; a genuinely unexpected rendering failure (a bug causing a component to throw during render) is caught by an error boundary, the React equivalent of Section 7.5's API-controller catch boundary. The same "expected → typed data, unexpected → exception-and-boundary" split from 7.2 holds across both languages, restated here as explicit cross-language consistency (extending Section 5.6's three cross-language principles with a fourth, error-handling-specific one).

7.8 What error handling is not a substitute for

Consistent with Section 3.10's equivalent caveat for the code review checklist: this section's rules produce consistent, well-structured error handling — they do not, by themselves, guarantee an error is handled correctly in the sense of choosing the right recovery action for a given failure. That judgment remains the responsibility of the engineer writing the handling code and the reviewer verifying it (Section 3's checklist), with this section providing the structural framework that judgment operates within, not a replacement for it.



8. Logging Conventions
8.1 Purpose — restating Volume 1's standard as binding, and closing the frontend gap

Volume 1, Part IV, Section 28 specified backend operational logging completely — structured logging, log levels, correlation scoping, sensitive-data exclusion. This section restates that as the binding standard (no changes), and closes a genuine gap: Volume 2 never specified an equivalent frontend logging standard, and Volume 4's infrastructure/CI tooling has its own implicit logging patterns that were never reconciled with Volume 1's discipline. This section unifies all three.

8.2 Backend logging — authoritative source

Volume 1, Part IV, Section 28 is the complete standard. Structured logging exclusively (Section 28.2's prohibition on string interpolation, enforced via the Roslyn analyzer named there); the six-level severity table (Section 28.3); correlation-scope propagation via ILogger.BeginScope (Section 28.4); the sensitive-data denylist (Section 28.5); tiered retention (Section 28.6, operationalized in Volume 4, Section 11.3's Elasticsearch ILM policy). Nothing in this section modifies any of it.

8.3 Frontend logging — the standard Volume 2 never specified

Browser-based structured logging follows the identical philosophy Volume 1, Section 28.2 established, adapted to the browser environment:

typescript
// Required convention — structured, matching Volume 1 §28.2's backend pattern exactly:
logger.info('Flow published', { flowId, userId, tenantId });

// Prohibited — the frontend equivalent of Volume 1 §28.2's banned string interpolation:
console.log(`Flow ${flowId} published by ${userId}`);

Frontend logs are exported via the browser OpenTelemetry SDK to the same Collector gateway (Volume 4, Section 8.4) already receiving Core Web Vitals RUM data — meaning frontend operational logs join the exact same correlation, scrubbing (Volume 4, Section 8.6), and retention (Volume 1, Section 28.6, via Volume 4, Section 11.3) pipeline as backend logs, rather than living in a separate, browser-console-only world invisible to the same investigative workflow Volume 1, Section 28.4 designed for backend correlation. A raw console.log/console.error call is prohibited in production code paths (permitted only in local-development-only code, clearly marked, per Volume 4, Section 1's dev-environment scope) — enforced via the same ESLint tooling already governing Volume 2's other conventions (Section 5.3).

8.4 Frontend-specific correlation

Every frontend log statement automatically carries the current CorrelationId (propagated from the initiating API request, Volume 1, Section 28.4) plus a frontend-specific SessionId (distinct from AiSession, Volume 3, Section 5.2 — this is the browser tab's own lifecycle identifier) — giving an engineer investigating a customer-reported frontend issue the same "reconstruct exactly what happened" capability Volume 1, Section 28.4 described for backend investigation, now genuinely spanning both the browser and server side of a single user interaction.

8.5 CI/infrastructure logging — reconciling Volume 4's implicit patterns

Volume 4's CI pipeline (Section 5–6) and infrastructure tooling (Helm hooks, migration Jobs) produce their own operational output, which — until this section — was never explicitly reconciled with Volume 1's structured-logging discipline. This section closes that gap: CI/CD pipeline logs (Volume 4, Section 5.7's pipeline-observability data) and infrastructure Job logs (the migration Job, Volume 1, Part VI, Section 37.5) are captured as structured output wherever the underlying tool supports it (GitHub Actions' own structured step-output capability, kubectl logs output parsed and re-emitted in structured form by the same Collector pipeline, Volume 4, Section 8.2) — brought into the same observability pipeline as application logs specifically so a deployment-correlated investigation (Volume 4, Section 10.8's deployment-annotation correlation) can trace from "this deployment happened" through "here's what the migration Job logged" through "here's what changed in application behavior afterward" as one continuous, correlated narrative, not three disconnected log sources an engineer must manually stitch together.

8.6 Log level consistency across languages

Volume 1, Section 28.3's six-level table (Trace/Debug/Information/Warning/Error/Critical) is the canonical severity vocabulary — frontend logging (8.3) and CI/infrastructure logging (8.5) both map to this exact same six-level scale, never an independently-invented severity taxonomy, ensuring Volume 4, Section 7.5's three-tier alerting-severity model (which itself derives from this same underlying vocabulary) applies uniformly regardless of which layer of the stack a given log record originated from.

8.7 What gets logged — restating the "when," not just the "how"

Beyond format (8.2–8.5) and severity (8.6): every state-changing operation (a command handled, per Volume 1, Section 13; a mutation dispatched, per Volume 2, Section 8.2) logs at Information level on success and Warning/Error on failure, as a baseline — this is distinct from and complementary to Volume 1, Part IV, Section 27's audit trail (which records who did what for compliance) — operational logging additionally records enough technical detail (which layer, what parameters, what the actual failure was) to support the debugging use case Section 8.4 described, a genuinely different purpose from audit's compliance purpose even when both are triggered by the same underlying event, restating Volume 1, Section 27.7's Audit-vs-Logging distinction one final time as this document set's last word on a distinction it needed to make repeatedly across four volumes.

8.8 Enforcement

Every logging rule in this section is enforced by the same tooling-first philosophy this entire Part has applied consistently: Roslyn analyzers for .NET (8.2, inherited from Volume 1), ESLint rules for TypeScript (8.3), and a CI-pipeline linting step verifying structured-output usage where tooling supports it (8.5) — logging convention violations fail the build, joining every other coding-standard violation Section 5 already established as a build-blocking, not merely review-flagged, category of issue.


____________________________________________________________________________________________________________________________________________________


9. Testing Pyramid
9.1 Purpose — one diagram for four volumes' worth of testing tiers

Volume 1, Part VI, Section 40.6 specified backend testing tiers. Volume 2, Section 19.2 specified a parallel four-tier frontend model. Volume 3, Section 19.2 specified offline/online AI evaluation as a distinct regime. Volume 4, Sections 17.5, 18.5, and 20.7 specified disaster-recovery, chaos-engineering, and restore testing. None of these were ever drawn as one picture. This section is that picture — the single testing pyramid every engineer on this project should be able to point to and locate their own work within.

9.2 The unified pyramid
                              ▲
                             ╱ ╲
                            ╱   ╲     E2E / System Tests
                           ╱     ╲    Volume 2 §19.8 (Nexus testing itself via
                          ╱───────╲   its own Web module) + Volume 4 §5.2 stage 6
                         ╱         ╲  Slowest, fewest, highest confidence
                        ╱  Integr-  ╲
                       ╱   ation     ╲  Volume 1 §40.6 (Infrastructure/API
                      ╱───────────────╲ IntegrationTests) + Volume 2 §19.6 (MSW-backed)
                     ╱                 ╲
                    ╱   Component /      ╲  Volume 1 §40.6 (Application tests) +
                   ╱    Application       ╲ Volume 2 §19.4 (component + visual regression)
                  ╱───────────────────────╲
                 ╱                         ╲
                ╱   Unit / Domain            ╲  Volume 1 §40.6 (Domain.Tests, zero
               ╱────────────────────────────── ╲ infrastructure dependency) + Volume 2
              ╱                                 ╲ §19.3 (hook unit tests) + Volume 5 §5's
             ╱___________________________________╲ Python evaluation-tooling unit tests
                                                      Fastest, most numerous, run on every commit

     ═══════════════════════════════════════════════════════════════
     Orthogonal, not part of the vertical stack above:

     AI Evaluation (Volume 3 §19.2-19.4)     — offline evaluation sets gate every
                                                  capability-engine release; online
                                                  metrics run continuously in production

     Resilience Testing (Volume 4 §17.5,      — quarterly DR game days, continuous
       §18.5, §20.7)                             chaos engineering, weekly restore tests

     Adversarial Testing (Volume 2 §19.11,    — plugin-sandbox and guardrail-bypass
       Volume 3 §17.9)                            attempts, deliberately hostile inputs
9.3 Why AI evaluation and resilience testing sit outside the classic pyramid

This is a deliberate structural choice worth explaining rather than a diagram simplification: the classic testing pyramid (unit → integration → E2E) measures correctness of code given known inputs — a fundamentally different question from quality of AI-generated output given open-ended real-world input (Volume 3's concern) or survival of infrastructure given a catastrophic failure condition (Volume 4's concern). Forcing these into the same vertical hierarchy would misrepresent their actual relationship to the pyramid's tiers — an AI evaluation set isn't "slower and more expensive than a unit test but faster than an E2E test" in any meaningful sense; it's answering a different kind of question entirely, using a different kind of pass/fail criterion (structural validity, grounding accuracy, calibration — Volume 3, Section 19.3's table — rather than a deterministic assertion). Drawing them as orthogonal, rather than jamming them into a fifth tier, is more honest about what they actually measure.

9.4 Test-tier ownership and the "who writes what" question
Tier	Written by	Reviewed against
Unit/Domain	The engineer implementing the change	Section 3's code review checklist §3.2 items
Component/Application	The engineer implementing the change	Section 3's §3.3, §3.7 items
Integration	The engineer implementing the change, for changes touching persistence/external boundaries	Section 3's §3.4 items
E2E/System	Shared ownership — new user-journey coverage added by whichever team owns that journey's primary feature module (Volume 2, §6)	Volume 2, §19.8's dogfooding design
AI Evaluation	The Volume 3 capability-engine owner (Volume 3, §19.7's per-metric ownership)	Volume 3, §19.3's release gate
Resilience	Infrastructure/platform team (Volume 4, §7.6's ownership model)	Volume 4, §17.5, §18.5

This table directly extends Volume 4, Section 7.6's per-SLI ownership model and Volume 1, Part II, Section 7.6's bounded-context-as-team-ownership-unit principle to testing specifically — the same organizational structure this document set has used consistently for operational ownership now governs test-writing ownership too, rather than testing responsibility being assigned through an independent, parallel structure.

9.5 Coverage expectations per tier

Consistent with this document set's preference for tooling-enforced standards over aspirational guidance (Section 5.6, 5.7): unit/domain tier coverage is measured and enforced at a high threshold (e.g., 90%+ line coverage on Nexus.Domain, directly achievable given that layer's zero-dependency design per Volume 1, Part III, Section 15.5 making it fully unit-testable) — coverage thresholds decrease progressively up the pyramid (component/application tier, then integration tier), reflecting that higher tiers are inherently more expensive per test and correctness at lower tiers reduces the marginal value of exhaustive higher-tier coverage, not that higher-tier correctness matters less. E2E/System tier coverage is measured differently — not by code-line percentage but by use-case coverage against Volume 0's use-case catalog (Part II, Section 11) — every named use case (UC-1 through UC-7) has at least one E2E test exercising it, tracked as a checklist against that catalog rather than a percentage metric that wouldn't meaningfully apply to this tier.

9.6 Flake tolerance per tier

Directly extending Volume 1, Part IV, Section 29.5's platform flake-rate metric (a production concern) to this document set's own test suite (a development concern, the analogous but distinct question of "how often does a test fail for reasons unrelated to a real regression"): unit/domain and component/application tiers have effectively zero flake tolerance (a flaky unit test is treated as a bug in the test, fixed or deleted, never tolerated as "normal") — integration and E2E tiers have a defined, monitored, non-zero flake budget (Volume 2, Section 19.9 already anticipated this for the E2E suite specifically), consistent with those tiers' inherent exposure to real infrastructure timing variance that lower tiers, by design, don't touch.

9.7 Where this pyramid is enforced

Every tier in Section 9.2 maps directly to a specific stage in Volume 4, Section 5.2's CI/CD pipeline — this pyramid is not a separate, aspirational document from the pipeline that actually runs; it is the conceptual model the pipeline's stage ordering (fast, cheap tiers first; slow, expensive tiers only after cheaper tiers pass, per Volume 4, Section 5.2's sequential-dependency design) directly implements. A reader who understands this section's pyramid already understands why Volume 4's pipeline is shaped the way it is.



10. Performance Benchmarking
10.1 Purpose — one benchmarking practice across three volumes' worth of performance gates

Volume 1, Part VI, Section 38 specified backend performance engineering against NFR-PERF-1 through NFR-PERF-4. Volume 2, Section 17 specified frontend bundle-size budgets and Lighthouse CI gates. Volume 4, Section 19.5 specified a quarterly standalone load-testing cadence beyond the per-release gate. This section is where these three, individually-correct practices become one benchmarking discipline with consistent regression-detection and historical-tracking behavior.

10.2 Benchmark inventory — restating, not duplicating
Benchmark	Target	Source	Gate location (Volume 4 §5.2)
Step-dispatch p95 latency	<50ms (NFR-PERF-1)	Volume 1, Part VI §38.2	Stage 8, staging verification
Flow CRUD p95 latency	<200ms (NFR-PERF-2)	Volume 1, Part VI §38.3	Stage 8
Trend-dashboard load p95	<2s (NFR-PERF-3)	Volume 1, Part VI §38.4	Stage 8
AI request timeout compliance	<15s (NFR-PERF-4)	Volume 1, Part VI §38.5	Stage 8
Frontend LCP/INP/CLS	Lighthouse CI thresholds	Volume 2, §17.9	Stage 8
Frontend bundle size	Per-bundle KB budgets	Volume 2, §17.2	Stage 3, build
Concurrent execution capacity	500 (NFR-SCALE-1)	Volume 1, Part VI §38.8	Stage 8
CI build time	<10min cold, <3min warm	Volume 4, §2.9	Meta — measures the pipeline itself

Every row already has a fully-specified target and origin — this section's contribution is not new targets, it is the shared measurement and regression-tracking infrastructure underneath all of them.

10.3 Historical tracking — every benchmark run is a data point, not a pass/fail event

Every benchmark in 10.2's table, on every run (both the per-release gate, stage 8, and Volume 4, Section 19.5's quarterly standalone load test), writes its result to a persistent time-series store — the same Prometheus instance already receiving production metrics (Volume 4, Section 9), tagged distinctly (environment=ci-benchmark) so benchmark history is queryable and dashboard-visible (Volume 4, Section 10.3's Platform Health dashboard gains a benchmark-trend panel) alongside, but clearly distinguished from, actual production telemetry. This is what turns "did this release pass its performance gate" (a single-run, binary question) into "is this metric trending in a concerning direction across the last 20 releases" (a trend question) — the more valuable question, and one none of Volumes 1, 2, or 4's individually-specified gates could answer in isolation, since each only evaluated its own single run against a fixed threshold.

10.4 Regression detection — relative, not just absolute

Beyond each benchmark's absolute NFR-derived threshold (10.2), this section adds a relative regression check: a release that passes its absolute threshold but represents a statistically significant regression versus the trailing 10-release average (e.g., step-dispatch p95 creeping from 30ms to 45ms over several releases — each individually still under the 50ms absolute threshold, but a clear, real degradation trend) triggers a Warning-severity flag (Volume 4, Section 7.5's severity model) even though it wouldn't fail the absolute gate. This directly closes a gap the absolute-threshold-only approach in Volumes 1, 2, and 4 left open: a slow, creeping regression that never crosses the hard NFR threshold in any single release can still silently erode headroom release after release until a single additional small change is the one that finally crosses the line — this relative check catches the trend well before that point.

10.5 Benchmark environment consistency

Every benchmark run (per-release and quarterly) executes against the identical staging topology (Volume 4, Section 5.3) with identical resource sizing (Volume 4, Section 3.3) — a benchmark run against inconsistent hardware or scaled-differently infrastructure produces numbers that aren't meaningfully comparable across runs, silently invalidating Section 10.3 and 10.4's trend-tracking value. This is enforced by running every benchmark through the exact same staging-environment Helm values (Volume 4, Section 4.3) rather than a separately-configured "benchmark environment" that could drift from staging's actual configuration over time.

10.6 Benchmark failure response — the same severity as a functional bug, restated once more

Directly restating Volume 1, Part VI, Section 38.9's "performance regression as a first-class defect category," Volume 2, Section 17.9's equivalent frontend statement, and Volume 4, Section 19's capacity-planning discipline, unified here as this document set's single, final statement of the principle: any benchmark failure — absolute threshold breach (10.2) or relative regression flag (10.4) — is triaged with the same urgency as a functional test failure, tracked in the same incident/risk-register discipline Volume 0, Section 22 established, never deferred to a "performance backlog" reviewed at leisure. This principle has now been stated in five different places across this document set (Volumes 1, 2, 4 individually, and this section unifying them) — the repetition is deliberate, not redundant documentation: a principle this consistently restated across independently-authored sections is a genuine, load-bearing organizational value, not an incidental stylistic echo.

10.7 AI capability latency — a distinct benchmarking concern, cross-referenced not duplicated

Volume 3, Part IV, Section 15's model-routing decisions and Part V, Section 19's evaluation metrics already track AI capability latency and quality as part of that volume's own, distinct evaluation regime (Section 9.3 of this Part already explained why AI evaluation sits outside the classic pyramid). This section's benchmark inventory (10.2) includes only the platform-level NFR-PERF-4 timeout-compliance check — the deeper question of whether a specific capability's latency is good, not just within its timeout budget, remains Volume 3's evaluation-metrics domain, referenced here rather than duplicated, consistent with this Part's general practice of unifying overlapping concerns without erasing genuinely distinct ones.

10.8 Load testing cadence — restating Volume 4's schedule as this section's authoritative reference

Volume 4, Section 19.5 already specified the quarterly standalone load-test cadence in full detail (independent of any specific release, validating growth-projection assumptions against the current system). This section adopts that cadence as authoritative for the full benchmark suite (10.2), not only the concurrent-execution-capacity benchmark Volume 4, Section 19.5 originally focused on — every benchmark in this section's inventory runs at both the per-release cadence (Volume 4, Section 5.2, stage 8) and this quarterly, standalone cadence, giving Section 10.3's trend-tracking two different sampling frequencies: dense (every release) for fast-moving regression detection, and a periodic, deliberately-isolated full-system baseline (quarterly) that Section 10.5's environment-consistency requirement makes trustworthy as a genuine apples-to-apples comparison over longer time horizons.



11. Security Practices
11.1 Purpose — the master checklist behind "security designed in, not bolted on"

Architecture Principle 20.5 (Volume 1, Section 20.5) established "security and compliance posture is designed in, not bolted on" as a governing principle, and every subsequent volume has honored it in its own scope — Volume 1's authorization-first command handlers, Volume 3's guardrails and provider-security review, Volume 4's secrets architecture and network policies. This section is where that principle becomes an actual, checkable practice: threat modeling requirements, a secure-coding checklist, and the testing/disclosure cadence that verifies all of it continuously, not just at design time.

11.2 Threat modeling — required for a defined category of change

A lightweight threat-modeling exercise (STRIDE-based, chosen for its accessibility to engineers without dedicated security-specialist training) is a required part of the design review for any change that: introduces a new trust boundary (directly extending Volume 1, Part I, Section 2.5's boundary catalog — a new one requires this exercise before implementation), handles a new category of sensitive data (Volume 4, Section 14.2's secret-classification table gaining a new row), or adds a new external integration (a new AI provider, Volume 3, Part IV, Section 14.9's contract-test-plus-security-review requirement). This threat-modeling requirement is attached to the same "ADR required?" PR checkbox (Section 2.3) already governing architectural deviations — a change requiring threat modeling is, by definition, also a change requiring an ADR, since a new trust boundary or sensitive-data category is exactly the kind of pattern-deviation Volume 1, Part VI, Section 40.8 already requires documentation for.

11.3 Secure coding checklist — consolidating, not duplicating, four volumes of security rules

This checklist deliberately does not restate Section 3's code review checklist (which already consolidated most security-relevant review items in its Section 3.5) — it adds only the items that are secure-coding practice rather than review-time verification:

Input validation at every trust boundary, restated as a practice: every point where data crosses a boundary catalogued in Volume 1, Part I, Section 2.5 (target system → Nexus, third-party module → orchestrator, browser → API) validates and sanitizes input at that specific boundary, never relying on a downstream layer to catch what an upstream layer should have rejected.
Least privilege as a default, not an optimization — every new credential, API scope, or Kubernetes RBAC grant (Volume 4, Section 3.9) starts at the narrowest plausible scope and is widened only with explicit justification, never granted broadly "to be safe" or "in case it's needed later."
No security control implemented exactly once — restating Volume 1, Part IV, Section 26.3's defense-in-depth philosophy as a general coding practice: a security-relevant guarantee (tenant isolation, credential scoping, guardrail enforcement) is verified at more than one layer wherever architecturally feasible, following the pattern already established for tenant isolation (three layers), the sandbox boundary (network policy + mTLS + application scoping), and AI guardrails (input-stage + output-stage checks).
Fail closed, not open — any security-relevant check whose own execution fails (a guardrail check that errors out, per Volume 3, Section 17.2's evaluate method throwing rather than returning) is treated as a failure of the operation it was protecting, never silently bypassed to let the operation proceed anyway.
11.4 Dependency and supply-chain security — cross-reference to Section 12

Volume 4, Section 5.2's stage 4 (Security & Vulnerability Scanning) already gates every PR on dependency and image vulnerability scanning. This section's contribution is the practice this gate depends on being meaningful: Section 12 (Dependency Management) specifies the ongoing, scheduled (not just per-PR) scanning cadence and the remediation SLA that keeps the dependency tree this scanning inspects actually current — a per-PR scan is only as good as the vulnerability database and dependency versions it's checking against, both of which degrade in usefulness over time without the ongoing practice Section 12 specifies.

11.5 Penetration testing

An independent, third-party penetration test is conducted at minimum annually against the managed/hosted production environment, and additionally before any major architectural change to a trust boundary (Section 11.2's threat-modeling trigger, extended here to require external validation for the highest-risk category of change, not just internal threat-modeling review). Findings are triaged with the same severity model already established across this document set (Volume 4, Section 7.5's three-tier model, applied to security findings specifically: a critical finding blocks release until remediated, consistent with Volume 1, Architecture Principle 20.4's fail-loud philosophy applied to the most consequential category of defect this platform can have).

11.6 Vulnerability disclosure policy

A public, documented responsible-disclosure process (a security contact, a defined acknowledgment and remediation-timeline commitment) is maintained — this is a genuinely new commitment this section introduces, not previously specified anywhere in Volumes 0–4, added here because Volume 0's Business Objective 5 (enterprise trust, "true at GA, not promised as a roadmap item") implies exactly this kind of external-facing security posture, and its absence would be a real, visible gap for Persona Elena's review process (Volume 0, UC-7) to notice. External researchers reporting a genuine vulnerability through this channel receive the same severity-triage treatment (11.5) as an internally-discovered finding, with a defined SLA (e.g., critical findings acknowledged within 24 hours) — treating external disclosure as a valued, structured input to this platform's security posture rather than an unwelcome surprise to be managed defensively.

11.7 Security training and the review checklist's own maintenance

Every engineer completes security-awareness training covering this section's practices and Section 3.5's review checklist specifically, on onboarding and annually thereafter — directly serving the same "tooling-enforced, not convention-dependent" philosophy (Section 5.6) applied to the one category of standard where pure tooling enforcement genuinely cannot cover everything (recognizing a novel social-engineering attempt, judging whether a new integration's data flow deserves threat modeling, per 11.2's judgment-dependent trigger) — training is this section's explicit acknowledgment that some security practice depends on human judgment tooling can't fully substitute for, the security-specific instance of the same caveat Section 7.8 and Section 3.10 both already stated for their respective domains.

11.8 Relationship to this platform's own security-focused volumes

This section is deliberately a practice document, not an architecture document — Volume 1, Part IV (Authentication, Authorization, Multi-tenancy, Audit) and Volume 3, Part V (Guardrails, Security) and Volume 4, Part IV (Secrets, Vault, TLS) already specified the actual security architecture in full. This section specifies the ongoing human and process discipline that keeps that architecture correctly implemented and continuously verified as the system evolves — the difference between "the system was designed securely" (those three volumes' claim) and "the system remains securely implemented, checked continuously, as engineers who didn't originally design it keep changing it" (this section's claim), a distinction worth stating explicitly since it's the entire reason this section exists as a companion to, not a replacement for, those architectural volumes.



12. Dependency Management
12.1 Purpose — the ongoing practice behind Volume 4's per-PR scanning gate

Volume 4, Section 5.2's stage 4 gates every PR on dependency vulnerability scanning — a point-in-time check. This section specifies the continuous practice that makes that point-in-time check meaningful over time: version-pinning policy, scheduled (not just PR-triggered) scanning, remediation SLAs, and license-compliance review, across all three languages this document set has standardized on (Section 5).

12.2 Version pinning — exact versions, never floating ranges

Every dependency — NuGet packages (.NET), npm packages (React/TypeScript), and pip packages (Python, per Section 5.4's scoped usage) — is pinned to an exact version in the respective lock file (packages.lock.json, package-lock.json, poetry.lock or requirements.txt with hashes), never a floating range (^1.2.0, ~1.2.0) in the committed lock artifact. This directly serves Section 10.5's benchmark-environment-consistency principle extended to dependency resolution generally: a floating-range dependency means two builds at different times, from the identical source commit, could resolve to different actual dependency versions — directly undermining Volume 4, Section 2.6's SHA-based image-tagging guarantee that a given image tag corresponds to fully deterministic, reproducible build inputs, not just deterministic source code.

12.3 Dependency update cadence — scheduled, not reactive-only

Beyond Volume 4, Section 5.2's per-PR vulnerability gate (which only catches a newly introduced vulnerable dependency), a scheduled dependency-update process (weekly automated PRs via Dependabot or equivalent, proposing updates to the latest patch/minor versions of every dependency) runs continuously — this is what keeps the dependency tree itself current enough that Section 11.4's vulnerability scanning remains meaningful, since a scanning tool checking a badly-outdated dependency tree against a vulnerability database finds only vulnerabilities in versions nobody would reasonably still be running, while missing the practical reality that an outdated tree is itself a growing risk surface regardless of whether a specific CVE has been filed against the exact pinned version yet.

12.4 Update review and merge — the same pipeline, no shortcut

Automated dependency-update PRs (12.3) pass through the identical CI pipeline (Volume 4, Section 5.2) and code-review process (Section 2) as any human-authored PR — no auto-merge without human review, even for a patch-level update, because Section 5.6's "tooling-enforced, not convention-dependent" philosophy applies to catching problems, not to bypassing the review that would catch a dependency update silently breaking something the automated test suite doesn't cover. This is a deliberate, conservative choice consistent with Section 1.7's "no shortcuts even under pressure" principle already established for hotfixes, applied here to the analogous temptation to fast-track routine-seeming dependency bumps.

12.5 Remediation SLA for known vulnerabilities
Severity	Remediation SLA
Critical	48 hours from disclosure/detection
High	7 days
Medium	30 days
Low	Next scheduled dependency-update cycle (12.3)

This table directly extends Volume 4, Section 7.5's severity-tiering philosophy to dependency vulnerabilities specifically, and gives Section 11.4's cross-reference from Security Practices a concrete, numeric commitment rather than a vague "addressed promptly" — a Critical-severity dependency vulnerability is treated with the same urgency Section 11.5 established for penetration-test findings, since an unpatched, exploitable dependency is functionally equivalent to a known, unfixed vulnerability in this platform's own code from an attacker's perspective.

12.6 License compliance

Every dependency's license is scanned (as part of the same automated pipeline stage, Volume 4, Section 5.2) against an approved-license allowlist (permissive licenses — MIT, Apache 2.0, BSD variants — approved by default; copyleft licenses — GPL, AGPL — requiring explicit legal review before introduction, given Volume 0, Section 16.2's open-core licensing model's own need for clarity about what can and cannot be redistributed under Nexus's chosen terms) — a dependency introducing an unapproved license fails the same CI gate as a vulnerability finding, directly protecting Volume 0's licensing strategy (Section 16) from being quietly compromised by a transitive dependency's incompatible license terms, a risk that licensing strategy itself didn't anticipate needing to guard against at the individual-dependency level.

12.7 Dependency footprint minimization — the plugin-SDK special case

Directly extending Volume 1, Part V, Section 36.2's decision to publish Nexus.Contracts (the AutomationModule interface) as its own minimal, independently-versioned package: this package's own dependency footprint is held to a stricter standard than the rest of the codebase — zero or near-zero third-party dependencies beyond what .NET's base class library provides — since every dependency Nexus.Contracts carries is a dependency every third-party module developer (Persona Raj, Volume 0, Section 10.5) inherits merely by referencing the SDK to build a module at all. A dependency appropriate for Nexus.Infrastructure's internal use is not automatically appropriate for Nexus.Contracts, and this distinction is a specific, named exception to this section's otherwise-uniform dependency policy.

12.8 Locked technology stack aging — Volume 0's decisions, revisited on a schedule

Beyond individual dependency versions (12.2–12.6): Volume 0, Section 21's locked technology choices themselves (.NET version, React version, PostgreSQL major version) are reviewed against their own upstream support lifecycles on an annual cadence — a .NET version approaching its own end-of-support date, for instance, triggers a planned major-version migration project tracked with the same rigor as any other significant architectural initiative, rather than being discovered as an urgent, unplanned problem only once the locked version has already gone unsupported. This is the dependency-management-level expression of Volume 1, Part VI, Section 40.8's ADR discipline applied to the platform's own foundational technology choices, not just to individual libraries built on top of them — Volume 0's "locked" decisions were never intended as permanently frozen, only as stable until deliberately, visibly revisited.

12.9 Closing note for this Part

This section closes Part III: Section 9 unified this document set's testing tiers into one pyramid, Section 10 unified its performance benchmarks into one tracked practice, Section 11 consolidated its security principles into an ongoing discipline, and this section closes the loop on keeping the dependencies underlying all of it current, licensed appropriately, and free of known vulnerabilities within a defined SLA. Part IV (Release & Delivery) now turns to how verified, quality-gated change actually ships — release management, the final synthesis of every CI/CD gate this document set has specified, the Definition of Done that ties every prior standard together into one checklist, and documentation standards for maintaining this document set itself going forward.


____________________________________________________________________________________________________________________________________________________


13. Release Management
13.1 Purpose — one product version across four independently-versioned components

This document set has specified versioning for individual artifacts — container images by Git SHA (Volume 4, Section 2.6), Helm charts semantically (Volume 4, Section 4.5), the REST API/plugin contract/prompt templates via Section 6's unified policy. None of these is "the Nexus version" a customer or Persona Sofia's leadership review would reference. This section specifies that product-level version and how it correlates every component-level artifact beneath it.

13.2 Product versioning scheme

Nexus ships under a single product semantic version (MAJOR.MINOR.PATCH, e.g., 2.4.1), automatically derived from Conventional Commits (Section 4.3's type-to-version-impact table) across every merge since the last release — a feat commit anywhere in the monorepo (backend, frontend, AI capability engines, or infrastructure charts) proposes a minor bump; any BREAKING CHANGE: footer proposes a major bump; fix/perf commits propose a patch bump. This single version number is what appears in release notes, the changelog, and Volume 2's UserSettingsPage "About" panel — the version a customer actually sees and references in a support conversation.

13.3 Component correlation — one product version, several artifact identifiers
Product version:  v2.4.1
   │
   ├── Backend images (Volume 4 §2.6):    tagged by Git SHA, correlated via release manifest
   ├── Frontend bundle (Volume 2 §17):     versioned alongside backend, same SHA correlation
   ├── Helm chart (Volume 4 §4.5):         chart version bumped only if topology changed
   │                                         this release (may lag product version)
   ├── REST API version (Volume 1 §30.6):  /api/v1/ or /api/v2/ — independent, slower-moving
   │                                         semantic scheme per Section 6.2
   ├── AutomationModule contract (V1 §18.7): independent, slowest-moving per Section 6.2
   └── Prompt template versions (V3 §3.3):   independent per-capability, per Section 6.2

The product version (13.2) is a release-train identifier, not a repackaging of every component's own independent version — Section 6's unified versioning policy already established that the REST API, plugin contract, and prompt templates version independently and more slowly, by design, since their consumers (Persona Marcus's CI integrations, Persona Raj's modules) need stability the product version's more frequent cadence would violate if those contracts moved in lockstep with it. A release manifest (a machine-readable artifact generated at release time) is the authoritative mapping from one product version to the exact set of component versions/SHAs that shipped together — this manifest, not manual cross-referencing, is what answers "what exact API version and chart version did v2.4.1 ship with."

13.4 Release cadence

Product releases (13.2) ship on a regular, predictable cadence (bi-weekly, chosen to balance Section 1.5's short-branch-lifetime trunk-based philosophy — which naturally supports frequent releases, since main is always deployable per Volume 4, Section 5.3 — against the operational overhead of Section 13.6's release-note curation and Volume 4, Section 5.5's manual production-promotion-timing decision, which benefits from not happening continuously) — every merge to main is technically release-able immediately (Volume 4, Section 5.2's fully-automated gate suite), but the scheduled cadence is a deliberate business/communication rhythm distinct from technical readiness, directly extending Volume 4, Section 5.5's "production promotion is a timing decision, not a correctness gate" principle to the product-release level specifically.

13.5 Changelog generation

The changelog is generated automatically from Conventional Commits (Section 4) since the prior release, grouped by type (Section 4.3's taxonomy) and, where relevant, cross-referenced to the ADR (Section 2.3's checkbox) or ticket that motivated the change — this automated generation is reviewed and lightly edited by a release owner before publication (never fully unedited machine output reaching customers, since a fix(orchestration): correct retry backoff calculation commit message, while accurate for engineering purposes, may need a customer-facing rewrite for clarity) but the underlying data (what changed, by commit) is never hand-assembled from scratch, directly avoiding the class of error where a manually-maintained changelog silently omits a real change.

13.6 Coordinated multi-component releases — when Section 13.3's independence has limits

Most releases ship backend, frontend, and infrastructure changes together under one product version with no special coordination needed, since Volume 4, Section 5.2's pipeline builds and deploys all of them from the same main state. The exception requiring explicit coordination: a release containing a REST API major version bump (Section 6.2, requiring Volume 1, Section 30.6's dual-version-serving deprecation window) is flagged in the release manifest (13.3) with an extended validation period — Volume 4, Section 5.2's stage 8 (Staging Verification) runs an additional check specifically confirming both API versions are correctly served concurrently before this category of release is eligible for production promotion, a stricter gate than the standard release's stage 8 requires.

13.7 Hotfix releases — restating Section 1.7, now at the release-management level

Directly extending Section 1.7's "hotfixes follow the identical branch/PR/pipeline path, no shortcut" rule to release-numbering specifically: a hotfix ships as an out-of-cadence patch release (13.2's patch component incrementing outside the normal bi-weekly rhythm, 13.4), through the exact same automated pipeline and manual production-promotion decision as any scheduled release — the only difference from a scheduled release is timing, never process rigor, consistent with this document set's now-repeated insistence (Section 1.7, Section 12.4) that urgency is never a valid reason to skip a gate.

13.8 Release communication

Every release (scheduled or hotfix) is announced through the customer-facing changelog (13.5) and, for releases affecting Volume 0's named enterprise-trust-relevant surfaces (a security fix, a change to AI-optionality behavior, a change to data-residency posture) or coordinated multi-component releases (13.6), an additional, more prominent notification — directly extending Volume 4, Section 17.6's DR-event communication principle ("a DR event handled competently but silently... would undermine exactly the enterprise trust this document set has repeatedly prioritized") to routine release communication as the same underlying principle applied to a far more common, non-emergency scenario.

13.9 Post-release monitoring

Every release is followed by an elevated-attention monitoring window (Volume 4, Section 10.8's automatic deployment annotation makes this concretely actionable — the Platform Health dashboard, Volume 4 Section 10.3, is actively watched, not just passively available, for a defined period after each production promotion), with Volume 4, Section 7.4's error-budget-consumption tracking specifically reviewed against the just-shipped release's own contribution — directly closing the loop from Section 10.3's benchmark trend-tracking (did this release regress a benchmark) to real production behavior (is this release actually behaving as its benchmarks predicted once it's serving genuine customer traffic, not just staging load).



14. CI/CD Quality Gates
14.1 Purpose — the master gate manifest

Section 3 consolidated scattered code-review rules. Section 9 consolidated scattered testing tiers. This section performs the same consolidation for every automated quality gate specified across all five volumes, mapped onto the nine pipeline stages Volume 4, Section 5.2 already defined — the single manifest answering "what, exactly, does a change have to pass before it reaches a customer," with every gate's origin cited.

14.2 The complete gate manifest
Pipeline stage (Volume 4 §5.2)	Gate	Origin
1. Lint & Static Analysis	.NET Roslyn analyzers (strongly-typed IDs, dependency direction, structured logging)	Volume 1, §40.2
	ESLint import-boundary + TypeScript strict mode	Volume 2, §5.3, §4.7
	ruff/mypy for Python tooling	Volume 5, §5.5
	Commit-message format (Conventional Commits)	Volume 5, §4.7
	Helm lint, Dockerfile lint	Volume 4, §4.6, §2
2. Unit & Component Tests	Domain/Application tests (zero infra dependency)	Volume 1, §40.6
	Component + hook tests	Volume 2, §19.3–19.4
	Visual regression (Storybook baselines)	Volume 2, §9.7, §19.4
	AI prompt offline evaluation sets	Volume 3, §3.6, §19.3
3. Build & Image Creation	Bundle-size budget	Volume 2, §17.2
	Docker multi-stage build, layer-cache verification	Volume 4, §2.2, §2.9
4. Security & Vulnerability Scanning	Image CVE scanning	Volume 4, §2.5
	Dependency vulnerability + license scanning	Volume 5, §12.1, §12.6
	AI guardrail adversarial suite	Volume 3, §17.9
	Plugin-sandbox adversarial suite	Volume 2, §19.11
5. Integration Tests	Infrastructure/API integration tests (real test-container DB/Redis)	Volume 1, §40.6
	Frontend integration tier (MSW)	Volume 2, §19.6
	RLS policy performance verification	Volume 4, §13.4
6. End-to-End Tests	Full Nexus-testing-itself E2E suite	Volume 2, §19.8
	Accessibility (axe-core) integration checks	Volume 2, §18.9, §19.7
7. Deploy to Staging	Migration Job gate (schema before app rollout)	Volume 1, §37.5
	Helm chart deployment	Volume 4, §4.7
8. Staging Verification	E2E smoke subset	Volume 2, §19.9
	Load test against NFR-SCALE-1	Volume 1, §38.8
	Lighthouse CI performance gate	Volume 2, §17.9
	AI evaluation-set regression check	Volume 3, §19.3
	Performance benchmark absolute + relative regression check	Volume 5, §10.2, §10.4
	Dual-API-version serving check (major-version releases only)	Volume 5, §13.6
9. Promote to Production	Manual approval (timing decision only)	Volume 4, §5.5
14.3 Reading this table — every gate traces to exactly one origin

Every row in 14.2 cites the single section where that gate was originally, substantively specified — this table is deliberately a map, not a re-specification; a reader wanting the full reasoning behind any individual gate follows the citation back to its origin volume rather than expecting this section to re-derive it. This is the same "define once, reference everywhere" discipline this document set has applied since Volume 2 (Sections 9.4, 10.6, 12.5 there), now applied at this document set's largest scale — one table spanning all five volumes.

14.4 Gate additions — the process for extending this manifest

A new gate is added to this manifest through the same process as any other architectural pattern in this document set: specified substantively in the volume/section where it naturally belongs (a new AI capability's evaluation gate belongs in Volume 3; a new infrastructure check belongs in Volume 4), then added to this table in the same PR (directly extending Section 3.9's "checklist maintained in the same PR that establishes the pattern" discipline to this manifest specifically) — this table is never edited to add a gate whose substantive specification lives nowhere else, keeping the "map, not specification" property in 14.3 true by construction rather than by discipline alone.

14.5 Gate removal — a higher bar than addition

Removing a gate from this manifest requires an ADR (Section 2.3's checkbox) explicitly justifying why the risk that gate was originally added to catch is no longer present or is now covered by a different, still-active gate — consistent with Volume 1, Part VI, Section 40.8's general deviation-requires-ADR discipline, but called out here specifically because gate removal is a category of change this document set's authors consider meaningfully higher-risk than gate addition: an unnecessary gate costs pipeline time (a bounded, measurable cost, Volume 4 Section 2.9's build-budget discipline already tracks this); a wrongly-removed gate risks reintroducing a defect class this entire document set spent considerable effort preventing in the first place.

14.6 Gate performance — the pipeline's own budget, restated

Directly restating Volume 4, Section 5.7's "the pipeline monitors itself" and Section 6.8's PR/nightly cost split: this manifest's full gate suite (14.2) does not all run on every single commit-level push — Volume 4, Section 6.8 already specified that every-PR runs a representative, budget-conscious subset (targeting under 20 minutes total feedback latency) while the complete manifest, including every gate in 14.2 at full fidelity, runs nightly and before any tagged release (Section 13.4's cadence). This section's manifest is the complete, authoritative list of what exists; Volume 4, Section 6.8 remains authoritative for when each gate actually runs at what fidelity.

14.7 This manifest as the literal Definition of Done's foundation

Section 15 (Definition of Done) is built directly on this manifest — every gate in 14.2 that applies to a given category of change (a backend-only fix doesn't trigger Volume 2's frontend-specific gates; a documentation-only change doesn't trigger performance benchmarking) becomes a checklist item there. This section's manifest is the exhaustive, mechanically-enforced substrate; Section 15 is the human-facing checklist derived from it, scoped per change type.



15. Definition of Done
15.1 Purpose — the human-facing checklist derived from Section 14's manifest

Section 14 specified the mechanically-enforced gate manifest. This section is its human-facing counterpart — the checklist an engineer and reviewer use to judge "is this actually done," scoped by change type, since not every gate in Section 14.2 applies to every change. This is the final synthesis this entire document set has been building toward: the single checklist that, if genuinely satisfied, means a change embodies every standard Volumes 0 through 5 collectively established.

15.2 Universal criteria — every change, regardless of type
 Passes every gate in Section 14.2's manifest applicable to the files changed (mechanically verified by CI, not self-assessed)
 PR description complete per Section 2.3's template, including an honest "ADR required?" answer
 Code review checklist (Section 3) satisfied, with review from the owning team per Section 1.9's routing
 Commit messages follow Section 4's conventions
 No new console.log/string-interpolated logging introduced (Section 8.2–8.3)
 No new hardcoded secret, credential, or design-token value (Volume 4 §4.8; Volume 2 §2.9)
15.3 Additional criteria — backend changes (Volume 1 scope)
 New commands inherit AuditableCommandHandler or carry justified exemption (Volume 1, §27.2)
 New aggregates/entities follow strongly-typed-identifier and composition-vs-association discipline (Volume 1, §10.8, §35.4)
 New queries against scaling-sensitive tables include EXPLAIN ANALYZE output (Volume 1, §38.6)
 Domain/Application unit test coverage added for new business logic (Volume 5, §9.5)
15.4 Additional criteria — frontend changes (Volume 2 scope)
 Import boundaries respected (Volume 2, §5.3)
 New interactive elements meet WCAG 2.1 AA — keyboard operability and ARIA verified, not assumed (Volume 2, §18.4, §18.9)
 Bundle-size budget check passes or an explicit, reviewed exception is documented (Volume 2, §17.2)
 Module-parity test (Volume 2, §19.5) still passes if the change touches the schema-driven editor or canvas
15.5 Additional criteria — AI capability changes (Volume 3 scope)
 Evaluation-set update included and offline gate passes (Volume 3, §3.6, §19.3)
 Every AiPlanningResult field remains populated on every code path (Volume 3, §2.3)
 Guardrail changes are additive-only, never weakening a baseline check (Volume 3, §17.5–17.6)
 New capability output types remain structured (no new free-text field) (Volume 3, §3.4)
15.6 Additional criteria — infrastructure changes (Volume 4 scope)
 Helm chart change passes lint, template-diff review, and ephemeral-cluster deployment test (Volume 4, §4.6)
 NetworkPolicy or Kubernetes RBAC changes receive elevated review (Volume 4, §3.5, §4.9)
 Cost-impact estimate included for changes with material infrastructure-cost implications (Volume 4, §21.8)
 Backup/restore or DR runbook updated if the change affects a component Section 17/20 (Volume 4) specifies procedures for
15.7 Additional criteria — architectural deviations
 ADR drafted and approved by the architecture owner before merge, not after (Volume 1, §40.8)
 ADR cites the specific principle(s) it deviates from, by number (Volume 0, §20.9's citation discipline)
 Threat-modeling exercise completed if the change introduces a new trust boundary or sensitive-data category (Volume 5, §11.2)
15.8 What "Done" explicitly does not mean

Consistent with Section 3.10's and Section 7.8's equivalent caveats: satisfying this checklist means a change meets this document set's collective, established standards — it does not mean the change is necessarily the right change, that its underlying design is optimal, or that no further iteration will ever be needed. "Done" in this section's sense is a quality-and-consistency bar, not a claim of perfection or finality — a satisfied Definition of Done is the condition for merging with confidence that the change won't silently violate this project's accumulated architectural and process discipline, which is a meaningful, achievable, and honest claim, deliberately narrower than an unachievable claim of overall correctness or optimality.

15.9 Using this checklist in practice

This checklist is not read top-to-bottom as a manual gate on every PR — Section 15.2 applies universally; Sections 15.3–15.7 apply only to the categories of change the PR actually touches, determined directly from the same "which volume(s) / bounded context(s) does this touch" field Section 2.3's PR template already requires. A PR touching only Volume 4 infrastructure never needs to satisfy Section 15.5's AI-specific criteria — this checklist is scoped automatically by what the PR description already declares, not applied as one undifferentiated wall of items every change must wade through regardless of relevance.

15.10 This checklist's own maintenance

Directly extending Section 3.9's and Section 14.4's maintenance discipline to this final synthesis: any new standard established anywhere in a future amendment to Volumes 0–5 is added to the relevant subsection here (15.2–15.7) in the same change that establishes it — this is the last, and highest-leverage, instance of this document set's "documentation as code, updated where it's created" principle, since this checklist is the one artifact every single future change will be measured against.



16. Documentation Standards
16.1 Purpose — the standard for code-adjacent documentation, and for this document set itself

Every prior volume practiced "documentation as code" in its own scope — Volume 1, Part VI, Section 40.7's ActionSchema description requirement; Volume 4, Section 7.7's versioned runbooks and Section 10.2's dashboard-as-code. This section unifies that practice into one standard, and then does something no prior section has done: specifies how this six-volume document set itself — the specification these sixteen sections of Volume 5 close out — is maintained, reviewed, and kept honest going forward, directly fulfilling the governance mechanism Volume 0's RISK-13 flagged as required from the very first volume.

16.2 Code-adjacent documentation — the unified standard

Every artifact this document set has established a documentation requirement for follows one rule: documentation lives in version control, in the same repository, updated in the same PR as the code/configuration it describes — never in a separate wiki, a separate document store, or a separate review process. This applies uniformly to: ActionSchema/ParamSpec descriptions (Volume 1, §40.7), Grafana dashboards (Volume 4, §10.2), incident runbooks (Volume 4, §7.7), the code review checklist (Volume 5, §3), the CI/CD gate manifest (Volume 5, §14), and the Definition of Done (Volume 5, §15) itself.

16.3 ADRs — the format, consolidated

Architecture Decision Records have been required by name since Volume 0, Section 20.9, and invoked by nearly every subsequent volume (Volume 1, §40.8; Volume 4, §14.5; Volume 5, §11.2, §14.5, §15.7) without this document set ever specifying their actual format until now:

markdown
# ADR-NNNN: [Short, descriptive title]

## Status
[Proposed | Accepted | Superseded by ADR-XXXX]

## Context
[What situation motivates this decision — link the specific requirement,
 risk, or limitation that makes a decision necessary]

## Decision
[What was decided, stated plainly]

## Principle(s) deviated from or extended
[Cite by number — e.g., "Architecture Principle 20.3" or "UX Principle 1.4" —
 per Volume 0 §20.9's citation discipline]

## Consequences
[What becomes easier, what becomes harder, what risk this introduces or closes]

## Alternatives considered
[What else was evaluated, and why it wasn't chosen — mirroring the
 alternatives-considered format Volume 0 §21's technology decisions used]

Every ADR is stored in version control (16.2's rule applied to ADRs specifically), numbered sequentially, and linked from the PR that implements it (Section 2.3's checkbox) — an ADR is never retroactively written after the implementing PR has already merged, per Volume 1, Section 40.8's explicit "approved before merge, not merged first and documented later" requirement, restated here as the final, canonical statement of that rule.

16.4 Documentation review — the same rigor as code

A documentation-only PR (Section 4.3's docs commit type) passes through the identical review process (Section 2) as a code change — no separate, lighter-weight review path for documentation, because this document set's own internal consistency (a section citing another section correctly, a table accurately reflecting the architecture it describes) is exactly the kind of property that degrades under casual, unreviewed editing, the same reasoning Volume 1, Section 40.7 already applied to ActionSchema documentation specifically, now generalized to all documentation this platform maintains.

16.5 Maintaining this six-volume document set specifically

This is the section Volume 0's RISK-13 ("the scope of this documentation program itself... outpaces the team's capacity to keep it consistent and current") was written to eventually be answered by. The answer, concretely:

Ownership: each volume has a named owning role (a Volume 1 owner for backend architecture, a Volume 2 owner for frontend, and so on, directly mirroring Volume 4, Section 7.6's per-SLI ownership model applied to documentation authority) — a change to any volume requires that volume's owner's review, in addition to Section 2's standard PR review.
Amendment process: a correction or extension to any volume follows exactly the ADR process (16.3) if it changes an architectural decision, or a standard documentation PR (16.4) if it clarifies or corrects without changing the underlying decision — the distinction between these two cases is itself judged using this document set's own established vocabulary (does this change a "decision" or only its "documentation," a distinction Volume 1, Section 40.8 already implicitly relied on).
Cross-volume consistency checks: because this document set's central discipline is that every section traces to an upstream commitment (Volume 0's business objectives → Volume 1–4's architecture → Volume 5's process), a scheduled quarterly review specifically checks for drift — a place where a later volume's citation of an earlier volume's decision no longer accurately reflects that decision, because the earlier volume was amended without the citing section being updated. This is the concrete, scheduled practice preventing exactly the failure mode this project encountered once, live, during its own writing — the Volume 1 Unit-of-Work correction that Volume 2 was already citing before the fix, requiring Volume 2's citing sections to be revisited and confirmed still accurate once Volume 1 changed underneath them.
Version correlation with the product: this document set's own version (a straightforward semantic version, incrementing per Section 13.2's same scheme) is referenced in the product's release manifest (Section 13.3) — "what did this document set say the architecture was, as of the release that shipped" is always answerable precisely, the documentation equivalent of Section 13.3's component-correlation discipline applied reflexively to the documentation itself.
16.6 The traceability chain, restated one final time

Every section across all six volumes has, at some point, cited an upstream commitment — a business objective (Volume 0, Part I), a requirement (Volume 0, Part II), an architecture principle (Volume 1, Section 20), a UX principle (Volume 2, Section 1), an AI boundary (Volume 3, Section 1.4), an infrastructure decision (Volume 4), or a process standard (this volume). This chain is this document set's single most consistently-applied discipline, present throughout its 138 sections, and its continued integrity — verifiable by following any section's citations backward to Volume 0's original business case, and forward to this volume's process enforcement — is the actual, concrete measure of whether this documentation program succeeded at the goal Volume 0 set for it at the very beginning: not simply producing a large volume of text, but producing a genuinely internally-consistent, traceable specification where a reader can always ask "why does this exist" and receive a real answer, not an assertion.

16.7 Closing note — the six-volume project complete

This section closes Volume 5, and with it, the complete document set: Volume 0 (Product Blueprint) established the vision, market case, platform shape, and architectural foundation. Volume 1 (Backend Architecture) specified the domain model, engineering patterns, cross-cutting services, API, and data layer in full implementation detail — including a real, load-bearing correction made mid-project when a genuine crash-durability gap was found and fixed. Volume 2 (Frontend Architecture) specified the UX principles, application architecture, core product surfaces, platform integration, and quality discipline of the client experience, with an honest, deliberately-scoped account of what it does and does not attempt. Volume 3 (AI Architecture) specified the planning engine, knowledge infrastructure, seven capability engines, provider abstraction, and — per Volume 0's explicit mandate — the guardrails, security, and evaluation discipline that make Nexus's AI layer trustworthy rather than merely capable. Volume 4 (Infrastructure Architecture) specified how all of the above is built, deployed, observed, secured, and kept running reliably and affordably, closing every forward reference the earlier volumes had deliberately deferred to it. Volume 5 (this volume) specified the human and process discipline — source control, coding standards, testing, security practice, and release management — that governs how this entire system is actually built and maintained day to day, closing with this section's account of how the document set itself stays honest over time.

Six volumes, and — with this section — the complete engineering bible this project set out to build, incrementally, exactly as proposed at its outset: not all at once, but volume by volume, each one complete before the next began, each one tracing back to the vision the very first section established and forward to the process discipline this final section now closes the loop on.


